# AIASK 统一决策流水线落地评审稿

> 评审日期：2026-03-19
> 评审对象：
> - `统一决策流水线优化方案.md`
> - `统一决策流水线方案可行性审查报告.md`
> 评审方式：本地代码核对 + 关键测试复核

---

## 1. 结论

统一决策流水线方案 **可以做，而且值得做**，但前提是按“协议优先、摘要优先、灰度替换”的方式推进。

更准确地说：

- 架构方向正确，符合当前仓库从“能力集合”升级为“统一决策产品”的演进方向。
- 现有代码已经具备 `stock_context`、`quant_context`、`event_context`、`rule_gate` 的大部分素材和复用件。
- 首版最适合做成：
  - `get_unified_decision_summary`
  - `get_unified_decision_details`
  - BFF 统一 DTO
  - 助手页统一决策卡片
- 如果按“单个巨型工具一次性替代全部旧入口”推进，实施风险会明显升高。

建议评级：

- 架构可行性：高
- 首版落地可行性：中高
- 一次性全量替换可行性：中

---

## 2. 已核实的代码事实

### 2.1 可直接复用的基础能力

- BFF MCP 网关已经支持优先消费 `structuredContent`，并对文本 JSON 做兜底解析。
- 助手页已经具备“卡片摘要 + 原始明细展开”的交互雏形。
- `get_investment_analysis` 已能输出价格、估值、基本面、技术、动量、风险等结构化上下文。
- `smart_stock_diagnosis` 已经是“证据聚合器”而非单纯硬编码总分器。
- 量化工具层已具备：
  - 因子画像
  - 条件收益分布
  - 相似形态
  - 信号命中率
  - OOS 验证
- 文本聚合与事件抽取已存在，但仍以规则/关键词驱动为主。
- 合规、执行、风险、用户画像相关 manager 已存在，可为 `rule_gate` 提供复用件。

### 2.2 已核实的真实约束

- BFF 当前 MCP 连接池默认大小为 `3`，工具调用仍是同步请求-响应。
- 助手接口当前没有把认证后的 `userId` 继续传给决策链。
- 用户相关数据当前分散在多套来源中：
  - `app_users`
  - `users`
  - `user_profile_snapshots`
- 统一结构化输出契约还没有形成，BFF 目前依赖较多字段别名和兜底归一化。

### 2.3 我已做的本地验证

- 运行了与统一决策最相关的 4 条 MCP 测试，全部通过：
  - `should_i_buy` 基于 `analysis_context` 回退
  - `smart_stock_diagnosis` 使用上下文聚合
  - `factor_profile` 增强字段
  - `quant` 相似形态与命中率
- 运行了 3 条 manager 元数据一致性测试，全部通过：
  - `user_manager`
  - `risk_manager`
  - `event_manager`

这说明方案依赖的关键底座不是“纸面存在”，而是已有一定回归覆盖。

---

## 3. 可直接做

以下内容可以直接进入实施，不需要等待大规模重构完成：

### 3.1 统一对象协议

- 定义 `stock_context`
- 定义 `quant_context`
- 定义 `event_context`
- 定义 `gate_result`
- 定义 `UnifiedDecisionSummary`
- 定义 `UnifiedDecisionDetails`

这是当前最应该先做的事情，因为现有代码最大的问题不是能力不足，而是输出契约分散。

### 3.2 `stock_context` 首版构建

首版完全可以复用现有能力拼装：

- `get_investment_analysis` 作为骨架
- 市场快照/资金流/产业链/文本信号作为补充
- 给关键字段补齐：
  - `source_chain`
  - `updated_at`
  - `data_quality`
  - `fallback_reason`
  - `cached`

### 3.3 `quant_context` 首版构建

首版素材已基本齐全，可直接做编排层，不需要新造研究引擎：

- `get_factor_profile`
- `get_conditional_returns`
- `find_similar_patterns`
- `get_signal_hit_rate`
- `validate_factor_oos`

建议首版只做“量化证据集”，不要直接让量化层输出最终买卖结论。

### 3.4 BFF 统一接口

可以直接新增：

- `POST /assistant/unified-decision`
- `POST /assistant/unified-decision/details`

并新增统一卡片 DTO，替代当前大量字段兜底归一化逻辑。

### 3.5 前端统一决策入口

现有助手页已经是天然落点，首版不一定要再开新页面。

更稳妥的做法是：

- 先在 `apps/web/app/assistant/page.tsx` 增加“统一决策模式”
- 首屏只展示 summary
- 明细层点击后再拉 details

### 3.6 旧入口灰度并行

以下入口都适合先保留：

- `should_i_buy`
- `smart_stock_diagnosis`
- `decision_manager`

统一主链先作为新入口上线，用 diff 对照旧逻辑，而不是立刻替换。

---

## 4. 需要先补

以下内容不补，方案也能写代码，但很容易在接入阶段卡住。

### 4.1 助手链路的用户上下文透传

当前 `chat` 链路已经能拿到认证用户并注入上下文，但 `assistant` 链路没有完成同样的透传。

需要补齐：

- `assistant.controller` 从 `req.user` 读取 `userId`
- `assistant.service` 继续向统一决策入口透传 `userId`
- 明确无 `userId` 时的 fallback 策略

### 4.2 用户数据源统一

这是当前最容易被忽略、但后面最可能反复返工的一点。

现在用户信息分散在：

- `app_users`
- `users`
- `user_profile_snapshots`

如果不先定义统一口径，`user_gate` 很容易出现：

- 登录用户和 MCP 用户不是同一套人
- KYC、偏好、画像读到不同来源
- 同一个 `userId` 在不同层返回不同结果

建议至少先确定：

- 统一主标识
- 哪张表是权威用户表
- MCP 用户 manager 是映射 `app_users` 还是继续保留兼容层

### 4.3 结构化输出契约

统一决策相关工具不应继续采用“自由 dict + BFF 猜字段”的方式。

需要补齐：

- Pydantic 输出模型
- 稳定字段命名
- `structuredContent` 优先返回
- summary/details 明确分层

### 4.4 `stock_context` 的 fallback 设计

`get_investment_analysis` 当前更像“纯 DB 聚合器”，不是完整高可用上下文构建器。

如果直接把它当最终 builder，会遇到：

- DB 缺数据时整体失败
- 无法稳定向统一决策提供完整底座

建议把它作为骨架，而不是唯一来源。

### 4.5 `event_context` 分级

事件层首版必须区分：

- `warning`
- `downgrade`
- `hard_veto`

其中 `hard_veto` 首版只允许来源于强结构化风险事件，例如：

- ST/退市风险
- 重大处罚
- 财报爆雷
- 停牌或重大风险公告

### 4.6 性能与加载策略

统一决策不能首屏拉全量证据。

建议固定为：

- `summary`：结论、信心、核心理由、闸门标记、最小来源信息
- `details`：量化证据、事件证据、原始上下文、融合细节

否则当前同步 MCP 调用链和小连接池会成为真实瓶颈。

### 4.7 前端卡片结构升级

现有 `DecisionCard` 还不认识以下新字段：

- `gateFlags`
- `vetoReason`
- `positionSignal`
- `finalScore`
- `rawAiAction`
- `rawEventBias`

这部分不复杂，但必须补，否则统一决策落地后仍会被压缩成旧卡片表达力。

---

## 5. 不建议首版做

以下内容建议明确排除在首版范围之外：

### 5.1 单个超大工具全量返回

不建议首版就做一个巨型 `get_unified_decision`，把所有上下文、证据、事件、闸门一次性返回。

原因：

- 调用耗时会明显放大
- MCP 连接池占用会增加
- 首屏体验会退化为“重型报告页”

### 5.2 关键词文本直接作为硬 veto

当前文本抽取仍偏规则驱动，不能把关键词命中直接等价为强风险 veto。

首版应只把它作为：

- warning
- downgrade
- 解释性证据

### 5.3 一次性下线旧入口

不建议首版就替换掉：

- `should_i_buy`
- `smart_stock_diagnosis`
- `decision_manager`

更好的做法是保留一段并行对照期。

### 5.4 固定全局融合权重

不建议首版就把 `AI:Quant:Event` 写死为全局常量。

更稳妥的方式是：

- 首版先做规则优先级和简单风格差异
- 后续再用真实 diff 数据调整融合权重

### 5.5 首版做成超复杂前端报告页

首版前端重点应该是“好读、可解释、可展开”，而不是字段越多越好。

建议展示顺序：

- 结论层
- 理由/风险层
- 闸门层
- 证据展开层
- 原始层

---

## 6. 推荐实施顺序

### Phase 0：统一协议

- 定义 summary/details 契约
- 定义 context/gate/fusion 对象
- 定义 assistant 用户透传协议

### Phase 1：上下文构建

- 做 `stock_context`
- 做 `quant_context`
- 做 `event_context`

### Phase 2：规则与融合

- 做 `rule_gate`
- 做 `decision_fusion`
- 明确 veto/downgrade/position_cap 语义

### Phase 3：BFF 与前端接入

- 接 `summary`
- 接 `details`
- 升级决策卡片

### Phase 4：灰度替换旧入口

- 保留旧入口
- 加 diff 对照
- 观察一致性与延迟

### Phase 5：性能与回归

- contract tests
- diff audit
- latency baseline
- regression tests

---

## 7. 最终落地判断

如果按以下方式实施，本方案成功率较高：

- 先统一对象协议
- 先补用户透传链路
- 先做 summary/details 双层入口
- 先把事件层做成分级证据
- 先保留旧入口灰度并行

如果按以下方式实施，风险会显著提高：

- 先写一个单体大工具
- 先做全量首屏
- 先让关键词文本直接 veto
- 先下线旧入口

一句话总结：

**这个方案不是不能做，而是必须“轻首版、强契约、慢替换”。只要按这个节奏推进，它是当前项目非常合理的主线升级方向。**

