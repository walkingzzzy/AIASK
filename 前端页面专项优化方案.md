# 前端页面专项优化方案

> 审查对象：`apps/web`
> 审查时间：2026-03-14
> 审查方式：本地启动 `apps/web`、复用 3001 BFF、确认 `packages/akshare-mcp` 运行；使用 Playwright 访问页面、采集桌面/平板/移动端截图与基础指标。
> 说明：本报告尽量避开 [docs/plans/前端生产化改造清单.md](docs/plans/前端生产化改造清单.md) 已覆盖的架构级问题，重点补充“真实页面可见、会影响用户使用”的布局、交互与体验问题。
> 指标口径：LCP / CLS 为实验室口径；FID 为 Playwright 首次键盘输入近似值。

## 1. 总览

### 1.1 覆盖范围

- 已审查页面：38 个
- 用户侧页面：33 个
- 管理后台页面：5 个
- 已保存截图：`docs/screenshots/*-desktop.png`
- 响应式检查：Desktop `1440x960`、Tablet `1024x1366`、Mobile `390x844`

### 1.2 整体问题统计

| 维度 | 统计 |
| --- | --- |
| 缺少明确 `h1` 的页面 | 11 |
| 存在未命名表单控件的页面 | 24 |
| CLS ≥ 0.1 的页面 | 2 |
| LCP ≥ 2.5s 的页面 | 3 |
| 控制台出现真实错误的页面 | 6 |

### 1.3 严重性分级

#### 🔴 严重

1. `settings/audit-log` 在接口返回 `403` 时仍渲染“暂无审计日志”，把权限错误伪装成空数据，容易误导用户。
2. `stock` 首屏 CLS 达到 `0.224`，报价头部、KPI 区和行动卡在数据回来后整体下跳，影响首屏判断。
3. `strategy-market` CLS 达到 `0.115`，工厂大盘信息量过载且首屏没有真正的“选策略”入口，页面意图被运行遥测覆盖。
4. `assistant` 页面底部固定风险提示与全局移动底部导航叠加，移动端存在遮挡风险。

#### 🟠 高

1. `alerts`、`research`、`portfolio`、`backtest`、`assistant`、`settings` 等关键表单页仍大量依赖 placeholder 代替 label。
2. `home`、`research`、`alerts`、`notifications`、`settings` 首屏 LCP 偏慢，且空态卡片过多，前两屏可用信息不足。
3. `watchlist`、`paper-trading`、`notifications`、`chat`、`settings/security`、`settings/audit-log` 只有 `h2` 没有 `h1`，语义层级不一致。
4. `chat-config-modal` 仍缺少显式 `role="dialog"`、焦点管理和 ESC 关闭。

#### 🟡 中

1. 多个页面空态只有“暂无数据”，没有下一步动作。
2. 多个分析页筛选区在移动端仅靠换行堆叠，缺少字段分组与主次操作区分。
3. 管理后台五个页面全部缺少 `h1`，且概览页 LCP 偏慢。

#### 🟢 低

1. 登录页与行情页结构清晰，属于可继续打磨的状态反馈问题。
2. 多数页面已具备基础 focus-visible 和 loading/error 组件，但落地不均衡。

## 2. 通用问题汇总

### 2.1 共性问题 A：关键表单仍是“视觉上有标题，语义上没标签”

高频页面：`alerts`、`research`、`portfolio`、`backtest`、`assistant`、`settings`、`watchlist`

问题表现：

- 桌面端还能靠上下文猜字段含义，移动端换行后会丢失语义。
- 屏幕阅读器无法稳定读出字段名称。
- 表单报错时，错误与具体字段的绑定关系弱。

建议代码示例：

```tsx
function Field({
  id,
  label,
  error,
  children,
}: {
  id: string;
  label: string;
  error?: string | null;
  children: React.ReactNode;
}) {
  const errorId = `${id}-error`;
  return (
    <div className="flex flex-col gap-1">
      <label htmlFor={id} className="text-xs text-text-secondary">
        {label}
      </label>
      {children}
      {error ? (
        <p id={errorId} role="alert" className="text-xs text-danger">
          {error}
        </p>
      ) : null}
    </div>
  );
}
```

### 2.2 共性问题 B：权限错误被空态吞掉

高风险页面：`settings/audit-log`

问题表现：

- 接口已返回 `403`，页面却显示“暂无审计日志”。
- 用户会误判为“系统没有记录”而不是“当前无权限”。

建议代码示例：

```tsx
if (logsQ.error) {
  return (
    <ErrorState
      text={logsQ.error}
      hint="当前账号没有访问该数据的权限"
      onRetry={() => logsQ.refetch()}
    />
  );
}

if (!logs.length) {
  return <EmptyState text="暂无审计日志" />;
}
```

### 2.3 共性问题 C：首屏没有给异步区块预留稳定高度

高风险页面：`stock`、`strategy-market`、`home`

问题表现：

- 初始首屏先渲染一个瘦页面，数据回来后整块内容下跳。
- 在股票详情和策略超市这类需要“快速扫一眼做判断”的页面里，位移比纯慢更糟。

建议代码示例：

```tsx
<section className="min-h-[180px]">
  {query.isPending ? (
    <SkeletonCard className="h-[180px]" />
  ) : (
    <KpiGrid cols={4}>{/* real content */}</KpiGrid>
  )}
</section>
```

### 2.4 共性问题 D：响应式外壳存在 SSR/CSR 分叉

证据来源：Playwright 在管理后台捕获到 hydration mismatch；源码中 `AppShell` 直接根据 `useMobile()` 切换两套 DOM。

相关文件：

- `apps/web/components/app-shell.tsx`
- `apps/web/hooks/use-mobile.ts`

建议代码示例：

```tsx
export function useMobile(breakpoint = 768) {
  const [isMobile, setIsMobile] = useState<boolean | null>(null);

  useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${breakpoint}px)`);
    const sync = () => setIsMobile(mq.matches);
    sync();
    mq.addEventListener('change', sync);
    return () => mq.removeEventListener('change', sync);
  }, [breakpoint]);

  return isMobile;
}
```

更推荐直接让导航容器的显示与隐藏由 CSS 控制，而不是首屏渲染两套完全不同的结构。

### 2.5 共性问题 E：弹窗 a11y 不完整

高风险页面：`chat` 的 `ChatConfigModal`

问题表现：

- 弹窗没有 `role="dialog"` 和 `aria-modal`
- 没有 focus trap
- 没有 ESC 关闭
- 关闭后未恢复焦点

建议代码示例：

```tsx
<div
  role="dialog"
  aria-modal="true"
  aria-labelledby={titleId}
  onKeyDown={handleKeyDown}
  className="glass-strong rounded-2xl p-6"
>
  <h3 id={titleId}>LLM 配置</h3>
  ...
</div>
```

### 2.6 共性问题 F：空态缺少“下一步”

高频页面：`home`、`market`、`research`、`paper-trading`、`risk`、`alerts`、`notifications`、`macro`

建议代码示例：

```tsx
<EmptyState text="暂无告警规则">
  <div className="mt-3 flex gap-2">
    <button onClick={openCreateForm}>创建告警</button>
    <Link href="/market">先去查看行情</Link>
  </div>
</EmptyState>
```

## 3. 逐页分析

## 3.1 核心页面

### `/login` 登录页

截图：[docs/screenshots/login-desktop.png](docs/screenshots/login-desktop.png)

- 指标：LCP `96ms` / CLS `0.000` / FID `1ms`
- 发现问题：🟢 页面结构干净，但提交区只有按钮状态，没有稳定的表单级错误承载区；WebSocket 警告在控制台出现时用户界面没有降级提示。
- 优化建议：补一个固定高度的错误提示槽位；登录失败时在按钮下方展示原因，不要只依赖 toast。
- 优先级：`P2`

### `/` 首页 / 市场看板

截图：[docs/screenshots/home-desktop.png](docs/screenshots/home-desktop.png)

- 指标：LCP `2300ms` / CLS `0.059` / FID `0ms`
- 发现问题：🟠 首屏信息密度过高，但真正可执行的信息密度偏低。自选、持仓、告警、风控等多个卡片都在空态，用户先看到的是“暂无”，不是“我现在该做什么”。
- 发现问题：🟡 模块异步加载时没有统一占位高度，首页在空态、加载态、加载完成态之间会产生明显重排。
- 优化建议：把首页改成“今日任务台”排序，首屏只保留 4 块：市场状态、待处理任务、个人资产摘要、最近动作；次级模块折叠到第二屏。
- 优化建议：套用通用示例 C 和 F；首页卡片统一 `min-h`，空态加“去自选 / 去建组合 / 去设告警”的下一步按钮。
- 优先级：`P1`

### `/market` 市场行情

截图：[docs/screenshots/market-desktop.png](docs/screenshots/market-desktop.png)

- 指标：LCP `656ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 页面结构稳定，但默认进入后主区大量是“暂无 K 线 / 暂无行情数据”，用户第一次落地很像坏页面。
- 发现问题：🟡 查询、预设视图、标签页都在首屏，但主动作层级不明显，尤其移动端会变成一整条按钮墙。
- 优化建议：进入页面时优先用最近浏览代码、自选第一只或默认示例代码预填查询结果，不要让第一眼就是空画布。
- 优化建议：保留“查询”作为主按钮，预设视图改为次级下拉或 segmented control，减少按钮噪音。
- 优先级：`P2`

### `/research` 研究分析

截图：[docs/screenshots/research-desktop.png](docs/screenshots/research-desktop.png)

- 指标：LCP `2716ms` / CLS `0.008` / FID `1ms`
- 发现问题：🟠 过滤区有 3 个无语义表单控件；“时间范围 / 关键词 / 查询”在桌面还能理解，移动端换行后语义丢失。
- 发现问题：🟠 首屏默认同时给出“研报 / 公告 / 资讯与分析”，但结果为空时没有给出下一步推荐，用户很难判断是条件不对还是数据本来没有。
- 优化建议：套用通用示例 A；把筛选区改成两行栅格，第一行“标的 + 时间范围”，第二行“关键词 + 查询”。
- 优化建议：空态文案改成“近 30 天未命中，试试近 90 天 / 市场新闻 / 研报搜索”，给出直接操作。
- 优先级：`P1`

### `/backtest` 回测中心

截图：[docs/screenshots/backtest-desktop.png](docs/screenshots/backtest-desktop.png)

- 指标：LCP `1192ms` / CLS `0.070` / FID `1ms`
- 发现问题：🟠 桌面首屏就有 5 个未命名控件；高级参数展开后更像参数墙，不像任务流。
- 发现问题：🟡 回测参数和结果区之间缺少明显边界，初次用户不容易知道应该先填哪些字段。
- 优化建议：把回测页拆成 `基础参数 / 策略参数 / 成本假设` 三组 `fieldset`，每组只保留 3-4 个首屏字段。
- 优化建议：给“高级参数”增加模板预设，如“短线 / 中线 / 保守成本”，减少自由填写负担。
- 优先级：`P1`

### `/portfolio` 组合管理

截图：[docs/screenshots/portfolio-desktop.png](docs/screenshots/portfolio-desktop.png)

- 指标：LCP `1132ms` / CLS `0.008` / FID `1ms`
- 发现问题：🟠 页面把“输入 portfolioId 查详情”“创建组合”“添加持仓”“做优化/风控/压测”堆在同一层，路径不清晰。
- 发现问题：🟠 4 个未命名输入会让创建流程和持仓流程都变得难以理解，尤其是 `portfolioId` 对新用户并不友好。
- 优化建议：拆成三个主区域：`我的组合列表`、`创建组合`、`当前组合操作`。选中某个组合后再出现优化/风控/加仓动作。
- 优化建议：点击“创建成功”后自动选中新组合，不要要求用户再手输 `portfolioId`。
- 优先级：`P1`

### `/paper-trading` 模拟交易

截图：[docs/screenshots/paper-trading-desktop.png](docs/screenshots/paper-trading-desktop.png)

- 指标：LCP `1140ms` / CLS `0.001` / FID `1ms`
- 发现问题：🟠 页面没有 `h1`，语义主标题缺失。
- 发现问题：🟡 当账户为空时，首屏仍显示完整绩效区、持仓区、成交区和下单区，信息很多，但真正可执行动作不够聚焦。
- 发现问题：🟡 “撮合: 未知 / 净值: 未知”放在首屏顶部，会制造系统不稳定感，但缺少解释或恢复动作。
- 优化建议：空仓场景首屏只保留 `账户状态 + 下单卡片 + 示例持仓说明`，把绩效与持仓区折叠到第二屏。
- 优化建议：主标题改成 `h1`，状态徽标后增加“查看原因”或“手动刷新”的解释文案。
- 优先级：`P1`

### `/strategy-market` 策略超市

截图：[docs/screenshots/strategy-market-desktop.png](docs/screenshots/strategy-market-desktop.png)

- 指标：LCP `532ms` / CLS `0.115` / FID `0ms`
- 发现问题：🔴 工厂运行态大盘占满首屏，真实用户任务“挑策略 / 比策略 / 加组合”被埋到折叠外；页面命名叫“超市”，但首屏更像“工厂运维大盘”。
- 发现问题：🔴 CLS 超阈值，工厂摘要、运行历史、对比表、趋势图加载后页面整体下跳。
- 发现问题：🟡 搜索框无可访问名称。
- 优化建议：默认只显示一块精简的工厂状态条，详情折叠到“展开工厂运行态”；把策略卡片上移到首屏。
- 优化建议：套用通用示例 C；为工厂遥测区固定骨架高度，并把“暂无已发布策略”改成带 CTA 的空态，如“运行一次工厂 / 查看候选策略 / 去历史快照”。
- 优先级：`P0`

### `/alerts` 告警中心

截图：[docs/screenshots/alerts-desktop.png](docs/screenshots/alerts-desktop.png)

- 指标：LCP `2488ms` / CLS `0.051` / FID `1ms`
- 发现问题：🟠 创建表单存在 5 个未命名控件，是本轮最典型的“可看不可读”页面之一。
- 发现问题：🟠 条件操作符和状态筛选直接显示为 `>` / `>=` / `active` / `inactive` / `all`，对真实用户不够友好。
- 发现问题：🟡 空态只有“暂无告警数据”，没有引导用户先创建哪类告警。
- 优化建议：给 `指标 / 条件 / 阈值 / 状态` 全部加 label，并把原始英文状态改成人类语言，如“生效中 / 已停用 / 全部”。
- 优化建议：提供 3 个常见模板按钮：`价格突破`、`RSI 超卖`、`成交量放大`，把创建成本从“填空题”改成“选模板再微调”。
- 优先级：`P1`

### `/chat` AI 对话

截图：[docs/screenshots/chat-desktop.png](docs/screenshots/chat-desktop.png)

- 指标：LCP `1296ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟠 页面只有 `h2` 没有 `h1`；输入框也没有语义标签。
- 发现问题：🟠 `ChatConfigModal` 缺少对话框语义、焦点陷阱和 ESC 关闭。
- 发现问题：🟡 没配置 LLM 时，主输入区直接禁用，首屏对“为什么不能发消息”解释仍偏弱。
- 优化建议：把页头提升为 `h1`，并给输入区补一个隐藏 label，如“聊天输入框”。
- 优化建议：`ChatConfigModal` 套用通用示例 E；保存成功后把焦点返回“设置”按钮。
- 优先级：`P1`

## 3.2 扩展路由

### `/stock` 个股详情

截图：[docs/screenshots/stock-desktop.png](docs/screenshots/stock-desktop.png)

- 指标：LCP `2256ms` / CLS `0.224` / FID `0ms`
- 发现问题：🔴 个股头部、KPI 网格、行动卡都依赖异步数据，没有稳定骨架；CLS 已超出推荐阈值很多。
- 优化建议：给报价头部和 8 个 KPI 预留固定骨架，避免 `q` 为空时只剩一个瘦标题。
- 优先级：`P0`

### `/watchlist` 自选股

截图：[docs/screenshots/watchlist-desktop.png](docs/screenshots/watchlist-desktop.png)

- 指标：LCP `1336ms` / CLS `0.009` / FID `0ms`
- 发现问题：🟡 页面只有 `h2`；搜索输入框没有 label；删除操作仍使用原生 `confirm`。
- 优化建议：补 `h1`，搜索区套用通用示例 A，分组删除改为统一 `ConfirmDialog`。
- 优先级：`P2`

### `/fundamental` 基本面

截图：[docs/screenshots/fundamental-desktop.png](docs/screenshots/fundamental-desktop.png)

- 指标：LCP `1160ms` / CLS `0.080` / FID `0ms`
- 发现问题：🟡 股票代码和维度选择仍是 placeholder-only；切换内容时首屏有明显回流。
- 优化建议：套用示例 A 和 C。
- 优先级：`P2`

### `/technical` 技术分析

截图：[docs/screenshots/technical-desktop.png](docs/screenshots/technical-desktop.png)

- 指标：LCP `1128ms` / CLS `0.031` / FID `1ms`
- 发现问题：🟡 股票代码与指标选择无显式 label；初始空态没有推荐下一步。
- 优化建议：补 label，并把“点击按钮开始分析”改成带示例参数的引导。
- 优先级：`P2`

### `/fund-flow` 资金流向

截图：[docs/screenshots/fund-flow-desktop.png](docs/screenshots/fund-flow-desktop.png)

- 指标：LCP `1068ms` / CLS `0.050` / FID `0ms`
- 发现问题：🟡 输入区标签语义不足，首屏信息偏数据板，缺少“我该怎么用这页”的行动入口。
- 优化建议：补 label，并增加“加入自选 / 查看个股 / 去风险页”的快捷动作。
- 优先级：`P2`

### `/sentiment` 情绪分析

截图：[docs/screenshots/sentiment-desktop.png](docs/screenshots/sentiment-desktop.png)

- 指标：LCP `1268ms` / CLS `0.087` / FID `0ms`
- 发现问题：🟡 股票代码输入无 label；版面在桌面上正常，移动端容易退化为上下堆叠的长表单。
- 优化建议：套用示例 A，并把主结论卡固定到首屏顶部。
- 优先级：`P2`

### `/valuation` 估值分析

截图：[docs/screenshots/valuation-desktop.png](docs/screenshots/valuation-desktop.png)

- 指标：LCP `1228ms` / CLS `0.007` / FID `0ms`
- 发现问题：🟡 代码输入无显式 label；页面完成度不错，但缺少空态下一步。
- 优化建议：补 label，空态增加“查看同行估值 / 回到个股详情”。
- 优先级：`P2`

### `/risk` 风控中心

截图：[docs/screenshots/risk-desktop.png](docs/screenshots/risk-desktop.png)

- 指标：LCP `2312ms` / CLS `0.059` / FID `0ms`
- 发现问题：🟠 空态直接把原始 JSON 与长原因堆在 UI 上，可读性差，像调试面板。
- 发现问题：🟡 参数输入仍无显式 label。
- 优化建议：把“无组合 / 无模拟持仓”总结成一句业务语言，并提供“去创建组合 / 去模拟交易”按钮。
- 优先级：`P1`

### `/assistant` 智能助手

截图：[docs/screenshots/assistant-desktop.png](docs/screenshots/assistant-desktop.png)

- 指标：LCP `1980ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟠 页面有 5 个未命名输入，且 5 个主按钮权重相同，决策成本高。
- 发现问题：🔴 底部固定免责声明与全局移动底部导航叠加，移动端存在遮挡内容和按钮的风险。
- 优化建议：把“分析类型”收敛成主流程选择器，参数区根据选择动态展开。
- 优化建议：免责声明改成页面内 `sticky` 或给主内容区增加 `pb-[calc(56px+48px)]` 并避开移动底部导航。
- 优先级：`P0`

### `/notifications` 通知中心

截图：[docs/screenshots/notifications-desktop.png](docs/screenshots/notifications-desktop.png)

- 指标：LCP `2164ms` / CLS `0.001` / FID `2ms`
- 发现问题：🟡 页面只有 `h2`；空态比较单薄，没有引导用户去创建上游触发源。
- 优化建议：补 `h1`，空态增加“去告警中心 / 去策略超市 / 去模拟交易”入口。
- 优先级：`P2`

### `/settings` 设置中心

截图：[docs/screenshots/settings-desktop.png](docs/screenshots/settings-desktop.png)

- 指标：LCP `4264ms` / CLS `0.001` / FID `3ms`
- 发现问题：🟠 本轮最慢页面；账户资料和改密区的 5 个表单控件都没有语义 label。
- 发现问题：🟡 账户信息、安全日志、活跃会话都塞在一个页面里，切页虽用 Tab，但首屏仍然偏重。
- 优化建议：资料区和改密区都改成真正的 `<label>` + `htmlFor`；按 Tab 懒加载次级数据，降低首屏 LCP。
- 优先级：`P1`

### `/settings/security` 安全设置

截图：[docs/screenshots/settings-security-desktop.png](docs/screenshots/settings-security-desktop.png)

- 指标：LCP `1508ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 页面没有 `h1`；验证码输入区仅在 setup 后出现，缺少更明确的步骤感。
- 优化建议：补 `h1`，把 2FA 流程做成 1/2/3 步 stepper。
- 优先级：`P2`

### `/settings/audit-log` 审计日志

截图：[docs/screenshots/settings-audit-log-desktop.png](docs/screenshots/settings-audit-log-desktop.png)

- 指标：LCP `1424ms` / CLS `0.001` / FID `0ms`
- 发现问题：🔴 Playwright 捕获到真实 `403`，但页面最终显示“暂无审计日志”；这是错误信息被空态吞掉的典型问题。
- 发现问题：🟡 页面没有 `h1`。
- 优化建议：优先修复错误分支，接口报错时直接显示 `ErrorState`，并把该页权限说明写清楚。
- 优先级：`P0`

### `/user` 用户中心

截图：[docs/screenshots/user-desktop.png](docs/screenshots/user-desktop.png)

- 指标：LCP `1308ms` / CLS `0.080` / FID `1ms`
- 发现问题：🟡 首屏把个人信息、资产、订阅、组合、快捷入口一次性堆完，空态文案较长。
- 发现问题：🟡 仍有 1 个无语义选择控件。
- 优化建议：压缩空态文案长度，把“快捷入口”做成卡片按钮，不要和资产信息混在一起。
- 优先级：`P2`

### `/register` 注册页

截图：[docs/screenshots/register-desktop.png](docs/screenshots/register-desktop.png)

- 指标：LCP `1912ms` / CLS `0.000` / FID `0ms`
- 发现问题：🟢 页面本身清晰，但比登录页慢很多；建议补注册成功后的后续引导。
- 优化建议：注册成功后直接提示“去完善资料 / 去首页 / 去体验模拟交易”。
- 优先级：`P2`

### `/factor` 因子研究

截图：[docs/screenshots/factor-desktop.png](docs/screenshots/factor-desktop.png)

- 指标：LCP `1348ms` / CLS `0.001` / FID `1ms`
- 发现问题：🟡 表单控件较多，5 个控件无 label；更像内部工作台而不是产品页。
- 优化建议：套用示例 A，按“因子定义 / 样本池 / 导出”拆块。
- 优先级：`P2`

### `/factor-analysis` 因子分析

截图：[docs/screenshots/factor-analysis-desktop.png](docs/screenshots/factor-analysis-desktop.png)

- 指标：LCP `1152ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 2 个表单控件无 label；信息密度偏分析台风格。
- 优化建议：补 label，突出“当前所选样本 / 当前分析窗口”。
- 优先级：`P2`

### `/search` 智能搜索

截图：[docs/screenshots/search-desktop.png](docs/screenshots/search-desktop.png)

- 指标：LCP `1312ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 搜索输入无 label；首屏强调结果，缺少示例 query。
- 优化建议：输入框上方补“你可以搜什么”的示例 chips。
- 优先级：`P2`

### `/data` 数据中心

截图：[docs/screenshots/data-desktop.png](docs/screenshots/data-desktop.png)

- 指标：LCP `500ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 输入控件无 label；可用性问题主要集中在术语门槛而非视觉。
- 优化建议：给字段加 label，并在空态中展示示例标的。
- 优先级：`P2`

### `/strategy` 策略研究

截图：[docs/screenshots/strategy-desktop.png](docs/screenshots/strategy-desktop.png)

- 指标：LCP `1424ms` / CLS `0.001` / FID `1ms`
- 发现问题：🟡 5 个无 label 控件，首屏偏“内部操作台”。
- 优化建议：区分“策略试验”和“组合落地”两条路径，减少同时暴露的输入项。
- 优先级：`P2`

### `/options` 期权分析

截图：[docs/screenshots/options-desktop.png](docs/screenshots/options-desktop.png)

- 指标：LCP `1308ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 输入区无 label；空态缺少“无期权标的时怎么办”的引导。
- 优化建议：补 label，并给“没有期权链”加说明和示例标的。
- 优先级：`P2`

### `/macro` 宏观分析

截图：[docs/screenshots/macro-desktop.png](docs/screenshots/macro-desktop.png)

- 指标：LCP `2268ms` / CLS `0.047` / FID `0ms`
- 发现问题：🟡 图表区和数据表区都容易落入“暂无可用历史数据”，但页面没有下一步动作。
- 优化建议：空态时提供“切换指标 / 查看最近同步时间 / 去数据中心”。
- 优先级：`P2`

### `/tdx` TDX 联动

截图：[docs/screenshots/tdx-desktop.png](docs/screenshots/tdx-desktop.png)

- 指标：LCP `1256ms` / CLS `0.001` / FID `1ms`
- 发现问题：🟡 5 个无 label 控件，属于高复杂度工具页。
- 优化建议：补 label，并按“公式 / 预警 / 消息下发”拆成标签或步骤。
- 优先级：`P2`

## 3.3 管理后台

### `/admin` 管理后台概览

截图：[docs/screenshots/admin-desktop.png](docs/screenshots/admin-desktop.png)

- 指标：LCP `2740ms` / CLS `0.001` / FID `3ms`
- 发现问题：🟠 页面没有 `h1`；概览页首屏较慢。
- 发现问题：🟡 快速导航全部是普通链接块，缺少当前系统状态的优先警报。
- 优化建议：补 `h1`，把“异常项 / 待处理项”置顶，而不是先展示版本信息。
- 优先级：`P2`

### `/admin/tools` 管理 - MCP 工具

截图：[docs/screenshots/admin-tools-desktop.png](docs/screenshots/admin-tools-desktop.png)

- 指标：LCP `1292ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 页面没有 `h1`；信息结构合理，但更偏监控台。
- 优化建议：补 `h1`，把异常工具行固定在表格顶部。
- 优先级：`P2`

### `/admin/cache` 管理 - 缓存

截图：[docs/screenshots/admin-cache-desktop.png](docs/screenshots/admin-cache-desktop.png)

- 指标：LCP `1324ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 页面没有 `h1`；“清除全部缓存”是高风险动作，但缺少二次确认。
- 优化建议：清除动作接入 `ConfirmDialog`，并展示受影响前缀数量。
- 优先级：`P1`

### `/admin/dead-letters` 管理 - 死信队列

截图：[docs/screenshots/admin-dead-letters-desktop.png](docs/screenshots/admin-dead-letters-desktop.png)

- 指标：LCP `1304ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 页面没有 `h1`；卡片视图适合少量数据，不适合后续批量排查。
- 优化建议：当死信超过 20 条时自动切换成表格视图，并支持按工具/时间筛选。
- 优先级：`P2`

### `/admin/users` 管理 - 用户管理

截图：[docs/screenshots/admin-users-desktop.png](docs/screenshots/admin-users-desktop.png)

- 指标：LCP `1284ms` / CLS `0.001` / FID `0ms`
- 发现问题：🟡 页面没有 `h1`；用户表格缺少搜索和快速筛选。
- 优化建议：补 `h1`，加入按角色/状态筛选和用户名搜索。
- 优先级：`P2`

## 4. 通用实施建议

### 4.1 优先处理的跨页面问题

1. 修复 `settings/audit-log` 的 403 被空态吞掉问题。
2. 修复 `stock` 与 `strategy-market` 的首屏布局位移。
3. 补齐高频表单页的 label / error / describedby 体系。
4. 修复 `chat` 弹窗和 `assistant` 移动端底部叠层。
5. 统一缺失 `h1` 的页面头部语义。

### 4.2 建议新增的验收门槛

- 核心页面桌面端 CLS < `0.05`
- 核心页面桌面端 LCP < `2.5s`
- 所有关键输入项必须有 `label` 或 `aria-label`
- 权限错误页必须显示显式错误，不得伪装成空态
- 弹窗必须满足 `role="dialog" + focus trap + ESC`

## 5. Sprint 路线图

### Sprint 1：阻断误导与布局抖动

- 修复 `settings/audit-log` 权限错误显示
- 修复 `stock` 首屏骨架与 `strategy-market` 首屏布局稳定性
- 修复 `assistant` 底部固定条与移动底部导航冲突
- 修复 `AppShell` / `useMobile` 的 hydration 分叉

### Sprint 2：关键操作页 a11y 与流程梳理

- `alerts`、`research`、`portfolio`、`backtest`、`settings` 表单语义化
- `chat` 与 `chat-config-modal` 的可访问性补齐
- `paper-trading`、`watchlist` 替换原生确认框，统一敏感操作确认体验

### Sprint 3：任务导向重排

- 首页改成任务流优先
- `strategy-market` 把“选策略”抬到首屏
- `risk`、`notifications`、`macro`、`research` 补“下一步动作”空态

### Sprint 4：扩展页与管理后台统一收尾

- 补齐所有扩展分析页的 label、`h1` 和空态引导
- 管理后台页头统一、增加搜索筛选与危险操作确认
- 做一次 Lighthouse / Playwright 回归，复测 LCP、CLS、FID

## 6. 审查附件

- 用户侧审查摘要：`docs/frontend-user-audit.json`
- 管理后台审查摘要：`docs/frontend-admin-audit.json`
- 截图目录：`docs/screenshots/`

