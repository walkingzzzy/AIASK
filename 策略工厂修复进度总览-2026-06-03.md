# 策略工厂修复进度总览

日期：2026-06-03
版本：REPAIR-STATUS-V1
对照基线：
- `策略工厂实际架构现状梳理-2026-06-02.md`（ARCH-MAP-V2：三宗原罪 + 漏斗墙证据）
- `策略工厂倒置架构设计方案-2026-06-02.md`（§7 路线 P0→P4）
- `策略工厂Alpha源接线修复方案-2026-06-03.md`（ALPHA-WIRING：P-A→P-D）

> 一句话结论：**漏斗管道层已基本疏通、alpha 接线已打通并端到端验证（候选不再是
> fixed_defaults 填充物）；但"策略真正进 observe / 进 incubating"仍被最后一道
> Gate-3 盈利断头台（P3 严出）挡住——这是当前唯一的硬阻断，且属未动的架构 track。**

---

## 1. 已落地 commit 序列（master，可复核）

| commit | 阶段 | 内容 |
|---|---|---|
| `56d1818` | 基线 | 验证面板 sample_size 根因修复 + 架构审查/倒置设计文档 |
| `cae0180` | P0 | DSL import 路径修复 + Gate-2 top-k floor clamp |
| `a07d042` | P0 | Deduplicator 阈值 env 可配 |
| `68c7e58` | P0 | 文档：实证漏斗墙 + 撤回 LLM 降级假设 |
| `174ac83` | P1-D | regime 标签（前向测量分状态） |
| `87c1235` | P1-A | Layer-1 宽进 observe 准入 |
| `9bd858b` | P1 | 无契约 observe 策略的 proxy signal-evidence |
| `2504c17` | A-Wiring P-A | 把已挖出 gp_/rl_ 因子接回 spawner + 合成语义契约 |
| `6741ccc` | 文档 | P-A 方案文档 |
| `356ea4a` | A-Wiring P-B | factor_pool gateway 放宽 toggle（消费未 promote 的活跃因子） |
| `e796704` | 文档 | P-B 完成 + ③ 端到端验证结果 |
| `3e5932e` | A-Wiring P-D | factor_ic 经典分支阈值 env 可配 |

---

## 2. 对照设计方案 §7 路线（P0→P4）

| 路线阶段 | 计划改动 | 实际状态 | 验收达成度 |
|---|---|---|---|
| **P0 过渡验证** | F（top-k + DSL 编译） | ✅ 已做（`cae0180`/`a07d042`） | 代码层达成；"≥1 策略进 incubating"靠 P0 修补**未**达成（需后续层） |
| **P1 宽进** | A + D | ✅ 已做（`174ac83`/`87c1235`/`9bd858b`） | 准入链打通、regime 标签上线、proxy evidence 0→1；但 observe 策略数 >10 **未**达成 |
| **P2 画像化** | C + E（`ObservationLifecyclePolicy`） | ❌ 未做 | 淘汰仍非 skill_lcb 驱动 |
| **P3 严出** | B（PromotionGate 用前向数据、Gate-3 不前置） | 🟡 部分（`87c1235` 准入不再前置 Gate-3；`+ 跨regime晋升门`已加，默认OFF） | 晋升门已加"跨regime正skill"要求；PromotionGate 喂前向序列仍待接 |
| **P4 判决** | — | ❌ 未到 | 需 P2/P3 落地后观察 2-4 周 |

> 额外：方案 §6 点名的 **alpha 源原罪**，由 ALPHA-WIRING 方案（P-A/P-B/P-D）处理——
> 这是 §7 路线之外、但与 P0-P4 并行的一条线。

---

## 3. 对照 ALPHA-WIRING 方案（P-A→P-D）

| 阶段 | 状态 | 关键证据 |
|---|---|---|
| **P-A** 泛因子 IC 接入 + 合成契约 | ✅ 已做并单测 | OFF 零变化 / ON 产出带 prediction_contract+evidence_chain 的 gp_/rl_ 候选 |
| **P-B** factor_pool gateway 放宽 | ✅ 已做并单测 | 5 个 active+DSL 但 quality_status=null 的强因子可被消费 |
| **P-C** factor_ic_history 全量新鲜度 | ⏸ 运维任务（未做） | 非接线阻断项；详见数据维护手册 §3 |
| **P-D(d)** 经典分支阈值 env 可配 | ✅ 已做并单测 | 默认 0.03 零变化 / 可收紧 |
| P-D(a/b/c) OOS·去相关·衰减裁判 | ⏸ 依赖既有组件集成（未做） | — |

---

## 4. 端到端真实验证（决定性证据）

run `factory_run_1780465026_ca199f69`（2026-06-03，三 toggle 全开，--once，206.5s）：

| 指标 | 历史基线（ARCH-MAP §11 墙1） | 本次 |
|---|---|---|
| `source_counts` | `{quota_fill:1}` | `{fear_greed:2, **factor_ic:1, factor_pool:2**, quota_fill:6}` |
| `factor_pool` 原始数 | 0 | **5**（cap 到 2） |
| `quota_fill_mode` | `no_signal_fallback` | `signal_aligned` |
| spawned（全链） | ~1 | **37** |
| Gate 链 | 几乎全杀 | 37→23→9→4/4 |
| **gate_3_passed** | 0 | **0**（2 个进 G3 因 win_rate<0.40 被拒） |
| `strategy_signal_evidence` | 0 → 1（proxy） | **仍 1**（proxy） |

**读法**：
- ✅ **alpha 源墙已破**：factor_ic / factor_pool lane 首次出货，候选不再是 fixed_defaults 填充物，
  且发生在 LLM overload 走规则兜底时（接线不依赖 LLM）。
- ❌ **盈利断头台未破**：候选流到 Gate-3 仍被 win_rate<0.40 二元杀掉，
  真实策略没进 incubating，evidence 仍是 1 行 proxy。

---

## 5. 三宗原罪 → 当前状态

| 原罪（ARCH-MAP） | 修复手段 | 当前状态 |
|---|---|---|
| 1. 漏斗倒置（证明在前/观察在后） | P1-A 宽进 observe 准入 | 🟡 准入链打通，但 Gate-3 仍前置（P3 未做）→ 半通 |
| 2. regime-blind 平均断头台 | P1-D regime 标签 | ✅ 前向测量已分状态 |
| 3. 盈利断头台（资本级门装观察口） | P3 严出（B） | 🟡 部分——准入已不前置 Gate-3（P1-A）；跨regime晋升门已加（默认OFF） |
| 原罪溯源：alpha 源是随机 TA | ALPHA-WIRING P-A/P-B/P-D | ✅ 已接已挖出的 gp_/rl_ 因子，端到端验证出货 |

---

## 6. 进展更新 + 下一步

**P3 严出（change-B）进展**：本轮已实证发现并修两点：
1. ③ run 实测：Gate-3 失败的候选**已不直接 rejected**——其中 1 个进了 `observe_incubation`
   lane（P1-A 生效），1 个进 `diagnostic`。所以"Gate-3 前置断头台"在准入侧已被 P1-A 部分破除。
2. 真正缺的是**晋升门未做 regime 条件化**（change-B req #3）：`promotion_ready` 只看全局
   skill_lcb，没要求"跨主要 regime 都有正 skill"，而 forward_verifier 早已产出 `hit_rate_by_regime`
   却没人消费。本轮新增**跨 regime 晋升门**（默认 OFF toggle）补上这条接线。

**仍待做**：
- 完整 PromotionGate（喂前向序列做 deflated Sharpe/多重检验）——change-B 的统计武器搬迁部分。
- P2 `ObservationLifecyclePolicy`：核心日决策（promote/observe/halt on skill_lcb）其实已在
  `metrics_recorder._derive_decision`，但尚未抽成独立 policy + 接 regime 维度。

**建议下一步（按杠杆排序）**：
1. **完成 PromotionGate**（前向序列 + DSR/多重检验），并开 `PROMOTION_CROSS_REGIME` toggle 验证。
2. **P2 画像化**：把 `_derive_decision` 抽成 `ObservationLifecyclePolicy` + 接 regime。
3. **P-C 运维**：非交易时段同步 factor_ic_history（提升 trend 精度，非阻断）。
4. **P-D a/b/c**：OOS/鲁棒门 + 去相关 + 衰减裁判集成。

> 注意：所有新增能力 toggle 默认 OFF，默认行为与历史一致（零变化）。

### 本轮新增（P3 change-B 跨 regime 晋升门）

- `strategy-factory/domain/constants.py`：新增 `STRATEGY_FACTORY_PROMOTION_CROSS_REGIME_ENABLED`
  （默认 OFF）+ `..._MIN_N`（默认 20），并经 facade/`__init__` 导出。
- `akshare-mcp/.../strategy_lifecycle_shared/common.py`：新增 `evaluate_cross_regime_skill`
  （消费 `hit_rate_by_regime`，任一达标 regime 标签 skill_lcb≤0 即不通过；样本不足不阻断）。
- `akshare-mcp/.../strategy_lifecycle_shared/overview.py`：`promotion_ready` 在 toggle ON 时
  叠加跨 regime 门，未过则加 `cross_regime_skill_lcb_non_positive:<dim:label>` blocker。
- 测试：`akshare-mcp/tests/test_cross_regime_promotion_gate.py`（5 passed）；
  回归 `test_public_contracts.py` + `test_spawner_smoke.py`（15 passed）。

---

## 7. 诚实边界

- **已验证（实证）**：上述所有 ✅ 项均有单测 + DB 实测 + 一次真实 cycle 支撑；
  P3 change-B 跨 regime 门有 5 个单测 + 导出回归测试。
- **未承诺**：接线不制造 edge；这些 gp_/rl_ 因子向前到底行不行，要等观察农场积累前向数据
  （+ 完整 PromotionGate）后才能由数据回答（设计方案 §8 / P4）。
- **范围说明**：完整 PromotionGate（前向序列统计武器）与 P2 policy 抽取尚未做；
  本轮只补了 change-B 的 regime 条件化接线，默认 OFF。
