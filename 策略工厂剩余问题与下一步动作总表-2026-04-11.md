# 策略工厂剩余问题与下一步动作总表

文档日期：2026-04-11

关联文档：
- `策略工厂架构优化方案-2026-04-08.md`
- `候选生成引擎AI升级方案-2026-04-08.md`
- `候选生成引擎AI升级开发清单-2026-04-09.md`
- `策略工厂A-B质量提升开发清单-2026-04-09.md`

目标：把当前策略工厂仍未收口的问题，统一整理成一张“问题 -> 现状证据 -> 影响 -> 下一步动作”的总表，作为后续继续开发和验收的单一跟踪入口。

说明：
- 本表已按 2026-04-11 的代码核对做了写实修订。
- 其中“主链改造已完成/已落地”优先指代码、接口、页面和测试链路已存在。
- 其中 run_id、cohort 比例、默认 steady-state、live-ready 等内容仍属于运行证据口径，后续应以实时 `factory_status / factory_runs / factory_run_detail` 与数据库快照复核。

---

## 一、当前总览

截至 2026-04-11，从代码与接口形态看，策略工厂已经完成了大部分主链改造：

- 候选生成已经从单纯规则补位，升级到了 `hypothesis -> lowering -> candidate` 的双阶段链路
- feedback 已经从预算节流器升级为搜索路由器
- Gate-3 已经做了 strict 化、cohort 化、批内相关性修正
- run-level 的 raw/effective/strict/live 质量口径已经能稳定落到 `factory_status / factory_runs / factory_run_detail`
- `momentum`、`ma_cross`、`quality_factor` 都已经不再是纯 `raw D` 世界

但当前还不能判断为“默认配置下已经稳定量产高质量 live-ready 策略”。也就是说，当前更接近“能力主链已落地、运行结果仍待持续验收”。最主要的剩余问题共有 6 类。

当前文档记录的运行基线：

- 最近一次被 readiness 挡住的默认 run：`factory_run_1775874046_da5aad36`
- 最近一次默认真实成功 run：`factory_run_1775875548_3aa36019`
- 最新默认状态：`success`
- 最新默认 readiness 决策：`proceed`
- 最新默认 run-level raw 分布：`B=3, C=1`
- 最新受控 `quality_factor` 真实 run：`factory_run_1775875771_deab672c`
- 当前 cohort：`A=6, B=16, C=30, D=348`
- `raw_validation_b_rate=0.04`
- `raw_b_or_above_rate=0.055`
- `strict_incubation_ready_rate=0.195`
- `live_candidate_ready_rate=0.0075`

---

## 二、剩余问题总表

| 编号 | 问题 | 当前真实证据 | 影响 | 下一步动作 |
|---|---|---|---|---|
| 1 | 默认工厂 steady-state 仍需继续验证稳定性 | `factory_run_1775874046_da5aad36` 曾被 `governed_candidate_pool_blocked_ratio_high` 挡成 `skipped`；修复后 `factory_run_1775874896_1f798cd0` 已经 `readiness=proceed`，而最新默认真实 run `factory_run_1775875548_3aa36019` 已经是 `success`，并跑出 `submitted=4`、`raw={B:3,C:1}` | 默认工厂已经不再被同一条 readiness 规则持续挡死，也不再稳定停在 `partial`，但还需要继续证明这种成功不是单轮现象，而是 steady-state 可复现 | 保留 readiness 与 autonomy 状态修复，继续观察默认 run 的连续性；下一步把“最近 3 次默认 run 连续 success 且持续有 raw B/A 样本”作为新验收口径 |
| 2 | 总体 raw 质量分布仍然偏弱 | 当前 cohort `A=6, B=16, C=30, D=348`，`raw_validation_b_rate=0.04`，`raw_b_or_above_rate=0.055` | 说明高质量样本虽然已经出现，但总体仍然是 `D` 主导，离“稳定量产高质量策略”还有明显距离 | 继续把优化目标锁定在 `raw_validation_grade_distribution`、`raw_total_score`、`strict/live` 上，而不是只看 `submitted` 或 `gate_3_passed` |
| 3 | 三个重点 family 已明显改善，但默认路径下仍不够均衡 | `momentum` 已能稳定出现 `raw A/B`；`ma_cross` 已出现非零 `raw B`；最新默认真实 run `factory_run_1775875548_3aa36019` 里 `quality_factor` 已到 `raw C / 54`，受控真实 run `factory_run_1775875771_deab672c` 已打出 `quality_factor raw A/B` | 系统的 family specialization 已明显比之前健康，但 `quality_factor` 仍更依赖专项/受控路径，默认路径下的稳定性还不如 `momentum` 和 `ma_cross` | 继续让 `quality_factor` 的 `raw B/A` 从受控 run 推进到默认 run 可复现；后续 family 优化继续按桶位拆，不再只按 family 大类看 |
| 4 | strict 已明显改善，live 仍需从“非零”走向“稳定” | 全局仍是 `strict_incubation_ready_rate=0.195`、`live_candidate_ready_rate=0.0075`；但最新受控 `quality_factor` run `factory_run_1775875771_deab672c` 已做到 `strict_incubation_ready_rate=1.0`、`live_candidate_ready_rate=0.5` | 说明 live-ready 不再是“完全为零”，但距离默认路径稳定产出 live-ready 样本还有明显距离 | 继续针对 live gate 的 formal multiple-testing、PBO、White RC、SPA 做专项提分；优先追踪已经 raw `B/A` 且 strict 通过、但 live 仍失败的样本族谱 |
| 5 | 外部 LLM 稳定性仍是运行风险 | 受控真实 run 中仍观察到 provider 波动与 `HTTP503`；虽有 fallback，但不代表外部探索稳定 | 会影响 hypothesis 生成和 open DSL 探索的稳定性，使默认工厂在外部探索上波动较大 | 继续做 provider 稳定性治理：健康检查、限流、重试、超时、降级策略与 observability 一起推进；目标是把“可用”提升到“稳定可用” |
| 6 | 物理架构收口仍未彻底完成 | `factor_research.py` 已变成壳，但真实复杂度仍集中在 `packages/strategy-factory/src/strategy_factory/application/research/factor_research_builder.py`，当前仍接近 `2000` 行 | 后续继续改 feedback / allocation / quality / family panel 时，仍有较高回归风险 | 继续把 `factor_research_builder.py` 拆成 `allocation / routing / reward-debt / panel synthesis` 等子模块，降低核心研究主链的集中复杂度 |

---

## 三、按优先级排序的下一步动作

### P0：先恢复默认 steady-state

1. 拆默认 `readiness=blocked` 的阻断构成，确认 governed pool 和 evidence debt 在默认门控下的真实占比
2. 让默认 `factory_run_once` 能连续进入 `submit` 链路，而不是主要依赖受控 run 证明能力
3. 把“默认工厂是否持续产出”作为第一验收指标，而不是继续先做 family 小优化

### P1：继续把质量分布向右推

1. 保持 `momentum` 的 `raw A/B` 可复现
2. 继续扩大 `ma_cross` 的非零 `raw B` 样本，从边缘桶位推进到主桶位
3. 重点补 `quality_factor` 主桶，把它从“弱改善”推进到“稳定 raw B”

### P2：缩小 strict 与 live 的断层

1. 对已经 `strict_incubation_ready=true` 但 `live_candidate_ready=false` 的样本做专题审计
2. 识别最常见的 live 拦截因子：`PBO`、`White RC`、`SPA`、trade quality 等
3. 让下一轮优化不只追 `raw B`，还要追 `live_candidate_ready_rate`

### P3：继续做运行与架构收口

1. 把外部 LLM provider 稳定性做成单独治理对象
2. 继续拆 `factor_research_builder.py`
3. 把默认运行问题和复杂度问题都从“可解释”推进到“可消除”

---

## 四、后续验收建议

后续不建议再只看单轮最优 run，而应该以这 4 组指标作为统一验收口径：

1. 默认工厂连续 3 次真实 run 都不被 readiness 挡住，并能进入 `submit`
2. 最近 3 次默认真实 run 的 `raw_b_or_above_rate` 都显著高于当前 cohort 基线 `0.055`
3. `momentum / ma_cross / quality_factor` 至少有 2 条在默认路径稳定出现 `raw B`，且 `quality_factor` 不再主要依赖受控 run
4. `live_candidate_ready_rate` 不只非零，而且能在默认真实 run 中持续高于当前基线 `0.0075`

只有这 4 组指标一起成立，才更接近“策略工厂已经完成从能跑到能稳定产出高质量策略的改造”。
