{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AIASK desktop main window with minimal permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
