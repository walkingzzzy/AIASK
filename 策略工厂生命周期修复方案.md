# 策略工厂生命周期修复方案（24小时实测校正版）

## 2026-05-05 24小时运行实测结论

本次在真实数据库和源码环境下启动策略工厂连续运行（`run_factory_scheduler.py`，PID 52441），对生成策略进行了全面质量审查。**结论：策略工厂核心生命周期管线存在根本性断裂，修复优先级需要重新排序。**

### 实测环境

- 源码直接启动 `StrategyFactoryScheduler`，continuous 模式
- 数据库：127.0.0.1:5432/stockdb（TimescaleDB + pgvector）
- 策略库存：draft 62, submitted 62, rejected 123, archived 25
- LLM：DeepSeek API（200 OK 正常响应）
- readiness 硬门禁：已临时关闭以允许周期运行

### 实测发现的核心问题

| # | 问题 | 严重度 | 实测证据 |
|---|------|--------|---------|
| 1 | **去重完全失效** | P0 | 20份"动量突破·20日2%"完全相同，dedup_report 全部显示 `vector_checked=false, duplicate=false` |
| 2 | **所有策略卡在 warmup** | P0 | 62个 submitted 策略的 pipeline_stage 全部为 warmup，readiness_score=0，0 个 listed/incubating |
| 3 | **孵化指标是合成占位数据** | P0 | 所有策略 incubation_metrics 完全相同：3 signals, 1 order, 1 trade, nav=0.999965, sharpe=0 |
| 4 | **回测指标完全重复** | P1 | 4个不同ID的momentum策略 backtest 结果一模一样（total_return=0.19997, sharpe=2.174） |
| 5 | **策略参数为空** | P1 | 所有策略 params 中 lookback_days, threshold, signal_rule 均为 NULL |
| 6 | **策略类型严重单一** | P2 | 仅 momentum (20份) 和 topn_equity_portfolio (41份) 活跃，value_factor 56个全被拒 |
| 7 | **无风险/验证数据** | P2 | 所有策略的 risk 和 validation 周期指标为空 |
| 8 | **质量报告空洞** | P2 | passed=true 但 dedup 数据缺失，risk/validation 为空 |

---

## 根因分析

### 问题 1：去重完全失效

**现象**：
```json
// 所有4个新提交momentum策略的 dedup_report 完全相同：
{
  "duplicate": false,
  "vector_checked": false,
  "param_similarity": 0.0,
  "vector_similarity": 0.0,
  "reason": "未命中重复策略"
}
```

**根因**：pgvector 向量后端未初始化。`vector_checked=false` 说明向量相似度检查被完全跳过。策略生成器（`CandidateGenerationService`）使用 rule-first 模式产出模板策略，所有同类型策略的向量表示相同或缺失，导致 `param_similarity=0.0`。

**影响**：20份完全相同的 momentum 策略、41份完全相同的 topn_equity_portfolio 策略、39份完全相同的 multi_factor 策略。策略超市被垃圾策略淹没。

**修复方向**：
- 检查 pgvector 扩展是否已启用，向量表是否有数据
- `Deduplicator` 需要在向量后端不可用时回退到参数级别的哈希去重
- `CandidateGenerationService` 需要确保每次生成使用不同的随机种子/参数组合

---

### 问题 2：所有策略卡在 warmup — 生命周期完全停滞

**现象**：
```
pipeline_stage: warmup (全部62个submitted)
pipeline_status: collecting
readiness_score: 0
signal_quality.effective_n: NULL
signal_quality.skill_lcb: NULL
signal_quality.coverage_ratio: NULL
signal_quality.stability_gap: NULL
next_action: collect_more_samples
```

**根因**：`_derive_snapshot()` 调用 `get_signal_stats()` 获取信号质量数据，但纸交易系统仅产出了合成占位数据（见问题3），导致 `effective_n < 30`、`coverage_ratio < 0.35`，所有策略被判定为 warmup。

**影响**：0 个 listed，0 个 incubating。策略提交后永远无法晋升到实盘。elimination checker 没有任何 listed 策略可以扫描。整个生命周期管线在 submitted → incubating 这一步完全断裂。

**修复方向**：
- 需要确认纸交易/信号跟踪系统是否在正常运行
- `signal_tracker` 的 daily cycle 是否在产生真实信号
- 如果没有真实交易环境，至少需要历史回放产生模拟信号数据

---

### 问题 3：孵化指标是合成占位数据

**现象**（全部策略完全相同）：
```
metric_date: 2026-05-05
stage: warmup
total_signals: 3
total_orders: 1
total_trades: 1
nav: 0.999965
sharpe_ratio: 0
hit_rate_5d: 0
decision: observe
```

**根因**：纸交易系统（paper trading）在策略提交时创建了占位性质的初始指标，但没有后续的真实交易/信号产生。所有策略只有一条 metric 记录，且低于所有硬门禁阈值（MIN_OBSERVED_DAYS=20, MIN_TRADE_DAYS=10）。

**影响**：
- 问题 2 的直接原因
- `resolve_incubation_pipeline_stage()` 因 `effective_n=3 < 30` 将所有策略判为 warmup
- 观察期硬门禁（Gap 7）虽已实现但从未被触发，因为策略根本到不了 graduation_ready

**修复方向**：
- `signal_tracker` 需要对 submitted/incubating 策略按日产生真实信号
- 或者实现历史回放模式：用历史行情模拟纸交易，批量产生孵化指标
- 当前 1条 metric/策略 的状态需要增加到至少 20+ 条

---

### 问题 4：回测指标完全重复

**现象**（4个不同ID的momentum策略）：
```
total_return: 0.19997405806095747
sharpe_ratio: 2.174250568427576
max_drawdown: 0.04998006704376288
win_rate: 0.5625
trade_count: 32
```

**根因**：回测引擎按策略类型+名称缓存结果。因为所有 momentum 策略的名称都是"动量突破·20日2%"、参数都为空，回测返回同一份缓存数据。

**影响**：回测数据不能区分不同策略，Gate-2（完整回测筛选）失去意义。

**修复方向**：
- 回测缓存键必须包含策略 ID 或 params hash
- 生成策略时必须赋予不同的参数组合

---

### 问题 5：策略参数为空

**现象**：所有策略 `params` 中 `lookback_days`, `threshold`, `signal_rule`, `universe` 均为 NULL。

**根因**：`CandidateGenerationService` 的 rule-first 模式只生成策略名称和类型，没有填充具体参数。策略是"模板"而非"实例"。

**影响**：
- 去重无法基于参数相似度（`param_similarity=0.0`）
- 回测无法区分策略（参数相同）
- 策略没有实际可执行的交易规则

**修复方向**：
- `CandidateGenerationService` 需要为每个策略生成具体的参数组合
- 至少需要：lookback_days, threshold, signal_rule, universe_filter

---

### 问题 6-8（次要但系统性问题）

| 问题 | 根因 | 修复方向 |
|------|------|---------|
| 策略类型单一 | spawner 只对 momentum/topn 类型产出候选 | 修复 spawner 的事件源配置，激活全部 17 种策略类型 |
| value_factor 全被拒 | value_factor 的 Gate-1 筛选条件过严或回测数据不足 | 检查 Gate-1 参数，确认因子数据可用性 |
| 无风险/验证数据 | risk_report 和 validation_report 未在提交时生成 | lifecycle_coordinator 需在 formal_incubation 中触发 risk/validation 报告 |
| 质量报告空洞 | passed=true 但无实质内容 | quality_gate 报告需填充 dedup/risk/validation 字段 |

---

## 此前 Gap 修复状态再评估

基于实测结果，此前 9 个 Gap 的修复状态需要重新评估：

| Gap | 原评估 | 实测后评估 | 说明 |
|-----|--------|-----------|------|
| Gap 1 样本量分层晋级 | 基本完成 | **未验证** | 代码正确，但因无真实信号数据（effective_n=3），从未被触发 |
| Gap 2 skill trend | 基本完成 | **未验证** | 代码正确，但因 signal_quality 全 NULL，降级逻辑从未执行 |
| Gap 3 策略拥挤度 | 部分完成 | **未验证** | 代码已接入 snapshot，但 crowding_report 因无信号向量而无实际输出 |
| Gap 4 策略类型映射 | 基本完成 | **基本完成** | _REGIME_MAP 正确包含 15 种类型 |
| Gap 5 信号-交易归因 | 部分完成 | **未验证** | 归因模块存在但无真实交易数据可供归因 |
| Gap 6 压力测试 | 未实质完成 | **未验证** | 代码已接入 lifecycle_coordinator，但 backtest_fn 路径未覆盖 |
| Gap 7 观察期硬门禁 | 基本完成 | **未验证** | 代码正确，但因策略到不了 graduation_ready，降级逻辑从未触发 |
| Gap 8 策略说明书 | 部分完成 | **未验证** | fallback 模式可用，LLM 路径未走 |
| Gap 9 淘汰机制 | 部分完成 | **未触发** | 0 个 listed 策略，elimination checker 无扫描对象 |

**核心结论：Gap 1-9 的代码修复大部分写对了，但因为上游数据链路（纸交易信号、回测参数化、向量去重）断裂，所有修复都无法在实际流程中被验证。**

---

## 修订后的问题分级

### P0 — 阻断性（策略工厂无法产生任何有效输出）

| # | 问题 | 症状 |
|---|------|------|
| P0-1 | **纸交易信号缺失** | 所有策略只有 1 条合成 metric（3 signals/1 trade），effective_n 远低于 30 |
| P0-2 | **去重完全失效** | pgvector 未启用，20/41/39 份完全相同的策略分别被标记为 unique |
| P0-3 | **策略参数为空** | lookback_days/threshold/signal_rule 全 NULL，策略不可执行 |

### P1 — 严重（影响策略质量和多样性）

| # | 问题 | 症状 |
|---|------|------|
| P1-1 | **回测缓存键错误** | 同名策略共享回测结果，sharpe=2.174 完全重复 |
| P1-2 | **策略类型单一** | 仅 2/17 种策略类型在活跃生成 |
| P1-3 | **生命周期停滞** | 0 个 listed/incubating，P0-1 的直接后果 |

### P2 — 增强性（数据面和证据面不完整）

| # | 问题 |
|---|------|
| P2-1 | 无 risk/validation 报告 |
| P2-2 | 质量报告空洞（dedup 数据缺失） |
| P2-3 | value_factor 100% 被拒 |
| P2-4 | 压力测试 backtest_fn 路径未验证 |

---

## 修订后的实施顺序

| 阶段 | 修复项 | 核心目标 |
|------|--------|---------|
| **第一轮** | P0-1 纸交易信号 | 让 submitted 策略产生 20+ 条真实/回放 incubation metric，使 effective_n 达标 |
| **第一轮** | P0-3 策略参数化 | 让 CandidateGenerationService 为每个策略填充具体参数 |
| **第二轮** | P0-2 去重修复 | 启用 pgvector 或实现参数哈希回退去重 |
| **第二轮** | P1-1 回测缓存修复 | 回测键包含策略 ID 或 params hash |
| **第三轮** | P1-2 策略多样性 | 修复 spawner 事件源，激活全部策略类型 |
| **第四轮** | P1-3 生命周期验证 | 在 P0-1 修复后，验证 Gap 1/2/5/6/7/9 的代码修复是否真正生效 |
| **第五轮** | P2-1/P2-2/P2-3 证据面补全 | risk/validation 报告生成，质量报告填充，value_factor 调参 |

---

## 2026-05-05 本轮代码修复落地状态

### 已完成修复

| 阻断点 | 修复状态 | 代码证据 |
|--------|----------|----------|
| `topn_equity_portfolio` 被 Gate-0 拦截 | **已完成** | `quality_gates_parts/evaluation.py` 已加入 `_VALID_STRATEGY_TYPES`；`domain/constants.py` 已加入 `CATEGORY_MINIMUMS` 和 `BACKTEST_TYPE_THRESHOLDS` |
| 孵化历史回放指标积累过慢 | **已完成第一阶段加速** | `_resolve_replay_dates()` 不再只取第一个可用标的，改为合并最多 8 个候选标的的 K 线交易日；`incubation_pipeline_run` 会对少于 30 条 metrics 的 submitted/incubating 策略自动回放，默认 `replay_max_dates=80`，下限 30，上限 250 |
| Gate-1 样本过少、阈值偏硬 | **已完成默认修正** | `GATE1_PASS_RATIO` 默认从 0.45 提到 0.65；`GATE1_SHARPE_MIN` 默认从 0.15 降到 0.05；`GATE1_REPRESENTATIVE_COUNT` 默认从 2 提到 4，非 bulk 路径强制不少于 4 个代表标的 |
| 压力测试不是生命周期门禁 | **已完成** | `formal_incubation` 在 `status_transition("incubating")`、开户、孵化管线、向量画像之前先跑压力测试；`review` 留在 `submitted`，`reject` 进入 `rejected`，只有 `pass` 才进入正式孵化副作用 |
| 压力测试代理指标伪装为历史回测 | **已完成** | `run_stress_test_simple()` 有 `backtest_fn` 时逐个历史场景调用真实回测；无 `backtest_fn` 时标记 `evidence_mode="backtest_metrics_proxy"`、`diagnostic_only=True`，且不能返回 `pass` |
| 拥挤度 `review` 未阻断自动评审 | **已完成** | `recommendation in {"review", "reject"}` 均会让 `crowding_blocked=True`；`graduation_ready + review` 降级为 `candidate`，`next_action="crowding_review_required"`，`auto_review=False` |

### 新增测试覆盖

| 测试文件 | 覆盖点 |
|----------|--------|
| `packages/strategy-factory/tests/test_full_market_topn.py` | `topn_equity_portfolio` 不再触发 `invalid_strategy_type`，并已进入分类配额与回测阈值表 |
| `packages/akshare-mcp/tests/test_incubation_history_replay.py` | 历史回放会合并多个目标标的的市场交易日，不再只依赖单一标的 |
| `packages/strategy-factory/tests/test_backtest_filter_required_samples.py` | Gate-1 混合验证路径实际使用 4 个代表标的 |
| `packages/strategy-factory/tests/test_stress_test.py` | 真实 `backtest_fn` 历史场景路径和 proxy fallback 路径分离，proxy 不能通过压力门禁 |
| `packages/strategy-factory/tests/test_lifecycle_coordinator.py` | 压力测试 `pass/reject/review/proxy` 对正式孵化副作用、状态和 task result 的影响 |
| `packages/akshare-mcp/tests/test_incubation_pipeline_observation_gate.py` | 真实 `_derive_snapshot()` 覆盖观察期降级、拥挤度 `review` 阻断、拥挤度 `approve` 放行 |

### 当前验证结果

```bash
PYTHONPATH=packages/akshare-mcp/src:packages/strategy-factory/src pytest -q \
  packages/strategy-factory/tests/test_full_market_topn.py \
  packages/strategy-factory/tests/test_backtest_filter_required_samples.py \
  packages/akshare-mcp/tests/test_incubation_history_replay.py \
  packages/strategy-factory/tests/test_lifecycle_coordinator.py \
  packages/strategy-factory/tests/test_stress_test.py \
  packages/akshare-mcp/tests/test_incubation_pipeline_observation_gate.py \
  packages/akshare-mcp/tests/test_strategy_lifecycle_shared_runtime.py
```

结果：`48 passed`。

同时已对修改文件执行 `python -m py_compile`，通过。

### 仍未完成的运行层问题

本轮修复解决的是“门禁逻辑、类型注册、历史回放加速、压力门禁闭环、拥挤度 review 阻断”。以下问题仍不能算已完成：

| 问题 | 当前状态 | 后续动作 |
|------|----------|----------|
| 去重完全失效 | **未修复** | 需要检查 pgvector 初始化，并为向量不可用场景增加参数哈希/结构哈希兜底去重 |
| 策略参数为空 | **未修复** | 需要在 CandidateGenerationService / StrategySpawner 中为每类策略生成可执行参数组合 |
| 回测缓存结果重复 | **未修复** | 需要让回测缓存键包含 strategy id、params hash 或 contract hash |
| 策略类型产出单一 | **未修复** | 需要修复 spawner 事件源和 fill budget，使 17 类策略都有实际生成入口 |
| risk/validation 报告空洞 | **未修复** | 需要在提交/正式孵化路径补齐风险与验证报告证据 |
| 24 小时真实运行闭环 | **待复测** | 当前单元测试通过；仍需重新跑真实工厂周期，确认 submitted 策略能积累足够 metrics 并推进到 incubating/listed |

### 本轮结论

此前四个 review findings 已经通过代码修复和真实单元路径验证；新诊断中的前三个优先修复项（`topn_equity_portfolio` 注册、孵化指标积累加速、Gate-1 放宽与代表样本扩大）也已落地。但这还不是“策略工厂全链路恢复”：去重、策略参数化、回测缓存键、多类型生成和真实 24 小时运行复测仍是下一轮阻断项。

---

## 第一轮：纸交易信号 + 策略参数化

### P0-1：纸交易信号缺失

**当前状态**：`signal_tracker` 的 daily cycle 在运行（日志可见），但只产出 1 条占位 metric/策略。

**需要确认**：
1. `signal_tracker` 是否只对新创建的策略产生信号？是否存在历史数据回填机制？
2. 纸交易账户（`strategy_incubation_accounts`）是否正确创建？
3. `strategy_incubation_metrics` 的 daily 记录是否应该由 `signal_tracker` 还是 `incubation_pipeline` 产生？

**建议修复**：
1. 检查 `signal_tracker` 的 daily cycle 逻辑，确认它在为所有 submitted/incubating 策略产生每日信号
2. 如果没有历史数据回填，需要实现：对已提交策略，用历史行情回放产生至少 20 个交易日的 incubation metric
3. 或者降低观察期硬门禁阈值用于测试（临时方案，不推荐）

### P0-3：策略参数为空

**受影响文件**：
- `packages/akshare-mcp/src/akshare_mcp/services/strategy_autonomy_components/` — CandidateGenerationService
- 策略生成器（rule-first / LLM 模式）

**建议修复**：
1. `CandidateGenerationService` 在生成策略时填充具体参数：
   - momentum: lookback_days (10/20/60), threshold_pct (1%/2%/3%)
   - multi_factor: factor_weights (具体权重分配)
   - volatility_breakout: lookback_days, breakout_multiple
   - 等等
2. 参数从预设范围中随机采样，确保多样性
3. LLM 模式应提示模型输出结构化参数

---

## 判断

此前 Gap 1-9 的代码修复在静态测试中通过（20 passed），但在真实运行中因为上游数据链路断裂而无法验证。核心问题不是门禁阈值或判定逻辑，而是：

1. **纸交易不产生信号** → 所有策略没有信号质量数据 → 永远卡在 warmup
2. **去重不看向量** → 生成100份相同策略也不会被拦截
3. **策略没有参数** → 回测缓存碰撞，去重无法比较参数

这三个问题修好后，之前做的 Gap 1-9 代码修复才能真正进入验证阶段。当前应优先修 P0-1 和 P0-3，让至少一个策略走通 submitted → incubating → listed 的完整路径。
