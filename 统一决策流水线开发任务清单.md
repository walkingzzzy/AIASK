# AIASK 统一决策流水线开发任务清单

> 基于：
> - `统一决策流水线优化方案.md`
> - `统一决策流水线方案可行性审查报告.md`
> - `统一决策流水线落地评审稿.md`
> 目的：将方案收束为可执行的开发任务、文件改造点、PR 切分建议与验收标准

---

## 1. 总体策略

本项目最适合采用以下推进方式：

1. 先定协议
2. 再做 MCP 侧上下文构建与融合
3. 再接 BFF 接口
4. 再接前端 summary/details
5. 最后做灰度替换与 diff 验证

不建议反过来从页面或单一大工具先做。

---

## 2. 任务分解总览

### WP0：协议与口径统一

目标：

- 定义统一决策对象
- 明确用户透传链路
- 明确首屏 summary 与展开 details 边界

交付物：

- 新文档：`docs/plans/统一决策对象协议.md`
- 统一字段表
- summary/details DTO
- assistant 用户透传规则

依赖：

- 无

建议优先级：

- P0

### WP1：MCP 上下文构建层

目标：

- 产出 `stock_context`
- 产出 `quant_context`
- 产出 `event_context`

交付物：

- 新服务文件
- 新统一工具入口
- 基础 contract tests

依赖：

- WP0

建议优先级：

- P0

### WP2：规则闸门与融合层

目标：

- 统一 `rule_gate`
- 统一 `decision_fusion`
- 形成 summary/details 结果

交付物：

- 新 gate/fusion 服务
- 统一 summary/details MCP 工具

依赖：

- WP1

建议优先级：

- P0

### WP3：BFF 接入层

目标：

- 新增统一决策 API
- 将 `userId` 透传到统一决策入口
- 统一 DTO

交付物：

- 新 service/controller DTO
- assistant 兼容层

依赖：

- WP0
- WP2

建议优先级：

- P0

### WP4：前端 summary/details 展示

目标：

- 在助手页接入统一决策模式
- 卡片支持闸门、veto、仓位建议

交付物：

- 页面入口
- 卡片增强
- details 懒加载

依赖：

- WP3

建议优先级：

- P1

### WP5：旧入口灰度与差异对比

目标：

- 保留旧入口并做 diff
- 观察一致性与回退风险

交付物：

- `legacy_mode`
- 新旧差异记录
- 回归观察窗口

依赖：

- WP4

建议优先级：

- P1

### WP6：性能、回归、基线

目标：

- 建立 contract、latency、diff、regression 四类验证

交付物：

- 测试
- 基线结果
- 上线前检查项

依赖：

- WP2
- WP3
- WP4

建议优先级：

- P1

---

## 3. 具体文件任务

## 3.1 WP0：协议与口径统一

### 新增文件

- `docs/plans/统一决策对象协议.md`

### 文档中必须明确的对象

- `StockContext`
- `QuantContext`
- `EventContext`
- `GateResult`
- `UnifiedDecisionSummary`
- `UnifiedDecisionDetails`
- `UnifiedDecisionRequest`

### 文档中必须明确的字段

- `source_chain`
- `updated_at`
- `data_quality`
- `fallback_reason`
- `cached`
- `user_id`
- `risk_level`
- `kyc_level`
- `profile_summary`

### 必须明确的口径决策

- `userId` 来源于 BFF 认证用户，而不是前端自由传入
- `summary` 首屏字段边界
- `details` 按需展开字段边界
- `event_warning` 与 `hard_veto` 的区分标准
- 无用户上下文时的 fallback 规则

### 验收标准

- BFF、MCP、前端三方都能按同一字段表开发
- 不再依赖“看到数据再猜字段”

---

## 3.2 WP1：MCP 上下文构建层

### 建议新增文件

- `packages/akshare-mcp/src/akshare_mcp/services/decision_context_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_quant_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_event_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_contracts.py`

### 建议修改文件

- `packages/akshare-mcp/src/akshare_mcp/tools/decision.py`
- `packages/akshare-mcp/src/akshare_mcp/server.py`
- `packages/akshare-mcp/src/akshare_mcp/services/__init__.py`

### `stock_context` 复用来源

- `packages/akshare-mcp/src/akshare_mcp/tools/investment_analysis.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/industry_chain_manager.py`
- 现有市场/资金流/研究/公告工具

### `quant_context` 复用来源

- `packages/akshare-mcp/src/akshare_mcp/tools/factor_profile.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/quant.py`

### `event_context` 复用来源

- `packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py`
- `packages/akshare-mcp/src/akshare_mcp/services/event_extraction.py`
- `packages/akshare-mcp/src/akshare_mcp/services/llm_alpha.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/event_manager.py`

### 建议新增 MCP 工具

- `build_stock_context`
- `build_quant_context`
- `build_event_context`

### 关键实现要求

- `build_stock_context` 不直接照搬 `get_investment_analysis`，而是把它作为骨架并补 fallback
- `build_quant_context` 返回证据，不返回最终动作
- `build_event_context` 首版输出分级事件，不直接 hard veto
- 全部 builder 都带统一元数据字段

### 验收标准

- 任意有效股票代码都能稳定返回三个 context
- 单一来源失败时仍能返回降级结果

---

## 3.3 WP2：规则闸门与融合层

### 建议新增文件

- `packages/akshare-mcp/src/akshare_mcp/services/decision_rule_gate.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_fusion.py`

### 建议修改文件

- `packages/akshare-mcp/src/akshare_mcp/tools/decision.py`

### 建议新增 MCP 工具

- `run_decision_gate`
- `fuse_decision_payload`
- `get_unified_decision_summary`
- `get_unified_decision_details`
- `get_unified_decision`

### Gate 复用来源

- `packages/akshare-mcp/src/akshare_mcp/tools/managers/compliance_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/risk_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/user_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py`

### 首版必须做的规则

- `user_gate`
  - 可选增强
  - 无用户信息时不阻断主链
- `market_gate`
  - 停牌/ST/极端波动/流动性
- `event_gate`
  - 仅结构化强事件可触发 hard veto

### `summary` 最小字段建议

- `action`
- `confidence`
- `finalScore`
- `summary`
- `reasons`
- `risks`
- `gateFlags`
- `vetoReason`
- `positionSignal`
- `dataProvenance`

### `details` 建议字段

- `stock_context`
- `quant_context`
- `event_context`
- `gate_result`
- `fusion_reasoning`
- `raw_ai_output`

### 验收标准

- summary 可在首屏直接使用
- details 可独立按需拉取
- 出现 downgrade/veto/position_cap 时能清楚解释原因

---

## 3.4 WP3：BFF 接入层

### 建议新增文件

- `apps/bff/src/assistant/assistant-unified.service.ts`
- `apps/bff/src/assistant/dto/unified-decision.dto.ts`

### 建议修改文件

- `apps/bff/src/assistant/assistant.controller.ts`
- `apps/bff/src/assistant/assistant.service.ts`
- `apps/bff/src/assistant/assistant.module.ts`
- `apps/bff/src/mcp-gateway/mcp-gateway.service.ts`

### 新增接口

- `POST /assistant/unified-decision`
- `POST /assistant/unified-decision/details`

### 必须改的链路

- 从 `req.user` 读取认证用户
- 生成 `userId`
- 向 MCP 统一决策入口透传 `userId`
- 对无 `req.user` 的情况走默认 fallback

### assistant 侧改造建议

- 保留旧接口：
  - `/assistant/diagnosis`
  - `/assistant/should-buy`
  - `/assistant/should-sell`
- 新增 unified 接口，不立刻替换旧接口
- `AssistantService` 不再长期扩张字段兜底规则，统一交给 new DTO

### 验收标准

- assistant 统一接口能拿到用户身份
- assistant 新旧接口可并行工作

---

## 3.5 WP4：前端 summary/details 展示

### 建议修改文件

- `apps/web/app/assistant/page.tsx`
- `apps/web/components/decision-card.tsx`
- `apps/web/lib/api.ts`
- `apps/web/hooks/use-api-mutation.ts`

### 可选新增文件

- `apps/web/components/unified-decision-panel.tsx`
- `apps/web/components/unified-decision-details.tsx`

### 页面策略

- 先复用 `apps/web/app/assistant/page.tsx`
- 不必首版就新开 `apps/web/app/decision/page.tsx`

### 首版交互

- 点击“统一决策”按钮后请求 summary
- summary 卡片展示：
  - 结论
  - 核心理由
  - 风险
  - 闸门标记
  - 仓位建议
- 展开详情后再请求 details

### `DecisionCard` 要补的字段

- `finalScore`
- `gateFlags`
- `vetoReason`
- `positionSignal`
- `rawAiAction`
- `rawEventBias`

### 验收标准

- 首屏只展示摘要
- details 懒加载成功
- 用户不需要跳多个页面拼结论

---

## 3.6 WP5：旧入口灰度与差异对比

### 建议修改文件

- `apps/bff/src/assistant/assistant.service.ts`
- `packages/akshare-mcp/src/akshare_mcp/tools/decision.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/decision_manager.py`

### 灰度策略

- 保留旧入口不删
- 新入口单独开放
- 加 `legacy_mode`
- 同一股票可同时返回：
  - unified 结果
  - legacy 结果
  - diff 解释

### 首批 diff 对比对象

- `should_i_buy`
- `smart_stock_diagnosis`
- `decision_manager(action='analyze')`

### 验收标准

- 能解释新旧结果差异
- 出现显著分歧时可定位到：
  - 量化差异
  - 事件差异
  - gate 差异
  - 数据源差异

---

## 3.7 WP6：测试与基线

### 建议新增测试文件

- `packages/akshare-mcp/tests/test_unified_decision_contracts.py`
- `packages/akshare-mcp/tests/test_unified_decision_builders.py`
- `packages/akshare-mcp/tests/test_unified_decision_gate_fusion.py`
- `apps/bff/test/assistant-unified.e2e-spec.ts`

### 测试类型

- schema contract test
- summary/details snapshot test
- latency benchmark
- old/new diff audit
- regression test

### 至少要覆盖的场景

- 有完整数据
- 缺财务数据
- 缺事件数据
- 无用户画像
- event warning
- hard veto
- risk downgrade
- summary 成功但 details 部分降级

### 验收标准

- contract 稳定
- summary/details 字段不漂移
- MCP 和 BFF 接口兼容

---

## 4. 建议 PR 切分

## PR-1：协议与 DTO

范围：

- 文档协议
- BFF DTO
- MCP contracts

目标：

- 不碰复杂业务逻辑，先把字段口径固定下来

## PR-2：MCP 上下文 builders

范围：

- `build_stock_context`
- `build_quant_context`
- `build_event_context`

目标：

- 先把素材层统一出来

## PR-3：Gate 与 Fusion

范围：

- `run_decision_gate`
- `fuse_decision_payload`
- `get_unified_decision_summary`
- `get_unified_decision_details`

目标：

- 形成真正的统一决策输出

## PR-4：BFF 接口接入

范围：

- assistant unified service/controller
- `userId` 透传

目标：

- 打通 web 到 MCP 主链

## PR-5：前端 summary/details

范围：

- assistant 页面
- decision card 增强

目标：

- 让用户能实际使用统一决策

## PR-6：灰度与 diff

范围：

- legacy 并行
- diff 展示
- 回归观察

目标：

- 安全替换旧逻辑

## PR-7：测试与基线

范围：

- contract
- latency
- diff audit

目标：

- 给上线和迭代提供稳定护栏

---

## 5. 实施先后依赖

严格依赖链：

- WP0 -> WP1 -> WP2 -> WP3 -> WP4

可并行项：

- WP6 可以从 WP2 开始同步补
- WP5 可以在 WP4 完成后插入执行

不建议并行的组合：

- 在协议未定前并行做 BFF DTO 和前端展示
- 在 gate 未定前并行做 old/new diff

---

## 6. 当前已知风险与对应任务

### 风险 1：用户数据源分裂

对应任务：

- WP0 明确统一口径
- WP3 补 `userId` 透传

### 风险 2：首屏过重

对应任务：

- WP2 强制 summary/details 分层
- WP4 做 details 懒加载

### 风险 3：事件误判

对应任务：

- WP1/WP2 明确 `warning` 与 `hard_veto` 分级

### 风险 4：旧逻辑替换过快

对应任务：

- WP5 灰度并行
- WP6 diff 与回归

### 风险 5：结构化输出仍然松散

对应任务：

- WP0/WP1 引入统一 contract
- WP3 削减 BFF 字段猜测逻辑

---

## 7. 建议排期

如果按“小步快跑 + 多 PR”推进，建议节奏：

- 第 1 周：
  - WP0
  - WP1
- 第 2 周：
  - WP2
  - WP3
- 第 3 周：
  - WP4
  - WP5
  - WP6

如果按单人推进，建议至少把以下三件事视为同一里程碑：

- 协议完成
- summary/details MCP 入口完成
- assistant 页面可用

---

## 8. 最终执行建议

第一优先级，不是写 `get_unified_decision` 代码，而是先把以下 3 件事确定下来：

1. 统一字段契约
2. `userId` 与用户画像口径
3. summary/details 边界

这三件事一旦确定，后面的 builder、gate、fusion、BFF、前端，都会变成标准实现问题；如果这三件事不先定，后面每一层都会返工。

