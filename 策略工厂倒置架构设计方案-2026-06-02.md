# 策略工厂倒置架构设计方案（深度版）

日期：2026-06-02
版本：INVERT-DESIGN-V2（深度代码级设计）
状态：设计提案 / 待评审
关联：`策略工厂实际架构深度审查-2026-06-02.md`（ARCH-MAP-V2，现状与三宗原罪的代码定位）

---

## 0. 设计原则与口号

把当前的"**证明盈利 → 才允许孵化**"，倒置为"**便宜地大量孵化 → 用向前真实数据测准确率画像 → 现实持续选择 → 严出到资本**"。

回测统计武器**不删除**，而是从"孵化前置门"**搬迁**为"晋升到真实资本的门"，且作用对象从**回测内数据**改为**向前实测数据**。

> 口号：**观察先行，现实选择；要带状态标签的命中率画像，不要回测盈利断头台。**

---

## 1. 为什么是倒置而非重写（关键前提）

深度审查（ARCH-MAP-V2 §5）已证实：**用户想要的"向前命中率测量系统"在代码里已完整存在**，只是被接在了漏斗下游、默认关闭。需要的零件清单：

| 零件 | 文件:行 | 现状 | 倒置后角色 |
|---|---|---|---|
| `ForwardVerifier.verify` | `incubation_factory/forward_verifier.py:70` | 已实现方向命中/前向IC/Sharpe/skill_lcb | **Layer 2 核心测量器** |
| `INCUBATION_PROFILES` | `forward_verifier.py:22-50` | 已按 high/medium/low_freq 分档 | Layer 2 分档观察参数 |
| `_compute_skill_lcb` | `forward_verifier.py:220-233` | Wilson 下界 - 0.5 基线 | **Layer 2/3 的核心晋升信号** |
| `HitRateReporter` | `incubation_factory/hit_rate_reporter.py` | 已按 family/stage 聚合 + 趋势 | Layer 2 仪表盘 + 配额反馈 |
| `SignalGenerator.generate` | `incubation_factory/signal_generator.py:24` | **策略无关**，任何带 DSL 的策略都能跑 | Layer 2 每日向前信号生成 |
| paper 账户体系 | `paper_accounts` 表 + `ensure_account` | 已支持 stage 字段 | Layer 1 落地承载 |
| `_list_paper_observation_strategies` | `intake.py:191` / `runner.py:550` | 默认 toggle OFF 的旁路 | **提升为主干默认路径** |
| Gate-3 统计武器 | `submission_gate/runner_parts/multiple_testing.py` | 准入前置门 | **搬迁为 Layer 3 晋升门** |

**关键技术事实**：`SignalGenerator.generate`（signal_generator.py:24）的签名是 `generate(db, strategy, signal_date)`——它只需要一个带 DSL 参数的 strategy dict，**不检查 status**。这证明观察层在技术上可以对任何被准入的策略运行，不需要它先是 `incubating`。倒置无需新写测量引擎。

---

## 2. 倒置前后对照表

| 维度 | 现状（门禁漏斗） | 倒置后（观察农场） | 代码落点 |
|---|---|---|---|
| 进孵化条件 | Gate-3 回测盈利全达标 | Gate-0 结构合法 + DSL 可编译 + 非重复 | `intake.py` 主入口改写 |
| 选择机制 | 回测统计二元 pass/fail | 向前实测画像 + 时间 + 持续淘汰 | `forward_verifier` 输出驱动 |
| 评价对象 | "平均赚不赚钱"单一盈利数 | 多维画像（命中率/方向/幅度/一致性/分regime） | `ForwardVerifier` 扩展 |
| 市场状态 | regime-blind（120天平均） | regime-tagged（每信号带状态标签） | `forward_verifier` + `signal_generator` 加字段 |
| 严格统计位置 | 孵化**前**（前置门） | 孵化**后**（晋升资本门），作用于前向数据 | Gate-3 逻辑搬迁到 promotion |
| 认知产出 | 零（`strategy_signal_evidence` 0 行） | 每策略持续产生带状态前向数据 | 同上 |

---

## 3. 新三层结构（替代单一 9 关漏斗）

### Layer 1 — 准入（宽进、便宜、零资本）

**准入条件**（替代 Gate-1/2/3 前置）：
1. Gate-0 结构校验通过（保留，`quality_gates`）。
2. DSL 可编译（保留，挡掉 §零产出方案 §13.3 那个 `dsl_compile_failed:ModuleNotFoundError`）。
3. 非重复（保留 dedup，但阈值放宽，见 §6.3）。

**动作**：直接 `incubation_service.ensure_account(db, strategy, stage="observe")`，写 paper 账户，进入 Layer 2。**不跑 Gate-1/2/3 的盈利证明。**

**保留的防护**：宽进 ≠ 无防护。垃圾（结构非法/不可编译/重复）仍被挡。回测仍可作为**软信息**记录进画像，但不再是生死门。

### Layer 2 — 观察（向前测量、持续、多维画像）

每个 observe-stage 策略，每个交易日：
1. `SignalGenerator.generate` 生成当日信号（signal_generator.py:24，已存在）。
2. T+N 后 `ForwardVerifier.verify` 记录前向结果，**每个信号打市场状态标签**（新增，见 §5）。
3. 累积成**画像**而非单一数：
   - `primary_hit_rate` 方向命中率（已有，forward_verifier.py:131）
   - `forward_ic` / `forward_sharpe`（已有）
   - `primary_skill_lcb` 命中率95%置信下界（已有，防小样本运气）
   - **预测幅度 vs 实际幅度误差**（新增，见 §5.2）
   - **回测预期 vs 实盘一致性**（新增，见 §5.3）
   - **分 regime 命中率**（新增，见 §5.1）

**淘汰规则**（替代二元 pass/fail）：
- 不是"某数不达标即死"。
- 是"`skill_lcb` 累积 N 周仍显著为负 **或** 回测-实盘一致性崩坏 **或** 所有 regime 下命中率都 ≤ 0.5"才退役。
- 给策略**时间**证明自己（`INCUBATION_PROFILES` 的 min_days/min_trades 已定义，forward_verifier.py:22-50）。

### Layer 3 — 晋升资本（严出、稀少、用前向数据）

这里才用严格统计武器，但**作用于 Layer 2 累积的前向真实数据**，不是回测：
- 向前 `skill_lcb` 显著为正（如 > 0.03 持续 N 周）。
- **跨市场状态稳定**（不是只在单一 regime 活——这正是破原罪 2 的关键）。
- deflated Sharpe / 多重检验（搬迁 `multiple_testing.py` 的 live 分支，但喂前向序列；防"观察了很多个策略"的选择偏差）。
- 通过 → 候选真实资本；否则留 Layer 2 继续观察。

```
准入(宽) ──大量──> 观察(向前测画像+regime标签) ──少数──> 晋升资本(严,用前向数据)
   Gate-0+可编译+非重复    ForwardVerifier累积N周        skill_lcb显著正+跨regime稳定
```

---

## 4. 核心改动清单（精确到文件 + 函数 + 改法）

### 改动 A【必需】放开 Layer 1 准入

- **文件**：`strategy-factory/.../services/admission_authority.py::SubmissionAdmissionAuthority.resolve` + `akshare-mcp/.../incubation_factory/intake.py::scan_and_accept`
- **现状**：`resolve` 只有 `formal_track_eligible=True` 才给 `formal_incubation` lane → `lifecycle_coordinator.py:606` 才设 `incubating`；`intake.py:185` 只认 `incubating`。
- **改法**：
  1. 新增准入档 `observe`：候选只要 Gate-0 通过 + DSL 可编译 + 非重复，`admission_authority` 给一个 `observe_admit` 结果，由 lifecycle 写 `stage='observe'` paper 账户（复用 `ensure_account`，已存在）。
  2. `intake.scan_and_accept` 把 `_list_paper_observation_strategies`（intake.py:191）从 toggle 旁路提升为**默认主路径**（去掉 `paper_intake_enabled()` 的默认 False 限制，或默认 ON）。
- **风险**：低。observe 是零资本 paper，宽进不碰钱。

### 改动 B【必需】Gate-3 从准入门搬到晋升门

- **文件**：`submission_gate/runner_parts/multiple_testing.py`（`_evaluate_trade_profile` / `_live_multiple_testing_reasons`）+ `lifecycle_coordinator.py:606`
- **现状**：Gate-3 通过才 `status='incubating'`（lifecycle_coordinator.py:606）。
- **改法**：
  1. Gate-3 不再是进 observe 的条件。
  2. 新建 `PromotionGate`（可复用 `multiple_testing.py` 的统计函数），输入 = `ForwardVerifier` 累积的前向序列（不是回测 metrics），输出 = "是否够格 stage='candidate'/真实资本"。
  3. `lifecycle_coordinator` 的 `formal_incubation` 分支改为"晋升"语义：只有 Layer 2 前向数据达标才走。
- **风险**：中。需保证 PromotionGate 的输入契约（前向序列）与现有统计函数兼容。

### 改动 C【必需】评价从二元盈利改为多维画像

- **文件**：`forward_verifier.py::verify` 返回结构（已有大部分）+ 新增淘汰判定器
- **现状**：`verify` 已返回画像（forward_verifier.py:153-180），但下游没有"基于画像的淘汰/晋升决策器"消费它，且准入端仍用 `_evaluate_trade_profile` 的二元 `passed`。
- **改法**：
  1. 新建 `ObservationLifecyclePolicy`：消费 `verify` 画像，输出 `keep_observing` / `retire` / `promote`。
  2. 淘汰条件：`primary_skill_lcb < 0` 且 `primary_effective_n ≥ min_trades` 且累积 ≥ min_days。
  3. **不**用单一 win_rate/Sharpe 一刀切。
- **风险**：低。纯新增决策逻辑，不动测量。

### 改动 D【必需】给前向数据打市场状态标签（破 regime-blind）

- **文件**：`forward_verifier.py::verify` + `signal_generator.py::generate` + `strategy_signal_evidence` 表 schema
- **现状**：`verify` 把所有信号 hit 混在一起算 `primary_hit_rate`（forward_verifier.py:130），无 regime 维度。
- **改法**：
  1. 信号生成/验证时，给每条 evidence 附当日市场状态标签（趋势/震荡 by ADX 或均线斜率、波动率档 by ATR 分位、恐贪 by 现有 fear_greed_index）。
  2. `verify` 增加 `hit_rate_by_regime` 聚合输出（按标签分组算命中率 + skill_lcb）。
  3. 晋升门（改动 B）要求"跨主要 regime 都有正 skill"，而非全局平均正。
- **风险**：中。需接入市场状态分类器（恐贪指数已有，趋势/波动率需新增轻量计算）。

### 改动 E【配套】幅度 + 一致性度量

- **文件**：`forward_verifier.py::verify`
- **改法**：
  1. **幅度准确度**：记录 `predicted_magnitude`（若策略输出）vs 实际 `forward_return` 的误差分布；无预测幅度则跳过。
  2. **回测-实盘一致性**：把策略回测期望（已存于 quality_report.backtest_metrics）与向前 `forward_sharpe`/`hit_rate` 对比，输出 `live_vs_backtest_consistency`。一致性崩坏（实盘远差于回测）是过拟合的**向前**证据。
- **风险**：低。纯增量指标。

### 改动 F【配套】先修通现有漏斗两个堵点（过渡期验证）

- 在倒置落地前，先按零产出方案 §13.5 修 `_resolve_gate_2_top_k`（policy.py:396 比例制退化为1）+ DSL 编译 `ModuleNotFoundError`，让**至少一个策略走完现有漏斗进 incubating**，验证 `ForwardVerifier` 确实能跑出前向命中率数据（当前 `strategy_signal_evidence` 表 0 行，从未被验证过）。
- **目的**：用最低成本证明测量层可用，再做架构倒置。

---

## 5. 三个新增度量的设计细节

### 5.1 市场状态标签（regime tagging）

每条信号 evidence 记录时附：
- `trend_regime`：`uptrend` / `downtrend` / `range`（按标的 60 日均线斜率 + ADX）。
- `vol_regime`：`low` / `mid` / `high`（按 ATR 历史分位）。
- `sentiment_regime`：`fear` / `neutral` / `greed`（复用 `fear_greed_index`，已有）。

`ForwardVerifier.verify` 输出新增：
```python
"hit_rate_by_regime": {
    "uptrend":   {"hit_rate": ..., "skill_lcb": ..., "n": ...},
    "range":     {"hit_rate": ..., "skill_lcb": ..., "n": ...},
    ...
}
```

### 5.2 幅度准确度

- 若策略信号带 `predicted_return`/`target_pct`：记录 `magnitude_error = |predicted - realized|`，聚合 MAE / 分位。
- 若不带：标记 `magnitude_available=False`，画像只用方向命中。

### 5.3 回测-实盘一致性

```python
"live_vs_backtest_consistency": {
    "backtest_sharpe": <from quality_report>,
    "forward_sharpe":  <from ForwardVerifier>,
    "backtest_hit_rate_proxy": <win_rate from backtest>,
    "forward_hit_rate": <primary_hit_rate>,
    "consistency_score": forward / max(backtest, eps),  # <0.5 = 过拟合向前证据
}
```

---

## 6. 必须正视的设计风险与边界

### 6.1 不要把命中率变成新的单一断头台【最重要】

- 70% 命中率仍可能亏钱（小赚多次大亏几次）；35% 命中率仍可能赚钱（趋势跟踪）。
- 所以命中率是**画像的重要维度，不能取代盈亏/幅度成为唯一标准**。
- Layer 2 用**多维画像 + 时间**；Layer 3 才做综合判决。
- 用 `skill_lcb`（命中率置信下界，已实现）而非裸命中率，防"观察样本少→运气好→误判有skill"。

### 6.2 宽进会冲垮 Layer 2 算力吗？

- paper 信号生成 + 前向验证是廉价操作（无真实下单）。
- 用 `INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT`（已有，默认 50）控批量。
- 加 observe 容量上限 + LRU 淘汰（skill_lcb 最差的先退役）。

### 6.3 dedup 阈值

- 现状 0.85/0.93 在 1 个候选时一撞就归零（零产出方案 §13）。
- 宽进后候选基数大，dedup 仍保留（防同质泛滥），但阈值可适度放宽，且按"画像差异"而非纯 K 线相似度去重。

### 6.4 这套设计能/不能解决什么

**能（架构层）**：
- 策略终于流动，孵化层不再饿死（`strategy_signal_evidence` 从 0 行变成持续积累）。
- 系统从"认知死"变"认知活"：产出带 regime 标签的前向准确率知识。
- 评价贴近真实市场（向前、分状态、多维），不被 regime-blind 平均误杀。

**不能（alpha 源层，ARCH-MAP §6）**：
- 不会凭空造 edge。若候选仍是 `_jitter`/`_jitter_f`（serialization.py:560-566）随机调参的通用 TA，Layer 2 大概率诚实报告"前向命中率≈50%、skill_lcb≈0"。
- **但这恰恰是价值**：用最低成本、最快速度给出"alpha 源到底行不行"的诚实判决——这是过去 6 个月被结构性掩盖的答案。

---

## 7. 实施路线（建议顺序与验收）

| 阶段 | 改动 | 验收标准 |
|---|---|---|
| P0 过渡验证 | F（修 top-k + DSL 编译） | 至少 1 策略进 incubating，`strategy_signal_evidence` 出现非 0 行，`ForwardVerifier` 跑出真实 hit_rate |
| P1 宽进 | A + D | observe stage 策略数 > 10，每条信号带 regime 标签 |
| P2 画像化 | C + E | `ObservationLifecyclePolicy` 上线，淘汰基于 skill_lcb 而非单一 win_rate |
| P3 严出 | B | PromotionGate 用前向数据；Gate-3 不再是 observe 前置 |
| P4 判决 | — | 观察 2-4 周，用 `HitRateReporter` 数据回答"alpha 源行不行"，决定是否换源/上资本 |

---

## 8. 决策框架（给拍板的人）

- **若目标 = 稳定产出能进实盘的策略，且候选源仍是随机调参通用 TA**：倒置架构能让你**快速证实这条路行不通**，省下继续投管道的人力。这本身是高价值的负结果。
- **若目标 = 一个研究筛选/探矿工具，偶尔捞到值得人工深挖的候选**：倒置架构**正是它该有的样子**——宽进、向前测、现实选、严出。心态从"印钞机"改成"探矿机 + 真实记分牌"。

无论哪条：**先做倒置，再谈 alpha**。因为只有倒置后的系统，才能诚实地告诉你 alpha 源的真相——而这正是当前漏斗架构在结构上禁止你得到的东西。

---

# 第二部分：新架构完整说明（流程 / 状态机 / 数据流）

> 前面的 §1-§8 讲"改哪里"。本部分讲"新架构整体长什么样、一个策略从生到死完整走一遍是什么样"。

## 9. 一张图看懂新旧架构差异

### 9.1 旧架构（当前 · 门禁漏斗）

```
生成 ─> Gate0 ─> PreGate ─> Gate1 ─> top-k ─> Gate2 ─> dedup ─> Gate3(回测盈利证明)
                                                                      │
                                              全部达标才 ──────────────┘
                                                                      ↓
                                              status='incubating' ─> 孵化(才开始向前测量)
                                                                      │
   ★ 任意一关归零 = 整条归零；6 个月 = 0 个走到这里                     ↓
                                              ForwardVerifier(命中率/前向IC) ← 永远没输入
```
特征：**证明在前，观察在后**；单一长串联；二元生死；回测内数据决定一切。

### 9.2 新架构（倒置 · 观察农场）

```
                          ┌─────────── Layer 1 准入（宽进/零资本）───────────┐
生成 ─> Gate0(结构) ─> DSL可编译 ─> 非重复 ─> 建 paper 账户 stage='observe'  │
                          └────────────────────────────────────────────────┘
                                          │ 大量策略（几十~几百）并行进入
                                          ↓
   ┌──────────────────── Layer 2 观察（向前测量/持续/并行）─────────────────────┐
   │  每日: SignalGenerator 生成信号 → T+N 后 ForwardVerifier 记录命中(带regime标签) │
   │  累积画像: hit_rate / skill_lcb / forward_ic / 幅度误差 / 回测-实盘一致性       │
   │  6 阶段状态机自动推进(已存在 resolve_incubation_pipeline_stage):              │
   │      warmup → observe → candidate → graduation_ready                        │
   │                    ↓(skill_lcb<0 / 一致性崩坏 / 全regime≤0.5)                 │
   │                  failed (退役，释放观察槽)                                    │
   └────────────────────────────────────────────────────────────────────────────┘
                                          │ 累积 N 周、graduation_ready 的少数策略
                                          ↓
   ┌──────────── Layer 3 晋升资本（严出/稀少/用前向数据）────────────┐
   │  PromotionGate: 前向 skill_lcb 显著正 + 跨 regime 稳定          │
   │               + deflated Sharpe/多重检验(喂前向序列)            │
   │  通过 → stage='candidate'→'listed' → 真实资本候选              │
   │  不通过 → 留 Layer 2 继续观察                                  │
   └──────────────────────────────────────────────────────────────┘
```
特征：**观察在前，证明在后**；宽进-并行-严出；多维画像+时间选择；前向真实数据决定晋升。

---

## 10. 关键事实：6 阶段前向状态机已经实现，只是跑在空集上

深度审查发现：用户想要的"按命中率/前向证据推进的状态机"**代码里完整存在**——`strategy_lifecycle_shared/incubation.py::resolve_incubation_pipeline_stage`（第 12-78 行）。

它的判定逻辑**完全是前向证据驱动**（摘录自源码）：

```python
# warmup: 证据不足
if primary_effective_n < 20 or coverage_ratio < 0.25:
    stage = "warmup"
# failed: 近期 skill 转负 / 稳定性崩坏 / 风险累积
elif recent_primary_skill_lcb < -0.03 or stability_gap > 0.10 or open_risk_count >= 3:
    stage = "failed"
# graduation_ready: 主次窗口 skill_lcb 都显著正 + 覆盖率高 + 稳定
elif (primary_effective_n >= 60 and secondary_effective_n >= 30
      and primary_skill_lcb > 0 and secondary_skill_lcb > 0
      and recent_primary_skill_lcb > 0 and coverage_ratio >= 0.75
      and stability_gap <= 0.05 and open_risk_count == 0):
    stage = "graduation_ready"
# observe / candidate: 中间态
```

配套的 `resolve_incubation_action_plan`（同文件第 159-280 行）已实现完整的**前向决策**：`continue_observe` / `freeze_and_revise` / `signal_vacuum_warning`(信号真空预警) / `ready_for_promotion_review` / `cleanup_low_confidence_candidate`，并按 `negative_skill_streak`（连续负 skill 计数）、`signal_vacuum_days`（信号真空天数）自动调预算/runtime。

**这就是新架构的 Layer 2/3 引擎——它已经写好了。** 倒置要做的不是发明它，而是**把它的输入从"空的 incubating 集"换成"宽进的 observe 集"**。这是本方案"倒置非重写"最强的证据。

---

## 11. 状态机：策略的完整生命周期（新架构）

### 11.1 双轴状态模型

新架构用**两个轴**描述一个策略，而非单一 `status`：

| 轴 | 字段 | 取值 | 含义 |
|---|---|---|---|
| 业务状态 | `strategies.status` | `observing` / `listed` / `retired` | 粗粒度生命周期 |
| 孵化阶段 | `paper_accounts.incubation_stage` | `warmup`/`observe`/`candidate`/`graduation_ready`/`failed`/`listed` | 细粒度前向证据阶段 |

（注：现有 `incubation_stage` 字段已支持 warmup/paper/candidate/listed，见 `incubation_parts/context.py:50`；新架构把 `paper` 细化为 observe，并接上 `resolve_incubation_pipeline_stage` 的 6 阶段。）

### 11.2 完整状态迁移图

```
[生成] → (Gate0+可编译+非重复通过)
   │
   ↓  status=observing, stage=warmup           ← Layer 1 准入（不再要回测盈利证明）
[warmup]  积累信号中（primary_effective_n<20 或 coverage<0.25）
   │  每日 SignalGenerator 生成信号 + ForwardVerifier 验证
   ↓  证据够了（n≥20, coverage≥0.25）
[observe]  skill_lcb 还不稳定 / 覆盖率中等
   │  ├─ skill_lcb 持续为正、证据增厚 ────────────────┐
   │  └─ skill_lcb<-0.03 / 稳定性崩坏 / 信号真空 ──> [failed] → status=retired（释放槽）
   ↓
[candidate]  主窗口 skill_lcb>0 + execution_audit 通过
   │  ├─ 主次窗口都达标（n≥60/30, 双 skill_lcb>0, coverage≥0.75, 稳定）┐
   │  └─ 退化 ──────────────────────────────────────> [observe]/[failed]
   ↓
[graduation_ready]  前向证据全面达标
   │  → PromotionGate（Layer 3）: 跨 regime 稳定 + deflated Sharpe(前向)
   │  ├─ 通过 ──> status=listed, stage=listed（真实资本候选）
   │  └─ 不通过 ──> 留 candidate/observe 继续观察
   ↓
[listed]  进入真实资本监控（runtime risk 持续看护，退化则 retire）
```

### 11.3 与旧状态机的关键差异

| | 旧 | 新 |
|---|---|---|
| 进入观察的前提 | Gate-3 回测盈利达标 | 仅结构合法+可编译+非重复 |
| 谁来淘汰 | 回测统计一刀切 | 前向 skill_lcb + 时间 + regime（状态机自动推进） |
| `failed` 的依据 | 回测 IC/Sharpe 不达标 | **向前**近期 skill 转负 / 信号真空 / 稳定性崩坏 |
| 晋升资本的依据 | （走不到） | 前向证据 N 周达标 + 跨 regime 稳定 |

---

## 12. 数据流：一条信号的完整旅程

```
1. [准入] 策略 X 进 observe，建 paper_account(stage=warmup)
              写 strategies(status=observing)

2. [每日 T] SignalGenerator.generate(db, X, today)
              → 编译 X 的 DSL → 取目标股行情 → 规则引擎产信号
              → 写 strategy_signals 表（方向 direction, 目标股, 预测幅度?）
              → 同步 paper_orders（模拟下单，零真实资本）
              → 打市场状态标签: trend_regime/vol_regime/sentiment_regime【新增】

3. [T+N] ForwardVerifier.verify(db, X)
              → 读 strategy_signal_evidence（每条信号的 forward_returns）
              → 算 hit = (return>0 & dir>0) or (return<0 & dir<0)
              → 聚合: primary_hit_rate, skill_lcb, forward_ic, forward_sharpe
              → 分 regime 聚合: hit_rate_by_regime【新增】
              → 算幅度误差、回测-实盘一致性【新增】
              → 写 strategy_incubation_metrics（每日一条画像）

4. [状态推进] resolve_incubation_pipeline_stage(画像)
              → 返回 warmup/observe/candidate/graduation_ready/failed
              → 写 strategy_incubation_pipeline_snapshots
              → resolve_incubation_action_plan → continue/freeze/promote/cleanup

5. [汇总] HitRateReporter
              → 按 family/stage/regime 聚合命中率仪表盘
              → skill_lcb 趋势(improving/declining) → 反馈生成端配额

6. [晋升或退役]
              graduation_ready + PromotionGate 通过 → status=listed（资本候选）
              failed → status=retired，释放 observe 槽位
```

**当前 vs 新**：第 2-6 步的引擎全部已存在，但当前只对 `status='incubating'` 的策略跑（第 1 步产不出），所以 `strategy_signal_evidence` 表 0 行。新架构第 1 步宽进后，这条数据流立即活起来。

---

## 13. 数据模型改动（最小集）

| 表 | 改动 | 说明 |
|---|---|---|
| `strategies.status` | 新增取值 `observing` | 区别于旧的 `submitted`/`incubating` |
| `paper_accounts.incubation_stage` | 启用 `observe`/`graduation_ready`/`failed` | 已有字段，扩展取值 |
| `strategy_signal_evidence` | 每条信号新增 `trend_regime`/`vol_regime`/`sentiment_regime` | 支持分 regime 命中率 |
| `strategy_incubation_metrics` | 画像新增 `hit_rate_by_regime`/`magnitude_mae`/`live_vs_backtest_consistency` | ForwardVerifier 输出扩展 |
| `strategy_factory_runs.summary` | 新增 `observe_admitted_count`/`observe_promoted_count`/`observe_retired_count` | 新漏斗的可观测性 |

无需新建表——全部是已有表加字段或启用已有字段。

---

## 14. 新旧并存与迁移（不停机）

新架构不要求一次性切换，可灰度并存：

1. **影子期**：保留旧漏斗，同时开 Layer 1 宽进（toggle `STRATEGY_FACTORY_OBSERVE_FARM_ENABLED`）。两条路并行，observe farm 不碰旧 incubating 逻辑。
2. **观察期 2-4 周**：用 `HitRateReporter` 比对——observe farm 的前向命中率分布 vs 旧漏斗（旧漏斗大概率仍 0 产出）。
3. **切换**：确认 observe farm 数据健康后，把旧漏斗的 Gate-3 降级为 Layer 3 PromotionGate，旧 `incubating` 主路径退役。
4. **回滚**：toggle 关闭即回到纯旧漏斗，零风险。

---

## 15. 新架构能回答的核心问题（旧架构回答不了）

| 问题 | 旧架构 | 新架构 |
|---|---|---|
| AI 生成的策略向前命中率多少？ | 不知道（0 个走到测量） | 每策略每日一条画像 |
| momentum 在趋势市 vs 震荡市分别准吗？ | 不知道（regime-blind 平均） | `hit_rate_by_regime` 直接给 |
| 回测好的策略实盘也好吗？ | 不知道 | `live_vs_backtest_consistency` 直接给 |
| 我们的 alpha 源（通用 TA）到底行不行？ | **结构上无法回答** | 2-4 周观察后用数据判决 |
| 哪些 family 该加配额、哪些该冷却？ | 拍脑袋 | `HitRateReporter` skill_lcb 趋势驱动 |

**这是新架构最根本的价值：把一个"认知上死的"系统变成"认知上活的"——它持续产出关于"什么策略在什么市场状态下有多大预测力"的真实知识，而这正是用户要的"命中率/涨跌/预测/回测情况"，也是旧漏斗在结构上禁止你得到的东西。**

---

# 第三部分：可行性论证与技术支撑（联网调研）

> 本部分用学界论文 + 业界实践,对倒置架构的每一根支柱做可行性背书。所有外部内容均转述并标注来源,具体数字以原文为准。

## 16. 五根支柱的外部证据

### 支柱 1 —— "向前测试/纸面交易作为正式遴选环节"是行业标准做法

倒置架构 Layer 1→2 把"宽进 + 向前观察"作为主遴选机制。这与业界推荐的标准工作流一致:

- 业界推荐的标准序列正是 **"回测 → walk-forward 验证 → 纸面交易 2-4 周(同一套策略逻辑) → 部署"**——纸面/向前阶段是部署前的必经桥梁,不是可选项。([blockchain-council](https://blockchain-council.org/cryptocurrency/automated-crypto-trading-backtest-paper-trade-deploy-bots-safely/))
- 向前测试回答的是回测回答不了的问题:"实时、bar 未收盘时,这套规则能否被一致地执行";"回测告诉你过去会怎样,纸面交易告诉你当下大致会怎样"。两者都必要,互不替代。([obside](https://obside.com/trading-strategies/paper-trading)、[aquafunded](https://www.aquafunded.com/blogs/paper-trading-vs-backtesting))

> 支撑结论:把向前观察提升为主遴选环节,符合行业共识,不是激进设计。内容已改写以符合授权要求。

### 支柱 2 —— "宽进 + 大量并行候选 + 向前遴选"有顶级量化机构的实证范式

倒置架构 Layer 1 宽进、Layer 2 大量并行观察的模式,正是顶级量化平台的真实做法:

- WorldQuant(2007 年从 Millennium 分拆)据报道在其实盘基础设施上部署约 **400 万个独立 alpha 信号**——不是一个有 400 万参数的大模型,而是海量弱信号并行,各自被验证、组合。([youngandcalculated](https://youngandcalculated.substack.com/p/how-quant-hedge-funds-actually-build))
- DSR 文献明确描述了这种范式的风险与对策:"如果你测了 1000 个策略变体只挑最好的,Sharpe 2.0 可能只是随机噪声的期望最大值"——所以并行海选**必须配多重检验校正**(见支柱 5)。([paperswithbacktest](https://paperswithbacktest.com/wiki/deflated-sharpe-ratio))

> 支撑结论:"海量弱候选并行观察、靠样本外遴选"是被验证的工业范式。本方案 Layer 1/2 是其轻量版。**但必须在 Layer 3 配 DSR 类校正**,否则"观察很多个"本身会制造选择偏差——本方案 §4 改动 B、§6.1 已纳入。

### 支柱 3 —— "市场状态条件化评价"被学界证明显著优于 regime-blind 平均

倒置架构改动 D(给前向数据打 regime 标签、分状态聚合命中率)直击原罪 2(regime-blind 平均)。学界证据强:

- 高波动和 regime 切换会让趋势跟踪、均值回归等传统策略失效——这正是"把好坏行情平均成噪声"误杀条件型 edge 的根源。([arxiv 2601.19504](https://arxiv.org/abs/2601.19504))
- regime-aware 框架的实证收益显著:某 Wasserstein-HMM regime 模型相对等权/买入持有基准,Sharpe **2.18 vs 1.59 / 1.18**,最大回撤 **-5.43% vs -14.62%**。([arxiv 2603.04441](https://arxiv.org/html/2603.04441v1))
- 另一 regime-aware ML 组合(RegimeFolio,34 只美股大盘 2020-2024)报告累计收益 137%、Sharpe 1.17、回撤低 12%、预测准确率提升 **15-20%**。([arxiv 2510.14986](https://arxiv.org/html/2510.14986v1))

> 支撑结论:按市场状态分层评价,不仅"更贴近真实",学界实证它能显著提升风险调整后收益与预测准确率。改动 D 有硬支撑。内容已改写以符合授权要求。

### 支柱 4 —— "用户要命中率而非胜率"——但命中率也不能单用,需多维画像

用户主张"不要胜率,要命中率/涨跌预测准确率"。调研结论:**方向正确,但要害是"任何单一比率都不能单独决定生死",必须配期望值/盈亏比**。这正是本方案 §6.1 的核心警告,有压倒性业界共识:

- "40% 命中率的交易者可以比 65% 命中率的更赚钱";"高命中率可能藏着负偏度——赢 9 次各 100、输 1 次 1500,90% 命中率在数学上无意义"。([nexusfi](https://nexusfi.com/a/risk-management/trading-performance-metrics)、[alphametrics](https://alphametrics.substack.com/p/why-win-rate-is-a-vanity-metric))
- 期望值(expectancy = f(命中率, 盈亏比))才是"系统是否真能赚钱"的单一数;命中率孤立看是"虚荣指标"。([nexusfi expectancy](https://nexusfi.com/a/risk-management/expectancy)、[thecantillonreport](https://thecantillonreport.substack.com/p/why-your-win-rate-is-lying-to-you))

> 支撑结论:用户"看命中率"的直觉对——它是比回测盈利更诚实的"预测力"度量;但本方案坚持把它放进**多维画像(命中率 + 幅度 + skill_lcb + 期望/盈亏)**,而非取代盈利成为新断头台。这与业界共识完全吻合。代码侧 `ForwardVerifier` 用 `skill_lcb`(命中率置信下界)而非裸命中率,正是对"负偏度/小样本运气"的防御。

### 支柱 5 —— "严格统计搬到晋升门、作用于向前数据"有 DSR/多重检验理论背书

倒置架构改动 B 把 Gate-3 的 deflated Sharpe / 多重检验从"准入前置"搬到"晋升资本门",且喂向前数据。这有 López de Prado / Bailey 的理论支撑:

- DSR(2014, López de Prado & Bailey)专门校正两大业绩虚高来源:**多重检验下的选择偏差** + 非正态分布。它把观测 Sharpe 向下调整,给出"真实 Sharpe > 0 的概率"。([Wikipedia DSR](https://en.wikipedia.org/wiki/Deflated_Sharpe_ratio)、[SSRN 2460551](http://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551))
- López de Prado《多重检验危机的数据科学解法》:"实证金融的大多数发现是假的,源于多重检验下的选择偏差"。([SSRN 3177057](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3177057))
- "真正算数的 track record 只有一种:真实资本、真实滑点、真实操作约束、最好经审计的实盘记录。"([resonanzcapital](https://resonanzcapital.com/insights/evaluating-backtests-and-simulated-track-records-in-systematic-investing))

> 支撑结论:把严格统计放在"晋升资本"这道真正该严的门,且用向前数据——既符合 DSR 的设计意图(对抗选择偏差),又符合"只有实盘记录才算数"的业界铁律。代码侧 `multiple_testing.py` 已实现 DSR/PBO/reality check,搬迁即可复用。

## 17. "孵化期 / 试用期"本就是行业标准制度

倒置架构的"Layer 2 观察期 N 周才晋升资本",对应业界成熟的"孵化期"制度:

- 对冲基金/CTA 通常**孵化 3-12 个月**积累实盘 track record,再转为正式基金对外募资;track record 越长越可信。([investmentlawgroup](https://www.investmentlawgroup.com/perspectives/launching-an-incubator-hedge-fund/)、[b2broker](https://b2broker.com/fr/library/hedge-fund-incubator-explained-the-smart-way-to-launch-your-fund/))
- 孵化期被定义为"策略证明自己能在真实世界运转、不爆仓、不失声的安静但关键的时期"。([quantreo](https://www.newsletter.quantreo.com/p/incubation-live-monitoring))

> 支撑结论:本方案的"先孵化观察、达标才上资本",不是杜撰,是对冲基金行业的标准制度。我们只是把它**自动化、并行化、提前到 paper 阶段**。

## 18. 可行性总评:技术上可行,工程上低风险,认知上必要

| 维度 | 评级 | 依据 |
|---|---|---|
| 技术可行性 | **高** | Layer 2/3 引擎(`ForwardVerifier`/`resolve_incubation_pipeline_stage`/6 阶段状态机/`HitRateReporter`)代码已存在;倒置是"换输入源",非重写 |
| 行业符合度 | **高** | 向前测试(支柱1)、并行海选(支柱2,WorldQuant)、regime 条件化(支柱3)、多维画像(支柱4)、DSR 晋升门(支柱5)、孵化期(§17)全部有学界/业界背书 |
| 工程风险 | **低** | 无需新建表(§13);可 toggle 灰度并存(§14);回滚零风险 |
| 数据前置 | **已满足** | DB 有 5138 只股票 ≥220 日 K 线(零产出方案 §2.6),足够 regime 标签与前向验证 |

### 唯一的根本性限制(必须诚实写明)

可行性论证支持的是**架构**,不是**alpha 源**。学界同样压倒性地指出:

- 65-85% 已发表因子不可复现(Hou-Xue-Zhang);"实证金融大多数发现是假的"(López de Prado)。
- 随机调参的通用 TA(本系统 `_jitter`/`_jitter_f` 生成,见现状审查 §6)命中率本就接近随机。

**所以倒置架构 100% 能做到的是:用最低成本、最快速度、最诚实的方式,告诉你"我们的 alpha 源到底有没有预测力"。它不能保证 alpha 源一定有 edge——那取决于候选源本身。** 但这恰恰是当前漏斗架构在结构上禁止你得到的答案,也是过去 6 个月零产出的真正代价。

### 一句话可行性结论

> **倒置架构在技术、工程、行业实践三个层面都被充分支撑、风险低、且复用现有代码;它必然能让系统从"认知死"变"认知活",持续产出带市场状态标签的前向预测力数据。它唯一不能替你解决的是"alpha 源行不行"——但它能让你 2-4 周内用数据得到这个答案,而不是再等 6 个月。**

## 19. 参考来源汇总

学界/论文：
- Hou, Xue, Zhang《Replicating Anomalies》— SSRN 2961979
- Harvey & Liu《A Census of the Factor Zoo》— SSRN 3341728
- Bailey & López de Prado《The Deflated Sharpe Ratio》— SSRN 2460551
- López de Prado《A Data Science Solution to the Multiple-Testing Crisis》— SSRN 3177057
- 《In/Out-of-Sample Sharpe Ratios for Linear Predictive Models》— arxiv 2501.03938
- regime-aware：arxiv 2601.19504 / 2603.04441 / 2510.14986

业界/实务：
- 向前测试 vs 回测：blockchain-council / obside / aquafunded / traderspost
- WorldQuant 海量 alpha：youngandcalculated / worldquant.com
- 命中率 vs 期望值：nexusfi / alphametrics / thecantillonreport
- 孵化期制度：investmentlawgroup / b2broker / quantreo / resonanzcapital

> 注：以上外部内容均为转述与概括,已改写以符合授权要求;关键数字与结论以各来源原文为准。外部内容来自非受控来源,作为方向性佐证,不构成投资建议。
