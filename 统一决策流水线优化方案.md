# AIASK 统一决策流水线优化方案

> 创建日期：2026-03-19
> 适用范围：`apps/web`、`apps/bff`、`packages/akshare-mcp`
> 目标：将现有“多工具、多页面、多 manager”的能力，收束为一条可解释、可控、可审计、可执行的统一决策主链

---

## 1. 背景与结论

结合参考流程图和当前项目实际能力，结论非常明确：

- 当前项目不缺功能面，缺的是“统一决策协议”和“统一主链”。
- 现有系统已经具备市场、基本面、估值、新闻、因子、回测、风险、执行、告警等完整零件。
- 参考流程图最值得借鉴的不是某个模型节点，而是以下 5 层分工：
  - `stock_context`：统一股票上下文
  - `quant_context`：统一量化证据上下文
  - `AI layer`：基于上下文做解释和推理
  - `rule gate`：在输出前后做强规则闸门
  - `decision fusion`：融合 AI、量化、事件、风险后得到最终建议

当前 AIASK 更像“强大的能力仓库”，而不是“统一的决策流水线产品”。本方案的核心就是把已有能力编排成一条主链。

---

## 2. 当前项目现状

### 2.1 已有优势

- Web、BFF、MCP 三层架构已成熟，具备统一接入基础。
- 前端已有市场、研究、基本面、技术、估值、回测、组合、风险、助手、策略等页面。
- BFF 已有大量业务模块，具备编排能力。
- MCP 已有 158 个工具，覆盖行情、因子、回测、风控、组合、执行、文本信号、预警等能力域。
- 项目内部已经意识到“工具不应越权替 AI 做结论”，已有相关优化文档和事件驱动架构草案。

### 2.2 已落地但分散的能力原型

- 统一分析卡片雏形：
  - `apps/bff/src/assistant/assistant.service.ts`
- 情绪/KYC/安全约束雏形：
  - `apps/bff/src/chat/tools/system-prompt.ts`
- 量化上下文雏形：
  - `packages/akshare-mcp/src/akshare_mcp/tools/factor_profile.py`
  - `packages/akshare-mcp/src/akshare_mcp/tools/quant.py`
- 文本信号与事件素材雏形：
  - `packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py`
  - `packages/akshare-mcp/src/akshare_mcp/services/llm_alpha.py`
- 执行闸门与合规闸门雏形：
  - `packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py`
  - `packages/akshare-mcp/src/akshare_mcp/tools/managers/compliance_manager.py`
- 事件驱动主链草案：
  - `策略工厂/EVENT_DRIVEN_FACTORY_REBUILD_PLAN.md`

### 2.3 当前核心缺口

1. 没有统一的 `build_stock_context`。
2. 没有统一的 `build_quant_context`。
3. 事件信号还没有提升为“高优先级决策变量”。
4. BFF 仍主要消费单个工具结果，而不是消费统一融合结果。
5. `should_i_buy`、`decision_manager` 一类工具仍存在较重的硬编码评分逻辑。
6. 前端页面很多，但没有一个真正代表“统一决策主链”的核心视图。

### 2.4 实施前置约束

为了避免“方向正确、实施受阻”，本方案补充以下前置约束：

1. **必须先定义 MCP 结构化输出契约**
   - 统一决策相关工具不能继续采用“随手返回 dict”的松散风格。
   - 首版必须定义明确 schema，并尽量走结构化输出。
2. **必须先定义用户上下文透传链路**
   - 方案中的 `user_gate` 依赖 `userId / riskLevel / kycLevel / profileSummary`。
   - 当前聊天链路具备用户上下文，助手接口链路不足，因此需要单独补协议。
3. **必须采用“摘要优先、明细展开”的交互模式**
   - 当前 BFF 与 MCP 仍是同步请求-响应模式。
   - 统一决策首屏不能一次性拉全量明细，否则会放大连接池与延迟压力。
4. **必须保留旧入口兼容与灰度比对**
   - `should_i_buy`
   - `smart_stock_diagnosis`
   - `decision_manager`
   - 这些入口不能一次性下线，应保留 diff 对照和回归观察窗口。

---

## 3. 参考流程图中最值得借鉴的部分

### 3.1 值得直接吸收

- 先构建上下文，再做推理，不要一上来就输出结论。
- 量化层输出的是概率、样本、分位、历史相似性，而不是一句“可以买”。
- 事件层不是普通特征，而是可能触发 `upgrade`、`veto`、`降权` 的优先级信号。
- AI 层负责解释和归因，不负责伪装成硬编码打分器。
- 规则闸门必须显式化，而不是分散在 prompt、工具、执行层的不同位置。
- 最终输出必须保留“原始结论 + 融合结论 + veto 原因 + 风险限制 + 数据来源”。

### 3.2 不建议直接照搬

- 不要把 `KNN`、向量距离、`prob_score` 设计成唯一核心。
- 不要把 `AI:Quant = 0.7:0.3` 写死为全局常量。
- 不要再造一个更复杂的单体 `should_i_buy` 打分器。
- 不要让 AI 绕过候选池直接在全市场自由发散。

---

## 4. 目标架构

## 4.1 总体思路

将决策链路重构为：

```text
市场/财务/新闻/公告/研报/用户画像/风险偏好
    ↓
build_stock_context
    ↓
build_quant_context + build_event_context
    ↓
ai_decision_layer
    ↓
rule_gate_layer
    ↓
decision_fusion_layer
    ↓
final_decision_payload
    ↓
BFF 决策卡片 / 前端统一决策页 / 执行建议 / 告警联动
```

## 4.2 分层定义

### A. `stock_context`

职责：

- 汇总股票“静态与动态基础信息”。
- 作为所有后续分析层的统一输入。

建议包含字段：

```json
{
  "code": "600519",
  "name": "贵州茅台",
  "market_snapshot": {},
  "technical_snapshot": {},
  "fundamental_snapshot": {},
  "valuation_snapshot": {},
  "fund_flow_snapshot": {},
  "text_signal_snapshot": {},
  "industry_chain_snapshot": {},
  "risk_profile": {},
  "user_profile": {},
  "data_provenance": [],
  "as_of": "2026-03-19"
}
```

### B. `quant_context`

职责：

- 输出可供 AI 推理的量化证据。
- 不直接输出最终买卖建议。

建议包含字段：

```json
{
  "factor_profile": {},
  "conditional_returns": {},
  "signal_hit_rate": {},
  "similar_patterns": {},
  "oos_validation": {},
  "probability_targets": {
    "1d": {},
    "3d": {},
    "5d": {},
    "10d": {},
    "20d": {}
  },
  "confidence_meta": {
    "sample_size": 0,
    "stability": "",
    "data_quality": ""
  }
}
```

### C. `event_context`

职责：

- 把新闻、公告、研报、产业链、资金流转换成高优先级事件证据。
- 输出 `upgrade`、`downgrade`、`veto` 候选。

首版边界：

- 文本类事件先只作为 `warning / downgrade` 依据。
- 只有结构化强事件才允许进入 `hard veto`，例如：
  - 重大处罚
  - ST/退市风险
  - 财报爆雷
  - 停牌或重大风险公告

建议包含字段：

```json
{
  "events": [],
  "event_score": 0,
  "event_direction": "positive",
  "event_intensity": 0.0,
  "event_horizon": "short_term",
  "event_level": "warning",
  "candidate_actions": ["upgrade"],
  "hard_veto_eligible": false,
  "veto_candidates": [],
  "evidence_links": []
}
```

### D. `ai_decision_layer`

职责：

- 读取 `stock_context`、`quant_context`、`event_context`。
- 产出原始解释、原始建议、原始信心，而不是直接完成全部流程。

输出示例：

```json
{
  "raw_action": "watch",
  "raw_confidence": 0.66,
  "raw_summary": "估值合理偏低，但短线事件驱动不足",
  "bull_points": [],
  "bear_points": [],
  "uncertainties": [],
  "recommended_horizon": "swing"
}
```

### E. `rule_gate_layer`

职责：

- 在 AI 输出前后统一做规则校验。
- 规则应从“隐式”改为“显式对象”。

分为三类：

- `user_gate`
  - KYC、风险等级、集中度、情绪偏差
- `market_gate`
  - 流动性、停牌、ST、极端波动、时段限制
- `event_gate`
  - 重大负面公告、监管处罚、财报爆雷、板块系统性风险

输出示例：

```json
{
  "blocked": false,
  "downgraded": true,
  "gate_flags": ["kyc_limit", "event_warning"],
  "veto_reason": [],
  "position_cap": 0.15
}
```

### F. `decision_fusion_layer`

职责：

- 融合 AI 原始判断、量化证据、事件证据、规则闸门，得到最终输出。

建议输出字段：

```json
{
  "final_action": "watch",
  "final_confidence": 0.61,
  "final_score": 63,
  "raw_ai_action": "buy",
  "raw_quant_bias": "neutral_to_positive",
  "raw_event_bias": "negative",
  "fusion_reasoning": [],
  "veto_applied": false,
  "position_signal": {
    "suggested_max_weight": 0.1,
    "entry_style": "staged",
    "stop_loss_hint": null
  }
}
```

### G. `mcp_output_contract`

职责：

- 将统一决策链的输出约束为稳定 schema。
- 减少 BFF 与前端的兜底适配成本。

建议要求：

- 所有统一决策相关工具必须定义明确输出模型。
- 输出字段应有稳定命名，不依赖运行时拼装。
- 所有关键节点至少包含：
  - `source_chain`
  - `updated_at`
  - `data_quality`
  - `fallback_reason`
  - `cached`

推荐实现方式：

- Python 侧使用 Pydantic 模型统一定义 schema。
- MCP 返回优先采用结构化输出，而不是文本 JSON 再解析。
- BFF 仅做字段映射，不做业务级猜测修复。

---

## 5. 与当前代码的映射关系

## 5.1 可直接复用的模块

### `stock_context` 复用来源

- 行情与 K 线：
  - `market/*`
- 基本面与估值：
  - `fundamental/*`
  - `valuation/*`
- 资金流：
  - `fund-flow/*`
- 产业链：
  - `get_industry_chain`
- 个股新闻/公告/研报：
  - `get_stock_news`
  - `get_stock_notices`
  - `get_research_reports`
- 综合投资画像底座：
  - `get_investment_analysis`

### `quant_context` 复用来源

- 因子画像：
  - `get_factor_profile`
- 条件收益分布：
  - `get_conditional_returns`
- 历史相似形态：
  - `find_similar_patterns`
  - `search_by_kline`
- 信号命中率：
  - `get_signal_hit_rate`
- 样本外验证：
  - `validate_factor_oos`

### `event_context` 复用来源

- 文本聚合信号：
  - `get_stock_text_signals`
- 市场新闻与事件：
  - `get_market_news`
  - `event_manager`
- 产业链映射：
  - `industry_chain_manager`

### `rule_gate` 复用来源

- 用户画像与情绪：
  - `user_manager`
  - `update_user_profile`
- 合规：
  - `compliance_manager`
- 执行闸门：
  - `execution_manager`
- 风险：
  - `risk_manager`
- 预警：
  - `alerts_manager`

## 5.2 需要弱化或重构的模块

- `packages/akshare-mcp/src/akshare_mcp/tools/decision.py`
  - 从“硬编码评分器”重构为“上下文驱动的决策工具”
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/decision_manager.py`
  - 从“综合打分 manager”重构为“统一融合 manager”
- `apps/bff/src/assistant/assistant.service.ts`
  - 从“适配单个工具输出”升级为“消费统一决策 payload”

## 5.3 重用边界原则

为了避免重复造轮子，需明确以下边界：

- `get_investment_analysis`
  - 作为 `stock_context` 的优先基础画像来源之一
- `smart_stock_diagnosis`
  - 逐步转为统一决策链的“轻解释入口”或兼容层
- `comprehensive_manager`
  - 不再继续扩张为新的总分入口，而是逐步下沉为素材聚合来源
- `should_i_buy`
  - 不再承担唯一主决策职责，后续退化为兼容模式或旧版对照入口

---

## 6. 建议新增的服务与接口

## 6.1 MCP 层新增服务

建议新增以下服务文件：

- `packages/akshare-mcp/src/akshare_mcp/services/decision_context_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_quant_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_event_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_rule_gate.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_fusion.py`

## 6.2 MCP 层新增工具

建议新增工具：

- `build_stock_context(code, as_of, profile)`
- `build_quant_context(code, horizon, profile)`
- `build_event_context(code, lookback_days, profile)`
- `run_decision_gate(payload, user_id, mode)`
- `fuse_decision_payload(stock_context, quant_context, event_context, gate_result)`
- `get_unified_decision_summary(code, user_id, horizon, explain)`
- `get_unified_decision_details(code, user_id, sections)`
- `get_unified_decision(code, user_id, horizon, explain, mode="summary")`

其中最重要的是：

- `get_unified_decision_summary`
  - 面向 BFF 与前端首屏的统一主入口
  - 仅返回摘要卡片、top reasons、gate flags、minimal provenance
- `get_unified_decision_details`
  - 按需返回量化证据、事件证据、闸门明细、原始上下文
- `get_unified_decision`
  - 作为兼容包装器保留，默认内部走 `summary` 模式

## 6.3 BFF 层新增接口

建议新增：

- `apps/bff/src/assistant/assistant-unified.service.ts`
- `POST /assistant/unified-decision`
- `POST /assistant/unified-decision/details`

返回统一 DTO：

```ts
type UnifiedDecisionCardDto = {
  action: string;
  confidence: number | null;
  finalScore: number | null;
  summary: string;
  reasons: string[];
  risks: string[];
  gateFlags: string[];
  vetoReason: string[];
  positionSignal: {
    suggestedMaxWeight?: number | null;
    entryStyle?: string | null;
    stopLossHint?: string | null;
  };
  dataProvenance: Array<string | { source?: string; dataset?: string; timestamp?: string }>;
  raw: Record<string, unknown>;
};
```

并补充请求侧字段：

```ts
type UnifiedDecisionRequestDto = {
  code: string;
  userId?: string | null;
  horizon?: 'short' | 'swing' | 'mid';
  explain?: boolean;
  mode?: 'summary' | 'details';
};
```

## 6.4 前端新增页面

建议新增或强化统一决策入口：

- `apps/web/app/assistant/page.tsx`
  - 增加“统一决策模式”
- 或新增：
  - `apps/web/app/decision/page.tsx`

页面应展示：

- 结论卡
- AI 解释
- 量化证据（折叠展开）
- 事件证据（折叠展开）
- 闸门结果
- 仓位建议
- 数据来源与时间戳

首版交互要求：

- 首屏只展示摘要层。
- 明细层点击后再拉详情接口。
- 避免把统一决策页做成一次性重型报告页。

---

## 7. 分阶段实施方案

## 7.1 Phase 0：统一协议定义

周期：2 至 3 天

目标：

- 先定义对象协议，再改业务逻辑。

交付物：

- `docs/plans/统一决策对象协议.md`
- `stock_context` / `quant_context` / `event_context` / `gate_result` / `final_decision_payload` JSON Schema
- `UnifiedDecisionSummary` / `UnifiedDecisionDetails` 输出 schema
- `assistant` 链路用户上下文透传协议
- 结构化输出契约说明（schema、source_chain、fallback_reason、cached）

完成标准：

- BFF、MCP、前端三方对字段语义达成一致。
- 明确哪些字段首屏返回，哪些字段按需展开。
- 明确 `user_gate` 在无用户上下文时的 fallback 规则。

## 7.2 Phase 1：上下文构建层

周期：5 至 7 天

目标：

- 做出 `build_stock_context`、`build_quant_context`、`build_event_context`。

关键动作：

- 复用现有工具，不推倒重来。
- 优先复用 `get_investment_analysis` 作为股票基础画像来源之一。
- 为所有字段增加 `source_chain`、`updated_at`、`data_quality`。
- 对 `quant_context` 增加样本数、历史分位、回看窗口说明。
- 对 `event_context` 区分 `warning` 与 `hard veto` 候选来源。

完成标准：

- 对任一股票都能稳定返回三类 context。
- 文本事件不会直接触发硬 veto，除非来源于结构化强事件。

## 7.3 Phase 2：闸门与融合层

周期：4 至 6 天

目标：

- 将 KYC、情绪、合规、事件 veto、仓位限制统一显式化。

关键动作：

- 统一 `rule_gate` 数据结构
- 将 `execution_manager`、`compliance_manager` 中可复用的规则抽出为决策前置闸门
- 为不同投资风格配置动态融合权重
- 将 `user_gate` 设计为可选增强层，避免因缺失用户上下文导致整体失败

建议初始策略：

- 保守型：规则闸门优先级最高
- 平衡型：量化与 AI 权重接近
- 激进型：AI 与事件驱动权重略高，但不能绕过合规和风险硬闸门

完成标准：

- 最终输出中能明确看到是否发生 `downgrade`、`veto`、`position_cap`。
- `hard veto` 仅由结构化强风险事件或合规/风控硬闸门触发。

## 7.4 Phase 3：BFF 与前端接入

周期：3 至 5 天

目标：

- 用统一 payload 驱动助手页和统一决策页。

关键动作：

- BFF 改为优先调用 `get_unified_decision_summary`
- 明细证据通过 `get_unified_decision_details` 按需展开
- 将现有 `DecisionCardDto` 升级为 `UnifiedDecisionCardDto`
- 前端增加“证据展开层”和“规则闸门层”
- 助手接口补充用户上下文读取与传递逻辑

完成标准：

- 用户能在一个页面看完整的“结论 + 原因 + 风险 + 数据来源 + 仓位建议”。
- 首屏响应控制在可接受范围内，明细按需加载。

## 7.5 Phase 4：替代旧逻辑与回归

周期：5 至 7 天

目标：

- 用统一主链逐步替代旧的分散入口。

优先替代顺序：

1. 助手页中的 `should_buy`
2. 部分 `smart_stock_diagnosis`
3. `decision_manager` 的直接总分输出

保留策略：

- 保留旧工具一段时间用于兼容与灰度对比
- 允许 `legacy_mode=true`

完成标准：

- 新旧入口可并行对比
- 能输出差异原因而不是只比较最终 action

## 7.6 Phase 5：测试、基线与灰度

周期：4 至 6 天

目标：

- 在不破坏现有入口的前提下验证统一主链质量。

关键动作：

- 新增 `UnifiedDecisionPayload` contract tests
- 新增 BFF 接口兼容测试
- 建立新旧入口 diff 报表
- 建立 MCP 调用延迟基线
- 建立前端首屏与展开明细耗时基线

建议测试类型：

- schema contract test
- snapshot test
- latency benchmark
- regression test
- old/new decision diff audit

完成标准：

- 字段稳定
- 性能可接受
- 新旧入口差异可解释
- 灰度期间无明显可用性回退

---

## 8. 推荐的技术落地原则

### 8.1 工具职责原则

- MCP 工具负责拿数据、算统计、产证据。
- AI 负责解释、归因、语言组织。
- Rule Gate 负责边界控制。
- Fusion 层负责最终决策。

### 8.2 置信度原则

- 置信度不等于分数。
- 置信度至少应受以下因素影响：
  - 样本数
  - 数据完整度
  - 多证据一致性
  - 事件冲突程度
  - OOS 稳定性

### 8.3 数据可追溯原则

- 每个关键结论都要能追溯：
  - 来源工具
  - 数据时间
  - 降级链
  - 计算窗口
  - 是否缓存
  - 是摘要结论还是明细证据

### 8.4 风险优先原则

- 风险闸门优先级必须高于推荐强度。
- 事件负面 veto 应能覆盖量化正向。
- 合规硬闸门应能覆盖 AI 与量化结论。
- 关键词文本信号不能直接作为硬 veto 唯一依据。

---

## 9. 关键验收指标

## 9.1 产品指标

- 统一决策页首屏返回时间可控
- 用户在单页完成决策浏览，不再需要跨多个页面拼信息
- 每次建议都有来源和风险解释

## 9.2 技术指标

- `get_unified_decision` 成功率 > 95%
- 所有 context 均带 `source_chain`
- 决策 payload 字段稳定，不因单一数据源失败而整体崩溃
- 发生降级时，明确输出 `fallback_reason`

## 9.3 研究指标

- 量化证据不再只有单点值
- 事件证据能进入最终融合
- AI 输出不再完全依赖硬编码打分器

---

## 10. 风险与注意事项

### 10.1 主要风险

- 统一上下文对象过大，导致响应变慢
- 旧工具字段风格不统一，适配成本高
- 事件抽取如果仍停留在关键词级别，容易产生误判
- 前端如果一次性展示过多字段，用户会觉得复杂

### 10.2 控制方案

- 分层懒加载：首屏只返回摘要，展开后再拉明细
- schema 优先：先统一字段，再逐步兼容旧逻辑
- 事件层先做“结构化增强”，再考虑更复杂语义模型
- 前端采用“结论层 / 证据层 / 闸门层 / 原始层”四段式展示
- 用户上下文缺失时必须有 fallback，不得导致主链失败

---

## 11. 最终建议

本次优化不应被定义为“再加几个分析工具”，而应定义为：

**把 AIASK 从能力集合，升级为统一决策产品。**

最优落地路线不是大规模重写，而是：

1. 先定义统一决策对象协议
2. 先补 MCP 结构化输出契约与用户上下文透传
3. 再把现有工具装配成 `stock_context`、`quant_context`、`event_context`
4. 再抽出 `rule_gate`
5. 最后形成 `summary + details` 版统一决策入口

这样做的收益最大：

- 与现有能力兼容
- 能快速做出首版
- 可逐步替代旧逻辑
- 最符合项目当前架构现实

---

## 12. 建议的下一步动作

如果按执行优先级排序，建议立刻开始以下 3 件事：

1. 在 `packages/akshare-mcp` 中定义统一决策对象协议、Pydantic 输出模型和 builder 服务草稿。
2. 在 `apps/bff` 中补用户上下文透传，并新增 `summary + details` 统一决策接口。
3. 在前端助手页增加统一决策结果卡，但首版只展示摘要层，明细按需展开。

这三步完成后，项目就会从“多页面、多工具、多结论入口”开始转向“单一主链、可解释、可审计”的成熟形态。
