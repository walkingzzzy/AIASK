# 策略工厂实际架构深度审查

日期：2026-06-02
版本：ARCH-MAP-V2（深度代码审查版）
目的：基于**逐函数代码审查**，钉死策略工厂的真实运行架构，把"漏斗设计"的架构缺陷定位到**具体文件、行号、代码片段**，为"倒置架构"设计提供事实地基。

> 关联：`策略工厂零产出根因修复方案-2026-06-02.md`（技术 bug 层）/ `策略工厂倒置架构设计方案-2026-06-02.md`（设计层）。
> 本文档处理**架构/设计哲学层**——比技术 bug 严重，因为它是"设计本身就拧反了"。

---

## 0. 审查范围与方法

审查方式：直接读源码，每个论断都附**文件路径 + 行号 + 代码片段**。覆盖的核心文件：

| 模块 | 文件 |
|---|---|
| 候选生成 | `strategy-factory/.../domain/spawner_parts/{serialization,factories,part_5}.py` |
| 漏斗 Gate-1/2 | `strategy-factory/.../application/quality_gates_parts/{reporting,policy}.py` |
| 漏斗 Gate-3 | `strategy-factory/.../application/submission_gate/runner_parts/multiple_testing.py` |
| 验证引擎 | `aiask-quant-core/.../validation.py` |
| 状态机/准入 | `strategy-factory/.../application/services/{lifecycle_coordinator,admission_authority}.py` |
| 孵化接纳 | `akshare-mcp/.../incubation_factory/{intake,runner}.py` |
| 前向测量 | `akshare-mcp/.../incubation_factory/{forward_verifier,hit_rate_reporter}.py` |

---

## 1. 真实数据流全景（逐段对到代码 + 实测数字）

```
┌──────────────── 策略工厂（生产 + 漏斗）────────────────┐
│ [生成] StrategySpawner.spawn        ~28-31 候选/cycle  │
│        spawner_parts/serialization.py _from_* + _fill_gaps │
│   ↓                                                    │
│ [Gate-0] 结构校验      quality_gates       28→28        │
│   ↓                                                    │
│ [Pre-Gate] 便宜过滤    quality_gates       28→14        │
│   ↓                                                    │
│ [Gate-1] 3股快筛       reporting.py:514    14→3         │
│   ↓                                                    │
│ [top-k] 比例选择       policy.py:396       3→1   ★瓶颈  │
│   ↓                                                    │
│ [Gate-2] 完整回测      backtest_filter     1→1          │
│   ↓                                                    │
│ [dedup] 去重           deduplicator        1→0   ★瓶颈  │
│   ↓                                                    │
│ [Gate-3] 统计验证      multiple_testing.py 要求全达标 ★★★│
│   ↓                                                    │
│ [状态机] lane→status   lifecycle_coordinator.py:606    │
│          只有 formal_incubation → status='incubating'  │
└────────────────────────────────────────────────────────┘
                          ↓ （5 轮实测：0 个到达这里）
┌──────────────── 孵化工厂（接纳 + 前向测量）─────────────┐
│ [intake] scan_and_accept   intake.py:185               │
│          只扫 list_strategies("incubating")            │
│   ↓                                                    │
│ [Phase 3] ForwardVerifier.verify  forward_verifier.py:70│
│           ← 真实命中率/前向IC/Sharpe 在这里才第一次被算 │
└────────────────────────────────────────────────────────┘
```

5 轮真实 cycle 实测漏斗（见零产出修复方案 §13）：`spawned 26-31 → Gate-1 1-3 → Gate-2 1 → dedup 0-1 → gate_3_input 0-1 → incubating 0`。**6 个月零策略进孵化，根因是这条 9 关串联链路任意一关归零即全链归零。**

---

## 2. 原罪 1 —— 证明先于观察（逻辑倒置）【最严重】

### 2.1 代码证据：incubating 状态只由 formal_incubation lane 设置

`application/services/lifecycle_coordinator.py` 第 605-609 行：

```python
}.get(lane, "incubation_budget_deferred_queue")
if lane == "formal_incubation":
    result.final_status = "incubating"
    step, _ = await self._step("status_transition", _status_update("incubating", queue_reason))
    ...
    incubation_gateway.ensure_account(db, enriched_data, ...)   # 绑定孵化账户
```

而 lane 是怎么来的？`admission_authority.py::SubmissionAdmissionAuthority.resolve` —— 只有当 `formal_track_eligible=True`（要求 `runtime_bootstrap_eligible` + `strict_incubation_ready` + `semantic_runtime_match` 等全部成立）时才走 `formal_incubation`。其余全部落到 `observe_incubation` / `deferred_submission` / `rejected`。

### 2.2 代码证据：孵化接纳只认 incubating

`incubation_factory/intake.py` 第 183-188 行：

```python
async def _list_incubating_strategies(self, db: Any) -> list[dict[str, Any]]:
    """加载所有 incubating 状态的策略。"""
    if hasattr(db, "list_strategies"):
        return await db.list_strategies("incubating", limit=500)
    return []
```

`scan_and_accept`（intake.py:18）的 docstring 自己写明准入条件：
```
1. status = 'incubating'（策略工厂 Gate-3 通过后设置）
```

### 2.3 倒置的本质

把 2.1 + 2.2 串起来：**一个策略要进入孵化（被建账户、被 ForwardVerifier 测量），前提是它已经过了 Gate-3 的全套回测盈利证明。**

但孵化（`ForwardVerifier.verify`）才是**产生**"这策略到底行不行"证据的地方。所以设计是循环的：

> **要进入"用来发现策略行不行的实验"，前提是先证明"策略行"。**
> 如果已经能证明它行，就不需要做这个实验了。

这等价于"必须先证明你会游泳，才允许你下水学游泳"。这是**自我击溃的架构**，不是参数问题。

---

## 3. 原罪 2 —— 无视市场状态的时间平均（regime-blind）

### 3.1 代码证据：120 期被压成单一平均 IC

`aiask-quant-core/validation.py` `WalkForwardValidator._build_summary`（约第 194-239 行）把每个 fold 的 OOS IC **取均值**：

```python
oos_ics = [r.test_ic for r in results]
oos_mean = float(np.mean(oos_ics))
oos_std = float(np.std(oos_ics)) if len(oos_ics) > 1 else 0.0
oos_ir = oos_mean / oos_std if oos_std > 0 else 0.0
...
return ValidationSummary(n_folds=len(results), oos_ic_mean=oos_mean, oos_ic_ir=oos_ir, ...)
```

整个评价塌缩成 `oos_ic_mean` / `oos_ic_ir` 两个标量。**没有任何一处按市场状态（趋势/震荡、波动率档、恐贪）分层。**

### 3.2 代码证据：门槛是「分级静态常量」，按准入等级分档，但没有一档随市场状态变化

`domain/constants.py` 里阈值其实是**按准入等级分三档**的（research / incubation / live），不是单一死常量。以 `statistical_validation` 为例：

```python
# RESEARCH_ADMISSION_THRESHOLDS（research 档）
"statistical_validation": { "walk_forward_ic_ir_min": 0.20, "purged_kfold_ic_min": 0.01, "bootstrap_ci_lower_min": -0.01 }
# INCUBATION_ADMISSION_THRESHOLDS（incubation 档）
"statistical_validation": { "walk_forward_ic_ir_min": 0.30, "purged_kfold_ic_min": 0.02, "bootstrap_ci_lower_min":  0.00 }
# LIVE_ADMISSION_THRESHOLDS（live 档）
"statistical_validation": { "walk_forward_ic_ir_min": 0.45, "purged_kfold_ic_min": 0.04, "bootstrap_ci_lower_min":  0.02 }
# 另有 GATE1_SHARPE_MIN = 0.15（Gate-1 快筛）
```

**关键不在于"单一 vs 分档"，而在于：这三档全是按「准入等级」切的静态常量，没有任何一档随「市场状态（regime）」变化。** 同一条 momentum 策略，无论当前是大牛市趋势还是震荡市，在 incubation 档下都用同一个 `wf_ic_ir≥0.30` 卡——分级解决的是"研究/孵化/实盘宽严不同",解决不了"趋势市/震荡市该用不同标准"。原罪 2 因此依然成立:**评价维度里完全没有 regime 这一轴**。

### 3.3 后果：条件型 edge 被平均成噪声后误杀

实测证据（零产出方案 §2.2）：value_factor 样本内 Sharpe 1.46，但**平均** OOS IC = -0.089 被拒。momentum 在趋势市有效、震荡市失效；把两段行情平均到一起 → IC≈0 的噪声 → 门槛毙掉。**真实世界几乎所有 edge 都是状态条件型的**，而这套设计在数学上系统性地消灭它们。用户说的"每天股市情况都不一样却用死板固定逻辑"——这就是代码层的实锤。

---

## 4. 原罪 3 —— 盈利断头台代替准确率测量

### 4.1 代码证据：准入是二元盈利 pass/fail

`submission_gate/runner_parts/multiple_testing.py::_evaluate_trade_profile`（第 368-560 行）逐条把指标和阈值比，任一不达标就 `reasons.append(...)`，最后：

```python
"passed": len(reasons) == 0,
"passed_strict": len(reasons) == 0,
```

被检查的全是**盈利/风险类**硬指标，例如第 533-545 行的命中率/盈亏比：

```python
if strategy_type in {"momentum", "ma_cross", "volatility_breakout", "breakout"}:
    _min_wr, _min_pf = 0.40, 1.8
...
if win_rate > 0 and win_rate < _min_wr:
    reasons.append(f"win_rate {win_rate:.3f} < {_min_wr:.3f}")
if profit_factor > 0 and profit_factor < _min_pf:
    reasons.append(f"profit_factor {profit_factor:.3f} < {_min_pf:.3f}")
```

注意：这些 `win_rate`/`profit_factor` 全是**回测内**算出来的，不是向前实测的。准入阶段**从不问**："这策略向前预测涨跌的命中率多少？预测幅度对不对？回测 vs 实盘差多少？"

### 4.2 它只问"平均赚不赚钱"，然后一刀切

`_evaluate_trade_profile` 检查的 12 项（post_cost_sharpe / total_return / target_layer_oos_return / max_drawdown / trade_density / parameter_stability / win_rate / profit_factor / expectancy ...）全是"这段回测平均下来的盈利画像"。它把一个**假设**当成**产品**来验收。

---

## 5. 关键发现：用户想要的"命中率测量"系统里已经实现，但被漏斗饿死

### 5.1 ForwardVerifier 就是用户想要的东西

`incubation_factory/forward_verifier.py::verify`（第 70-180 行）逐信号算**方向命中**（第 108-115 行）：

```python
primary_return = self._extract_forward_return(forward_returns, primary_horizon)
if primary_return is not None:
    hit = (primary_return > 0 and direction > 0) or (primary_return < 0 and direction < 0)
    primary_hits.append(1.0 if hit else 0.0)
    primary_directions.append(float(direction))
    primary_raw_returns.append(float(primary_return))
```

返回的画像（第 153-180 行）正是用户要的"命中率/涨跌/前向表现"：

```python
"primary_hit_rate": ...,          # 方向命中率
"recent_primary_hit_rate": ...,   # 近 20 信号命中率
"primary_skill_lcb": ...,         # 命中率 95% 置信下界（防小样本运气）
"forward_ic": ...,                # 前向 IC
"forward_sharpe": ...,            # 前向 Sharpe
"stability_gap": ...,             # 近期 vs 整体稳定性
```

它甚至按策略频率分了孵化档（`INCUBATION_PROFILES`，第 22-50 行：high/medium/low_frequency，各自不同的 min_days / min_trades / horizon）。`_compute_skill_lcb`（第 220-233 行）用 Wilson 下界减 0.5 基线——**这是统计上正确的"AI 有没有 skill"度量**，远比回测 win_rate 诚实。

### 5.2 但它只在 incubating 之后才跑

`incubation_factory/runner.py::run_once` Phase 2（第 250-272 行）：

```python
incubating = await self._list_incubating(db)                       # 主路径
paper_observation = await self._list_paper_observation(db)         # toggle 旁路
diagnostic_observation = await self._list_diagnostic_observation(db) # toggle 旁路
all_strategies = list(incubating) + list(paper_observation) + list(diagnostic_observation)
```

`_list_paper_observation`（runner.py:550-577）和 `_list_diagnostic_observation`（runner.py:579-602）都**默认 toggle OFF 即返回空**：

```python
if not paper_intake_enabled():       # 默认 False
    return []
```

### 5.3 结论：正确的零件，接在了错误的位置

测量层（ForwardVerifier / HitRateReporter / SignalGenerator）**造对了**，但它被放在 Gate-3 **下游**，只消费 `incubating` 策略。而漏斗产不出 `incubating`（5 轮 = 0），所以测量层**永远没有输入**。

DB 佐证：`strategy_signals` 113 条、`strategy_signal_event_snapshots` 193 条、`strategy_signal_evidence` **0 条**（ForwardVerifier 的输入表是空的）。**测量引擎装好了，但油箱是空的。**

---

## 6. 原罪溯源：alpha 源本身是"随机抖动的通用 TA"

### 6.1 代码证据：参数靠 random jitter 生成

`domain/spawner_parts/serialization.py` 第 559-567 行：

```python
@staticmethod
def _jitter(base: int, lo: int, hi: int) -> int:
    delta = max(1, int(base * 0.2))
    return max(lo, min(hi, base + random.randint(-delta, delta)))

@staticmethod
def _jitter_f(base: float, lo: float, hi: float) -> float:
    delta = max(0.01, base * 0.15)
    return round(max(lo, min(hi, base + random.uniform(-delta, delta))), 2)
```

候选生成 = 在 momentum/ma_cross/value_factor 等通用族的基准参数上做 ±15-20% 随机抖动。`_fill_gaps`（serialization.py:389）+ `_preferred_fill_types`（factories.py:296）在配额不足时用这些通用族补齐。

### 6.2 这意味着什么

工厂每轮生成的 ~30 个候选，本质是**全市场最被研究、最被套利、edge 最接近零的通用技术因子的随机调参版**。这不是 bug，但它决定了：即使漏斗完全修好、测量层喂饱，产出真 edge 的基础命中率本就只有 1-5%（学界共识，见零产出方案 §10.1）。**这是 alpha 源层问题，不是架构能解决的。**

---

## 7. 为什么会被设计成漏斗（动机溯源，非辩护）

漏斗是一个**反过拟合免疫系统**。Gate-3 的 deflated Sharpe / PBO / reality check（`multiple_testing.py` 的 live 分支 + `_live_multiple_testing_reasons`）是为对抗回测过拟合而生——过拟合是真问题（Hou-Xue-Zhang：65% 已发表因子不可复现）。

**但它犯了范畴错误**：把"能不能投真实资本"的高门槛，错用到了"值不值得观察一下"的门上。

| 门 | 该有的严格度 | 当前实际 |
|---|---|---|
| 投真实资本？ | 严格证明 ✅ | 严格 |
| 值不值得 paper 观察学习？ | 又便宜又宽松 | **同样严格（用资本级门槛）** ❌ |

它把资本级门槛装在观察级门口，使"孵化"被默认成昂贵的事，所以必须层层把守。**这一个范畴错误衍生出全部三宗原罪。**

而团队其实隐约知道哪里不对——他们加了 `observe_incubation`/`diagnostic_observation` lane、paper 账户、`_list_paper_observation`。**正确的本能（观察层）造出来了，却被从属于错误的主干（盈利断头台），变成默认关闭的旁路 toggle。**

---

## 8. 认知层判决：当前系统"认知上是死的"

- 跑 6 个月、5 轮真实 cycle，**产生零知识**：`strategy_signal_evidence` 表 0 行，没有任何一条向前命中率数据被记录。
- 它的拒绝**不产生信息**：基于 regime-blind 的平均回测统计杀策略，`success_no_strategy` 是它唯一会说的话。
- 它既产不出策略，连"为什么 alpha 源不行"都回答不了——因为没有任何策略走到能被前向测量的地方。

健康系统应"认知上活"：每个流过的策略产生带状态标签的前向准确率数据点，累积成"什么策略在什么时候有效"的知识。当前架构在**结构上禁止**这种知识产生。

---

## 9. 分层判决：架构问题 vs 根本不可行

| 层 | 结论 | 可修性 |
|---|---|---|
| 漏斗设计（原罪 1/2/3） | 架构问题（倒置 + regime-blind + 盈利断头台） | **可重构**。真实孵化农场就是反着做的。 |
| alpha 源（原罪溯源，§6） | 随机抖动通用 TA，命中率本就 1-5% | **不可靠**。重构不会凭空造 edge。 |
| 重构的真正价值 | 让系统从"认知死"变"认知活" | 终于能**诚实、快速、连续地测出** AI 策略有没有预测力——这是当前设计结构上禁止回答的问题。 |

**重构不保证赚钱，但它保证你能用最低成本拿到"alpha 源到底行不行"的诚实判决**——而这正是过去 6 个月被结构性掩盖的那个答案。

---

## 10. 下一步

本文档（ARCH-MAP-V2）钉死现状与三宗原罪的代码位置。
配套设计文档 `策略工厂倒置架构设计方案-2026-06-02.md` 给出"观察先行、向前测命中率、现实选择、资本门后置"的可实施规格，复用本文档 §5 已证实存在的 ForwardVerifier/HitRateReporter/paper 零件。


---

## 11. 实测漏斗证据（2026-06-03 跑通真实 DB 追加，校正前序假设）

> 本节用 `data/db/akshare_mcp.sqlite3` 最近 6 次真实 cycle（run id 721–726）的 `strategy_factory_runs.summary/stages` 实测数据，钉死候选实际死在哪一环。**结论校正了前序会话"LLM 降级是压垮候选基数的主因"这一假设——该假设被实验证伪。**

### 11.1 最近 60 次 run 状态分布

```
partial_llm:          45
success_no_strategy:  15
（含策略进孵化的:        0）
strategy_signal_evidence 表:  0 行（与 §8 一致）
```

### 11.2 单 cycle 漏斗（run 726 partial_llm vs run 725 LLM 全关，funnel 完全一致）

| 环节 | run 726（LLM 4 次请求） | run 725（LLM 0 次请求） |
|---|---|---|
| spawned | 24 | 27 |
| gate_0_passed | 24 | 27 |
| pre_gate_passed | 12 | 13 |
| **gate_1_passed** | **1**（fail 11） | **1**（fail 12） |
| gate_2_passed | 1 | 1 |
| backtest_passed | 1 | 1 |
| **after_dedup** | **0** | 0（部分 cycle=1） |
| **gate_3_input** | **0** | 0 |
| gate_3_passed | 0 | 0 |

**决定性事实**：run 725 的 `external_llm_network_request_count=0`（LLM 完全没参与），漏斗与开了 LLM 的 run 726 **逐位一致**。⇒ **LLM 降级不是压垮候选基数的原因**。到达 gate_1 的真实候选基数（≈12）与 LLM 在不在场无关。前序"根因 C / LLM cooldown 级联"是错误的发力点，已撤回。

### 11.3 真正的多重墙（按约束强度排序，全部有 stages 实证）

**墙 1 — spawn 实际只产 ~1 个真候选/cycle**
`spawn.summary`：`candidate_count=1`、`source_counts={quota_fill:1}`、`quota_fill_mode_counts={no_signal_fallback:1}`、`parameter_source_counts={fixed_defaults:1}`。
即：那条唯一进入验证的候选是"无信号兜底配额、固定默认参数"的填充物——edge 最低的那类（印证用户 query 23 根因 1a「没有真实 alpha 来源」）。"spawned 24-27"里其余是 bulk_stock_matrix 的 TA 变体，几乎全在 gate_1 被杀。

**墙 2 — gate_1 杀 11–12 / 12**
bulk-matrix 通用 TA 变体没有 edge（因子动物园）。与 LLM 无关（§11.2 已证）。

**墙 3 — 主 Deduplicator 杀掉唯一存活者（约 80% cycle）**
`deduplicate.summary`（run 726）：`existing_count=217`、`existing_scan_count=39`、`coarse_hit_ratio=1.0`、`kept_count=0`、`dropped_count=1`、`param_threshold=0.85`、`vector_threshold=0.93`、`refresh_mode=refresh_metrics_only`。
即：唯一存活的固定默认填充物，与已有 217 条策略（含工厂自己历次留下的 audit-only 填充物）高度重合，被判为"刷新复用"而 `kept_count=0`。只有当 `existing_scan_count=0`（run 723）时才 `kept_count=1`。
**该阈值此前是 `normalizers.py` 里的硬编码类属性，无 env override**（已在 2026-06-03 改为可配置，见下）。

**墙 4 — 即便存活，gate_3 把它路由到 research_only / audit_only，而非 incubating**
`submit`（run 723，唯一 `gate_3_input=1` 的样本）：`research_only_count=1`、`created_audit_only=1`、`formal_incubation_count=0`、`observe_incubation_count=0`、`diagnostic_observation_count=0`、`gate_3_passed=0`。
即：固定默认填充物即使过了 dedup，也不会进孵化——它被记成审计/研究条目就结束了。而能把"次正式候选"放进可观察状态的三条宽进通道全部 toggle 默认 OFF：
- `observe_d_grade_enabled()` = False
- `diagnostic_observation_enabled()` = False
- `paper_intake_enabled()` = False

### 11.4 与 §7「原罪 1 倒置」的闭环

§7 说"观察层造好了却被默认关闭"。§11.4 用实测确认了它的后果链：**墙 3（无 env 的硬 dedup）+ 墙 4（宽进通道默认 OFF）共同保证：即使偶有候选走到底，也无法进入任何可前向测量的状态**，于是 `strategy_signal_evidence` 永远 0 行。这不是 LLM、不是 top-k、不是 DSL 编译——是 admission 结构本身把一切挡在 incubating 之外。

### 11.5 已落地的过渡期可配置化（2026-06-03，默认零行为变化）

| 改动 | 文件 | 默认值 | 作用 |
|---|---|---|---|
| `GATE2_TOPK_FLOOR` / `GATE2_TOPK_USE_CEIL` | `domain/constants.py` + `policy.py` | floor=1 / ceil=False（逐位等价旧逻辑） | 抬高小样本下 Gate-2 取数下限，并 clamp 不超候选池 |
| DSL import 修复 | `strategy_spec/dsl_builder.py` | — | `.strategy_dsl`→`..strategy_dsl`，修掉每次单标的趋势 DSL 编译的静默 ModuleNotFound |
| `STRATEGY_FACTORY_DEDUP_PARAM_THRESHOLD` / `..._VECTOR_THRESHOLD` | `deduplicator_parts/normalizers.py` | 0.85 / 0.93（不变） | 过渡期可放松 dedup（抬高阈值=只折叠近乎完全相同的策略），解墙 3 |

> **诚实声明**：上述代码改动单元级已验证（DSL 编译 `dsl_compiled=True` 无 ModuleNotFound；top-k clamp 不溢出且默认逐位等价；dedup 阈值默认 0.85/0.93、env 0.97/0.99 生效）。但它们都是**上游修补**，单凭它们**无法**让策略进 incubating——真正的墙是 §11.3 的墙 3+4。要让"第一条策略进 incubating + `strategy_signal_evidence` 出现非 0 行"，必须叠加"放松 dedup（env）+ 打开 observe 通道（toggle）"，或按倒置架构方案 §改动 A/F 把宽进改成主干。端到端验证仍在进行中，未达成前不宣称成功。


---

## 12. P1 端到端打通后的两个剩余阻断点（2026-06-03 实测,精确定位）

> P1 改动A(宽进准入)+ 改动D(regime 标签)落地后,**第一条策略走通漏斗**(run 729:gate_3_passed=1、created=1、进 paper 账户)。但 `strategy_signal_evidence` 仍 0 行。本节用实测把"为什么 0 行"拆成两个**机械阻断点**,并指出系统其实已有绕过 evidence_chain 的代理路径。

### 12.1 阻断点 1 — 前向收益恒 0:K线数据新鲜度,不是代码 bug

`SignalTracker.backfill_forward_returns` 实测:`5D pending_seen=113, computed=0`。深挖 `_compute_forward_returns_batch`(signal_tracker_parts/specs.py:542)逻辑**正确**——它按 `signal_date` 在 `kline_1d` 收盘序列里定位,取 +N 根之后的收盘算 `(future-base)/base`。computed=0 的真因是数据:

| 事实 | 实测值 |
|---|---|
| 信号日期 | 2026-05-28(113 条)/ 2026-06-03(141 条) |
| 信号标的 | 688411/688472/688778/601601/...(共 7 个 code) |
| `kline_1d` 中这些 code 的最新 K 线 | **688xxx 止于 2026-05-20**,大盘股止于 06-02 |
| 后果 | signal_date(05-28)在 K 线序列(止于 05-20)里**根本不存在** → `index_by_date.get(signal_date)=None` → 跳过 |

⇒ **前向收益引擎是对的,但喂给它的 K 线数据比信号日期还旧**。修复是数据运维(`sync_stale_klines` 把覆盖延伸到 signal_date + N 个交易日),不是改测量代码。叠加 T+N 固有延迟:今天的信号本就要等 N 个交易日才有真实前向收益。

### 12.2 阻断点 2 — 无 evidence 行:缺的是 paper 订单,不只是 evidence_chain

实测新策略 `factory_1780453180_64bbe057`:`strategy_signals=3` 但 `paper_orders=0 / paper_trades=0`。`backfill_strategy_signal_evidence_native` 返回 `status=empty`——因为它从 **paper 订单/成交**反向合成 evidence,没有订单就没有输入。

**关键发现(修正用户的"缺 evidence_chain"判断)**:系统其实已有一条**不依赖 evidence_chain 的代理证据路径**——`backfill_strategy_signal_evidence_native`(queries.py:164)在 `build_signal_evidence_records` 返回空时,会用 `paper_execution_backfill` 模式从 paper 执行合成 `proxy_only=True` 的 evidence。所以 fixed_defaults 填充物**能**产出 evidence,前提是它先有 paper 订单。

paper 订单为何没生成?`sync_signals_to_orders`(incubation_parts/specs.py:2)有 `allowed_codes = _resolve_strategy_target_codes(strategy)` 过滤:只有信号 code 落在策略目标池内才转订单。填充物的目标池与当日生成的信号 code(688xxx)不匹配 → 信号被过滤 → 0 订单 → 0 代理 evidence。

### 12.3 两个阻断点的修复方向(诚实区分:已验证机制 vs 数据/行为依赖)

| 阻断点 | 性质 | 修复 | 可否纯代码解决 |
|---|---|---|---|
| 1. 前向收益恒 0 | 数据新鲜度 + T+N 延迟 | `sync_stale_klines` 把 K 线覆盖延伸过 signal_date+N;等 N 个交易日 | 否(无未来价格,改代码也变不出收益) |
| 2. 无 evidence 行 | 信号→订单转化被目标池过滤 | 让 observe 策略的信号→订单不被目标池硬过滤(或对 observe lane 放宽),触发已存在的 native 代理 evidence 回填 | 部分(放宽过滤是代码;但仍需有执行才有 evidence) |

**结论**:P1 在架构层成功(策略流动 + 准入倒置 + regime 测量能力就绪);测量管道(ForwardVerifier/native 代理回填/前向收益引擎)经代码审查**全部正确**。剩余两个阻断点都是"数据/执行喂给正确管道"的问题,不是管道本身的 bug。这正是倒置架构的价值兑现:把"为什么没产出"从模糊的"管道有问题"精确收敛到两个可操作的喂数据动作。
