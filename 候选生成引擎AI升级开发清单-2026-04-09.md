# 候选生成引擎 AI 升级开发清单

文档日期：2026-04-09

关联方案：
- `候选生成引擎AI升级方案-2026-04-08.md`
- `策略工厂架构优化方案-2026-04-08.md`

目标：把“候选生成引擎 AI 升级方案”拆成可直接实施、可验收、可排期的开发清单。

说明（2026-04-11 代码核对修订）：
- 本文中的 `[x]` 优先表示代码、接口、页面或测试链路已经存在。
- 本文中的“连续 3 次真实 run”“质量回归”“forward coverage 提升”“promotion-ready rate 高于规则基线”等条目，仍属于运行验收项，只有在默认真实 run 稳定满足时才应勾选。
- 本次修订特别更正了测试清单里若干已存在但未勾选的项目。

---

## 一、总目标

- [x] 把候选生成从“规则模板 + 变体扩展 + 受控 DSL 填空”升级为“研究假设生成 -> 候选编译 -> 回测/门禁淘汰”双阶段链路
- [x] 把 feedback 从“预算节流器”升级为“搜索路由器”
- [x] 把 Gate-3 从“前松后硬且统计口径偏宽”升级为“分阶段 + cohort 对齐”的统计门
- [x] 把策略对象从“协议完整”升级为“经济完整”

补充说明（截至 2026-04-11）：

上面的开发主链已经基本落地，但候选生成升级方案还不能算“结果上完全完成”。当前仍有六类现实问题需要继续跟踪：

1. 默认工厂 steady-state 仍会被 readiness/governed pool 挡住，最新默认 run `factory_run_1775874046_da5aad36` 为 `skipped`。
2. 当前真实 cohort 仍以 `raw D` 为主，`raw_validation_b_rate=0.04`，`raw_b_or_above_rate=0.055`，说明候选质量右移还不够深。
3. `momentum / ma_cross / quality_factor` 的提升不均衡，`quality_factor` 主桶仍未稳定进入 `raw B`。
4. `strict_incubation_ready_rate=0.195` 已有改善，但 `live_candidate_ready_rate=0.0075` 仍然过低，说明生成链路已能产出更好候选，但 live 质量还未稳定兑现。
5. 外部 LLM 虽已接入主链并具备 fallback，但真实 run 中仍观察到 provider 波动与 `HTTP503`，外部探索率还不够稳定。
6. 研究主链的物理复杂度还偏高，`factor_research_builder.py` 仍接近 `2000` 行，后续继续改生成/反馈/quality 时的回归风险仍在。

---

## 二、P0：基线与观测补齐

### P0.1 真实运行观测补齐

- [x] 在 `factory_run_detail` / `factory_status` 中固定输出以下字段：
  - `external_llm_provider_health_status`
  - `external_llm_provider_control_mode`
  - `candidate_local_attempt_count`
  - `task_local_attempt_count`
  - `cohort_effective_trials`
  - `refresh_existing_count`
  - `spawn_revision_from_existing_count`
  - `unique_family_holding_universe_count`
  - `economic_semantics_missing_count`
- [x] 在 run summary 中增加 `research_only_count`、`deferred_submission_count`、`validation_grade_distribution`
- [x] 在 research artifact 中增加 `family_reward_table` / `family_debt_table` / `search_route_actions`

涉及路径：
- `packages/strategy-factory/src/strategy_factory/application/cycle_runner.py`
- `packages/strategy-factory/src/strategy_factory/application/services/readiness_service.py`
- `packages/strategy-factory/src/strategy_factory/application/research_plane_contract.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_mgr_lifecycle.py`

验收：
- [x] `factory_status`
- [x] `factory_runs`
- [x] `factory_run_detail`
  三个出口都能看到新增字段，且无空壳字段

### P0.2 当前质量基线固化

- [x] 固化最近真实 run 的质量基线快照
- [x] 固化当前提交策略的 `validation_grade`、`zero-signal`、`forward coverage`、`promotion-ready rate`
- [x] 形成 L1/L2/L3 风格对照基线，避免后续“候选数增长”被误判为质量提升
  - 当前通过 `generation_lane_quality_panel` 暴露规则层 / 历史引导 / L2 hypothesis / L3 open DSL 的对照面板
  - 若 submitted 策略的持久化载荷未保留 `historical_guided` 痕迹，cohort 口径下的 L1 会并入 L0，这属于证据粒度限制而不是功能缺失

涉及路径：
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_mgr_lifecycle.py`

验收：
- [x] 能明确拿到“升级前基线”并与升级后按同口径对比

---

## 三、L1：历史反馈引导参数采样

### L1.1 ParameterDistributionRegistry

- [x] 新增 `ParameterDistributionRegistry`
- [x] 支持按 `strategy_type` 提取历史高表现参数分布
- [x] 只允许使用 OOS 通过样本构建分布
- [x] 样本不足时自动 fallback 到现有固定参数列表
- [x] 加入稳定性约束，避免采样围绕噪声锚点震荡

涉及路径：
- `packages/strategy-factory/src/strategy_factory/domain/spawner.py`
- 新增：`packages/strategy-factory/src/strategy_factory/domain/parameter_distribution_registry.py`

验收：
- [x] `_varied_defaults()` 可以走历史分布采样
- [x] 样本不足时行为与现状兼容
- [x] 有历史样本的类型，参数不再高度集中在固定锚点

### L1.2 配额补位修正

- [x] `_fill_gaps()` 第一阶段接入历史参数分布采样
- [x] 去掉纯无信号补位对统计的污染
- [x] 在 run summary 中记录 quota fill 来源与质量

涉及路径：
- `packages/strategy-factory/src/strategy_factory/domain/spawner.py`

验收：
- [x] 补位候选可区分“有信号补位”和“历史引导补位”
- [x] 无信号补位占比明显下降

---

## 四、L2：研究假设生成 -> 候选编译

### L2.1 Hypothesis 层

- [x] 新增 `LLMHypothesisGenerator`
- [x] hypothesis 至少包含：
  - `alpha_hypothesis`
  - `failure_mode`
  - `target_universe_hypothesis`
  - `family_hint`
  - `holding_rationale`
  - `alpha_half_life`
  - `cost_sensitivity_grid`
  - `position_model`
  - `capacity_assumption`
  - `validation_focus`
- [x] hypothesis 单独持久化，不能只保留最终 candidate

涉及路径：
- 新增：`packages/akshare-mcp/src/akshare_mcp/services/strategy_hypothesis_generator.py`
- `packages/akshare-mcp/src/akshare_mcp/services/_strategy_llm_provider_prompt.py`
- `packages/akshare-mcp/src/akshare_mcp/services/strategy_autonomy_components.py`

验收：
- [x] 每个 L2 候选都能追溯到 hypothesis artifact
- [x] hypothesis 缺关键经济字段时直接失败，不走默认补丁

### L2.2 Compiler 层

- [x] 新增 `HypothesisLoweringCompiler`
- [x] 负责：
  - hypothesis -> candidate contract
  - candidate contract -> DSL / params
  - schema / target alignment / risk boundary / window 合规校验
- [x] compile 失败直接丢弃，不做默认模板修补

涉及路径：
- 新增：`packages/strategy-factory/src/strategy_factory/application/hypothesis_lowering_compiler.py`
- `packages/strategy-factory/src/strategy_factory/application/candidate_contract.py`

验收：
- [x] hypothesis 和 final candidate 可一一映射
- [x] compiler failure 有明确审计原因

### L2.3 StrategySpawner 接入 LLM 生成器

- [x] 研究生成主链接入 `llm_generator`，由 research runner / autonomy generation 驱动 AI 候选生成
- [x] `StrategySpawner` 保留本地 seed / fallback 职责，不再承担 AI 主导生成器角色
- [x] provider degraded / suppress 时，允许 replay 已持久化 hypothesis，而不是整段失去 AI 搜索能力

涉及路径：
- `packages/strategy-factory/src/strategy_factory/domain/spawner.py`
- `packages/strategy-factory/src/strategy_factory/application/research/runner.py`
- `packages/strategy-factory/src/strategy_factory/application/_autonomy_task_selection.py`

验收：
- [x] L2 可单独开关
- [x] external provider suppressed 时仍能保留部分 hypothesis -> compile 能力

### L2.4 Prompt 与 Normalize 改造

- [x] hypothesis prompt 放宽，不再直接卡死最终 DSL 字段组合
- [x] compiler 层承担严格格式校验
- [x] normalize 层禁止继续替代关键经济字段
- [x] compact 模式不再等价于“只生成 1 个候选”

涉及路径：
- `packages/akshare-mcp/src/akshare_mcp/services/_strategy_llm_provider_prompt.py`
- `packages/akshare-mcp/src/akshare_mcp/services/_strategy_llm_provider_normalize.py`
- `packages/akshare-mcp/src/akshare_mcp/services/strategy_llm_provider.py`

验收：
- [x] LLM 候选的 `family × holding bucket × universe` 组合数高于当前规则基线
- [x] 关键经济字段缺失时不再被默认模板静默补齐

### L2.5 尝试次数语义拆分

- [x] 拆分：
  - `candidate_local_attempt_count`
  - `task_local_attempt_count`
  - `cohort_effective_trials`
  - `factory_global_attempt_count`
- [x] 工厂全局尝试数只用于观测与 registry，不再直接进入单 candidate 交易门

涉及路径：
- `packages/strategy-factory/src/strategy_factory/application/research/runner.py`
- `packages/strategy-factory/src/strategy_factory/application/submission_gate.py`

验收：
- [x] 单候选统计惩罚不再被 factory-stage 最大尝试数抬高

---

## 五、Feedback：从节流器升级为搜索路由器

### F1. 搜索路由表

- [x] 新增 `family × universe × holding bucket × generator_mode` reward/debt 表
- [x] feedback 输出显式动作：
  - `family_retire`
  - `family_freeze`
  - `family_cooldown`
  - `family_explore`
  - `universe_shrink`
  - `universe_expand`
  - `holding_promote`
  - `holding_demote`
  - `generator_mode_shift`

涉及路径：
- `packages/strategy-factory/src/strategy_factory/application/factor_research.py`
- `packages/strategy-factory/src/strategy_factory/application/_budget_feedback.py`

验收：
- [x] feedback 不再只输出 multiplier/control mode
- [x] family order / universe / holding bucket 能被真实改写

### F2. refresh / revision 收紧

- [x] `refresh_existing` 仅允许在 `tested_object` 不变且 `signal / forward / review` 连续改善时触发
- [x] 其他情况强制分叉新 lineage 或淘汰
- [x] 为同一 lineage 设置连续 refresh/revision 上限

涉及路径：
- `packages/strategy-factory/src/strategy_factory/application/deduplicator.py`

验收：
- [ ] `refresh_existing + spawn_revision_from_existing` 占比持续下降
- [ ] 低质量 family 不再长期空转

---

## 六、Gate-3：分阶段 + cohort 对齐

### G1. Incubation Gate 前移收紧

- [x] `trade_density` 从 warning 升级为明确阻断或强降权
- [x] `parameter_perturbation_trade_stability` 从 warning 升级为明确阻断或强降权
- [x] `validation_grade=D` 不再仅在 provisional 路径 warning 放行
- [x] `review score / reviewer grade` 纳入 strict incubation 排序与阻断

涉及路径：
- `packages/strategy-factory/src/strategy_factory/application/submission_gate.py`
- `packages/strategy-factory/src/strategy_factory/application/quality_reporting.py`

验收：
- [x] `validation_grade=D` 在 strict incubation / governed promotion 路径最终收敛到 0 通过率

### G2. Formal multiple-testing cohort 化

- [x] DSR / PBO / White RC / SPA 的 registry key 升级为：
  - `family`
  - `target universe / target pool`
  - `holding bucket`
  - `generator_mode`
  - `validation profile`
  - `lineage / tested-object bucket`
- [x] 不再主要依赖“目标标的 + 固定代表股”宽面板
- [x] LLM 同批候选加入批内相关性修正

涉及路径：
- `packages/strategy-factory/src/strategy_factory/application/submission_gate.py`

验收：
- [x] formal gate 对同类候选竞争集更敏感
- [x] 单标的 bulk target-only 策略不再被宽 family registry 误伤

---

## 七、L3：开放型 DSL 候选

### L3.1 DSL 候选开放与编译

- [x] 支持 `llm_defined` / open DSL 候选
- [x] 先做语法与语义校验，再进入回测
- [x] 设置每轮 L3 候选上限

涉及路径：
- `packages/strategy-factory/src/strategy_factory/application/strategy_dsl.py`
- `packages/strategy-factory/src/strategy_factory/application/submission_gate.py`

验收：
- [x] L3 候选可以独立统计通过率与失败原因

### L3.2 风控与经济语义真实执行

- [x] 修复 `DslRuleStrategy.risk_rules` 执行缺口
- [x] 把 `position_model`
- [x] `cost_sensitivity_grid`
- [x] `capacity_assumption`
- [x] `holding_rationale`
  真正接入回测执行，而不是仅作为 metadata

涉及路径：
- `packages/strategy-factory/src/strategy_factory/application/backtest_filter.py`
- `packages/strategy-factory/src/strategy_factory/application/candidate_contract.py`
- DSL 相关执行器路径

验收：
- [x] 经济完整性字段被真实执行
- [x] 回测不再默认退回 `single_name/equal_weight + fixed cost`

---

## 八、测试与验证清单

### 单元测试

- [x] `ParameterDistributionRegistry`
- [x] `LLMHypothesisGenerator`
- [x] `HypothesisLoweringCompiler`
- [x] attempt 语义拆分
- [x] cohort trials registry
- [x] refresh/revision cap
- [x] economic completeness execution

### 集成测试

- [ ] `StrategySpawner.spawn()` 在 L0/L1/L2/L3 下都可降级运行
- [x] provider degraded / suppress 时 hypothesis replay 仍可工作
- [x] `factory_status` / `factory_run_detail` 能展示新增字段
- [x] Gate-3 strict incubation 与 live gate 口径对齐

### 真实运行验收

- [ ] 连续 3 次真实 run 统计 `unique family × holding bucket × target universe` 提升
- [ ] 连续 3 次真实 run 的 `zero-signal ratio` 下降
- [ ] 连续 3 次真实 run 的 `forward coverage` 提升
- [ ] 连续 3 次真实 run 的 `validation_grade=D` 提交占比下降
- [ ] 连续 3 次真实 run 的 `research_only / deferred_submission` 占比下降

---

## 九、建议实施顺序

### 第一批

- [x] P0.1 真实运行观测补齐
- [x] P0.2 当前质量基线固化
- [x] L1.1 ParameterDistributionRegistry
- [x] L1.2 配额补位修正

### 第二批

- [x] L2.1 Hypothesis 层
- [x] L2.2 Compiler 层
- [x] L2.4 Prompt 与 Normalize 改造
- [x] L2.5 尝试次数语义拆分

### 第三批

- [x] F1 搜索路由表
- [x] F2 refresh / revision 收紧
- [x] G1 Incubation Gate 前移收紧
- [x] G2 formal multiple-testing cohort 化

### 第四批

- [x] L3.1 DSL 候选开放与编译
- [x] L3.2 风控与经济语义真实执行
- [ ] 真实运行验收与质量回归

---

## 十、最终验收口径

- [ ] 候选质量不再主要体现为“产出更多 research_only 样本”
- [x] `validation_grade=D` 不再混入 strict incubation / governed promotion 主路径
- [x] `refresh_existing` 不再成为主导生成路径
- [ ] LLM 候选的多样性、forward coverage、promotion-ready rate 实际高于规则基线
- [x] 生成对象不再只是 schema 完整，而是具备真实执行的经济语义
