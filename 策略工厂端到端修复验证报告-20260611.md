# 策略工厂端到端断链修复 — 验证报告

> 日期：2026-06-11
> 配套诊断报告：`策略工厂端到端断链诊断报告-20260611.md`
> 验证 session：`verify_e2e_20260611`（5 轮，开启 remediation + paper intake + 新接入的 SignalTracker）
> 性质：基于数据库实证的修复前后对比验证。

## 一、修复项与验证状态

| 编号 | 问题 | 修复 | 验证状态 |
|---|---|---|---|
| P0-1 | 信号生成断点（SignalTracker 从不在运行循环调度，孵化每轮 signals=0） | quality session 每轮孵化前接入 `SignalTracker.run_once()` | ✅ 实测生效 |
| P0-2 | remediation 对真实样本重编译误判失败（`_is_compiled_dsl_ready` 把语义契约缺失当成 DSL 未编译） | 区分"DSL 可执行性"与"语义契约完整性"，仅 DSL 类 gap 才判未就绪 | ✅ 实测生效 |
| P1-1 | recompile 标记未落库（`runtime_recompile_backfill` 不在 json budget 白名单，超限被丢弃） | 加入 `STRATEGY_PARAM_FULL_KEYS` 白名单 | ✅ 实测生效 |
| P1-2 | remediation 每轮仅扫 50 个 + 反复处理已完成样本 | 已编译样本短路跳过（不刷 updated_at）+ batch_limit 50→200 | ✅ 实测生效 |

## 二、修复前后对比（数据库实证）

| 指标 | 修复前 | 修复后（前 4 轮） |
|---|---|---|
| 单轮信号生成 | 全系统每天 ~1 个 | **48 / 45 / 42 / 40**（每轮稳定，逐轮） |
| 今日新信号(strategy_signals) | ~1 | **60+ 持续增长** |
| signal_forward_returns | 长期 0 增长 | 本轮 +32，累计 258 |
| observe 样本 compiled_dsl | 84（仅生成自带） | **84→102→123** 持续补齐 |
| observe 带 recompile 标记 | **0 / 全库** | **146**（标记成功落库） |
| remediation recompiled/轮 | 6（且未落库） | 首轮 **108**，之后稳定 9（已补样本被正确跳过） |
| paper 交易 | 6/9 后归零 | 恢复（今日 +2 单 +2 成交，量仍小） |

## 三、关键证据（日志）

SignalTracker 每轮真实运行并产信号（修复前从不被调用）：
```
SignalTracker: completed in 25.9s — 48 signals, 202 event snapshots, 32 fwd returns, 1 incubation orders
SignalTracker: completed in 24.3s — 45 signals
SignalTracker: completed in 22.5s — 42 signals
SignalTracker: completed in 23.0s — 40 signals
```

remediation 每轮真实补 DSL 并落库（首轮大批补齐，之后增量）：
```
recompile remediation: scanned=200 recompiled=108  (首轮)
recompile remediation: scanned=200 recompiled=9    (后续，已补的被 already_compiled 跳过)
```

## 四、结论

### 已解决（核心死结打通）
修复前整条链"完全空转、永远 0 信号"的死结**已解开**，有实证：
- 信号生成从"每天 1 个"→"每轮 40-48 个"，48 个不同策略产信号
- observe 样本持续获得 compiled_dsl（84→123），不再因无 DSL 而产不出信号
- 前向收益开始计算（forward_returns +32/轮），纸面交易恢复

### 仍需运营时间（非代码缺陷）
- `auto_promoted=0`、paper 交易量仍小：链路打通了，但策略走完 warmup→graduation→真实交易
  需要持续多轮积累足够前向证据（effective_n 达标）。这是**时间问题，不是断点**。
- remediation 存量推进仍偏慢：每轮新提交 7 个占据 updated_at DESC 窗口前列，存量老样本（2900+）
  补齐速度受限。已编译的不重复处理（✅），但全量覆盖需较多轮次。可后续按"未处理优先"排序进一步优化。

### 诚实边界
本次修复解决的是"链路断裂导致永久空转"的工程缺陷。修复后机制已正确运转、数据在流动。
但"策略真正走到实盘交易"仍取决于这套机制持续运行、积累出统计显著的前向证据——那是运营过程，
不在单次代码修复的能力边界内。

## 五、回归
全部相关单测通过：recompile_backfill + warmup_stall + instrument_profile + lifecycle_runtime
+ storage_json_caps = 36 项 passed。json budget 白名单改动未破坏字节预算逻辑。
