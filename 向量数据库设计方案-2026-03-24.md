# 向量数据库设计方案

日期：2026-03-24  
范围：`packages/akshare-mcp`、`packages/strategy-factory`、TimescaleDB / PostgreSQL / pgvector 及市场与量化数据链路  
目标：基于当前项目数据库现状、live DB 事实和官方资料，给出可执行的向量数据库统一设计与优化方案

---

## 1. 执行摘要

### 1.1 结论

当前项目的数据库主设计方向是正确的：

- `TimescaleDB / PostgreSQL` 已经承担行情、财务、因子、策略、执行等主数据面。
- 时序数据、关系过滤、历史回溯、事务一致性、业务 join 仍应以 PostgreSQL 为核心。
- 当前真正成熟的向量能力只存在于 `策略域`，而不是市场文本或量化模式域。

本次审查后的明确判断是：

1. 当前项目不是“没有向量数据库”，而是“只有策略域有完整的 pgvector 能力，市场/量化侧仍是碎片化状态”。
2. 短中期主方案应继续采用 `TimescaleDB / PostgreSQL + pgvector`，而不是立即迁移到独立向量数据库。
3. 当前首要问题不是“选型错误”，而是 `schema 漂移`、`向量 collection 治理缺失`、`文本没有 chunk 化`、`量化派生向量未统一接入`。
4. 只有当市场文档、因子记忆、相似模式、策略画像等向量集合显著扩大，且过滤/混合检索/多向量需求明显上升时，才需要评估 `Qdrant`；更大规模独立向量集群才考虑 `Milvus`。

### 1.2 推荐路径

- 近期：统一 PostgreSQL 内的向量层，复用 `strategy_vector_*` 架构，补市场文档和量化模式向量。
- 中期：如继续坚持 PostgreSQL 主平面，但 ANN 规模和性能压力增大，可评估 `pgvectorscale`。
- 远期：当向量查询明显成为独立工作负载，且需要更强的 payload indexing、多向量或租户隔离能力时，再评估 `Qdrant`。

---

## 2. 本次审查范围与方法

本次方案不是脱离代码的抽象设计，而是基于三类事实：

1. 仓库当前实现。
2. live DB 运行态核验结果。
3. 官方文档与一手资料。

### 2.1 审查过的关键实现

- 数据库初始化与 pgvector 启用：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_base.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_base.py#L150)
- 市场主 schema：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_market.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_market.py#L110)
- 市场侧“向量表”定义：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_market.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_market.py#L731)
- 因子持久化：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/factor_storage.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/factor_storage.py#L38)
- 数据处理链路：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/data_sync.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/data_sync.py#L329)
- 市场文本入库：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/market_context.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/market_context.py#L59)
- K 线相似搜索：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/vector_search.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/vector_search.py#L203)
- 策略向量 schema：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_strategy.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_strategy.py#L663)
- 策略向量平台与检索：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/vector_platform.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/vector_platform.py#L409)
- pgvector HNSW 索引管理：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/strategy_vector.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/strategy_vector.py#L527)
- 文本 embedding 服务：[/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/text_embedding.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/text_embedding.py#L19)

### 2.2 live DB 核验事实

核验结果基于本地运行的 TimescaleDB 实例，而不是只看 schema 文件。

- 已安装扩展：
  - `timescaledb 2.25.2`
  - `vector 0.8.1`
- hypertable：
  - `kline_1d`
  - `north_fund_flow`
  - `stock_fund_flow`
  - `stock_quotes`
- 关键表数据量：
  - `stocks`: 5485
  - `kline_1d`: 902472
  - `stock_quotes`: 388
  - `financials`: 5488
  - `factor_values`: 2640
  - `factor_ic_history`: 13
  - `vector_documents`: 757
  - `stock_embeddings`: 0
  - `pattern_vectors`: 0
  - `strategy_vector_profiles`: 31
  - `strategy_vector_profile_store`: 31
  - `strategy_vector_index_snapshots`: 1
  - `strategy_vector_index_items`: 4
  - `strategy_vector_index_item_store`: 4
  - `vector_index_registry`: 2

### 2.3 live DB 发现的关键问题

- `financials` 实际列名还是 `code`，而 schema 代码已按 `stock_code` 设计。
- `stocks` 实际列名还是 `code`，而 schema 代码已按 `stock_code` 设计。
- `stock_quotes` 中旧字段 `change` / `pre_close` 和新字段 `change_amt` / `prev_close` 并存。
- `vector_documents` 有数据，但平均文本长度仅约 `23~29` 字，仍是短标题/摘要仓，不是可用的语义文档库。
- `stock_embeddings` / `pattern_vectors` 为空，说明市场向量层并未真实启用。
- `strategy_vector_profile_store` / `strategy_vector_index_item_store` 已有 HNSW partial index。
- `strategy_vector_profiles` 中同时存在 `120 dim` 与 `3072 dim`，而 active snapshot 只覆盖 `4` 条 `120 dim` 向量，说明 index/version/model/dim 治理并不统一。

---

## 3. 当前数据库与数据处理设计的真实状态

### 3.1 数据处理主链路

当前 K 线数据链路是：

`SimpleCache -> TimescaleDB -> 外部 API -> 异步写回 DB`

这条链路在工程上是合理的，因为：

- 高频读可以先命中内存缓存。
- 历史查询优先读 TimescaleDB，避免重复打外部 API。
- API 回源后异步持久化，既兼顾吞吐，也为后续研究和回测沉淀数据。
- 已具备队列、worker、重试、dead-letter 的完整骨架，适合作为后续“向量回填任务”的执行基座。

### 3.2 市场/量化数据库设计

目前市场和量化主数据层分为三类：

1. `时序事实表`
   - `kline_1d`
   - `stock_quotes`
   - `north_fund_flow`
   - `stock_fund_flow`

2. `关系/维表`
   - `stocks`
   - `financials`
   - `research_reports`
   - `market_blocks`

3. `量化持久化表`
   - `factor_values`
   - `factor_ic_history`

这个层次划分本身没有问题。  
问题在于：向量层没有成为第四层“统一派生检索层”，而是散落在多个不同实现里。

### 3.3 当前向量相关实现的实际状态

#### 市场侧

- `vector_documents`：本质是全文文本表，不是 embedding 库。
- `stock_embeddings`：空表。
- `pattern_vectors`：空表。
- `vector_search.py`：仍是进程内向量化和局部索引检索，不是数据库级 ANN。

#### 策略侧

- `strategy_vector_profiles`：保存画像元数据和 JSON 形式 embedding 归档。
- `strategy_vector_profile_store`：保存 pgvector 向量。
- `strategy_vector_index_snapshots`：管理版本快照。
- `strategy_vector_index_items` / `*_store`：保存 ANN 检索平面。
- `vector_index_registry`：管理 index 版本和激活状态。

结论：

- 市场/量化侧向量能力仍是分散的实验态。
- 策略侧已经具备生产化雏形。
- 因此应该复用策略侧架构，而不是重新引入一套独立向量平台。

---

## 4. 为什么当前不建议立刻迁出 PostgreSQL

### 4.1 当前工作负载的本质

你们当前项目的数据访问模式不是“纯向量检索优先”，而是典型的混合型：

- 先按 `stock_code`、`trade_date`、`doc_type`、`source`、`event window`、`profile_type` 过滤。
- 再执行向量近邻或相似检索。
- 同时需要频繁回表读取事实数据、策略元数据、财务指标、事件上下文。

这类工作负载的核心仍然是：

- 强过滤
- 强 join
- 时序关联
- 事务一致性
- 元数据治理

这些都天然更适合保留在 PostgreSQL/TimescaleDB 主平面。

### 4.2 当前向量规模还不大

按 live DB 现状：

- 策略画像仅 31 条。
- active ANN item 仅 4 条。
- 市场文本仅 757 条，而且还不是 chunk 化语义文档。
- 市场侧模式向量还是空仓。

在这个规模下，立即拆独立向量数据库只会引入：

- 双写
- 一致性治理
- 独立备份恢复
- 权限和连接管理
- 搜索回源编排

而不会带来实际收益。

### 4.3 当前真正短板不在向量引擎

当前性能瓶颈和检索质量短板更可能来自：

1. 文本未 chunk 化。
2. 向量 collection 维度/模型混放。
3. 没有 recall 评测基线。
4. schema 漂移导致过滤和回填治理不稳定。
5. 市场和量化派生对象没有进入统一向量层。

因此，当前阶段的正确顺序是：

先统一数据模型和索引治理，再决定是否要迁引擎。

---

## 5. 目标架构：统一向量层

### 5.1 设计原则

统一向量层必须满足以下原则：

1. 一个逻辑索引只服务一个实体家族。
2. 一个逻辑索引只能对应一个 `model_id + dim + metric + normalization + version`。
3. 所有 ANN 检索都必须先有元数据过滤策略。
4. 文本检索必须保留 `lexical + dense` 双通道。
5. JSON 归档层和 pgvector 查询层要分开。
6. 所有索引都必须具备 `registry / snapshot / status / rebuild` 治理能力。
7. 向量层只存“派生对象”，不替代事实表。

### 5.2 建议的逻辑 collection 拆分

建议将统一向量层拆成以下 collection：

#### `market_doc_chunks`

用途：

- 新闻、公告、研报、电话会纪要、宏观简报的 chunk 检索
- RAG
- 事件相似度匹配

输入对象：

- 原始文档
- chunk 文本
- 标题、来源、发布日期、股票代码、标签

#### `kline_pattern_embeddings`

用途：

- K 线窗口形态相似检索
- 模式聚类
- 案例回放

输入对象：

- 固定窗口
- 固定频率
- 固定复权口径
- 固定向量化方法

#### `stock_profile_embeddings`

用途：

- 股票相似性搜索
- 风格迁移
- 同行业/同画像检索

输入对象：

- 基本面
- 风格因子摘要
- 风险暴露
- 估值状态
- 产业链特征

#### `factor_candidate_embeddings`

用途：

- 因子研究记忆
- 候选因子相似性搜索
- 因子去重
- 因子解释聚类

输入对象：

- 因子定义
- 公式
- 解释文本
- OOS/IC/稳健性摘要

#### `strategy_behavior_embeddings`

用途：

- 继续承接当前策略画像检索
- 作为统一向量层的第一个已成熟子域

输入对象：

- 策略画像
- 行为模式
- 投影摘要

---

## 6. 建议的数据模型

### 6.1 不建议继续沿用的市场侧老表

以下三张表不应继续作为未来统一向量层核心：

- `vector_documents`
- `stock_embeddings`
- `pattern_vectors`

原因：

- 表意不清。
- 没有统一 metadata。
- 没有 model/dim/version 治理。
- 没有 JSON archive 与 pgvector store 分层。
- 已与策略侧成熟方案不一致。

### 6.2 建议的统一表设计

建议抽象出一套通用 schema，而不是每个业务域再各自造轮子。

#### `vector_collections`

用途：

- collection 注册表
- 约束 `entity_family / model_id / dim / metric / version`

核心字段：

- `collection_name`
- `entity_family`
- `backend`
- `metric`
- `model_id`
- `vector_dim`
- `normalization`
- `status`
- `active_version`
- `metadata`

#### `vector_profiles`

用途：

- 保存归档层对象
- 可审计
- 可回放

核心字段：

- `id`
- `collection_name`
- `entity_type`
- `entity_id`
- `stock_code`
- `profile_type`
- `model_id`
- `vector_dim`
- `metric`
- `version`
- `signature`
- `embedding_json`
- `metadata`
- `created_at`
- `updated_at`

#### `vector_profile_store`

用途：

- pgvector 实体查询层

核心字段：

- `profile_id`
- `collection_name`
- `entity_type`
- `entity_id`
- `stock_code`
- `profile_type`
- `model_id`
- `vector_dim`
- `metric`
- `version`
- `embedding vector`
- `metadata`
- `updated_at`

#### `vector_index_snapshots`

用途：

- index 构建快照
- 支持 rebuild、切换、审计

核心字段：

- `collection_name`
- `index_version`
- `status`
- `vector_dim`
- `metric`
- `model_id`
- `profile_count`
- `index_params`
- `metadata`
- `built_at`
- `activated_at`

#### `vector_index_items`

用途：

- coarse bucket / ANN 索引平面
- 与策略侧现有设计兼容

核心字段：

- `collection_name`
- `index_version`
- `profile_id`
- `entity_id`
- `bucket_id`
- `coarse_score`
- `embedding_json`
- `metadata`

#### `vector_index_item_store`

用途：

- pgvector 检索层

核心字段：

- `item_id`
- `collection_name`
- `index_version`
- `profile_id`
- `entity_id`
- `vector_dim`
- `embedding vector`
- `metadata`

### 6.3 通用 metadata 规范

统一 metadata 至少应支持以下字段：

- `entity_type`
- `entity_id`
- `stock_code`
- `doc_type`
- `source`
- `published_at`
- `trade_date`
- `tenant_id`
- `model_id`
- `dim`
- `metric`
- `version`
- `status`
- `tags`

说明：

- `entity_id` 用于唯一标识派生对象。
- `stock_code` 用于高频过滤。
- `model_id/dim/metric/version` 用于治理和索引隔离。
- `published_at/trade_date` 用于时点一致性控制。

---

## 7. 针对各类数据对象的具体设计

### 7.1 文档向量：`market_doc_chunks`

当前问题：

- `vector_documents` 只存短文本。
- 平均文本长度过短，不具备 RAG 价值。
- 没有 chunk、没有 embedding、没有 hybrid retrieval。

目标设计：

1. 保留原始文档表。
2. 增加文档 chunk 表。
3. 每个 chunk 生成 dense embedding。
4. 同时对 chunk 文本建 `tsvector` 索引。

建议字段：

- `doc_id`
- `chunk_id`
- `stock_code`
- `doc_type`
- `source`
- `title`
- `chunk_text`
- `published_at`
- `language`
- `model_id`
- `vector_dim`
- `embedding`
- `tsv`

查询路径：

1. 先按 `stock_code/doc_type/published_at` 过滤。
2. dense 检索取 top-k。
3. lexical 检索取 top-k。
4. 做 hybrid rerank。

### 7.2 K 线模式向量：`kline_pattern_embeddings`

当前问题：

- 当前 `vector_search.py` 主要在进程内向量化和索引。
- `pattern_vectors` 为空。
- 没有数据库级可复用索引，也没有版本治理。

目标设计：

- 将每个模式窗口固化为可重建对象。
- 固定以下维度作为 collection 约束：
  - `window_size`
  - `bar_freq`
  - `adjust`
  - `vector_method`
  - `vector_dim`
  - `metric`

建议 entity key：

- `entity_id = {stock_code}:{end_date}:{window_size}:{method}:{adjust}`

查询路径：

1. 先按 `window_size/method/adjust/vector_dim` 过滤。
2. 走 pgvector ANN。
3. 回表取窗口收益统计与后验表现。

### 7.3 股票画像向量：`stock_profile_embeddings`

适用对象：

- 股票相似搜索
- 多因子风格映射
- 行业替代检索

建议输入：

- 估值摘要
- 财务稳健性
- 因子暴露
- 波动与流动性
- 事件上下文摘要

不建议：

- 直接把逐日 `factor_values` 整表向量化。

建议：

- 先聚合成截面画像后再生成向量。

### 7.4 因子研究记忆：`factor_candidate_embeddings`

当前问题：

- `factor_values` 有数据，但覆盖浅，时间范围仅近期。
- 因子研究与向量记忆尚未统一。

目标设计：

- 保存候选因子定义和研究证据摘要。
- 用于去重、回顾、检索相似候选、识别重复研究。

建议输入：

- 因子名称
- 公式文本
- 使用字段
- IC/OOS/稳健性摘要
- 风险说明
- 适用市场状态

---

## 8. 索引与查询优化策略

### 8.1 pgvector 作为主方案的索引建议

依据 `pgvector` 官方建议，当前项目应采用以下策略：

1. 高频近邻检索使用 `HNSW`。
2. 索引按 `collection + version + vector_dim (+ profile_type)` 做 partial index。
3. 常用过滤字段单独建立 B-tree / partial index。
4. 大批量回填后再构建 ANN 索引，不要边写边建。
5. 对归一化向量优先考虑 inner product。

适合你们当前项目的组合是：

- `B-tree` 负责过滤
- `HNSW` 负责近邻
- `JSONB` 负责扩展 metadata
- `snapshot/registry` 负责版本治理

### 8.2 过滤优先策略

当前项目大多数向量查询都不是全库搜索，而是先过滤再检索。

因此应执行：

1. 先按 `collection_name/model_id/version/vector_dim` 过滤。
2. 再按业务字段过滤，如 `stock_code/doc_type/window_size/trade_date`。
3. 最后做 ANN。

如果过滤字段离散度较低，可以使用 partial index。  
如果未来 collection 规模显著扩大，可进一步按 collection 或日期做 partitioning。

### 8.3 高维向量优化

当前 live DB 中已经出现 `3072 dim` 文本向量。

建议：

- 文本向量优先评估更小模型。
- 对高维向量评估 `halfvec` 或半精度方案。
- 避免在同一个 collection 中混入 `120 dim` 和 `3072 dim`。
- 不要在同一 active index 中混模型和混维度。

### 8.4 评测与验收

必须建立以下基线，不然向量优化没有客观标准：

- `Recall@K`
- `nDCG@K`
- P50 / P95 延迟
- 过滤后召回率
- index build/rebuild 耗时
- 存储空间占用
- 回填吞吐

至少保留两套检索结果用于对照：

- exact search baseline
- ANN result

否则只看延迟而不看召回，会直接把向量检索做坏。

---

## 9. 数据治理与 schema 修复优先级

当前最需要先处理的不是 ANN 参数，而是数据库治理。

### 9.1 必须优先修复

#### `code` / `stock_code` 命名漂移

影响：

- ORM / SQL 混乱
- ETL 和回填脚本不稳定
- 向量 metadata 过滤字段不统一
- 新旧 schema 难以兼容

建议：

- 统一主键和外键命名为 `stock_code`
- 提供兼容视图或迁移期别名列
- 修复所有脚本和存储层接口

#### `stock_quotes` 旧新字段并存

影响：

- 数据定义不稳定
- 查询层容易误读

建议：

- 明确唯一标准字段
- 将旧字段迁移为兼容层，不再作为主写入字段

### 9.2 向量治理必须新增的规则

必须建立以下约束：

1. 一个 `collection_name` 只能绑定一个 `model_id + dim + metric + normalization`。
2. 一个 `index_version` 只能服务一种向量规格。
3. active index 切换必须通过 snapshot 激活，不允许隐式覆盖。
4. rebuild 任务必须记录参数、输入规模、召回基线和完成时间。

---

## 10. 技术选型判断矩阵

### 10.1 PostgreSQL + pgvector

适用条件：

- 主库仍是 PostgreSQL/TimescaleDB
- 强依赖关系过滤和时序 join
- 向量规模中小
- 希望复用现有事务/备份/权限体系

对当前项目的判断：

- 当前最佳方案

### 10.2 PostgreSQL + pgvectorscale

适用条件：

- 继续留在 PostgreSQL 生态
- 但 ANN 规模和延迟要求上升
- 需要更强的磁盘级 ANN 扩展能力

对当前项目的判断：

- 中期优先备选

### 10.3 Qdrant

适用条件：

- 多向量检索
- named vectors
- payload filtering 很重
- hybrid retrieval 很重
- 多租户隔离需求更强

对当前项目的判断：

- 中期备选，不建议现在迁

### 10.4 Milvus

适用条件：

- 更大规模独立向量集群
- 向量搜索成为主业务面
- 团队可承担独立运维

对当前项目的判断：

- 现阶段不建议

---

## 11. 分阶段实施路线图

### Phase 0：schema 与治理修复

目标：

- 统一命名
- 修复漂移
- 明确 active index 规则

工作项：

- 统一 `code -> stock_code`
- 清理 `stock_quotes` 字段定义
- 为策略向量 collection 加入 `model_id/dim/version` 约束
- 建立统一 `vector_collections` 注册表

验收：

- 所有新写入路径只使用统一字段
- 所有向量 index 都可追溯到明确规格

### Phase 1：统一策略向量层

目标：

- 将现有 `strategy_vector_*` 治理模式提升为通用框架

工作项：

- 抽象 registry/snapshot/store/index-item 通用接口
- 将策略域迁入统一 collection 命名规范
- 补充 exact-vs-ANN 评测

验收：

- 策略域向量查询只依赖统一接口

### Phase 2：市场文本向量化

目标：

- 从 `vector_documents` 过渡到 `market_doc_chunks`

工作项：

- 文档 chunk 化
- dense embedding 回填
- `tsvector` 索引
- hybrid retrieval

验收：

- 支持按 `stock_code/doc_type/date` 过滤后的 dense + lexical 混合检索

### Phase 3：量化模式向量化

目标：

- 将 K 线相似模式从进程内索引升级为持久化向量检索

工作项：

- 构建 `kline_pattern_embeddings`
- 固化 `window_size/method/adjust/frequency`
- 建立 pgvector HNSW
- 回表返回 forward return 统计

验收：

- 相似模式搜索不再依赖进程内临时索引

### Phase 4：股票画像与因子研究记忆

目标：

- 打通市场、量化、研究三类派生对象的统一向量层

工作项：

- 股票画像向量
- 因子候选向量
- 因子研究记忆去重与相似检索

验收：

- 股票相似搜索和因子研究检索统一使用 collection 治理

### Phase 5：是否升级到 pgvectorscale / Qdrant

触发条件：

- 数据量和查询量明显增长
- PostgreSQL 内 ANN 成本升高
- hybrid / named vectors / 多租户过滤需求超出当前实现边界

决策原则：

- 优先评估 `pgvectorscale`
- 再评估 `Qdrant`
- `Milvus` 只在大规模独立向量集群场景考虑

---

## 12. 推荐的近期落地清单

建议按优先级直接执行以下事项：

1. 修复 `stocks` / `financials` / `stock_quotes` 的 schema 漂移。
2. 停止将 `vector_documents` 当作“向量数据库”表述，改名或标注为文本上下文表。
3. 抽象策略域现有的 `registry + snapshot + store + ANN item` 模型为通用向量层。
4. 为所有 collection 增加强约束：
   - `model_id`
   - `vector_dim`
   - `metric`
   - `normalization`
   - `version`
5. 落地 `market_doc_chunks`，替代 `vector_documents` 的短文本存储模式。
6. 落地 `kline_pattern_embeddings`，替代进程内 K 线模式检索。
7. 建立 exact-vs-ANN 的检索评测任务。
8. 当向量规模明显放大后，再评估 `pgvectorscale`。

---

## 13. 官方资料与参考依据

### PostgreSQL / pgvector

- `pgvector` 官方 README：<https://github.com/pgvector/pgvector>
  - 参考点：
    - HNSW / IVFFlat
    - partial index
    - partitioning
    - filtering
    - iterative scans
    - `halfvec`
    - normalized vectors + inner product
    - bulk load 后建索引

### Timescale / pgvectorscale

- `pgvectorscale` 官方仓库：<https://github.com/timescale/pgvectorscale>
  - 参考点：
    - PostgreSQL 生态内的 ANN 扩展能力
    - StreamingDiskANN
    - 适合继续保留主库在 PostgreSQL 时作为中期升级路径

### Qdrant

- Qdrant Indexing：<https://qdrant.tech/documentation/concepts/indexing/>
- Qdrant Vectors：<https://qdrant.tech/documentation/concepts/vectors/>
  - 参考点：
    - payload index
    - on-disk payload index
    - named vectors
    - 数据类型优化
    - hybrid / sparse + dense

### Milvus

- Milvus Filtered Search：<https://milvus.io/docs/filtered-search.md>
- Milvus Partition Key：<https://milvus.io/docs/use-partition-key.md>
  - 参考点：
    - scalar filtering
    - filtered search
    - partition key
    - 更大规模向量集合管理

---

## 14. 最终判断

对当前项目而言，正确的问题不是：

- “我们要不要现在上独立向量库？”

而是：

- “我们如何先把市场、量化、策略三条向量链路统一成一个受治理的向量层？”

当前最合理的工程路线是：

1. 保持 `TimescaleDB / PostgreSQL` 作为主数据平面。
2. 以 `pgvector` 为近期主向量方案。
3. 复用策略域已存在的向量治理模型。
4. 补齐市场文本 chunk 化和量化模式持久化向量检索。
5. 等工作负载真实增长后，再决定是否引入 `pgvectorscale` 或 `Qdrant`。

这条路径与当前项目结构最一致，迁移成本最低，技术债最可控，且能直接把现有“策略域已成熟、市场域未完成”的不平衡状态拉平。
