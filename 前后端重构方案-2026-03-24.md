# 前后端重构方案

日期：2026-03-24  
范围：`apps/web`、`apps/bff`、`packages/akshare-mcp`  
关联报告：`/Users/mac/Desktop/股票/项目审查报告-MCP核心-2026-03-24.md`  
关联清单：`/Users/mac/Desktop/股票/MCP服务股票能力改造清单-2026-03-24.md`

---

## 1. 方案目标

本方案不再重复“系统已经实现了什么”，只回答四个问题：

1. 当前系统为什么需要重构。
2. 重构后前端、BFF、MCP 应该形成什么分层关系。
3. 哪些能力应优先产品化，才能把系统从“能力集合”变成“股票工作台”。
4. 重构应按什么阶段推进，每阶段的交付件和验收标准是什么。

文档结构按以下顺序展开：

- `第 2 章` 先说明为什么当前系统必须重构
- `第 3-4 章` 定义重构原则和目标产品结构
- `第 5 章` 定义前端目标方案
- `第 6 章` 审查参考项目，提炼可复用模式
- `第 7 章` 把前端 AI 工作台拆成可执行实施清单
- `第 8-9 章` 分别补 BFF 和 MCP 配套方案
- `第 10-11 章` 再落到实施阶段和模块文件映射

本次判断：

- 当前系统的主要问题不是能力不足，而是 `能力分散`、`入口割裂`、`产品承载浅`。
- 当前系统的主要优势是 `MCP 能力深`、`BFF 模块全`、`股票分析链路覆盖广`。
- 当前系统的主要短板是 `前端工作区设计弱`、`任务流不连续`、`高价值后端能力没有前端入口`。

---

## 2. 当前问题诊断

### 2.1 总体判断

当前系统已经具备完整的三层结构：

- `apps/web`：页面很多，覆盖面广。
- `apps/bff`：模块化程度高，已经不是简单代理。
- `packages/akshare-mcp`：是真正的业务能力核心。

但当前产品层存在明显反差：

- `MCP 很强，前端很散。`
- `接口很多，用户主流程不强。`
- `分析能力深，持续跟踪能力弱。`

### 2.2 前端问题

#### 2.2.1 显示设计问题

- 全局视觉语言偏通用 SaaS 模板，不像专业股票工作台。
- 全局主题使用 `system-ui`、蓝紫渐变、玻璃卡片，金融信息密度不够。
- 页面大量使用“卡片 + 长滚动”，不适合高频盯盘、对比、复盘场景。
- 缺少 `紧凑模式`、`固定列`、`多面板布局`、`自定义工作区`、`多图联动`。

#### 2.2.2 功能设计问题

- 主导航按模块分组，但没有按用户任务编排。
- 首页不是以 `watchlist-first` 为中心，而是偏“展示型总览”。
- 搜索页不是完整选股器，缺少条件筛选、结果保存、结果二次加工。
- 研报页更像资讯列表，不像研究事件台。
- 风险、执行、绩效、事件、技能等高价值能力缺少独立产品页。

#### 2.2.3 使用体验问题

- 未登录或初始状态下，多个页面容易出现空数据或 401 噪音。
- 很多页面虽然补了空状态说明，但仍然停留在单页提示，不是跨页任务流。
- 页面之间缺少统一“下一步动作”语义，例如：
  - 选股结果无法自然进入自选或组合。
  - 研报事件无法自然进入提醒。
  - 决策结果无法自然进入模拟交易或风险跟踪。

### 2.3 BFF 问题

- 路由能力很多，但门面层还不够产品化。
- API 命名存在版本化不一致问题：既有 `/market/*`，也有 `/v1/options/*`、`/v1/macro/*`、`/v1/screener/*`、`/v1/skills/*`。
- 一部分 MCP Manager 已经存在，但 BFF 还没有形成清晰的 ViewModel 聚合接口。
- 部分页面仍然直接消费偏底层数据结构，而不是稳定的页面模型。

### 2.4 MCP 产品化问题

- MCP 运行时能力已达 `145` 个工具，但前端只承载了其中一部分。
- 以下类别已经有能力，但没有充分产品化：
  - `screening`
  - `performance`
  - `execution`
  - `compliance`
  - `skills`
  - `event/news calendar`
  - `vector`
  - `decision`

---

## 3. 重构原则

### 3.1 产品原则

- 以 `股票工作台` 为目标，不再以“页面数量”作为成熟度指标。
- 以 `任务闭环` 为设计中心，不再以模块罗列作为导航中心。
- 以 `watchlist / portfolio / strategy subscription` 作为持续跟踪主线。
- 以前端 `三栏工作台 + 常驻 AI Copilot` 作为统一交互骨架，不再把 AI 做成孤立页面。
- AI 不是“问答页功能”，而是全站协作层，必须能读取页面上下文并驱动页面动作。

### 3.2 架构原则

- 前端负责工作区、交互、页面模型消费，不直接承担业务拼装。
- BFF 负责产品语义、页面聚合、权限和稳定接口。
- MCP 负责领域能力、计算、研究、执行规划和底层数据工具。

### 3.3 交付原则

- 每个重构阶段都必须带可见页面交付，不接受“只改中台不改产品”。
- 每条新入口都必须定义“上游来源”和“下游动作”。
- 每个工作台页面都必须有：
  - 初始状态
  - 空状态
  - 错误状态
  - 下一步动作
  - 数据来源与时间说明

---

## 4. 目标产品结构

### 4.1 目标应用骨架

前端目标形态不再是“左侧导航 + 中间单页内容 + 独立助手页”的传统分析站，而是参考 `/Users/mac/Desktop/区块链/desktop` 当前实现，升级为统一工作台：

- 左侧：`一级导航 + 二级导航 + 工作区切换`
- 中间：`主工作区详情页`
- 右侧：`常驻 AI Copilot 栏`

对应参考实现：

- 三栏骨架：`/Users/mac/Desktop/区块链/desktop/src/App.tsx`
- 工作台布局：`/Users/mac/Desktop/区块链/desktop/src/components/Layout/MainLayout.tsx`
- 布局样式：`/Users/mac/Desktop/区块链/desktop/src/components/Layout/MainLayout.css`
- Copilot 侧栏：`/Users/mac/Desktop/区块链/desktop/src/components/CopilotSidebar/index.tsx`
- Copilot 状态：`/Users/mac/Desktop/区块链/desktop/src/stores/copilot.ts`

结构要求：

- 左侧导航必须可折叠，在窄屏下优先压缩导航而不是挤压中间详情。
- 中间主工作区承接当前页面的表格、图表、详情、表单、时间线和结果面板。
- 右侧 Copilot 必须是应用级常驻栏，可展开、收起、切换会话，不是页面内弹窗。
- `/assistant` 页面可以保留，但降级为 `全屏专家模式 / 高级报告模式`，不再是唯一 AI 入口。

### 4.2 目标导航结构

建议将当前导航重构为六个一级工作区：

1. `工作台`
   - 首页工作台
   - 自选股
   - 我的组合
   - 我的策略订阅
2. `市场`
   - 行情看板
   - 个股详情
   - 板块与主题
   - 事件日历
3. `研究`
   - 条件选股
   - 研报与公告
   - 基本面
   - 技术面
   - 估值
   - 因子研究
4. `策略`
   - 策略超市
   - 回测
   - 因子分析
   - 统一决策
5. `交易`
   - 模拟交易
   - 执行中心
   - 风控中心
   - 告警与通知
6. `系统`
   - 数据中心
   - 技能中心
   - 宏观与期权
   - 设置与审计

### 4.3 目标用户主流程

建议围绕三条主流程做产品化：

#### 流程 A：发现标的

`条件选股 / 语义选股`  
-> `加入自选或候选池`  
-> `看事件和研报`  
-> `统一决策`  
-> `设置提醒`

#### 流程 B：研究一只股票

`个股详情`  
-> `基本面 / 技术面 / 情绪 / 资金流 / 估值交叉验证`  
-> `事件与公告时间线`  
-> `生成研究结论`  
-> `加入组合或模拟交易`

#### 流程 C：持续跟踪与执行

`自选 / 组合 / 策略订阅`  
-> `提醒与通知`  
-> `执行与合规检查`  
-> `模拟交易 / 执行计划`  
-> `风险 / 绩效复盘`

---

## 5. 前端重构方案

### 5.1 前端重构目标

将当前 Web 从“多页面分析站”重构为“`三栏工作台 + 常驻 Copilot + 页面动作联动` 的股票终端”。

章节边界：

- `第 5 章` 只回答目标前端应该长什么样
- `第 6 章` 解释参考项目为什么值得借鉴
- `第 7 章` 再把目标方案拆成实际实施清单

### 5.2 三栏工作台设计

参考 `/Users/mac/Desktop/区块链/desktop` 的前端实现，当前项目应采用以下骨架：

#### 5.2.1 左侧导航栏

左栏负责“去哪”，不是“展示摘要”。

要求：

- 保留一级工作区分组，但改为更强任务导向。
- 支持折叠、当前页高亮、最近访问、固定入口。
- 支持 `watchlist / portfolio / strategy / alert / recent stock` 快速跳转。
- 支持 AI 触发导航，AI 可直接把用户带到左栏任一页面。

#### 5.2.2 中间主工作区

中栏负责“当前任务的详情与操作”，必须支持高密度信息和强交互。

要求：

- 页面默认采用 `标题区 + 状态条 + 关键操作条 + 主内容区` 结构。
- 主内容区优先使用 `表格 / 时间线 / 分栏详情 / 标签页`，减少长滚动卡片堆叠。
- 关键页面支持 `保存视图`、`记住筛选条件`、`固定列`、`多面板`。
- 中栏所有关键对象都要能被 AI 读取和操作，例如当前股票、当前 tab、筛选条件、选中行、最近结果。

#### 5.2.3 右侧 AI Copilot 栏

右栏负责“协作、解释、执行下一步动作”，不是只显示聊天记录。

要求：

- 作为应用级常驻栏存在，支持展开、折叠、会话切换、清空会话。
- 支持连续对话，不因页面切换丢上下文。
- 显示工具调用状态、动作卡片、执行结果，不只是文本回答。
- 能直接调用左栏导航和中栏页面动作，实现“说一句话 -> 页面联动完成”。
- 支持从局部卡片、表格行、详情区发起“Ask AI”，把局部对象注入当前会话。

#### 5.2.4 响应式与模式切换

要求：

- 桌面端默认三栏。
- 中等宽度下优先压缩左栏宽度，再压缩右栏宽度。
- 小屏保留 `主内容 + 抽屉式 Copilot`，但数据结构和交互协议不变。
- 保留 `/assistant` 作为全屏沉浸模式，用于长报告、统一决策详情、复杂多轮诊断。

### 5.3 AI Copilot 联动协议

参考项目的关键价值不在视觉，而在 `Copilot + PageContext + PageActionBus` 协议。当前项目应明确新增这一层。

#### 5.3.1 PageContext

每个核心页面都必须向 AI 暴露统一上下文，建议最小结构：

```ts
type PageContext = {
  page: string;
  currentTab?: string;
  stockCode?: string;
  filters?: Record<string, unknown>;
  selectedItem?: unknown;
  summary?: Record<string, unknown>;
};
```

上下文至少覆盖：

- 当前页面标识
- 当前 tab
- 当前股票代码
- 当前筛选条件
- 当前选中行 / 当前卡片对象
- 页面主数据摘要

参考实现：

- `/Users/mac/Desktop/区块链/desktop/src/hooks/usePageContext.ts`
- `/Users/mac/Desktop/区块链/desktop/src/stores/copilot.ts`

#### 5.3.2 PageActionBus

AI 必须能对页面发结构化动作，而不是只给用户一段操作建议。

建议动作协议：

```ts
type AiAction = {
  type: 'navigate' | 'fill' | 'execute';
  target: string;
  payload?: Record<string, unknown>;
  action?: string;
  label: string;
};
```

支持的基本动作：

- `navigate`：切到某个页面或 tab
- `fill`：填入搜索词、股票代码、筛选条件、表单字段
- `execute`：执行页面动作，例如选股、刷新、加入自选、生成决策、创建提醒

参考实现：

- `/Users/mac/Desktop/区块链/desktop/src/core/PageActionBus.ts`
- `/Users/mac/Desktop/区块链/desktop/src/types/index.ts`

#### 5.3.3 Copilot 消息结构

右栏消息不应只有 `role + content`，而应支持结构化消息项：

- `text`
- `tool_call`
- `action`

这样前端才能展示：

- 工具调用进行中状态
- 工具调用结果摘要
- 可点击动作按钮
- 动作确认文案

参考实现：

- `/Users/mac/Desktop/区块链/desktop/src/types/index.ts`
- `/Users/mac/Desktop/区块链/desktop/src/core/CopilotEngine.ts`

### 5.4 页面接入规范

每个核心页面必须按统一模式接入 Copilot。

#### 5.4.1 必须接入的页面

第一批至少包括：

- `/watchlist`
- `/stock`
- `/search`
- `/research`
- `/paper-trading`
- `/portfolio`
- `/risk`
- `/decision`

#### 5.4.2 每页必须实现的能力

- `usePageContext(...)` 或等价 Provider，实时上报页面上下文。
- `pageActionBus.register(route, handler)` 或等价注册机制，响应 AI 动作。
- 页面局部 `AskAiButton`，允许把表格行、卡片、图表区、详情摘要注入对话。
- 页面顶部或结果区提供“下一步动作”，与 AI 可触发动作保持一致。

#### 5.4.3 典型联动场景

必须支持以下高频链路：

- “打开宁德时代详情页并切到估值 tab”
- “把当前选股结果里市值最小的 10 只加入自选组”
- “基于当前股票生成统一决策并展开风险说明”
- “从研究页直接创建提醒或加入组合候选池”
- “在模拟交易页填入股票代码、账户、数量并生成执行计划”

参考页面接入方式：

- `/Users/mac/Desktop/区块链/desktop/src/pages/ContractAnalysis/index.tsx`
- `/Users/mac/Desktop/区块链/desktop/src/pages/PaperTrading/index.tsx`
- `/Users/mac/Desktop/区块链/desktop/src/pages/PersonalSpace/index.tsx`
- `/Users/mac/Desktop/区块链/desktop/src/components/AskAiButton.tsx`

### 5.5 重点工作流

#### 5.5.1 工作台首页重做

当前首页偏总览展示，建议重做为：

- 左侧全局导航：工作台、市场、研究、策略、交易、系统
- 中央首页内容：自选股摘要、最近浏览、关键异动、事件提醒、新闻摘要、今日任务
- 右侧常驻 Copilot：解释异动、打开详情、生成提醒、串联下一步操作

目标：

- 首页成为高频进入页，而不是一次性展示页。

#### 5.5.2 自选股工作台

新增或重构为独立核心页，必须支持：

- 分组
- 排序
- 自定义列
- 列字段保存
- 快捷加入提醒
- 快捷进入研究
- 快捷进入模拟交易
- 快捷进入统一决策
- 快捷 “Ask AI” 分析当前选中股票或当前分组

#### 5.5.3 条件选股工作台

新增独立页面，不再让搜索页承担选股器职责。

必须支持：

- 自然语言选股
- 条件组合选股
- 保存筛选器
- 导出结果
- 结果加入自选组
- 结果进入事件跟踪
- AI 可填充条件、解释条件、执行筛选并对结果排序

#### 5.5.4 事件与催化剂工作台

新增独立页面，承接：

- 公告
- 财报时间
- 分红送配
- IPO / 可转债
- 研报更新
- 重要宏观节点

目标：

- 从“资讯流”升级为“时间轴和催化剂管理”。

#### 5.5.5 执行中心

从模拟盘页拆出执行中心，承接：

- 合规检查
- TWAP / VWAP 执行计划
- 执行任务状态
- 软闸门警告
- 执行后复盘
- AI 可基于当前持仓、账户和风险约束生成执行计划

#### 5.5.6 绩效与归因中心

新增独立页，承接：

- 模拟盘绩效
- 组合绩效
- 回测绩效
- 基准对比
- 风险收益拆解
- 归因结果

### 5.6 前端设计系统重构

#### 5.6.1 设计方向

从“玻璃态展示”改为“专业终端态”。

建议：

- 降低渐变和装饰强度，减少无意义的氛围层。
- 提供 `default / compact / dense` 三档密度。
- 提供 `table-first` 的高密度组件。
- 提供 `三栏工作台` 和 `可保存布局的多面板容器`。
- 统一金融色语义：
  - 涨跌
  - 风险等级
  - 数据 freshness
  - 缓存状态
  - 降级状态
- 页面风格从“长滚动营销页式 section”改成“终端式标题栏 + 状态条 + 数据主体”。
- AI 栏样式必须与主工作区是同一系统，不单独做成悬浮聊天窗风格。

#### 5.6.2 组件层新增

建议新增以下基础组件：

- `WorkbenchLayout`
- `WorkbenchLeftNav`
- `WorkbenchContent`
- `CopilotDock`
- `CopilotSessionSwitcher`
- `CopilotMessageList`
- `ActionCard`
- `ToolCallCard`
- `AskAiButton`
- `PageContextProvider / usePageContext`
- `PageActionBus`
- `ResizablePanelGroup`
- `StickyDataGrid`
- `WatchlistTable`
- `EventTimeline`
- `WorkspaceSwitcher`
- `SavedViewManager`
- `FreshnessTag`
- `DegradeBanner`

建议优先复用当前已有能力，而不是另起一套聊天前端：

- 以 `apps/web/store/chat-store.ts` 为基础，升级为 `CopilotStore`，补齐页面上下文、动作卡片、工具状态。
- 以 `apps/web/app/assistant/page.tsx` 为基础，拆出可复用的消息流、结果卡片和专家模式页面。
- 以 `apps/web/components/app-shell.tsx` 为基础，重构为真正的三栏工作台壳层。

## 6. 参考项目深度审查：前端与 AI 设计原理

本节基于参考项目 `/Users/mac/Desktop/区块链/desktop` 的实际实现，不做抽象猜测，只总结已经落地的设计。

### 6.1 参考项目的真实前端形态

参考项目不是“多页面网站 + 独立 AI 页面”，而是桌面工作台：

- 应用入口：`/Users/mac/Desktop/区块链/desktop/src/App.tsx`
- 三栏容器：`/Users/mac/Desktop/区块链/desktop/src/components/Layout/MainLayout.tsx`
- 布局样式：`/Users/mac/Desktop/区块链/desktop/src/components/Layout/MainLayout.css`
- Copilot 侧栏：`/Users/mac/Desktop/区块链/desktop/src/components/CopilotSidebar/index.tsx`

实际结构：

- 左栏：工作区导航，可折叠
- 中栏：当前页面工作区
- 右栏：应用级常驻 Copilot

关键点不是“右边有聊天框”，而是：

- Copilot 属于应用壳层，不属于任何具体页面
- 页面切换时 Copilot 不消失
- 左栏折叠、右栏展开会共同影响主工作区宽度分配
- 响应式策略优先保护中间主内容，其次压缩左右两栏

这意味着它的设计目标是 `工作台协作`，不是 `内容浏览`。

### 6.2 AI 的实现链路

参考项目的 AI 不是简单的 `input -> fetch -> markdown`，而是四层结构：

1. `CopilotStore`
2. `CopilotEngine`
3. `PageContext`
4. `PageActionBus`

对应文件：

- 状态层：`/Users/mac/Desktop/区块链/desktop/src/stores/copilot.ts`
- Hook 层：`/Users/mac/Desktop/区块链/desktop/src/hooks/useCopilot.ts`
- 引擎层：`/Users/mac/Desktop/区块链/desktop/src/core/CopilotEngine.ts`
- 页面上下文：`/Users/mac/Desktop/区块链/desktop/src/hooks/usePageContext.ts`
- 页面动作总线：`/Users/mac/Desktop/区块链/desktop/src/core/PageActionBus.ts`
- 类型定义：`/Users/mac/Desktop/区块链/desktop/src/types/index.ts`

实现逻辑：

- 用户在右栏输入问题，或点击页面里的 `Ask AI`
- `useCopilot` 读取当前 `PageContext`
- `CopilotEngine` 把 `系统提示词 + 页面上下文 + 当前页工具集 + 客户端动作工具` 一并发给模型
- 模型返回后，引擎把返回内容拆成：
  - 文本
  - MCP 工具调用
  - 客户端动作按钮
- MCP 工具立即执行
- 客户端动作只渲染按钮，不直接执行
- 用户点击动作按钮后，才通过 `PageActionBus` 触发页面联动

这套设计说明参考项目的 AI 不是“会话机器人”，而是 `可解释、可协作、可执行页面动作的副驾驶系统`。

### 6.3 AI 的结构化输出设计

参考项目最重要的设计不是聊天样式，而是输出协议：

```ts
type AiAction = {
  type: 'navigate' | 'fill' | 'execute';
  label: string;
  target: string;
  payload?: Record<string, unknown>;
  action?: string;
  confirmText?: string;
};
```

客户端工具定义在：

- `/Users/mac/Desktop/区块链/desktop/src/core/clientTools.ts`

只有三类动作：

- `navigate_page`
- `fill_form`
- `execute_action`

这表示参考项目没有采用“AI 直接操作 DOM”的做法，而是采用：

- 页面语义级动作
- 用户可见动作按钮
- 高风险操作二次确认

这是正确方向，原因有三点：

- 可控，不会出现模型直接乱点页面
- 可测试，动作是稳定协议，不依赖 DOM 结构
- 可维护，页面只需要实现动作解释器，不需要向 AI 暴露内部组件细节

### 6.4 AI 如何按页面裁剪能力

参考项目虽然是全局常驻 Copilot，但它不是“全应用无边界代理”。

工具裁剪逻辑在：

- `/Users/mac/Desktop/区块链/desktop/src/core/toolGroups.ts`

实现原则：

- 每个页面有自己的 MCP 工具白名单
- 某些页面 tab 还有更细粒度的工具白名单
- 当前页面只把相关工具发给模型

设计意义：

- 降低模型误调用无关工具的概率
- 让回答更贴近当前任务
- 控制提示词和工具上下文长度

对当前股票项目，这个原则必须保留。股票工作台不能把 `145` 个 MCP 工具无差别全部暴露给右栏 AI。

### 6.5 AI 如何配置

参考项目的配置入口在：

- `/Users/mac/Desktop/区块链/desktop/src/pages/Settings/index.tsx`
- `/Users/mac/Desktop/区块链/desktop/src/core/llmConfig.ts`
- `/Users/mac/Desktop/区块链/desktop/src/stores/settings.ts`
- `/Users/mac/Desktop/区块链/desktop/src/core/credentials.ts`

配置内容不是绑定某一家模型厂商，而是 `OpenAI 兼容接口`：

- `baseUrl`
- `apiKey`
- `model`

配置流程：

1. 用户在 Settings 填写 LLM 服务地址、Key、模型名
2. 前端可主动测试连接
3. 前端可主动读取可用模型列表
4. 保存后，Copilot 和页面内深度 AI 分析优先使用该配置

安全设计：

- 桌面态优先走 Tauri 安全存储
- 正式态避免退化到本地不安全存储
- web fallback 仅用于开发调试

运行模式：

- `configured`
- `mock`
- `unavailable`

这个状态会直接显示在 Copilot 顶部，用户一眼就知道当前是不是接了真实模型。

### 6.6 AI 如何使用

参考项目提供三种使用方式：

1. `右栏全局连续对话`
2. `页面局部 Ask AI`
3. `页面专用 AI 视图`

对应实现：

- 全局对话：`/Users/mac/Desktop/区块链/desktop/src/components/CopilotSidebar/*`
- 局部入口：`/Users/mac/Desktop/区块链/desktop/src/components/AskAiButton.tsx`
- 页面接入示例：
  - `/Users/mac/Desktop/区块链/desktop/src/pages/ContractAnalysis/index.tsx`
  - `/Users/mac/Desktop/区块链/desktop/src/pages/PaperTrading/index.tsx`
  - `/Users/mac/Desktop/区块链/desktop/src/pages/ChainMonitor/index.tsx`
  - `/Users/mac/Desktop/区块链/desktop/src/pages/PersonalSpace/index.tsx`

使用原理：

- `Ask AI` 不是简单打开聊天框
- 它会把当前卡片、当前统计块、当前账户、当前策略等局部对象注入 AI 上下文
- 用户不需要重复描述“我在看什么”

这正是 AI 工具体验与传统聊天页的本质区别。

### 6.7 页面如何接入 AI

参考项目里的页面接入模式非常统一：

1. 页面启动时 `usePageContext(...)`
2. 页面挂载时 `pageActionBus.register(route, handler)`
3. 局部卡片或表格挂 `AskAiButton`

这套模式的价值在于：

- 页面只负责暴露自己的上下文
- 页面只负责解释自己能执行的动作
- AI 层不需要知道页面内部状态机和组件结构

对股票项目，这意味着每个核心页都要有自己的：

- `page id`
- `current tab`
- `selected stock`
- `filters`
- `summary`
- `action handlers`

### 6.8 参考项目的设计原理

综合来看，参考项目的 AI 设计原理不是“把大模型接进页面”，而是以下五条：

1. `AI 是应用协作层，不是独立工具页`
2. `AI 驱动页面语义动作，不驱动 DOM`
3. `页面拥有上下文解释权，AI 只发意图`
4. `高风险动作必须用户确认`
5. `工具权限必须按页面裁剪`

对当前股票项目，这五条应直接继承。

### 6.9 不建议照搬的部分

虽然参考项目的 AI 架构值得复用，但有些实现不适合原样复制到股票工作台：

1. `视觉风格`
   - 参考项目整体是 glassmorphism + Fluent 风格，偏桌面安全产品
   - 股票终端应改成更高密度、更稳定、弱装饰的金融工作台风格

2. `PageContext 依赖项过窄`
   - 当前 `usePageContext` 主要监听 page/address/chain/currentTab
   - 对 `selectedItem`、`data` 等变化更新不够严格
   - 股票项目应把 `stockCode / filters / selected rows / summary` 全部纳入上下文刷新依赖

3. `PageActionBus 使用固定延迟触发动作`
   - 参考实现导航后固定等待 `100ms` 再执行动作
   - 对重页面、懒加载页面存在竞态风险
   - 股票项目应改为 `route ready / page mounted / handler ready` 后再派发

4. `配置边界不够清晰`
   - Settings 中的 AI 保存会触达后端 `/api/v1/llm/backend-config`
   - 对单用户桌面产品可接受
   - 对多用户股票工作台，应明确区分：
     - 用户私有 AI 配置
     - 平台默认 AI 配置
     - 租户级 AI 配置

5. `消息渲染顺序偏结果导向`
   - 当前消息按 `tool_call -> text -> action` 重组
   - 更利于稳定展示
   - 但会损失原始时序
   - 股票项目若强调“分析过程透明”，可考虑增加时间线式工具调用视图

### 6.10 对当前股票项目的直接启示

当前股票项目在前端上不应只“参考三栏式布局”，而应完整参考以下模式：

- 三栏应用壳层
- 常驻 Copilot
- 按页面裁剪工具
- `PageContext`
- `PageActionBus`
- 局部 `Ask AI`
- 结构化动作按钮
- 高风险动作确认

同时要明确做两项本地化改造：

1. 从“合约分析 / 风控 / 链监控”语义切换为“行情 / 研究 / 决策 / 执行 / 复盘”语义
2. 从“桌面安全产品视觉”切换为“金融终端高密度视觉”

## 7. 股票项目前端 AI 落地实施清单

本节把上面的设计原则转成可执行改造项，目标是让团队可以直接按模块拆任务。

边界说明：

- 本章只定义 `前端 AI 工作台` 本身应该如何落地。
- 涉及 BFF 门面和 MCP 能力编排的详细设计，分别放到 `第 8 章` 和 `第 9 章`。
- 前端章节不再重复定义完整后端接口清单，只保留依赖边界和接入要求。

### 7.1 第一层：应用壳层改造

目标是先把当前 Web 站点改成工作台壳层，而不是先改单个业务页。

需要改造：

- `apps/web/app/layout.tsx`
  - 增加应用级 `WorkbenchProvider`
  - 挂载全局 `CopilotDock`
  - 统一处理鉴权、全局通知、右栏会话状态
- `apps/web/components/app-shell.tsx`
  - 从当前分组导航壳层升级为三栏布局
  - 支持左栏折叠、右栏展开收起、中栏自适应
- `apps/web/app/globals.css`
  - 建立三栏工作台 token
  - 建立密度模式
  - 建立 Copilot、表格、状态条统一视觉变量

完成标准：

- 所有业务路由都运行在同一工作台壳层下
- Copilot 在页面切换时保持常驻
- 左右栏变化不会破坏中栏主内容可读性

### 7.2 第二层：Copilot 基础设施

目标是把当前聊天能力升级成工作台 Copilot，而不是继续扩展独立 `/chat` 页。

建议新增：

- `apps/web/store/copilot-store.ts`
- `apps/web/lib/copilot-engine.ts`
- `apps/web/lib/copilot-message-items.ts`
- `apps/web/lib/page-action-bus.ts`
- `apps/web/hooks/use-page-context.ts`
- `apps/web/hooks/use-copilot.ts`
- `apps/web/hooks/use-ask-ai.ts`
- `apps/web/components/workbench/copilot-dock.tsx`
- `apps/web/components/workbench/copilot-message-list.tsx`
- `apps/web/components/workbench/tool-call-card.tsx`
- `apps/web/components/workbench/action-card.tsx`
- `apps/web/components/ask-ai-button.tsx`

当前可复用来源：

- `apps/web/store/chat-store.ts`
- `apps/web/components/chat-message.tsx`
- `apps/web/lib/chat-api.ts`
- `apps/web/app/assistant/page.tsx`

需要补齐的能力：

- 会话列表
- 页面上下文缓存
- 结构化消息项
- 工具调用状态
- 动作按钮
- 执行动作确认
- 会话持久化

完成标准：

- 右栏不再只是纯文本消息流
- 每条 assistant 消息可同时包含文本、工具状态、动作按钮
- 会话切换、清空、恢复都可稳定工作

### 7.3 第三层：PageContext 协议

目标是让 AI 始终知道用户当前在看什么、筛了什么、选了什么。

建议统一上下文模型：

```ts
type StockPageContext = {
  page: string;
  currentTab?: string;
  stockCode?: string;
  stockCodes?: string[];
  filters?: Record<string, unknown>;
  selectedItem?: unknown;
  selectedRows?: unknown[];
  summary?: Record<string, unknown>;
};
```

每类页面必须上报：

1. `watchlist`
   - 当前分组
   - 当前排序
   - 当前列配置
   - 当前选中股票
2. `stock`
   - 当前股票
   - 当前 tab
   - 当前指标摘要
3. `search`
   - 当前条件
   - 当前结果数量
   - 当前选中结果
4. `research`
   - 当前股票或主题
   - 当前时间线筛选
   - 当前选中文档
5. `portfolio / paper-trading / risk`
   - 当前账户
   - 当前持仓
   - 当前任务或告警对象

完成标准：

- 页面内任何关键选择变化后，Copilot 获取到的上下文同步更新
- AI 不需要用户重复输入“我现在看的是哪只股票”

### 7.4 第四层：PageActionBus 协议

目标是让 AI 可以稳定驱动业务页动作，但不直接控制 DOM。

建议动作类型保持三类：

- `navigate`
- `fill`
- `execute`

但实现上要比参考项目更严格：

1. 不使用固定 `100ms` 延迟派发
2. 采用 `route ready + page mounted + handler ready` 后再执行
3. 所有 `execute` 动作都支持：
   - 风险等级
   - 确认文案
   - 执行来源
   - trace id

建议新增动作总线能力：

- `register(route, handler)`
- `unregister(route)`
- `dispatch(action)`
- `dispatchWhenReady(route, action)`
- `reportRouteReady(route)`

完成标准：

- AI 导航后，页面动作稳定送达，不依赖渲染时机碰运气
- 所有执行类动作都可追踪来源和确认记录

### 7.5 第五层：首批页面接入矩阵

应按业务价值而不是按页面数量推进。

P0 首批页面：

1. `apps/web/app/page.tsx`
   - 首页工作台
   - 接入右栏 Copilot
   - 支持从摘要卡片直接 `Ask AI`
2. `apps/web/app/watchlist/page.tsx`
   - 支持选中股票、分组、列配置上下文
   - 支持 AI 加提醒、进研究、进决策
3. `apps/web/app/stock/page.tsx`
   - 支持切 tab、切股票、生成统一决策、加入组合候选
4. `apps/web/app/search/page.tsx`
   - 支持 AI 填选股条件、执行筛选、解释结果
5. `apps/web/app/research/page.tsx`
   - 支持 AI 聚合研报、公告、事件，并生成后续动作

P1 第二批页面：

1. `apps/web/app/paper-trading/page.tsx`
2. `apps/web/app/portfolio/page.tsx`
3. `apps/web/app/risk/page.tsx`
4. `apps/web/app/decision/page.tsx`
5. `apps/web/app/alerts/page.tsx`

完成标准：

- 首批页面都具备 `PageContext + PageActionBus + AskAiButton`
- 右栏可以跨这些页面连续协作

### 7.6 第六层：AI 入口收敛

当前股票项目已有多个 AI 入口：

- `/assistant`
- `/chat`
- 可能还有页面内局部诊断面板

应调整为统一分层：

1. `右栏 Copilot`
   - 默认 AI 入口
   - 承担连续协作
2. `/assistant`
   - 全屏专家模式
   - 承担长报告、统一决策详情、复杂对比
3. `/chat`
   - 评估后并入 Copilot 或降级为调试入口

完成标准：

- 用户不再困惑“到底去哪个 AI 页面”
- AI 入口从分裂状态收敛为一个主入口加一个专家模式入口

### 7.7 第七层：跨层配套要求

前端 AI 工作台要成立，必须有 BFF 和 MCP 的配套，但详细设计不在本章展开。

前端侧只约束三类依赖边界：

1. `Copilot 会话与消息`
   - 前端需要稳定的会话读写接口
   - 返回结构必须支持 `text + toolCalls + actions + items`
2. `页面 ViewModel`
   - 前端需要工作台级聚合接口，而不是继续消费零散工具响应
3. `MCP 工具暴露策略`
   - 前端 AI 只能拿到按页面裁剪后的工具集合
   - 不直接依赖底层全部工具 schema

详细设计位置：

- BFF 详见 `第 8 章`
- MCP 详见 `第 9 章`

完成标准：

- 前端不再自己拼装底层业务语义
- 前端与后端的契约收敛为 `Copilot 协议 + ViewModel 协议 + 动作协议`

### 7.8 第八层：执行交互与审计要求

股票项目比参考项目更需要把执行风险边界写死。

前端层面必须做到：

- 所有 `execute` 动作都带风险级别
- 高风险动作默认二次确认
- 用户确认后记录：
  - 用户 id
  - 当前页面
  - 股票代码
  - 动作名
  - 参数
  - 时间
  - trace id
- 前端只发起执行申请，不直接绕过后端合规链路

完成标准：

- AI 不能直接绕过风控做动作
- 所有动作都有审计链路

### 7.9 第九层：测试清单

至少补齐以下测试：

1. 壳层测试
   - 左栏折叠
   - 右栏展开
   - 中栏宽度自适应
2. Copilot 测试
   - 会话创建
   - 会话恢复
   - 结构化消息渲染
   - 动作按钮渲染
3. PageContext 测试
   - 切换股票后上下文更新
   - 切换 tab 后上下文更新
   - 切换筛选条件后上下文更新
4. 动作总线测试
   - 导航后动作可靠送达
   - 执行类动作确认后再执行
5. 业务联动测试
   - `search -> watchlist`
   - `stock -> decision`
   - `research -> alerts`
   - `portfolio -> execution`

验收标准：

- 右栏 Copilot 不是“看起来能用”，而是关键链路都被自动化覆盖

### 7.10 第十层：推荐实施顺序

建议按以下顺序推进：

1. 先改壳层和 Copilot 基础设施
2. 再改 `watchlist / stock / search / research`
3. 再做 `portfolio / paper-trading / risk / alerts`
4. 最后整合 `/assistant`、专家模式、会话持久化和 saved workspace

原因：

- 先建立 AI 工作台基础骨架，后面页面接入才不会返工
- 先覆盖研究闭环，再覆盖执行闭环，价值最大

---

## 8. BFF 重构方案

### 8.1 BFF 重构目标

把当前 BFF 从“模块很多的接口层”重构成“产品语义清晰的门面层”。

本章承接 `7.7 跨层配套要求`，只展开 BFF 该提供什么，不重复前端接入细节。

### 8.2 API 分层建议

建议把接口分成三层：

1. `tool facade`
   - 对 MCP 单能力做标准封装
2. `view model facade`
   - 面向页面的聚合接口
3. `workflow facade`
   - 面向任务流的接口

### 8.3 新增或强化的 BFF 门面

#### 8.3.1 Workbench API

新增：

- `GET /workbench/overview`
- `GET /workbench/watchlist-feed`
- `GET /workbench/tasks`
- `GET /workbench/insights`

#### 8.3.2 Screener API

在现有 `/v1/screener` 基础上增加产品语义：

- 保存筛选条件
- 查询历史筛选
- 将结果加入 watchlist / portfolio candidate list
- 结果摘要与统计

#### 8.3.3 Event API

新增统一事件门面：

- `GET /events/calendar`
- `GET /events/by-code`
- `GET /events/important`
- `POST /events/subscribe`

#### 8.3.4 Performance API

新增：

- `GET /performance/portfolio`
- `GET /performance/paper-trading`
- `GET /performance/backtest`
- `GET /performance/attribution`

#### 8.3.5 Execution API

从模拟盘中抽离执行域：

- `POST /execution/check-compliance`
- `POST /execution/plan`
- `GET /execution/tasks`
- `GET /execution/tasks/:id`

### 8.4 路由规范化

建议统一为：

- 公共业务路由：`/api/<domain>`
- 内部工具或平台能力：`/api/platform/<domain>`

避免继续增加混合风格的 `/v1/*` 与非 `/v1/*` 并存。

---

## 9. MCP 产品化方案

### 9.1 MCP 重构目标

MCP 不需要再追求工具数量增长，重点是把已有能力转化为明确产品对象。

本章承接 `7.7 跨层配套要求`，只展开 MCP 该如何向工作台和 Copilot 暴露能力。

### 9.2 优先产品化的能力

#### 9.2.1 选股能力

优先承接：

- `screener_manager`
- `semantic_stock_search`
- `parse_selection_query`
- `search_similar_stocks`

产出对象：

- `screen result`
- `saved screener`
- `candidate list`

#### 9.2.2 事件能力

优先承接：

- `event_manager`
- `get_stock_notices`
- `get_ipo_info`
- `get_market_news`
- `get_stock_news`

产出对象：

- `event calendar`
- `event timeline`
- `event subscription`

#### 9.2.3 绩效能力

优先承接：

- `performance_manager`
- `risk_manager`
- `portfolio_manager`
- `paper_trading_manager`

产出对象：

- `performance snapshot`
- `attribution report`
- `risk-adjusted report`

#### 9.2.4 执行能力

优先承接：

- `execution_manager`
- `compliance_manager`

产出对象：

- `execution task`
- `soft gate warning`
- `compliance result`

#### 9.2.5 技能能力

优先承接：

- `list_skills`
- `run_skill`

产出对象：

- `skill catalog`
- `task template`
- `automation-ready workflow`

---

## 10. 分阶段实施计划

本章把 `第 7 章前端实施清单`、`第 8 章 BFF 配套`、`第 9 章 MCP 配套` 合并为时间顺序明确的推进计划。

### 10.1 P0：工作台骨架重构

目标：

- 建立 `三栏工作台 + 常驻 Copilot` 骨架。
- 补齐最缺的高频入口。

对应章节：

- 前端：`7.1 - 7.6`
- BFF：`8.3.1` 中与工作台首页和会话能力直接相关的门面
- MCP：`9.2.1 - 9.2.2` 中与选股和事件相关的高频承接

交付：

- 重构 `AppShell` 为三栏式工作台壳层。
- 新增 `CopilotDock / CopilotStore / PageContext / PageActionBus / AskAiButton` 基础设施。
- 重做首页为工作台首页。
- 重做自选股页为核心工作台。
- 新增条件选股页。
- 新增事件日历页。
- 将 `/assistant` 调整为全屏专家模式。
- 调整一级导航。
- 统一空状态、错误状态、freshness 和来源标识。

验收标准：

- 用户能从首页完成“看自选 -> 看事件 -> 看研究 -> 设提醒”闭环。
- 用户能从选股页完成“选股 -> 加入自选 -> 查看详情”闭环。
- 用户能在右侧 Copilot 中直接触发页面导航、填参与执行动作。
- 高价值页面都具备清晰下一步动作。

### 10.2 P1：执行、绩效、研究中台产品化

目标：

- 把已有高级能力拉到前台。

对应章节：

- 前端：`7.5 - 7.8`
- BFF：`8.3.3 - 8.3.5`
- MCP：`9.2.2 - 9.2.4`

交付：

- 新增执行中心。
- 新增绩效与归因中心。
- 研报页升级为研究事件台。
- 策略超市强化用户消费语义。
- BFF 增加 workbench / event / performance / execution 门面。
- 第一批核心页全部接入 `PageContext + PageActionBus`。

验收标准：

- 用户能从统一决策或模拟盘自然进入执行中心。
- 用户能查看组合或策略的绩效和归因。
- 研究页能按时间线组织公告、研报和新闻。
- AI 能跨 `watchlist / stock / research / paper-trading / risk` 连续协作。

### 10.3 P2：工作区和高级协同

目标：

- 形成真正的终端体验。

对应章节：

- 前端：`7.9 - 7.10`
- BFF：继续补全工作流门面和统一契约
- MCP：继续从“可调用工具”升级到“稳定产品对象”

交付：

- 保存布局与多面板工作区。
- 技能中心与任务模板中心。
- 跨页 workspace state。
- 统一 saved views。
- 多终端同步工作区配置。
- Copilot 会话与工作区状态联动同步。

验收标准：

- 用户可以保存自己的盯盘布局和研究布局。
- 用户可以把选股、研究、决策、执行串成固定模板。

---

## 11. 模块改造映射

### 11.1 前端重点文件

- `apps/web/app/layout.tsx`
  - 接入全局三栏工作台骨架和应用级 Copilot 挂载点。
- `apps/web/components/app-shell.tsx`
  - 从现有分组导航重构为三栏工作台壳层。
- `apps/web/store/chat-store.ts`
  - 升级为 `CopilotStore` 或拆分出 `copilot-store.ts`，补齐会话、页面上下文、动作卡片、工具状态。
- `apps/web/app/assistant/page.tsx`
  - 改为全屏专家模式，不再作为唯一 AI 入口。
- `apps/web/components/chat-message.tsx`
  - 升级为支持 `text / tool_call / action` 的结构化消息渲染。
- `apps/web/components/global-overlays.tsx`
  - 评估并整合 Copilot 的全局挂载、通知和动作反馈。
- `apps/web/store/stock-context.ts`
  - 作为 `PageContext.stockCode` 的基础来源之一。
- `apps/web/app/page.tsx`
  - 重做首页工作台。
- `apps/web/app/watchlist/page.tsx`
  - 升级为核心工作台。
- `apps/web/app/stock/page.tsx`
  - 升级为详情工作台，支持 tab 切换和 AI 动作接入。
- `apps/web/app/search/page.tsx`
  - 从“搜索页”拆分为“选股工作台 + 相似搜索辅助页”。
- `apps/web/app/research/page.tsx`
  - 升级为研究事件台。
- `apps/web/app/paper-trading/page.tsx`
  - 精简为模拟交易视图，执行能力外移，同时接入 AI 执行动作。
- `apps/web/app/risk/page.tsx`
  - 与绩效中心联动。
- `apps/web/app/decision/page.tsx`
  - 提升导航权重。
- `apps/web/app/globals.css`
  - 重做三栏布局 token、密度体系和 Copilot 样式变量。
- `apps/web/components/workbench/*`
  - 新增工作台骨架、Copilot 栏、会话切换、动作卡片等前端基础设施。
- `apps/web/hooks/use-page-context.ts`
  - 新增页面上下文注册钩子。
- `apps/web/lib/page-action-bus.ts`
  - 新增页面动作总线。
- `apps/web/components/ask-ai-button.tsx`
  - 新增局部对象注入式 AI 入口。

### 11.2 BFF 重点模块

- `apps/bff/src/app.module.ts`
  - 保持模块结构，但新增 workbench / event / performance / execution 门面模块。
- `apps/bff/src/chat/*`
  - 从通用聊天接口升级为支持结构化动作、工具状态、页面上下文输入的 Copilot 门面。
- `apps/bff/src/assistant/*`
  - 与右侧 Copilot 共享统一决策、诊断、解释能力，不再只服务独立 `/assistant` 页面。
- `apps/bff/src/screener/*`
  - 从工具接口升级到产品接口。
- `apps/bff/src/research/*`
  - 增加事件时间轴语义。
- `apps/bff/src/paper-trading/*`
  - 拆分 execution 相关逻辑。
- `apps/bff/src/strategy/*`
  - 强化订阅、持仓、策略关联视图。
- `apps/bff/src/skills/*`
  - 为前端技能中心提供目录、分组和模板语义。

### 11.3 MCP 重点承接

- `screening`
- `event/news`
- `performance`
- `execution`
- `compliance`
- `skills`

---

## 12. 非目标

本轮重构不建议把重点放在以下方向：

- 再增加大量独立分析页。
- 继续扩展零散工具入口。
- 优先做炫技型视觉改版而不改工作流。
- 把实盘交易接入作为当前阶段核心目标。

当前优先级应明确放在：

- `工作台`
- `选股`
- `事件`
- `执行`
- `绩效`

---

## 13. 最终建议

如果只做一轮重构，建议严格按下面顺序执行：

1. 先重构 `AppShell`，建立 `三栏工作台 + 常驻 Copilot + PageContext / PageActionBus` 骨架。
2. 再重做首页、自选股、个股详情、选股页，建立 `watchlist-first` 主流程。
3. 新增事件日历、执行中心、绩效中心，补齐研究到交易的闭环。
4. 统一 `/assistant`、`/chat`、右侧 Copilot 的能力分层，避免 AI 入口继续分裂。
5. 最后再做 saved workspace、skills center 和高级布局。

一句话概括：

当前系统应该停止继续堆“更多页面”，转向重构为一个以 `三栏工作台 + 常驻 AI 协作` 为基础、以 `自选 / 研究 / 决策 / 执行 / 复盘` 为主线的股票工作台。
