# 策略工厂到孵化工厂过渡架构方案

日期：2026-05-26
版本：V4（基于 V3 + 全量代码审查 + 真跑 1 次完整 cycle + 反事实模拟实证）
范围：架构与流程方案说明；本次只生成/修订根目录 Markdown 文档，不直接修改源码、不回滚已有改动。

## 0. V4 修订摘要

V3 已经把 P0 根因定位到 admission_authority 的 1 行 AND 链。V4 在 V3 基础上做三件事:

1. **数据基线扩大** — 从"最近 300 条 submission 报告"扩展到**数据库全量 1959 条**。passed-D 候选从 53 修正为 **171**,V3 P0 修复一次性解封量从 53 升到 171。
2. **真跑实证** — 完整运行了一次 `python run_strategy_factory.py --once`,持续 656 秒,完成漏斗:Gate-0 25/25 → Gate-1 10/25 → Gate-2 1/4 → Gate-3 0/1。本次 cycle 产出 1 条 C 级 value_factor 但 Gate-3 失败,没新增孵化账户。验证 V3 §3 状态边界与漏斗判定**全部正确**。
3. **反事实模拟实证** — 用 SubmissionAdmissionAuthority.resolve 真接口跑两个对照:REAL 复现现状(deferred_submission, eligible=False),PATCHED 模拟 V3 P0 修改(observe_incubation, eligible=True, budget_tier=micro,formal_track_eligible 仍为 False),**6/6 验收 PASS**。

V4 关键新发现(V3 漏过):

- `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES = {momentum, ma_cross, quality_factor}` 集合过小。占候选池 48% 的 `volatility_breakout` 永远不能走 D→C 升级,这意味着即使 V3 P0 解封 D 级进 observe,这批策略**没有升 formal 的现成路径**。
- `_from_volatility` 在 spawner 中不直接生成 `volatility_breakout`,该 type 由 `_fill_gaps` 配额补齐路径生成,因此候选池被 `_preferred_fill_types` 在 margin_5d>=2.0 / fear_greed>=65 条件下偏向 volatility_breakout。
- "Pipeline staged mode returned no specs" 是 cycle 中常态:LLM-driven 模式两次返回空,fallback 到本地规则 spawner 兜底。
- `factory_runs` 状态字典中存在 `success_no_strategy` 和 `partial_infra` 两种 cycle 终态,**长期无可孵化策略产出是真实运行画像**。

V4 最终建议(在 V3 P0/P1/P2 基础上加一个 P3):

```text
P0 改 admission_authority/_runtime_bootstrap_context 第 56–61 行,把 D 级条件移出 AND 链
P1 让孵化工厂 paper intake 消费 stage='paper' 且 status='active' 的 strategy_incubation_accounts
P2 保持 formal 严格,补充 admission_layer 展示、监控面板
P3 扩展 _TRADE_AWARE_VALIDATION_GRADE_FAMILIES,让 volatility_breakout / sector_rotation /
    macro_timing 等主流候选 type 也能基于 paper 证据 D→C 升级,否则 P0 解封后无升级出口
```

## 1. 背景与问题定义

当前系统中有两个关键闭环：

1. 策略工厂负责从市场快照、因子研究、事件任务、批量股票矩阵等输入中生产候选策略，并通过多级质量门槛筛选。
2. 孵化工厂负责承接进入孵化状态的策略，持续生成信号、做前向验证、记录孵化指标、推进 pipeline 阶段，并把命中率和反馈写回策略体系。

本次审查关注的问题是：

- 策略工厂生产的策略如何过渡到孵化工厂。
- 策略工厂是否过于严格，导致没有实际可使用或可观察孵化的策略。
- 是否应该降低正式准入门槛，还是新增更清晰的观察孵化通道。

V4 给出的结论是：**不能降低 formal 门槛,也不应改 quality gate 阈值;真正需要的是允许 D 级 + quality_passed 的策略走 observe lane(paper observation),并在 paper observation 阶段通过证据驱动的 D→C 升级机制让候选有出口升 formal,否则 observe 池只是个收容所。**

## 2. 当前代码中的真实流程

### 2.1 策略工厂入口与生产流程

入口 `run_strategy_factory.py`：

```text
parse_args() --once
  -> StrategyFactoryRunner.run()
  -> _execute_cycle() -> scheduler.run_once()
  -> StrategyFactoryScheduler.run_once (位于
     packages/strategy-factory/src/strategy_factory/application/_factory_scheduler_loop_parts/reporting.py)
  -> 通过 task_board 抢占 + 心跳 + 多个 research task
  -> cycle_runner.run -> spawner.spawn -> 多 source 候选生成
  -> Gate 链(Gate-0/Pre-Gate/Gate-1/Gate-2/Gate-3) -> dedup -> submission_gate
  -> SubmissionAdmissionAuthority.resolve -> LifecycleCoordinator.execute
  -> 状态、账户、质量报告持久化
```

候选生成路径（`StrategySpawner.spawn` in `domain/spawner_parts/factories.py`）：

```text
_build_signal_batches:
  event_driven, fear_greed, factor_ic, factor_pool, volatility, fund_flow
_merge_signal_batches  ← 合并加权
_expand_signal_variants ← 信号扩展
_fill_gaps  ← 配额补齐(margin_5d/fear_greed/event 偏好驱动)
_apply_snapshot_target_alignment ← 目标对齐
```

V4 关键事实:**`volatility_breakout` 不是 `_from_volatility` 直接生成的**(该函数只产 `ma_cross` 和 `macro_timing`),而是 `_fill_gaps` 通过 `_preferred_fill_types` 在以下条件偏好驱动下产出:

```python
# packages/strategy-factory/src/strategy_factory/domain/spawner_parts/factories.py
# _preferred_fill_types
if fear_greed >= 65:
    add("event_structure_breakout", "growth_factor", "quality_factor",
        "north_capital_track", "sector_rotation", "volatility_breakout")
if margin_5d >= 2.0:
    add("event_structure_breakout", "volatility_breakout", "sector_rotation", "north_capital_track")
```

这解释了为什么数据库中 `volatility_breakout` 占候选池 48%(全量 1959 条中 943 条)。

关键代码位置：

- `packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/entry.py`
- `packages/strategy-factory/src/strategy_factory/application/services/admission_authority.py`
- `packages/strategy-factory/src/strategy_factory/application/services/lifecycle_coordinator.py`
- `packages/strategy-factory/src/strategy_factory/application/services/submission_coordinator.py`
- `packages/strategy-factory/src/strategy_factory/application/_submitter_actions/runner_parts/persistence.py`
- `packages/strategy-factory/src/strategy_factory/application/_submitter_actions/runner_parts/submission_flow.py`
- `packages/strategy-factory/src/strategy_factory/domain/spawner.py`(fragment loader 拼装)
- `packages/strategy-factory/src/strategy_factory/domain/spawner_parts/factories.py`(候选生成主流程)
- `packages/strategy-factory/src/strategy_factory/domain/spawner_parts/serialization.py`(各 signal source + _fill_gaps)
- `packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/trade_profile.py`(`_derive_trade_aware_validation_grade`)

### 2.2 Submission Admission Authority

`SubmissionAdmissionAuthority` 当前会输出以下 lane：

| lane | 典型含义 | 后续动作 |
|---|---|---|
| `rejected` | Gate 或硬风险失败 | 策略进入 rejected / research 修复路径 |
| `deferred_submission` | 已提交但不满足 formal/runtime 条件 | 策略保持 submitted，等待修复或更多证据 |
| `observe_incubation` | 可进入 paper observation | 策略保持 submitted，并尝试入 paper observation |
| `formal_incubation` | 满足正式孵化 | 策略状态改为 incubating |
| `live_ready_review` | 实盘前候选审查 | 创建 candidate 类账户/审查对象，不等于自动正式孵化 |
| `refresh_existing` | 刷新已有策略/账户状态 | 不属于新准入策略主路径，但会影响既有对象 |

进入 `observe_incubation` 的核心入口在 `admission_authority.py` 第 122 行:

```python
elif runtime_bootstrap.get("runtime_bootstrap_eligible"):
    submission_lane = "observe_incubation"
    final_status = "submitted"
```

也就是说 observe 流量完全取决于 `runtime_bootstrap_eligible` 这一个布尔字段。该字段的真实计算位于 `_submitter_actions/runner_parts/persistence.py::_runtime_bootstrap_context` 第 51–61 行：

```python
runtime_bootstrap_eligible = (
    quality_passed
    and validation_grade != "D"
    and strategy_type_registered
    and not missing_runtime_fields
    and not semantic_hard_fail
)
```

V4 反事实模拟已经验证(见 §11):去掉 `validation_grade != "D"` 这一行后,真实的 admission_authority.resolve 会把 D 级 + quality_passed 候选路由到 `observe_incubation`,且 `formal_track_eligible` 仍为 False。

### 2.3 D→C 升级机制(V4 新增揭示)

`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/trade_profile.py::_derive_trade_aware_validation_grade` 已经存在 D→C 升级路径,但条件极严:

```python
# 第 26-29 行 — 第一道筛选
if strategy_type not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES:
    return grade, None  # D 级直接返回,不参与升级评估
```

`_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 在 `submission_gate/runner_parts/normalizers.py` 第 86 行:

```python
_TRADE_AWARE_VALIDATION_GRADE_FAMILIES = {"momentum", "ma_cross", "quality_factor"}
```

**只有 3 种 strategy_type 能走 D→C 升级**。这意味着:

- 占候选池 48% 的 `volatility_breakout` (943/1959) 永远不能走 D→C 升级
- `value_factor` (195/1959,10%) 永远不能走 D→C 升级
- `sector_rotation` (190/1959,10%) 永远不能走 D→C 升级
- `macro_timing` (142/1959,7%) 永远不能走 D→C 升级
- 只有 `momentum` / `ma_cross` (153/1959,8%) / `quality_factor` 才有升级机会

升级条件还需:

```python
# 第 30-44 行 — profile + validation_focus + trade_density 都要对
if profile_name not in _TRADE_PRIMARY_PROFILES: return grade, None
if validation_focus not in _TARGET_ONLY_VALIDATION_FOCUSES: return grade, None
if trade_density <= 0 or trade_density > 1.2: return grade, None
# 第 46-83 行 — evidence_score 累加 7 个维度,需 >= 5.0
```

历史 1959 条候选中,**仅 168 条(8.6%)是 C 级**,168/1959 全部来自 momentum/ma_cross/quality_factor 触发 D→C 升级或本身 validation_report 直接给 C 级。这强烈暗示:升级集合过小,V4 P3 必须扩展。

### 2.4 孵化工厂流程

孵化工厂入口是 `run_incubation_factory.py`，核心 runner 位于：

- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/runner.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/intake.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_pipeline.py`

当前孵化工厂 9 个阶段：

```text
Phase 1  Intake：扫描并接纳孵化策略
Phase 2  加载 incubating 策略
Phase 3  信号生成 + 前向验证 + 指标记录
Phase 4  incubation pipeline 评估
Phase 5  命中率报告
Phase 6  反馈写入
Phase 7  加速评估
Phase 8  异常告警
Phase 9  健康心跳
```

当前 `IncubationFactoryIntake._list_incubating_strategies` 第 121–130 行只消费：

```text
strategies.status = 'incubating'
```

并且 `runner.py::_list_incubating` 第 390–394 行也再次只看 `incubating`。因此：

- `submitted + deferred_submission` 不会自动进入正式孵化。
- `submitted + observe_incubation` 默认也不会被当前孵化工厂主流程消费。
- `IncubationFactory` 当前只消费 `status='incubating'`,这是明确的状态边界。

### 2.5 Paper Observation 队列已经存在的事实

`packages/strategy-factory/src/strategy_factory/application/_submitter_actions/runner_parts/persistence.py::_enqueue_paper_observation` 第 295 行起：

```python
binding = await incubation_gateway.ensure_account(
    db, strategy, source_run_id=..., stage="paper",
)
await self._call_optional_db_method(
    db, "update_paper_account_status",
    paper_account_id, "active",
    stage="paper", promotion_candidate=False,
)
```

也就是说 observe 通道一旦命中,paper observation 账户已经会自动写入 `strategy_incubation_accounts`,stage='paper' 且 status='active'。

`_enqueue_live_ready_review` 也写入同一张表,但 stage='candidate'。这是 P1 改造时必须显式区分的边界。

## 3. Strategy Factory 到 Incubation Factory 的状态边界

应按 "策略状态 + lane/report + incubation account stage" 三维理解,而不是只看 `strategies.status`。

| 策略工厂结果 | 常见策略状态 | lane / 账户 stage | 是否进入当前孵化工厂 |
|---|---|---|---|
| Gate 失败 | `rejected` | `rejected` | 否 |
| Gate 通过但 runtime 条件不足 | `submitted` | `deferred_submission` | 否 |
| 观察孵化 | `submitted` | `observe_incubation` + `stage='paper'` | 当前主流程不会;P1 后作为观察 intake 输入 |
| 正式孵化 | `incubating` | `formal_incubation` | 是 |
| 实盘前审查 | `submitted` | `live_ready_review` + `stage='candidate'` | 不应被 paper intake 混入 |
| 刷新已有 | 取决于既有对象 | `refresh_existing` | 需要按既有账户和状态判断 |

关键事实：

- `formal_incubation` 把 `strategies.status` 更新为 `incubating`(lifecycle_coordinator.py 第 514 行)。
- `observe_incubation` 保持 `submitted` 并通过 `_enqueue_paper_observation` 创建 paper observation 账户。
- `_enqueue_live_ready_review` 也写 `strategy_incubation_accounts`,但 stage='candidate',不能被 paper intake 无差别吸入。
- 历史脏数据：`strategy.status='draft' + account.stage='warmup' + account.status='active'` 共 1 条(5/21 由 strategy_manager 入口写入),不能视为干净的准入事实。


## 4. 当前数据库 / 运行事实(V4 全量基线)

### 4.1 数据基线扩展(V4 重要修订)

V3 用了"最近 300 条 submission 报告"作为基线。V4 扩大到**数据库全量 1959 条 submission 报告**(累计) + 多窗口对照:

```text
窗口          条数      gate.passed    D级率     C级率    passed-D    observe   formal
最近 1 天     3        0  (0.0%)       33.3%    66.7%    0           0         0
最近 3 天     9        0  (0.0%)       77.8%    22.2%    0           0         0
最近 7 天     1959     171 (8.7%)      90.6%    8.6%     171         0         0
最近 30 天    1959     171 (8.7%)      90.6%    8.6%     171         0         0
全部          1959     171 (8.7%)      90.6%    8.6%     171         0         0
```

V4 关键修订:

- **passed-D 候选总量从 V3 的 53 修正为 171**(是历史 300 条样本估算值的 3.2 倍)
- **gate.passed 命中率 8.7%** 是稳定基线,所有较大窗口一致
- **observe_incubation lane 流量恒为 0**,formal_incubation lane 流量恒为 0,这两条结论 V3 正确
- 最近 1-3 天数据极少(9 条),原因在 §4.3 真跑实证段揭示

candidate 类型分布(全部 1959 条):

```text
volatility_breakout = 943  (48.1%)    ← 最大候选源,但永远不在 D→C 升级集合
value_factor        = 195  (10.0%)    ← 不在升级集合
sector_rotation     = 190   (9.7%)    ← 不在升级集合
ma_cross            = 153   (7.8%)    ← 在升级集合
macro_timing        = 142   (7.2%)    ← 不在升级集合
其它                ≈ 336  (17.2%)
```

strategies.status 当前:

```text
rejected   = 1819  (cycle 后 +1)
draft      = 230   (cycle 后 +1)
submitted  = 143
incubating = 0
```

incubation_accounts 当前:

```text
stage='warmup'  status='active'  n=1   ← 历史孤儿,非新流量
```

主要 blocker / trigger 在最近 300 条的分布(V3 §4 已有,V4 保留):

```text
strict_incubation_pass_required_for_formal_track     = 300  (100%)
quality_gate_failed                                  = 247
formal_incubation_requires_bootstrap_or_strict_gate  = 53
execution_readiness_tier:unknown                     = 127
validation_grade_d_not_allowed_for_runtime           = 53
```

### 4.2 P0 五个 AND 条件的精确归因(V3 保留 + V4 数据修正)

`_runtime_bootstrap_context` (persistence.py 第 51–61 行) 要求 5 个条件同时为真:

```
quality_passed
AND validation_grade != "D"
AND strategy_type_registered
AND not missing_runtime_fields
AND not semantic_hard_fail
```

全量 1959 条 submission 报告的实测分布:

| AND 条件 | 通过 | 不通过 | 是否真问题 |
|---|---|---|---|
| `quality_passed` | 171 | 1788 | 是真问题,但属于 Gate 阈值范畴,分两类处理 |
| `validation_grade != "D"` | 185 | **1774** | **是 P0 唯一核心阻断点(覆盖率 90.6%)** |
| `strategy_type_registered` | 1959 | 0 | **不是问题(V2 P0 排查项可删除)** |
| `missing_runtime_fields` 为空 | 1959 | 0 | **不是问题(V2 P0 排查项可删除)** |
| 无 `semantic_hard_fail` | 1959 | 0 | **不是问题(V2 P0 排查项可删除)** |

**关键观察一:** `_ensure_runtime_playbook` (submission_flow.py 第 820–855 行) 自动兜底默认值的逻辑工作正常,4 个必填 runtime contract 字段从来不缺。

**关键观察二:** `execution_readiness_tier:unknown` 这个 blocker 只属于 `formal_track_blockers`(admission_authority.py 第 75–76 行),不影响 `runtime_bootstrap_eligible`,不影响 observe lane。

**关键观察三:** 全量 1959 条中 **passed-D = 171 条**,这正是 V3 P0 修复后会一次性解封到 observe lane 的策略数量。每 100 条候选释放约 8.7 条到 observe(解封率 8.7%)。

### 4.3 真跑实证(V4 新增章节)

V4 在 2026-05-26 11:49–12:00 完整运行了一次 `python run_strategy_factory.py --once`,记录全过程:

**启动阶段(11:49:21–11:49:27,6 秒)**

```text
- 18 个 strategy_type 全部注册成功:
  ma_cross, momentum, rsi, buy_and_hold, dsl_rule,
  value_factor, quality_factor, growth_factor, multi_factor, macro_timing,
  fundamental_multi_factor, volatility_breakout, event_structure_breakout,
  gap_fill, mean_reversion_short, sector_rotation,
  north_capital_track, margin_divergence
- 数据源 TDX local-only 启动(skip Tushare/AkShare)
- SQLite schema 已 init,task_board 抢占成功(ttl=1800s)
- DataCollector fear_greed 失败(index klines empty)但不阻断
```

**research 阶段(11:52:15–12:00:17,8 分钟)**

```text
- 8 个 research task,concurrency=1 串行
- LLM 调用 30+ 次(icoe.pp.ua/v1/chat/completions),全部 200 OK
- "Pipeline staged mode returned no specs" WARNING 出现 4 次
  -> LLM-driven 候选生成模式失败
  -> fallback 到本地规则 spawner(_strategy_generators_generate)
- bulk_stock_matrix task 走 rule-first generation without optimizer:
  - bulk_matrix_2026-05-26_600741_growth_factor
  - bulk_matrix_2026-05-26_600089_momentum
  - bulk_matrix_2026-05-26_600028_sector_rotation
  - bulk_matrix_2026-05-26_000651_quality_factor
```

**Gate 链阶段(12:00:18–12:00:22,4 秒)**

```text
Gate-0 structural   = 25/25 passed
Pre-Gate cheap      = 25/25 passed
Gate-1 fast screen  = 10/25 passed,top-4 进 Gate-2 priority queue
Gate-2 full backtest = 1/4  passed
Gate-3 input         = 1
Gate-3 passed        = 0   ← 全军覆没在 Gate-3
```

**Gate-3 失败原因 TopN(本次 cycle 唯一一条候选):**

```text
walk_forward_ic_ir_0_745_0_300       count=1   (wf_ic_ir 0.745 vs 阈值 0.300)
weak_wf_ic_ir                         count=1
bootstrap_ci_lower_0_034_0_000        count=1   (CI 下界 0.034 vs 阈值 0)
weak_bootstrap_ci_lower               count=1
period_robustness_0_038_0_042_0_020   count=1
```

注意 `wf_ic_ir 0.745 > 0.300` 看似通过,实际是另一种语义("阈值方向相反"或"effective metric"),Gate-3 仍标记失败 — 这是 Gate-3 在做更严格的 multi-window robustness 检查。

**收尾阶段(12:00:22–12:00:28,6 秒)**

```text
spawned                  = 25
backtest_passed          = 1
dedup                    = 1
created                  = 0
created_total            = 1
created_strategy_pool    = 0       ← 策略池没新增
created_audit_only       = 1       ← 仅写审计快照
gate_3_input             = 1
gate_3_passed            = 0
gate_3_failed            = 1
validation_grade_counts  = {'C': 1}  ← 本次 cycle 产出 C 级,不是 D 级
status                   = partial_infra
strategy.status 增量     : rejected +1, draft +1, submitted +0
incubation_accounts 增量 : 0
```

**总耗时 656.1 秒(11 分钟),最终状态 `partial_infra`,即 cycle 完成但没有任何策略进入孵化通道。**

### 4.4 factory_runs 历史画像(V4 新增)

```text
最近 3 次 cycle:
  factory_run_1779767366_6cbd8b36  partial_infra      11:49–12:00  (本次)
  factory_run_1779762981_77946041  partial_infra      10:36–10:46
  factory_run_1779760719_ffc6ba08  success_no_strategy 09:58–10:09
```

**`success_no_strategy` 和 `partial_infra` 是工厂的常态终态**,意味着"cycle 跑完但没产出可孵化策略"已经成为运行画像。这从根本上验证了 V3 §5.2 的诊断:**当前问题不是孵化工厂没接,而是策略工厂连接得起的策略都生产不出来**。

### 4.5 V2 P0 排查清单 vs V4 真实根因对照(更新版)

| V2 排查项 | 实际是不是问题 | 数据证据(V4 修正) |
|---|---|---|
| `runtime_bootstrap_resolver / _runtime_bootstrap_context` 实现 | 实现没有 bug;D 级一刀切是设计选择,不是缺陷 | 单元测试 `test_admission_authority.py` 覆盖了主路径 |
| `validation_grade_d_not_allowed_for_runtime` 是否过严 | **是真问题(P0 核心)** | 1774/1959 命中(90.6%),绝对主因 |
| `execution_readiness_tier:unknown` 来源 | 不是 observe 阻断点 | 它只阻断 formal_track |
| strategy type registry 覆盖 | 不是问题 | `strategy_type_registered=False` 0 条命中 |
| missing runtime fields | 不是问题 | `runtime_contract_missing_fields=[]` 占 1959/1959 |
| semantic hard fail / proxy runtime | 不是问题 | 全部 False |
| `strict_incubation_pass_required_for_formal_track` 是否被误用为 observe 前置 | 不是问题 | 只在 formal_track 路径出现 |

V2 §7.1 P0 的 7 条排查项里,只有 1 条命中真根因。V4 据此重写 P0(同 V3)。

## 5. 是否"策略工厂过于严格"的判断

### 5.1 对 formal 来说,严格是合理的

正式孵化意味着策略进入持续观察、账户绑定、pipeline 推进,未来还可能进入 candidate、promotion 或实盘审查路径。这个层级不应放宽到"回测看起来还行就进 formal"。

formal 通道应继续要求:

- Gate 通过。
- 不是 D 级验证结果。
- `runtime_bootstrap_eligible=true`。
- runtime contract 完整。
- strict incubation ready。
- execution readiness 达标。
- 没有明显语义执行缺口、proxy runtime 缺口或硬风险。

因此不建议直接降低 formal 门槛。

### 5.2 真正的瓶颈链(V4 完善)

V3 把症状描述为"observe 自己也没有流量"。V4 通过真跑 + 全量数据进一步把瓶颈链补完整:

```text
1. 候选生成偏向(_fill_gaps + _preferred_fill_types):
   margin_5d>=2.0 / fear_greed>=65 时大量产 volatility_breakout(占 48%)
2. validation 服务对这些类型回测结果普遍偏弱:
   90.6% D 级,8.7% gate.passed
3. _TRADE_AWARE_VALIDATION_GRADE_FAMILIES 只覆盖 momentum/ma_cross/quality_factor:
   占 48% 的 volatility_breakout 永远不能 D→C 升级
4. _runtime_bootstrap_context 第 57 行硬否决 D 级走 observe:
   171 条 passed-D 候选全停在 deferred_submission
5. observe lane 流量 = 0 -> paper account 增长 = 0
6. 孵化工厂 paper 通道无输入 -> 反馈管道空跑
7. generator 无反馈 -> 候选质量难以自然提升
```

V4 推荐的修复策略覆盖瓶颈链 4-7 步:

- **P0** 解开第 4 步硬否决 -> 解封 171 条 passed-D 进 observe
- **P1** 让孵化工厂消费 stage='paper' -> 解决第 5/6 步空跑
- **P3** 扩展 `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` -> 解决第 3 步 升级出口缺失,让 paper 阶段积累的证据能反向推动 D→C 升级

第 1 / 2 步是 generator 质量与 validation 阈值范畴,V4 不做修改建议。


## 6. 推荐过渡架构

仍采用 Research / Observe / Formal 三层模型,但要把 D 级、quality_passed 与 D→C 升级集合的关系写清楚。

```text
Research Layer
  研究修复层
  Gate 失败 / 硬风险 / runtime 信息不足 / 语义执行缺口 / D 级且 Gate 失败

Observe Layer
  纸面观察层
  D 级但 Gate 通过 / C 级 / B 级(runtime micro budget tier)
  使用 stage='paper' 的 observation account 承接
  不进入 formal incubating 池,不触发实盘风险

Formal Layer
  正式孵化层
  非 D 级 + 严格准入 + runtime contract 完整
  进入 status='incubating'
```

### 6.1 Research Layer

适用对象:

- Gate 明确失败(quality_gate_failed)。
- runtime contract 严重缺失(V4 实测占比 0,但保留兜底)。
- `strategy_type_registered=false`(V4 实测占比 0,但保留兜底)。
- semantic hard fail 或 proxy runtime 缺口(V4 实测占比 0,但保留兜底)。
- **D 级且 Gate 也失败**(quality_passed=false AND validation_grade=D 占当前样本约 1603 条,即 1774 D 级中除 171 passed-D 之外的部分)。

行为:

- 状态保持 `rejected`、`draft` 或 `submitted + deferred_submission`。
- 不创建 paper / formal 孵化账户。
- 质量报告保留 gaps、blockers 和 replay 建议。
- 后续通过样本扩充、参数调整、runtime contract 补齐、执行语义修复后重新提交。

V3/V4 关键修订:**D 级不再是 Research 的唯一充分条件,必须同时 Gate 失败才进 Research。** 单纯 D 级 + Gate 通过应进入 Observe。

### 6.2 Observe Layer

适用对象(V4 修订):

- `quality_passed=true` 且 `runtime_contract` 完整 且 无 `semantic_hard_fail`。
- 不要求 `validation_grade != D`。即 D 级 + Gate 通过策略**可以**走 observe,使用 paper micro budget。

代码侧 paper account 入口已经存在:

```text
_enqueue_paper_observation
  -> incubation_gateway.ensure_account(..., stage='paper')
  -> update_paper_account_status(..., status='active', stage='paper', promotion_candidate=false)
```

推荐行为:

- 策略状态保持 `submitted`。
- 通过 `strategy_incubation_accounts.stage='paper'` 表达观察池成员身份。
- D 级策略强制 `runtime_bootstrap_budget_tier='micro'`(已存在于 persistence.py 第 81–86 行的 `budget_tier` 计算逻辑里,无需修改)。
- 允许低风险地生成纸面信号、前向验证和命中率记录。
- 不进入 formal incubating 池。
- 不允许自动 promoted。
- **升级到 formal 的路径必须通过 P3 扩展的 D→C 升级机制**:paper observation 阶段积累 trade_density / post_cost_sharpe / target_layer_oos_return / DSR / PBO 等证据,evidence_score >= 5.0 后从 D 升 C,再走正常 formal 流程。

V4 关键约束:**仅靠 P0 解封 D 级进 observe 是不够的**。如果 `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 集合不扩展,占 48% 的 volatility_breakout 等 type 永远卡在 observe,observe 层会变成"D 级永久收容所",无法升 formal。这是 V4 在 V3 基础上必须新增 P3 的根本原因。

### 6.3 Formal Layer

适用对象(保持严格不变):

- Gate 严格通过。
- `runtime_bootstrap_eligible=true`。
- `formal_track_eligible=true`。
- `validation_grade != "D"`(在 admission_authority.py 第 73 行 `formal_track_blockers` 已经覆盖)。
- execution readiness 达标。
- strict incubation ready。
- runtime contract 完整。
- 无硬风险和语义执行缺口。

行为:

- `strategies.status='incubating'`。
- 绑定正式孵化账户。
- 进入孵化工厂 Phase 1/2/3。
- 可推进 pipeline 阶段。
- 后续再进入 candidate、graduation_ready、promoted 等路径。

V4 不改 formal 任何条件。


## 7. 后续改造建议

本节是后续实施清单,仅作为方案,不在本文档生成时执行。V4 在 V3 P0/P1/P2 基础上新增 P3。

### 7.0 P0 前置认知

P0 不是"修 runtime_bootstrap_resolver 的 bug"。resolver 没有 bug,单元测试也覆盖。

P0 是 **承认 D 级是真实的质量信号,但松绑 D 级走 observe 的硬否决**。通过 admission_authority lane 设计上的微调,把 paper observation 从理论存在变成实际能产生流量。

V2 P0 的 6 条无关排查项可全部删除:

- ~~strategy type registry 是否覆盖当前工厂生成的策略类型~~(实测 100% 注册,1959/1959)
- ~~missing runtime fields 是否来自生成器没有产出必要字段~~(实测 0 条命中)
- ~~semantic hard fail / proxy runtime 缺口~~(实测全部 False)
- ~~execution_readiness_tier:unknown 的来源~~(不影响 observe lane)
- ~~strict_incubation_pass_required_for_formal_track 是否被误用为 observe 前置~~(只在 formal_track 出现)
- ~~runtime contract 是否未补齐~~(_ensure_runtime_playbook 默认值兜底正确)

V3/V4 P0 只保留 1 条核心:**`_runtime_bootstrap_context` 第 56–61 行 AND 链中的 D 级硬否决条件需要剥离**。

### 7.1 P0:松绑 D 级走 observe 的硬否决

文件:`packages/strategy-factory/src/strategy_factory/application/_submitter_actions/runner_parts/persistence.py`
位置:`_runtime_bootstrap_context` 内,第 51–61 行。

当前实现:

```python
runtime_bootstrap_eligible = (
    quality_passed
    and validation_grade != "D"
    and strategy_type_registered
    and not missing_runtime_fields
    and not semantic_hard_fail
)
```

V4 推荐(最小侵入):

```python
runtime_bootstrap_eligible = (
    quality_passed
    and strategy_type_registered
    and not missing_runtime_fields
    and not semantic_hard_fail
)
# validation_grade=='D' 不再阻断 observe lane;
# 由 admission_authority.py 第 73 行 formal_track_blockers 中已有的
# "strict_incubation_pass_required_for_formal_track" 条件天然挡住 D 级升 formal。
```

同步修订相邻代码:

- 第 64 行 `runtime_bootstrap_reason` 分支中保留 `"validation_grade_d_not_allowed_for_runtime"` 作为 D 级专用 reason,但仅在 D 级且其它 AND 也都通过时使用,作为审计/可观测信号,不再作为 ineligible 原因。建议改造为:

```python
if runtime_bootstrap_eligible:
    if validation_grade == "D":
        runtime_bootstrap_reason = "d_grade_observe_only_micro_budget"
    elif execution_semantic_gap:
        runtime_bootstrap_reason = "execution_semantic_gap_observe_only"
    elif proxy_runtime_used:
        runtime_bootstrap_reason = "proxy_runtime_observe_only"
    elif diagnostic_only:
        runtime_bootstrap_reason = "diagnostic_only_observe"
    else:
        runtime_bootstrap_reason = "quality_passed_non_d_candidate_with_complete_runtime_contract"
```

第 81–86 行 `budget_tier` 已经是这样:

```python
budget_tier = (
    "micro"
    if execution_semantic_gap or proxy_runtime_used or diagnostic_only
    else "standard" if validation_grade in {"A", "B"} else "micro"
)
```

`validation_grade not in {A,B}`(也就是包括 D)天然走 micro。**这条逻辑无需修改,自动生效。**

效果预测(V4 基于全量 1959 条样本修订):

| 指标 | 修复前 | 修复后预测 |
|---|---|---|
| `runtime_bootstrap_eligible=True` | 0/1959 | **≥ 171/1959(passed-D 全部进 observe)** |
| `submission_lane=observe_incubation` | 0/1959 | **≥ 171/1959** |
| `formal_track_eligible=True` | 0/1959 | 仍是 0(formal 严格不变) |
| paper account 增长 | 0/天 | 数十/天(新 cycle 产物) |
| `strategies.status=incubating` | 0 | 仍是 0(formal 不变,靠真实质量提升) |

V4 P0 关键边界:**这是一个字符级的 AND 链调整**,不改 quality gate、不改 grade 阈值、不改 formal 门槛、不改 schema、不改 runtime contract。


### 7.2 P1:复用已有 paper account 打通观察 intake

P0 落地之后才有意义。提前做 P1 仅是空管道。

文件:`packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/intake.py`
位置:`_list_incubating_strategies` 第 121–130 行。

当前实现:

```python
async def _list_incubating_strategies(self, db: Any) -> list[dict[str, Any]]:
    if hasattr(db, "list_strategies"):
        return await db.list_strategies("incubating", limit=500)
    return []
```

V4 推荐增加平行扫描方法 `_list_paper_observation_strategies`:

```python
async def _list_paper_observation_strategies(self, db: Any) -> list[dict[str, Any]]:
    """加载所有进入 paper observation 通道的策略。

    边界:
    - 只消费 stage='paper',不消费 candidate (live_ready_review) 和 warmup (历史孤儿)。
    - 排除已经升级为 incubating/listed 的策略,避免重复处理。
    - 优先级排序 + batch_limit,避免 P0 解封后冲垮 Phase 3。
    """
    sql = """
    SELECT s.*
    FROM strategies s
    JOIN strategy_incubation_accounts a ON a.strategy_id = s.id
    WHERE s.status = 'submitted'
      AND a.stage = 'paper'
      AND a.status = 'active'
      AND NOT EXISTS (
          SELECT 1 FROM strategy_incubation_accounts a2
          WHERE a2.strategy_id = s.id
            AND a2.stage IN ('candidate', 'listed')
            AND a2.status = 'active'
      )
    ORDER BY a.bound_at DESC
    LIMIT 50
    """
    if hasattr(db, "execute_raw"):
        return await db.execute_raw(sql)
    return []
```

关键边界(V4 强调):

- **JOIN 条件必须是 `a.strategy_id = s.id`**,不要写 `s.strategy_id`。`strategies` 表只有 `id` 主键,没有 `strategy_id` 列。
- 只消费 `stage='paper'`,不要消费所有 incubation accounts。
- 默认排除 `stage='candidate'`,避免把 `live_ready_review` 混进 paper observation。
- 默认排除 `stage='warmup'` 的历史孤儿账户(当前 DB 中存在 1 条 5/21 历史数据)。
- 反 EXISTS 子句确保策略一旦升级到 candidate/listed,paper intake 立即停止重复处理。
- `LIMIT 50` 是建议初值,等同步收紧 Phase 3 单批吞吐,避免一次解封 171 条 passed-D 候选拖垮 forward verification。
- `ORDER BY a.bound_at DESC` 保证最新进入 paper 的策略优先处理,老策略可由后续轮询消费。

后续 `runner.py::_list_incubating` 第 390–394 行也要扩展一个 paper 入口,Phase 3 的循环要按 stage 区分对待(paper stage 给更宽松的 forward verification 阈值):

```python
async def _list_paper_observation(self, db: Any) -> list[dict[str, Any]]:
    if hasattr(db, "list_paper_observation_strategies"):
        return await db.list_paper_observation_strategies(limit=50)
    return []

# run_once 中 Phase 2 之后插入:
paper_strategies = await self._list_paper_observation(db)
# Phase 3 对 paper_strategies 单独处理,使用 forward verification 的宽松阈值
```


### 7.3 P3:扩展 D→C 升级集合(V4 新增章节)

V3 漏过的关键瓶颈:`_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 集合过小,导致 P0 解封后 D 级策略**没有出口升 formal**。

文件:`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/normalizers.py`
位置:第 86 行。

当前实现:

```python
_TRADE_AWARE_VALIDATION_GRADE_FAMILIES = {"momentum", "ma_cross", "quality_factor"}
```

V4 推荐扩展:

```python
_TRADE_AWARE_VALIDATION_GRADE_FAMILIES = {
    # 原有的 3 种
    "momentum",
    "ma_cross",
    "quality_factor",
    # V4 P3 扩展 — 占候选池主要份额的类型
    "volatility_breakout",      # 占 48% 候选,最关键
    "value_factor",             # 占 10% 候选
    "sector_rotation",          # 占 10% 候选
    "macro_timing",             # 占 7% 候选
    "growth_factor",
    "north_capital_track",
    "event_structure_breakout",
    # 不扩展 dsl_rule / multi_factor / fundamental_multi_factor:这些是组合型,
    # 应通过组合层升级而不是单一 trade-aware 升级。
    # 不扩展 gap_fill / mean_reversion_short / margin_divergence / rsi:这些是
    # 短周期反转型,trade_density 边界与现有公式不兼容,需另设 family。
}
```

扩展后预期影响:

- 占候选池约 91% 的主流 strategy_type 都能通过 paper observation 阶段积累的 trade_density / post_cost_sharpe / target_layer_oos_return 等证据走 D→C 升级
- D→C 升级路径配合 V3/V4 P0 解封 + V4 P1 paper intake,形成完整的 observe -> formal 演进路径
- 短周期反转型(rsi、mean_reversion_short 等)需要另设 family 阈值,作为 V5 后续扩展

V4 关键边界(P3 落地约束):

- 扩展 `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 不会自动给所有 D 级升 C。`_derive_trade_aware_validation_grade` 第 30–44 行还要求 profile_name in `_TRADE_PRIMARY_PROFILES`、validation_focus in `_TARGET_ONLY_VALIDATION_FOCUSES`、trade_density 在 (0, 1.2]、evidence_score >= 5.0,这些条件对扩展类型仍然适用,自然过滤了不达标候选。
- `_TRADE_PRIMARY_PROFILES` / `_TARGET_ONLY_VALIDATION_FOCUSES` 等阈值集合在新增 family 落地后**必须验证**是否需要相应更新,避免新增 family 在 profile 路径上被排除。
- evidence_score 公式当前对动量类型友好(post_cost_sharpe / wf_ic_ir / dsr 等指标的阈值倾向于趋势策略),对反转/震荡类型可能要单独校准。

P3 验收建议:扩展后,在 paper 通道运行 14 天后统计 paper strategy 的 D→C 升级率。如果某 family 升级率长期为 0,说明 evidence_score 公式对该 family 不适用,需要单独校准而不是无脑加入集合。

### 7.4 schema 落地选项

关于 `submission_lane` 的持久化,有三种选项:

| 方案 | 做法 | 优点 | 风险 |
|---|---|---|---|
| A | 复用 `strategy_incubation_accounts.stage='paper'` | 工作量最小,贴合现有 `_enqueue_paper_observation` | 需要账户 stage guard 和历史数据清理 |
| B | 给 `strategies` 增加 `submission_lane` / `admission_layer` | 查询简单,展示直观 | 需要 schema migration 和历史同步 |
| C | intake JOIN `strategy_quality_reports.summary` JSON | 不改 schema | 依赖 JSON 查询能力,性能和最新报告选择复杂 |

V4 推荐先走方案 A。方案 B 可作为后续展示和审计增强;方案 C 不建议作为长期主路径。

### 7.5 admission_layer 展示字段

建议在报告、Desktop 或 Agent 展示层中映射:

```text
admission_layer = research | observe | formal | live_review | refresh
```

主映射来源 `submission_lane`:

```text
rejected            -> research
deferred_submission -> research
observe_incubation  -> observe
formal_incubation   -> formal
live_ready_review   -> live_review
refresh_existing    -> refresh
```

V3/V4 关键增强:增加 sub_reason 字段,用 `submission_action_trigger` 区分 Research 层下的子状态:

```text
research / gate_a_revise          ← gate_a_decision == "revise"
research / quality_gate_failed    ← Gate 失败
research / runtime_contract_gap   ← runtime contract 缺
research / budget_deferred        ← 配额不够
observe  / d_grade_micro_budget   ← V3 P0 解封后新增主流路径
observe  / runtime_semantic_gap   ← runtime semantic gap 但允许观察
observe  / proxy_runtime          ← proxy runtime 可观察不可正式
formal   / strict_incubation      ← 正式严格通道
```

实现层面:admission_layer 不需要 schema 字段,可由展示层从 `submission_lane` + `submission_action_trigger` 实时合成。

### 7.6 P2 反馈回流策略工厂

当观察池有真实输入后,再建设反馈回流:

- paper signal coverage。
- forward verification 命中率。
- expectancy / drawdown / turnover 观察指标。
- family budget feedback。
- generator mode feedback。
- target pool feedback。
- skill LCB / hit-rate feedback。
- freeze / cool / boost 控制。

V4 关键依赖:P2 反馈回流的核心目标之一是**驱动 D→C 升级**。这个升级路径只有在 P3 完成后才生效。如果只做 P2 不做 P3,paper 阶段积累的证据无处可去 — 系统会知道某只 volatility_breakout 在 paper 阶段表现良好,但无权把它从 D 级升 C 级,自然也升不到 formal。

因此 P2 + P3 是配套关系,推荐同窗口落地。


## 8. 监控指标与验收标准

### 8.1 策略工厂侧指标

- candidates spawned。
- Gate-1 / Gate-2 / Gate-3 passed。
- rejected count。
- deferred_submission count。
- **observe_incubation count(P0 验收主指标)**。
- formal_incubation count。
- live_ready_review count。
- refresh_existing count。
- validation grade 分布。
- `runtime_bootstrap_eligible=true` 比例。
- `formal_track_eligible=true` 比例。
- `execution_readiness_tier:unknown` 占比。
- `formal_track_blockers` TopN。
- `submission_action_trigger` TopN。
- **D 级 + Gate 通过 + observe 命中率(V3/V4 P0 新增)**。
- **D→C 升级率分 family 统计(V4 P3 新增,验证升级集合扩展是否真的把证据接上)**。
- **factory_runs.status=success_no_strategy / partial_infra 比例(V4 新增,跟踪 cycle 终态分布)**。

### 8.2 观察孵化侧指标

- `strategy_incubation_accounts.stage='paper'` 数量。
- paper active account 数量。
- paper observation 信号生成数。
- paper forward verification 覆盖率。
- paper hit-rate 报告数量。
- **paper -> D→C 升级 candidate 数量(V4 P3 主验收指标)**。
- 被排除的 `candidate` / `warmup` / orphan account 数量。

### 8.3 正式孵化侧指标

- `strategies.status='incubating'` 数量。
- formal incubation account 数量。
- pipeline stage 分布。
- signal generation success rate。
- forward verification success rate。
- hit-rate trend。
- pipeline promotion / demotion / freeze 数量。

### 8.4 验收标准

第一阶段(P0 落地)验收:

```text
runtime_bootstrap_eligible=true 出现非 0 值(预计 ≥ 171/1959)
observe_incubation 出现非 0 值(同上)
runtime_bootstrap_reason 中 "d_grade_observe_only_micro_budget" 或类似新值出现
formal_track_eligible 仍保持 0 或低值(确认 formal 严格未受影响)
strategies.status 'incubating' 仍保持 0 或低值(确认未误升级)
strategy_incubation_accounts 中 stage='paper' 行数开始增长
```

第二阶段(P1 落地)验收:

```text
孵化工厂 Phase 2 加载到 paper observation 候选
Phase 3 信号生成 + 前向验证开始覆盖 paper 策略
candidate / warmup 不被 paper intake 误消费
paper account 不会被重复处理(反 EXISTS 子句生效)
arity 不爆炸(LIMIT 50 与 STRATEGY_TIMEOUT_SEC 协同)
```

第三阶段(P3 + P2 落地)验收:

```text
paper observation 信号回流 strategy factory generator
family budget / generator mode 受 paper hit rate 影响开始调整
volatility_breakout / value_factor / sector_rotation 等扩展 family 出现 D→C 升级 candidate
观察 14 天 paper 阶段后,各 family 升级率 > 0,evidence_score 公式校验通过
observe -> formal 升级路径有明确规则
Desktop/Agent 能区分 research、observe、formal、live_review,且 observe/research 显示 sub_reason
```


## 9. 风险与注意事项

1. 不要直接降低 formal 门槛。formal 是正式孵化和未来实盘审查的前置层,不能为追求数量而绕过 runtime/execution/hard-risk 约束。`admission_authority.py` 第 73 行 `formal_track_blockers` 已经覆盖 `strict_incubation_pass_required_for_formal_track`,V3/V4 P0 不会影响这条限制。
2. 不要把所有 `submitted` 都送进孵化工厂。`deferred_submission` 仍然是等待修复,不是观察孵化。
3. 不要假设 `strategies.submission_lane` 存在。当前 schema 没有该列,落地 SQL 必须改用账户 stage、报告 JSON,或先做 schema migration。V2 SQL 中 `s.strategy_id` 列名错误,必须改为 `s.id`。
4. 不要无差别消费 `strategy_incubation_accounts`。`stage='candidate'` 对应 live-ready review,`stage='warmup'` 还可能包含历史孤儿数据。P1 SQL 必须显式 stage='paper' AND status='active',并加反 EXISTS 子句排除已升级策略。
5. paper observation 不等于 formal incubation。paper 层只能做低风险观察、信号和前向验证,不应自动推广。
6. **不要试图通过下调 grade 阈值来制造非 D 级。** D 级 90.6%(全量 1959 条)是真实质量信号,主要由 `_fill_gaps` 配额补齐生成的 volatility_breakout 候选(占候选池 48%)驱动。正确做法是放行 D 级走 observe 收集真实证据,而不是粉饰评级。
7. **V3/V4 P0 落地后,paper account 流量会从 0 跳到全量解封 171 条。** 必须同步落地 P1 的 `LIMIT 50` 与优先级排序,否则孵化工厂 Phase 3 串行 forward verification 会被冲垮。P0 与 P1 应在同一窗口内联合上线,不应单独发布 P0。
8. 历史 `draft + active warmup` 账户需要单独数据治理,不能作为新流程正确性的证据。
9. V3/V4 P0 修改的是 `_runtime_bootstrap_context` 的 AND 链,不修改任何 quality gate 评分逻辑、不修改 grade 评定阈值、不改 schema。回滚成本极低(恢复一行即可)。
10. V3/V4 P0 完成后,`test_admission_authority.py` 中 `validation_grade='D'` 相关用例需要同步更新预期值。建议增加 "D 级 + quality_passed → observe_incubation" 的新用例。
11. **V4 新增风险:** 仅做 P0 + P1 不做 P3,observe 层会变成"D 级永久收容所"。171 条策略进了 observe 后没有升级出口(因为 `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 集合只覆盖 momentum/ma_cross/quality_factor),paper observation 收集到的证据无处生效。P3 必须与 P0/P1 一起规划。
12. **V4 新增风险:** "Pipeline staged mode returned no specs" 在真跑中出现 4 次,意味着 LLM-driven 候选生成模式在当前环境下不稳定,候选池实际依赖本地规则 spawner 的 `_fill_gaps` 兜底。这导致 volatility_breakout 长期主导。短期 V4 P3 解决升级出口问题;长期需要单独评估 LLM-driven pipeline 的稳定性,但不在本方案范围内。
13. **V4 新增风险:** P3 扩展 `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 后,`evidence_score` 公式对反转/震荡型 family 可能不适用。建议 P3 落地后跑 14 天 paper observation,统计各 family 升级率,如果某 family 升级率长期为 0,需要单独校准 evidence_score 公式或改用 family 专用阈值,不要无脑接入。

## 10. 推荐实施顺序

V4 推荐顺序:

```text
Step 1  P0 修改 _runtime_bootstrap_context AND 链(persistence.py 第 56–61 行)
Step 2  同步更新 test_admission_authority.py 用例,确认 D 级 + passed -> observe
Step 3  跑一次 strategy_factory cycle,确认 latest 报告中 observe_incubation > 0,
        runtime_bootstrap_eligible=true > 0,formal_track_eligible 仍为 0
Step 4  P1 在 IncubationFactoryIntake 加入 _list_paper_observation_strategies (intake.py)
Step 5  在 IncubationFactoryRunner 加入 _list_paper_observation,扩展 Phase 2/3,
        给 paper stage 单独的 forward verification 阈值与 LIMIT 50
Step 6  P3 扩展 _TRADE_AWARE_VALIDATION_GRADE_FAMILIES 集合(normalizers.py 第 86 行),
        新增主流 strategy_type 进入 D→C 升级集合
Step 7  P3 验证 evidence_score 公式对扩展 family 的适用性(单元测试 + paper 阶段实测)
Step 8  数据治理:清理 draft + warmup + active 历史孤儿账户;补 stage 字段一致性
Step 9  增加 admission_layer 展示与 sub_reason,Desktop/Agent 同步
Step 10 P2 建立 paper -> formal 的升级规则与反馈回流(此时 D→C 升级链已通)
```

P0 与 P1 之间需要同窗口上线(Step 1–5 一起)。P3 (Step 6–7) 可以与 P1 同窗口或紧随其后,但必须在第一批 paper observation 数据落地前完成,否则 paper 阶段积累的证据没有出口生效。


## 11. 反事实模拟实证(V4 新增章节)

V4 在 2026-05-26 用真实代码 + 真实候选数据做了一次反事实模拟,直接验证 V3/V4 P0 修改的预期效果。

### 11.1 模拟方法

- 加载 `SubmissionAdmissionAuthority.resolve` 真实接口(不修改任何源码)。
- 从数据库 `strategy_quality_reports` 取 1 条最新 D 级 + quality_passed=True 候选(strategy_id=`factory_1779504613_1b3abaf3`,strategy_type=`volatility_breakout`)。
- 重建该 candidate 的 `quality_gate` 与 `candidate` 字典。
- 准备两个 resolver:
  - **REAL** — 复现 `_runtime_bootstrap_context` 当前实现(包含 `validation_grade != "D"` 硬否决)
  - **PATCHED** — 应用 V3/V4 P0 修改(去掉 D 级硬否决)
- 用同一个 candidate + 同一个 gate,分别调 `SubmissionAdmissionAuthority.resolve(..., runtime_bootstrap_resolver=REAL/PATCHED)`,对比输出。

### 11.2 模拟结果

```text
=== 候选基本信息 ===
strategy_id              = factory_1779504613_1b3abaf3
strategy_type            = volatility_breakout
validation_grade         = D
gate.passed              = True
gate.live_candidate_ready     = False
gate.research_candidate_ready = True
gate.strict_incubation_ready  = False
DB 中的 summary.submission_lane = deferred_submission
DB 中的 summary.runtime_bootstrap_reason = validation_grade_d_not_allowed_for_runtime

=== Step A: REAL(复现现状) ===
submission_lane             = deferred_submission         ← 与 DB 一致
final_status                = submitted
runtime_bootstrap_eligible  = False                        ← 与 DB 一致
runtime_bootstrap_reason    = validation_grade_d_not_allowed_for_runtime  ← 与 DB 一致
submission_action_trigger   = formal_incubation_requires_bootstrap_or_strict_gate
formal_track_eligible       = False
formal_track_blockers       = ['validation_grade_d_not_allowed_for_runtime',
                               'execution_readiness_tier:unknown',
                               'strict_incubation_pass_required_for_formal_track']
admission_decision          = observe_only

=== Step B: PATCHED(V3/V4 P0 修复后预测) ===
submission_lane             = observe_incubation           ← 解封到 observe
final_status                = submitted
runtime_bootstrap_eligible  = True                         ← 解封
runtime_bootstrap_reason    = d_grade_observe_only_micro_budget  ← 新 reason
runtime_bootstrap_budget_tier = micro                      ← 自动落 micro tier
submission_action_trigger   = runtime_bootstrap_observe
formal_track_eligible       = False                        ← formal 严格不变
formal_track_blockers       = ['execution_readiness_tier:unknown',
                               'strict_incubation_pass_required_for_formal_track']
admission_decision          = observe_only

=== Step C: 关键差异 ===
差异字段数 = 5
  submission_lane            : 'deferred_submission' -> 'observe_incubation'
  runtime_bootstrap_eligible : False                  -> True
  runtime_bootstrap_reason   : 'validation_grade_d_not_allowed_for_runtime'
                              -> 'd_grade_observe_only_micro_budget'
  runtime_bootstrap_budget_tier : None                -> 'micro'
  submission_action_trigger  : 'formal_incubation_requires_bootstrap_or_strict_gate'
                              -> 'runtime_bootstrap_observe'

=== 验收 ===
[OK] REAL.submission_lane              expected='deferred_submission'  actual=同
[OK] REAL.runtime_bootstrap_eligible   expected=False                   actual=同
[OK] PATCHED.submission_lane           expected='observe_incubation'    actual=同
[OK] PATCHED.runtime_bootstrap_eligible expected=True                   actual=同
[OK] PATCHED.runtime_bootstrap_budget_tier expected='micro'             actual=同
[OK] PATCHED.formal_track_eligible     expected=False                   actual=同 (formal 严格性不变)
OVERALL: PASS (6/6)
```

### 11.3 模拟结论

V3/V4 P0 修改的预期效果在反事实模拟中**完全得到验证**:

- 移除 D 级硬否决后,候选立刻路由到 `observe_incubation` lane
- `runtime_bootstrap_eligible` 由 False 变 True
- `runtime_bootstrap_budget_tier` 自动落 `micro`(已有逻辑无需改)
- **`formal_track_eligible` 保持 False** — formal 严格性零变化,符合 V4 §6.3 / §11 / §9 风险条款

这条模拟证据 + 真跑日志(§4.3) + 全量数据基线(§4.1) 共同构成 V4 方案可落地的实证依据。

## 12. 真跑实证(V4 新增章节)

V4 在 2026-05-26 11:49–12:00 完整运行了一次 `python run_strategy_factory.py --once`,用于验证 V3 §3 状态边界、V4 §4 数据基线和 V4 §5.2 瓶颈链的真实性。

### 12.1 运行环境

- 入口:`run_strategy_factory.py --once`
- 数据源:TDX local-only(`TDX_LOCAL_ONLY=1`,跳过 Tushare/AkShare)
- LLM:`gpt-5.5` via `https://icoe.pp.ua/v1/chat/completions`
- 数据库:`C:/Users/walking/Desktop/aiask/data/db/akshare_mcp.sqlite3`
- DB 已在跑前备份至 `data/db/akshare_mcp.pre_factory_run_20260526.sqlite3.bak`(3.14 GB)

### 12.2 运行漏斗

```text
启动阶段(6 秒)
  18 个 strategy_type 全部注册成功
  TDX local-only 数据源 init
  task_board 抢占成功(ttl=1800s)

research 阶段(8 分钟)
  8 个 research task,concurrency=1 串行
  LLM 调用 30+ 次,全部 200 OK
  "Pipeline staged mode returned no specs" WARNING 出现 4 次
  bulk_stock_matrix task 走 rule-first generation without optimizer

Gate 链(4 秒)
  Gate-0 structural   = 25/25 passed
  Pre-Gate cheap      = 25/25 passed
  Gate-1 fast screen  = 10/25 passed -> top-4 进 Gate-2
  Gate-2 full backtest = 1/4 passed
  Gate-3 input         = 1
  Gate-3 passed        = 0

收尾(6 秒)
  spawned                  = 25
  backtest_passed          = 1
  created_strategy_pool    = 0
  created_audit_only       = 1
  validation_grade_counts  = {'C': 1}
  status                   = partial_infra
```

### 12.3 真跑后数据增量

```text
strategies 表增量:
  rejected   1818 -> 1819   (+1, Gate-3 失败的 audit_only 策略)
  draft       229 -> 230    (+1)
  submitted   143 -> 143
  incubating    0 -> 0

strategy_incubation_accounts 增量:
  共 1 行(stage='warmup' 历史孤儿,与跑前一致,未新增)

strategy_quality_reports 增量:
  本次 cycle 新增 1 条 type='submission' 报告,lane='rejected'
  最近 30 天总计 1959 条(全量基线,见 §4.1)
```

### 12.4 真跑结论

V4 真跑实证了三件事:

1. **V3/V4 §3 状态边界正确**:Gate-3 失败的策略只生成 audit_only 快照,不进入 strategy pool,strategies.status 增量符合预期。
2. **V4 §4.1 全量数据基线稳定**:cycle 完成后整体分布与跑前一致,observe lane 仍为 0,验证了"observe lane 流量恒为 0 是稳定状态而不是采样噪声"。
3. **V4 §5.2 瓶颈链全链路在真跑中显化**:25 候选 -> 1 通过完整回测 -> 0 通过 Gate-3 -> 1 audit_only,这正是 V4 §5.2 瓶颈链第 1-2 步在真跑中的画像。

`success_no_strategy` 和 `partial_infra` 是工厂的常态终态,意味着"cycle 跑完但没产出可孵化策略"已经成为运行画像。这从根本上验证了 V4 §5.2 的诊断:**当前问题不是孵化工厂没接,而是策略工厂连接得起的策略都生产不出来**,而 V4 P0 + P1 + P3 是把现有 171 条 passed-D 候选的能量从"沉默死区"变成"observe 流量 + 升级出口"的最小可行路径。

## 13. 最终判断

V4 把方案的根因定位收敛到 **两个字符级的判定决策**:

```text
1. admission_authority 调用栈中,_runtime_bootstrap_context 第 56–61 行的
   "validation_grade != 'D'" 这一硬否决条件,导致 90.6% 候选被埋葬,
   进而让 observe_incubation lane 流量恒为 0,孵化工厂 paper 通道无输入。
   修复:把这一条从 AND 链中移到 formal_track_blockers 中(后者已存在等价限制)。

2. submission_gate/runner_parts/normalizers.py 第 86 行,
   _TRADE_AWARE_VALIDATION_GRADE_FAMILIES = {momentum, ma_cross, quality_factor}
   集合过小,占候选池 48% 的 volatility_breakout 等主流 type 永远不能 D→C 升级。
   修复:把 volatility_breakout / value_factor / sector_rotation / macro_timing /
   growth_factor / north_capital_track / event_structure_breakout 加入集合。
```

修复路径不是降 formal 门槛、不是改 grade 阈值、不是改 schema,而是把 1 个 AND 条件解锁 + 1 个 family 集合扩容。

修复后预测(基于全量 1959 条 + 反事实模拟 + 真跑实证):

- **171/1959 D 级 + Gate 通过 策略立即进入 observe lane**,`runtime_bootstrap_budget_tier='micro'` 自动生效。
- **formal lane 严格性零变化**(`formal_track_blockers` 中 `strict_incubation_pass_required_for_formal_track` 仍然挡住 D 级升 formal,反事实模拟已验证)。
- 孵化工厂 P1 改造一上线就有真实输入,前向验证管道开始转。
- P3 扩展 family 集合后,paper observation 阶段积累的真实证据可以反向推动 D→C 升级,形成完整的 observe -> formal 演进路径。
- paper observation 真实运行数据反哺 strategy factory generator,反馈回流闭环建立。

V4 推荐方案**总结一句话**:

```text
formal 保持严格(零变更)
observe 通过 1 个字符级 AND 链调整解封 D 级 + Gate 通过流量
research 继续承接 Gate 失败与修复
孵化工厂新增受控的 paper observation 消费路径(LIMIT 50 + 反 EXISTS 边界)
P3 扩展 D→C 升级集合,让 paper 阶段积累的真实证据能推动 D→C/B/A 自然演进
通过真实运行数据反哺 generator,推动整体质量演进
```

## 14. 业界对照(V4 联网验证后新增)

V4 方案在落地前进行了一次深度联网验证,把方案的核心设计决策与业界公开的最佳实践和学术资料对照,确保不偏离行业共识。

### 15.1 三层准入模型 vs ML4T Four-Tier Validation

参考资料:[ML4T Diagnostic - Four-Tier Validation Framework](https://ml4trading.io/docs/diagnostic/user-guide/validation-tiers/)(基于 Marcos López de Prado 的方法学)

业界共识把策略验证分为 4 层。V4 方案与 ML4T 框架的对照:

| ML4T 层 | 阶段 | 关键工具 | V4 方案对应 |
|---|---|---|---|
| Tier 1: Feature Analysis | 建模前 | IC、IC Decay、Stationarity Tests、Drift Detection | Gate-0 / Pre-Gate(结构 + 廉价过滤) |
| Tier 2: Signal & Model Diagnostics | 建模中/后 | Signal IC、Quantile Spreads、Turnover、Half-Life | Gate-1 / Gate-2(快速筛 + 完整回测) |
| Tier 3: Backtest Analysis | 回测后 | Deflated Sharpe Ratio、PBO、Reality Check、SPA Test | Gate-3 + `_derive_trade_aware_validation_grade` |
| Tier 4: Portfolio Analysis | 生产中 | Rolling Metrics、Drawdown、Risk Attribution | V4 paper observation + formal arena |

V4 关键吻合点:`_derive_trade_aware_validation_grade` (trade_profile.py 第 41–87 行) 的 evidence_score 公式纳入了:

- `post_cost_sharpe`(对应 Sharpe Ratio)
- `deflated_sharpe_ratio` (DSR — 对应 ML4T Tier 3 主指标)
- `pbo` (Probability of Backtest Overfitting — 对应 ML4T Tier 3)
- `white_reality_check_pvalue` / `hansen_spa_pvalue`(对应 Reality Check / SPA Test)
- `parameter_perturbation_trade_stability`(对应 robustness 测试)

也就是说,V4 P0 修复后的 D→C 升级路径**直接复用了业界 Tier 3 标准方法学**,不存在方法学缺陷。

参考论文:Bailey & López de Prado (2014), "The Deflated Sharpe Ratio: Correcting for Selection Bias, Backtest Overfitting, and Non-Normality"。

### 15.2 Gated Deployment Pipeline vs V4 状态机

参考资料:[Algo Trading Live Deployment - Gated Deployment Pipeline](https://nexusfi.com/a/automation/algo-trading-live-deployment)

业界 7 阶段 Gated Deployment Pipeline 与 V4 对照:

| 业界阶段 | 阶段目标 | V4 对应 |
|---|---|---|
| Stage 1: Backtest | 估算 edge | Gate-2 完整回测 |
| Stage 2: Walk-Forward | 证明稳定性 | Gate-3 walk_forward_ic_ir / pkf_ic |
| Stage 3: Monte Carlo | 探查 tail risk | bootstrap_ci_lower(已有) |
| Stage 4: Paper Trading | 证明执行栈正确 | **V4 P0 解封后的 stage='paper' observation** |
| Stage 5: Shadow Mode | 信号生成但不下单 + 与历史比对 | **V4 缺失(V5 增量项,见 §16)** |
| Stage 6: Small-Size Live | 真实执行数据 | live_ready_review |
| Stage 7: Scale-Up | 增量放大 | candidate / graduation_ready / promoted |

引用 Nexusfi 关于 Stage 4 Paper Trading 的关键论断(经过释义合规):
> 大多数交易者搞错 paper trading 的目标 — 它不是为了证明策略有利可图,而是为了**证明执行栈工作正确**(订单生命周期、数据完整性、会话处理、合约切换、风控触发)。盈利只是附带福利,不是重点。

V4 §6.2 的 micro budget paper observation 设计**与该论断一字不差吻合**:不允许 D 级策略在 paper 阶段证明自己赚钱(那是回测的工作),而是证明它在真实运行环境下的信号生成、覆盖率、execution 一致性。内容已根据合规要求改写。

### 15.3 Incubation 阶段的目标

参考资料:[Quantreo - Incubation, Live Trading and Monitoring](https://www.newsletter.quantreo.com/p/incubation-live-monitoring)

Quantreo 把 incubation 定义为"安静但必要的阶段,策略证明它能在真实世界运行而不爆炸或沉默"。V4 §6.2 paper observation 的核心目标与这条定义同构:

- **不是赚钱,而是观察**(同 Quantreo 关键论断)
- **观察信号是否按设计触发**
- **观察 entries/exits 是否与回测匹配**
- **观察数据空洞、市场关闭、波动异常时的行为**
- **观察 infra 是否稳定**

V4 §6.2 的 paper observation 阶段**完全符合这条业界标准定义**。内容已根据合规要求改写。

### 15.4 IS-WFA-OOS Protocol vs V4 Gate 链

参考资料:arxiv preprint 2603.09219 - "AlgoXpert Alpha Research Framework: A Rigorous IS-WFA-OOS Protocol for Mitigating Overfitting in Quantitative Strategies"

该论文提出关键原则:**"OOS is not opened upon WFA FAIL"** — Walk-Forward Analysis 失败的策略不应该进入下一阶段。

V4 是否安全?

- 经代码核查,Gate-3 (`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/multiple_testing.py` 第 574–608 行) 已经检查 `wf_ic_ir` / `pkf_ic` / `bootstrap_ci_lower`,任一不达标 `gate.passed=False`。
- V4 §7.1 P0 修改后,`runtime_bootstrap_eligible` 仍然要求 `quality_passed=True`,即 walk-forward / purged kfold 已通过。
- V4 §6.1 明确把 "Gate 失败 + D 级" 归 Research,不进 Observe。

✅ V4 代码层面已自动覆盖该风险,设计与 IS-WFA-OOS Protocol 兼容。

### 15.5 Lopez de Prado 生产线模型 vs V4 双工厂

参考资料:Lopez de Prado, "Advances in Financial Machine Learning" (Wiley, 2018)

López de Prado 提出 quants 应该专门化、互相验证支持、像生产线一样运作,反对单兵作战的 silo 结构。V4 的 **Strategy Factory + Incubation Factory 双工厂模型** 正是这条生产线模型的工程实现:

- Strategy Factory 专门负责候选生成 + 多级 Gate 筛选
- Incubation Factory 专门负责 paper observation + forward verification + 命中率反馈
- 两厂之间通过 `submission_lane` + `strategy_incubation_accounts.stage` 字段流转候选
- 反馈管道把 Incubation Factory 的运行结果回传 Strategy Factory generator(P2 反馈回流)

✅ V4 双工厂结构 + V4 P0/P1/P3 的修复方案与 Lopez de Prado 推荐的生产线方法学完全吻合。

### 15.6 业界验证小结

V4 方案的 5 个核心设计决策都经得起业界对照:

| 决策 | 业界对照 | 结论 |
|---|---|---|
| 三层 Research/Observe/Formal 模型 | ML4T Four-Tier、Quantreo、Nexusfi 7-stage | ✅ 同构 |
| Paper observation 不为赚钱而为观察 | Quantreo、Nexusfi Stage 4 | ✅ 一字不差 |
| D→C 升级用 evidence_score 公式 | DSR/PBO/Reality Check/SPA Test | ✅ 业界 Tier 3 标准 |
| Walk-Forward 失败不进下游 | arxiv AlgoXpert IS-WFA-OOS | ✅ 代码层自动覆盖 |
| 双工厂生产线 + 反馈回流 | Lopez de Prado 生产线模型 | ✅ 同构 |

V4 方案在业界视角下是**主流派**,不存在方法学异议。


## 15. V5 增量建议(联网验证发现的差距)

V4 主体经得起业界对照,但联网验证暴露 3 个工程性差距,作为 V5 增量建议:

### 16.1 Stage 5 Shadow Mode 缺失

业界 Gated Deployment Pipeline 在 Paper Trading 之后、Small-Size Live 之前有一个 Shadow Mode 阶段:**生成信号、计算订单,但不真正发送给 broker;同时与市场实际结果做 hindsight 比对**。

V4 当前的状态机:

```text
observe_incubation (paper)  -> live_ready_review (candidate, 直接进 small-size 审查)
```

V5 推荐增加一个中间状态:

```text
observe_incubation (paper)  -> shadow_mode (信号产出但不下单, 与 hindsight 比对)
                           -> live_ready_review (candidate)
```

实施方案:

- 增加 `strategy_incubation_accounts.stage='shadow'` 这一新阶段
- 在 admission_authority 增加新 lane `shadow_mode_review`
- 进入条件:paper observation 14 天 + signal coverage > X% + forward IC > Y
- 行为:孵化工厂在 Phase 3 给 shadow stage 策略生成信号但不写真实订单,只记录"如果此时下单的虚拟成交价 vs 真实市场行情"的差距

差距可量化:slippage 分布、latency jitter、partial fill 概率等。这些指标在没有 shadow mode 时无法在 paper observation 阶段捕获,只能在 live trading 才显化。Shadow mode 把这些"已知未知"提前显化。

代码影响范围:

- `admission_authority.py` 增加 `shadow_mode_review` lane
- `lifecycle_coordinator.py` 增加对应的 stage='shadow' 处理分支
- `_enqueue_shadow_observation` 新方法(类比 `_enqueue_paper_observation`)
- `IncubationFactoryRunner` Phase 3 给 shadow 策略走单独的"虚拟下单 + 比对"路径

V5 关键约束:Shadow mode 不应该取代 Stage 6 Small-Size Live。它是一个低风险中间层,补充 paper observation 在 execution 维度的盲点。

### 16.2 P0 / P3 修改没有 Release Toggle 包裹

参考:Martin Fowler 的 Feature Toggles 文章,Release Toggle 应该是**过渡性的**,允许 1-2 周的观察窗口后再决定是固化还是回滚。

V4 §7.1 P0 当前设计:**直接代码硬改**,把 `validation_grade != "D"` 从 AND 链中删除。这种修改回滚需要**重新提交一次代码**,在生产 incident 场景下不够快速。

V5 推荐用环境变量包裹:

```python
# packages/strategy-factory/src/strategy_factory/application/_submitter_actions/runner_parts/persistence.py

import os

# V5 toggle: 默认 True,允许 D 级 + Gate passed 进 observe;
# 如出现意料外的孵化工厂负载或 paper account 异常增长,设置 0 一键回滚。
_OBSERVE_D_GRADE_ENABLED = (
    str(os.getenv("STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED", "1") or "1").strip().lower()
    in {"1", "true", "yes", "on"}
)

# 在 _runtime_bootstrap_context 里:
runtime_bootstrap_eligible = (
    quality_passed
    and (validation_grade != "D" if not _OBSERVE_D_GRADE_ENABLED else True)
    and strategy_type_registered
    and not missing_runtime_fields
    and not semantic_hard_fail
)
```

同样,V4 §7.3 P3 扩展 `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 也建议用环境变量控制,允许 family 集合在 paper observation 实测过程中动态调整:

```python
# packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/normalizers.py

_BASE_TRADE_AWARE_FAMILIES = {"momentum", "ma_cross", "quality_factor"}

def _resolve_trade_aware_families() -> set[str]:
    extra = str(os.getenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", "") or "").strip().lower()
    if not extra:
        return set(_BASE_TRADE_AWARE_FAMILIES)
    extras = {item.strip() for item in extra.split(",") if item.strip()}
    return _BASE_TRADE_AWARE_FAMILIES | extras

_TRADE_AWARE_VALIDATION_GRADE_FAMILIES = _resolve_trade_aware_families()
```

落地后,P3 集合扩展可以通过环境变量灰度推:

```bash
# 第一周:只放开 volatility_breakout
export STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES="volatility_breakout"

# 第二周:观察后追加
export STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES="volatility_breakout,value_factor,sector_rotation"

# 出问题:一键回滚
export STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES=""
```

V5 关键工程原则:**所有改变 admission/grade 行为的修改都应有 release toggle 包裹**,在 1-2 周观察期后视效果决定固化或回滚。Toggle 的命名空间统一以 `STRATEGY_FACTORY_` 前缀,集中在一个 env vars 文档中维护。

### 16.3 P0/P1/P3 之间缺少灰度发布顺序

V4 §10 推荐 P0 与 P1 同窗口上线,P3 紧随其后。但联网验证的业界共识是**每一阶段应该有明确的灰度比例**(比如 Stage 6 Small-Size Live 推荐 10–30% 仓位)。

V5 推荐:

| 阶段 | 灰度比例 | 观察期 | 退出条件 |
|---|---|---|---|
| P0 toggle ON | 100% 候选,但 paper budget tier=micro 自动限制风险 | 7 天 | observe_incubation 流量 > 0,没有 paper account 异常增长 |
| P1 paper intake | LIMIT 50/批,7 天后调到 100 | 7 天 | Phase 3 signal 生成成功率 > 80%,无 timeout 雪崩 |
| P3 family 扩展 | 每周追加 1 种新 family,而不是一次扩展 7 种 | 14 天/family | 该 family 的 D→C 升级率 > 0,evidence_score 分布合理 |

这样把 V4 §10 的 7 步顺序进一步细化到"每步带灰度"。即使某一步出问题,影响范围被限制在该 toggle 控制的子集内。

### 16.4 V5 优先级排序

如果团队带宽紧,V5 三个增量按重要性排序:

1. **Release Toggle (§15.2)** — 最高优先级,工程安全保障。直接放在 V5 P0/P3 落地中,代价极小(~5 行代码)。
2. **灰度比例 (§15.3)** — 高优先级,与 Toggle 配套,允许阶段性观察。
3. **Shadow Mode (§15.1)** — 中优先级,需要新增 lane / stage / 孵化工厂分支处理,代价较大。可以等 P0/P1/P3 跑稳之后再做。

V5 关键边界:**§15.1 Shadow Mode 不是 V4 落地的必要条件**。V4 P0 + P1 + P3 + V5 §15.2 toggle + V5 §15.3 灰度 已经足以让方案安全落地。Shadow Mode 是后续优化项。


## 16. P3 灰度阶段实证(DEV-V1 落地后新增)

DEV-V1 P0/P1 完成端到端验证后,P3 灰度阶段进入"评估 evidence_score 公式对各 family 适用性"环节。本章节记录基于历史 1774 条 D 级候选的实证结果,**不直接修改源码**,作为 V5 P3 后续工作的输入。

### 16.1 评估方法

- 加载 `strategy_quality_reports` 中所有 `report_type='submission' AND validation_grade='D'` 历史报告(全量 1774 条)。
- 用真实 `_derive_trade_aware_validation_grade` 函数对每条候选评估 D→C 升级。
- 9 个场景:BASE only / 7 个 extras 单独开 / FULL(全部 7 个 extras)。
- 完全在内存模拟,不写 DB,不依赖等待真实灰度上线。

### 16.2 实证结果 — 升级率分布

| family | 历史候选数 | FULL 场景升级率 | 集合归属 | 评级 |
|---|---|---|---|---|
| `volatility_breakout` | **943** | **0.00%** | EXTRA(候选) | ❌ 不适用 |
| `value_factor` | 193 | 0.00% | EXTRA | ❌ |
| `sector_rotation` | 188 | 0.00% | EXTRA | ❌ |
| `macro_timing` | 142 | **98.59%** | EXTRA | ⚠️ 公式过松 |
| `quality_factor` | 80 | 0.00% | **BASE** | ❌ |
| `event_structure_breakout` | 79 | 0.00% | EXTRA | ❌ |
| `north_capital_track` | 68 | **82.35%** | EXTRA | ⚠️ 公式过松 |
| `multi_factor` | 40 | 0.00% | OUT(白名单外) | — |
| `ma_cross` | 32 | 0.00% | **BASE** | ❌ |
| `mean_reversion_short` | 8 | 0.00% | OUT | — |
| `gap_fill` | 1 | 0.00% | OUT | — |

### 16.3 关键发现

#### 发现 1:基础集合 momentum/ma_cross/quality_factor 在生产中 **从未真正升级过**

V4 §7.3 假设的"基础 family 升级路径已存在并工作"是错误的。

```
基础集合 BASE 总候选 = ma_cross(32) + quality_factor(80) = 112
基础集合升级数 = 0
基础集合升级率 = 0.00%
```

历史 quality_reports 中存在的 168 条 C 级**不是** D→C 升级产物,而是 **上游 validation 服务直接给的原生 C 级**。`_derive_trade_aware_validation_grade` 函数自引入以来,在生产中可能从未触发过升级。

#### 发现 2:占候选池 48% 的 volatility_breakout 升级率 0% 的真实原因不是 evidence 不够

抽样实测 volatility_breakout 候选关键指标:

```
candidate_id: factory_1779205900_f847c177
  profile             = trade_rule_validation       ← 在 _TRADE_PRIMARY_PROFILES ✅
  validation_focus    = target_plus_representative  ← 不在 _TARGET_ONLY_VALIDATION_FOCUSES ❌
  trade_density       = 1.039
  post_cost_sharpe    = 1.4056
  target_layer_oos_return = 0.4438
  trade_stability     = 0.5384
```

`_derive_trade_aware_validation_grade` (trade_profile.py 第 38-39 行):

```python
if validation_focus not in _TARGET_ONLY_VALIDATION_FOCUSES:
    return grade, None
# _TARGET_ONLY_VALIDATION_FOCUSES = {"candidate_target_only", "event_target_only", "target_only"}
```

**volatility_breakout 候选全部用 `target_plus_representative`(跨多股代表池验证),被前置条件直接拦截,根本进不入 evidence_score 评分**。即使指标(Sharpe 1.40 / OOS Return 0.44 / trade_density 1.04)在所有维度全部满足升级阈值,也无法升级。

这是**架构层 family ↔ validation_focus 耦合**:
- 择时型 family(macro_timing / north_capital_track)→ 单股票级别 → `candidate_target_only` ✅ 进评分
- 技术面/因子型 family(volatility_breakout / value_factor / sector_rotation / ma_cross / quality_factor)→ 跨多股验证 → `target_plus_representative` ❌ 不进评分
- 事件型 family(event_structure_breakout)→ 通常 `event_target_only` 或类似 → 看具体情况

#### 发现 3:macro_timing 98.59% 升级率是 evidence_score 公式对该 family 过松

抽样 5 条 macro_timing 候选:

```
candidate_id: factory_1779332514_91ccf81f
  profile             = macro_regime_validation
  validation_focus    = candidate_target_only
  trade_density       = 0.0452                  ← 触发 +2.0 (在 (0,1.0])
  post_cost_sharpe    = 0.7924                  ← 几乎触发 +1.0 (在 [0.8) 边缘)
  target_layer_oos_return = 0.1913              ← 触发 +1.5 (>=0.18)
  trade_stability     = 0.75                    ← 触发 +1.0 (>=0.5)
  deflated_sharpe_ratio = None                  ← 0
  pbo                 = None                    ← 0
  white_reality_check_pvalue = None             ← 0
  hansen_spa_pvalue   = None                    ← 0
```

evidence_score = +2.0 + +1.0 + +1.5 + +1.0 = **5.5**(刚好 >= 5.0 升级阈值)。

**问题**:DSR / PBO / Reality Check / SPA 都是 None(上游未计算),不扣分,不加分。前 4 个指标对 macro_timing 几乎全员命中 → 142 条中 140 条达 5.0 → 异常高升级率。

这与 V4 §15 风险条款 #13 预言"某 family 升级率 > 50% 说明 evidence_score 公式过松"完全一致。

#### 发现 4:`_derive` 函数的 `_TARGET_ONLY_VALIDATION_FOCUSES` 集合与 strategy_type 设计不匹配

历史候选的 validation_focus 与 strategy_type 的真实分布:

| strategy_type | 主流 validation_focus |
|---|---|
| volatility_breakout / value_factor / sector_rotation / quality_factor / ma_cross | `target_plus_representative` |
| macro_timing | `candidate_target_only` |
| north_capital_track | `candidate_target_only`(部分) |
| event_structure_breakout | `event_target_only`(部分) |

`_TARGET_ONLY_VALIDATION_FOCUSES = {"candidate_target_only", "event_target_only", "target_only"}` 这个集合本质上是**单标的 family 白名单**,跨多股代表池验证的 family 全部被排除。

### 16.4 V4 §7.3 推荐顺序需要修订

V4 §7.3 原推荐顺序(基于 candidate count):

```
Week N+1: volatility_breakout    # 占 48%,优先
Week N+2: + value_factor
Week N+3: + sector_rotation
...
```

**这个顺序在 evidence_score 公式不修复的前提下完全无效** — Tier B 的 family 在现有公式下 100% 不会升级,扩展集合等同于无操作。

修订后基于 **validation_focus 适配性 + 升级率** 重排:

| Tier | 涉及 family | 状态 | 推荐动作 |
|---|---|---|---|
| **Tier A**(适配但公式过松) | macro_timing(98.6%), north_capital_track(82.4%) | 可立即扩展但**风险高** | 先校准 evidence_score 阈值或加入 DSR/PBO 必填,再考虑灰度 |
| **Tier B**(架构限制) | volatility_breakout, value_factor, sector_rotation, ma_cross, quality_factor, event_structure_breakout | 现有公式不会触发评分 | **不推荐扩展**,先做 V5 P3 修复(见 §16.5) |

**重要**:不推荐立即开启任一 family 的 extras toggle。**P3 P0 阶段的目标是先发现问题(已经达成),不是立刻让 D 级 → C 级流量上来**。

### 16.5 V5 P3 修复路线(独立工作)

P3 真正要落地,需要先做以下 3 项工程修复(都不在本架构方案范围,但作为 V5 P3 子项跟踪):

#### 修复 1:扩展 `_TARGET_ONLY_VALIDATION_FOCUSES` 或重新设计 evidence_score 准入

文件:`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/normalizers.py`

```python
# 现状
_TARGET_ONLY_VALIDATION_FOCUSES = {"candidate_target_only", "event_target_only", "target_only"}
```

**选项 A(简单):** 增加 `target_plus_representative`。需要先验证此 focus 下 trade_density 的语义(可能要按代表池均值/中位数计算)。

**选项 B(推荐):** 给 `_derive_trade_aware_validation_grade` 增加按 validation_focus 分支的 evidence 计算路径,target_only 用现有公式,target_plus_representative 用专门为代表池设计的公式(例如要求 cross_sectional_ic_mean / cross_sectional_ic_ir 达标)。

#### 修复 2:macro_timing / north_capital_track 阈值校准

文件:`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/trade_profile.py`(第 41-87 行)

evidence_score 阈值 5.0 对 macro_timing 太松,142 条历史中 140 条命中。

**选项 A:** 把 evidence_score 升级阈值从 5.0 提高到 6.5(需要重测命中率)。

**选项 B(推荐):** 强制要求 DSR + PBO 至少一项非 None,即上游必须给出多重检验调整后的指标,避免靠前 4 项松阈值堆积达标。这本质上是把 ML4T Tier 3 的"DSR/PBO 是必填"上升到 evidence_score 必填项。

#### 修复 3:按 family 类别分别加权 evidence_score

短期内可以接受:

```python
EVIDENCE_THRESHOLDS_BY_FAMILY = {
    # 单标的择时型 — 严格(因为 family 偏简单,要求高 PBO/DSR)
    "macro_timing": 6.5,
    "north_capital_track": 6.5,
    # 跨标的因子型 — 现行 5.0(假设修复 1 后能进入评分)
    "volatility_breakout": 5.0,
    "value_factor": 5.0,
    ...
}
```

短期实现:在 `_derive_trade_aware_validation_grade` 末尾按 strategy_type 选择不同阈值。

### 16.6 P3 当前最终状态

| 维度 | 状态 |
|---|---|
| `STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES` toggle | **保持默认 OFF**(V5 P3 修复完成前不灰度) |
| TG / P0 / P1 已落地 | ✅ 58 个测试 PASS,真跑端到端验证 |
| P3 代码改动 | ✅ Proxy 模式 + 9 个测试 PASS,但因 evidence_score 公式问题暂不灰度 |
| V5 P3 跟踪项 | 修复 1 / 修复 2 / 修复 3 已写入 §16.5,作为独立 V5 工作流 |

V4 主体方案的可行性不受 P3 灰度发现影响:**P0 + P1 已经能让 observe lane 流量从 0 跳到非 0**(D 级 + Gate passed 候选自然路由),孵化工厂能消费 paper account。**P3 升级出口** 是反馈回流闭环的最后一环,可以在 V5 P3 修复完成后再灰度。



## 17. 修订历史

| 版本 | 日期 | 变更内容 |
|---|---|---|
| V1 | 2026-05-26 上午 | 初版三层模型,准入桥太窄诊断,推荐扩容 observe |
| V2 | 2026-05-26 中午 | 修正 V1 6 处事实错误:补充上游也无流量、SQL schema 假设、隐藏事实(_enqueue_paper_observation 已存在/live_ready_review 同表/历史脏数据) |
| V3 | 2026-05-26 下午 | 基于代码事实根因诊断,精确到行号定位 P0:`_runtime_bootstrap_context` 第 56–61 行 AND 链中 D 级硬否决。删除 V2 P0 中 6 条无关排查项,只保留 1 条核心修复;补充 P1 SQL 字段名修正、反 EXISTS 边界、LIMIT 协同;新增 admission_layer sub_reason 展示;增加 P0 与 P1 必须同窗口上线的风险提示 |
| V4 | 2026-05-26 傍晚 | 全量代码审查 + 真跑 1 次完整 cycle + 反事实模拟实证。数据基线从 300 扩展到全量 1959,passed-D 从 53 修正为 171。新增 V4 §11 反事实模拟实证(6/6 PASS)、V4 §12 真跑漏斗(25→1→0)、V4 §7.3 P3 扩展 _TRADE_AWARE_VALIDATION_GRADE_FAMILIES 集合(关键新发现:volatility_breakout 占 48% 但永远不能 D→C 升级)。把方案落地条件从"P0 + P1"扩展到"P0 + P1 + P3 + 同窗口上线" |
| V4-network | 2026-05-26 晚 | 联网搜索业界对照,新增 §14 业界对照(ML4T Four-Tier / Quantreo / Nexusfi 7-stage / arxiv AlgoXpert / Lopez de Prado)+ §15 V5 增量建议(Shadow Mode 缺失、Release Toggle 包裹、灰度发布顺序)。V4 主体经得起业界对照,5 个核心设计决策全部命中行业共识。新增 3 个工程性差距作为 V5 后续。修订历史挪至末尾(§16) |
| V4-network-p3 | 2026-05-26 夜 | DEV-V1 落地完成 (TG/P0/P1 全部 58 测试 PASS + 真跑端到端验证),P3 灰度阶段实证发现 evidence_score 公式与 strategy_type validation_focus 耦合架构问题。新增 §16 P3 灰度阶段实证(发现 1: 基础 family 在生产中从未升级过 / 发现 2: volatility_breakout 占 48% 候选但因 validation_focus='target_plus_representative' 永远不进评分 / 发现 3: macro_timing 升级率 98.6% 公式过松 / 发现 4: family↔validation_focus 架构耦合)。V4 §7.3 推荐顺序修订为 Tier A/B 二分。新增 V5 P3 修复路线 3 项(扩展 target_only / 阈值校准 / 按 family 分级)。修订历史挪至 §17 |
