# 策略工厂 G3 全拦死锁 — 诊断与根治方案

> 生成日期：2026-06-09
> 范围：`packages/strategy-factory` 提交质量门 + 孵化调度链路 + 启动脚本
> 性质：最初为只读代码审查 + **实测日志核对**产出的诊断报告；2026-06-10 起追加落地修复与单轮验证记录。
> 重大修订：2026-06-09。**第零节为实测结论，推翻了后文第一~八节基于"G3全拦"假设的推断。**
>           以第零节为准；后文保留作为"统计门/契约/评级"次级关卡的参考。

---

## 零、实测结论（基于真实运行日志，最高权威）

### 0.1 核心发现：G3 没有全拦,真正的断点在"孵化调度只认 incubating 状态"

实测日志 `strategy_factory_step_c.log`(2026-06-04 01:29→12:59,90 轮 cycle):

| 指标 | 实测值 | 说明 |
|------|--------|------|
| G3 input | 31 | |
| **G3 passed** | **16 (52%)** | **G3 在正常放行,非全拦** |
| G3 failed | 15 | |
| created_strategy_pool | 25 | 确实入池 |
| submitted | 16 | |
| created_audit_only | 6 | |
| eliminated | 0 | |
| **独立 paper_account** | **仅 2 个** | inc_3960cde8 / inc_257db292 |
| **独立 incubating 策略** | **仅 2 个** | 全天再无新增 |
| FOREIGN KEY / skip quality report | **0 次** | 此日志无 FK bug |
| spawned→backtest passed | **1255→66 (5.3%)** | 生成端漏斗严重枯竭 |

**结论:G3 通过率 52%、入池 25 个,但全天只有 2 个策略变成 `incubating` 状态、建了 2 个 paper_account。**
"全拦"的真实含义不是"G3 拦截",而是**入池策略绝大多数停在 `submitted` 状态,
没有流转到 `incubating`,导致孵化/纸面交易循环只反复处理那 2 个老策略。**

### 0.2 真正的根因:双孵化通道 + 调度器 status 不匹配（已代码级证实）

`admission_authority.py` 的状态机决定 final_status,**只有一个分支给 `incubating`**:

| 分支(行) | 条件 | submission_lane | final_status |
|---------|------|----------------|-------------|
| `:161-164` | `formal_track_eligible`(严格门全过+budget=formal) | formal_incubation | **`incubating`** |
| `:103-107` | wide_intake observe(G3未过宽进) | observe_incubation | `submitted` |
| `:128-138` | live_candidate 但 runtime 未就绪 | observe_incubation | `submitted` |
| `:173-176` | runtime_bootstrap observe | observe_incubation | `submitted` |
| 其余 | research/deferred | deferred_submission | `submitted`/`rejected` |

两条互不连通的孵化通道:
- **formal 通道**:`final_status=incubating` → `PaperTradingBridge.on_strategy_incubated`(建 paper_account)
  → `PaperTradingScheduler._load_incubating_strategies` **只查 `status='incubating'`**(`paper_trading_scheduler.py:128`) ✅ 有人消费
- **observe 通道**:`final_status=submitted` + lane=observe_incubation
  → `lifecycle_coordinator.py:579-592 _enqueue_paper_observation`
  → `incubation_gateway.ensure_account(stage="paper")`(另一套账户,**status 不是 incubating**)
  → 由**独立的 incubation_factory** `intake.py:204 _list_paper_observation_strategies`(查 `stage='paper'`)消费

**断点:`PaperTradingScheduler` 与 `IncubationIntake` 是两个查询口径不同的消费者。**
strategy_factory 自带的 PaperTradingScheduler 只认 `incubating`,
**完全看不到 observe 通道的 `submitted`/stage=paper 策略**。
observe 策略能否被处理,完全依赖 incubation_factory 进程是否在跑 + `paper_intake_enabled()`。

### 0.3 为什么只有 2 个进 formal(incubating)

`formal_track_eligible` 要求 `strict_incubation_ready`(incubation 级评估零 blocker),
即必须过 `MIN_GRADE=S`(`start_three_factories.sh:41`) + incubation 档 IC≥0.02 等全部严格门。
实测评级分布 **B:13 / C:14 / D:2,零个 S 级** → 几乎无策略能命中 formal 分支
→ 全天仅 2 个侥幸进 incubating。这是 0.1 的"只有 2 个"的直接原因。

### 0.4 真实失败码(交易质量门"差一点"型,非死锁)

实测 gate_3 失败码 topN:`win_rate_0.386<0.400`、`post_cost_sharpe_0.096<0.100`、
`profit_factor_1.268<1.500`、`trade_density_2.324>1.200`、`purged_kfold_ic_0.004<0.020`。
特征:**候选质量贴着阈值差一点**,失败分散,无单一恒定死锁。
purged_kfold_ic 实测 **0.004/0.007**(比预想更低,是真·弱信号),非恒定 0.011~0.013。

### 0.5 实测确认的真实问题清单(替换原"5重死锁")

| # | 真实问题 | 证据 | 严重度 |
|---|---------|------|--------|
| R1 | 孵化调度 PaperTradingScheduler 只认 `incubating`,observe 策略沉底 | `paper_trading_scheduler.py:128` vs `admission_authority` 双通道 | **P0 主因** |
| R2 | 生成端漏斗枯竭 spawned→backtest 仅 5.3% | 日志 1255→66 + `no_executable_specs`×8 | **P0 产能** |
| R3 | MIN_GRADE=S 使 formal 通道几乎不可达,评级实测无 S | `start_three_factories.sh:41` + 评级分布 | P1 |
| R4 | 候选质量贴阈值,交易门"差一点"失败 | 失败码 topN | P1 策略质量 |
| R5 | 根入口 numpy 缺失致并行启动崩溃 | `strategy_factory_parallel.log` 32 行 | P0 前置 |
| R6 | 两消费者(PaperTradingScheduler / IncubationIntake)口径割裂 | `intake.py:204` vs scheduler:128 | P1 架构 |

> 注:原文第一~八节描述的 record-only / trade_prediction_contract / 评级死锁,
> **在 step_c 日志中未表现为全拦**(passed=16 证明)。它们是 formal 通道内部的次级关卡,
> 在 R1/R2 解决后才可能成为下一道墙。**落地以本节 R1-R6 为准。**

### 0.6 实证溯源:"全拦"的真实历史(git + 双日志对比已证实)

**关键:用户报告的"全拦"是 6/1 的现象,6/4 已被 wide-intake 缓解一半。**

| 日期 | 日志 | gate_3_passed | created_strategy_pool | created_audit_only | 状态 |
|------|------|--------------|----------------------|-------------------|------|
| 6/1(4 次 once 运行) | `logs/strategy_factory_once_*.err.log` | **全部 0** | **全部 0** | 3~4 | **真·全拦** |
| 6/4(90 轮) | `strategy_factory_step_c.log` | 16/31 | 25 | 6 | 已放行 |

**时间线根因(git 已证实)**:
- commit **`87c1235` "P1 change-A Layer-1 wide-intake observe admission"**(约 6/3)
  原文:*"When ON, structurally-valid candidates enter observe_incubation even if Gate-3 not passed, instead of rejected"*
- 该 commit 改了 `admission_authority.py`(+22 行,即 `:103-107` wide_intake 分支)、
  `persistence.py`(+26)、`_runtime_toggles.py`(+12,新增 `STRATEGY_FACTORY_WIDE_INTAKE_OBSERVE_ENABLED` 默认 OFF)。
- **6/1 时该功能未合入** → gate 未过的候选唯一去向是 reject/audit_only → `created_strategy_pool=0` → 全拦。
- **6/4 时启动脚本 `:30 WIDE_INTAKE_OBSERVE_ENABLED=1` 打开** → gate 未过的候选改走 observe_incubation
  → `final_status=submitted` → `created_strategy_pool=25` → 不再全拦。

**已澄清的原"待核实"项**:
- ✅ "全拦"来自 **6/1 的 4 次 once 运行**(wide-intake 上线前),非 step_c。
- ✅ FK 报错:step_c(6/4) **0 次**;6/1 日志也无 import 错误。numpy 崩溃**仅** `strategy_factory_parallel.log`(6/6 那次并行启动)。
- ✅ R1 性质已定:wide-intake 让候选进 `submitted`/observe_incubation,但 strategy_factory 自带的
  `PaperTradingScheduler` 只查 `incubating`,**确认为消费者口径割裂**(observe 策略须靠独立 incubation_factory 进程消费)。

**仍需现场确认(非阻断)**:
- observe 策略进 incubation_factory 后是否真产生纸面交易/晋升(需 incubation_factory 运行日志,本仓未见)。
- 用户当前看到的"全拦"若仍存在,需确认是否 incubation_factory 进程未启动 → observe 策略虽入池但无人消费,表现仍像"全拦"。

### 0.7 最终定性:"全拦"分两个相态

1. **6/1 相态(已被代码修复)**:wide-intake 未上 → gate 未过即 reject → 真全拦。`87c1235` + 脚本开关已解决。
2. **6/4 之后的相态(当前真问题)**:候选能入 observe_incubation(submitted),但
   - **R1**:strategy_factory 的 PaperTradingScheduler 只认 incubating,observe 策略须靠 incubation_factory 消费,口径割裂;
   - **R3**:formal(incubating)通道因 MIN_GRADE=S 近乎不可达(实测仅 2/90 轮);
   - **R2**:生成端枯竭(spawned→backtest 5.3%)。
   → 表象仍是"几乎没有策略真正进入孵化/纸面交易",但机制已从"硬 reject"变为"沉淀在 observe 层无人推进"。

---

## 一、问题陈述（注:以下为初版基于"全拦"假设的推断,已被第零节修订）

策略工厂运行后，**所有生成的策略在 G3 环节全部被拦住**，无一进入孵化阶段。
用户三连问：

1. 为什么 G3 把所有策略全部拦住了？
2. 为什么要拦？
3. 为什么非要在策略尚未进入孵化阶段时，就要求它证明赚钱能力？

---

## 二、根因总览

"全拦"**不是单一开关**，而是 **5 个独立机制叠加**，任意一个都足以拦死全部策略。
更关键的是：**启动脚本把其中 3 个保险同时打到最严档**，形成闭环死锁。

| 编号 | 机制 | 代码位置 | 代码默认 | 启动脚本设定 | 是否当前生效 |
|----|------|---------|---------|------------|------------|
| A | gate3 record-only(只记录不放行) | `_runtime_toggles.py:231-232` | `True` | `=1` | ✅ 生效 |
| B | 预测契约非 ready 即硬拒 | `entry.py:203` / `normalizers.py:461` | 恒拦 | — | ✅ 生效 |
| C | 交易预测晋升门硬拦 | `trade_prediction_promotion_gate.py` | `False`(仅诊断) | `=1`(变硬拦) | ✅ 被脚本激活 |
| D | 最低验证评级 = S 级 | `_runtime_toggles.py:225-228` | `"S"` | `="S"` | ✅ 生效 |
| E | 交易感知评级上限 = C 级 | `trade_profile.py:83-85` | 硬上限 | — | ✅ 生效(与D构成数学死锁) |

**最致命的是 D + E 组合**：准入要求 ≥ S 级(`order["S"]=4`)，
而纯交易策略经 `_derive_trade_aware_validation_grade` 评级，**数学上限只能到 C 级(`order["C"]=1`)**。
`validation_grade_at_least("C","S")` 恒为 `False` → **任何纯交易策略都不可能达标，100% 被拦**。

---

## 三、逐机制详细诊断

### 机制 A — gate3 record-only 默认开启（表层全拦）

**代码**：`packages/strategy-factory/src/strategy_factory/application/_runtime_toggles.py:231`
```python
def strategy_factory_gate3_record_only_enabled() -> bool:
    return _env_bool("STRATEGY_FACTORY_GATE3_RECORD_ONLY_ENABLED", default=True)
```

**触发链**：
`_submitter_actions/runner_parts/orchestrator.py:18`
```python
gate3_record_only = bool(_gate3_record_only_enabled())          # True
execution_options = SubmissionExecutionOptions(read_only=bool(read_only or gate3_record_only))  # read_only=True
```
→ 进入 `orchestrator.py:107-196` 的 record_only 分支：
```python
submission_lane = "gate3_record_only"
final_status   = "gate3_recorded"
record_action.update({
    "next_step": "await_manual_record_review",
    "strategy_created": False,
    "lifecycle_action_executed": False,
})
```

**后果**：无论 gate 本身 `passed` 与否，所有策略只被"记录"，
`strategy_created=False`、`lifecycle_action_executed=False`、等人工复核，**永不真正进孵化**。

---

### 机制 B — 预测契约非 ready 即硬拒（gate 内真凶之一）

**代码**：`submission_gate/runner_parts/entry.py:203-217`
```python
if str(semantic_runtime_context.get("trade_prediction_contract_status") or "").strip().lower() != "ready":
    contract_reasons = _merge_text_items(
        ["trade_prediction_contract_not_ready"],
        list(semantic_runtime_context.get("trade_prediction_contract_reject_reasons") or []),
    )
    normalized = normalize_quality_gate_result({
        **normalized,
        "passed": False,            # ← 硬判失败
        "passed_strict": False,
        "provisional_pass": False,
        "reasons": _merge_text_items(normalized.get("reasons"), contract_reasons),
        "hard_fail_reasons": _merge_text_items(normalized.get("hard_fail_reasons"), contract_reasons),
    })
```
同一逻辑镜像在 `normalizers.py:461-462`：
```python
if trade_prediction_contract_status != "ready":
    hard_fail_reasons.append("trade_prediction_contract_not_ready")
```

**status 如何产生**：
`entry.py:15` 每次入口都调 `apply_resolved_candidate_envelope()`
→ `candidate_contract_parts/evaluation.py:276` `freeze_trade_prediction_contract()`
→ `trade_prediction_contract.py:736 validate_trade_prediction_contract()`：
```python
status = TRADE_PREDICTION_CONTRACT_READY if not reject_reasons else TRADE_PREDICTION_CONTRACT_REJECTED
```
"ready" 要求 10 个必填字段(`_REQUIRED_FIELDS`，行 47-58)齐全，且：
- `direction` ∈ {up, down, neutral}，否则 `invalid:direction`
- `confidence` 非空，否则 `invalid:confidence`

**死锁点**：`direction`/`confidence` 由 `derive_trade_prediction_contract()`(行 515-598)
**仅能从候选的语义字段派生**：`direction_resolution` / `alpha_thesis` / `confidence_calibration` /
`prediction_contract` / `trade_plan.entry_bias`。
**一个纯回测产出的候选**(只有 metrics、params、risk_rules)这些语义预测字段全为空
→ `direction=None`、`confidence=None`
→ `reject_reasons=["invalid:direction","invalid:confidence"]`
→ `status="rejected"`
→ `entry.py:203` 硬判 `passed=False`。

**这就是 gate 内部判负的真凶，且与机制 A 独立**：即便关掉 record-only，gate 仍 `passed=False`。

---

### 机制 C — 交易预测晋升门被启动脚本激活成硬拦

**代码**：`trade_prediction_promotion_gate.py`
```python
TRADE_PREDICTION_PROMOTION_MIN_SAMPLE_N    = 30
TRADE_PREDICTION_PROMOTION_MIN_SCORE_LCB_95 = 0.55
```
- `hard_block = bool(gate_enabled and not diagnostic_passed)`
- `gate_enabled` 来自 `strategy_trade_prediction_promotion_gate_enabled()`，**代码默认 False**(仅诊断)。

**但启动脚本激活了它**(见第四节)，于是：
新策略 `outcomes` 为空 → `_aggregate_outcomes` 命中三条拒绝理由：
```
trade_prediction_score_v2_missing
trade_prediction_insufficient_samples   # ok_sample_n(0) < 30
trade_prediction_lcb_below_threshold    # lcb=None < 0.55
```
→ `diagnostic_passed=False` → `hard_block=True`。

**这正是"非要在进孵化前证明赚钱能力"的机制**：
要求 30 条已结算预测样本、95% 置信下界 ≥ 0.55，而**未孵化策略不可能有这些样本** → 鸡生蛋死锁。
晋升门挂在 `_submitter_actions/runner_parts/persistence.py:627` 的 live-ready promotion review，
**位置本身正确**(晋升阶段)，问题是启动脚本把它在准入期就激活了。

---

### 机制 D + E — 评级门数学死锁（最隐蔽、最致命）

**机制 D**：`_runtime_toggles.py:225-228`
```python
def strategy_factory_min_validation_grade() -> str:
    raw = os.getenv("STRATEGY_FACTORY_MIN_VALIDATION_GRADE", "S")  # 默认 S
    grade = normalize_validation_grade(raw)
    return grade if grade in GRADE_RANKS else "S"
```
准入判定 `trade_profile.py:221-230`：
```python
minimum_validation_grade = _minimum_grade_for_admission(admission_level)   # "S"
context["minimum_validation_grade_met"] = _validation_grade_meets_minimum(validation_grade, minimum_validation_grade)
if minimum_validation_grade and not context["minimum_validation_grade_met"]:
    blockers.append(f"validation_grade_..._below_minimum_s_for_{admission_level}")
```

**机制 E**：`trade_profile.py:83-85`，交易感知评级的**数学上限**：
```python
if evidence_score < 5.0:
    return grade, None
return "C", f"trade_aware_validation_grade_upgrade:..."   # ← 满分也只升到 C
```
评级序：`GRADE_RANKS = {"D":0,"C":1,"B":2,"A":3,"S":4,"SS":5,"SSS":6}`

**数学死锁**：纯交易策略评级上限 = C(rank 1)，准入要求 = S(rank 4)。
`validation_grade_at_least("C","S")` = `1 >= 4` = **永远 False**。
→ **任何纯交易策略数学上不可能通过准入，100% 被拦**，与 A/B/C 完全独立。

**research 层例外**：`trade_profile.py:205-208` research 级别直接返回空 blockers(永远放行)，
但 `incubation`/`live` 级别被 D+E 拦死 → **没有任何策略能升到 incubation 以上** → 全拦。

---

## 四、启动脚本：把所有保险打到最严（闭环根源）

**文件**：`start_three_factories.ps1` / `start_three_factories.sh` /
`scripts/factories/run_three_factories.py` / `scripts/factories/run_three_factories_24h.ps1`

`start_three_factories.ps1:62-66`：
```powershell
Set-DefaultEnv -Name "STRATEGY_TRADE_PREDICTION_PROMOTION_GATE_ENABLED" -Value "1"   # 机制C激活→硬拦
Set-Env        -Name "STRATEGY_FACTORY_MIN_VALIDATION_GRADE"            -Value "S"   # 机制D最严
Set-Env        -Name "STRATEGY_FACTORY_GATE3_RECORD_ONLY_ENABLED"       -Value "1"   # 机制A开启
```
`scripts/factories/run_three_factories.py:221-225` 同样设定。
`run_three_factories_24h.ps1:126` 设 `MIN_VALIDATION_GRADE=S`。

**结论**：启动脚本同时开启机制 A、激活机制 C、把机制 D 设到 S，
与代码内恒拦的机制 B、数学上限的机制 E 叠加，构成 **5 重闭环死锁**。

---

## 四点五、observe-first 为何形同虚设（机制交互与执行顺序）

启动脚本开了 `STRATEGY_FACTORY_OBSERVE_FIRST_ENABLED=1`，设计意图是"观察优先放行"。
代码里也确有一条观察赦免通道 `maybe_grant_provisional_incubation`
(`quality_reporting_parts/policy.py:6`，在 `entry.py:153` 调用)。
**但它对纯回测/纯交易候选被三重断路，赦免给了也拿不到、或拿到又被收回**：

**断路 1 — 候选资格门槛(policy.py:15-22)**
```python
if gate.get("passed"): return gate
if not is_factory_provisional_candidate(strategy): return gate   # 需 tags 含 factory/auto_generated 且为技术/AI原型类型
if not has_only_statistical_gate_failures(gate): return gate     # ★ 关键断路
if not risk_report: return gate
```

**断路 2 — 失败码白名单(normalizers.py:243-255)**
`has_only_statistical_gate_failures` 要求 `reason_codes` **全部**匹配 5 个统计前缀：
```
walk_forward_ic_ir / purged_k_fold_ic / bootstrap_ci_lower / parameter_sensitivity / multi_period_ic
```
而纯回测候选的失败码是 `trade_prediction_contract_not_ready`、
`validation_grade_..._below_minimum_s_for_incubation` —— **不在白名单**
→ 返回 False → 断路 1 第三行 `return gate`(原样返回 `passed=False`)，**赦免根本不触发**。

**断路 3 — 即便赦免成功也被机制 B 覆盖(执行顺序)**
`entry.py` 执行序：`:153 provisional 赦免`(设 `passed=True`) → **再到** `:203 机制 B`。
机制 B 检查 `trade_prediction_contract_status != "ready"` 时**无条件覆写** `"passed": False`
(`entry.py:211`)。所以即使某候选侥幸通过断路 1/2 拿到 `provisional_pass=True`，
也会在 :203 被打回 False。

**结论**：observe-first 在实现上**形同虚设**——观察赦免要么因失败码不在白名单而不触发，
要么触发后被后置的机制 B 撤销。这就是"observe-first 开着却仍全拦"的原因，
也是根治时**机制 B 必须改成不覆盖 `provisional_pass`** 的硬性理由。

---

## 五、回答三个问题

1. **为什么全拦**
   = A(默认 record-only 不放行) + B(纯回测候选预测契约恒 rejected) +
   D&E(评级门数学死锁，C 级 < S 级要求) + C(脚本激活晋升门硬拦)。
   五重叠加，任一即可全拦。

2. **为什么要拦**
   设计意图是风控 / 反过拟合：hard_constraints 红线(回撤>50%、负收益、零交易)、
   要求"可交易的预测契约"、要求统计可信的盈利证据(LCB 95%)。**动机正确**，
   但把"晋升级证据"误用为"准入级硬条件"，并叠加了不可达的评级上限。

3. **为什么没进孵化就要求证明赚钱能力**
   = 机制 C 的**位置/激活时机错误** + 机制 D&E 的**评级错配**：
   赚钱证据(30+ 预测样本、LCB≥0.55)本应在孵化期产生、晋升时校验，
   却被启动脚本在准入期就激活；同时评级门要求纯交易策略达到它数学上不可能的 S 级。
   → 未孵化策略拿不出它不可能有的证据，自锁。

---

## 六、根治方案（代码 + 启动脚本两端，从逻辑上杜绝）

**总原则**：保留风控红线(hard_constraints)，把"盈利证据"要求**移回它本该在的晋升阶段**；
准入阶段只看回测健康度 + 结构契约；纯交易/纯回测候选以**观察(diagnostic)身份**进孵化，
由孵化期积累真实 direction/confidence/预测样本，晋升门再用积累证据判定盈利能力。

> 口径确认：用户已选定机制 B 采用「降级为观察身份放行」，不采用「准入即自动兜底 direction/confidence」(避免注入虚假预测语义污染晋升证据)。

### 改动 1 — 机制 A：record-only 默认翻转
**文件**：`_runtime_toggles.py:232`
- `default=True` → `default=False`
- 理由：record-only 是诊断/灰度用途，不应是常态默认。

### 改动 2 — 机制 B（核心）：预测契约非 ready 时分级处理，不再硬拒
**文件**：`submission_gate/runner_parts/entry.py:203-217` 与 `normalizers.py:445-462`

**判定条件(可直接落地)**：在 entry.py:203 的分支前，先对契约失败做"缺失 vs 损坏"分类。
依据 `freeze_trade_prediction_contract` 已返回的 `reject_reasons`(`trade_prediction_contract.py:743-749`)：
- `missing:<field>` = 派生缺失(纯回测候选无预测语义) → **可降级放行**
- `invalid:contract_version` / `invalid:direction` / `invalid:confidence` = 显式契约字段非法 → **仍硬拒**

```
status = trade_prediction_contract_status
reject_reasons = semantic_runtime_context.get("trade_prediction_contract_reject_reasons") or []
has_invalid = any(str(r).startswith("invalid:") for r in reject_reasons)
contract_source = (semantic_runtime_context 或 contract).get("contract_source")
is_derived = (contract_source == "derived_from_legacy_contract")

if status == "ready":
    pass                                   # 正常放行,不变
elif has_invalid:
    passed = False                         # 损坏契约仍硬拒(保留)
else:  # 全为 missing:* 且 is_derived 的派生缺失
    # 不再覆写 passed;保留统计门结果
    diagnostic_only = True
    review_mode = "incubation_only"
    warnings += ["trade_prediction_contract_not_ready", *reject_reasons]
    # 关键:不得把 trade_prediction_contract_not_ready 加进 hard_fail_reasons
```

**两处必须同步改，否则无效**：
1. `entry.py:203-217`：把无条件 `"passed": False` 改为上面的分级；
   **且不得覆写已有的 `provisional_pass=True`**(否则四点五断路 3 仍在)。
2. `normalizers.py:445-462` `_resolve_semantic_runtime_context`：
   `if trade_prediction_contract_status != "ready": hard_fail_reasons.append(...)`
   改为仅在 `has_invalid` 时加 `hard_fail_reasons`，否则加 `warnings`。
   (此处 hard_fail_reasons 会被上层 `_gate_a_payload`/`_gate_b_payload` 当 reject blocker，必须降级)

### 改动 3 — 机制 D&E：放宽准入评级（已定值）
**定值决策**：`MIN_VALIDATION_GRADE` 由 `"S"` 改为 **`"C"`**。
理由：交易感知评级数学上限为 C(`trade_profile.py:85`)，设 C 可让满足交易证据(evidence_score≥5.0)
的策略恰好达标进 incubation；低于 C(即 D/无评级)仍被拦，保留对劣质策略的过滤。

- **代码侧**：`_runtime_toggles.py:226` `os.getenv("STRATEGY_FACTORY_MIN_VALIDATION_GRADE", "S")` → 默认 `"C"`。
- **启动脚本侧**：四个脚本里的显式 `="S"` 同步改 `="C"`(见改动 5)。
- **不改** `trade_profile.py:85` 的 C 上限：保持交易策略评级语义不变,仅放宽准入门。
- `incubation`/`live` 仍可保留更高 committee/promotion 阈值(`_review_gate_thresholds`)，
  本改动只确保策略**能进 incubation 观察**，不降低后续晋升标准。

### 改动 4 — 机制 C：晋升门留在晋升阶段，准入期不激活
**文件**：启动脚本
- 准入期不应 `STRATEGY_TRADE_PREDICTION_PROMOTION_GATE_ENABLED=1`；
  该门应仅在孵化后的 live-ready 晋升评审生效(代码默认 False 即正确语义)。
- 若需保留晋升期硬拦，确保它只作用于已积累 30+ 样本的孵化后策略。

### 改动 5 — 启动脚本全面核查
**文件**：`start_three_factories.ps1` / `.sh` / `scripts/factories/run_three_factories.py` /
`run_three_factories_24h.ps1`
- `STRATEGY_FACTORY_GATE3_RECORD_ONLY_ENABLED` → 不设或 `0`
- `STRATEGY_FACTORY_MIN_VALIDATION_GRADE` → `C`/`D`(见改动 3)
- `STRATEGY_TRADE_PREDICTION_PROMOTION_GATE_ENABLED` → 准入期不设(见改动 4)
- `INCUBATION_FACTORY_GATE3_RECORD_ONLY_MIN_GRADE`(`run_three_factories.py:215`=S) 一并复核

---

## 七、改造后的目标逻辑链

```
纯回测候选
  → 准入门(G3/gate_b): 仅校验 回测健康度 + 结构契约 + hard_constraints 红线
  → 评级达到可进 incubation 的档位(C/D)，以观察(diagnostic)身份进孵化
  → 孵化期产生真实 direction/confidence 预测样本，逐步积累 outcomes
  → 晋升门(机制C，此时才激活): 用 30+ 样本、LCB95%≥0.55 判定盈利能力
  → 达标者晋升正式池 / live
```

**死锁从根上消除**：盈利证据的要求被移回晋升阶段；
准入阶段不再要求未孵化策略拿出它不可能有的预测样本与不可达的 S 级评级。

---

## 八、落地次序、测试影响与验证

### 8.1 推荐落地次序（按依赖与风险排序）
1. **改动 5 + 4(启动脚本)** — 最低风险、可立即解锁：把脚本里 `="S"`→`="C"`、
   record-only/promotion-gate 关掉。不动代码即可先验证"是否还全拦"。
2. **改动 3(代码默认值)** — 与脚本一致，防止有人不经脚本直接跑时再次踩 S 级死锁。
3. **改动 1(record-only 默认)** — 翻转 default，使代码默认安全。
4. **改动 2(机制 B 分级)** — 核心逻辑改动，风险最高，放最后，配套单测一起上。

> 先脚本后代码的理由:脚本改动可逆、影响面小,能快速确认根因判断正确;
> 代码改动(尤其改动2)需配套测试,放最后降低回归风险。

### 8.2 会被打破的现有测试（必须同步更新）
| 测试 | 现断言 | 改后应为 |
|------|--------|---------|
| `test_runtime_toggles.py:268-271` | `min_validation_grade()=="S"`，`validation_grade_at_least("B","S") is False` | 默认改 `"C"`；断言相应调整 |
| `test_runtime_toggles.py:276-277` | `gate3_record_only_enabled() is True` | 改为 `is False` |
| `test_trade_prediction_promotion_gate_p4.py` | 晋升门 enabled 行为 | 复核：准入期不激活，仅晋升期 |
| `test_strategy_factory_quality_fixes.py:83` | `setenv MIN_VALIDATION_GRADE "S"` | 视用例语义决定是否改 C |
| `test_admission_authority.py:500` | `setenv ... "S"` | 同上 |

> 注意：`test_runtime_toggles.py:30` 的 fixture 会清这些 env，断言以代码默认值为准 — 改默认值必同步改断言。

### 8.3 逐步验证清单
1. **解锁验证**：用一个纯回测候选(无 direction/confidence 语义)跑提交流程，
   断言 `final_status` 进入 incubation/observe，**而非** `gate3_recorded`。
2. **观察身份验证**：断言该候选 gate 结果带 `diagnostic_only=True` / `review_mode=incubation_only`，
   且 `trade_prediction_contract_not_ready` 出现在 `warnings` 而非 `hard_fail_reasons`。
3. **损坏契约仍拒验证**：构造一个 `invalid:direction`(显式契约方向非法)的候选，
   断言它**仍** `passed=False` — 证明改动 2 没把损坏契约也放行。
4. **风控红线未削弱**：构造 max_drawdown>50% / 负收益 / 零交易候选，
   断言 hard_constraints 仍拒(`hard_constraints.py` 路径不受本方案影响)。
5. **晋升门仍生效**：对一个已积累<30 样本的孵化后策略，在晋升期开启 promotion gate，
   断言 `hard_block=True` — 证明盈利证据要求被正确保留在晋升阶段。
6. **observe-first 真生效**：断言 `provisional_pass=True` 的候选不再被 entry.py:203 打回
   (回归四点五断路 3)。

### 8.4 回滚预案
所有改动均由 env 包裹或为默认值翻转，回滚成本低：
- 脚本：恢复 `="S"` / `RECORD_ONLY=1` / `PROMOTION_GATE=1` 即回到旧行为。
- 代码默认值(改动1/3)：env 显式设回旧值即可覆盖，无需回滚代码。
- 改动 2(逻辑)：保留 `has_invalid` 分支,若需紧急收紧,可临时让 else 分支也 `passed=False` 复现旧行为。

---

## 九、关键代码位置索引

| 主题 | 文件:行 |
|------|--------|
| gate3 record-only toggle | `_runtime_toggles.py:231-232` |
| min_validation_grade toggle | `_runtime_toggles.py:225-228` |
| record-only 分支 | `_submitter_actions/runner_parts/orchestrator.py:18, 107-196` |
| 预测契约硬拒 | `submission_gate/runner_parts/entry.py:203-217` |
| 预测契约硬拒(镜像) | `submission_gate/runner_parts/normalizers.py:461-462` |
| 契约 validate/status | `trade_prediction_contract.py:736-756` |
| 契约必填字段 | `trade_prediction_contract.py:47-58` |
| direction/confidence 派生 | `trade_prediction_contract.py:515-665` |
| 准入评级 blocker | `submission_gate/runner_parts/trade_profile.py:197-256` |
| 交易感知评级上限 C | `submission_gate/runner_parts/trade_profile.py:83-85` |
| 晋升门阈值 | `trade_prediction_promotion_gate.py:12-14` |
| 晋升门调用点 | `_submitter_actions/runner_parts/persistence.py:627` |
| 启动脚本 env(ps1) | `start_three_factories.ps1:62-66` |
| 启动脚本 env(py) | `scripts/factories/run_three_factories.py:215-225` |
| 启动脚本 env(24h) | `scripts/factories/run_three_factories_24h.ps1:126` |

---

## 十、2026-06-10 单轮修复与检测记录

### 10.1 执行原则

本轮按"每跑一次、检测一次、文档记录一次"执行，不使用 48 小时持续运行脚本，不新增脚本。
工厂运行均使用项目已有入口：

- `python scripts/factories/run_market_event_ingest.py --once`
- `python scripts/factories/run_factor_mining_factory.py --once`
- `python scripts/factories/run_strategy_factory.py --once --dispatch-run --parallel-full-cycles --dispatch-concurrency 5 --dispatch-shard-size 1 --dispatch-default-universe --dispatch-default-universe-limit 5 --execution-mode stock_first_observe_primary`
- `python scripts/factories/run_incubation_factory.py --json`

说明：`scripts/factories/run_incubation_factory.py` 当前不接受 `--once`，默认不加 `--daemon` 即为单轮运行。

### 10.2 LLM 配置

已将 `.env` 中的 LLM base URL 统一切换到 `https://api.aisenyu.com`，并配置 API key。为避免泄露密钥，本文档只记录变量名，不记录密钥值。

已核对变量：

- `OPENAI_BASE_URL`
- `OPENAI_API_KEY`
- `STRATEGY_LLM_BASE_URL`
- `STRATEGY_LLM_API_KEY`
- `FACTOR_LLM_BASE_URL`
- `FACTOR_LLM_API_KEY`

策略工厂运行日志确认使用 `gpt-5.5 (https://api.aisenyu.com)`，LLM HTTP 请求返回 `200 OK`。

### 10.3 本轮运行结果

| 阶段 | 结果 |
|------|------|
| Market Event Ingest | saved docs 107，normalized 99，bridged events 50，signals 196 |
| Factor Mining Factory | raw 21，evolved 21，quick evidence passed 8/21，validated 0，admitted 0，pool_size 5；`llm_primary` 180s 超时，其余引擎继续 |
| Strategy Factory | 修复后 5 个新 observe 策略进入 `submitted + stage=paper` 链路，`gate_3_recorded=0` |
| Incubation Factory | `paper_observation_intake.scanned=5`，`recognized=5`，无 phase failure |

Strategy Factory 修复后产生的新 observe 策略：

- `factory_1781020728_4cce35c1`
- `factory_1781020728_8b7f1bf9`
- `factory_1781020801_c9fddaf0`
- `factory_1781020905_f1769e54`
- `factory_1781020905_a5182b86`

对应质量报告 `4032` 至 `4036` 已记录 observe handoff 关键字段：

- `submission_lane=observe_incubation`
- `paper_lane_ready=true`
- `incubation_factory_required=true`
- `paper_observation_backlog_status=queued`

补充检测发现：这些历史报告生成时 `final_status` 尚未稳定写入 quality report summary；本轮已补代码与回归测试，后续新报告会同时写入 `final_status=submitted`。为保持运行证据可追溯，未回写修改历史 DB 报告。

对应 paper account：

- `inc_6826ae3f`
- `inc_31a6cb74`
- `inc_5fddb9f1`
- `inc_338a5af4`
- `inc_b63daec8`

Incubation Factory 单轮消费后写入 `incubation_factory.paper_observation_recognized` 事件 `14939` 至 `14943`，上述账户从 `stage=paper,status=active` 推进到 `stage=warmup,status=active`。单轮结束后 active paper backlog 为 0。

### 10.4 本轮代码根治点

本轮确认旧问题不再是 `gate3_record_only`，实际残留断点是：

`prediction_contract_conflict_resolution_rule_missing` 会在 frozen `trade_prediction_contract_status=ready` 时被 semantic/evidence audit 当作 pre-observe hard fail，导致结构合法的 observe 策略仍无法进入 paper observation。

修复已落在：

- `packages/strategy-factory/src/strategy_factory/application/_submitter_actions/runner_parts/persistence.py`
- `packages/strategy-factory/tests/test_admission_authority.py`

修复语义：

- 当 frozen trade prediction contract 已 ready 且有 hash/payload 时，`prediction_contract_conflict_resolution_rule_missing` 从 observe 前硬拒降级，不再阻断 `observe_incubation`。
- 同步处理 `evidence_alignment_audit.hard_fail_reasons` 中的同类误判。
- 显式坏契约仍硬拒，例如 `invalid:direction` 不放行。
- 最大回撤、负收益、零交易、runtime semantic mismatch、formal incubation 严格条件仍保持阻断能力。

### 10.5 测试与验收

已通过 targeted regression：

- `uv run pytest packages/strategy-factory/tests/test_admission_authority.py packages/strategy-factory/tests/test_trade_prediction_contract_p0.py`：30 passed
- `uv run pytest packages/strategy-factory/tests/test_runtime_toggles.py packages/strategy-factory/tests/test_admission_authority.py packages/strategy-factory/tests/test_candidate_pipeline_observe_first.py packages/strategy-factory/tests/test_readiness_service.py packages/akshare-mcp/tests/test_paper_observation_intake.py packages/akshare-mcp/tests/test_diagnostic_observation_runner.py`：115 passed

2026-06-10 追加 `final_status` 公开字段补丁后，再次运行同一组 targeted tests：115 passed。

验收结论：

- `STRATEGY_FACTORY_GATE3_RECORD_ONLY_ENABLED` 默认和启动脚本均为 0。
- `STRATEGY_FACTORY_MIN_VALIDATION_GRADE` 默认和启动脚本均为 C。
- 准入期 `STRATEGY_TRADE_PREDICTION_PROMOTION_GATE_ENABLED` 不再默认开启。
- `INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=1` 保留。
- G3 未通过但结构合法的 observe 策略能进入 `submitted + stage=paper`，并被 Incubation Factory 单轮稳定消费。
- 新质量报告 summary 会稳定输出 `submission_lane`、`final_status`、`paper_lane_ready`、`paper_account_id`、`incubation_factory_required`、`paper_observation_backlog_status`。
- G3 通过且满足 formal 条件的策略仍保留 `incubating` formal 语义。
- 本轮 `gate_3_recorded=0`，不存在 record-only 卡死。

### 10.6 剩余非 G3 事项

以下不是 G3 死锁，但会影响后续产能或质量：

- Factor Mining 本轮 `validated=0/admitted=0`，说明因子侧有效供给仍弱。
- `llm_primary` 曾出现 180s timeout，需要继续观察新 LLM endpoint 的稳定性。
- `prediction_contract_conflict_resolution_rule_missing` 仍可作为 formal 质量提示存在，但不再阻断 observe paper handoff。
- `intraday_source_unavailable_db_only` 属于盘中数据源退化，不属于 G3 根因。

---

## 十一、2026-06-10 第二轮单次运行与审查记录

### 11.1 执行范围

本轮继续按"现有脚本单轮运行、单轮检测、文档记录"执行，未启动 24h/48h/watchdog 常驻脚本，未新增脚本。

日志目录：

- `logs/factory_single_round/single_round_renewed_20260610_003940`

实际执行入口：

- `uv run --project packages/akshare-mcp python scripts/factories/run_market_event_ingest.py --once`
- `uv run --project packages/akshare-mcp python scripts/factories/run_factor_mining_factory.py --once`
- `uv run --project packages/akshare-mcp python scripts/factories/run_strategy_factory.py --once --dispatch-run --parallel-full-cycles --dispatch-concurrency 5 --dispatch-shard-size 1 --dispatch-default-universe --dispatch-default-universe-limit 5 --execution-mode stock_first_observe_primary`
- `uv run --project packages/akshare-mcp python scripts/factories/run_incubation_factory.py --json`
- `uv run --project packages/akshare-mcp python scripts/ops/run_dev_v1_ops.py status`
- `uv run --project packages/akshare-mcp python scripts/dev_v1_ops/query/_query_incubation_status.py`

运行时说明：直接使用系统 `python` 或 `uv run` 根目录环境会缺少 `numpy`/`httpx`，本轮已确认应使用 `packages/akshare-mcp` 的 uv project 环境来运行这些跨包工厂脚本。

### 11.2 Market Event Ingest

单轮成功：

- saved docs：106
- normalized total：103
- normalized verified：30
- normalized provisional：73
- bridged events：50
- signals：175
- error_count：0

### 11.3 Factor Mining Factory / 因子超市

单轮完成：

- run_id：`mining_1781023515_f59ae49e`
- engines：`llm_primary`、`gp_classic`、`mcts_guided`、`rl_alphagen`、`rule_seed`
- raw candidates：21
- evolved candidates：21
- quick evidence passed：11/21
- validated：0
- admitted：0
- pool_size_after：5

质量审查：

- `llm_primary` 仍出现 180s timeout。
- 因子验证中出现 `ConstantInputWarning` 与 `invalid value` 数值警告，说明部分候选因子存在横截面恒定、NaN/Inf 或无效面板问题。
- 本轮没有新因子进入 active pool，因子供给仍弱。
- 因子超市当前状态：active 5、quarantine 47、retired 14；active pool 平均 `current_ic≈0.0493`、平均 `pool_weight≈0.39`。

结论：因子超市可被策略工厂读取和使用，但新挖掘因子质量不足，本轮对策略生成的增量贡献有限。

### 11.4 Strategy Factory / G3 审查

单轮 dispatch 5 个 shard，DB run id `2171` 至 `2175`。

聚合结果：

- created_strategy_pool：9
- submitted：9
- gate_3_input：9
- gate_3_passed：0
- gate_3_failed：9
- gate_3_recorded：0
- created_audit_only：0

质量报告 `4037` 至 `4045`：

- `submission_lane=observe_incubation`：9/9
- `final_status=submitted`：9/9
- `paper_lane_ready=true`：9/9
- `incubation_factory_required=true`：9/9
- `paper_observation_backlog_status=queued`：9/9
- validation grade：B=3、C=3、D=3

新策略：

- `factory_1781025192_ecc9ac8b`
- `factory_1781025192_48dca438`
- `factory_1781025192_2cf00256`
- `factory_1781025248_4b158e54`
- `factory_1781025248_1525a5fd`
- `factory_1781025248_f7c8b348`
- `factory_1781025309_5e8a75d6`
- `factory_1781025310_94598fca`
- `factory_1781025367_42c7f985`

G3 failure top reasons 聚合：

- `prediction_contract_conflict_resolution_rule_missing`：5
- `weak_wf_ic_ir`：3
- `weak_pkf_ic`：2
- 其他为 win rate、profit factor、walk-forward IC/IR、purged-kfold IC 等质量阈值问题。

结论：`prediction_contract_conflict_resolution_rule_missing` 仍作为质量提示出现，但没有再阻断 observe 入池；本轮 G3 没有退回 `gate3_recorded`，也没有把结构合法 observe 策略卡死。

记录一个汇总一致性问题：本轮 `strategy_factory_runs.summary` 对 created runs 未稳定持久化 `gate_3_recorded=0` 字段，但日志、quality reports、incubation events 均证明 record-only 未发生。该问题属于状态汇总完整性，不是 G3 链路阻塞。

### 11.5 Incubation Factory / 孵化情况

单轮成功：

- run_id：`5ac4ebfd06d0`
- paper observation intake：recognized 9/9
- verification total：11
- incubating_count：2
- paper_count：9
- diagnostic_count：0
- metrics_recorded：11
- phase_failures：0
- pipeline stage_counts：`warmup=11`

新 paper observation recognized event：

- `14975` 至 `14983`

新账户：

- `inc_e8bea6a8`
- `inc_070f1fa8`
- `inc_c47fd952`
- `inc_07c0e10e`
- `inc_8d0fce4c`
- `inc_e89a3bcb`
- `inc_230f27f5`
- `inc_916edd6d`
- `inc_a6f17faf`

单轮消费后，active `submitted + stage=paper` backlog 为 0。

### 11.6 MCP / Manager 面审查

现有状态脚本结果：

- `INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=1`
- `STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=1`
- observe_incubation 累计：805
- `incubation_factory.paper_observation_recognized` 累计：798
- P1 SQL 当前命中：0，说明 paper backlog 已被消费。

Targeted MCP/manager contract tests：

- `uv run --project packages/akshare-mcp pytest packages/akshare-mcp/tests/test_strategy_mgr_capabilities_health.py packages/akshare-mcp/tests/test_tool_argument_contract.py`
- 结果：5 passed

结论：本轮未发现 MCP/manager 面导致 G3 或 paper handoff 中断的问题。

### 11.7 本轮结论

本轮再次验证 G3 根治链路有效：

- 策略工厂实际生成了 9 个 observe 策略。
- G3 未通过的策略没有被 record-only 卡死，而是进入 `submitted + stage=paper` 的 observe handoff。
- 质量报告公开字段已稳定包含 `final_status=submitted`、`paper_lane_ready=true`、`incubation_factory_required=true`。
- Incubation Factory 单轮识别并消费 9/9 paper observation，最终 backlog 为 0。

本轮主要剩余问题不属于 G3：

- LLM endpoint 有间歇性 ConnectTimeout/ReadTimeout，但也有 `HTTP/1.1 200 OK` 成功调用。
- 因子挖掘质量仍弱，validated/admitted 均为 0。
- 策略质量整体仍偏观察级，B/C/D 各 3，没有 formal incubation 新增。
- `strategy_factory_runs.summary` 的 `gate_3_recorded=0` 字段持久化仍需后续统一汇总口径。

---

## 十二、2026-06-10 第三轮单次运行与审查记录

### 12.1 执行范围

本轮继续执行"单轮运行、单轮检测、文档记录"，未启动 24h/48h/watchdog 常驻脚本，未新增脚本。

日志目录：

- `logs/factory_single_round/single_round_renewed3_20260610_013529`

实际执行入口：

- `uv run --project packages/akshare-mcp python scripts/factories/run_market_event_ingest.py --once`
- `uv run --project packages/akshare-mcp python scripts/factories/run_factor_mining_factory.py --once`
- `uv run --project packages/akshare-mcp python scripts/factories/run_strategy_factory.py --once --dispatch-run --parallel-full-cycles --dispatch-concurrency 5 --dispatch-shard-size 1 --dispatch-default-universe --dispatch-default-universe-limit 5 --execution-mode stock_first_observe_primary`
- `uv run --project packages/akshare-mcp python scripts/factories/run_incubation_factory.py --json`
- `uv run --project packages/akshare-mcp python scripts/ops/run_dev_v1_ops.py status`
- `uv run --project packages/akshare-mcp python scripts/dev_v1_ops/query/_query_incubation_status.py`
- `uv run --project packages/akshare-mcp pytest packages/akshare-mcp/tests/test_strategy_mgr_capabilities_health.py packages/akshare-mcp/tests/test_tool_argument_contract.py`

### 12.2 Market Event Ingest

单轮成功：

- saved docs：106
- normalized total：103
- normalized verified：30
- normalized provisional：73
- bridged events：50
- signals：175
- error_count：0

### 12.3 Factor Mining Factory / 因子超市

单轮完成：

- run_id：`mining_1781026716_c5dc084a`
- engines：`llm_primary`、`gp_classic`、`mcts_guided`、`rl_alphagen`、`rule_seed`
- raw candidates：21
- evolved candidates：21
- quick evidence passed：8/21
- validated：0
- admitted：0
- pool_size_after：5

质量审查：

- `llm_primary` 继续 180s timeout。
- 因子验证继续出现 `ConstantInputWarning`，部分候选因子横截面恒定导致 IC 不可定义。
- 因子超市状态未改善：active 5、quarantine 47、retired 14；active pool 平均 `current_ic≈0.0493`、平均 `pool_weight≈0.39`。
- 连续三轮新因子均未入池，问题集中在因子候选质量/验证有效性，不是 Strategy Factory G3。

### 12.4 Strategy Factory / G3 审查

单轮 dispatch 5 个 shard，DB run id `2176` 至 `2180`。

聚合结果：

- created_strategy_pool：3
- submitted：3
- gate_3_input：3
- gate_3_passed：0
- gate_3_failed：3
- gate_3_recorded：0
- created_audit_only：0

质量报告 `4046` 至 `4048`：

- `submission_lane=observe_incubation`：3/3
- `final_status=submitted`：3/3
- `paper_lane_ready=true`：3/3
- `incubation_factory_required=true`：3/3
- `paper_observation_backlog_status=queued`：3/3
- validation grade：B=2、C=1

新策略：

- `factory_1781028142_8adeded8`
- `factory_1781028216_6fd75c1c`
- `factory_1781028270_b4f66d78`

G3 failure top reasons 聚合：

- `prediction_contract_conflict_resolution_rule_missing`：3
- `post_cost_sharpe_0_086_0_100`：1
- `post_cost_sharpe_0_255_0_100`：1
- `profit_factor_1_147_1_800`：1
- `profit_factor_0_723_1_800`：1
- `win_rate_0_231_0_400`：1
- `target_layer_oos_return_0_047_0_010`：1
- `expectancy_466_4348_0_negative_expected_value`：1

结论：`prediction_contract_conflict_resolution_rule_missing` 仍持续出现为质量提示，但第三轮仍没有阻断 observe handoff；G3 failed 候选能进入 observe paper 轨道，未出现 `gate3_recorded`。

### 12.5 Incubation Factory / 孵化情况

单轮成功：

- run_id：`61fe1f5a676f`
- paper observation intake：recognized 3/3
- verification total：5
- incubating_count：2
- paper_count：3
- diagnostic_count：0
- metrics_recorded：5
- phase_failures：0
- pipeline stage_counts：`warmup=5`

新 paper observation recognized event：

- `15014` 至 `15016`

新账户：

- `inc_21387f8b`
- `inc_98418535`
- `inc_5c3adc88`

单轮消费后，active `submitted + stage=paper` backlog 为 0。

### 12.6 MCP / Manager 面审查

现有状态脚本结果：

- `INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=1`
- `STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=1`
- observe_incubation 累计：808
- `incubation_factory.paper_observation_recognized` 累计：801
- P1 SQL 当前命中：0，说明 paper backlog 已被消费。

Targeted MCP/manager contract tests：

- `uv run --project packages/akshare-mcp pytest packages/akshare-mcp/tests/test_strategy_mgr_capabilities_health.py packages/akshare-mcp/tests/test_tool_argument_contract.py`
- 结果：5 passed

### 12.7 本轮结论

第三轮继续证明 G3 根治链路有效：

- Strategy Factory 产生 3 个 observe 策略。
- 3/3 均进入 `submitted + stage=paper` 的 observe handoff。
- Incubation Factory 稳定识别并消费 3/3，最终 backlog 为 0。
- 没有出现 record-only 卡死，也没有出现候选入池后无人推进。

连续三轮共同暴露的非 G3 问题：

- LLM endpoint 有间歇性 ConnectTimeout/ReadTimeout，但仍有成功 `200 OK` 调用。
- 因子挖掘连续 `validated=0/admitted=0`，候选质量和验证有效性需要单独治理。
- 策略仍主要停留在 observe 级别，没有新增 formal incubation。
- `strategy_factory_runs.summary` 对 created run 的 `gate_3_recorded=0` 字段仍存在汇总口径不一致，但日志、quality report、incubation event 证明实际链路未卡死。
