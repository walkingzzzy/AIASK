# 向量数据库落地实施方案

日期：2026-03-24  
关联总方案：[/Users/mac/Desktop/股票/向量数据库设计方案-2026-03-24.md](/Users/mac/Desktop/股票/向量数据库设计方案-2026-03-24.md)  
目标：把“统一向量层”从设计结论细化为可执行的迁移计划、模块改造点、回填任务拆分和验收标准

---

## 1. 交付物定义

本实施方案的交付物分四类：

1. 统一向量层 schema
2. 市场文本 / K 线模式 / 股票画像 / 因子记忆的 collection 规范
3. 数据回填与索引构建任务
4. 检索接口、评测指标与切换策略

建议最终落地为以下仓库改动：

- 新增统一向量 schema 初始化模块
- 新增统一向量存储 mixin
- 新增文档 chunk 与 embedding 回填任务
- 新增 K 线模式持久化构建任务
- 将策略域向量平台迁入统一治理层
- 为市场和量化检索增加统一查询入口

---

## 2. 实施边界

### 2.1 这一轮实施包含什么

- 建立统一向量层表结构
- 统一 collection / model / dim / version 治理
- 为市场文档和 K 线模式接入 pgvector
- 抽象策略域现有的向量索引治理模型
- 增加回填、重建、激活、回滚流程

### 2.2 这一轮实施不包含什么

- 不替换现有 TimescaleDB 主事实仓
- 不将所有因子日频明细全部向量化
- 不立即引入独立向量数据库
- 不在第一阶段引入复杂 reranker 服务

---

## 3. 目标架构

### 3.1 逻辑分层

统一向量层建议拆成四层：

1. 原始对象层
   - 新闻、公告、研报原文
   - K 线窗口
   - 股票画像快照
   - 因子候选定义

2. 派生对象层
   - 文档 chunk
   - 模式窗口描述
   - 画像摘要
   - 因子研究摘要

3. 向量归档层
   - 保存 embedding JSON、signature、metadata、版本

4. 向量查询层
   - pgvector 存储
   - HNSW partial indexes
   - snapshot / registry / active version

### 3.2 运行链路

目标运行链路如下：

`raw object`
-> `normalize`
-> `chunk / derive`
-> `embed`
-> `save profile archive`
-> `save pgvector store`
-> `build or refresh ANN snapshot`
-> `activate version`

---

## 4. 模块改造点

### 4.1 新增统一向量 schema 初始化

建议新增模块：

- `packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_vector.py`

职责：

- 初始化统一向量 collection 表
- 初始化 profile / store / snapshot / item 表
- 初始化市场文档 chunk 辅助表
- 初始化 K 线模式窗口表

并在现有 `schema_base.py` 的 `_init_tables()` 中接入。

### 4.2 新增统一向量存储 mixin

建议新增模块：

- `packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/vector_unified.py`

职责：

- collection 注册
- profile upsert
- pgvector store upsert
- snapshot 保存与激活
- index item replace
- pgvector HNSW 索引创建
- 检索接口

现有策略域相关逻辑可逐步从 `strategy_vector.py` 中抽象迁移到这里。

### 4.3 市场文本链路改造

涉及现有模块：

- `market_context.py`
- `text_embedding.py`

建议新增：

- `services/document_chunking.py`
- `services/vector_backfill.py`

改造方向：

1. `save_vector_documents()` 不再直接作为最终检索入口。
2. 原始文档继续保留，但新增 chunk 派生流程。
3. chunk 写入后异步生成 embedding。
4. embedding 成功后写入统一向量层。

### 4.4 K 线模式检索改造

涉及现有模块：

- `vector_search.py`

建议改造为双后端：

- `python/index` 继续作为 fallback
- 默认优先走 `db/pgvector`

新增模块建议：

- `services/pattern_embedding_pipeline.py`

职责：

- 生成固定窗口模式对象
- 计算模式向量
- 入库统一向量层
- 构建模式 ANN 索引

### 4.5 策略域接入统一治理

涉及现有模块：

- `schema_strategy.py`
- `strategy_vector.py`
- `vector_platform.py`

改造策略：

- 第一阶段不重写策略域算法。
- 先保留现有表。
- 但补 `collection_name/model_id/vector_dim/version` 约束。
- 第二阶段将通用部分抽离到统一向量存储层。

原因：

- 策略域是当前唯一已跑通的向量能力，不应在第一阶段做高风险重构。

---

## 5. collection 规范

### 5.1 命名规则

建议统一命名规则：

- `market_doc_chunks`
- `kline_pattern_embeddings`
- `stock_profile_embeddings`
- `factor_candidate_embeddings`
- `strategy_behavior_embeddings`

禁止：

- 同一个 collection 混多个维度
- 同一个 collection 混多个模型
- 同一个 active version 混多种向量化方法

### 5.2 必备约束

每个 collection 必须明确：

- `entity_family`
- `model_id`
- `vector_dim`
- `metric`
- `normalization`
- `version`
- `status`

### 5.3 版本策略

建议版本命名：

- `v1`
- `v2`
- `auto_YYYYMMDD_HHMMSS`

规则：

- `v1/v2` 用于人工定义的大版本
- `auto_*` 用于系统重建快照
- `active_version` 只能由 snapshot 激活流程更新

---

## 6. 数据迁移策略

### 6.1 Phase 0：先修 schema 漂移

本阶段必须先完成，否则向量层会建立在不稳定字段之上。

工作项：

1. 统一 `stocks.code -> stock_code`
2. 统一 `financials.code -> stock_code`
3. 明确 `stock_quotes` 的唯一标准字段
4. 更新所有读写接口、脚本和检查工具

建议执行方式：

- 先加兼容列或视图
- 再回填
- 最后切主写路径

验收：

- 新代码只使用统一命名
- 旧字段只作为兼容层存在

### 6.2 Phase 1：建立统一向量层骨架

工作项：

1. 新增统一向量 schema
2. 注册 collection
3. 建立 profile / store / snapshot / item 基础能力
4. 建立 HNSW 动态索引创建接口

验收：

- 可为任一 collection 完成 profile upsert、index build、activate

### 6.3 Phase 2：迁移市场文档检索

工作项：

1. 保留原 `vector_documents` 作为原始上下文源
2. 新建 `market_documents` 与 `market_doc_chunks`
3. 为 chunk 生成 embedding
4. 建立 hybrid retrieval

验收：

- 给定股票、日期和文档类型，可返回 dense + lexical 混合检索结果

### 6.4 Phase 3：迁移 K 线模式检索

工作项：

1. 固化模式窗口对象
2. 写入 `kline_pattern_windows`
3. 计算模式 embedding
4. 切换 `vector_search.py` 的默认后端到 DB

验收：

- 相似模式查询可在 DB 内完成
- `python/index` 只作为 fallback

### 6.5 Phase 4：股票画像与因子研究记忆

工作项：

1. 引入股票画像快照
2. 引入因子候选研究摘要
3. 为研究对象接入统一检索

验收：

- 股票相似搜索与因子去重可以基于统一 collection 运行

---

## 7. 回填任务拆分

### 7.1 市场文档回填任务

任务名建议：

- `vector_backfill_market_docs`

执行流程：

1. 读取新闻、公告、研报原文
2. 规范化文本
3. 生成 chunk
4. 写入 `market_doc_chunks`
5. 调用 `text_embedding.py`
6. 保存 profile / store
7. 批量构建 ANN snapshot

幂等键建议：

- `doc_id + chunk_no + model_id + version`

失败处理：

- embedding 失败写 dead-letter
- 长文本截断与清洗记录到 metadata

### 7.2 K 线模式回填任务

任务名建议：

- `vector_backfill_kline_patterns`

执行流程：

1. 为指定股票和窗口读取 K 线
2. 固定 `window_size/freq/adjust/method`
3. 生成窗口对象
4. 计算模式向量
5. 保存 profile / store
6. 构建 snapshot 与 ANN index

幂等键建议：

- `stock_code + end_date + window_size + method + adjust + version`

### 7.3 股票画像回填任务

任务名建议：

- `vector_backfill_stock_profiles`

执行流程：

1. 读取估值、财务、风格暴露、波动和行业信息
2. 生成稳定画像摘要
3. 生成 embedding
4. 保存 profile / store

幂等键建议：

- `stock_code + profile_date + model_id + version`

### 7.4 因子研究记忆回填任务

任务名建议：

- `vector_backfill_factor_candidates`

执行流程：

1. 读取候选因子定义
2. 抽取公式、字段、验证摘要、风险说明
3. 生成文本摘要与 embedding
4. 保存 profile / store

幂等键建议：

- `factor_name + signature + model_id + version`

---

## 8. 索引构建与激活流程

### 8.1 构建流程

统一要求：

1. 批量写入 archive/store
2. 统计 `vector_dim`
3. 记录 snapshot 为 `building`
4. 创建 partial HNSW index
5. 写入 index items
6. 评估 recall / latency
7. 满足阈值后激活

### 8.2 索引命名规则

建议：

- `idx_vps_hnsw_{collection}_{version}_{dim}_{metric}`
- `idx_vis_hnsw_{collection}_{version}_{dim}_{metric}`

### 8.3 激活规则

激活前必须记录：

- `sample_count`
- `vector_dim`
- `metric`
- `model_id`
- `build_duration_ms`
- `recall_at_10`
- `p95_latency_ms`

激活条件建议：

- `recall_at_10 >= 0.95`
- `p95_latency_ms` 低于 exact baseline

### 8.4 回滚规则

如果新版本检索质量劣化：

- 将 `active_version` 回退到上一快照
- 保留失败版本但标记为 `inactive` 或 `failed`
- 记录失败原因和参数

---

## 9. 查询接口设计

### 9.1 通用查询接口

建议统一提供一个存储层接口：

`search_vector_collection(collection_name, query_embedding, filters, limit, metric, index_version)`

存储层责任：

- 过滤条件规范化
- 优先使用 active index
- ANN -> fallback -> exact
- 统一返回 audit 信息

返回字段建议：

- `entity_id`
- `entity_type`
- `stock_code`
- `similarity`
- `collection_name`
- `index_version`
- `backend_used`
- `fallback_used`
- `fallback_reason`
- `metadata`

### 9.2 文档检索接口

建议：

- dense
- lexical
- hybrid

至少返回：

- `doc_id`
- `chunk_id`
- `title`
- `chunk_text`
- `doc_type`
- `source`
- `published_at`
- `dense_score`
- `lexical_score`
- `hybrid_score`

### 9.3 模式检索接口

至少返回：

- `stock_code`
- `end_date`
- `window_size`
- `method`
- `similarity`
- `forward_return_5d`
- `forward_return_10d`
- `forward_return_20d`

---

## 10. 代码实施顺序

建议按以下顺序提交改造：

1. schema 漂移修复
2. 统一向量 schema 与存储层
3. 策略域约束补齐
4. 市场文档 chunk 化与 embedding 回填
5. 文档 hybrid retrieval
6. K 线模式持久化检索
7. 股票画像与因子研究记忆

原因：

- 先修 schema 才能稳住主键和过滤逻辑
- 先抽象统一存储层，再迁各域，能避免重复实现
- 先做文档，再做模式，因为文档链路更直接复用现有 `text_embedding.py`

---

## 11. 监控与验收指标

### 11.1 数据质量

- chunk 数量
- 空向量率
- embedding 失败率
- 回填吞吐
- dead-letter 数量

### 11.2 检索质量

- `Recall@10`
- `MRR@10`
- `nDCG@10`
- hybrid 命中率

### 11.3 性能

- exact baseline P95
- ANN P95
- 索引构建耗时
- 向量表大小
- HNSW 索引大小

### 11.4 运行稳定性

- active version 切换次数
- 回滚次数
- 查询 fallback 比例

---

## 12. 风险清单

### 12.1 高风险项

#### schema 漂移未先修复

后果：

- metadata 过滤不一致
- 回填任务和读路径持续出现双写分叉

#### 将多维度向量混入同一 collection

后果：

- 无法稳定建索引
- 检索结果不可解释

#### 只看延迟不做召回评测

后果：

- ANN 看似更快，但结果质量显著下降

#### 文本 chunk 过短或过长

后果：

- 过短会像当前 `vector_documents` 一样几乎无语义价值
- 过长会稀释主题并增加 embedding 成本

### 12.2 可接受风险

#### 第一阶段继续保留策略域旧表

判断：

- 可接受
- 因为目标是低风险迁移，不是一次性重写

---

## 13. 近期建议的实施清单

建议先做以下 10 项：

1. 输出 `code -> stock_code` 的迁移脚本与兼容视图方案。
2. 新增统一向量 schema 初始化模块。
3. 新增统一向量存储 mixin。
4. 为策略域 collection 补 `model_id/vector_dim/version` 强约束。
5. 新建 `market_documents` / `market_doc_chunks`。
6. 新建文档 chunk 化与 embedding 回填任务。
7. 为文档 chunk 加 `tsvector` 索引。
8. 新建 `kline_pattern_windows` 与模式向量回填任务。
9. 将 `vector_search.py` 增加 DB 后端。
10. 建立 ANN 与 exact 检索基线评测。

---

## 14. 最终建议

当前项目最稳妥的落地方式不是“再加一个向量系统”，而是：

1. 先把 PostgreSQL 内的向量能力统一起来。
2. 复用策略域已经存在的治理模型。
3. 优先打通市场文档和 K 线模式两条最明确的向量场景。
4. 用评测结果决定是否需要进一步升级到 `pgvectorscale` 或迁移到 `Qdrant`。

只要这条路线执行到位，当前项目就会从“策略域有向量、市场域没有统一检索”的状态，进入“数据库主平面稳定、向量派生层清晰、可治理、可回滚”的状态。
