# AIASK 前端能力补全开发方案

> 日期：2026-05-28
> 依据：`artifacts/frontend-tour-2026-05-28/frontend-vs-backend-deep-audit.md`
> 原则：**最小改动、最大收益**；优先"后端只改 1 文件、前端 0 改动"的路径

---

## 一、总体策略

项目前端采用**动态 catalog 渲染**架构：
- `FinancialManagerWorkspace` 从 `GET /v1/desktop/financial-manager/catalog` 拉取 group + action 列表
- 前端 **不硬编码** 任何 action，而是遍历 `catalog.groups` 渲染 sidebar + 遍历 `catalog.actions` 渲染卡片
- 因此：**后端在 `server.py:FINANCIAL_MANAGER_ACTIONS` 加条目 → 前端自动出现新能力**

分三个阶段推进：
1. **P0（1-2 天）**：仅改 `server.py` 一个文件，扩充 FinancialManager catalog
2. **P1（3-5 天）**：新增 2-3 个独立前端 workspace（估值、交易计划、资金流）
3. **P2（5-10 天）**：深度面板（回测对比图表、因子 IC dashboard、NAV 曲线）

---

## 二、P0 阶段：扩充 FinancialManager Catalog（后端 1 文件改动）

### 改动文件
`packages/agent/src/aiask_agent/server.py`

### 新增 group 定义（加入 `FINANCIAL_MANAGER_GROUPS`）

```python
# 在现有 7 个 group 后追加：
{"id": "valuation", "label": "估值分析", "description": "DCF/DDM/相对估值/情景估值/共识估值"},
{"id": "decision", "label": "买卖决策", "description": "综合买卖建议、决策共识和闸门"},
{"id": "trade-plan", "label": "交易计划", "description": "入场方案/止损止盈/关键价位/仓位"},
{"id": "fund-flow", "label": "资金流向", "description": "北向资金、行业/概念资金流、个股资金流"},
{"id": "fundamental", "label": "基本面", "description": "杜邦分析、同行对比、综合基本面"},
{"id": "macro", "label": "宏观经济", "description": "GDP/CPI/PMI/M2 等宏观指标"},
{"id": "alerts", "label": "告警", "description": "价格/指标告警和组合条件告警"},
{"id": "limit-up", "label": "涨停与龙虎", "description": "涨停板统计、龙虎榜详情"},
```

### 新增 action 定义（加入 `FINANCIAL_MANAGER_ACTIONS`）

```python
# ─── 估值分析 group ───
{"capability_id": "valuation", "action_id": "dcf", "group": "valuation", "label": "DCF 估值",
 "mode": "read_only", "mcp_tool": "dcf_valuation",
 "default_params": {"code": "600519", "growth_rate": 0.05, "discount_rate": 0.1}},
{"capability_id": "valuation", "action_id": "ddm", "group": "valuation", "label": "DDM 估值",
 "mode": "read_only", "mcp_tool": "ddm_valuation",
 "default_params": {"code": "600519", "growth_rate": 0.05, "required_return": 0.1}},
{"capability_id": "valuation", "action_id": "relative", "group": "valuation", "label": "相对估值",
 "mode": "read_only", "mcp_tool": "relative_valuation",
 "default_params": {"code": "600519"}},
{"capability_id": "valuation", "action_id": "scenario_dcf", "group": "valuation", "label": "情景 DCF",
 "mode": "read_only", "mcp_tool": "scenario_dcf_valuation",
 "default_params": {"code": "600519", "industry": "消费"}},
{"capability_id": "valuation", "action_id": "consensus", "group": "valuation", "label": "估值共识",
 "mode": "read_only", "mcp_tool": "valuation_consensus",
 "default_params": {"code": "600519"}},
{"capability_id": "valuation", "action_id": "historical", "group": "valuation", "label": "历史估值",
 "mode": "read_only", "mcp_tool": "get_historical_valuation",
 "default_params": {"code": "600519", "days": 90}},

# ─── 买卖决策 group ───
{"capability_id": "decision", "action_id": "should_buy", "group": "decision", "label": "买入建议",
 "mode": "read_only", "mcp_tool": "should_i_buy",
 "default_params": {"code": "600519"}},
{"capability_id": "decision", "action_id": "should_sell", "group": "decision", "label": "卖出建议",
 "mode": "read_only", "mcp_tool": "should_i_sell",
 "default_params": {"code": "600519", "buy_price": 1800, "holding_days": 30}},
{"capability_id": "decision", "action_id": "consensus", "group": "decision", "label": "决策共识",
 "mode": "read_only", "mcp_tool": "decision_consensus",
 "default_params": {"code": "600519"}},
{"capability_id": "decision", "action_id": "unified", "group": "decision", "label": "统一决策",
 "mode": "read_only", "mcp_tool": "get_unified_decision",
 "default_params": {"code": "600519", "detail_level": "summary"}},

# ─── 交易计划 group ───
{"capability_id": "trade-plan", "action_id": "generate", "group": "trade-plan", "label": "生成交易计划",
 "mode": "read_only", "mcp_tool": "generate_trade_plan",
 "default_params": {"code": "600519", "capital": 1000000, "style": "balanced"}},
{"capability_id": "trade-plan", "action_id": "stop_levels", "group": "trade-plan", "label": "止损止盈",
 "mode": "read_only", "mcp_tool": "calculate_stop_levels",
 "default_params": {"code": "600519", "entry_price": 1800}},
{"capability_id": "trade-plan", "action_id": "key_levels", "group": "trade-plan", "label": "关键价位",
 "mode": "read_only", "mcp_tool": "get_key_levels",
 "default_params": {"code": "600519"}},

# ─── 资金流向 group ───
{"capability_id": "fund-flow", "action_id": "north", "group": "fund-flow", "label": "北向资金",
 "mode": "read_only", "mcp_tool": "get_north_fund",
 "default_params": {"days": 30}},
{"capability_id": "fund-flow", "action_id": "sector", "group": "fund-flow", "label": "行业资金流",
 "mode": "read_only", "mcp_tool": "get_sector_fund_flow",
 "default_params": {"top_n": 20}},
{"capability_id": "fund-flow", "action_id": "concept", "group": "fund-flow", "label": "概念资金流",
 "mode": "read_only", "mcp_tool": "get_concept_fund_flow",
 "default_params": {"top_n": 20}},
{"capability_id": "fund-flow", "action_id": "stock", "group": "fund-flow", "label": "个股资金流",
 "mode": "read_only", "mcp_tool": "get_stock_fund_flow",
 "default_params": {"code": "600519"}},
```


```python
# ─── 基本面 group ───
{"capability_id": "fundamental", "action_id": "analyze", "group": "fundamental", "label": "基本面分析",
 "mode": "read_only", "mcp_tool": "fundamental_analysis_manager", "mcp_action": "analyze",
 "default_params": {"code": "600519"}},
{"capability_id": "fundamental", "action_id": "dupont", "group": "fundamental", "label": "杜邦分析",
 "mode": "read_only", "mcp_tool": "fundamental_analysis_manager", "mcp_action": "dupont",
 "default_params": {"code": "600519"}},
{"capability_id": "fundamental", "action_id": "compare", "group": "fundamental", "label": "同行对比",
 "mode": "read_only", "mcp_tool": "fundamental_analysis_manager", "mcp_action": "compare",
 "default_params": {"code": "600519", "peers": ["000858", "002304"]}},

# ─── 宏观经济 group ───
{"capability_id": "macro", "action_id": "indicators", "group": "macro", "label": "宏观指标",
 "mode": "read_only", "mcp_tool": "macro_manager", "mcp_action": "get_indicators",
 "default_params": {"indicators": ["cpi", "pmi"], "limit": 12}},
{"capability_id": "macro", "action_id": "overview", "group": "macro", "label": "市场概览",
 "mode": "read_only", "mcp_tool": "macro_manager", "mcp_action": "market_overview",
 "default_params": {}},

# ─── 告警 group ───
{"capability_id": "alerts", "action_id": "check", "group": "alerts", "label": "检查所有告警",
 "mode": "read_only", "mcp_tool": "check_all_alerts",
 "default_params": {"status": "active"}},
{"capability_id": "alerts", "action_id": "create_indicator", "group": "alerts", "label": "创建指标告警",
 "mode": "read_only", "mcp_tool": "create_indicator_alert",
 "default_params": {"code": "600519", "indicator": "price", "condition": ">", "value": 2000}},

# ─── 涨停与龙虎 group ───
{"capability_id": "limit-up", "action_id": "list", "group": "limit-up", "label": "涨停板列表",
 "mode": "read_only", "mcp_tool": "limit_up_manager", "mcp_action": "list",
 "default_params": {}},
{"capability_id": "limit-up", "action_id": "statistics", "group": "limit-up", "label": "涨停统计",
 "mode": "read_only", "mcp_tool": "limit_up_manager", "mcp_action": "statistics",
 "default_params": {}},
{"capability_id": "trading-data", "action_id": "block_trades", "group": "limit-up", "label": "大宗交易",
 "mode": "read_only", "mcp_tool": "trading_data_manager", "mcp_action": "block_trades",
 "default_params": {"limit": 20}},
```

### 工作量评估

| 改动 | 行数 | 风险 |
|---|---|---|
| 新增 8 个 group 到 `FINANCIAL_MANAGER_GROUPS` | ~16 行 | 零风险（追加 tuple 条目） |
| 新增 ~30 个 action 到 `FINANCIAL_MANAGER_ACTIONS` | ~120 行 | 低风险（新条目不影响已有） |
| 总计 | ~136 行 Python | 前端无需改动 |

### 验证方式
1. 启动 Agent (`python -m uvicorn aiask_agent.server:app`)
2. 前端刷新金融经理台 → 左侧 sidebar 出现 8 个新 group
3. 点击任意新 action → JSON 表单自动填充 `default_params` → 点击"运行查询" → 显示结果


---

## 三、P1 阶段：新增独立前端 Workspace（3-5 天）

### 3.1 估值分析 Workspace

**目标**：给用户一个专门的估值页面，比 FinancialManager 的 JSON 表单更友好。

**文件**：`desktop/src/features/valuation/ValuationWorkspace.tsx`

**功能**：
- 输入：股票代码、行业选择、增长率/折现率滑块
- 5 路径卡片：DCF / DDM / 相对估值 / 情景 DCF / 共识
- 每个卡片显示：每股价值、当前价格、溢价/折价比
- 共识区：median / mean / min / max + dispersion 告警
- 调用 API：`api.financialManagerQuery({ capability_id: "valuation", action_id: "consensus", params: {...} })`

**路由集成**：在 `views.ts` 加一个 `{ id: "valuation", label: "估值", icon: TrendingUp, ... }`，App.tsx switch 加一条。

### 3.2 交易计划 Workspace

**文件**：`desktop/src/features/trade-plan/TradePlanWorkspace.tsx`

**功能**：
- 输入：股票代码、入场价（或自动取最新价）、资金量、风格选择
- 输出三栏：
  - 关键价位（支撑/阻力 + 强度 + 来源算法）
  - 止损止盈（ATR 止损 / 结构止损 / 1:1/1:2/1:3 止盈位）
  - 完整计划（方向 + 置信度 + 3 个场景化方案 + 仓位建议）
- 调用：`generate_trade_plan` / `calculate_stop_levels` / `get_key_levels` 三个 MCP 工具

### 3.3 资金流看板

**文件**：`desktop/src/features/fund-flow/FundFlowWorkspace.tsx`

**功能**：
- Tab：北向资金 / 行业资金流 / 概念资金流 / 个股资金流
- 北向：近 N 天净流入折线（纯 CSS 实现）
- 行业/概念：Top 20 表格（净流入排序）
- 个股：主力/大单/中单/小单流入柱状对比

### 3.4 Dead-letter 面板（加入 DataSyncWorkspace）

**改动**：在现有 `DataSyncWorkspace.tsx` 底部新增一个 section

**功能**：
- 调用 `api.readOnlyTool("get_dead_letters", { limit: 50 })` 显示失败记录列表
- 一键清空按钮 → `api.hermesToolCall("clear_dead_letters", {})`
- 显示每条 dead-letter 的 code / period / error / timestamp

---

## 四、P2 阶段：深度面板与图表（5-10 天）

### 4.1 回测对比页

**文件**：`desktop/src/features/backtest/BacktestCompareWorkspace.tsx`

**功能**：
- 列出已保存回测（`backtest_manager.list`）
- 选 2-4 个回测 → 调用 `backtest_manager.compare`
- 渲染：净值曲线（CSS gradient line）、年化/夏普/最大回撤对比表、交易明细折叠
- 无第三方图表库，用 SVG polyline + CSS 实现

### 4.2 因子 IC Dashboard

**文件**：`desktop/src/features/factor/FactorICDashboard.tsx`（嵌入 QuantResearchWorkspace 或独立）

**功能**：
- 选因子 + 股票池 → 调用 `calculate_factor_ic`
- 展示：Normal IC / Rank IC / IC_IR / bootstrap CI / 衰减曲线（multi-window）
- 分组回测结果：5 组收益柱状图

### 4.3 Paper Trading NAV 曲线

**文件**：扩展 `IncubationFactoryPanel.tsx` 或 `FinancialManagerWorkspace`

**功能**：
- 调用 `paper_trading_manager.nav_history` 拉取 NAV 时间序列
- SVG 折线图展示净值走势
- 配合 `matching_status` / `nav_status` 显示撮合引擎健康

### 4.4 Strategy Factory 深度面板

**改动**：在 `StrategyFactoryPanel.tsx` 里增加 tab

**新增 tab**：
- **Candidates**：从 `strategy_review_snapshot` payload 里拆出 candidates 列表渲染
- **Quality Reports**：从 factory_runs 数据拆出质量报告
- **TopN**：调用 `factory_topn_latest` 展示最新一轮的 top N 策略

### 4.5 任务清单面板

**改动**：在 InspectorPanel 的"摘要" tab 里追加一个折叠区

**功能**：
- 调用 `api.readOnlyTool("agent_todo_list", {})` 和 `api.readOnlyTool("agent_subgoal", { action: "status" })`
- 渲染当前任务 plan + subgoal 进度


---

## 五、不纳入本方案的能力（确认为安全设计）

以下后端能力**有意不暴露前端 UI**，本方案不涉及：

| 能力 | 不暴露原因 |
|---|---|
| Hermes Full 写操作（file_write/patch/terminal/browser 操作/execute_python） | 模型在对话循环内调用，用户不应手动触发 |
| 多模态生成（image/video/TTS/STT） | 同上 |
| Live broker 下单 | V1 固定 blocked |
| Gateway 直发消息 | 统一走 ActionIntent 审批链 |
| TUI gateway | vendor 不挂载 |
| DEV-V1 ops CLI | 运维场景，不进桌面端 |
| run_all_factories supervisor | 进程级管理，不适合 Web 化 |
| Computer Use | 安全边界 |
| agent_delegate_task / session_handoff | 多代理协作，后续版本考虑 |

---

## 六、实施路线图

```
Week 1 (Day 1-2): P0 — server.py 扩充 catalog
  ├── 添加 8 个 group + ~30 个 action
  ├── 测试 Agent 启动 + 前端金融经理台自动渲染
  └── 合并到 main

Week 1-2 (Day 3-7): P1 — 新 Workspace
  ├── ValuationWorkspace (Day 3-4)
  ├── TradePlanWorkspace (Day 4-5)
  ├── FundFlowWorkspace (Day 5-6)
  ├── DataSync dead-letter section (Day 6)
  └── 路由集成 + 工作流卡片入口 (Day 7)

Week 2-3 (Day 8-14): P2 — 深度面板
  ├── BacktestCompareWorkspace + SVG 图表 (Day 8-10)
  ├── FactorICDashboard (Day 10-11)
  ├── Paper Trading NAV 曲线 (Day 11-12)
  ├── Strategy Factory deep tabs (Day 12-13)
  └── Inspector 任务清单 (Day 14)
```

---

## 七、技术约束

1. **不引入新依赖**：图表用 SVG polyline + CSS gradient 实现（参考现有 MetricCard / StatusBadge 风格）
2. **不改动 API 鉴权模型**：新 action 全部走现有 `require_api` + `control_authorized` 链路
3. **不改动 ActionIntent 白名单**：P0 新增的 action 全部是 `mode: "read_only"`，无需改 `intents.py:ALLOWED_ACTIONS`
4. **保持 MCP 工具名不变**：catalog 里的 `mcp_tool` 字段指向已注册的 MCP 工具名（如 `dcf_valuation`），后端通过 `_wrapped_mcp_lookup` 自动查找 `agent_mcp_akshare_dcf_valuation`
5. **保持前端动态渲染**：不在前端硬编码任何新 action 的特殊 UI，只利用 FinancialManagerWorkspace 的通用 JSON form → 结果展示流程
6. **P1 新 Workspace 可选**：如果时间紧张，P0 完成后用户已经能在金融经理台操作所有新能力（只是 UX 体验不如专属页）

---

## 八、预期成果

| 阶段 | 新增可用能力 | 用户可见变化 |
|---|---|---|
| P0 完成 | +30 个 financial action | 金融经理台 sidebar 从 7 → 15 个 group |
| P1 完成 | +3 个专属 Workspace | 工作流卡片新增"估值分析""交易计划""资金流向" |
| P2 完成 | +5 个深度面板 | 回测净值曲线、因子 IC、NAV、策略 TopN、任务清单 |

覆盖率变化（预估）：
- L3 金融推理层：10% → **65%**
- L4 执行细节层：45% → **70%**
- 整体 FinancialManager action 覆盖：34/350+ → **64/350+**（翻倍）


---

## 九、可行性验证结论

### 验证方法
逐项对照源码确认方案中每个技术假设：

### 9.1 P0 核心假设验证

| # | 假设 | 验证结果 | 证据 |
|---|---|---|---|
| 1 | 前端动态渲染 catalog | ✅ 可行 | `FinancialManagerWorkspace.tsx` 遍历 `catalog.groups` / `catalog.actions`，无硬编码 |
| 2 | `_wrapped_mcp_lookup` 能解析 `mcp_tool` 后缀 | ✅ 可行 | `server.py:736-753` 明确用 `lowered.endswith(raw_tool)` 做后缀匹配 |
| 3 | `dcf_valuation` 等工具名存在 | ✅ 已确认 | `tools/valuation.py` 用 `@mcp.tool()` 注册了全部 6 个估值工具 |
| 4 | `should_i_buy` 等决策工具名存在 | ✅ 已确认 | `tools/decision.py` + `_decision_buy.py` 有 `@mcp.tool()` 注册 |
| 5 | `generate_trade_plan` 工具名存在 | ✅ 已确认 | `tools/trade_plan_parts/actions.py` 有 `@mcp.tool()` 注册 |
| 6 | `calculate_stop_levels` / `get_key_levels` 存在 | ✅ 已确认 | `tools/stop_levels.py` / `tools/key_levels.py` |
| 7 | `get_north_fund` / `get_sector_fund_flow` 等存在 | ✅ 已确认 | `tools/fund_flow_north.py` / `fund_flow_sector.py` |
| 8 | `check_all_alerts` / `create_indicator_alert` 存在 | ✅ 已确认 | `tools/alerts.py` |
| 9 | `fundamental_analysis_manager` 接受 `action + params` | ✅ 已确认 | 统一 action + kwargs 协议 |
| 10 | 独立工具（无 `mcp_action`）直接传 params | ✅ 已确认 | `_manager_arguments` 当 `mcp_action` 为空时返回 `dict(params)` |
| 11 | 不需修改 `intents.py` | ⚠️ 需注意 | P0 新增全部为 `read_only` 无需改白名单；`create_indicator_alert` 有写副作用但暂用 read_only 模式，正式上线前应改为 intent |
| 12 | 不需修改前端代码 | ✅ 可行 | 前端 `FinancialManagerWorkspace` 完全从 backend catalog 驱动 |

### 9.2 P1 假设验证

| # | 假设 | 验证结果 |
|---|---|---|
| 1 | 新建 workspace 只需加 view + App.tsx switch | ✅ 已有 22 个 workspace 参照模式 |
| 2 | 新 workspace 调 `api.financialManagerQuery()` 即可 | ✅ 该方法接受任意 capability_id + action_id + params |
| 3 | SVG 图表无第三方依赖 | ✅ 项目 package.json 无图表库，现有 MetricCard 用纯 CSS |

### 9.3 已识别风险

| 风险 | 级别 | 缓解措施 |
|---|---|---|
| `create_indicator_alert` 是写操作但标为 read_only | 中 | P0 阶段标注 `⚠️ stateful`，正式上线前加入 intent 白名单 |
| MCP 工具未注册时显示 `missing_mcp_tool` | 低 | `_financial_catalog_payload` 已处理此情况，前端显示"不可用" |
| 部分工具需要 Agent 连接 AkShare MCP 服务器 | 低 | 这是运行时依赖，非代码问题；status 字段会反映 |
| `decision_consensus` 调用较慢（内部跑 5 个决策工具） | 低 | 前端已有 loading 状态；可在 default_params 加 timeout 提示 |

### 9.4 最终结论

**方案整体可行**。P0 阶段（扩充 `server.py` 一个文件）的技术路径完全经过源码验证：
- 后缀匹配机制确保 `mcp_tool` 名称能正确解析到 `agent_mcp_akshare_*` 包装名
- 独立工具和 manager 工具的参数传递路径均已确认
- 前端无需任何改动即可自动渲染新 group 和 action
- 唯一需要额外处理的是写操作类告警创建（建议 P0 先标 read_only 让用户可测试，后续补 intent 链路）


---

## 十、联网验证补充（2026-05-28）

### 10.1 MCP SDK 安全性

- **CVE-2025-66416**（DNS rebinding，影响 mcp < 1.23.0）：项目使用 **mcp==1.25.0**（lock 文件确认），**不受影响** ✅
- 项目 MCP 服务器运行在 localhost + 不使用 SSE transport（走 stdio），即便旧版也不受此 CVE 影响

### 10.2 FastAPI 性能无风险

- 搜索结果确认：FastAPI 在大量静态配置（tuple/dict）场景下无性能问题，瓶颈在 I/O 和数据库
- 本方案新增 ~30 个 dict 条目到 `FINANCIAL_MANAGER_ACTIONS` tuple，模块加载时一次性构造，不影响请求处理性能
- `_wrapped_mcp_lookup` 在每次请求时遍历 tool names（~100 个），这是 O(n) 但 n 极小（< 200），无需优化

### 10.3 React 动态表单渲染

- 搜索结果指出：动态表单在 **40+ 字段**时可能有性能问题（因重渲染）
- 本方案金融经理台每次只渲染一个 group 的 action（最多 8 个），每个 action 的 form 字段 ≤ 8 个（`formFields()` 限制 `.slice(0, 8)`）
- 完全在安全范围内，无性能风险 ✅

### 10.4 SVG 图表可行性（P2）

- 搜索结果确认：SVG polyline 在 **< 1000 数据点** 场景下表现优秀
- 金融净值曲线通常 250 个交易日 = 250 个点，完全在 SVG 最佳区间
- headway.io 有完整的 React SVG Line Chart 教程（无第三方库），与方案 P2 思路一致
- 如果未来需要 > 1000 点（分钟级数据），可考虑 Canvas 降级，但 P2 阶段无此需求

### 10.5 Vite + React SPA 新增页面

- 搜索结果确认：Vite + React SPA 的 view 路由通过条件渲染（if/switch）或 wouter/react-router 实现
- 本项目使用 **条件渲染模式**（`App.tsx` 里 `{mainView === "xxx" && <Component />}`），新增 view 只需加一条，完全标准
- 不引入 react-router，保持现有模式一致性

### 10.6 综合结论

**联网研究未发现任何阻碍方案实施的问题。** 唯一值得后续关注的是：
1. MCP SDK 保持更新（当前 1.25.0 安全）
2. 如果告警创建需要正式化为 ActionIntent，需修改 `intents.py`（已在 9.3 风险表中标注）
