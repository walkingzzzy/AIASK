# 项目审查报告（以 MCP 服务为核心）

审查日期：2026-03-24  
审查范围：`apps/web`、`apps/bff`、`packages/akshare-mcp`、`packages/strategy-factory`，以及相关数据库、向量检索、因子挖掘实现  
审查目标：确认项目当前“实际实现了什么”，梳理前后端、MCP 服务、策略工厂、数据库、向量能力、因子挖掘之间的真实协同关系，并给出流程审查、功能审查和风险清单

---

## 1. 执行摘要

### 1.1 总体判断

当前项目的真实核心是 `packages/akshare-mcp` 提供的 Python MCP 服务，而不是 Web 页面或 BFF 本身。  
Web 和 BFF 已经形成比较完整的产品承载层，但大部分核心业务能力仍然以 MCP 工具、Manager 编排器、服务层与 TimescaleDB 持久化为主。

从“代码存在”与“闭环已跑通”两个维度区分，当前状态可以概括为：

- `MCP 核心能力`：已形成大规模能力面，是真正的业务核心。
- `BFF 编排层`：不是简单透传，已经承担了 `stdio 连接池`、HTTP 接口、缓存、错误语义归一化、少量 ViewModel 组装。
- `策略工厂`：已从 MCP 内部兼容层迁出为独立包，存在真实的调度器、候选生成、评审、孵化、运行时风险、向量画像、事件投影链路。
- `数据库`：存在真实的 TimescaleDB + PostgreSQL 双轨承载，不是只在内存里跑 Demo。
- `向量能力`：存在两套实现，通用相似检索主要是进程内 `python/index`，策略工厂行为向量则有 TimescaleDB 持久化与可选 `pgvector` 增强。
- `因子挖掘`：不是概念页面，`quant_manager` 已经承载候选生成、验证、研究记忆、候选池治理与调度。

### 1.2 结论

项目已经具备“以 MCP 为核心”的真实工程结构，且策略工厂与因子挖掘不是空壳。  
但当前仍然存在几个明显的工程问题，优先级最高的是：

1. 因子调度器的 LLM 候选生成链路有断点。
2. 多个高风险运维接口只做了登录校验，没有做管理员隔离。
3. 搜索接口的 query 参数校验存在可用性问题。
4. 策略向量索引“快照”实际会被覆盖，历史审计证据不足。

---

## 2. 审查方法与基线

本次审查以仓库当前代码为准，不以 README 的静态描述为准。  
采用的方法是：

- 盘点仓库结构与入口文件
- 核查 MCP 工具注册、Manager 编排、服务层与存储层
- 核查 BFF Controller/Service 与 MCP 的真实调用关系
- 核查 Web 页面是否真正接入后端能力
- 抽查数据库 schema、向量平台、策略工厂、因子挖掘主路径
- 运行局部验证命令确认基本现状

### 2.1 本次确认到的运行时基线

- Web 页面数量：`39`
- BFF 模块目录数量：`34`
- MCP 运行时工具数量：`145`
- `tools/managers` 目录下 Python 文件数量：`40`

### 2.2 本次执行的验证

- `npm test -w apps/bff`：通过，`11/11`
- 通过 `PYTHONPATH=src:../strategy-factory/src python -c ... build_tool_registry()` 核对运行时工具注册表：得到 `145` 个工具

说明：

- 本次没有对全量 e2e、全量 Python 测试、全量真实数据源联调做完整回归。
- 本次审查重点是“代码实现现状”和“模块协同关系”，不是生产环境 SLA 验证。

---

## 3. 项目实际架构

当前项目的真实主链路是：

`Web (Next.js)`  
-> `BFF (NestJS)`  
-> `McpGatewayService (stdio 连接池)`  
-> `Python FastMCP Server`  
-> `Manager / Service / Storage`  
-> `TimescaleDB / PostgreSQL / artifacts`

### 3.1 Web 层

`apps/web` 已经不是简单展示页，至少以下分域有明确产品承载：

- `/strategy-market`
- `/strategy-market/[id]`
- `/factor`
- `/factor-analysis`
- `/search`
- `/market`
- `/research`
- `/portfolio`
- `/paper-trading`
- `/risk`
- `/admin/*`

其中最成熟、最直接绑定 MCP 核心能力的页面是：

- `apps/web/app/strategy-market/page.tsx`
- `apps/web/app/strategy-market/[id]/page.tsx`
- `apps/web/app/factor/page.tsx`
- `apps/web/app/factor/components/factor-mining-workbench.tsx`

### 3.2 BFF 层

`apps/bff` 的职责不是重写业务逻辑，而是：

- 暴露 HTTP API
- 做登录与角色校验
- 做 MCP `stdio` 客户端连接池
- 做缓存与退化包装
- 做错误映射、traceId 传递与部分 DTO/view model 归一化

关键入口：

- `apps/bff/src/app.module.ts`
- `apps/bff/src/main.ts`
- `apps/bff/src/mcp-gateway/mcp-gateway.service.ts`

其中 `McpGatewayService` 是关键桥梁，负责：

- 在 BFF 进程中维护 MCP 连接池
- 通过 `python -m akshare_mcp.server` 方式连接 MCP
- 区分 `full` / `tool-only` 启动 profile
- 统一解析 MCP 工具响应
- 提取 `structuredContent` / `content` 并做 fallback 元数据归一化

### 3.3 MCP 层

`packages/akshare-mcp` 是当前项目的真实业务核心。

核心入口：

- `packages/akshare-mcp/src/akshare_mcp/server.py`
- `packages/akshare-mcp/src/akshare_mcp/tool_registry.py`

MCP 启动后会注册多个能力域：

- market
- finance
- fund_flow
- macro
- news
- options
- technical
- backtest
- portfolio
- valuation
- decision
- search
- semantic
- research
- alerts
- quant
- sentiment
- vector
- skills
- managers
- factor_profile
- basic_data
- data_sync

并在非 `tool-only` profile 下启动后台服务：

- `FactorScheduler`
- `MatchingEngine`
- `NavEngine`
- `SignalTracker`
- `StrategyFactory`
- `StartupValidator`
- `DataSyncScheduler`

### 3.4 策略工厂分域包

`packages/strategy-factory` 已经是独立包，不再只是 MCP 内部文件夹。

当前结构：

- `api`
- `application`
- `domain`
- `infrastructure`

当前状态应理解为：

- 新包已承载策略工厂主要实现
- `akshare_mcp.services.strategy_factory` 仍保留兼容导出层
- 旧路径的存在主要是为了兼容历史调用和 monkeypatch 测试面

---

## 4. 以 MCP 为核心的实际功能面

### 4.1 MCP 工具能力

运行时注册表实测为 `145` 个工具，能力域覆盖包括：

- 行情与市场数据
- 财务与估值
- 资金流与新闻事件
- 技术分析与回测
- 组合与风险
- 量化研究与因子验证
- 搜索与语义检索
- 向量检索
- Skills 与 Manager 编排器
- 数据同步与缓存运维
- 策略超市 / 策略工厂 / 运行时风控 / 晋级流水线

### 4.2 Manager 编排模式已经成型

项目不是靠前端拼装原子工具，而是广泛采用 `action + kwargs/params` 的 Manager 模式。

核心 Manager 包括：

- `strategy_manager`
- `quant_manager`
- `portfolio_manager`
- `paper_trading_manager`
- `risk_manager`
- `performance_manager`
- `execution_manager`
- `decision_manager`
- `data_sync_manager`
- `vector_search_manager`

这意味着项目已经进入“AI/前端/BFF 调用流程化能力”的阶段，而不是只有原子工具。

---

## 5. 模块审查

## 5.1 前端实际实现情况

### 5.1.1 策略超市 / 工厂审查页面

`apps/web/app/strategy-market/page.tsx` 已经直接调用：

- `/strategy-market/ranking`
- `/strategy-market/factory/status`
- `/strategy-market/capabilities`
- `/strategy-market/daily-snapshot`
- `/strategy-market/factory/runs`

并且页面提供“立即运行一轮工厂”的按钮，说明产品层已经直接暴露工厂触发入口。

`apps/web/app/strategy-market/[id]/page.tsx` 与 `use-strategy-detail-page.ts` 则把策略详情拆为三类视图：

- 策略概览
- 实盘跟踪
- 工厂审查

工厂审查页会进一步按需拉取：

- review report
- events
- incubation overview / metrics / pipeline
- paper account / orders / nav
- risk events / risk snapshots / runtime alerts / runtime control
- vector profiles / ANN search / index snapshots
- AI experiments / task runs
- domain events / domain projection / projection snapshots

这说明 `strategy-market` 已经是当前最完整的产品化分域。

### 5.1.2 因子研究 / 因子挖掘页面

`apps/web/app/factor/page.tsx` 不是单纯做传统因子展示，而是明确区分两条链路：

- 普通因子研究：因子库、因子计算、IC、回测、OOS、稳健性
- AI 因子挖掘：候选生成、验证、研究记忆、候选池、回放、调度器

`apps/web/app/factor/components/factor-mining-workbench.tsx` 已经接入：

- `POST /factor/llm-mining`
- `POST /factor/validate-candidate`
- `POST /factor/research-memory`
- `POST /factor/candidate-registry`
- `POST /factor/replay-episode`
- `GET /factor/scheduler-status`
- `POST /factor/scheduler-run-now`

因此“AI 因子挖掘”在当前项目中确实已经部分产品化。

### 5.1.3 前端结论

当前前端不是空壳，但并不是“全 MCP 全量产品化”。  
真正成熟的产品化区域主要是：

- 策略超市 / 策略工厂
- 因子研究 / 因子挖掘

其它页面更多是分域接入，不代表每个 MCP 工具都已经有完整 UI 闭环。

---

## 5.2 BFF 实际实现情况

### 5.2.1 核心定位

BFF 的真实价值主要在三点：

1. `stdio` 方式连接 Python MCP，并做连接池化
2. 将 MCP 工具转换成 Web 前端可消费的 HTTP API
3. 为策略域、因子域等重点分域增加缓存、鉴权、错误语义与少量聚合

### 5.2.2 策略域不是简单透传

`apps/bff/src/strategy/strategy.service.ts` 对 `strategy_manager` 做了较重的业务包装：

- 排行缓存
- 工厂运行缓存
- 自动刷新定时器
- 详情页 `view_model` 归一化
- 错误码到 HTTP 异常的映射

说明 BFF 在策略域已经具有明显的“产品后端”角色。

### 5.2.3 因子域是 `quant_manager` 的 Web 包装层

`apps/bff/src/factor/factor.service.ts` 基本围绕两条能力做包装：

- 原子因子工具：`calculate_factor`、`calculate_factor_ic`、`backtest_factor`、`validate_factor_oos`、`factor_robustness_check`
- 编排能力：`quant_manager`

其中真正有研究闭环价值的是后者。

### 5.2.4 BFF 结论

BFF 当前是“强编排 + HTTP 承载 + 缓存/鉴权层”，不是重复实现业务逻辑。  
项目真正的业务收敛点仍然在 Python MCP。

---

## 5.3 MCP 服务实际实现情况

### 5.3.1 真实核心

`packages/akshare-mcp/src/akshare_mcp/server.py` 已经形成成熟的服务启动入口，特点包括：

- 全量工具注册
- 启动 profile 控制
- HTTP transport 安全基线
- 后台服务生命周期管理
- atexit 关闭与共享客户端清理

### 5.3.2 工具注册真实存在

`tool_registry.py` 能够反射所有注册工具的：

- category
- module
- implementation path
- signature
- docstring
- parameter metadata

这不是文档概念，而是运行时 introspection。

### 5.3.3 MCP 结论

项目不是“围绕 MCP 协议做包装”，而是“业务本身就是以 MCP 工具与 Manager 为第一等公民设计的”。  
这一点是当前项目最清晰、最稳定的工程事实。

---

## 5.4 策略工厂实际实现情况

### 5.4.1 当前已实现的能力

从 `strategy_manager`、`strategy_factory` 包和相关 BFF/前端入口来看，策略工厂当前已覆盖：

- 工厂调度器
- 候选生成
- 评审与质量门控
- 提交与状态流转
- 孵化账户与模拟盘
- 运行时风控
- 运行告警与控制平面
- 晋级评审
- 领域事件与投影快照
- 行为向量画像与 ANN 索引治理
- AI 生成实验记录

### 5.4.2 关键证据

核心入口包括：

- `packages/strategy-factory/src/strategy_factory/application/factory_scheduler.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_mgr_lifecycle.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_mgr_runtime.py`
- `apps/bff/src/strategy/*.controller.ts`

### 5.4.3 当前实际状态

策略工厂已经具备“真实后端实现 + 前端产品承载 + 数据库存证”的条件。  
它不是一个 PPT 模块。  
但要强调：

- 有实现，不等于所有调度闭环在当前环境都稳定执行
- 有页面，不等于所有阶段都存在完整的 run evidence

这也是本次审查始终区分“实现存在”和“闭环稳定”的原因。

---

## 5.5 数据库实际实现情况

### 5.5.1 BFF 数据库

`apps/bff/src/db/db.service.ts` 使用 `pg` 连接 PostgreSQL，负责：

- 启动时连通性检查
- 自动执行 `apps/bff/migrations/*.sql`
- 在 `DATABASE_URL` 存在时启用持久化能力

它是 BFF 自己的 app-side 数据库层。

### 5.5.2 MCP 数据库

`packages/akshare-mcp/src/akshare_mcp/storage/timescaledb` 是更核心的数据层：

- `TimescaleDBAdapter` 由多个 Mixin 组合
- 启动时初始化 market schema 和 strategy schema
- 会 best-effort 执行 `CREATE EXTENSION IF NOT EXISTS vector`
- `supports_pgvector()` 决定向量增强路径是否可用

### 5.5.3 数据库存储结论

项目数据库不是单一用途。

- BFF DB：偏应用层与迁移
- MCP DB：偏研究、策略、工件、时序数据、向量与运行时证据

这种双轨设计是当前项目的真实现状。

---

## 5.6 向量数据库 / 向量检索实际实现情况

### 5.6.1 不是独立外部向量数据库服务

当前项目的“向量数据库”不是 Qdrant / Milvus / Chroma 这类独立服务。  
实际是两类实现并存：

1. 通用相似检索：`VectorSearchEngine`
   - 进程内 `python/index`
   - 主要用于 K 线相似检索等场景
2. 策略工厂行为向量：`StrategyVectorPlatform`
   - 持久化到 TimescaleDB JSON 表
   - 若 `pgvector` 可用，则镜像到 `vector` 列并建立 HNSW 索引

### 5.6.2 当前已实现的策略向量能力

已确认存在：

- `strategy_vector_profiles`
- `vector_index_registry`
- `strategy_vector_index_snapshots`
- `strategy_vector_index_items`
- `strategy_vector_profile_store`（pgvector gated）
- `strategy_vector_index_item_store`（pgvector gated）

并且 `StrategyVectorPlatform` 已实现：

- 画像构建
- 批量建档
- 持久化 ANN 索引构建
- `pgvector` 路径检索
- fallback 到 persisted ANN 或 full scan
- 健康检查
- HNSW 索引管理

### 5.6.3 向量结论

当前项目已经具备“策略向量平台”，但它依赖 TimescaleDB/pgvector 组合，而不是独立向量库服务。  
因此对外描述时应说“内嵌式向量平台 / pgvector 增强路径”，而不是“已接入独立向量数据库集群”。

---

## 5.7 因子挖掘实际实现情况

### 5.7.1 已实现能力

`quant_manager` 已经实现以下关键链路：

- `llm_factor_mining`
- `validate_factor_candidate`
- `factor_research_memory`
- `factor_candidate_registry`
- `replay_factor_episode`
- `scheduler_status`
- `scheduler_run_now`

### 5.7.2 因子研究与治理链

当前因子挖掘并不是单点工具，而是一条有治理含义的链路：

`候选生成`  
-> `DSL 编译与验证`  
-> `研究记忆`  
-> `候选池注册表`  
-> `调度器状态/运行`

同时，策略工厂中的 `FactorResearchBuilder` 会继续消费：

- `factor_ic_history`
- `factor_candidate_registry(active_pool)`
- `factor_scheduler.status`

这说明因子挖掘已经与策略工厂产生了真实耦合，而不是两套完全孤立的实现。

### 5.7.3 因子挖掘结论

当前项目已经具备“AI 因子候选生成 + 验证 + 治理”的基础闭环。  
但调度器与运行期自动化部分仍存在缺口，见后文问题清单。

---

## 6. 模块协同流程审查

## 6.1 策略工厂主流程

当前真实链路大致为：

`前端 /strategy-market`  
-> `BFF StrategyMarketService`  
-> `strategy_manager`  
-> `strategy_mgr_* handlers`  
-> `strategy_factory package / runtime services / db`

这一链路已经能够覆盖：

- 状态查看
- 工厂手动触发
- 历史运行记录
- 评审报告
- 孵化和模拟盘
- 运行时风控与告警
- 向量治理
- 事件投影

### 协同评价

- 协同是存在的
- 数据流是清晰的
- 证据可回溯到数据库表
- BFF 与前端都已经围绕它构建产品层

这是当前项目最成熟的一条闭环

## 6.2 因子挖掘主流程

当前真实链路大致为：

`前端 /factor workbench`  
-> `BFF FactorController / FactorService`  
-> `quant_manager`  
-> `LLM provider / compiler / validation pipeline / memory / registry / scheduler`

### 协同评价

- 候选生成、验证、治理的代码是打通的
- 前端已经暴露主要操作入口
- 与策略工厂之间存在“研究结果复用”

但自动调度链目前不够稳，原因见问题清单

## 6.3 搜索与向量协同

搜索分域当前是：

- `similarStocks` -> `search_similar_stocks`
- `semanticSearch` -> `semantic_stock_search`
- `searchByKline` -> `search_by_kline`

其中 `search_by_kline` 最终调用 `VectorSearchEngine.find_similar_patterns()`。  
说明通用向量检索能力已经进入 BFF 与 Web 可调用路径。

---

## 7. 关键问题清单

## 7.1 P1：因子调度器的 LLM 候选生成链路断开

### 现象

`FactorScheduler` 在开启 `FACTOR_SCHEDULER_LLM_MINING` 时会调用：

- `quant_manager(action="llm_factor_mining")`

但它没有传 `codes`。

### 代码证据

- `packages/akshare-mcp/src/akshare_mcp/services/factor_scheduler.py:240-247`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/quant_manager.py:757-763`

### 影响

`llm_factor_mining` 明确要求 `code` 或 `codes`，所以调度器这条自动化链在当前实现下不会真正生成候选因子。

### 判断

这是实打实的功能断链，不是文档问题。

---

## 7.2 P1：高风险运维接口未做管理员隔离

### 现象

多个会改变系统状态或触发高成本作业的接口没有加 `@Roles('admin')`，只依赖全局登录态。

### 典型接口

- `POST /strategy-market/factory/run-once`
- `POST /strategy-market/ai/generate`
- `POST /strategy-market/vector-indexes/rebuild`
- `POST /strategy-market/vector-indexes/cleanup`
- `POST /strategy-market/:id/risk-scan/run`
- `POST /strategy-market/:id/runtime-control`
- `POST /factor/scheduler-run-now`

### 代码证据

- `apps/bff/src/rbac/auth.guard.ts:13-29`
- `apps/bff/src/rbac/roles.guard.ts:15-36`
- `apps/bff/src/strategy/strategy-factory.controller.ts:22-25,52-55`
- `apps/bff/src/strategy/strategy-vector.controller.ts:47-67`
- `apps/bff/src/strategy/strategy-risk.controller.ts:33-89`
- `apps/bff/src/factor/factor.controller.ts:342-349`

### 影响

- 任意已登录用户可能触发系统级运行任务
- 可能改写索引、控制模式、运行告警状态
- 会导致审计边界和租户边界弱化

### 判断

这是权限边界设计问题，优先级应高。

---

## 7.3 P2：搜索接口 query 参数校验存在可用性问题

### 现象

`SearchController` 的 query DTO 中使用了 `@IsNumber()`，但没有配合 `@Type(() => Number)` 或 `ParseIntPipe`。

与此同时，全局 `ValidationPipe` 只开启了：

- `whitelist: true`
- `transform: true`

没有看到显式 `enableImplicitConversion`。

### 代码证据

- `apps/bff/src/search/search.controller.ts:5-19`
- `apps/bff/src/main.ts:27`

### 影响

标准 URL 查询参数是字符串，`topN` / `limit` 很容易在校验阶段被判为非法。

### 判断

这是 BFF 接口层的可用性缺陷，影响直接用户调用。

---

## 7.4 P2：向量索引“快照”会被覆盖，历史审计证据不足

### 现象

`strategy_vector_index_snapshots` 表对 `(index_name, index_version)` 做了唯一约束，  
存储层 `save_strategy_vector_index_snapshot()` 又使用 `ON CONFLICT ... DO UPDATE`。

### 代码证据

- `packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/schema_strategy.py:703-723`
- `packages/akshare-mcp/src/akshare_mcp/storage/timescaledb/strategy_vector.py:196-223`

### 影响

同一 `index_name + index_version` 的多次 `building -> active -> failed` 状态会被覆盖，无法形成真正的历史快照序列。

### 判断

如果把它当“当前状态表”可以接受；  
如果把它当“快照/审计历史表”，当前实现是不成立的。

---

## 7.5 P3：策略域存在重复路由声明

### 现象

`GET /strategy-market/task-runs` 在两个 controller 中重复存在：

- `StrategyMarketController`
- `StrategyFactoryController`

### 代码证据

- `apps/bff/src/strategy/strategy.controller.ts:86-94`
- `apps/bff/src/strategy/strategy-factory.controller.ts:64-67`

### 影响

当前两边都调用同一个 service，短期影响有限。  
但这会降低后续维护的确定性，增加 DTO、缓存、权限、文档漂移风险。

---

## 7.6 P3：策略排名自动刷新存在启动期 readiness 噪声

### 现象

`StrategyMarketService` 在 `onModuleInit()` 即启动自动刷新计时器。  
本次执行 `npm test -w apps/bff` 时实际观察到了：

- `策略排名自动刷新失败 date=2026-03-24: BadGatewayException: 调用 strategy_manager.rank 失败`

### 代码证据

- `apps/bff/src/strategy/strategy.service.ts:36-55`

### 影响

在 MCP 尚未 ready 或测试桩未准备好的阶段，BFF 启动会主动打出错误日志。

### 判断

这是运维可观测性与启动顺序治理问题，不是核心功能断链，但建议修。

---

## 8. 真实能力与对外表述边界

当前项目可以明确对外表述的事实：

- 这是一个以 MCP 服务为核心的 A 股投研平台
- Python MCP 服务是主业务核心
- 策略工厂、因子挖掘、向量平台、TimescaleDB 持久化都已有真实实现
- Web 与 BFF 已经产品化承载了其中的重要子集

当前不应直接对外表述为既成事实的内容：

- 所有链路都已生产稳定
- 所有工具都已完整产品化
- 向量数据库是独立外部服务
- 所有策略工厂阶段都已有完整历史证据
- 因子调度器的 LLM 自动化已经稳定跑通

---

## 9. 整改优先级建议

## 9.1 第一优先级

1. 修复 `FactorScheduler -> llm_factor_mining` 缺少 `codes` 的断链
2. 为高风险运维接口补充 `@Roles('admin')` 或更细粒度权限
3. 为搜索接口 query 参数补充类型转换

## 9.2 第二优先级

1. 明确 `strategy_vector_index_snapshots` 是“状态表”还是“历史快照表”
2. 若保留历史证据，取消 `(index_name, index_version)` 的唯一覆盖模型
3. 清理重复路由与接口归属

## 9.3 第三优先级

1. 为 BFF 启动期自动刷新增加 readiness 判断
2. 将“产品页已接入能力”与“仅 MCP 内部可用能力”做清晰矩阵
3. 增强策略工厂与因子挖掘的运行证据与审计展示

---

## 10. 最终结论

从当前仓库代码来看，这个项目已经不是“前端页面 + 一些工具”的松散组合，而是一个清晰的：

- `MCP 核心业务层`
- `BFF 编排与承载层`
- `Web 产品层`
- `策略工厂独立包`
- `TimescaleDB / pgvector 增强数据层`

的完整工程体系。

其中最值得确认的事实是：

- MCP 是真实核心
- 策略工厂已经真实落地
- 因子挖掘已经真实落地
- 向量能力已经真实落地，但不是独立外部向量库
- 当前主要问题集中在权限边界、调度链完整性、审计留痕与接口治理

如果后续需要继续推进，建议先按第 9 节的优先级处理，再进入更细的性能、口径统一和观测性治理阶段。

