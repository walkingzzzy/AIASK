# 数据维护手册（AIASK / 策略工厂数据面）

日期：2026-06-03
版本：DATA-MAINTENANCE-V1
适用范围：`packages/akshare-mcp`、`packages/strategy-factory`、`packages/aiask-quant-core`
共用生产库：`data/db/akshare_mcp.sqlite3`（WAL 模式，约 3GB）

> 目的：把"数据新鲜度 / 同步 / 缓存 / 失败落盘"这些运维动作沉淀成可复核的 runbook，
> 避免再次出现"K线/因子 IC 过期 13 天 → 前向收益恒 0 / 因子 trend 失真"这类隐性故障。

---

## 0. 一分钟速查（最常用）

| 症状 | 先查 | 再修 |
|---|---|---|
| forward_returns 恒 0 | K线新鲜度（§2.1） | `sync_stale_klines`（§2.2） |
| 因子 trend 失真 / factor_ic 为空 | `factor_ic_history.maxdate`（§3.1） | FactorScheduler 重算（§3.2） |
| 同步任务卡住 / 报错堆积 | `get_sync_status`（§4.1） | 看 dead-letters（§4.2） |
| 磁盘涨太快 / 命中率低 | `get_cache_stats`（§5.1） | `clear_cache`（§5.2） |
| 策略工厂候选全是 fixed_defaults | K线 + factor_ic 双新鲜度 | §2 + §3 一起做 |

**统一前提**：所有命令用 MCP 工具优先；需要脚本时用
`packages/akshare-mcp/.venv/Scripts/python.exe` 跑，**输出写临时文件再读**
（PowerShell scrollback 会被长输出污染）。

---

## 1. 数据资产地图（钉死表名与来源）

| 表 | 内容 | 写入方 | 过期影响 |
|---|---|---|---|
| `kline_1d` | 日 K（time/code/open/high/low/close/volume/amount/turnover/change_pct） | 数据同步层 | 前向收益、因子 IC、回测全部依赖 |
| `factor_ic_history` | 因子横截面 IC（factor_name/ic_value/ic_date...） | 因子验证管线 / FactorScheduler | `_from_factor_ic` 的 trend 判断、factor_ic lane |
| `factor_pool_active` | 活跃因子池（factor_id/name/family/expression_dsl/status/validation_summary...） | 因子挖掘工厂 | factor_pool lane 出货 |
| `signal_forward_returns` | 信号前向收益（signal_id/forward_days/actual_return） | SignalTracker | 命中率、ForwardVerifier |
| `strategy_signal_evidence` | 信号语义 evidence | 孵化工厂 | 认知积累（知识产出） |
| `strategy_signals` / `strategy_factory_runs` | 信号 / 工厂 run 摘要 | 策略工厂 | 审计、诊断 |

DB 绝对路径：`c:\Users\walking\Desktop\aiask\data\db\akshare_mcp.sqlite3`

---

## 2. K线新鲜度维护

### 2.1 检查新鲜度

MCP 工具：`check_db_freshness`
- 参数：`codes`（可选，留空扫全库）、`max_stale_days`（默认 5）。
- 输出：每只股票最新 K 线日期、过期天数、缺失清单。

```
check_db_freshness(codes=["688411","601601"], max_stale_days=5)
check_db_freshness(max_stale_days=5)   # 全市场扫描（返回很大，慎用）
```

### 2.2 同步过期 K线

MCP 工具：`sync_stale_klines`
- 参数：`codes`（留空自动扫全库过期项）、`max_stale_days`（默认 5）。
- 行为：对过期股票从 API 拉最新并写库。

```
sync_stale_klines(codes=["688411","688472","688778","601601","601628","688981","601319"])
```

其它入口：
- `sync_kline_data(stock_code, period, start_date, end_date, limit, use_cache)` — 单只，带多层缓存。
- `batch_sync_klines(codes, start_date, end_date, period)` — 批量。

### 2.3 已知约束（T+N 固有延迟）

- 当天生成的信号，5D/10D/20D 前向收益必须等 N 个交易日后才有值，**当天为 0 是正确的，不是 bug**。
- 实测过：信号日 2026-05-28 的 3D 前向收益在数据补齐后可算（113 行非 0），5D 需等到约 06-04。

---

## 3. 因子 IC 新鲜度维护（P-C）

### 3.1 检查

直接查库（脚本写临时文件再读）：

```sql
SELECT MAX(ic_date), COUNT(*), COUNT(DISTINCT factor_name) FROM factor_ic_history;
```

- 健康判据：`MAX(ic_date)` 应接近最近交易日。
- 已知历史故障：2026-06-03 实测 `maxdate=2026-05-21`（过期 13 天），导致 `_from_factor_ic`
  的 trend 判断基于陈旧数据。

### 3.2 重算 / 回填

机制：`FactorScheduler.run_once`（`akshare_mcp/services/factor_scheduler_parts/specs.py`）
- 加载动态全 A 股宇宙，分批调 `quant_manager(action="batch_compute_factors", compute_ic=True, persist=True)`。
- **依赖**：需要对应股票的 K 线已新鲜（先做 §2）。
- **成本**：全市场（5000+ 股）网络密集，远超单轮策略工厂 cycle，属重运维任务，建议非交易时段分批跑。

### 3.3 性质判定（重要）

- 因子 IC 全量新鲜度同步**不是策略工厂接线/代码的阻断项**：90 天 IC 窗口内仍含强因子时，
  factor_ic / factor_pool lane 即可出货（已于 2026-06-03 端到端验证）。
- 它只影响 trend 判断精度，独立排期即可。

---

## 4. 同步队列与失败落盘（dead-letter）

### 4.1 队列状态

MCP 工具：`get_sync_status` → 返回队列指标 + dead-letter 概览。

### 4.2 失败落盘

- 落盘文件：`.mcp_cache/dead_letters/kline_save_failures.jsonl`
- MCP 工具：
  - `get_dead_letters(limit=20)` — 查看最近失败记录。
  - `clear_dead_letters()` — 清空（确认无需复盘后再清）。

### 4.3 交易日历

MCP 工具：`sync_trading_calendar(year)` — 同步指定年份交易日历；
`get_trading_dates(start_date, end_date, count)` — 查询交易日，用于校验 T+N。

---

## 5. 缓存维护

### 5.1 缓存统计

MCP 工具：`get_cache_stats` → 文件数、总大小(MB)、命中率、缓存目录、TTL 配置。
- 缓存目录：`.mcp_cache/`（含 `kline_v2_*`、`financials_*`、`fund_flow_*` 等）。

### 5.2 清缓存

MCP 工具：`clear_cache()` → 清空所有缓存（下次访问回源重建，短期命中率下降）。
- 触发时机：磁盘异常增长、缓存疑似脏数据、或验证回源逻辑时。

---

## 6. 标准维护流程（SOP）

### 6.1 日常巡检（建议每交易日盘后）

1. `check_db_freshness(max_stale_days=5)`（或只查核心宇宙）。
2. 查 `factor_ic_history.maxdate` 是否接近最近交易日。
3. `get_sync_status` 看队列与 dead-letter 是否堆积。

### 6.2 发现过期 → 修复

1. K线过期：`sync_stale_klines(...)` → 复查 staleness 归零。
2. 因子 IC 过期：先确保 K线新鲜 → 跑 FactorScheduler 重算 → 复查 maxdate。
3. dead-letter 有记录：`get_dead_letters` 复盘根因 → 修复后 `clear_dead_letters`。

### 6.3 策略工厂跑前预热（可选）

- 若要开 alpha 接线 toggle 跑真实 cycle（见 `策略工厂Alpha源接线修复方案-2026-06-03.md` ③），
  建议先做 6.2 第 1、2 步，让 factor_ic trend 基于新鲜数据。

---

## 7. 安全与纪律（务必遵守）

- **DB 备份**：高风险操作前先备份（仓库已有 `*.backup-*` 命名先例）。破坏性操作先确认。
- **临时脚本即用即删**：`_*.py`/`_*.txt`/`_*.json` 等，**绝不 commit 进 git**。
- **命令输出写临时文件再用 read 工具读**：避免 PowerShell scrollback 污染 / PSReadLine 崩溃。
- **全市场扫描/同步谨慎**：`check_db_freshness()` 无参会返回 5000+ 行；全量 IC 重算网络密集，
  非交易时段分批执行。
- **不裸 force、不动 git config**；只 stage 指定文件。

---

## 8. 附：本手册依据（可复核）

- 实测表与数据源：`kline_1d` / `factor_ic_history`(maxdate 2026-05-21) /
  `factor_pool_active`(33 行,active 5) / `signal_forward_returns`(3D 113 行非 0)。
- MCP 工具：`check_db_freshness` / `sync_stale_klines` / `sync_kline_data` /
  `batch_sync_klines` / `get_sync_status` / `get_dead_letters` / `clear_dead_letters` /
  `get_cache_stats` / `clear_cache` / `sync_trading_calendar` / `get_trading_dates`。
- 关联文档：`策略工厂Alpha源接线修复方案-2026-06-03.md`（§P-C 性质判定、③ 真实验证）。
