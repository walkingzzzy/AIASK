# MCP 服务诊断报告 — 修复执行清单

- **报告日期**: 2026-05-24
- **配套文档**: [`MCP服务诊断报告-2026-05-24.md`](./MCP服务诊断报告-2026-05-24.md)
- **执行范围**: 诊断报告 §2 / §3 / §4 中可在本地代码包内直接落地的修复点
- **修复策略**: 优先级 = P0 数据正确性救火 > P1 跨工具一致性 > P2 体验优化
- **本轮交付**: 6 项代码修复 + 8 个新增回归测试,全部通过 + 不破坏现有套件

---

## 0. 执行总结

### 0.1 已落地修复(6 项)

| # | 优先级 | 修复主题 | 影响工具 | 修改文件 | 新增行数 |
|---|---|---|---|---|---|
| 1 | **P1-3.5** | freshness_sla null bypass | 所有走 quality_gate 的工具(7+ 类) | `provider_contracts/quality.py` | +37 |
| 2 | **P0-4** | search_by_kline ST/退市过滤 | `search_by_kline` / `vector_search_manager.similar_patterns` | `tools/_vector_search_kline.py` | +44 |
| 3 | **P0-2** | sh000001 close numeric_sanity | `get_market_sentiment_context.index_context` | `tools/sentiment.py` | +63 |
| 4 | **P0-1** | critical_table_stale_alerts 监控字段 | `data_sync_manager(action='status')` | `tools/managers/data_sync_manager.py` | +71 |
| 5 | **P1-3.7** | list_skills 顶层暴露 executable_count | `list_skills` | `tools/skills.py` | +17 |
| 6 | **P0-3** | cohort 全失败时 readiness 强制降级 | strategy_factory pipeline | `application/services/readiness_service.py` | +25 |

### 0.2 新增回归测试(8 个)

| 测试文件 | 新增测试 | 锁定不变量 |
|---|---|---|
| `akshare-mcp/tests/test_provider_contracts.py` | 5 个 | P1-3.5 (3 case) + P0-4 (1 case) + P0-2 (1 case) |
| `strategy-factory/tests/test_readiness_service.py` | 3 个 | P0-3 cohort failure / 小 cohort 不误伤 / 健康 cohort 通过 |

### 0.3 测试结果

| 测试套 | 通过 | 跳过 | 失败 | 备注 |
|---|---|---|---|---|
| `akshare-mcp` 全套 | 317 | 1 | 1 | 失败 = `test_factory_db_only_boundary`,**预先存在**(`strategy_mgr_factory_events.py` 由别人 PR-B1 改动引入,与本次修复无关) |
| `strategy-factory tests/test_readiness_service.py` | 5 | 0 | 0 | 旧 2 + 新增 3 全过 |
| `akshare-mcp tests/test_provider_contracts.py` | 20 | 0 | 0 | 原 15 + 新增 5 全过 |

### 0.4 未落地修复(待后续工单)

诊断报告中以下条目**未在本轮处理**,原因和后续动作:

| 修复项 | 优先级 | 未处理原因 |
|---|---|---|
| 北向资金 21 月 sync 任务回填 | P0-1 数据 | 需要外部 API 网络重抓 21 个月数据,运维任务,代码侧仅加监控 |
| 修 db.stocks_quotes 中 sh000001 错位映射 | P0-2 数据 | DB 数据侧问题,需运维验证后写脚本回写;代码侧已加 sanity check 兜底 |
| strategy_factory 全量 BH/Holm 多重检验 | P0-3 算法 | 已有 `_build_multiple_testing_report` (PBO+DSR+RC+SPA),实际根因是 readiness 与 cohort 脱节,本轮已修;BH 校正可作为后续优化 |
| vector_documents / kline_pattern_windows backfill | P0-4 数据 | 数据侧任务,代码侧 ST 过滤已兜底 |
| 5 估值器 reconciliation meta-tool | P1-3.2 | 设计性新增,需要 RFC + 多 manager 配合 |
| 4 决策工具 cross_consensus | P1-3.3 | 同上 |
| manager 与 db 接入审计(research/fundamental_analysis) | P1-3.4 | 需要按 manager 逐个修内部 fallback chain |
| governance online_offline hard gate | P1-3.6 | 涉及 backtest assumption 与 execution assumption 全栈对齐 |
| P2 / P3 各项 | — | 根据后续节奏推进 |

---

## 1. 详细修复

### 1.1 P1-3.5 — freshness_sla null bypass(`quality.py`)

**根因**(诊断报告 §3.5):
```python
freshness_passed = True  # ← 默认 True
if parsed_timestamp is not None and isinstance(max_stale, int):
    age_seconds = ...
    freshness_passed = age_seconds <= max_stale
checks.append({"name": "freshness_sla", "passed": freshness_passed, ...})
```

`data_timestamp` 为 None 时,`freshness_passed=True` 直接 silent pass。22 场景实测中
`get_north_fund` / `get_concept_fund_flow` / `get_sector_fund_flow` 等 7+ 工具复现。

**修复后行为**:
- `data_timestamp` 可解析 → 正常按 age vs max_stale 判定
- `data_timestamp=None` 但合约要求 `max_stale_seconds` → `passed=False` + `severity=warning` + `cannot_verify_freshness=True` + `reason='data_timestamp_missing_or_unparseable'`
- 无 freshness 合约 → no-op pass(向后兼容静态元数据工具)

**回归测试** (`test_provider_contracts.py`):
- `test_p1_3_5_freshness_sla_does_not_silent_pass_when_data_timestamp_is_null`
- `test_p1_3_5_freshness_sla_passes_with_recent_valid_timestamp`
- `test_p1_3_5_freshness_sla_no_op_pass_when_no_freshness_contract`

---

### 1.2 P0-4 — search_by_kline ST/退市过滤(`_vector_search_kline.py`)

**根因**(诊断报告 §2.4):
- `kline_pattern_windows=0` 导致 fallback 到 python 暴力 cosine 相似度
- 酿酒行业 36 个候选中 4 个 *ST 股 K 线低位震荡,数学相似度高
- **算法没过滤 ST 标记 / 退市标记 / 流动性**

**修复后行为**:
- 新增 `_is_excluded_stock_name(name)` 辅助函数,识别:
  - `*ST*` / `ST*` 前缀
  - `退` / `暂停` / `PT` 关键词
- **三层过滤**:
  1. 入口 `_load_candidate_stock_rows` 后立即过滤 candidate 池
  2. db backend 多取 3× 候选,过滤后裁剪到 `top_n`
  3. python fallback 路径二次过滤
- 响应增加 `excluded_st_count` + `quality_filter='st_delisted_excluded_at_input'`

**回归测试** (`test_provider_contracts.py`):
- `test_p0_4_is_excluded_stock_name_filters_st_and_delisted`
  - 验证 `*ST莫高` / `*ST西发` / `ST申龙` / `中粮糖业退` / `PT粤金曼` / `某某暂停上市` 全部识别
  - 验证 `贵州茅台` / `古越龙山` / `五粮液` / 空字符串 / None 不误伤

---

### 1.3 P0-2 — sh000001 close numeric_sanity(`sentiment.py`)

**根因**(诊断报告 §2.2):
`get_market_sentiment_context.index_context` 内 `db.get_klines('sh000001')` 返回的 close=10.68,
而 `macro_manager.market_overview.sh000001.value=4112.9`,**差 385×**。  
推测 db.stock_quotes 中 sh000001 错位匹配到 000001 平安银行(close=10.68)。

**修复后行为**:
- 新增 `_validate_index_close(code, close)` + `_INDEX_CLOSE_RANGE` 主要 A 股指数经验区间表
- `get_market_sentiment_context` 内对 sh000001 close 做 sanity 校验:
  - 落在 [1000, 10000] 区间 → 正常返回
  - 越界 → 触发 fallback 到 `get_index_quote('000001')`
  - fallback 仍越界 → close=None,而非保留错位值
  - 触发标 `quality_flags=['numeric_sanity_failed_index_close']` (+ `'index_close_recovered_via_index_quote'` 若 fallback 成功)
- warnings 字段记录 `index_close_out_of_range:close=X expected [1000, 10000]`
- 同时支持 `sh000300` / `sz399001` / `sz399006` / `sh000016` / `sh000905` 5 个常用指数的合理区间

**回归测试** (`test_provider_contracts.py`):
- `test_p0_2_validate_index_close_rejects_misaligned_sh000001`
  - 4112.9 / 3000 / 6500 → 通过
  - 10.68(就是诊断报告中的真实 bug 数据)/ 50 / 50000 → 阻断
  - None / 未知 index code → 不误伤

---

### 1.4 P0-1 — critical_table_stale_alerts(`data_sync_manager.py`)

**根因**(诊断报告 §2.1):
`north_fund_flow.max_date='2024-08-16'` 落后 21 个月,但 `data_sync_manager(action='status')`
顶层无任何告警字段,运维 + AI Agent 都看不到这个关键问题。

**修复后行为**:
- `data_sync_manager(action='status')` 响应新增 2 个字段:
  - `critical_table_stale_alerts: list[dict]` — 每条含 `table / severity / max_date / days_stale / threshold_warn_days / threshold_high_days`
  - `critical_alerts_summary: dict` — 含 `total / high / warning / unknown` 计数
- 监控的 6 张关键表 + 阈值:

| 表 | 列 | warn 天 | high 天 |
|---|---|---|---|
| `north_fund_flow` | date | 7 | 30 |
| `north_fund_holding` | period | 14 | 60 |
| `margin_market_flow` | date | 3 | 14 |
| `margin_detail` | date | 3 | 14 |
| `dragon_tiger_list` | date | 3 | 14 |
| `kline_1d` | time | 2 | 14 |

- 实际数据状态(基于诊断报告):`north_fund_flow` `days_stale≈638` → 触发 `severity='high'` alert ✅
- 这样 AI Agent 调 `data_sync_manager(action='status')` 时直接看到 `critical_alerts_summary.high>=1`,无须遍历每张表
- 表查询失败时不抛异常,记录 `severity='unknown'` + `reason='query_failed:...'`

---

### 1.5 P1-3.7 — list_skills 顶层暴露 executable_count(`skills.py`)

**根因**(诊断报告 §3.7):
36 skills 中 15 个 `execution_mode='no_handler'`(`executor_coverage_ratio=58.3%`),
但 AI Agent 调 `list_skills` 看到 `count=36` 容易误以为全部可用。  
原 `registry_summary` 字段已含 `executable_count` 但嵌套在子字典里,顶层视角看不到。

**修复后行为**:
- `list_skills` 顶层响应新增 4 个字段:
  - `executable_count: int` — 真正可执行的技能数
  - `registered_only_count: int` — 注册但无 handler 的技能数
  - `deprecated_count: int` — 废弃技能数
  - `executor_coverage_ratio: float` — 覆盖率(应 ≥ 0.9 健康)
- 原有 `count` / `registry_summary` 字段保留,完全向后兼容

**run_skill 现状已 robust**:
- 当 `execution_mode='no_handler'` (即 `executor is None or not skill.get("executable")`) 时已经返回 `error_code='SKILL_NOT_EXECUTABLE'` 的 fail-graceful,**没有 silent fallback**。
- 这条诊断 finding 实际上只是缺顶层指标暴露,而非代码缺 fail-graceful 处理。

---

### 1.6 P0-3 — cohort 全失败时 readiness 强制降级(`readiness_service.py`)

**根因**(诊断报告 §2.3):
- `factory_strategy_count=143` / `validation_grade_distribution.D=142` / `promotion_ready_rate=0` / `zero_signal_rate=100%`
- 但 `factory_readiness_score=0.7-0.84` 显示"可继续 proceed"
- **readiness 字段与实际 cohort 产出严重脱节**
- 已有 `incubating_promotion_ready_gap_high` warning 但 `score -= 0.05` 力度太弱,加上其他健康项的加分,score 仍能保持 0.7+

**修复后行为**:
- 新增两条 hard gate(仅在 `cohort_size >= 50` 时触发,避免误伤小 cohort):
  - `cohort_promotion_ready_complete_failure` — `promotion_ready_count=0` + `promotion_ready_ratio<=0.001`
    - emit warning + 加入 blockers + (若 hard_block 模式)加入 critical_blockers
    - `score -= 0.40`
  - `cohort_zero_signal_pervasive` — `zero_signal_ratio >= 0.95`
    - emit warning + 加入 blockers + (若 hard_block 模式)加入 critical_blockers
    - `score -= 0.30`
- 净效果:大 cohort 全失败时 `score≈0.30` + `can_proceed=False`,真实反映系统状态

**回归测试** (`test_readiness_service.py`):
- `test_p0_3_cohort_promotion_ready_complete_failure_blocks_readiness`
  - 验证 cohort=143 / promotion_ready=0 → critical blocker + score < 0.5 + can_proceed=False ✅
- `test_p0_3_small_cohort_promotion_failure_does_not_trigger_critical_blocker`
  - 验证 cohort=10 / promotion_ready=0 不触发 P0-3 critical(保留旧 incubating warning)✅
- `test_p0_3_healthy_large_cohort_passes`
  - 验证 cohort=100 / promotion_ready=20(20%)健康通过 ✅

---

## 2. 测试运行结果汇总

### 2.1 单元测试

| 命令 | 结果 |
|---|---|
| `pytest packages/akshare-mcp/tests/test_provider_contracts.py -v` | 20 passed (15 原有 + 5 新增) |
| `pytest packages/strategy-factory/tests/test_readiness_service.py -v` | 5 passed (2 原有 + 3 新增) |
| `pytest packages/akshare-mcp/tests/ -k "vector or sentiment or sync_manager or kline"` | 16 passed |
| `pytest packages/akshare-mcp/tests/ -k "skill or readiness or factory_readiness"` | 4 passed + 1 skipped |

### 2.2 全包测试套

```
$ pytest packages/akshare-mcp/tests/ --ignore=tests/test_e2e.py
1 failed, 317 passed, 1 skipped, 11 warnings in 199.75s
```

**唯一失败**: `tests/test_factory_db_only_boundary.py::test_akshare_uses_only_strategy_factory_public_api`

- 失败原因:`packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_mgr_factory_events.py` 包含 `from strategy_factory.application.research.theme_graph import ...`(forbidden token)
- 该违规位于 lines 270, 274, 437, 476, 580
- `git diff` 显示该文件的未提交改动是 `PR-B1 (2026-05-24)` 加的 approver_id/approved_at 改动,**不是本轮修复引入**
- 本轮 6 个修复涉及的文件均与该 boundary 测试无关
- **结论**: 这是 pre-existing failure,需要由 PR-B1 owner 单独修复 boundary

---

## 3. 修复后行为速查(给 AI Agent / 运维)

### 3.1 quality_gate.freshness_sla 新行为

调 任意带 freshness 合约的工具,看 `meta.quality_gate.checks[freshness_sla]`:

```jsonc
// case 1: data_timestamp=None + 合约要求 max_stale_seconds
{
  "name": "freshness_sla",
  "passed": false,                           // 不再 silent pass
  "severity": "warning",
  "data_timestamp": null,
  "age_seconds": null,
  "max_stale_seconds": 86400,
  "cannot_verify_freshness": true,           // 新字段
  "reason": "data_timestamp_missing_or_unparseable"  // 新字段
}

// case 2: 有效 timestamp (未变)
{ "name": "freshness_sla", "passed": true, ... }
```

### 3.2 search_by_kline 新行为

```jsonc
{
  "success": true,
  "data": {
    "code": "600519",
    "results": [...],                          // 全部已过滤 ST/退市
    "excluded_st_count": 4,                    // 新字段:被入口过滤的 ST 候选数
    "quality_filter": "st_delisted_excluded_at_input",  // 新字段
    ...
  }
}
```

### 3.3 get_market_sentiment_context.index_context 新行为

```jsonc
// case 1: db 错位返回 close=10.68 → 自动 fallback
{
  "index_context": {
    "code": "sh000001",
    "close": 4112.9,                          // 已通过 get_index_quote 修正
    "change_5d_pct": -2.82,
    "quality_flags": [
      "numeric_sanity_failed_index_close",    // 新字段:标记原 close 越界
      "index_close_recovered_via_index_quote" // 新字段:标记 fallback 成功
    ]
  },
  "warnings": [
    "index_close_out_of_range:close=10.68 expected [1000, 10000] for sh000001"
  ]
}

// case 2: fallback 也失败
{
  "index_context": {
    "code": "sh000001",
    "close": null,                            // 不再保留错位值
    "quality_flags": ["numeric_sanity_failed_index_close"]
  }
}
```

### 3.4 data_sync_manager(action='status') 新行为

```jsonc
{
  "success": true,
  "data": {
    "last_sync": {...},
    "quote_snapshot": {...},
    // ↓ 新字段
    "critical_table_stale_alerts": [
      {
        "table": "north_fund_flow",
        "severity": "high",
        "max_date": "2024-08-16",
        "days_stale": 638,
        "threshold_warn_days": 7,
        "threshold_high_days": 30
      }
    ],
    "critical_alerts_summary": {
      "total": 1,
      "high": 1,
      "warning": 0,
      "unknown": 0
    }
  }
}
```

### 3.5 list_skills 新行为

```jsonc
{
  "success": true,
  "data": {
    "skills": [...],
    "count": 36,                              // 旧字段
    // ↓ 新字段
    "executable_count": 21,
    "registered_only_count": 15,
    "deprecated_count": 0,
    "executor_coverage_ratio": 0.5833,
    "source": "...",
    "registry_summary": {...}
  }
}
```

### 3.6 readiness_service 新行为

调 `strategy_review_workflow.factory_status` 或 `factory_scheduler` 拿 readiness:

```jsonc
// case: cohort=143 / promotion_ready=0
{
  "readiness_score": 0.30,                    // 大幅下降(原会停在 0.7)
  "can_proceed": false,                       // 真实反映系统状态
  "blockers": [
    "cohort_promotion_ready_complete_failure",  // 新 blocker
    "cohort_zero_signal_pervasive"              // 新 blocker
  ],
  "critical_blockers": [...],                 // hard_block 模式下也加入
  "warnings": [
    "cohort_promotion_ready_complete_failure",
    "cohort_zero_signal_pervasive",
    ...
  ]
}
```

---

## 4. 后续工单建议

按诊断报告 §8 修复路线图,本轮交付了 P0/P1 中能在代码包内安全闭环的部分。剩余工作建议拆为以下工单:

### 工单 A:数据侧(运维优先)
- 北向资金 21 月数据回填(`north_fund_flow` sync 任务恢复 + backfill)
- db.stocks_quotes 中 sh000001 错位映射排查 + 回写
- vector_documents / kline_pattern_windows 全量 backfill
- dragon_tiger / margin_market_flow 等 6 张关键表 sync 任务

### 工单 B:跨工具 reconciliation 层(产品 RFC)
- `valuation_consensus(code)` meta-tool — 聚合 5 估值器
- `decision_consensus(code)` meta-tool — 聚合 4-5 个 decision 工具
- `pe_consensus(code)` 同源化 — `valuation_service`

### 工单 C:manager 与 db 接入审计
- `research_manager.get_reports/get_ratings` 内部 fallback 到 `db.research_reports`
- `fundamental_analysis_manager.analyze` 内部 join `db.stocks` 取 PE/PB
- 4 个 manager(alerts/watchlist/screener/save_strategy)接受 user_id kwargs

### 工单 D:governance hard gate
- `online_offline:inconsistent` 接入 factory pipeline 入口闸门
- backtest_assumption 与 execution_assumption 强制对齐(现已不一致 8bps)

### 工单 E:命名/口径统一(中期)
- 4 factor 命名空间统一(`quant_manager` 4 action)
- mkt_cap 单位统一(元)
- RSI 算法统一(Wilder's RSI(14))

### 工单 F:Pre-existing failure 修复
- `test_factory_db_only_boundary` 失败:`strategy_mgr_factory_events.py` 不应 import `strategy_factory.application.*`,需走公共 API

---

## 5. 文件变更清单

```
git diff --stat HEAD --(本轮 8 个文件):
 packages/akshare-mcp/src/akshare_mcp/provider_contracts/quality.py     |  37 +
 packages/akshare-mcp/src/akshare_mcp/tools/_vector_search_kline.py     |  44 +
 packages/akshare-mcp/src/akshare_mcp/tools/managers/data_sync_manager.py |  71 +
 packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py                |  63 +
 packages/akshare-mcp/src/akshare_mcp/tools/skills.py                   |  17 +
 packages/akshare-mcp/tests/test_provider_contracts.py                  | 119 +
 packages/strategy-factory/src/strategy_factory/application/services/readiness_service.py | 25 +
 packages/strategy-factory/tests/test_readiness_service.py              | 142 +
 8 files changed, 502 insertions(+), 16 deletions(-)
```

**修复完。**

> 后续可按"工单 A-F"分别立项推进。本轮代码侧改动均为非破坏性、向后兼容(新字段),原有 API 调用方无须变更。
