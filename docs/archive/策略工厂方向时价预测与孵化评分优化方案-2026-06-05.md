# 策略工厂方向时价预测与孵化评分优化方案

日期：2026-06-05

## 1. 背景与问题

当前孵化工厂已经能够计算前向方向命中率，例如 T+5、T+10、T+20 的 `primary_hit_rate`、`skill_lcb`、`forward_sharpe` 等指标。

但这个口径仍然偏技术层面，不能完整回答真实交易中的核心问题：

- 明天或近期这只股票是涨还是跌？
- 大概在哪个时间段上涨或下跌？
- 是否能触达目标价区间？
- 应该在哪个时间段买入？
- 应该在哪个时间段卖出？
- 事件、新闻、资金流、因子证据是否领先市场，而不是事后解释？

因此需要把策略工厂和孵化工厂从“策略生成 + 前向窗口命中率”升级为：

> 方向优先的交易预测合约 + 时间段/价格路径/买卖点验证 + 事件与因子实时证据驱动。

## 2. 核心目标

### 2.1 策略工厂目标

策略工厂生成策略时，不再只输出策略类型、参数和回测结果，而是必须输出结构化交易预测：

- 股票代码
- 预测日期或预测窗口
- 方向概率
- 时间段预测
- 目标价区间
- 买入窗口
- 卖出窗口
- 止损和止盈计划
- 事件证据
- 因子证据
- 置信度和失效条件

### 2.2 孵化工厂目标

孵化工厂不再只验证 T+5/T+10/T+20 是否方向正确，而是逐项验证：

- 方向是否命中
- 时间段是否命中
- 目标价是否触达
- 买入窗口是否有效
- 卖出窗口是否有效
- 风险是否受控
- 事件和因子是否真的有预测贡献

### 2.3 因子挖掘目标

因子挖掘工厂需要支持短周期预测因子，而不是只关注中长期 IC：

- 次日方向 IC
- 开盘后 30 分钟方向 IC
- 上午/下午方向 IC
- 量价结构因子
- 资金流突变因子
- 板块联动因子
- 事件后漂移因子

### 2.4 事件驱动目标

事件驱动系统必须提供实时证据：

- 新闻
- 公告
- 研报
- 板块异动
- 资金流
- 政策事件
- 市场情绪突变

关键要求：事件必须带时间戳，并且验证事件是否领先行情反应。

## 3. 新准确率定义

AI 生成策略的准确率不应只看 `hit_rate`，而应定义为综合交易预测准确率。

建议评分权重：

| 维度 | 权重 | 说明 |
|---|---:|---|
| 方向预测准确率 | 40% | 明天/近期涨跌方向是否正确，最高权重 |
| 时间段预测准确率 | 20% | 是否在预测时间段内发生上涨或下跌 |
| 目标价/幅度准确率 | 15% | 是否触达目标价区间，涨跌幅是否接近 |
| 买卖计划收益质量 | 15% | 买入和卖出窗口是否产生真实收益 |
| 风险控制准确率 | 5% | 止损、最大不利波动、回撤是否可控 |
| 事件/因子证据质量 | 5% | 新闻、事件、因子是否提前解释并预测行情 |

综合评分：

```text
trade_prediction_score =
  0.40 * direction_score
+ 0.20 * time_bucket_score
+ 0.15 * target_price_score
+ 0.15 * trade_plan_score
+ 0.05 * risk_control_score
+ 0.05 * evidence_score
```

## 4. 策略工厂输出合约

新增 `TradePredictionContract`。

示例：

```json
{
  "strategy_id": "factory_xxx",
  "stock_code": "000001",
  "prediction_as_of": "2026-06-05T15:30:00+08:00",
  "target_trading_date": "2026-06-08",
  "prediction_horizon": "next_trading_day",
  "direction": "up",
  "direction_probability": 0.68,
  "confidence": 0.62,
  "time_buckets": [
    {
      "window": "09:45-10:30",
      "direction": "up",
      "target_price_range": [118.0, 122.0],
      "probability": 0.62
    },
    {
      "window": "14:00-14:30",
      "direction": "down",
      "target_price_range": [95.0, 100.0],
      "probability": 0.55
    }
  ],
  "entry_plan": {
    "window": "09:35-09:50",
    "price_range": [100.0, 103.0],
    "entry_condition": "volume_breakout_and_positive_fund_flow"
  },
  "exit_plan": {
    "take_profit_range": [118.0, 122.0],
    "stop_loss": 96.0,
    "latest_exit_window": "14:30-14:50"
  },
  "risk_plan": {
    "max_adverse_excursion_pct": 3.5,
    "max_position_pct": 10.0,
    "invalidation": "price_breaks_96_or_event_sentiment_turns_negative"
  },
  "evidence": {
    "factor_signals": [
      "intraday_momentum",
      "north_fund_flow",
      "volume_price_breakout"
    ],
    "event_signals": [
      {
        "event_type": "news",
        "event_time": "2026-06-05T14:10:00+08:00",
        "sentiment": "positive",
        "relevance": 0.81
      }
    ],
    "regime": {
      "trend_regime": "uptrend",
      "vol_regime": "normal",
      "sentiment_regime": "risk_on"
    }
  }
}
```

## 5. 孵化工厂验证指标

孵化工厂需要新增以下指标。

### 5.1 方向命中

```text
direction_hit = sign(predicted_direction) == sign(actual_forward_return)
```

输出：

- `next_day_direction_hit`
- `next_day_direction_hit_rate`
- `direction_skill_lcb`
- `direction_effective_n`

### 5.2 时间段命中

按 5 分钟、15 分钟、30 分钟或小时 bucket 验证。

```text
time_bucket_hit =
  predicted bucket direction == actual bucket direction
```

输出：

- `time_bucket_hit_rate`
- `time_bucket_skill_lcb`
- `best_bucket_hit`
- `missed_bucket_count`

### 5.3 目标价触达

```text
target_touch_hit =
  actual_high_or_low touches predicted target range
```

输出：

- `target_touch_rate`
- `target_price_error_pct`
- `target_reach_time`
- `target_not_reached_reason`

### 5.4 买入窗口质量

验证预测买入窗口是否合理。

输出：

- `entry_window_hit`
- `entry_price_slippage_pct`
- `max_favorable_excursion_after_entry`
- `max_adverse_excursion_after_entry`

### 5.5 卖出窗口质量

验证预测卖出窗口是否合理。

输出：

- `exit_window_hit`
- `exit_quality_score`
- `profit_capture_ratio`
- `drawdown_avoided_after_exit`

### 5.6 交易计划收益

模拟按照预测计划执行。

输出：

- `planned_trade_return`
- `planned_trade_r_multiple`
- `planned_trade_win`
- `planned_trade_max_drawdown`

### 5.7 事件和因子贡献

验证事件和因子是否真的领先。

输出：

- `event_lead_time_minutes`
- `event_direction_hit`
- `event_relevance_score`
- `factor_short_horizon_ic`
- `factor_direction_hit`

## 6. 数据层改造

### 6.1 新增交易预测表

建议新增：

```text
strategy_trade_predictions
```

核心字段：

- `prediction_id`
- `strategy_id`
- `stock_code`
- `prediction_as_of`
- `target_trading_date`
- `prediction_horizon`
- `direction`
- `direction_probability`
- `confidence`
- `entry_plan_json`
- `exit_plan_json`
- `risk_plan_json`
- `time_buckets_json`
- `evidence_json`
- `created_at`

### 6.2 新增预测结果表

建议新增：

```text
strategy_trade_prediction_outcomes
```

核心字段：

- `prediction_id`
- `strategy_id`
- `stock_code`
- `actual_trading_date`
- `direction_hit`
- `time_bucket_score`
- `target_touch_hit`
- `entry_score`
- `exit_score`
- `risk_score`
- `evidence_score`
- `planned_trade_return`
- `trade_prediction_score`
- `outcome_json`
- `evaluated_at`

### 6.3 分钟级行情要求

要验证“十点半涨到多少、下午两点半跌到多少”，必须有分钟级数据。

优先级：

1. 1 分钟或 5 分钟 K 线
2. 15 分钟 K 线
3. 30 分钟 K 线
4. 日线只能做兜底，不能验证盘中时间点

## 7. 策略工厂改造点

### 7.1 Candidate 生成

策略生成器必须输出 `TradePredictionContract`，不是自由文本。

### 7.2 LLM Prompt 改造

Prompt 必须要求：

- 输出方向概率
- 输出时间段预测
- 输出目标价区间
- 输出买卖窗口
- 输出止损止盈
- 输出事件证据
- 输出因子证据
- 输出失效条件

禁止只输出“看涨、建议买入”这类不可验证结论。

### 7.3 Router 权重改造

Stock-First Router 的评分从“策略可生成性”调整为“交易预测可验证性”。

新增权重：

```text
direction_predictability_weight = 0.40
intraday_timing_weight = 0.20
target_price_weight = 0.15
trade_plan_weight = 0.15
risk_control_weight = 0.05
event_factor_evidence_weight = 0.05
```

### 7.4 策略提交门

进入孵化前必须满足：

- 有结构化交易预测
- 有明确 target trading date
- 有方向概率
- 有买卖窗口
- 有风险计划
- 有因子或事件证据
- 能被孵化工厂回放验证

## 8. 孵化工厂改造点

### 8.1 SignalGenerator

当前负责生成/同步信号，需要扩展为写入结构化交易预测。

### 8.2 ForwardVerifier

从窗口级 `T+5/T+10/T+20` 扩展为：

- 次日方向验证
- 分钟级时间段验证
- 目标价触达验证
- 买入/卖出窗口验证
- 计划收益验证

### 8.3 MetricsRecorder

新增每日指标字段：

- `direction_hit_rate`
- `direction_skill_lcb`
- `time_bucket_hit_rate`
- `target_touch_rate`
- `entry_quality_score`
- `exit_quality_score`
- `trade_prediction_score`
- `planned_trade_return`
- `event_lead_score`
- `factor_short_horizon_ic`

### 8.4 HitRateReporter

命中率报告需要从“整体命中率”扩展为：

- 按股票
- 按策略类型
- 按市场状态
- 按时间段
- 按持仓周期
- 按事件类型
- 按因子类型

### 8.5 Promotion Gate

晋升不再只看 `skill_lcb` 和 `forward_sharpe`。

新增晋升条件：

```text
direction_skill_lcb > 0
time_bucket_skill_lcb >= 0
trade_prediction_score >= 0.60
planned_trade_return > 0
max_drawdown within risk_plan
effective_n >= minimum threshold
```

## 9. 因子挖掘工厂改造点

### 9.1 短周期因子目标

因子挖掘的目标函数新增：

- next-day direction IC
- intraday bucket direction IC
- target touch contribution
- event-conditioned IC
- regime-conditioned IC

### 9.2 因子入池标准

因子进入活跃池需要提供：

- 短周期 IC
- 分 regime 稳定性
- 样本数
- 最近衰减情况
- 对交易预测分数的贡献

### 9.3 因子反馈

孵化工厂应把以下结果反馈给因子挖掘：

- 哪些因子提高方向命中
- 哪些因子提高时间段命中
- 哪些因子只在特定 regime 有效
- 哪些因子在实盘前向验证中失效

## 10. 事件驱动改造点

### 10.1 事件结构

事件必须统一为：

```json
{
  "event_id": "event_xxx",
  "stock_code": "000001",
  "event_type": "news",
  "event_time": "2026-06-05T10:18:00+08:00",
  "source": "news_provider",
  "headline": "...",
  "summary": "...",
  "sentiment": "positive",
  "relevance": 0.82,
  "expected_direction": "up",
  "expected_horizon": "intraday"
}
```

### 10.2 事件验证

验证事件是否有效：

- 事件发生后 5/15/30/60 分钟收益
- 次日收益
- 事件方向命中
- 事件到行情反应的 lead time
- 事件是否已被市场提前消化

### 10.3 事件反馈

有效事件类型提高权重，无效事件类型降权。

## 11. 实施阶段

### P0：定义合约与评分

目标：

- 新增 `TradePredictionContract`
- 新增 `TradePredictionOutcome`
- 新增综合评分函数
- 策略工厂输出结构化预测
- 孵化工厂能够读取预测并生成空结果

验收：

- 策略工厂每个候选至少有一条结构化交易预测
- 没有预测合约的策略不能进入孵化

### P1：次日方向与目标区间验证

目标：

- 验证次日方向
- 验证目标价区间触达
- 验证止损止盈

验收：

- 生成 `direction_hit_rate`
- 生成 `target_touch_rate`
- 生成 `trade_prediction_score`

### P2：盘中时间段验证

目标：

- 引入分钟级或 5 分钟 K 线
- 验证上午/下午/固定 bucket 的方向
- 验证买入和卖出窗口

验收：

- 生成 `time_bucket_hit_rate`
- 生成 `entry_quality_score`
- 生成 `exit_quality_score`

### P3：事件与因子实时证据

目标：

- 接入事件流
- 事件带时间戳
- 事件与因子共同进入策略工厂 evidence

验收：

- 生成 `event_lead_score`
- 生成 `factor_short_horizon_ic`
- 生成按 event type/factor type 的命中率报告

### P4：晋升门与预算反馈

目标：

- 孵化晋升使用新综合评分
- 策略工厂预算按真实交易预测结果调整
- 因子挖掘根据孵化结果更新搜索方向

验收：

- `trade_prediction_score` 进入晋升门
- 低分 family 降权
- 高分 family 加预算
- 失效因子进入衰减或隔离

## 12. 风险与原则

### 12.1 不追求单点神谕

不要求 AI 预测“准确到 10:30 价格必到 120”。

应要求：

- 时间区间
- 价格区间
- 概率
- 置信度
- 失效条件

### 12.2 不允许事后解释

事件和因子必须在预测生成前或生成时存在。

事件时间必须早于行情验证窗口。

### 12.3 不允许样本不足时宣称准确

样本不足时必须标记：

```text
insufficient_samples
```

不能强行计算高命中率。

### 12.4 不允许只看方向忽略交易结果

方向对但买卖点差、回撤大、收益差，不能算优质策略。

### 12.5 不允许只看总体平均

必须按：

- 股票
- strategy_type
- regime
- holding_bucket
- event_type
- factor_type

分组观察，避免平均数掩盖失效场景。

## 13. 最终目标形态

策略工厂生成的是：

> 可验证的交易预测计划。

孵化工厂验证的是：

> 该交易预测计划在真实行情中的方向、时间、价格、买卖点、风险和证据质量。

因子挖掘提供的是：

> 能解释并预测短周期方向和价格路径的因子证据。

事件驱动提供的是：

> 带时间戳、可验证、领先市场反应的实时事件证据。

最终，AI 策略准确率不再是单一 `hit_rate`，而是：

```text
方向预测准确率
+ 时间段预测准确率
+ 目标价触达准确率
+ 买卖计划收益质量
+ 风险控制
+ 事件/因子证据质量
```

这才符合真实交易层面的策略质量评估。

---

## 14. 代码现状深度解析与修订结论

本节基于 2026-06-05 当前工作区代码进行对照。结论是：本方案方向正确，且与 AIASK 当前倒置架构、Stock-First 路由、孵化工厂前向验证、因子货架和反馈闭环基本兼容；但不能按“只改 Prompt”处理，必须作为跨包工程演进落地。

### 14.1 当前已经具备的工程基础

#### 14.1.1 策略工厂已有语义合同链，但不是交易预测合同

当前策略工厂和生成器已经围绕以下链路组织候选：

```text
evidence_chain -> prediction_contract -> trade_plan -> dsl
```

现有 LLM Prompt 已要求 candidate 必须包含 `evidence_chain`、`prediction_contract`、`trade_plan`、`risk_rules`、`validation_profile`、`target_symbols`、`stock_pool`、`dsl` 等字段，并要求 `prediction_contract.claims[]` 引用 `evidence_ids`，每条 claim 包含 `failure_condition`。

相关代码证据：

- `packages/akshare-mcp/src/akshare_mcp/services/_strategy_llm_provider_prompt.py`
- `packages/strategy-factory/src/strategy_factory/application/semantic_contract_parts/policy.py`
- `packages/aiask-quant-core/src/aiask_quant_core/strategy_dsl_parts/context.py`

但现有 `prediction_contract` 的语义是 `strategy_factory.prediction_contract.v1`，核心是 claim/evidence 解释链。它不是本方案所需的逐笔可验证交易预测合同，缺少：

- `target_trading_date`
- `prediction_as_of`
- `direction_probability`
- `time_buckets`
- `target_price_range`
- `entry_plan`
- `exit_plan`
- `risk_plan`
- 每条预测的 outcome 记录

因此修订结论是：不要替换现有 `prediction_contract`，而应新增独立字段 `trade_prediction_contract`。现有 `prediction_contract` 继续承担“为什么这么预测”的解释和审计职责，新增 `trade_prediction_contract` 承担“预测了什么、如何验证”的交易验证职责。

#### 14.1.2 孵化工厂已有前向方向验证和 regime 分桶基础

当前 `ForwardVerifier` 已经能基于 `strategy_signal_evidence` 中的历史信号和 `forward_returns` 计算：

- `primary_hit_rate`
- `secondary_hit_rate`
- `primary_skill_lcb`
- `secondary_skill_lcb`
- `recent_primary_skill_lcb`
- `coverage_ratio`
- `forward_ic`
- `forward_sharpe`
- `hit_rate_by_regime`

当前工作区还已有 `hit_rate_matrix.py`，用于聚合 `strategy_type x regime x holding_bucket` 的命中率矩阵，并对样本不足单元标记 `insufficient_samples`。这与本方案第 12.5 节“不允许只看总体平均”的原则高度一致。

相关代码证据：

- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/forward_verifier.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_matrix.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_reporter.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/metrics_recorder.py`

修订结论是：P1 的次日方向验证不应另起炉灶，应扩展 `ForwardVerifier` 的验证结果结构；P3/P4 的矩阵报告应复用 `hit_rate_matrix.py` 的聚合思想，把新指标纳入矩阵，而不是新增一套平行报表。

#### 14.1.3 信号追踪器已有日线前向收益回填

当前 Signal Tracker 已有 Phase B 前向收益回填流程，会从历史信号中找出待计算项，然后基于日线 K 线计算 `signal_forward_returns`。

相关代码证据：

- `packages/akshare-mcp/src/akshare_mcp/services/signal_tracker_parts/specs.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/signal_tracking.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/schema_strategy_parts/schema_definitions.py`

这说明 P1 的次日方向、目标区间、止盈止损日线兜底验证可直接沿用现有回填流程；但 P2 的盘中时间段验证不能复用当前日线回填，需要新增分钟线回放能力。

#### 14.1.4 存储层已有部分交易审计和事件证据字段

当前存储层已有：

- `kline_1d`
- `signal_forward_returns`
- `strategy_signal_evidence`
- `strategy_trade_positions`
- `strategy_trade_position_fills`
- `market_headline_labels`
- 交易审计字段：`entry_ts`、`exit_ts`、`entry_avg_price`、`exit_avg_price`、`gross_return`、`net_return`、`mfe`、`mae`、`price_path_audit_status`

相关代码证据：

- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_schema_market_phase_1.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_schema_market_phase_5.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_schema_market_phase_6.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/strategy_incubation_parts/reads.py`

这些字段可作为买卖计划、事件证据和风险控制验证的基础，但还不能替代 `strategy_trade_predictions` 和 `strategy_trade_prediction_outcomes`，因为现有表记录的是信号、证据、持仓和执行，而不是“预测本身”和“预测结果”。

#### 14.1.5 分钟线获取工具存在，但持久化和回放能力不足

当前 `get_minute_kline` 支持 `1m/5m/15m/30m/60m`，并有 AkShare、Sina、Tencent 多源降级。

相关代码证据：

- `packages/akshare-mcp/src/akshare_mcp/tools/market/kline.py`
- `packages/akshare-mcp/src/akshare_mcp/services/data_sync.py`

但是当前存储 schema 中明确可见的是 `kline_1d`，没有稳定的 `kline_intraday`、`kline_1m`、`kline_5m` 等持久化表。`data_sync.py` 中有 `kline_intraday` TTL 配置，但这只是缓存层语义，不等价于可回放的分钟线历史库。

修订结论是：P2 不能直接要求 ForwardVerifier 验证“10:30 是否到价”，必须先补分钟线持久化和回放服务。

#### 14.1.6 Stock-First Router 和因子货架已有雏形

当前工作区已有 `stock_strategy_router.py`，可基于个股画像路由到策略 family 和 holding bucket；也已有 `factor_catalog.py`，统一了因子元数据、方向、公式提示和 IC 历史指针。

相关代码证据：

- `packages/strategy-factory/src/strategy_factory/application/stock_strategy_router.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_catalog.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_mining_factory/`

这说明本方案第 7.3 节 Router 权重改造和第 9 节因子挖掘目标不是从零开始，而是应在已有 Router/Factor Catalog 上增加“交易预测可验证性”和“短周期预测贡献”维度。

#### 14.1.7 晋升门和预算反馈已有闭环

当前孵化工厂已有 Promotion Gate、HitRateReporter、FeedbackWriter，策略工厂已有预算反馈读取和 `budget_multiplier`、`skill_budget_multiplier` 等控制逻辑。

相关代码证据：

- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/promotion_gate.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_reporter.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/feedback_writer.py`
- `packages/strategy-factory/src/strategy_factory/application/_budget_feedback_parts/reporting.py`

修订结论是：P4 不需要重建反馈闭环，应把 `trade_prediction_score`、`direction_skill_lcb`、`time_bucket_skill_lcb`、`planned_trade_return` 作为新增输入接入现有门和预算系统。

### 14.2 当前明确缺失的能力

当前代码中未发现以下核心对象或表：

- `TradePredictionContract`
- `TradePredictionOutcome`
- `strategy_trade_predictions`
- `strategy_trade_prediction_outcomes`
- `trade_prediction_score`
- `time_bucket_hit_rate`
- `entry_quality_score`
- `exit_quality_score`
- `event_lead_score`
- 可持久回放的分钟级 K 线表

因此本方案的工程性质应定义为：

```text
在既有语义合同、信号证据、前向收益和孵化反馈闭环之上的交易预测验证层扩展。
```

而不是：

```text
替换现有 prediction_contract 或重写策略工厂。
```

## 15. 修订后的总体可行性判断

### 15.1 总体判断

可行性：中高。

主要原因：

- 架构上已经有策略工厂、孵化工厂、因子工厂、事件证据、前向收益、晋升反馈闭环。
- 现有语义合同链能承接“证据 -> 预测 -> 交易计划”的解释层。
- 现有孵化工厂能承接 T+N 方向验证和样本量控制。
- 当前工作区已有 Stock-First Router、命中率矩阵、因子货架等与本方案一致的前置能力。

主要限制：

- 交易预测合同和结果表未实现。
- 分钟线历史持久化未实现，盘中 bucket 回放不能只依赖实时工具。
- 事件领先性验证需要严格的 `event_time < prediction_as_of < validation_window` 时间约束。
- 样本不足时必须阻断晋升，不能用少量样本制造高命中率。

### 15.2 分阶段可行性

| 阶段 | 可行性 | 工程难度 | 主要阻塞 |
|---|---:|---:|---|
| P0 合约与存储 | 高 | 中 | 需要跨 `strategy-factory` 和 `aiask-quant-core` 定义稳定 schema |
| P1 次日方向与目标区间 | 高 | 中 | 需要把预测写入信号/孵化链路 |
| P2 盘中时间段与买卖窗口 | 中 | 高 | 需要分钟线持久化、回放、缺口标记 |
| P3 事件与因子领先性 | 中 | 高 | 需要事件时间戳治理和短周期因子标签 |
| P4 晋升门与预算反馈 | 高 | 中 | 依赖 P0-P3 指标稳定 |
| P5 桌面/Agent 可观测 | 高 | 中 | 需遵守 Agent `agent_*` 和 Desktop 只走 Agent HTTP 边界 |

## 16. 架构兼容性决策

### 16.1 不替换现有 `prediction_contract`

必须保留现有：

- `prediction_contract`
- `evidence_chain`
- `confidence_contract`
- `claim_to_trade_plan_map`
- `trade_plan_to_dsl_map`

新增：

```text
trade_prediction_contract
```

建议结构：

```json
{
  "contract_version": "strategy_factory.trade_prediction_contract.v1",
  "strategy_id": "factory_xxx",
  "candidate_id": "candidate_xxx",
  "stock_code": "000001",
  "prediction_as_of": "2026-06-05T15:30:00+08:00",
  "target_trading_date": "2026-06-08",
  "prediction_horizon": "next_trading_day",
  "direction": "up",
  "direction_probability": 0.68,
  "confidence": 0.62,
  "time_buckets": [],
  "entry_plan": {},
  "exit_plan": {},
  "risk_plan": {},
  "evidence_refs": {
    "evidence_ids": [],
    "claim_ids": [],
    "factor_ids": [],
    "event_ids": []
  },
  "validation_requirements": {
    "requires_daily_bar": true,
    "requires_intraday_bar": false,
    "min_effective_n": 30
  }
}
```

### 16.2 预测和结果必须独立落表

不要只把预测塞进 `params` 或 `metadata`，否则后续无法做：

- 单条预测回放
- 按 prediction_id 追踪 outcome
- 重算 score
- 按股票/策略类型/regime/事件类型/因子类型聚合
- 防止事后修改预测

新增表仍建议使用原方案中的两张表，但字段需要补充审计约束：

```text
strategy_trade_predictions
strategy_trade_prediction_outcomes
```

`strategy_trade_predictions` 应增加：

- `prediction_id`
- `candidate_id`
- `signal_id`
- `prediction_status`
- `source_contract_version`
- `frozen_payload_hash`
- `prediction_frozen_at`
- `created_by`

`strategy_trade_prediction_outcomes` 应增加：

- `score_version`
- `data_quality_status`
- `daily_bar_status`
- `intraday_bar_status`
- `event_lineage_status`
- `insufficient_reason`
- `recomputed_from_prediction_hash`

### 16.3 分钟线必须先持久化再验证

P2 前置表建议：

```text
kline_intraday
```

核心字段：

- `time`
- `code`
- `period`
- `open`
- `high`
- `low`
- `close`
- `volume`
- `amount`
- `source`
- `source_chain_json`
- `data_quality_status`
- `updated_at`

主键建议：

```text
PRIMARY KEY (code, period, time)
```

索引建议：

```text
idx_kline_intraday_code_period_time_desc
idx_kline_intraday_time
```

如果分钟线缺失，结果不能默认按日线补齐，而应输出：

```text
intraday_bar_status = missing
time_bucket_score = null
score_status = partial_daily_only
```

### 16.4 事件证据必须冻结时间线

事件/因子证据进入预测时必须固化：

- `event_id`
- `event_time`
- `source`
- `ingested_at`
- `prediction_as_of`
- `validation_window_start`
- `validation_window_end`

有效领先性要求：

```text
event_time <= prediction_as_of < validation_window_start
```

如果事件在验证窗口之后才进入系统，必须标记：

```text
evidence_score = null
event_lineage_status = post_hoc_rejected
```

### 16.5 评分不能单点神谕化

综合评分仍使用原方案权重，但必须增加 `score_status`：

- `ok`
- `partial_daily_only`
- `partial_intraday_missing`
- `insufficient_samples`
- `post_hoc_evidence_rejected`
- `data_quality_failed`

当关键样本不足或数据缺失时，不允许输出可晋升分数，只允许输出诊断分数。

## 17. 修订后的实施路线

### P0：合同、冻结、存储与兼容层

目标：

- 新增 `trade_prediction_contract` 数据结构。
- 新增 `TradePredictionContract`、`TradePredictionOutcome` 类型定义。
- 新增 `strategy_trade_predictions` 和 `strategy_trade_prediction_outcomes` schema。
- 增加预测冻结哈希，防止事后改预测。
- 增加从现有 `prediction_contract`、`trade_plan` 映射到 `trade_prediction_contract` 的兼容生成器。
- 让孵化工厂可以读取预测并生成 `empty_outcome`。

建议改造位置：

- `packages/strategy-factory/src/strategy_factory/api/contracts.py`
- `packages/strategy-factory/src/strategy_factory/application/semantic_contract_parts/`
- `packages/strategy-factory/src/strategy_factory/application/precompile_contract.py`
- `packages/akshare-mcp/src/akshare_mcp/services/_strategy_llm_provider_prompt.py`
- `packages/akshare-mcp/src/akshare_mcp/services/_strategy_llm_provider_normalize_parts/`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/`

验收：

- 每个进入孵化的 candidate 至少有一条冻结后的 `strategy_trade_predictions`。
- 没有 `target_trading_date`、`direction`、`direction_probability`、`risk_plan` 的预测不能进入正式孵化，只能进入诊断观察。
- 旧策略不被破坏，可由兼容层生成 `trade_prediction_contract.status = derived_from_legacy_contract`。

### P1：次日方向、目标价和日线风险验证

目标：

- 基于 `kline_1d` 和 `signal_forward_returns` 验证：
  - `next_day_direction_hit`
  - `target_touch_hit`
  - `target_price_error_pct`
  - `risk_control_score`
  - `planned_trade_return_daily_proxy`
- 生成第一版 `trade_prediction_score`。

建议改造位置：

- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/forward_verifier.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/metrics_recorder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_reporter.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/signal_tracking.py`

验收：

- 生成 `direction_hit_rate`、`direction_skill_lcb`、`target_touch_rate`、`trade_prediction_score`。
- 样本数小于门槛时输出 `insufficient_samples`，不得晋升。
- 日线无法验证盘中指标时输出 `partial_daily_only`。

### P2：分钟线持久化与盘中 bucket 回放

目标：

- 新增 `kline_intraday` schema。
- 将 `get_minute_kline` 的结果接入持久化和数据质量校验。
- 增加 `IntradayReplayService`，按交易日、代码、period 拉取可回放分钟 bars。
- 验证：
  - `time_bucket_hit_rate`
  - `time_bucket_skill_lcb`
  - `entry_window_hit`
  - `entry_price_slippage_pct`
  - `exit_window_hit`
  - `profit_capture_ratio`
  - `planned_trade_return`
  - `planned_trade_max_drawdown`

建议改造位置：

- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/`
- `packages/akshare-mcp/src/akshare_mcp/tools/market/kline.py`
- `packages/akshare-mcp/src/akshare_mcp/services/data_sync.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/`

验收：

- 对最近 N 个交易日可查询 `kline_intraday(code, period, time)`。
- 分钟线缺口被显式标记，不用日线伪造时间桶。
- 盘中验证结果能按 prediction_id 复算。

### P3：事件和因子领先性验证

目标：

- 把事件和因子统一进入 `trade_prediction_contract.evidence_refs`。
- 验证事件是否早于预测生成和行情窗口。
- 验证事件后 5/15/30/60 分钟和次日方向贡献。
- 验证因子短周期贡献：
  - `factor_direction_hit`
  - `factor_short_horizon_ic`
  - `event_conditioned_ic`
  - `regime_conditioned_ic`

建议改造位置：

- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_schema_market_phase_6.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_catalog.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_mining_factory/`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_matrix.py`

验收：

- 事件晚于预测时间时被拒绝计分。
- 按 `event_type`、`factor_type`、`regime` 输出命中率矩阵。
- 因子短周期 IC 不满足样本门槛时不能进入 active pool 权重提升。

### P4：晋升门和预算反馈接入

目标：

- 将 `trade_prediction_score` 接入 Promotion Gate。
- 将低分 family/target_pool/holding_bucket 写入反馈控制。
- 将高分且样本充足的 family 增加预算。
- 将失效因子送入衰减或隔离流程。

建议改造位置：

- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/promotion_gate.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/feedback_writer.py`
- `packages/strategy-factory/src/strategy_factory/application/_budget_feedback_parts/`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_mining_factory/feedback/`

晋升条件修订为：

```text
direction_skill_lcb > 0
trade_prediction_score_lcb >= 0.55
planned_trade_return_median > 0
max_drawdown within risk_plan
effective_n >= minimum threshold
score_status == ok
promotion_gate_dsr_passed == true
```

P2 未完成前，不允许 `time_bucket_score` 成为硬门，只能作为诊断项。

### P5：Agent/Desktop 可观测与操作入口

目标：

- Agent 暴露只读查询，名称必须以 `agent_` 开头。
- Desktop 只通过 Agent HTTP 查看预测评分、命中矩阵、数据缺口和反馈动作。
- 不暴露 raw manager action，不引入实盘交易动作。

建议新增 Agent 只读能力：

- `agent_trade_prediction_status`
- `agent_trade_prediction_outcomes`
- `agent_trade_prediction_matrix`
- `agent_intraday_data_quality`

验收：

- Desktop 不直接调用 MCP 或 manager。
- Agent 工具符合 `agent_*` 命名边界。
- 所有涉及交易执行的功能保持只读，不触发下单。

## 18. 修订后的评分定义

### 18.1 单条预测评分

```text
trade_prediction_score =
  0.40 * direction_score
+ 0.20 * time_bucket_score
+ 0.15 * target_price_score
+ 0.15 * trade_plan_score
+ 0.05 * risk_control_score
+ 0.05 * evidence_score
```

如果 P2 分钟线未完成，临时评分为：

```text
trade_prediction_score_daily_v1 =
  0.55 * direction_score
+ 0.20 * target_price_score
+ 0.15 * trade_plan_daily_proxy_score
+ 0.05 * risk_control_score
+ 0.05 * evidence_timestamp_score
```

临时评分必须标记：

```text
score_version = trade_prediction_score_daily_v1
score_status = partial_daily_only
```

### 18.2 聚合评分

用于晋升和预算的不是简单平均，而是：

```text
trade_prediction_score_lcb = Wilson/Bootstrap lower confidence bound of score pass rate
```

建议聚合维度：

- `strategy_id`
- `strategy_type`
- `stock_code`
- `target_pool`
- `holding_bucket`
- `trend_regime`
- `vol_regime`
- `sentiment_regime`
- `event_type`
- `factor_type`

### 18.3 样本门槛

建议默认门槛：

```text
single_strategy_min_n = 30
matrix_cell_min_n = 5
event_type_min_n = 20
factor_type_min_n = 30
intraday_bucket_min_n = 30
```

低于门槛只能展示诊断，不得触发晋升或预算加权。

## 19. 数据质量和防事后解释规则

### 19.1 预测冻结规则

一条预测进入孵化时必须冻结：

```text
frozen_payload_hash = sha256(canonical_json(trade_prediction_contract))
prediction_frozen_at = now()
```

Outcome 计算时必须记录：

```text
recomputed_from_prediction_hash
```

如果 hash 不一致：

```text
score_status = prediction_mutated_rejected
```

### 19.2 时间线规则

有效预测必须满足：

```text
prediction_as_of < target_trading_date market_open
```

有效事件证据必须满足：

```text
event_time <= prediction_as_of
```

有效因子证据必须满足：

```text
factor_snapshot_time <= prediction_as_of
```

否则计为：

```text
post_hoc_rejected
```

### 19.3 数据缺口规则

不允许用缺失数据制造命中。

缺口状态：

- `daily_bar_missing`
- `intraday_bar_missing`
- `intraday_partial_gap`
- `event_timestamp_missing`
- `factor_snapshot_missing`
- `market_suspended`
- `limit_up_down_untradable`

这些状态必须进入 `outcome_json`。

## 20. 测试与验收矩阵

### P0 测试

- 合同 schema 单测：合法/缺字段/非法时间线。
- 预测冻结 hash 单测。
- 旧 `prediction_contract` 到 `trade_prediction_contract` 的兼容映射单测。
- 存储 round-trip 单测。

### P1 测试

- 日线方向命中单测。
- 目标价触达单测。
- 止损止盈日线兜底单测。
- 样本不足 `insufficient_samples` 单测。
- `MetricsRecorder` 写入新增指标单测。

### P2 测试

- `kline_intraday` schema 和索引测试。
- 分钟线保存、读取、缺口检测测试。
- time bucket 命中测试。
- entry/exit window 质量测试。
- 盘中数据缺失时 `partial_intraday_missing` 测试。

### P3 测试

- 事件时间晚于预测时拒绝计分。
- 因子快照晚于预测时拒绝计分。
- 按 event/factor/regime 聚合矩阵测试。
- 短周期 IC 样本不足测试。

### P4 测试

- Promotion Gate 读取 `trade_prediction_score_lcb`。
- 低分 family 进入 cooldown/freeze。
- 高分 family 增加预算。
- 失效因子进入 decay/quarantine。

### P5 测试

- Agent 工具命名符合 `agent_*`。
- Desktop 只走 Agent HTTP。
- 不暴露 raw manager 或实盘交易动作。

## 21. 最终修订结论

本方案应落地为：

```text
交易预测验证层
= 现有语义合同链
+ 冻结的 trade_prediction_contract
+ 预测 outcome 表
+ 日线/分钟线回放
+ 事件/因子领先性验证
+ 孵化评分与预算反馈
```

优先级排序：

1. 先做 P0/P1，让系统能稳定回答“预测了什么、次日方向是否命中、目标价是否触达”。
2. 再做 P2，补分钟线持久化和盘中回放，回答“哪个时间段、买卖窗口是否有效”。
3. 再做 P3，把事件和因子的领先性纳入评分，避免事后解释。
4. 最后做 P4/P5，把评分接入晋升、预算、因子衰减和桌面可观测。

最重要的工程原则：

```text
不要把 LLM 输出当成准确率。
只把冻结预测在真实行情、真实时间线、真实数据质量约束下的可复算结果当成准确率。
```
