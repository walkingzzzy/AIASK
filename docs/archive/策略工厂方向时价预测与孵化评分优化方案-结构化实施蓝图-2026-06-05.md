# 策略工厂方向时价预测与孵化评分优化方案：结构化实施蓝图

日期：2026-06-05

适用范围：AIASK 当前工作区的策略工厂、孵化工厂、因子挖掘工厂、AKShare MCP 数据层、Agent/Desktop 可观测层。

本文是对 `策略工厂方向时价预测与孵化评分优化方案-2026-06-05.md` 的结构化工程拆解版。目标不是替换原始方案，而是把原方案整理成可开发、可测试、可验收、可分阶段上线的实施蓝图。

## 1. 执行结论

### 1.1 总体判断

方案可行，且与当前 AIASK 架构方向一致。当前代码已经具备语义合同链、前向收益验证、regime 分桶、命中率矩阵、Stock-First Router、因子货架、晋升门和预算反馈闭环。

但方案不能按“只改 Prompt”或“只新增几个指标”处理。它应被定义为一个新的交易预测验证层：

```text
交易预测验证层
= 现有 evidence_chain / prediction_contract / trade_plan / dsl 语义链
+ 新增 trade_prediction_contract
+ 新增 prediction/outcome 持久化
+ 日线与分钟线回放验证
+ 事件/因子领先性验证
+ 孵化评分、晋升门、预算反馈闭环
```

### 1.2 核心工程决策

- 保留现有 `prediction_contract`，不要替换它。
- 新增 `trade_prediction_contract`，专门描述“预测了什么、如何验证”。
- 新增 `strategy_trade_predictions` 与 `strategy_trade_prediction_outcomes` 两张表，预测和结果必须可追踪、可冻结、可复算。
- P2 盘中时间桶验证前，必须先落地分钟线持久化表 `kline_intraday` 和回放服务。
- P2 完成前，`time_bucket_score` 只能作为诊断项，不能作为硬晋升门。
- 事件和因子必须满足严格时间线：证据时间必须早于预测时间，预测时间必须早于验证窗口。
- 样本不足时必须输出 `insufficient_samples`，不能用少量样本宣称准确。

### 1.3 可行性分层

| 阶段 | 可行性 | 难度 | 优先级 | 结论 |
|---|---:|---:|---:|---|
| P0 合同、冻结、存储 | 高 | 中 | 最高 | 必须先做 |
| P1 次日方向和目标区间 | 高 | 中 | 最高 | 可复用现有日线前向验证 |
| P2 盘中时间段和买卖窗口 | 中 | 高 | 高 | 依赖分钟线持久化 |
| P3 事件和因子领先性 | 中 | 高 | 中高 | 依赖时间戳治理和短周期标签 |
| P4 晋升门和预算反馈 | 高 | 中 | 中 | 依赖 P0-P3 指标稳定 |
| P5 Agent/Desktop 可观测 | 高 | 中 | 中 | 只读、遵守 Agent 边界 |

## 2. 文档结构

后续章节按工程落地顺序组织：

- 第 3 节：当前代码现状对照。
- 第 4 节：目标架构与数据流。
- 第 5 节：交易预测合同设计。
- 第 6 节：数据库与持久化设计。
- 第 7 节：评分体系与统计口径。
- 第 8 节：策略工厂改造。
- 第 9 节：孵化工厂改造。
- 第 10 节：因子挖掘工厂改造。
- 第 11 节：事件驱动证据改造。
- 第 12 节：分钟线与数据质量改造。
- 第 13 节：晋升门、预算反馈和因子衰减。
- 第 14 节：Agent/Desktop 可观测。
- 第 15 节：P0-P5 实施路线。
- 第 16 节：验收标准和测试矩阵。
- 第 17 节：风险、边界和禁止事项。
- 第 18 节：可执行任务清单。

## 3. 当前代码现状对照

本节只描述当前工作区已经能从代码中确认的能力和缺口。设计和排期必须以这一节为事实基础。

### 3.1 已具备能力总览

| 领域 | 当前能力 | 可复用程度 | 说明 |
|---|---|---:|---|
| 策略语义合同 | `evidence_chain -> prediction_contract -> trade_plan -> dsl` | 高 | 可作为交易预测解释层 |
| LLM 生成约束 | Prompt 已要求结构化 evidence、claim、trade plan、risk rules | 高 | 需要扩展输出 `trade_prediction_contract` |
| 语义审计 | 已有 claim/evidence/trade step/dsl 映射和审计概念 | 高 | 不应被新合同替代 |
| 前向验证 | `ForwardVerifier` 计算 T+N 命中率、LCB、IC、Sharpe | 高 | P1 可直接扩展 |
| regime 分桶 | `hit_rate_by_regime` 与命中率矩阵已有雏形 | 高 | P3/P4 可复用 |
| 日线前向收益 | Signal Tracker 可回填 `signal_forward_returns` | 高 | P1 的日线验证基础 |
| 交易审计 | 持仓/成交/entry/exit/MFE/MAE 字段存在 | 中 | 可做交易计划质量输入 |
| 分钟线获取 | `get_minute_kline` 支持 1m/5m/15m/30m/60m | 中 | 只能说明可获取，不等于可回放 |
| Stock-First Router | 已有 `stock_strategy_router.py` | 中高 | 可增加交易预测可验证性权重 |
| 因子货架 | 已有 `factor_catalog.py` | 中高 | 可增加短周期预测贡献元数据 |
| 晋升门 | 已有 Promotion Gate 和 DSR/样本量思想 | 高 | 可接入新评分 |
| 预算反馈 | 已有 `budget_multiplier` 和 `skill_budget_multiplier` | 高 | 可接入新评分反馈 |
| Agent/Desktop 边界 | Agent 工具需 `agent_*`，Desktop 只走 Agent HTTP | 高 | P5 必须遵守 |

### 3.2 策略工厂现状

当前策略工厂已经要求候选策略带有结构化语义合同。核心链路是：

```text
evidence_chain
  -> prediction_contract.claims[]
  -> trade_plan entry/exit/steps
  -> dsl
```

现有 `prediction_contract` 的职责是表达“基于哪些证据提出了哪些 claim，以及 claim 在什么条件下失效”。它适合解释和审计，但不适合直接回答以下交易预测问题：

- 具体预测哪一天。
- 预测方向概率是多少。
- 目标价区间是多少。
- 哪个时间段上涨或下跌。
- 哪个窗口买入。
- 哪个窗口卖出。
- 风险计划如何逐条验证。
- 单条预测最终是否命中。

因此当前策略工厂的主要缺口不是“没有预测语义”，而是“没有逐笔交易预测合同”。

### 3.3 孵化工厂现状

当前孵化工厂的核心验证对象是历史信号的前向收益。典型输出包括：

- `primary_hit_rate`
- `secondary_hit_rate`
- `primary_skill_lcb`
- `secondary_skill_lcb`
- `recent_primary_hit_rate`
- `recent_primary_skill_lcb`
- `coverage_ratio`
- `forward_ic`
- `forward_sharpe`
- `hit_rate_by_regime`

这些指标能回答：

```text
策略信号在 T+N 窗口上方向是否有效。
```

但不能完整回答：

```text
某条结构化交易预测在目标交易日、目标时间段、目标价区间、买卖窗口和风险计划上是否有效。
```

因此孵化工厂应保留现有前向验证，同时新增 trade prediction outcome 验证分支。

### 3.4 信号追踪与前向收益现状

Signal Tracker 已有 Phase B 前向收益回填，会批量查找待计算信号，并基于日线 K 线计算 `signal_forward_returns`。

这说明：

- P1 的次日方向验证可以复用当前日线前向收益链路。
- P1 的目标价触达可以复用 `kline_1d.high/low`。
- P1 的日线止损止盈可以做兜底代理。
- P2 的盘中时间段验证不能复用当前日线回填，需要新增分钟线回放链路。

### 3.5 存储层现状

当前可见的关键表包括：

| 表 | 当前用途 | 对本方案的作用 |
|---|---|---|
| `kline_1d` | 日线 K 线 | P1 日线验证基础 |
| `signal_forward_returns` | 信号 T+N 前向收益 | P1 方向验证基础 |
| `strategy_signal_evidence` | 信号证据落库 | 可关联预测 evidence |
| `strategy_trade_positions` | 持仓和交易审计 | 可辅助交易计划收益质量 |
| `strategy_trade_position_fills` | 成交明细 | 可辅助 entry/exit 质量 |
| `market_headline_labels` | 新闻/公告标签 | 可辅助事件证据 |

当前明确缺失：

- `strategy_trade_predictions`
- `strategy_trade_prediction_outcomes`
- `kline_intraday`
- 单条预测冻结哈希字段
- 单条 outcome 复算版本字段

### 3.6 分钟线现状

当前已有 `get_minute_kline` 工具，支持：

- `1m`
- `5m`
- `15m`
- `30m`
- `60m`

并具备 AkShare、Sina、Tencent 降级逻辑。

但这只是获取能力，不是回放能力。要验证“09:45-10:30 是否上涨”“14:00 是否触达目标价”，必须有持久化分钟线和缺口检测。

P2 前不允许把以下指标作为正式晋升门：

- `time_bucket_hit_rate`
- `entry_window_hit`
- `exit_window_hit`
- `profit_capture_ratio`

### 3.7 因子挖掘现状

当前因子挖掘工厂已有：

- 因子生成引擎。
- 候选验证流程。
- active pool。
- decay maintenance。
- `factor_catalog.py` 统一因子元数据。
- IC 历史指针。

当前主要缺口是短周期预测目标还不充分：

- 次日方向 IC。
- 开盘后 30 分钟方向 IC。
- 上午/下午 bucket IC。
- event-conditioned IC。
- regime-conditioned short-horizon IC。
- 因子对 `trade_prediction_score` 的边际贡献。

### 3.8 事件驱动现状

当前已有新闻、公告、研报、市场文档、headline label、sentiment 等数据/工具基础，但事件领先性验证还需要补齐：

- 统一事件结构。
- `event_time`、`ingested_at`、`prediction_as_of` 的时间线约束。
- 事件与 prediction_id 的关联。
- 事件后 5/15/30/60 分钟收益验证。
- 事件证据被事后写入时的拒绝规则。

### 3.9 当前缺口清单

必须新增或扩展的能力如下：

| 缺口 | 必要性 | 所属阶段 |
|---|---:|---|
| `TradePredictionContract` 类型 | 必须 | P0 |
| `TradePredictionOutcome` 类型 | 必须 | P0 |
| `strategy_trade_predictions` 表 | 必须 | P0 |
| `strategy_trade_prediction_outcomes` 表 | 必须 | P0 |
| 预测冻结 hash | 必须 | P0 |
| 日线方向与目标价 outcome 计算 | 必须 | P1 |
| `trade_prediction_score_daily_v1` | 必须 | P1 |
| `kline_intraday` 表 | 必须 | P2 |
| 分钟线回放服务 | 必须 | P2 |
| bucket/entry/exit 回放指标 | 必须 | P2 |
| 事件领先性验证 | 必须 | P3 |
| 短周期因子贡献 | 必须 | P3 |
| 晋升门接入新评分 | 必须 | P4 |
| 预算反馈接入新评分 | 必须 | P4 |
| Agent/Desktop 只读可观测 | 应做 | P5 |

## 4. 目标架构与数据流

### 4.1 目标架构定位

新增能力应放在“交易预测验证层”，位于策略语义合同和孵化评分之间。

```text
策略生成/路由
  -> evidence_chain / prediction_contract / trade_plan / dsl
  -> trade_prediction_contract
  -> strategy_trade_predictions
  -> 日线/分钟线/事件/因子回放验证
  -> strategy_trade_prediction_outcomes
  -> trade_prediction_score / score_lcb
  -> Promotion Gate / HitRate Matrix / Budget Feedback / Factor Decay
```

### 4.2 包职责边界

| 包 | 职责 | 不应承担 |
|---|---|---|
| `strategy-factory` | 生成、规范化、审核 `trade_prediction_contract`；提交前质量门 | 不直接读写数据库细节，不直接调用分钟线接口 |
| `akshare-mcp` | 数据获取、孵化验证、事件/因子服务、工厂运行编排 | 不绕过 storage contract，不暴露 raw manager 给 Agent |
| `aiask-quant-core` | SQLite schema、storage adapter、回放读取接口、统计存储 | 不承担业务 Prompt 和 Agent 工具策略 |
| `agent` | 只读能力封装、`agent_*` 工具、HTTP API | 不直接暴露 manager action，不执行实盘交易 |
| `desktop` | 可观测 UI | 不直接调用 MCP 或 Python manager |

### 4.3 预测生命周期

单条交易预测必须经历以下状态：

```text
draft
  -> normalized
  -> frozen
  -> pending_outcome
  -> evaluated
  -> aggregated
```

状态定义：

| 状态 | 含义 | 允许动作 |
|---|---|---|
| `draft` | 生成器刚产出，未通过规范化 | 修复、补字段、拒绝 |
| `normalized` | 字段完整，时间线初步合法 | 冻结、提交孵化 |
| `frozen` | 已写入 hash，不允许改预测内容 | 等待行情验证 |
| `pending_outcome` | 预测日未结束或数据未齐 | 补数据、等待 |
| `evaluated` | 已生成 outcome | 复算、聚合 |
| `aggregated` | 已进入矩阵、晋升门或预算反馈 | 只读审计 |

禁止状态跳转：

- `draft -> evaluated`
- `normalized -> evaluated`
- `frozen -> draft`
- `evaluated -> frozen`

如果预测需要修订，必须生成新的 `prediction_id`，不能覆盖旧预测。

### 4.4 核心数据流

#### 4.4.1 P0/P1 日线数据流

```text
StrategyFactory candidate
  -> ensure_candidate_semantic_contract()
  -> build_trade_prediction_contract()
  -> freeze_prediction()
  -> save_strategy_trade_prediction()
  -> SignalTracker / IncubationFactory
  -> daily outcome verifier
  -> save_strategy_trade_prediction_outcome()
  -> MetricsRecorder / HitRateReporter
```

#### 4.4.2 P2 分钟线数据流

```text
get_minute_kline()
  -> normalize minute bars
  -> validate minute bars
  -> save kline_intraday
  -> IntradayReplayService
  -> time bucket / entry / exit replay
  -> update prediction outcome
```

#### 4.4.3 P3 事件和因子数据流

```text
market document / headline / factor snapshot
  -> timestamp normalization
  -> evidence_refs in trade_prediction_contract
  -> lineage validation
  -> event/factor contribution scoring
  -> hit rate matrix by event_type/factor_type/regime
```

#### 4.4.4 P4 反馈数据流

```text
prediction outcomes
  -> score aggregates
  -> score_lcb / pass_rate_lcb
  -> PromotionGate
  -> HitRateReporter feedback_actions
  -> FeedbackWriter
  -> StrategyFactory budget feedback
  -> FactorMiningFactory decay/quarantine
```

## 5. 交易预测合同设计

### 5.1 设计原则

`trade_prediction_contract` 只描述可验证预测，不替代 `prediction_contract`。

必须满足：

- 可冻结。
- 可复算。
- 可关联原始 evidence/claim/trade step。
- 可被日线验证。
- 可在分钟线可用时被盘中验证。
- 可在数据缺失时诚实降级。
- 可被聚合到策略、股票、family、regime、event、factor 维度。

### 5.2 最小必填字段

P0 必填字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `contract_version` | string | 是 | 固定为 `strategy_factory.trade_prediction_contract.v1` |
| `prediction_id` | string | 是 | 全局唯一 |
| `strategy_id` | string | 是 | 策略 ID |
| `candidate_id` | string | 否 | 候选 ID，旧策略可缺失 |
| `stock_code` | string | 是 | 证券代码 |
| `prediction_as_of` | datetime | 是 | 预测冻结前的生成时间 |
| `target_trading_date` | date | 是 | 目标交易日 |
| `prediction_horizon` | string | 是 | `next_trading_day`、`t_plus_5` 等 |
| `direction` | string | 是 | `up`、`down`、`flat` |
| `direction_probability` | number | 是 | 0 到 1 |
| `confidence` | number | 是 | 0 到 1 |
| `entry_plan` | object | 是 | 入场计划 |
| `exit_plan` | object | 是 | 出场计划 |
| `risk_plan` | object | 是 | 风险计划 |
| `evidence_refs` | object | 是 | 关联 evidence/claim/factor/event |
| `validation_requirements` | object | 是 | 数据和验证要求 |

### 5.3 完整合同示例

```json
{
  "contract_version": "strategy_factory.trade_prediction_contract.v1",
  "prediction_id": "tp_20260605_000001_factory_xxx_001",
  "strategy_id": "factory_xxx",
  "candidate_id": "candidate_xxx",
  "signal_id": null,
  "stock_code": "000001",
  "prediction_as_of": "2026-06-05T15:30:00+08:00",
  "target_trading_date": "2026-06-08",
  "prediction_horizon": "next_trading_day",
  "direction": "up",
  "direction_probability": 0.68,
  "confidence": 0.62,
  "time_buckets": [
    {
      "bucket_id": "tb_001",
      "window": "09:45-10:30",
      "direction": "up",
      "target_price_range": [118.0, 122.0],
      "probability": 0.62,
      "required_period": "5m"
    }
  ],
  "entry_plan": {
    "window": "09:35-09:50",
    "price_range": [100.0, 103.0],
    "entry_condition": "volume_breakout_and_positive_fund_flow",
    "fallback": "skip_if_open_gap_above_target_range"
  },
  "exit_plan": {
    "take_profit_range": [118.0, 122.0],
    "stop_loss": 96.0,
    "latest_exit_window": "14:30-14:50",
    "time_stop": "14:50"
  },
  "risk_plan": {
    "max_adverse_excursion_pct": 3.5,
    "max_position_pct": 10.0,
    "invalidation": "price_breaks_96_or_event_sentiment_turns_negative"
  },
  "evidence_refs": {
    "evidence_ids": ["ev_entry_signal"],
    "claim_ids": ["claim_entry"],
    "trade_step_ids": ["entry_1", "exit_1"],
    "factor_ids": ["intraday_momentum", "north_fund_flow"],
    "event_ids": ["event_xxx"]
  },
  "validation_requirements": {
    "requires_daily_bar": true,
    "requires_intraday_bar": true,
    "intraday_period": "5m",
    "min_effective_n": 30,
    "allow_daily_proxy_before_p2": true
  }
}
```

### 5.4 字段规范

#### 5.4.1 方向字段

允许值：

- `up`
- `down`
- `flat`

不允许：

- `看涨`
- `可能涨`
- `建议买入`
- `偏强`

中文描述可以放在 `rationale` 或 evidence summary，不允许作为机器字段值。

#### 5.4.2 时间字段

所有时间必须带时区或明确使用交易所本地时间。

建议：

```text
prediction_as_of: ISO-8601 datetime with timezone
target_trading_date: YYYY-MM-DD
time_buckets[].window: HH:MM-HH:MM
```

#### 5.4.3 价格区间字段

价格区间必须满足：

```text
lower > 0
upper > 0
lower <= upper
```

区间不能为空，不能写自由文本。

#### 5.4.4 置信度字段

`direction_probability` 表示方向概率。

`confidence` 表示合同整体可信度，允许综合考虑：

- 证据完整性。
- 数据质量。
- 模型/规则一致性。
- 样本覆盖。
- regime 匹配度。

#### 5.4.5 evidence_refs 字段

`evidence_refs` 必须只放 ID 引用，不放大段证据正文。证据正文仍由现有 `evidence_chain`、`strategy_signal_evidence`、事件表和因子表承载。

### 5.5 合同生成策略

合同来源分三类：

| 来源 | `prediction_status` | 说明 |
|---|---|---|
| LLM 原生输出 | `native_llm_contract` | LLM 直接输出完整合同 |
| 规则兼容生成 | `derived_from_legacy_contract` | 从现有 `prediction_contract`、`trade_plan`、`risk_rules` 推导 |
| 手工/系统模板 | `system_template_contract` | 用于旧策略或诊断观察 |

优先级：

```text
native_llm_contract
  > derived_from_legacy_contract
  > system_template_contract
```

### 5.6 合同规范化规则

规范化器必须执行：

- 补齐 `contract_version`。
- 标准化 `stock_code`。
- 标准化方向。
- 标准化时间字段。
- 校验 `prediction_as_of < target_trading_date market_open`。
- 校验 `direction_probability` 和 `confidence` 在 0 到 1。
- 校验 entry/exit/risk plan 完整性。
- 校验 evidence refs 至少引用一个 claim 或 evidence。
- 生成 `prediction_id`。
- 生成 canonical JSON。
- 生成 hash。

### 5.7 合同拒绝规则

以下情况必须拒绝正式孵化：

- 缺少 `target_trading_date`。
- 缺少 `direction`。
- 缺少 `direction_probability`。
- 缺少 `risk_plan`。
- `prediction_as_of` 晚于目标交易日开盘。
- evidence refs 为空。
- 价格区间非法。
- 入场和出场窗口不可解析。
- 合同 hash 复算不一致。

允许进入诊断观察但不能晋升的情况：

- 旧策略只能推导出日线方向。
- 缺分钟线要求但没有 time buckets。
- 有 direction 但无 target price。
- 有 target price 但无 entry/exit window。

## 6. 数据库与持久化设计

### 6.1 总体原则

预测和结果必须独立落表，不能只存入 `params` 或 `metadata`。

原因：

- 单条预测需要冻结。
- outcome 需要可复算。
- 评分版本需要可追溯。
- 后续矩阵聚合需要高效查询。
- 不能让 LLM 或后处理在事后修改历史预测。

### 6.2 表一：`strategy_trade_predictions`

用途：保存冻结后的交易预测合同。

建议字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `prediction_id` | TEXT PRIMARY KEY | 是 | 预测 ID |
| `strategy_id` | TEXT | 是 | 策略 ID |
| `candidate_id` | TEXT | 否 | 候选 ID |
| `signal_id` | TEXT | 否 | 后续信号 ID |
| `stock_code` | TEXT | 是 | 股票代码 |
| `prediction_as_of` | TEXT | 是 | 预测时间 |
| `target_trading_date` | TEXT | 是 | 目标交易日 |
| `prediction_horizon` | TEXT | 是 | 预测周期 |
| `direction` | TEXT | 是 | 方向 |
| `direction_probability` | REAL | 是 | 方向概率 |
| `confidence` | REAL | 是 | 整体置信度 |
| `prediction_status` | TEXT | 是 | 合同来源/状态 |
| `source_contract_version` | TEXT | 是 | 合同版本 |
| `entry_plan_json` | TEXT | 是 | 入场计划 |
| `exit_plan_json` | TEXT | 是 | 出场计划 |
| `risk_plan_json` | TEXT | 是 | 风险计划 |
| `time_buckets_json` | TEXT | 是 | 时间桶 |
| `evidence_refs_json` | TEXT | 是 | 证据引用 |
| `validation_requirements_json` | TEXT | 是 | 验证要求 |
| `contract_json` | TEXT | 是 | 完整 canonical contract |
| `frozen_payload_hash` | TEXT | 是 | 冻结 hash |
| `prediction_frozen_at` | TEXT | 是 | 冻结时间 |
| `created_by` | TEXT | 否 | 来源模块 |
| `created_at` | TEXT | 是 | 创建时间 |
| `updated_at` | TEXT | 是 | 更新时间 |

建议索引：

```sql
CREATE INDEX IF NOT EXISTS idx_strategy_trade_predictions_strategy
ON strategy_trade_predictions(strategy_id, target_trading_date DESC);

CREATE INDEX IF NOT EXISTS idx_strategy_trade_predictions_stock_date
ON strategy_trade_predictions(stock_code, target_trading_date DESC);

CREATE INDEX IF NOT EXISTS idx_strategy_trade_predictions_status
ON strategy_trade_predictions(prediction_status, target_trading_date DESC);
```

### 6.3 表二：`strategy_trade_prediction_outcomes`

用途：保存单条预测的验证结果和评分。

建议字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `outcome_id` | TEXT PRIMARY KEY | 是 | 结果 ID |
| `prediction_id` | TEXT | 是 | 预测 ID |
| `strategy_id` | TEXT | 是 | 策略 ID |
| `stock_code` | TEXT | 是 | 股票代码 |
| `actual_trading_date` | TEXT | 是 | 实际验证日 |
| `direction_hit` | INTEGER | 否 | 方向是否命中 |
| `direction_score` | REAL | 否 | 方向分 |
| `time_bucket_score` | REAL | 否 | 时间桶分 |
| `target_touch_hit` | INTEGER | 否 | 目标价是否触达 |
| `target_price_score` | REAL | 否 | 目标价分 |
| `entry_score` | REAL | 否 | 入场质量分 |
| `exit_score` | REAL | 否 | 出场质量分 |
| `trade_plan_score` | REAL | 否 | 交易计划分 |
| `risk_control_score` | REAL | 否 | 风险控制分 |
| `evidence_score` | REAL | 否 | 证据领先性分 |
| `planned_trade_return` | REAL | 否 | 按计划模拟收益 |
| `planned_trade_r_multiple` | REAL | 否 | R 倍数 |
| `planned_trade_max_drawdown` | REAL | 否 | 计划最大回撤 |
| `trade_prediction_score` | REAL | 否 | 综合分 |
| `score_version` | TEXT | 是 | 评分版本 |
| `score_status` | TEXT | 是 | `ok` / `partial_*` / `insufficient_*` |
| `data_quality_status` | TEXT | 是 | 数据质量状态 |
| `daily_bar_status` | TEXT | 是 | 日线状态 |
| `intraday_bar_status` | TEXT | 是 | 分钟线状态 |
| `event_lineage_status` | TEXT | 是 | 事件时间线状态 |
| `insufficient_reason` | TEXT | 否 | 样本不足原因 |
| `recomputed_from_prediction_hash` | TEXT | 是 | 复算使用的预测 hash |
| `outcome_json` | TEXT | 是 | 明细 |
| `evaluated_at` | TEXT | 是 | 评估时间 |
| `created_at` | TEXT | 是 | 创建时间 |

建议索引：

```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_trade_prediction_outcomes_prediction_version
ON strategy_trade_prediction_outcomes(prediction_id, score_version);

CREATE INDEX IF NOT EXISTS idx_trade_prediction_outcomes_strategy
ON strategy_trade_prediction_outcomes(strategy_id, actual_trading_date DESC);

CREATE INDEX IF NOT EXISTS idx_trade_prediction_outcomes_score
ON strategy_trade_prediction_outcomes(score_status, trade_prediction_score DESC);
```

### 6.4 表三：`kline_intraday`

用途：保存可回放分钟级 K 线。

建议字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `code` | TEXT | 是 | 股票代码 |
| `period` | TEXT | 是 | `1m` / `5m` / `15m` / `30m` / `60m` |
| `time` | TEXT | 是 | bar 时间 |
| `open` | REAL | 是 | 开盘 |
| `high` | REAL | 是 | 最高 |
| `low` | REAL | 是 | 最低 |
| `close` | REAL | 是 | 收盘 |
| `volume` | INTEGER | 否 | 成交量 |
| `amount` | REAL | 否 | 成交额 |
| `source` | TEXT | 是 | 数据源 |
| `source_chain_json` | TEXT | 否 | 降级链 |
| `data_quality_status` | TEXT | 是 | 数据质量 |
| `updated_at` | TEXT | 是 | 更新时间 |

主键：

```sql
PRIMARY KEY (code, period, time)
```

建议索引：

```sql
CREATE INDEX IF NOT EXISTS idx_kline_intraday_code_period_time_desc
ON kline_intraday(code, period, time DESC);

CREATE INDEX IF NOT EXISTS idx_kline_intraday_time
ON kline_intraday(time);
```

### 6.5 存储接口建议

在 storage adapter 增加以下方法：

```python
async def save_strategy_trade_prediction(payload: Mapping[str, Any]) -> dict: ...
async def get_strategy_trade_prediction(prediction_id: str) -> dict | None: ...
async def list_strategy_trade_predictions(strategy_id: str | None = None, stock_code: str | None = None, limit: int = 200) -> list[dict]: ...

async def save_strategy_trade_prediction_outcome(payload: Mapping[str, Any]) -> dict: ...
async def list_strategy_trade_prediction_outcomes(strategy_id: str | None = None, stock_code: str | None = None, limit: int = 200) -> list[dict]: ...

async def save_intraday_klines(rows: list[Mapping[str, Any]]) -> int: ...
async def list_intraday_klines(code: str, period: str, start_ts: str, end_ts: str) -> list[dict]: ...
```

### 6.6 兼容策略

旧策略没有 `trade_prediction_contract` 时：

- 不修改旧 `prediction_contract`。
- 尝试由 `prediction_contract.claims[]`、`trade_plan`、`risk_rules` 推导简化合同。
- 推导合同标记 `prediction_status = derived_from_legacy_contract`。
- 如果无法推导 `target_trading_date`，只允许诊断观察，不允许正式晋升。

### 6.7 数据保留策略

建议默认保留：

| 数据 | 保留期 | 原因 |
|---|---:|---|
| `strategy_trade_predictions` | 长期 | 审计需要 |
| `strategy_trade_prediction_outcomes` | 长期 | 评分复盘需要 |
| `kline_intraday 1m` | 90-180 天 | 体积较大 |
| `kline_intraday 5m` | 1-2 年 | 盘中回放主力 |
| `kline_intraday 15m+` | 2 年以上 | 聚合验证 |

保留期应通过配置控制，不能硬编码路径或强制清理。

## 7. 评分体系与统计口径

### 7.1 评分设计原则

评分不是让 AI 预测“精确到某分钟某价格”，而是验证它是否输出了可执行、可回放、可统计的交易预测计划。

评分必须满足：

- 单条预测可计算。
- 多条预测可聚合。
- 样本不足可识别。
- 数据缺失可降级。
- 事件和因子可做时间线审计。
- 评分版本可升级。
- 晋升使用置信下界，不使用简单平均。

### 7.2 完整评分公式

P2 分钟线持久化和回放能力完成后，使用完整评分：

```text
trade_prediction_score_v2 =
  0.40 * direction_score
+ 0.20 * time_bucket_score
+ 0.15 * target_price_score
+ 0.15 * trade_plan_score
+ 0.05 * risk_control_score
+ 0.05 * evidence_score
```

权重解释：

| 维度 | 权重 | 说明 | 数据要求 |
|---|---:|---|---|
| `direction_score` | 40% | 次日或目标窗口方向是否正确 | 日线即可 |
| `time_bucket_score` | 20% | 预测时间段内方向是否命中 | 分钟线 |
| `target_price_score` | 15% | 是否触达目标价区间，误差多大 | 日线可兜底，分钟线更准 |
| `trade_plan_score` | 15% | 入场/出场计划是否能产生真实收益 | 分钟线优先 |
| `risk_control_score` | 5% | 止损、MAE、回撤是否符合计划 | 日线可兜底 |
| `evidence_score` | 5% | 事件/因子是否领先行情 | 事件/因子时间戳 |

### 7.3 P1 日线临时评分

P2 未完成前，不应强行计算盘中时间桶。P1 使用日线临时评分：

```text
trade_prediction_score_daily_v1 =
  0.55 * direction_score
+ 0.20 * target_price_score
+ 0.15 * trade_plan_daily_proxy_score
+ 0.05 * risk_control_score
+ 0.05 * evidence_timestamp_score
```

限制：

- `score_version = trade_prediction_score_daily_v1`
- `score_status = partial_daily_only`
- 不能作为 live-ready 晋升硬门。
- 可以用于 observe/candidate 阶段排序。
- 可以用于诊断反馈和 family 降权，但不能单独提升预算上限。

### 7.4 单项评分定义

#### 7.4.1 `direction_score`

方向命中：

```text
direction_hit = sign(predicted_direction) == sign(actual_forward_return)
```

评分：

```text
direction_score = 1.0 if direction_hit else 0.0
```

如果预测方向为 `flat`：

```text
direction_score = 1.0 if abs(actual_forward_return) <= flat_threshold else 0.0
```

建议默认：

```text
flat_threshold = 0.003
```

#### 7.4.2 `time_bucket_score`

时间桶命中：

```text
time_bucket_hit =
  sign(predicted_bucket_direction) == sign(actual_bucket_return)
```

多时间桶评分：

```text
time_bucket_score =
  weighted_mean(bucket_hit_i, weight = bucket_probability_i)
```

如果分钟线缺失：

```text
time_bucket_score = null
intraday_bar_status = missing
score_status = partial_intraday_missing
```

#### 7.4.3 `target_price_score`

目标价触达：

```text
target_touch_hit =
  actual_high_low_path touches predicted_target_range
```

日线兜底：

```text
up prediction: daily_high >= target_lower
down prediction: daily_low <= target_upper
```

价格误差：

```text
target_price_error_pct =
  distance(actual_extreme, target_range) / reference_price
```

评分建议：

```text
target_price_score =
  1.0 if target_touch_hit
  else max(0.0, 1.0 - target_price_error_pct / tolerance_pct)
```

建议默认：

```text
tolerance_pct = 0.03
```

#### 7.4.4 `trade_plan_score`

交易计划分由 entry、exit 和计划收益组成：

```text
trade_plan_score =
  0.35 * entry_score
+ 0.35 * exit_score
+ 0.30 * planned_return_score
```

P1 日线代理：

```text
trade_plan_daily_proxy_score =
  0.50 * target_price_score
+ 0.30 * risk_control_score
+ 0.20 * direction_score
```

P2 完整 entry 分：

```text
entry_score =
  1.0 if entry_window_hit and entry_slippage <= max_allowed_slippage
  else degraded score based on slippage and missed window
```

P2 完整 exit 分：

```text
exit_score =
  profit_capture_ratio adjusted by drawdown_avoided_after_exit
```

#### 7.4.5 `risk_control_score`

风险控制分：

```text
risk_control_score =
  1.0 if mae <= risk_plan.max_adverse_excursion_pct and stop_loss_not_breached
  else degraded score
```

如果触发止损但计划正确执行：

```text
risk_control_score can remain positive
planned_trade_return may be negative
```

方向错但风险受控时不能让综合分过高，因此建议：

```text
if direction_score == 0 and planned_trade_return < 0:
  risk_control_score = min(risk_control_score, 0.5)
```

#### 7.4.6 `evidence_score`

证据分只验证“领先性”和“贡献”，不是验证文案是否合理。

```text
evidence_score =
  0.40 * evidence_timestamp_score
+ 0.30 * event_direction_score
+ 0.30 * factor_contribution_score
```

如果证据时间晚于预测时间：

```text
evidence_score = null
event_lineage_status = post_hoc_rejected
```

### 7.5 结果状态定义

`score_status` 必须显式记录：

| 状态 | 含义 | 能否晋升使用 |
|---|---|---:|
| `ok` | 数据完整，评分可用于晋升 | 是 |
| `partial_daily_only` | 只有日线验证，无分钟线 | 否 |
| `partial_intraday_missing` | 需要分钟线但缺失 | 否 |
| `insufficient_samples` | 样本不足 | 否 |
| `post_hoc_evidence_rejected` | 证据存在事后解释 | 否 |
| `data_quality_failed` | 行情或证据质量失败 | 否 |
| `prediction_mutated_rejected` | 预测 hash 不一致 | 否 |
| `market_suspended` | 停牌或不可交易 | 否，可诊断 |
| `limit_untradable` | 涨跌停不可按计划交易 | 否，可诊断 |

### 7.6 聚合评分

晋升和预算反馈不使用单条预测分，也不使用简单平均。应使用置信下界。

建议聚合指标：

```text
trade_prediction_pass = trade_prediction_score >= 0.60
trade_prediction_pass_rate = mean(trade_prediction_pass)
trade_prediction_score_mean = mean(trade_prediction_score)
trade_prediction_score_median = median(trade_prediction_score)
trade_prediction_score_lcb = bootstrap_lcb(trade_prediction_score)
trade_prediction_pass_lcb = wilson_lcb(trade_prediction_pass_rate, n)
```

晋升优先使用：

```text
trade_prediction_pass_lcb
planned_trade_return_median
direction_skill_lcb
effective_n
score_status coverage
```

### 7.7 样本门槛

默认门槛建议：

| 指标 | 最小样本 |
|---|---:|
| 单策略晋升 | 30 |
| 单策略诊断 | 5 |
| strategy_type 矩阵 cell | 5 |
| event_type 贡献 | 20 |
| factor_type 贡献 | 30 |
| intraday bucket | 30 |
| target_pool 预算提升 | 50 |

样本不足输出：

```text
score_status = insufficient_samples
insufficient_reason = "effective_n:12<30"
```

### 7.8 分组维度

所有聚合至少支持：

- `strategy_id`
- `strategy_type`
- `stock_code`
- `target_pool`
- `holding_bucket`
- `trend_regime`
- `vol_regime`
- `sentiment_regime`
- `event_type`
- `factor_type`
- `prediction_horizon`
- `score_version`

### 7.9 数据缺失处理

不允许用 fallback 数据伪造命中。

规则：

- 日线缺失：不计算 P1 核心评分。
- 分钟线缺失：不计算 P2 指标。
- 事件时间缺失：不计算 evidence_score。
- 因子快照时间缺失：不计算 factor_contribution_score。
- 停牌：从交易计划收益统计中剔除，但记录为 `market_suspended`。
- 涨跌停不可成交：记录为 `limit_untradable`，不能按理想成交价计算收益。

## 8. 策略工厂改造

### 8.1 改造目标

策略工厂的目标不是只生成策略 DSL，而是生成可验证交易预测计划。

改造后每个正式孵化 candidate 至少应包含：

- 现有 `evidence_chain`。
- 现有 `prediction_contract`。
- 现有 `confidence_contract`。
- 现有 `trade_plan`。
- 现有 `dsl`。
- 新增 `trade_prediction_contract`。

### 8.2 不改变的现有能力

以下能力必须保留：

- `evidence_chain -> prediction_contract -> trade_plan -> dsl` 生成顺序。
- `prediction_contract.claims[]` 引用 `evidence_ids`。
- 每条 claim 必须有 `failure_condition`。
- trade plan 节点必须可追溯到 claim。
- DSL metadata 必须保留 target_symbols、stock_pool、validation_profile。
- 提交门和质量门继续先判断基础合同完整性。

### 8.3 新增合同生成器

建议新增模块：

```text
packages/strategy-factory/src/strategy_factory/application/trade_prediction_contract.py
```

职责：

- 从 LLM candidate 读取 `trade_prediction_contract`。
- 从旧 `prediction_contract` 和 `trade_plan` 推导简化合同。
- 规范化字段。
- 校验时间线。
- 生成 `prediction_id`。
- 生成 canonical JSON。
- 生成冻结 hash。
- 输出 contract diagnostics。

建议函数：

```python
def normalize_trade_prediction_contract(candidate: Mapping[str, Any]) -> dict[str, Any]: ...
def derive_trade_prediction_contract(candidate: Mapping[str, Any]) -> dict[str, Any]: ...
def validate_trade_prediction_contract(contract: Mapping[str, Any]) -> dict[str, Any]: ...
def freeze_trade_prediction_contract(contract: Mapping[str, Any]) -> dict[str, Any]: ...
```

### 8.4 LLM Prompt 改造

Prompt 新增要求：

- 必须输出 `trade_prediction_contract`。
- 必须明确 `target_trading_date` 或 `prediction_horizon`。
- 必须给出 `direction` 和 `direction_probability`。
- 必须给出 entry/exit/risk plan。
- 如果输出 time buckets，必须给出 `window`、`direction`、`target_price_range`、`probability`。
- 事件和因子只能通过 ID 或结构化引用进入 `evidence_refs`。
- 禁止只输出“建议买入”“看涨”“偏强”等不可验证结论。

Prompt 中建议加入硬约束：

```text
trade_prediction_contract 是机器验证字段，不允许自由文本替代。
若无法给出 time_buckets，可将 time_buckets 置空，但必须说明 requires_intraday_bar=false。
若无目标价区间，不能进入正式孵化，只能进入 diagnostic observation。
```

### 8.5 Candidate 规范化

生成器后处理应执行：

```text
candidate
  -> normalize semantic contracts
  -> normalize trade_prediction_contract
  -> audit evidence refs
  -> audit prediction time line
  -> attach contract diagnostics
  -> quality/submission gate
```

新增诊断字段建议：

- `trade_prediction_contract_status`
- `trade_prediction_contract_missing_fields`
- `trade_prediction_contract_reject_reasons`
- `trade_prediction_contract_score_readiness`
- `trade_prediction_contract_hash`

### 8.6 提交门改造

进入正式孵化前必须满足：

- `trade_prediction_contract_status == ready`
- `prediction_as_of` 合法。
- `target_trading_date` 合法。
- `direction_probability` 合法。
- `risk_plan` 完整。
- `evidence_refs` 至少引用一个 evidence 或 claim。
- `frozen_payload_hash` 存在。

进入诊断观察的降级条件：

- 只能推导日线方向。
- 无 time buckets。
- 无目标价，但有方向和风险计划。
- 旧策略合同只能映射到 `derived_from_legacy_contract`。

禁止进入孵化：

- 无方向。
- 无目标交易日。
- 预测时间晚于验证窗口。
- evidence refs 为空。
- 入场/出场计划不可解析且没有兼容降级。

### 8.7 Stock-First Router 改造

当前 Router 已能从个股画像路由到 family 和 holding bucket。新增评分维度：

```text
direction_predictability_weight = 0.40
intraday_timing_weight = 0.20
target_price_weight = 0.15
trade_plan_weight = 0.15
risk_control_weight = 0.05
event_factor_evidence_weight = 0.05
```

Router 不应直接预测价格，而应输出：

- 该股票适合哪些 strategy family。
- 适合短/中/长哪个 holding bucket。
- 是否适合生成 time buckets。
- 是否需要事件催化。
- 是否需要分钟线验证。
- 不适合哪些策略类型。

新增 Router 输出建议：

```json
{
  "families": ["momentum", "event_structure_breakout"],
  "holding_period_bucket": "short",
  "prediction_verifiability": {
    "direction_predictability": 0.72,
    "intraday_timing_feasible": true,
    "target_price_feasible": true,
    "event_factor_evidence_available": true,
    "requires_intraday_bar": true
  },
  "exclusions": ["value_factor"],
  "rationale": ["trend_up", "event_catalyst", "volume_expansion"]
}
```

### 8.8 策略工厂输出验收

P0 验收：

- 新 candidate 包含 `trade_prediction_contract`。
- 旧 candidate 可生成兼容合同或被标记为诊断观察。
- 缺必填字段的 candidate 被提交门拒绝。
- 合同 hash 稳定，字段顺序变化不影响 hash。
- 不破坏现有 semantic contract 单测。

P1 验收：

- 每个进入孵化的策略至少保存一条 `strategy_trade_predictions`。
- `prediction_id` 可追溯到 strategy/candidate。
- 预测可被孵化工厂读取并生成 outcome。

## 9. 孵化工厂改造

### 9.1 改造目标

孵化工厂从“验证策略信号前向收益”扩展为：

```text
验证冻结交易预测在真实行情中的方向、价格、时间、买卖计划、风险和证据质量。
```

现有 `ForwardVerifier` 不应被替换，而应分层扩展：

```text
ForwardVerifier
  -> daily forward return verification
TradePredictionOutcomeVerifier
  -> prediction-level daily/intraday/event/factor verification
```

### 9.2 新增 Outcome Verifier

建议新增模块：

```text
packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/trade_prediction_verifier.py
```

职责：

- 读取 frozen predictions。
- 判断是否到达可验证时间。
- 加载日线/分钟线/事件/因子数据。
- 计算单条 outcome。
- 写入 `strategy_trade_prediction_outcomes`。
- 输出数据缺口诊断。

建议类：

```python
class TradePredictionOutcomeVerifier:
    async def verify_pending(self, db: Any, *, limit: int = 200) -> dict[str, Any]: ...
    async def verify_prediction(self, db: Any, prediction: dict[str, Any]) -> dict[str, Any]: ...
```

### 9.3 Daily Outcome 验证

P1 只做日线验证，计算：

- `actual_open`
- `actual_high`
- `actual_low`
- `actual_close`
- `actual_return`
- `direction_hit`
- `target_touch_hit`
- `target_price_error_pct`
- `risk_control_score`
- `trade_prediction_score_daily_v1`

日线验证输入：

- `strategy_trade_predictions`
- `kline_1d`
- `signal_forward_returns`

日线验证输出：

- `strategy_trade_prediction_outcomes`
- MetricsRecorder 新增字段。
- HitRateReporter 新增聚合字段。

### 9.4 Intraday Outcome 验证

P2 完成后增加：

- `actual_bucket_return`
- `time_bucket_hit`
- `time_bucket_score`
- `entry_window_hit`
- `entry_price_slippage_pct`
- `exit_window_hit`
- `profit_capture_ratio`
- `drawdown_avoided_after_exit`
- `planned_trade_return`
- `planned_trade_max_drawdown`
- `planned_trade_r_multiple`

分钟线验证输入：

- `strategy_trade_predictions.time_buckets_json`
- `entry_plan_json`
- `exit_plan_json`
- `risk_plan_json`
- `kline_intraday`

### 9.5 MetricsRecorder 改造

新增每日指标字段建议：

- `direction_hit_rate`
- `direction_skill_lcb`
- `trade_prediction_score_mean`
- `trade_prediction_score_median`
- `trade_prediction_pass_rate`
- `trade_prediction_pass_lcb`
- `target_touch_rate`
- `time_bucket_hit_rate`
- `entry_quality_score`
- `exit_quality_score`
- `planned_trade_return_median`
- `prediction_effective_n`
- `score_status_breakdown`

兼容策略：

- 保留原 `hit_rate_5d`、`skill_lcb_5d` 等字段。
- 新字段优先放入 `metadata`，待 schema 稳定后再拆列。
- 晋升门使用新字段前必须确认 `prediction_effective_n` 达标。

### 9.6 HitRateReporter 改造

新增报告维度：

- 按 strategy family。
- 按 holding bucket。
- 按 stock_code。
- 按 target_pool。
- 按 trend/vol/sentiment regime。
- 按 event_type。
- 按 factor_type。
- 按 score_version。
- 按 score_status。

报告输出建议：

```json
{
  "trade_prediction": {
    "overall": {},
    "by_family": {},
    "by_holding_bucket": {},
    "by_regime": {},
    "by_event_type": {},
    "by_factor_type": {},
    "score_status_breakdown": {}
  }
}
```

### 9.7 孵化流水线改造

Pipeline 阶段应增加预测验证检查：

| 阶段 | 新增检查 |
|---|---|
| `candidate` | 是否存在 frozen prediction |
| `observe` | 是否有 P1 outcome |
| `ready_for_review` | score_status 是否可用 |
| `graduation_ready` | score/pass LCB 是否达标 |
| `promoted` | P2/P3 数据是否满足策略类型要求 |

P2 前：

- 不能因为缺 time bucket 阻断普通日线策略观察。
- 但不能把缺 time bucket 的策略标为盘中预测能力已验证。

### 9.8 孵化工厂验收

P1 验收：

- 能读取 pending predictions。
- 能基于日线生成 outcome。
- 能输出 `score_status = partial_daily_only`。
- 能写入 outcome 表。
- 不影响原 ForwardVerifier 输出。

P2 验收：

- 能基于 `kline_intraday` 计算 time bucket、entry、exit。
- 分钟线缺口显式记录。
- 同一 prediction 可按 score_version 复算。

## 10. 因子挖掘工厂改造

### 10.1 改造目标

因子挖掘不再只关注中长期 IC，还要支持交易预测评分中的短周期贡献。

新增目标：

- 次日方向 IC。
- 盘中 bucket 方向 IC。
- 事件条件下 IC。
- regime 条件下 IC。
- 对 `trade_prediction_score` 的边际贡献。

### 10.2 因子货架扩展

`factor_catalog.py` 建议增加字段：

| 字段 | 说明 |
|---|---|
| `short_horizon_supported` | 是否支持短周期 |
| `supported_horizons` | `1d`、`5d`、`30m`、`am`、`pm` |
| `latest_short_ic` | 最新短周期 IC |
| `latest_direction_hit_rate` | 最新方向命中率 |
| `event_conditioned_ic` | 事件条件 IC |
| `regime_conditioned_ic` | regime 条件 IC |
| `prediction_score_contribution` | 对预测评分贡献 |
| `decay_status` | active / decaying / quarantine |

### 10.3 因子验证指标

新增验证输出：

```text
factor_short_horizon_ic
factor_direction_hit_rate
factor_bucket_ic
factor_event_conditioned_ic
factor_regime_conditioned_ic
factor_prediction_score_delta
```

### 10.4 因子入池门槛

短周期因子进入 active pool 必须满足：

- 样本数达标。
- `factor_short_horizon_ic` 不为负。
- 在至少一个 regime 中有效。
- 最近衰减不过快。
- 没有在 prediction outcomes 中显示负贡献。

建议规则：

```text
if factor_prediction_score_delta < -0.02 and effective_n >= 30:
  quarantine_or_decay
```

### 10.5 因子反馈闭环

孵化工厂应向因子工厂反馈：

- 哪些因子提高 direction_score。
- 哪些因子提高 time_bucket_score。
- 哪些因子提高 target_price_score。
- 哪些因子只在某 regime 有效。
- 哪些因子在前向验证中失效。

反馈事件建议：

```text
factor_prediction_feedback.generated
factor_prediction_feedback.decay_recommended
factor_prediction_feedback.quarantine_recommended
```

## 11. 事件驱动证据改造

### 11.1 统一事件结构

事件证据建议统一为：

```json
{
  "event_id": "event_xxx",
  "stock_code": "000001",
  "event_type": "news",
  "event_time": "2026-06-05T10:18:00+08:00",
  "ingested_at": "2026-06-05T10:19:10+08:00",
  "source": "news_provider",
  "headline": "...",
  "summary": "...",
  "sentiment": "positive",
  "relevance": 0.82,
  "expected_direction": "up",
  "expected_horizon": "intraday",
  "linked_prediction_ids": []
}
```

### 11.2 时间线约束

有效事件必须满足：

```text
event_time <= ingested_at <= prediction_as_of < validation_window_start
```

如果 `event_time` 缺失：

```text
event_lineage_status = event_time_missing
evidence_score = null
```

如果事件在预测之后才进入：

```text
event_lineage_status = post_prediction_ingest
evidence_score = null
```

如果事件在行情之后才补录：

```text
event_lineage_status = post_hoc_rejected
evidence_score = null
```

### 11.3 事件验证指标

新增指标：

- `event_lead_time_minutes`
- `event_direction_hit_5m`
- `event_direction_hit_15m`
- `event_direction_hit_30m`
- `event_direction_hit_60m`
- `event_next_day_direction_hit`
- `event_relevance_score`
- `event_decay_minutes`
- `event_absorbed_before_prediction`

### 11.4 事件聚合

按以下维度统计：

- `event_type`
- `source`
- `sentiment`
- `relevance_bucket`
- `expected_horizon`
- `stock_code`
- `strategy_type`
- `regime`

### 11.5 事件反馈

有效事件类型：

- 提高 Router 事件催化权重。
- 提高 LLM Prompt 中同类事件优先级。
- 进入策略工厂 evidence context。

无效事件类型：

- 降低权重。
- 标记为 weak evidence。
- 如果持续负贡献，加入 event type cooldown。

## 12. 分钟线与数据质量改造

### 12.1 改造目标

P2 的核心不是“能调用分钟线接口”，而是“能稳定复盘某个历史目标交易日的分钟路径”。

必须具备：

- 分钟线持久化。
- 分钟线缺口检测。
- 多源 fallback 记录。
- 回放窗口查询。
- 对停牌、涨跌停、半日交易、缺 bar 的处理。

### 12.2 数据源优先级

建议沿用当前数据源策略：

```text
local/TDX if available
  -> online TDX
  -> AkShare
  -> Sina
  -> Tencent
```

但必须遵守：

- `TDX_LOCAL_ONLY=1` 时不能强制在线 fallback。
- 不能硬编码 DB 路径。
- 数据源链必须写入 `source_chain_json`。

### 12.3 分钟线质量检查

每批分钟线保存前检查：

- 时间格式可解析。
- OHLC 均大于 0。
- `low <= open <= high`。
- `low <= close <= high`。
- volume 非负。
- 同一 `(code, period, time)` 去重。
- 交易时段内 bar 数大致合理。
- bar 时间不晚于当前时间。

质量状态：

- `ok`
- `partial_gap`
- `invalid_ohlc`
- `duplicate_dropped`
- `source_empty`
- `source_failed`
- `stale_snapshot`

### 12.4 回放服务

建议新增：

```text
packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/intraday_replay.py
```

职责：

- 查询 `kline_intraday`。
- 检查窗口覆盖。
- 对齐 time buckets。
- 计算 bucket return。
- 计算 entry/exit price。
- 计算 MFE/MAE。
- 返回 replay diagnostics。

建议接口：

```python
class IntradayReplayService:
    async def load_bars(self, db, code: str, date: str, period: str) -> dict: ...
    def evaluate_time_buckets(self, bars: list[dict], buckets: list[dict]) -> dict: ...
    def evaluate_entry_exit(self, bars: list[dict], entry_plan: dict, exit_plan: dict, risk_plan: dict) -> dict: ...
```

### 12.5 缺口处理

如果分钟线缺口影响验证窗口：

```text
intraday_bar_status = partial_gap
time_bucket_score = null
entry_score = null
exit_score = null
score_status = partial_intraday_missing
```

如果只是非关键窗口缺口：

```text
intraday_bar_status = partial_noncritical_gap
score_status may remain ok
outcome_json.records gap details
```

### 12.6 数据层验收

P2 数据层验收：

- 能保存和读取 `kline_intraday`。
- 同一 code/date/period 查询稳定。
- 缺口状态可复现。
- 多源 fallback 可审计。
- 不触发大规模在线同步作为单测前置。

## 13. 晋升门、预算反馈和因子衰减

### 13.1 改造目标

晋升门不再只看 T+N hit rate 和 forward Sharpe，而是增加交易预测质量。

但接入顺序必须保守：

```text
P1: trade_prediction_score_daily_v1 只做观察和诊断
P2: trade_prediction_score_v2 可进入晋升候选
P3: event/factor contribution 可进入预算和因子衰减
P4: score_lcb 可作为正式晋升硬门
```

### 13.2 Promotion Gate 新条件

新增晋升条件建议：

```text
direction_skill_lcb > 0
trade_prediction_pass_lcb >= 0.05
trade_prediction_score_median >= 0.60
planned_trade_return_median > 0
effective_n >= 30
score_status_ok_ratio >= 0.80
max_drawdown within risk_plan
promotion_gate_dsr_passed == true
```

说明：

- `trade_prediction_pass_lcb` 是 pass rate 的 Wilson 下界减去基准，不是平均分。
- `score_status_ok_ratio` 防止大量缺数据策略被错误晋升。
- P2 未完成前不要求 `time_bucket_score`，但也不能宣称盘中能力。

### 13.3 HitRateReporter 新增输出

报告新增：

```json
{
  "trade_prediction_summary": {
    "effective_n": 0,
    "score_mean": 0.0,
    "score_median": 0.0,
    "pass_rate": 0.0,
    "pass_lcb": 0.0,
    "planned_trade_return_median": 0.0,
    "score_status_breakdown": {}
  },
  "trade_prediction_by_family": {},
  "trade_prediction_by_regime": {},
  "trade_prediction_by_event_type": {},
  "trade_prediction_by_factor_type": {}
}
```

### 13.4 FeedbackWriter 新增动作

新增 feedback actions：

- `families_to_boost_by_prediction_score`
- `families_to_cool_by_prediction_score`
- `families_to_freeze_by_prediction_score`
- `target_pools_to_cool_by_prediction_score`
- `factors_to_decay`
- `factors_to_quarantine`
- `event_types_to_downweight`
- `event_types_to_boost`

### 13.5 策略工厂预算反馈接入

策略工厂读取预算反馈后新增控制：

```text
if family.trade_prediction_pass_lcb > threshold and effective_n >= threshold:
  increase skill_budget_multiplier

if family.trade_prediction_pass_lcb < 0 and effective_n >= threshold:
  cooldown or freeze family

if target_pool score_status_ok_ratio too low:
  suppress target_pool until data quality improves
```

### 13.6 因子衰减接入

因子衰减规则：

```text
if factor_prediction_score_delta < -0.02 and effective_n >= 30:
  mark factor as decay_candidate

if factor_prediction_score_delta < -0.05 and effective_n >= 50:
  quarantine factor

if factor only works in specific regime:
  keep factor active with regime constraint
```

### 13.7 晋升和反馈验收

P4 验收：

- Promotion Gate 能读取新评分聚合。
- 样本不足时不晋升。
- `partial_daily_only` 不能作为 live-ready 硬门。
- 低分 family 能产生 cooldown/freeze feedback。
- 高分 family 能产生 boost feedback。
- 失效因子能进入 decay/quarantine 建议。

## 14. Agent/Desktop 可观测

### 14.1 边界原则

Agent 和 Desktop 只做可观测和只读操作。

必须遵守：

- Agent 模型可见工具名必须以 `agent_` 开头。
- 不暴露 raw `strategy_manager`。
- 不暴露 live trading manager。
- Desktop 只调用 Agent HTTP API。
- 不因查看预测评分触发交易动作。

### 14.2 Agent 只读能力建议

建议新增只读能力：

| Agent 工具 | 用途 |
|---|---|
| `agent_trade_prediction_status` | 查看预测验证总体状态 |
| `agent_trade_prediction_outcomes` | 查询预测 outcome |
| `agent_trade_prediction_matrix` | 查询 score/hit matrix |
| `agent_intraday_data_quality` | 查询分钟线覆盖和缺口 |
| `agent_prediction_feedback_summary` | 查询预算/因子反馈摘要 |

### 14.3 Desktop 展示建议

Desktop 新增或扩展面板：

- 策略预测合同列表。
- 单条预测详情。
- 方向/目标价/时间桶/买卖计划评分。
- 数据缺口和 `score_status`。
- 按 family/regime/event/factor 的命中矩阵。
- 晋升门解释。
- 预算反馈动作。
- 因子衰减建议。

### 14.4 可观测字段

单条预测展示：

- `prediction_id`
- `strategy_id`
- `stock_code`
- `prediction_as_of`
- `target_trading_date`
- `direction`
- `direction_probability`
- `trade_prediction_score`
- `score_status`
- `data_quality_status`
- `outcome_summary`

聚合展示：

- `effective_n`
- `score_mean`
- `score_median`
- `pass_rate`
- `pass_lcb`
- `score_status_breakdown`
- `top_positive_factors`
- `top_negative_factors`
- `event_type_rank`

### 14.5 P5 验收

- Agent 工具通过命名策略。
- Desktop 不直接调用 MCP。
- 查询预测不触发 manager state mutation。
- 数据缺口和样本不足在 UI 中可见。
- 晋升原因和阻断原因可解释。

## 15. P0-P5 实施路线

### 15.1 P0：合同、冻结、存储与兼容层

目标：

- 定义 `TradePredictionContract`。
- 定义 `TradePredictionOutcome`。
- 新增 prediction/outcome schema。
- 新增预测冻结 hash。
- 新增旧合同兼容推导。
- 策略工厂能输出或推导 `trade_prediction_contract`。
- 孵化工厂能读取 prediction 并生成 empty outcome。

任务拆解：

| 任务 | 包 | 说明 |
|---|---|---|
| P0-1 类型定义 | `strategy-factory` | DTO/contract 类型 |
| P0-2 合同规范化器 | `strategy-factory` | normalize/validate/freeze |
| P0-3 Prompt 扩展 | `akshare-mcp` | LLM 输出合同 |
| P0-4 兼容推导 | `strategy-factory` | 旧合同转新合同 |
| P0-5 schema | `aiask-quant-core` | 新增两张表 |
| P0-6 storage API | `aiask-quant-core` | save/list/get 方法 |
| P0-7 孵化读取 | `akshare-mcp` | 读取 pending prediction |
| P0-8 单测 | 跨包 | schema、hash、验证 |

验收：

- 新 candidate 可生成 frozen prediction。
- 旧策略可降级生成 derived contract。
- 缺必填字段被拒绝。
- prediction hash 复算稳定。
- 新表 round-trip 通过。

### 15.2 P1：次日方向、目标价和日线风险验证

目标：

- 基于日线验证方向。
- 基于日线 high/low 验证目标价触达。
- 基于日线 low/high 验证风险兜底。
- 生成 `trade_prediction_score_daily_v1`。
- 写入 outcome。
- 接入 MetricsRecorder 和 HitRateReporter 诊断输出。

任务拆解：

| 任务 | 包 | 说明 |
|---|---|---|
| P1-1 Daily verifier | `akshare-mcp` | 日线 outcome 计算 |
| P1-2 Outcome writer | `akshare-mcp`/`aiask-quant-core` | 写结果 |
| P1-3 Score daily v1 | `akshare-mcp` | 临时评分 |
| P1-4 MetricsRecorder | `akshare-mcp` | 新指标 metadata |
| P1-5 HitRateReporter | `akshare-mcp` | 新 summary |
| P1-6 Pipeline 诊断 | `akshare-mcp` | observe 阶段可见 |
| P1-7 单测 | 跨包 | 日线验证 |

验收：

- 预测到期后可生成 outcome。
- `direction_hit` 正确。
- `target_touch_hit` 正确。
- `score_status = partial_daily_only`。
- 不影响旧 T+N 指标。

### 15.3 P2：分钟线持久化与盘中回放

目标：

- 新增 `kline_intraday`。
- 持久化分钟线。
- 建立 `IntradayReplayService`。
- 计算 time bucket、entry、exit、planned return。
- 生成 `trade_prediction_score_v2`。

任务拆解：

| 任务 | 包 | 说明 |
|---|---|---|
| P2-1 intraday schema | `aiask-quant-core` | 新增表和索引 |
| P2-2 save/list API | `aiask-quant-core` | 存取分钟线 |
| P2-3 kline tool bridge | `akshare-mcp` | 获取结果可落库 |
| P2-4 data quality | `akshare-mcp` | 缺口和 OHLC 校验 |
| P2-5 replay service | `akshare-mcp` | bucket/entry/exit |
| P2-6 score v2 | `akshare-mcp` | 完整评分 |
| P2-7 单测 | 跨包 | 分钟线和回放 |

验收：

- 可查询指定 code/date/period 的分钟 bars。
- 缺口状态可见。
- time bucket 命中可复算。
- entry/exit 分可复算。
- `score_status = ok` 只在数据完整时出现。

### 15.4 P3：事件与因子领先性

目标：

- 事件统一结构。
- 因子短周期贡献。
- 事件/因子时间线审计。
- 按 event/factor/regime 聚合。
- 反馈到 Router、Prompt 和因子池。

任务拆解：

| 任务 | 包 | 说明 |
|---|---|---|
| P3-1 event schema normalization | `akshare-mcp`/`aiask-quant-core` | 统一事件结构 |
| P3-2 event lineage verifier | `akshare-mcp` | 时间线审计 |
| P3-3 factor short IC | `akshare-mcp` | 短周期 IC |
| P3-4 factor catalog fields | `akshare-mcp` | 增加短周期元数据 |
| P3-5 contribution scoring | `akshare-mcp` | evidence_score |
| P3-6 matrix extension | `akshare-mcp` | event/factor 聚合 |
| P3-7 单测 | 跨包 | 时间线和贡献 |

验收：

- 事后事件不能计分。
- 因子快照晚于预测不能计分。
- 事件后 5/15/30/60 分钟收益可验证。
- 短周期因子贡献可聚合。

### 15.5 P4：晋升门和预算反馈

目标：

- 新评分进入 Promotion Gate。
- 低分 family 降权。
- 高分 family 加预算。
- 失效因子衰减或隔离。
- event type 权重调整。

任务拆解：

| 任务 | 包 | 说明 |
|---|---|---|
| P4-1 score aggregate | `akshare-mcp` | score_lcb/pass_lcb |
| P4-2 Promotion Gate | `akshare-mcp` | 新门槛 |
| P4-3 HitRateReporter feedback | `akshare-mcp` | 新反馈 |
| P4-4 FeedbackWriter | `akshare-mcp` | 写事件/控制 |
| P4-5 Budget feedback | `strategy-factory` | 读取新指标 |
| P4-6 Factor decay | `akshare-mcp` | 因子衰减 |
| P4-7 单测 | 跨包 | 晋升和反馈 |

验收：

- 样本不足不能晋升。
- `partial_daily_only` 不能 live-ready。
- 低分 family 进入 cooldown/freeze。
- 高分 family 可 boost。
- 因子负贡献可触发 decay/quarantine。

### 15.6 P5：Agent/Desktop 可观测

目标：

- Agent 暴露只读预测能力。
- Desktop 展示预测合同、结果、矩阵、缺口、反馈。
- 保持所有安全边界。

任务拆解：

| 任务 | 包 | 说明 |
|---|---|---|
| P5-1 Agent read APIs | `agent` | `agent_*` 工具 |
| P5-2 HTTP routes | `agent` | Desktop API |
| P5-3 Desktop types | `desktop` | 类型定义 |
| P5-4 Prediction panel | `desktop` | 预测和结果展示 |
| P5-5 Matrix panel | `desktop` | 聚合矩阵 |
| P5-6 Tests | `agent`/`desktop` | 边界和渲染 |

验收：

- Agent 工具名合规。
- Desktop 只走 Agent HTTP。
- 不触发交易动作。
- 阻断原因、数据缺口、样本不足可见。

## 16. 验收标准和测试矩阵

### 16.1 总体验收原则

每个阶段必须同时满足：

- 单元测试覆盖核心纯函数。
- storage round-trip 覆盖新增表和读写接口。
- 不破坏现有语义合同链。
- 不破坏现有 T+N 前向收益指标。
- 数据缺失时输出明确状态。
- 样本不足时不输出晋升结论。
- 所有新评分都带 `score_version`。

### 16.2 P0 测试矩阵

| 测试项 | 类型 | 关键断言 |
|---|---|---|
| 合同最小字段校验 | unit | 缺 `target_trading_date` 被拒绝 |
| 方向字段校验 | unit | 非 `up/down/flat` 被拒绝 |
| 时间线校验 | unit | `prediction_as_of` 晚于开盘被拒绝 |
| 价格区间校验 | unit | lower > upper 被拒绝 |
| evidence refs 校验 | unit | 空 refs 不能正式孵化 |
| canonical JSON | unit | 字段顺序不同 hash 一致 |
| freeze hash | unit | 修改合同后 hash 不一致 |
| legacy derive | unit | 旧 `prediction_contract` 可生成 derived 合同 |
| prediction table round-trip | storage | 保存和读取内容一致 |
| outcome empty row | storage | 可保存 pending/empty outcome |

### 16.3 P1 测试矩阵

| 测试项 | 类型 | 关键断言 |
|---|---|---|
| 日线方向命中 | unit | 上涨预测遇正收益命中 |
| 日线方向失败 | unit | 上涨预测遇负收益失败 |
| flat 方向 | unit | 小波动按 flat threshold 命中 |
| 目标价触达 | unit | high/low 触达区间 |
| 目标价误差 | unit | 未触达时误差分递减 |
| 风险控制 | unit | MAE 超阈值降分 |
| daily score | unit | `score_version = daily_v1` |
| 日线缺失 | unit | `data_quality_status = daily_bar_missing` |
| outcome 写入 | integration | prediction 到 outcome 全链路 |
| 不破坏 ForwardVerifier | regression | 原 `primary_hit_rate` 等仍存在 |

### 16.4 P2 测试矩阵

| 测试项 | 类型 | 关键断言 |
|---|---|---|
| `kline_intraday` schema | storage | 主键和索引存在 |
| 分钟线保存 | storage | 同 code/period/time upsert |
| 分钟线读取 | storage | 按窗口返回排序 bars |
| OHLC 校验 | unit | invalid OHLC 被拒绝或标记 |
| 缺口检测 | unit | 少关键 bar 标记 `partial_gap` |
| time bucket 命中 | unit | bucket return 方向正确 |
| entry window | unit | 窗口内可成交价正确 |
| exit window | unit | 止盈/止损/time stop 顺序正确 |
| planned return | unit | 收益和 R multiple 正确 |
| v2 score | unit | 数据完整时 `score_status = ok` |

### 16.5 P3 测试矩阵

| 测试项 | 类型 | 关键断言 |
|---|---|---|
| 事件时间线合法 | unit | `event_time <= prediction_as_of` 可计分 |
| 事件事后写入 | unit | `post_hoc_rejected` |
| 事件时间缺失 | unit | evidence score 为 null |
| 事件后收益 | unit | 5/15/30/60 分钟收益正确 |
| 因子快照合法 | unit | snapshot 早于预测可计分 |
| 因子快照晚于预测 | unit | factor score 为 null |
| event type 聚合 | unit | 按 event_type 输出矩阵 |
| factor type 聚合 | unit | 按 factor_type 输出矩阵 |
| regime conditioned IC | unit | 分 regime 输出 |
| 样本不足 | unit | `insufficient_samples` |

### 16.6 P4 测试矩阵

| 测试项 | 类型 | 关键断言 |
|---|---|---|
| score LCB | unit | Wilson/Bootstrap 下界正确 |
| 晋升通过 | unit | 样本充足且分数达标 |
| 样本不足阻断 | unit | 不晋升 |
| partial 状态阻断 | unit | `partial_daily_only` 不 live-ready |
| family cooldown | unit | 低分 family 输出 cooldown |
| family boost | unit | 高分 family 输出 boost |
| factor decay | unit | 负贡献因子输出 decay |
| feedback write | integration | 反馈事件写入 |
| budget read | integration | 策略工厂读取新反馈 |
| 不破坏旧反馈 | regression | 旧 `skill_budget_multiplier` 仍可用 |

### 16.7 P5 测试矩阵

| 测试项 | 类型 | 关键断言 |
|---|---|---|
| Agent 工具命名 | unit | 必须 `agent_*` |
| 禁止 raw manager | unit | 不暴露 `strategy_manager` |
| HTTP 只读 | integration | 查询不写状态 |
| Desktop API | frontend | 只调用 Agent HTTP |
| Prediction panel | frontend | 展示 score/status |
| Matrix panel | frontend | 展示分组矩阵 |
| 缺口展示 | frontend | 数据缺口可见 |
| 样本不足展示 | frontend | insufficient reason 可见 |
| 安全回归 | integration | 不触发交易动作 |

### 16.8 最小验证命令建议

根据实际实现文件选择最小测试集。建议阶段性命令：

```powershell
python -m pytest packages/strategy-factory/tests -q
python -m pytest packages/akshare-mcp/tests -q
python -m pytest packages/aiask-quant-core/tests -q
```

P5 涉及 Desktop 时：

```powershell
cd desktop
npm test -- --run
```

如果只改文档，不需要运行测试。

## 17. 风险、边界和禁止事项

### 17.1 最大风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 把 LLM 输出当准确率 | 产生虚假能力 | 只用冻结预测的真实 outcome 评分 |
| 分钟线缺失 | 盘中评分失真 | P2 前不把 time bucket 作为硬门 |
| 事后事件解释 | evidence 分虚高 | 严格时间线审计 |
| 样本不足 | 命中率不稳定 | LCB 和 min_n 门槛 |
| 修改现有 `prediction_contract` | 破坏语义链 | 新增 sibling 字段 |
| 直接接入晋升门 | 误晋升 | P1 先诊断，P4 再硬门 |
| 数据源不稳定 | outcome 不可复算 | 持久化、source_chain、data_quality_status |
| UI 误导 | 用户误判策略能力 | 展示 score_status 和缺口 |

### 17.2 禁止事项

禁止：

- 用自由文本替代机器字段。
- 直接修改历史 frozen prediction。
- 用日线伪造分钟线 time bucket。
- 事件晚于预测仍计入 evidence_score。
- 样本不足时给出晋升结论。
- 把 `partial_daily_only` 当成 live-ready。
- 让 Desktop 直接调用 MCP 或 manager。
- 暴露 raw `strategy_manager` 给 Agent。
- 任何自动实盘交易动作。
- 硬编码数据库路径。
- 强制在线数据源覆盖 `TDX_LOCAL_ONLY=1`。

### 17.3 降级策略

允许降级：

- 旧策略没有完整合同时，生成 `derived_from_legacy_contract`。
- 分钟线缺失时，保留 P1 日线诊断评分。
- 事件时间缺失时，跳过 evidence_score。
- 因子快照缺失时，跳过 factor contribution。
- 数据不足时，只展示诊断，不用于晋升。

### 17.4 版本策略

合同版本：

```text
strategy_factory.trade_prediction_contract.v1
```

评分版本：

```text
trade_prediction_score_daily_v1
trade_prediction_score_v2
```

后续新增评分必须保留旧版本复算能力。

### 17.5 审计原则

每个 outcome 必须能回答：

- 预测原文是什么。
- 预测何时冻结。
- 预测 hash 是什么。
- 使用哪个 score version。
- 使用哪些行情数据。
- 使用哪些事件和因子证据。
- 哪些数据缺失。
- 为什么能晋升或不能晋升。

## 18. 可执行任务清单

### 18.1 P0 任务清单

- 新增 `TradePredictionContract` 类型。
- 新增 `TradePredictionOutcome` 类型。
- 新增合同规范化器。
- 新增合同冻结 hash。
- 新增 legacy derive 兼容层。
- 扩展 LLM Prompt 输出要求。
- 扩展 LLM normalize 后处理。
- 新增 `strategy_trade_predictions` schema。
- 新增 `strategy_trade_prediction_outcomes` schema。
- 新增 storage save/get/list 方法。
- 新增 prediction round-trip 测试。
- 新增 hash 稳定性测试。
- 新增提交门缺字段测试。

### 18.2 P1 任务清单

- 新增 daily outcome verifier。
- 读取 pending predictions。
- 查询 `kline_1d`。
- 计算 direction hit。
- 计算 target touch。
- 计算 daily risk proxy。
- 计算 `trade_prediction_score_daily_v1`。
- 写入 outcome。
- MetricsRecorder 增加 metadata。
- HitRateReporter 增加 summary。
- Pipeline 展示诊断状态。
- 补日线 outcome 测试。

### 18.3 P2 任务清单

- 新增 `kline_intraday` schema。
- 新增 intraday storage API。
- 将 `get_minute_kline` 结果标准化为 intraday rows。
- 增加 source_chain 落库。
- 增加分钟线 DQA。
- 新增 `IntradayReplayService`。
- 计算 time bucket hit。
- 计算 entry score。
- 计算 exit score。
- 计算 planned trade return。
- 计算 `trade_prediction_score_v2`。
- 增加缺口处理测试。

### 18.4 P3 任务清单

- 统一事件结构。
- 补 `event_time`、`ingested_at`、`prediction_as_of` 审计。
- 将事件 ID 写入 `evidence_refs`。
- 计算事件后 5/15/30/60 分钟收益。
- 扩展 factor catalog 短周期字段。
- 计算短周期 factor IC。
- 计算 factor contribution。
- 扩展 hit rate matrix 到 event/factor。
- 增加 post-hoc rejection 测试。

### 18.5 P4 任务清单

- 计算 score/pass LCB。
- Promotion Gate 接入新聚合指标。
- HitRateReporter 生成新 feedback actions。
- FeedbackWriter 写入 prediction feedback。
- Strategy Factory 预算反馈读取新字段。
- Factor decay 接入 prediction score delta。
- 增加晋升阻断测试。
- 增加 family boost/cool/freeze 测试。

### 18.6 P5 任务清单

- 新增 Agent 只读工具。
- 新增 Agent HTTP route。
- 新增 Desktop API types。
- 新增预测状态 UI。
- 新增 outcome 详情 UI。
- 新增矩阵 UI。
- 新增数据缺口 UI。
- 新增反馈摘要 UI。
- 增加 Agent 命名边界测试。
- 增加 Desktop HTTP 边界测试。

### 18.7 推荐实施顺序

严格按以下顺序：

```text
P0-1 类型与合同
P0-5/P0-6 存储
P0-2/P0-4 规范化与兼容
P1-1/P1-3 日线验证和评分
P1-4/P1-5 指标和报告
P2-1/P2-2 分钟线存储
P2-5/P2-6 回放和完整评分
P3 事件/因子领先性
P4 晋升和反馈
P5 可观测
```

不要先做 UI，不要先接晋升门，不要先让 Prompt 输出复杂字段而没有存储和验证。

### 18.8 最终交付定义

完成后系统应能稳定回答：

- 某策略预测了哪只股票、哪天、什么方向、什么概率。
- 预测在真实行情中是否命中方向。
- 是否触达目标价。
- 是否在预测时间段发生。
- 入场和出场窗口是否有效。
- 风险是否受控。
- 事件和因子是否早于行情反应。
- 当前样本是否足够宣称有效。
- 为什么策略能晋升或不能晋升。
- 哪些 family、因子、事件类型应加权或降权。

最终原则：

```text
不要把 AI 生成内容当准确率。
只把冻结预测在真实行情、真实时间线、真实数据质量约束下的可复算 outcome 当准确率。
```

## 19. 代码落地索引与阅读入口

本章用于把前文设计映射到当前仓库代码。后续真正改代码时，应先按本章定位入口，再回到第 5-18 章执行字段、评分、测试和验收细节。

### 19.1 阅读顺序

建议阅读顺序：

1. 先读第 1 章，确认总体可行性和不替换 `prediction_contract` 的核心结论。
2. 再读第 3 章，确认当前代码已有能力和缺口。
3. 实施 P0 前读第 5、6、8、16.2、18.1 章。
4. 实施 P1 前读第 7、9、16.3、18.2 章。
5. 实施 P2 前读第 6.4、7.4、9.4、12、16.4、18.3 章。
6. 实施 P3 前读第 10、11、16.5、18.4 章。
7. 实施 P4 前读第 13、16.6、18.5 章。
8. 实施 P5 前读第 14、16.7、18.6 章。

不要从 UI 或晋升门开始实施。没有冻结预测、持久化 outcome 和样本门槛时，任何可视化和晋升判断都只是展示层幻觉。

### 19.2 P0 代码入口：合同、冻结和存储

优先入口：

- `packages/strategy-factory/src/strategy_factory/application/semantic_contract.py`
- `packages/strategy-factory/src/strategy_factory/application/semantic_contract_parts/policy.py`
- `packages/strategy-factory/src/strategy_factory/application/semantic_contract_parts/normalizers.py`
- `packages/strategy-factory/src/strategy_factory/application/candidate_contract.py`
- `packages/strategy-factory/src/strategy_factory/application/candidate_contract_parts/normalizers.py`
- `packages/strategy-factory/src/strategy_factory/application/submission_gate/runner.py`
- `packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/semantic_context.py`
- `packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/trade_profile.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/schema_strategy_parts/schema_definitions.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/schema_strategy_parts/writes.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/schema_strategy_parts/queries.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/schema_strategy_parts/mappers.py`

P0 最小产物：

- 新增 `trade_prediction_contract` 类型或规范化模块。
- 保留现有 `prediction_contract`，只新增 sibling 字段。
- 新增 frozen contract hash。
- 新增 legacy candidate 推导能力，但必须标记 `derived_from_legacy_contract`。
- 新增 `strategy_trade_predictions` schema。
- 新增 `strategy_trade_prediction_outcomes` schema。
- 新增保存、查询、列表读取接口。
- 新增合同 round-trip 测试和 hash 稳定性测试。

P0 禁止跳步：

- 不要直接让孵化评分读取 LLM 原文。
- 不要把 `trade_plan` 文本当作可验证合同。
- 不要修改历史 frozen prediction。
- 不要把缺字段的合同提交到孵化评分。
- 不要在没有 schema 的情况下先做 reporter 或 UI。

### 19.3 P1 代码入口：日线诊断评分

优先入口：

- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/runner.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/forward_verifier.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/metrics_recorder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_reporter.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_matrix.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_pipeline.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_schema_market_phase_1.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_strategy_crud_market.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/strategy_incubation.py`

P1 最小产物：

- 从 `strategy_trade_predictions` 读取 pending prediction。
- 用 `kline_1d` 计算次日方向命中。
- 用日线 high/low/close 计算目标价触达。
- 用日线区间估算风险代理指标。
- 生成 `trade_prediction_score_daily_v1`。
- 将 outcome 写入 `strategy_trade_prediction_outcomes`。
- 在报告中展示 `score_status`、缺口、样本数和分组维度。

P1 禁止跳步：

- `trade_prediction_score_daily_v1` 只能作为诊断分。
- 不允许把 P1 分数接入 live-ready 晋升硬门。
- 不允许用日线数据伪造分钟级入场、出场和 time bucket。
- 样本不足必须输出 `insufficient_samples`。

### 19.4 P2 代码入口：分钟线持久化和完整评分

优先入口：

- `packages/akshare-mcp/src/akshare_mcp/tools/market/kline.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/schema_market.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_schema_market_phase_5.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_schema_market_phase_6.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_schema_market_phase_7.py`
- `packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/_strategy_crud_market.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/forward_verifier.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/metrics_recorder.py`

P2 最小产物：

- 新增 `kline_intraday` schema。
- 将 `get_minute_kline` 结果标准化后落库。
- 写入 `source_chain`、`adjust`、`period`、`data_quality_status`。
- 新增分钟线缺口检测。
- 新增 `IntradayReplayService` 或等价回放模块。
- 计算 time bucket hit、entry score、exit score、planned trade return。
- 生成 `trade_prediction_score_v2`。

P2 禁止跳步：

- 没有 `kline_intraday` 前，不允许生成完整盘中评分。
- 分钟线缺失时必须输出 partial 或 missing 状态。
- `partial_intraday_missing` 不能进入 live-ready 晋升。
- 不允许将在线接口临时返回值直接当作可复算 outcome。

### 19.5 P3 代码入口：事件、因子和领先性

优先入口：

- `packages/akshare-mcp/src/akshare_mcp/services/factor_catalog.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_mining_factory/factory.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_mining_factory/engines/`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_matrix.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/feedback_writer.py`
- `packages/strategy-factory/src/strategy_factory/application/research/candidate_origin.py`

P3 最小产物：

- 统一事件 ID 和事件时间字段。
- 在 `evidence_refs` 中引用可审计事件或因子快照。
- 记录 `event_time`、`ingested_at`、`prediction_as_of`。
- 晚于预测冻结时间的事件必须标记 `post_hoc_rejected`。
- 计算事件后 5/15/30/60 分钟窗口收益。
- 扩展因子 catalog 的短周期指标。
- 将 event/factor 维度纳入 hit rate matrix。

P3 禁止跳步：

- 不允许用事后新闻解释预测准确率。
- 不允许只存事件文本而不存事件时间。
- 不允许把因子当前值当作预测时快照。
- 没有领先性审计时，`evidence_score` 必须降级或跳过。

### 19.6 P4 代码入口：晋升门、预算反馈和衰减

优先入口：

- `packages/strategy-factory/src/strategy_factory/application/quality_gates.py`
- `packages/strategy-factory/src/strategy_factory/application/quality_gates_parts/evaluation.py`
- `packages/strategy-factory/src/strategy_factory/application/quality_reporting.py`
- `packages/strategy-factory/src/strategy_factory/application/strategy_factory_json_budget.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/hit_rate_reporter.py`
- `packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/feedback_writer.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_mining_factory/factory.py`

P4 最小产物：

- 使用 `trade_prediction_score_v2` 和样本下界作为晋升候选条件。
- 对 family、factor、event_type 生成 boost、cooldown、freeze 或 decay 建议。
- 将 prediction outcome 写入策略工厂可读取的预算反馈。
- 保留现有 hit rate、skill LCB、IC、Sharpe 等指标，不做替换。
- 对低样本、partial outcome、数据缺失分别输出阻断原因。

P4 禁止跳步：

- 不允许 P1 诊断分直接晋升。
- 不允许只看均值，不看样本数和下界。
- 不允许将 `partial_daily_only` 当作完整评分。
- 不允许覆盖现有预算反馈，只能兼容扩展。

### 19.7 P5 代码入口：Agent 和 Desktop 可观测

优先入口：

- `packages/agent/src/aiask_agent/tool_registry.py`
- `packages/agent/src/aiask_agent/server.py`
- `packages/agent/src/aiask_agent/routes/`
- `desktop/src/services/aiaskApi.ts`
- `desktop/src/features/incubation/IncubationFactoryPanel.tsx`

P5 最小产物：

- 新增 `agent_trade_prediction_status` 只读工具。
- 新增 `agent_trade_prediction_outcomes` 只读工具。
- 新增 `agent_trade_prediction_matrix` 只读工具。
- 新增 Agent HTTP route。
- Desktop 只通过 Agent HTTP API 展示预测状态、outcome、矩阵、缺口和样本不足原因。
- UI 必须展示 `score_version`、`score_status`、`sample_n`、`data_quality_status`。

P5 禁止跳步：

- Agent 工具必须以 `agent_*` 命名。
- 不允许暴露 raw manager。
- 不允许 Desktop 直接调用 MCP 或 manager。
- 不允许 UI 触发任何实盘交易副作用。
- 不允许隐藏样本不足和数据缺口。

### 19.8 最小 PR 拆分建议

建议按以下 PR 拆分，避免单次改动过大：

- PR-1：`trade_prediction_contract` 类型、规范化、冻结 hash、单测。
- PR-2：`strategy_trade_predictions` 和 `strategy_trade_prediction_outcomes` schema、CRUD、round-trip 测试。
- PR-3：策略工厂 candidate 输出、submission gate、legacy derive 兼容。
- PR-4：P1 daily outcome verifier、metrics、reporter。
- PR-5：`kline_intraday` schema、分钟线落库、DQA。
- PR-6：intraday replay、`trade_prediction_score_v2`。
- PR-7：事件和因子领先性审计。
- PR-8：晋升门、预算反馈、因子衰减。
- PR-9：Agent 只读工具和 HTTP route。
- PR-10：Desktop 可观测 UI。

每个 PR 都必须带一个可回归的最小测试集。没有测试的结构性改造不能进入后续阶段。
