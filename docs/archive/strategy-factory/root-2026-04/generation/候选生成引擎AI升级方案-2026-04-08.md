# 候选生成引擎 AI 升级方案

文档日期：2026-04-08

文档性质：工程优化方案（已通过深度推理 + 联网学术验证）

适用范围：`packages/strategy-factory/src/strategy_factory/domain/spawner.py`、`packages/akshare-mcp/src/akshare_mcp/services/strategy_generators.py`、工厂 autonomy 路径

关联文档：`策略工厂架构优化方案-2026-04-08.md`、`策略工厂/策略工厂整改详细清单.md`

本次同步更新依据：2026-04-09 对 `packages/strategy-factory`、`packages/akshare-mcp` 的深度代码审计，以及真实运行批次 `factory_run_1775700638_54794a72` 的提交策略复检结果

学术依据：Zhizhuo Kou et al.「Automate Strategy Finding with LLM in Quant Investment」EMNLP 2025 Findings（HKUST/PKU）；Bailey & López de Prado「The Probability of Backtest Overfitting」；Bayesian Optimization with stability constraints for trading strategy parameter tuning（Nature Scientific Reports 2025）

> 2026-04-11 代码核对修订说明：
> 1. L1/L2 主链、attempt 语义拆分、feedback 路由、strict/cohort 化 Gate-3 在代码中已能对上。
> 2. `StrategySpawner` 仍应被视为本地 seed/fallback 规则生成器，而不是 AI 主导生成器。
> 3. 当前最大的未完成项仍是默认 steady-state、质量分布右移、live-ready 持续产出与 provider 稳定性，而不是“候选生成主链尚未接入 AI”。

---

## 一、结论先行

当前候选生成器的核心问题不是"没有接入 AI"，而是 **AI 的能力被架构约束在一个错误的位置上**——它只能对规则引擎已经产出的候选做 enrichment，无法在候选生成阶段发挥作用。

规则引擎（`StrategySpawner`）的 4 类信号批次 + 变体扩展 + 配额补位机制，从工程角度看是稳健的冷启动方案，但它有三个对 AI 而言的结构性弊端：

1. **策略类型边界永远锁死**：只能在 14 种预定义类型内产出候选，无法发现新策略逻辑
2. **信号→策略的映射不可学习**：人工假设被硬编码，历史回测结果无法反向影响生成行为
3. **配额补位制造无信号噪声**：补位候选没有市场依据，同时污染"生成质量→通过率"的统计关系

因此本次升级的目标不是推倒规则引擎，而是分三层演进：

- **L1（最小改动）**：让历史回测结果反馈影响参数采样，把随机游走变为有引导的搜索
- **L2（中等改动）**：把 LLM 从 enrichment 提升为 generation，让规则引擎退化为 fallback
- **L3（较大改动）**：引入策略逻辑层次的探索，让 AI 能提出当前 14 种类型之外的策略假设

本次同步修订还要额外强调 4 个现实约束：

1. **当前 LLM 并不是弱，而是被 Prompt 和 normalize 层强行压成了 DSL 填空机**
2. **当前 Gate-3 的问题不是单纯过严，而是 incubation 偏宽、live 偏晚，前后统计对象还存在错配**
3. **当前 feedback 闭环主要在做节流，没有真正把搜索从低质量 family 导向高质量新域**
4. **当前策略对象在 schema 上完整，但在经济意义上仍接近“信号原型”，不是完整交易程序**

截至 2026-04-11，还需要补充 6 个现实问题，避免误把“开发主链已落地”写成“整体结果已完成”：

1. 默认 steady-state 仍不稳定。最新默认 run `factory_run_1775874046_da5aad36` 为 `skipped`，说明候选生成链路还没有在默认门控下持续稳定地产出新样本。
2. 当前真实 cohort 仍然明显 `raw D` 偏多，`raw_validation_b_rate=0.04`、`raw_b_or_above_rate=0.055`，说明高质量候选虽已出现，但整体右移还不够。
3. `momentum / ma_cross / quality_factor` 的提升仍不均衡，其中 `quality_factor` 的主桶仍然偏弱，family specialization 还没有完全稳定。
4. `strict_incubation_ready_rate` 已明显改善，但 `live_candidate_ready_rate=0.0075` 仍很低，意味着候选生成对 live-ready 质量的支撑还不够稳定。
5. 外部 LLM 已不再是兼容层故障，但真实运行中仍观察到 provider 波动和 `HTTP503`，这会直接影响 hypothesis 生成和开放探索的稳定性。
6. 研究主链的物理复杂度仍然偏高，复杂度继续集中在 `packages/strategy-factory/src/strategy_factory/application/research/factor_research_builder.py`，后续继续改 L2/L3/feedback 仍有较高回归风险。

---

## 二、当前实现的真实边界（基于代码审查）

### 2.1 `StrategySpawner.spawn()` 的实际逻辑

```text
_build_signal_batches()
  ├── _from_fear_greed()     # 恐贪指数阈值触发，3-4 个固定模板
  ├── _from_factor_ic()      # 因子 IC > 0.03 触发，因子→类型固定映射
  ├── _from_volatility()     # 波动率阈值触发，2 个固定模板
  └── _from_fund_flow()      # 北向/融资流量触发，4 个固定模板

_expand_signal_variants()    # 强信号类型复制 ≤3 个变体，参数从固定列表轮取 ±20% 抖动

_fill_gaps()                 # 当类型数量不足配额时，无论有无信号依据强制生成
```

**核心问题**：`_varied_defaults()` 是整个生成系统的参数空间定义，它是一个 14-branch `if-elif` 链，每种策略类型只有 2-3 组固定参数基础值加随机抖动。

### 2.2 Autonomy 在主链中的实际位置

```text
spawn（规则生成候选）→ autonomy（LLM enrichment）→ quality_gate
```

当前 autonomy 阶段的作用：对规则引擎产出的候选添加假设描述、目标股票池、风险标签等注释字段。**LLM 不参与候选策略逻辑本身的生成**。

### 2.3 当前运行数据揭示的问题

根据 `factory_status` 最近数据：

- 最近一轮生成了 **145 个候选**，其中 **132 个来自 autonomy 生成**（但这 132 个并非 LLM 无中生有，而是由外部任务驱动的候选，与本地规则批次有根本区别）
- `incubating_count = 370`，但 `lifecycle_scan` 发现最常见阻塞原因是"有效信号数 0 < 10"
- `promotion_review_count = 0`

这说明：系统在持续产出候选，但产出的候选缺乏独特性和可验证的信号支撑，导致孵化池不断堆积而晋级率为零。

补充：`2026-04-09 10:10:38 +08:00` 的真实运行批次 `factory_run_1775700638_54794a72` 已进一步证明这一点。该批次虽然 `status=success`、`autonomy_task_count=28`、`created=3`、`submitted=3`，但同时存在：

1. `external_llm_provider_health_status=degraded`
2. `external_llm_provider_control_mode=suppress`
3. 提交的 3 个策略复检结果全部为 `research_only / deferred_submission`
4. 3 个策略全部 `validation_grade=D`
5. 3 个策略全部 `signal_total_signals=0`
6. 3 个策略全部 `forward_total_signals=0`

这意味着系统当前已经能稳定生产“研究样本”，但离“高质量、可孵化、可实盘”的策略仍有明显距离；瓶颈是结构性的，不是单次 run 异常。

### 2.4 当前 LLM 的真实问题不是“没接入”，而是“接入方式错误”

根据当前代码：

1. `strategy_llm_provider` 的 Prompt 对 `target_symbols`、指标集合、条件运算、窗口范围、候选数量都做了非常强的硬限制
2. 紧凑模式下甚至直接要求只生成 1 个高置信 candidate
3. normalize/precompile 层会对缺失的 `holding_horizon / trade_plan / risk_rules / position_sizing / portfolio_spec / execution_assumptions / validation_profile` 做默认值回填

这意味着当前 LLM 的职责更接近：

```text
程序先给定目标池、字段集合、指标白名单、窗口白名单、候选上限
-> LLM 在狭窄约束下补全 JSON / DSL
-> normalize 层用默认模板补足缺失交易语义
```

而不是：

```text
LLM 基于市场状态与历史证据提出高层假设
-> 系统再把假设编译为候选对象与 DSL
-> 再进入回测与门禁
```

因此，**L2 的正确升级方向不是“让 LLM 直接输出更多最终 DSL”，而是“让 LLM 先生成研究假设对象，再由编译层降到候选协议”**。

此外，当前 external provider 还会受到 provider health / generator-mode control 的直接 `suppress`。如果继续把 L2 理解成“在现有 autonomy lane 里让外部 LLM 直接输出最终 candidate / DSL”，那么只要 provider degraded 或 feedback freeze，AI 探索就会整体失血。因此 L2 的真正目标必须同时包含：

1. hypothesis lane 与 final candidate compile lane 解耦
2. 规则引擎退到 seed / fallback，而不是继续作为支配性主干
3. external provider 被抑制时，系统仍能复用已持久化 hypothesis artifact 做 replay / compile / reroute，而不是整段失去 AI 搜索能力

### 2.5 本轮审计同步的新增硬需求

这份方案在原始版本中更偏重“生成能力增强”，但根据最新代码审计，需要补上以下 4 个硬需求，否则即使上线 L1/L2/L3，也仍然只能稳定地产出中等偏下策略：

#### S1. 从“最终 DSL 直接生成”改为“双阶段生成”

必须把候选生成拆成两步：

```text
Hypothesis Generation
  -> 生成 alpha 假设、适用市场状态、目标持有期、失效条件、成本敏感性、仓位假设
  -> 生成 target universe / family / validation focus 候选边界

Candidate Lowering / DSL Compile
  -> 把假设对象编译为当前 candidate contract
  -> 再编译为 DSL / params
  -> 不合规直接丢弃，不做补丁式修补
```

原因：

1. 研究推理和严格格式化输出是两种完全不同的工作
2. 当前单 Prompt 同时承担“发现 alpha + 输出严格 JSON DSL”，模型会优先保格式而不是保研究质量
3. 只有先把高阶假设显式化，后续 feedback 才能学习“什么假设有效”，而不只是学习“什么参数有效”

#### S2. Gate-3 必须改成“分阶段 + 同类 cohort”的统计门

新增要求：

1. incubation 阶段不能再把 `trade_density`、`parameter_perturbation_trade_stability`、`validation_grade` 长期只当 warning 看待
2. live 阶段的 DSR / PBO / White Reality Check / Hansen SPA 不能只基于“目标标的 + 固定代表股”拼出来的面板，而要尽量基于 `family × universe × holding bucket × generator_mode` 的同类候选 cohort
3. `attempt_count` 不能再把工厂级总尝试数直接摊到单个候选头上；应拆成：
   - `candidate_local_attempt_count`
   - `task_local_attempt_count`
   - `cohort_effective_trials`
   工厂级总尝试数只能进入 registry 层的 multiple-testing 修正，不能直接抬高单候选 trade gate 阈值
4. `validation_grade=D` 不能继续仅在 provisional incubation 里作为 warning 留存；在 strict incubation / governed promotion 路径中，至少必须成为明确阻断或强降权信号

#### S3. feedback 必须驱动“搜索路由”，而不只是“预算节流”

新增要求：

1. zero-signal / low-signal / evidence-debt 不能只映射为 `budget_multiplier` 和 `control_mode`
2. feedback 需要显式输出：
   - family retire / freeze / cooldown / explore
   - universe retire / shrink / expand
   - holding horizon promote / demote
   - generator_mode budget shift
3. `refresh_existing` 与 `spawn_revision_from_existing` 需要继续收紧，避免系统围绕低质量 family 反复做 tested object 微修订
4. feedback 需要维护 `family × universe × holding bucket × generator_mode` 的 reward / debt 表，而不是只维护 budget multiplier
5. `refresh_existing` 只允许在 `tested_object` 未变化且 `signal / forward / review` 证据连续改善时触发；否则强制分叉新 lineage 或直接淘汰
6. 同一 lineage 的连续 `refresh/revision` 次数必须有硬上限（建议 2-3 次），防止低质量 family 长期空转

目标不是“在旧 family 上更克制地空转”，而是“把搜索预算主动切到更有希望的新域”。

#### S4. 策略对象必须从“协议完整”升级为“经济完整”

当前对象协议已经包含 `holding_horizon / trade_plan / risk_rules / position_sizing / portfolio_spec / execution_assumptions`，但这些字段大量来自模板与默认值，并不等于策略已经完成交易层推理。

因此新增要求：

1. L2/L3 候选必须显式给出 `holding_rationale`
2. 必须显式给出 `alpha_half_life` 或等价持有期衰减解释
3. 必须显式给出 `cost_sensitivity_grid`
4. 必须显式给出 `position_model` 与 `capacity_assumption`
5. 回测层必须真的执行这些字段，而不是只把它们当 metadata 落库
6. 若候选仍只能回退到 `single_name/equal_weight + fixed commission/slippage` 这一类默认假设，必须被显式标注为“经济语义缺失”，不能再与经济完整候选共用同一质量口径

如果做不到这一点，系统产出的仍然只是“带完整字段的信号原型”。

---

## 三、L1：历史反馈影响参数采样

### 3.1 目标

把 `_varied_defaults()` 中的参数选择从"固定列表+随机抖动"改为"从历史高表现候选的参数分布中采样"。

不改变策略类型边界，不改变信号触发逻辑，只改变参数的选取方式。

### 3.2 方案设计

**新增组件**：`ParameterDistributionRegistry`

```python
class ParameterDistributionRegistry:
    """
    从历史回测结果中提取各策略类型的高表现参数分布。
    提供带历史引导的参数采样方法，替换 _varied_defaults() 中的固定列表。
    """

    def build_from_history(self, db, strategy_type: str) -> dict:
        """
        从 DB 中查询该类型历史 sharpe > 阈值的候选记录，
        提取其参数字段的统计分布（均值、标准差、分位数）。
        """
        ...

    def sample(self, strategy_type: str, idx: int) -> dict:
        """
        若该类型有足够历史数据，从分布中采样；
        否则 fallback 到原来的 _varied_defaults() 固定值。
        """
        ...
```

**修改点**：`StrategySpawner._varied_defaults()` 接受可选的 `registry` 参数

```python
def _varied_defaults(self, strategy_type: str, idx: int,
                     registry: ParameterDistributionRegistry | None = None) -> dict:
    if registry is not None:
        result = registry.sample(strategy_type, idx)
        if result:
            return result
    # 原有固定列表逻辑作为 fallback
    ...
```

### 3.3 实施要点

| 要点 | 说明 |
|------|------|
| **最小侵入** | 不改变 `spawn()` 接口签名，`registry=None` 时行为与现有完全一致 |
| **数据依赖** | 需要历史候选的 `strategy_type`、`params`、`backtest_sharpe` 字段，这些字段已经存在于 DB |
| **样本不足降级** | 某类型历史样本 < 10 时，自动 fallback 到固定参数列表，避免分布失真 |
| **更新频率** | 建议每次工厂运行前加载一次，不需要实时更新 |
| **OOS 限定（关键）** | 分布提取必须严格限定在样本外（OOS）验证通过的候选。不得从全量历史最优中提取，否则会将"在历史数据上的封闭式最优化偏差"直接注入新一轮采样。正确做法：只从 Purged K-Fold 或 Walk-Forward 的样本外 Sharpe > 0.3 的候选中提取分布。 |
| **稳定性约束** | 参数分布采样需要加入稳定性约束（方差惩罚），防止贝叶斯优化在噪声金融数据中向较差参数区间反复震荡（见 Nature Scientific Reports 2025 同类研究） |

### 3.4 验收指标

- 有历史反馈的策略类型，其变体的平均 Sharpe 应高于纯随机抖动基线
- `_varied_defaults` 产出的参数不再高度集中在 `[10, 20, 30]` 等固定锚点

---

## 四、L2：LLM 从 Enrichment 升级为 Generation

### 4.1 目标

让 LLM 参与候选策略的生成，但**不再要求它一步直接输出最终 candidate / DSL**。正确目标应是：基于当前市场快照和历史通过率特征，先提出新的策略假设（含策略逻辑描述、目标参数范围、目标股票池约束、持有期与成本假设），再由编译层降为当前候选协议。规则引擎退化为 fallback：LLM 失败时才使用规则批次填充。

### 4.1a L2 的关键修订：先假设、后编译

L2 应拆成两层组件，而不是继续扩张当前单一 `LLMCandidateGenerator`：

```python
class LLMHypothesisGenerator:
    """
    输入：市场快照 + factor_research + governed pool 摘要 + 历史高通过率摘要
    输出：高层研究假设对象
    字段至少包括：
    - alpha_hypothesis
    - failure_mode
    - target_universe_hypothesis
    - family_hint
    - holding_rationale
    - alpha_half_life
    - cost_sensitivity_grid
    - position_model
    - validation_focus
    """


class HypothesisLoweringCompiler:
    """
    将 hypothesis object 编译为当前 candidate contract / DSL。
    不做补丁式修补：
    - contract 不完整 -> 丢弃
    - target alignment 不合规 -> 丢弃
    - DSL 编译失败 -> 丢弃
    """
```

**原因**：

1. 当前 Prompt 的核心问题是“格式约束压过研究推理”
2. 当前 normalize 层会默认补齐交易字段，这会掩盖 LLM 没有真正思考持有期 / 成本 / 仓位的事实
3. hypothesis object 是后续 feedback 学习的最小可解释单元，比直接比较 DSL 参数更有价值

L2 的 Prompt/约束也需要从“直接硬卡最终输出”改成“两层约束”：

1. hypothesis prompt 允许自由提出 `family / horizon / universe / failure_mode / cost assumption / position model`
2. compiler 层再执行 indicator catalog、DSL/schema、target alignment、risk boundary、window 合规校验
3. normalize 层不允许继续用默认模板替代 hypothesis 缺失的关键经济字段；缺失即视为 hypothesis 不完整

### 4.2 方案设计

**新增组件**：`LLMCandidateGenerator`

```python
class LLMCandidateGenerator:
    """
    以 LLM 为主驱动的候选生成器。
    输入：市场快照 + 历史高通过率候选摘要 + 当前因子研究 artifact
    输出：结构化候选列表，格式与 StrategySpawner._make() 产出兼容
    """

    async def generate(
        self,
        snapshot: dict,
        factor_research: dict,
        high_pass_summary: list[dict],   # 历史通过 Gate-3 的候选特征摘要
        budget: int,
        allowed_types: list[str],         # 当前配额不足的类型
    ) -> list[dict]:
        """
        调用 LLM，要求返回符合当前约束的策略候选 JSON。
        对输出做结构化校验，不合格的候选直接丢弃（不做补救式修改）。
        """
        ...

    def _build_prompt(self, snapshot, factor_research, high_pass_summary, budget, allowed_types) -> str:
        """
        Prompt 设计原则：
        1. 给出当前市场状态摘要（恐贪、因子 IC、资金流向、波动率）
        2. 给出历史通过率最高的候选特征（类型、参数范围、信号触发条件）
        3. 明确要求输出 JSON schema，字段与 spawner._make() 对齐
        4. 明确禁止输出不在 allowed_types 内的类型（除非 allow_new_types=True）
        """
        ...

    def _validate_output(self, raw_output: str, allowed_types: list[str]) -> list[dict]:
        """
        严格校验 LLM 输出：
        - strategy_type 必须在允许列表内（L2 阶段约束，L3 放开）
        - params 字段类型必须匹配该类型的 schema
        - 不合法的候选直接丢弃，不修补
        """
        ...
```

上面这个 `LLMCandidateGenerator` 仍可保留，但应视作 `LLMHypothesisGenerator + HypothesisLoweringCompiler` 的兼容外壳，而不是最终形态。

**修改点**：`StrategySpawner.spawn()` 新增 `llm_generator` 可选参数

```python
async def spawn(
    self,
    snapshot: dict,
    llm_generator: LLMCandidateGenerator | None = None
) -> list[dict]:
    # 若有 LLM 生成器，先尝试 LLM 生成
    if llm_generator is not None:
        llm_candidates = await llm_generator.generate(
            snapshot=snapshot,
            factor_research=snapshot.get("factor_research", {}),
            high_pass_summary=self._load_high_pass_summary(snapshot),
            budget=SPAWNER_TARGET_TOTAL,
            allowed_types=list(CATEGORY_MINIMUMS.keys()),
        )
        if len(llm_candidates) >= SPAWNER_TARGET_TOTAL * 0.6:
            # LLM 产出足够时，只用规则引擎补充类型配额缺口
            quota_candidates = self._fill_gaps(snapshot, llm_candidates)
            return [*llm_candidates, *quota_candidates]

    # LLM 不可用或产出不足时，完整运行规则引擎（原有逻辑不变）
    return self._rule_based_spawn(snapshot)
```

### 4.3 历史高通过率摘要的构建

LLM 的核心上下文输入之一是"历史上什么样的候选通过了 Gate-3"。这需要构建一个轻量摘要：

```python
def _load_high_pass_summary(self, snapshot: dict) -> list[dict]:
    """
    从 DB 查询近 90 天内 gate3_passed=True 的候选，
    提取 strategy_type / 核心参数范围 / 触发信号类型 / Sharpe 范围，
    压缩为 ≤20 条摘要，注入 Prompt。
    """
```

### 4.4 实施要点

| 要点 | 说明 |
|------|------|
| **向后兼容** | `llm_generator=None` 时 `spawn()` 行为与现有完全一致 |
| **失败降级** | LLM 调用失败、超时、输出不合格时，自动回到规则引擎全量运行 |
| **产出格式约束** | LLM 输出必须严格通过 JSON Schema 校验，不允许"大致对就行"的修补。业界 2025 年主流做法是"推理规划"与"执行输出"解耦：LLM 输出高阶策略意图，专门的解析层将其转化为结构化 JSON，而非让 LLM 直接边推理边输出格式 |
| **配额补位仍保留** | `_fill_gaps()` 继续作为类型缺口的最终补充，但不再是无信号填充 |
| **LLM 尝试次数记录** | 必须记录本轮的 LLM 候选数、校验通过数、丢弃数，纳入 multiple-testing 修正 |
| **冷启动门槛（关键）** | 历史 Gate-3 通过且有 OOS 验证记录的候选数量 < 30（每类型 < 5）时，LLM Prompt 中不注入历史高通过率摘要，仅依据当前市场快照和因子 IC 生成。低于门槛时 LLM 候选占总候选比例上限从 60% 降至 30%，优先由规则引擎填充 |
| **批次内相关性修正（关键）** | 同一轮 LLM 生成的 N 个候选，在 PBO 计算时不应按 N 个独立候选计入，而应按等效独立数 K 计入（K = N / (1 + ρ\_mean)，其中 ρ\_mean 为候选间策略类型重叠系数）。具体实现：对同类型 LLM 候选只取其中最高 Sharpe 的代表进入 CSCV 路径 |
| **长期衰减监控** | 参照 EMNLP 2025 论文发现，LLM 策略的优势随时间可能因数据窥探偏差而衰减。应建立"LLM 候选 OOS 表现月度对比"看板，当连续 2 个月 LLM 候选 OOS Sharpe 低于规则候选时，自动回退到 L1 模式 |
| **必须保留 hypothesis artifact** | LLM 生成的高层假设对象必须单独持久化，供 feedback、promotion review 和 family retirement 使用，不能只保留最终 DSL |
| **research-first 优先级** | L2 的成功标准不是“LLM 返回了多少合法 JSON”，而是“是否提升了 `unique family × horizon × universe` 组合、`forward coverage` 与 `promotion-ready rate`” |

### 4.5 Prompt 设计关键约束

LLM 被要求产出的候选必须包含以下字段（与现有 `spawner._make()` 对齐）：

```json
{
  "strategy_type": "momentum",
  "params": {"lookback": 15, "threshold": 0.025},
  "spawn_reason": "因子IC=0.06上升，融资5日增速3.2%，短周期动量确认",
  "generation_reason": {
    "kind": "llm_generate",
    "source": "llm_candidate_generator",
    "summary": "...",
    "evidence": {"factor_ic": 0.06, "margin_5d": 3.2}
  }
}
```

新增 `kind: "llm_generate"` 以便在统计通过率时区分规则候选与 LLM 候选。

### 4.6 验收指标

- LLM 候选的 Gate-3 通过率 ≥ 规则候选通过率（否则说明 LLM 质量不够，不应提升其优先级）
- LLM 候选的平均 Sharpe ≥ 规则候选平均 Sharpe
- `incubating` 零信号策略中，LLM 候选占比显著低于规则候选

---

## 五、L3：策略逻辑层次的探索

### 5.1 目标

突破 14 种类型的硬性边界，让 LLM 能够提出当前类型体系之外的策略假设，经过 DSL 编译验证后进入候选流程。

### 5.1b L3 的硬性前置依赖（关键，非可选）

L3 的 DSL 候选在回测引擎中依赖 `DslRuleStrategy` 的完整执行，但当前代码审查发现 `DslRuleStrategy.generate_signals()` 并不实际使用策略中的 `risk_rules` 字段（已在整改清单中记录为 WP 之一）。这意味着：

- L3 产出的 DSL 候选即使包含 `stop_loss_pct / max_holding_days` 等风控规则，回测也不会执行它们
- 回测结果因此高估了候选的实际可行性
- 这是 L3 的工程硬伤，必须在 L3 上线前修复

同理，若 `position_model / cost_sensitivity_grid / capacity_assumption / holding_rationale` 继续只作为 metadata 落库而不进入回测执行，L3 的“开放型候选”也只是在扩大信号原型的种类，而不是扩大真实交易策略的种类。

**L3 不可在 `DslRuleStrategy.risk_rules` 完整执行修复前上线。**

### 5.2 方案设计

**核心变化**：候选的 `strategy_type` 不再限定于 14 个预定义值，而是允许 LLM 产出带有完整 DSL 逻辑的"开放型候选"。

```python
# L2：LLM 只能产出已知类型
{"strategy_type": "momentum", "params": {...}}

# L3：LLM 可以产出带 DSL 的新逻辑
{
  "strategy_type": "llm_defined",
  "dsl_spec": {
    "entry": "close > ma(20) and rsi(6) < 35 and north_fund_3d > 50e8",
    "exit": "close < ma(10) or holding_days > 10",
    "risk_rules": {"stop_loss_pct": 0.07, "max_position_pct": 0.15}
  },
  "hypothesis": "北向流入 + 短期超跌双重确认时，短期反弹概率高于均值",
  "spawn_reason": "北向3日净流入62亿，RSI<35，均线多头排列"
}
```

**新增校验层**：DSL 编译器先于回测运行

```python
async def _validate_llm_dsl_candidate(candidate: dict) -> dict | None:
    """
    对 LLM 产出的 dsl_spec 做语法校验和语义合理性检查：
    - 字段完整性（entry/exit/risk_rules 必须存在）
    - DSL 语法合法性（通过现有 strategy_dsl.compile_strategy_blueprint）
    - 基本逻辑一致性（持仓期范围、止损范围是否合理）
    若不通过，直接丢弃，不补救。
    """
```

### 5.3 实施要点

| 要点 | 说明 |
|------|------|
| **L3 是增量路径** | L1、L2 完整运行后才开启 L3，不跳步 |
| **DSL 编译器复用** | 现有 `strategy_dsl.compile_strategy_blueprint` 可直接用于语法校验 |
| **新类型的配额管理** | `llm_defined` 类型单独限额（如每轮 ≤5 个），避免无节制扩张 |
| **多重检验加权** | `llm_defined` 候选在 Gate-3 中需要额外的参数稳健性惩罚，防止 LLM 过度优化 |
| **类型沉淀机制** | 若某 `llm_defined` 候选多次通过 Gate-3，可以由人工将其归纳为新的命名类型，纳入 14 种体系 |

### 5.4 探索闭环

```text
LLM 产出新 DSL 候选
    ↓
DSL 编译器校验（语法 + 语义）
    ↓
回测引擎执行（含成本/滑点/容量）
    ↓
Gate-3 多重检验（含额外稳健性惩罚）
    ↓
通过 → 进入孵化 → 积累前向信号 → 晋级审查
    ↓（若多次晋级）
人工归纳为新命名类型 → 扩展 14 种类型边界
    ↓
历史通过率摘要更新 → 反馈给下一轮 LLM Prompt
```

---

## 六、三层升级的兼容性约束

### 6.1 不打断的接口

以下接口在三层升级过程中必须保持不变：

| 接口 | 位置 | 约束 |
|------|------|------|
| `StrategySpawner.spawn(snapshot)` | `spawner.py` | 新增参数必须有默认值，`spawn(snapshot)` 调用仍正常运行 |
| `strategy_manager` 全部 action | `strategy_manager.py` | 不改变，不删除 |
| BFF `/strategy-market/*` | `apps/bff/src/strategy` | 不改变 |
| `akshare_mcp.services.strategy_factory.*` | 旧路径 | 保持兼容导出 |

### 6.2 新增字段的向后兼容

所有新增 `generation_reason.kind` 枚举值（`llm_generate`、`llm_dsl_generate`）必须在统计报告和 `run summary` 中被旧逻辑安全忽略，不能导致字段解析异常。

### 6.3 降级保障

每一层升级都必须实现完整的降级路径：

```text
L3 失败/不可用 → 降级到 L2（LLM 仅已知类型生成）
L2 失败/不可用 → 降级到 L1（规则引擎 + 历史参数分布采样）
L1 失败/不可用 → 降级到 L0（原有规则引擎，行为完全不变）
```

---

## 七、配额补位的修正

当前 `_fill_gaps()` 是无信号的硬性填充，会污染"生成质量→通过率"的统计。建议分两步修正：

**第一步（配合 L1）**：补位候选也使用历史参数分布采样，而不是固定值

**第二步（配合 L2）**：当 LLM 已经产出足够候选时，彻底取消配额补位；当 LLM 未达到配额时，补位候选必须有市场信号依据（即至少有一个 `trigger_threshold` 被命中），纯无信号补位降为零

```python
def _fill_gaps(self, snapshot, current_candidates, require_signal: bool = False):
    if require_signal:
        # 只补有信号依据的类型缺口，无信号的类型缺口不补位
        eligible_types = [
            t for t in preferred_types
            if self._has_market_signal_for_type(snapshot, t)
        ]
    else:
        eligible_types = preferred_types
    ...
```

---

## 八、多重检验的适配

三层升级引入了更多候选来源，这会改变 multiple-testing 的修正需求：

| 候选来源 | 建议的 multiple-testing 权重 |
|---------|--------------------------|
| 规则批次信号触发（L0） | 基准（当前 Gate-3 已覆盖） |
| 规则变体扩展 | 基准 × 1.2（变体相关性高） |
| 配额补位 | 基准 × 1.5（无信号噪声更高） |
| LLM 已知类型（L2） | 基准（如通过率数据充分后可降低） |
| LLM 新逻辑/DSL（L3） | 基准 × 1.3（额外参数敏感性惩罚） |

具体实现：在 `submission_gate.py` 中，对候选的 `generation_reason.kind` 字段做加权修正，调整 `deflated_sharpe_ratio` 和 `PBO` 的修正力度。

### 8.1 本轮新增：attempt 语义必须拆分

当前代码审计表明，`attempt_count` 的数值惩罚本身不算特别重，但其来源语义有偏：工厂 autonomy 阶段的总尝试数可能被回写到单个候选上，后续 gate 再取 `factory/task/external` 的最大值，这会让单个候选承担超出其本地搜索过程的统计负担。

因此必须修订为：

1. `candidate_local_attempt_count`：当前 candidate 在本任务中的真实生成/修订尝试数
2. `task_local_attempt_count`：当前 task 的尝试数
3. `cohort_effective_trials`：进入 DSR/PBO/RC/SPA 的同类候选有效试验数
4. `factory_global_attempt_count`：仅用于观测，不直接进入单 candidate trade gate 阈值

`submission_gate.py` 的 penalty 逻辑只允许使用前 3 项，不再直接使用工厂全局最大值。

### 8.2 本轮新增：formal multiple-testing 的 cohort 口径

当前 registry 需要继续从“lineage + tested object”主导，升级成更贴近真实 alpha 竞争集的 cohort 口径：

```text
family
× target universe / target pool
× holding bucket
× generator_mode
× validation profile
× lineage / tested-object bucket
```

原因：

1. 同一单标的 trade rule 和同一多标的 quality factor 不应共享几乎同一套 effective trials
2. 当前真实运行中大量候选是单标的 bulk target-only 策略，若直接并入宽口径 family registry，会造成 formal gate 与真实交易对象脱节
3. 只有 cohort 口径收紧后，DSR / PBO / RC / SPA 才能更真实反映“这类策略是否已被过度搜索”
4. 当前 strict gate 中的 trade primary 与 supplemental statistical gate 不能长期保持“前者偏宽、后者只给 warning”的结构；incubation 与 live gate 的阻断口径需要前移对齐

---

## 九、实施节奏

### 阶段一（第 1-2 周）：L1 上线 + 配额补位第一步修正

交付：
1. `ParameterDistributionRegistry` 实现与单元测试
2. `_varied_defaults()` 接入历史参数分布采样
3. 配额补位改为使用历史参数分布
4. `run summary` 新增"历史引导采样命中率"字段

前提：不依赖 LLM 服务，纯 DB 查询，风险极低。

### 阶段二（第 3-5 周）：L2 上线 + 统计监控

交付：
1. `LLMCandidateGenerator` 实现，含 Prompt 设计与 JSON Schema 校验
2. `StrategySpawner.spawn()` 接入 `llm_generator` 可选参数
3. `generation_reason.kind` 枚举扩展 + `run summary` 分源统计
4. hypothesis artifact 持久化与 replay / compile 路径
5. `attempt_count` 语义拆分与 cohort-trials registry 接入
6. LLM 候选 vs 规则候选的通过率对比看板

前提：需要 LLM API 可用。L2 通过率未达标时，不允许升级到 L3。

### 阶段三（第 6-10 周）：L3 开放型候选实验

交付：
1. DSL 候选 JSON Schema 定义
2. `_validate_llm_dsl_candidate()` 实现
3. L3 候选配额控制（每轮 ≤5 个）
4. Gate-3 的 L3 额外稳健性惩罚实现
5. `validation_grade / trade_density / perturbation_stability` 的 strict incubation 阻断前移
6. `position_model / cost_sensitivity_grid / capacity_assumption / holding_rationale` 的回测真实执行
7. 新类型沉淀的人工归纳流程定义

前提：L2 候选通过率稳定高于规则候选后才开启。

---

## 十、与现有架构优化方案的关系

本方案聚焦于**候选生成引擎**的 AI 能力升级，是 `策略工厂架构优化方案-2026-04-08.md` 中"研究平面独立化（P3）"的具体实施子方案。

两者的关系：

| 现有架构优化方案 | 本方案 |
|----------------|--------|
| P0：运行口径收口 | 无交集，独立推进 |
| P1：证据闭环硬化 | 本方案 L2/L3 候选必须附带更完整的证据字段 |
| P2：governed pool 硬化 | 本方案 L2/L3 的历史高通过率摘要依赖 governed pool 质量 |
| P3：研究平面独立化 | **本方案是 P3 的生成器侧具体实现** |
| P4：平台服务层显式化 | `ParameterDistributionRegistry` 可作为平台服务的早期雏形 |

**推荐执行顺序**：P0 → P1 → L1（与 P2 并行）→ L2（与 P3 并行）→ L3（P4 完成后）

原因：P1 证据闭环硬化完成后，历史高通过率摘要才有足够的可信度输入给 L2 的 LLM Prompt；在此之前强行上线 L2，LLM 获得的历史上下文是有偏差的，可能产出更多噪声候选。

---

## 十一、学术与业界验证摘要

### 11.1 L2/L3 的学术支撑

**核心依据**：Zhizhuo Kou et al.「Automate Strategy Finding with LLM in Quant Investment」（HKUST/PKU，EMNLP 2025 Findings，arXiv:2409.06289）

该论文提出了与本方案高度对齐的三层框架：
1. **Seed Alpha Factory（对应 L2/L3）**：LLM 从多模态金融文献和市场数据中挖掘因子公式，生成独立分类的候选 Alpha 池（对应本方案 governed candidate pool）
2. **Multi-Agent 评估层（对应 Autonomy 路径）**：多智能体系统根据市场状态和风险偏好对候选进行筛选
3. **动态权重优化（对应 Gate-3 后的排序层）**：DNN 对通过评估的 Alpha 进行权重分配

**实测结果**：在 A 股市场（SSE 50）2023-2024 年度实现约 53.17% 累计收益，显著优于传统基准。

**关键警示（该论文同时指出）**：
- LLM 策略在短期内可能表现优异，但在更长时间跨度和更广泛资产池中，其优势因**幸存者偏差**和**数据窥探偏差**而减弱
- 规则基准方法在特定市场机制下反而表现更稳定
- 这直接支持了本方案"L2 候选 OOS 表现月度监控，连续 2 个月低于规则候选时自动回退"的设计

### 11.2 L1 的技术支撑

**核心依据**：贝叶斯优化 + 稳定性约束在量化策略参数调优中的标准实践

贝叶斯优化（Optuna TPE / Hyperopt）在量化策略参数空间中已是成熟技术：
- 样本效率高，能利用历史评估结果指导后续搜索
- 必须配合 Walk-Forward 或 Purged K-Fold 使用，防止封闭式历史最优化偏差
- 需要引入稳定性约束（方差惩罚），防止在噪声金融数据中向较差参数区间反复震荡

**关键应用要求**：L1 的分布提取必须严格限定在样本外验证通过的候选，这与原始方案的描述不够明确，已在本文档修订版中补充。

### 11.3 PBO/多重检验的 LLM 场景适用性

**核心依据**：Bailey & López de Prado「The Probability of Backtest Overfitting」

PBO/CSCV 框架在 LLM 候选生成场景下明确适用，且有特殊要求：
- 必须记录所有尝试（包括 LLM 生成但被校验丢弃的候选），否则会严重低估过拟合风险（文件抽屉问题）
- 同一轮 LLM 生成的候选因共享相同上下文，不满足独立性假设，需要批次内相关性修正
- PBO 应作为**治理工具**而非质量证明，与本方案的使用定位一致

## 十二、不建议做的事

1. **不要在 L1 未验证前直接跳到 L2**：没有历史参数分布引导的 LLM 生成，很可能产出与现有规则候选高度相关的变体，不会带来真正的多样性提升

2. **不要取消 `_from_fear_greed / _from_factor_ic / _from_volatility / _from_fund_flow` 规则批次**：这四类触发信号是可解释、可审计的，是 L3 探索型候选的对照基线，也是降级路径的最后保障

3. **不要把 LLM 候选的配额设定得过大**：L2 阶段 LLM 候选占总候选的比例不应超过 60%，保留规则候选作为对照

4. **不要跳过多重检验的权重适配**：新来源候选进入 Gate-3 时若不调整 PBO/DSR 修正力度，会系统性低估 AI 候选的过拟合风险

5. **不要把"LLM 候选产出数"作为优化目标**：正确的目标是"LLM 候选的 Gate-3 通过率"和"LLM 候选进入孵化后的有效信号积累率"

6. **不要在 `DslRuleStrategy.risk_rules` 未完整执行前上线 L3**：否则回测结果会系统性高估 DSL 候选的实际可行性，导致门禁失效

7. **不要跳过批次内相关性修正直接用原始 N 计入 PBO**：同一轮 LLM 生成的候选在 CSCV 路径中必须按等效独立数 K 处理（K = N / (1 + ρ\_mean)），否则 PBO 会被低估，导致系统性接受过拟合候选

8. **不要把 LLM 候选的"短期高 Sharpe"当作长期有效的证明**：学术研究证实 LLM 策略优势会随时间因数据窥探偏差而衰减，必须建立 OOS 月度对比监控，连续 2 个月低于规则候选时自动回退 L1

9. **不要继续把 feedback 只当节流器用**：如果 zero-signal / low-signal / evidence-debt 只能调 budget，不改变 family / horizon / universe 路由，那么系统仍会在旧搜索域里高频空转

10. **不要让 `validation_grade=D` 长期停留在“可 warning 放行”状态**：这会让研究样本与可孵化候选混在一起，削弱 governed pool 与 promotion review 的语义

---

## 十三、验收指标汇总

### 阶段一（L1）验收

| 指标 | 目标 |
|------|------|
| 历史分布采样命中率 | 有历史数据的类型 ≥ 90% 使用分布采样 |
| 参数集中度 | 高通过率候选的参数分布标准差 < 历史固定锚点集中度 |
| 规则候选平均 Sharpe | 相比纯随机抖动基线提升 ≥ 5% |

### 阶段二（L2）验收

| 指标 | 目标 |
|------|------|
| LLM 候选 Gate-3 通过率 | ≥ 规则候选通过率 |
| LLM 候选平均 Sharpe | ≥ 规则候选平均 Sharpe |
| LLM 候选在孵化中零信号率 | < 规则候选的零信号率 |
| LLM 调用失败自动降级率 | 100%（不允许因 LLM 失败导致工厂运行中断） |
| hypothesis artifact 完整率 | ≥ 95%（不允许只落最终 DSL，不落高层假设对象） |
| 候选多样性提升 | LLM 候选的 `family × holding bucket × target universe` 组合数显著高于规则候选 |
| refresh 依赖下降 | `spawn_revision_from_existing + refresh_existing` 占 LLM 候选比例低于规则候选 |

### 阶段三（L3）验收

| 指标 | 目标 |
|------|------|
| L3 候选 Gate-3 通过率 | ≥ L2 候选通过率 × 0.8（允许额外惩罚后的合理下浮） |
| 新类型发现率 | 每 30 轮至少有 1 个 `llm_defined` 候选被归纳为新命名类型 |
| L3 候选多重检验修正后 DSR | > 0（不允许系统性负 DSR） |
| 经济完整性执行率 | `holding_rationale / cost_sensitivity_grid / position_model / capacity_assumption` 均被回测真实执行，而不是只落 metadata |

### 跨阶段治理验收

| 指标 | 目标 |
|------|------|
| 候选多样性提升 | `LLM 候选 unique family × holding bucket × target universe` 组合数 ≥ 规则候选基线 × 1.5 |
| refresh 路径依赖下降 | `refresh_existing + spawn_revision_from_existing` 占比 ≤ 40% |
| forward coverage 改善 | 新候选 30 天内 `forward window coverage` ≥ 规则候选基线 |
| zero-signal 改善 | 新候选 `zero-signal ratio` 持续低于当前研究样本基线 |
| 经济默认值依赖下降 | 非 `single_name/equal_weight + fixed cost` 默认假设候选占比在 L3 阶段 ≥ 50% |
| strict gate 对齐 | `validation_grade=D` 候选在 strict incubation / governed promotion 路径的通过率最终收敛到 0 |
