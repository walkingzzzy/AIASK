# AKShare MCP 161工具对话式测试清单（2026-04-21）

## 范围与结论

- [x] 本清单基于当前对话线程中的真实 `mcp__akshare_mcp__` 调用，不是本地脚本代跑。
- [x] 运行时工具面已完成 `161/161` 实测覆盖。
- [x] 文档基线仍为 `155`，运行时基线为 `161`，存在 `6` 个 `runtime-drift`。
- [x] 测试时间为 `2026-04-21`。
- [x] 日线、涨停统计、涨停列表类结果主要核验到 `2026-04-20`。
- [x] 分钟线、盘口、成交明细、指数快照类结果主要核验到 `2026-04-21`。

## 覆盖结论

- [x] 已完成 manager 类、workflow 类、market 类、finance 类、portfolio 类、quant 类、alerts 类、research 类、decision 类、skills 类、data-sync 类、tracking/validation 类的真实 MCP 调用测试。
- [x] 已完成正例覆盖。
- [x] 已完成负例覆盖。
- [x] 已完成可恢复写路径覆盖：`execution_manager`、`data_warmup`、`watchlist_manager`、`paper_trading_manager`、`user_manager`、`update_user_profile`。
- [x] `execution_manager` 配置已恢复。
- [x] `data_warmup` 已清空。
- [x] 临时 watchlist 分组已删除，`default` 分组已恢复。
- [x] 模拟盘挂单已撤销，无 pending order 残留。

## 文档漂移

- [ ] 更新 [171工具全量对话式深度测试任务.md](/Users/mac/Desktop/股票/docs/171工具全量对话式深度测试任务.md) 中的工具总数：`155 -> 161`
- [ ] 将以下运行时已存在但文档未覆盖的工具补入文档：
  - `analyze_stock_product_workflow`
  - `calculate_stop_levels`
  - `check_db_freshness`
  - `generate_trade_plan`
  - `get_key_levels`
  - `sync_stale_klines`

## P0-P1 缺陷

- [ ] `analyze_stock_product_workflow(code="BAD")` 直接报 `INTERNAL_ERROR`，错误为 `normalize_code is not defined`
- [ ] `options_manager.calculate_greeks` 忽略传入参数，输出被默认值覆盖
- [ ] `get_limit_up_statistics(date="2026-99-99")` 直接抛未处理异常：`unsupported operand type(s) for -: 'NoneType' and 'datetime.timedelta'`
- [ ] `get_limit_up_stocks(date="2026-99-99")` 直接抛未处理异常：`unsupported operand type(s) for -: 'NoneType' and 'datetime.timedelta'`
- [ ] `create_indicator_alert` 的 `alert_id` 为确定性拼接，存在覆盖历史同 key 告警的风险
- [ ] `create_combo_alert` 接受非法 `logic="XOR"`，且当前 MCP 没有 combo alert 删除路径
- [ ] `get_tool_contract("generate_trade_plan")` 返回 `confirmation_required=true`，但真实执行时没有任何确认门禁

## 坏代码成功包装问题

- [ ] `get_stock_news("BAD")` 返回无关新闻而不是拒绝
- [ ] `sync_kline_data("BAD")` 成功回退并返回数据
- [ ] `analyze_stock_workflow("BAD")` 返回 `success=true`，且 `stock_profile.found=true`
- [ ] `get_investment_analysis("BAD")` 返回成功结果
- [ ] `run_simple_backtest("BAD")` 返回成功结果
- [ ] `smart_stock_diagnosis("BAD")` 返回成功结果
- [ ] `scenario_dcf_valuation("BAD")` 返回成功结果
- [ ] `get_realtime_quote("BAD")` 返回成功结果
- [ ] `get_key_levels("BAD")` 返回伪支撑/阻力位
- [ ] `generate_trade_plan("BAD")` 返回完整交易计划
- [ ] `get_north_fund_holding("BAD")` 返回空结果但 `success=true`
- [ ] `get_research_summary("BAD")` 返回空摘要但 `success=true`
- [ ] `get_historical_valuation("BAD")` 返回空历史但 `success=true`
- [ ] `get_signal_hit_rate("BAD")` 返回样本统计但 `success=true`
- [ ] `get_unified_decision("BAD")` 返回成功决策对象
- [ ] `fuse_decision_payload("BAD")` 返回成功融合结果
- [ ] `analyze_research_report("BAD")` 返回空研报分析但 `success=true`
- [ ] `analyze_stock_sentiment("BAD")` 返回完整情绪评分但 `success=true`
- [ ] `build_stock_context("BAD")` 返回完整上下文且含伪价格/技术/风险信息
- [ ] `build_quant_context("BAD")` 返回完整量化画像和 OOS 结果
- [ ] `build_event_context("BAD")` 返回无关新闻构造出的事件上下文
- [ ] `get_trade_details("BAD")` 在非交易时段被“当前非交易时段”提示掩盖，没有做代码校验

## 参数合同与输入校验缺陷

- [ ] `calculate_stop_levels(direction="sideways")` 未拒绝非法方向
- [ ] `calculate_technical_indicators(indicators=["XYZ"])` 未拒绝非法指标
- [ ] `check_candlestick_patterns(code="BAD")` 未做干净拒绝
- [ ] `check_db_freshness(max_stale_days=-1)` 未拒绝负值
- [ ] `get_margin_ranking(top_n=-1)` 未拒绝负值
- [ ] `get_north_fund_top(top_n=-1)` 未拒绝负值
- [ ] `get_block_trades(date="2026-99-99")` 未做合同化拒绝
- [ ] `search_stocks(keyword="")` 未拒绝空关键词
- [ ] `stress_test_portfolio(holdings=[])` 未合同化拒绝
- [ ] `factor_candidate_workflow(task="bad_task")` 未合同化拒绝
- [ ] `update_user_profile` 对超范围值采用 silent clip 并写入快照，而不是拒绝
- [ ] `sync_stale_klines(max_stale_days=-1)` 未拒绝负值
- [ ] `get_ipo_info(ipo_type=-1)` 返回空数据但 `success=true`
- [ ] `governance_check_workflow(target_type="bad_type")` 未拒绝非法类型
- [ ] `get_factor_library("bad_category")` 返回空列表但 `success=true`
- [ ] `get_conditional_returns("not_json")` 将坏 JSON 当作条件文本处理
- [ ] `get_market_news(limit=-1)` 未拒绝负值
- [ ] `optimize_portfolio(stocks=[])` 返回 `float division by zero`
- [ ] `get_kline_data(period="2m")` 没有拒绝非法周期，反而回退成日线并返回 `success=true`

## 管理器与编排行为缺陷

- [ ] `watchlist_manager.create_group` 未正确保存传入的 `name` 和 `color`
- [ ] `watchlist_manager.add_stocks` 向临时分组添加股票时，表现为把股票从 `default` 分组挪走而不是复制
- [ ] `parse_selection_query("随便来点厉害股票")` 在零条件抽取下仍返回 `parsed=true`
- [ ] `strategy_review_workflow` 对不存在策略 ID 仍返回顶层 `success=true`，只在内部步骤标记 `strategy not found`
- [ ] `get_stock_capital("BAD")` 失败信息是通用 `所有数据源均失败`，不是清晰的无效代码错误
- [ ] `get_cb_info("BAD")` 失败信息是通用后端失败，不是清晰的无效代码错误

## 估值与量化质量问题

- [ ] `dcf_valuation("600519")` 走了保守默认营收/利润率降级，结果可信度可疑
- [ ] `scenario_dcf_valuation` 存在同类默认值降级问题
- [ ] `factor_robustness_check` 坏参数直接泄露原始 `int()` 解析错误

## 告警存储现状

- [ ] 当前告警存储中已存在非法或脏数据，需要人工排查：
  - `alert__rsi_>` 的 `code` 为空
  - `combo_mcp_dialog_bad_combo_20260421` 的 `logic="XOR"`
- [ ] 当前 MCP 仅能删除 indicator alert，不能删除 combo alert

## 残留状态

- [ ] `update_user_profile` 虽已把最新快照恢复到原值，但快照历史是 append-only，无法无痕回滚
- [ ] `update_user_profile` 期间 `snapshot_count` 从 `36` 增加到 `39`
- [ ] 组合告警仍残留：
  - `combo_mcp_dialog_combo_20260421`
  - `combo_mcp_dialog_bad_combo_20260421`
- [ ] 历史工件/记录仍残留：
  - `art_mcp_data_quality_20260421`
  - `factor_llm_1776750970_cd358279`
  - `art_mcp_backtest_20260421`
  - `run-ca1ad8d751c0`
  - `run-1804ef069cfe`
  - backtest record `2aaf7d98`
- [ ] 模拟盘已无待成交订单，但撤单记录 `77718` 仍会留在历史中

## 已确认的良性行为

- [x] `get_index_quote("BAD")` 能正确拒绝
- [x] `get_kline("BAD")` 能正确拒绝
- [x] `get_minute_kline(period="2m")` 能正确拒绝
- [x] `get_order_book("BAD")` 能正确拒绝
- [x] `get_trading_dates(start_date="2026-01-01")` 能正确拒绝非法日期格式
- [x] `limit_up_manager(action="bad_action")` 能正确拒绝
- [x] `check_all_alerts(alert_type="bad_type")` 能正确拒绝
- [x] `quant_manager(action="bad_action")` 能正确拒绝
- [x] `alerts_manager(action="bad_action")` 能正确拒绝
- [x] `backtest_manager(action="bad_action")` 能正确拒绝
- [x] `benchmark_manager(action="bad_action")` 能正确拒绝
- [x] `data_validation(action="bad_action")` 能正确拒绝
- [x] `experiment_tracker(action="bad_action")` 能正确拒绝

## 建议处理顺序

- [ ] 先修 `P0-P1`：运行期异常、参数绑定错误、告警 ID 冲突、合同门禁失效
- [ ] 再修“坏代码成功包装”链路：统一 stock code normalize / validate / fail-fast
- [ ] 再修输入校验：枚举、日期、负数、空列表、坏 JSON
- [ ] 最后清理残留状态与文档漂移

