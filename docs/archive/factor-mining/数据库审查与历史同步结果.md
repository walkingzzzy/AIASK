# 数据库审查与历史同步结果

日期：2026-03-23

## 结论

- 需要审查数据库，也需要做历史数据同步。
- 本次已完成策略工厂核心市场数据的定向回填，主流程对外部指数源的依赖已显著下降。
- 当前 `fear_greed` 主链路已经可以直接依赖数据库中的指数日线数据运行。

## 审查前

### 核心指数日线

- `sh000001`: 0
- `sh000300`: 0
- `sh000016`: 0
- `sh000905`: 0
- `sz399001`: 0
- `sz399005`: 0
- `sz399006`: 0

### 核心股票日线

- `600519`: 426 条，区间 `2024-06-16` 到 `2026-03-18`
- `000858`: 174 条，区间 `2025-06-25` 到 `2026-03-15`
- `601318`: 165 条，区间 `2025-06-25` 到 `2026-03-02`
- `000001`: 262 条，区间 `2025-02-19` 到 `2026-03-18`

## 本次同步

同步范围：

- 核心指数：`sh000001`、`sh000300`、`sh000016`、`sh000905`、`sz399001`、`sz399005`、`sz399006`
- 核心股票：`600519`、`000858`、`601318`、`000001`
- 历史窗口：近 5 年
- 数据源：`Tushare Pro`
- 写入目标：`TimescaleDB.kline_1d`

同步结果：

- 指数回填：8470 行
- 股票回填：4840 行
- 指数失败数：0
- 股票失败数：0

## 审查后

以下代码均已补齐到约 5 年历史，最新日期到 `2026-03-19`：

- `sh000001`: 1210 条
- `sh000300`: 1210 条
- `sh000016`: 1210 条
- `sh000905`: 1210 条
- `sz399001`: 1210 条
- `sz399005`: 1210 条
- `sz399006`: 1210 条
- `600519`: 1210 条
- `000858`: 1210 条
- `601318`: 1210 条
- `000001`: 1210 条

## 运行验证

- 在人为打断外部指数拉取的情况下，`DataCollector` 仍可直接从数据库计算 `fear_greed`。
- 验证结果：`fear_greed_index=51`，`source_status=success`。

## 相关脚本

- 审查与回填脚本：
  [audit_sync_core_market_data.py](/Users/mac/Desktop/股票/scripts/audit_sync_core_market_data.py)

## 后续建议

- 将该脚本纳入运维日常，用于周期性补齐核心指数与核心股票历史。
- 下一阶段可扩展到：
  - active pool 涉及标的
  - 北向资金
  - 融资融券
  - 龙虎榜与板块级行情

## 第二轮扩展同步结果

### 审查结论

- 最新 `daily_snapshot.factor_research.active_candidate_pool` 为空，当前没有可额外据此扩展的候选标的池。
- `north_fund_flow` 原本只更新到 `2026-03-03`，存在明显滞后。
- 融资融券没有持久化表，属于数据链路缺失，不是简单“补历史”。

### 本次新增

- 在运行时 schema 中补充了以下表结构：
  - `north_fund_flow` 兼容字段迁移：`ggt_ss`、`ggt_sz`、`hgt`、`sgt`、`updated_at`
  - `margin_market_flow`
  - `margin_detail`
- 扩展脚本 [audit_sync_core_market_data.py](/Users/mac/Desktop/股票/scripts/audit_sync_core_market_data.py)，支持同步：
  - 北向资金近 N 天历史
  - 融资融券市场汇总
  - 融资融券个股明细

### 执行结果

- 北向资金：
  - 同步窗口：近 `365` 天
  - 写入行数：`235`
  - 最新日期：`2026-03-20`
- 融资融券：
  - 同步窗口：近 `60` 天
  - 覆盖交易日：`37`
  - `margin_market_flow`：`109` 行
  - `margin_detail`：`157492` 行
  - 失败交易日：`0`

### 当前状态

- `north_fund_flow`
  - `count=235`
  - 区间：`2025-03-24` 到 `2026-03-20`
- `margin_market_flow`
  - `count=109`
  - 区间：`2026-01-21` 到 `2026-03-20`
- `margin_detail`
  - `count=157492`
  - 区间：`2026-01-21` 到 `2026-03-20`

## 第三轮 DB-first 消费链改造结果

### 本轮目标

- 不只补数据，还要确认上层业务链路优先消费数据库。
- 避免 `daily_report`、`alternative_factors` 这类上层能力继续直接绕回外部 `fund_flow`。

### 本轮完成

- 新增数据库读取接口：
  - `get_north_fund_history(days, end_date)`
- 扩展已有接口：
  - `get_recent_north_fund_summary(..., end_date=...)`
- 上层消费改造：
  - `generate_daily_report()` 的 `capital_flow` 改为按 `report_date` 优先读取 `north_fund_flow`
  - `_compute_alternative_factors_for_code()` 的北向资金分量改为优先读取 `db.get_north_fund_history`

### 测试结果

- 新增测试：
  - [test_daily_report_db_first.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_daily_report_db_first.py)
  - [test_quant_mgr_helpers_db_first.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_quant_mgr_helpers_db_first.py)
- 联合回归结果：
  - `5 passed in 2.00s`

### 真实探针

- `db.get_north_fund_history(days=3, end_date='2026-03-22')`
  - 返回最新两日：
    - `2026-03-20`：`north_money=339088.06`
    - `2026-03-19`：`north_money=274632.03`
- `generate_daily_report('2026-03-22')`
  - `capital_flow.north_fund.source = north_fund_flow`
  - `capital_flow.north_fund.trade_date = 2026-03-20`
  - `capital_flow.main_fund.source = north_fund_flow`
- `_compute_alternative_factors_for_code('600519')`
  - `source_chain` 已包含 `db.get_north_fund_history`
  - `north_flow_score_raw` 已由数据库历史计算

### 当前结论

- 北向资金相关链路已经从“数据已落库但上层还常走外部接口”推进到“日报与另类因子链路真正 DB-first”。
- `insight_manager` 因为复用 `generate_daily_report()`，已经自动继承本轮改造成果。

## 第四轮工具层直连数据库验证

### 本轮目标

- 把 `fund_flow` 工具本身从“主要外部拉取”升级为“数据库优先，外部回退”。
- 避免未来其他 manager 直接调用 `get_north_fund()` / `get_margin_data()` 时，又绕回 AKShare / 东财。

### 本轮新增

- 数据库接口：
  - `get_margin_market_history(days, end_date)`
  - `get_margin_detail_latest(limit, ts_code, end_date)`
- 工具层改造：
  - `get_north_fund()` 现在优先读取 `north_fund_flow`
  - `get_margin_data()` 现在优先读取：
    - `margin_market_flow`
    - `margin_detail`

### 测试结果

- 新增测试：
  - [test_fund_flow_db_first.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_fund_flow_db_first.py)
- 联合回归：
  - `9 passed in 1.66s`

### 真实探针

- `get_north_fund(3)`
  - `success=true`
  - `source=north_fund_flow`
  - 最新样本日期：`2026-03-20`
- `get_margin_data(days=3)`
  - `success=true`
  - `source=margin_market_flow`
  - 最新样本日期：`2026-03-20`
- `get_margin_data('600519', days=3)`
  - `success=true`
  - `source=margin_detail`
  - 最新样本日期：`2026-03-20`

### 当前结论更新

- 北向资金、融资融券这两条核心资金流链路，已经从“数据落库”推进到“工具层直接消费数据库”。
- 后续即使别的上层模块没有单独做 DB-first 适配，只要复用这两个基础工具，也能默认获得数据库优先行为。

## 第五轮融资融券排行 DB-first 验证

### 本轮目标

- 把 `get_margin_ranking()` 也切到数据库优先。
- 补齐融资融券“摘要 / 明细 / 排行”三段链路的最后一个缺口。

### 本轮新增

- 数据库接口：
  - `get_margin_ranking(top_n, sort_by, end_date)`
- 工具层改造：
  - `get_margin_ranking()` 现在优先读取 `margin_detail_ranking`
  - 东财 datacenter 只保留 fallback

### 测试结果

- `test_fund_flow_db_first.py` 已补充排行测试
- 联合回归：
  - `11 passed in 1.62s`

### 真实探针

- `get_margin_ranking(top_n=5, sort_by='balance')`
  - `success=true`
  - `source=margin_detail_ranking`
  - 最新样本日期：`2026-03-20`
- `get_margin_ranking(top_n=5, sort_by='buy')`
  - `success=true`
  - `source=margin_detail_ranking`
  - 最新样本日期：`2026-03-20`

### 当前结论更新

- 融资融券三条常用入口：
  - `get_margin_data(days=...)`
  - `get_margin_data(stock_code=...)`
  - `get_margin_ranking(...)`
  现在都已经是数据库优先。
- 当前剩余问题不是“资金流工具是否还依赖外部接口”，而是个别证券名称映射仍不完整，导致排行返回里偶发 `name=""`。

## 第六轮名称映射与同步入口验证

### 本轮目标

- 消除排行里 ETF/场内基金名称缺失。

## 第七轮 factor_context 上下文入库结果

### 本轮目标

- 把因子挖掘真正依赖的文本/资金流上下文正式喂入数据库，而不只是代码层声明 DB-first。
- 覆盖四类数据：
  - `news`
  - `notice`
  - `research`
  - `stock_fund_flow`

### 本轮新增

- 新增脚本：
  - [audit_sync_factor_context_data.py](/Users/mac/Desktop/股票/scripts/audit_sync_factor_context_data.py)
- 新增持久化目标：
  - `vector_documents`
    - `doc_type in ('news', 'notice', 'research')`
  - `research_reports`
  - `stock_fund_flow`
- `data_sync_manager` 已支持 `type='factor_context'`

### 第一次真实同步（7 日窗口）

执行参数：

- `codes=600519,000858`
- `news_days=7`
- `notice_days=7`
- `item_limit=3`

执行前：

- `vector_documents_total=0`
- `research_reports=0`
- `stock_fund_flow=0`

执行结果：

- `news_saved=3`
- `notice_saved=0`
- `research_saved=6`
- `research_docs_saved=6`
- `fund_flow_saved=0`

第一次执行后：

- `vector_documents_total=9`
- `vector_documents_news=3`
- `vector_documents_notice=0`
- `vector_documents_research=6`
- `research_reports=6`
- `stock_fund_flow=0`

### 资金流问题定位与修复

定位结果：

- `get_stock_fund_flow('600519', prefer_db=False)` 当时返回：
  - `error='未获取到资金流向数据'`
- 根因不是数据库问题，而是工具层此前主要依赖东财 `push2/fflow/kline/get`
- 当日该接口对目标股票返回空 `klines`，导致 `stock_fund_flow` 无法入库

修复内容：

- `get_stock_fund_flow()` 已新增兜底链路：
  - `DB -> Tushare Pro moneyflow -> Eastmoney`
- 真实探针：
  - `600519` 返回 `source=tushare.moneyflow`
  - `000858` 返回 `source=tushare.moneyflow`

### 第二次真实同步（补资金流）

再次执行 7 日窗口同步后：

- `fund_flow_saved=2`
- 脚本内统计：
  - `stock_fund_flow=2`
  - `stock_fund_flow_latest_trade_date=2026-03-23`

### 公告窗口扩展验证（30 日）

执行参数：

- `codes=600519,000858`
- `news_days=30`
- `notice_days=30`
- `item_limit=5`

执行结果：

- `notice_saved=3`
- `research_saved=4`
- `research_docs_saved=4`
- `fund_flow_saved=2`

最终库内状态：

- `vector_documents_total=16`
- `vector_documents_news=3`
- `vector_documents_notice=3`
- `vector_documents_research=10`
- `research_reports=10`
- `stock_fund_flow=2`

样本验证：

- 公告文档：
  - `600519 / 2026-03-14 / 贵州茅台关于高级管理人员被实施留置的公告`
  - `600519 / 2026-03-04 / 贵州茅台关于回购股份实施进展的公告`
  - `000858 / 2026-02-28 / 五粮液关于董事长被留置的公告`
- 个股资金流：
  - `000858 / 2026-03-23 / main_net_inflow=-10894.41 / source=tushare.moneyflow`
  - `600519 / 2026-03-23 / main_net_inflow=9577.61 / source=tushare.moneyflow`

### 当前结论更新

- `factor_context` 已不再只是“代码支持 DB-first”，而是完成了真实样本入库验证。
- `factor_prompt_builder` 后续读取新闻/公告/研报/个股资金流时，已具备可消费的库内样本。
- 当前剩余问题不在同步入口本身，而在样本覆盖范围还偏小：
  - 目前只定向回填了 `600519`、`000858`
  - 下一步应扩展到 active pool / representative universe / 工厂当日目标股票

## 第八轮 factor_context 动态股票池扩展结果

### 本轮目标

- 把 `factor_context` 同步范围从固定代码扩到：
  - `active_pool`
  - `representative universe`
  - `当日 strategy_factory 研究任务目标股票`

### 本轮范围解析规则

- `representative`
  - 来源：`REPRESENTATIVE_STOCKS`
- `active_pool`
  - 来源：`daily_snapshot.factor_research.active_candidate_pool.top_candidates`
  - 通过 `artifact_id -> factor_candidate_record.codes` 反查股票代码
- `factory_targets`
  - 来源：`strategy_task_runs`
  - 解析：
    - `payload.research_task.target_symbols`
    - `payload.event_context.target_symbols`

默认 scope：

- `explicit,representative,active_pool,factory_targets`

### 真实范围探针

执行时点探针结果：

- `representative_codes=10`
- `active_pool.codes=0`
- `factory_targets.codes=8`
- 去重后 `resolved_codes=17`

本次实际解析出的代码：

- `600519`
- `000858`
- `601318`
- `600036`
- `000333`
- `002415`
- `600276`
- `601012`
- `300750`
- `000001`
- `601398`
- `601857`
- `601288`
- `601939`
- `600941`
- `601166`
- `600000`

说明：

- 当前最新 snapshot 日期是 `2026-03-22`
- 当前 `active_pool.artifact_ids` 为空，因此本轮新增股票主要来自 `factory_targets`

### 最小窗口真实预热

执行命令：

- [audit_sync_factor_context_data.py](/Users/mac/Desktop/股票/scripts/audit_sync_factor_context_data.py)
  - `--news-days 1`
  - `--notice-days 1`
  - `--item-limit 1`

执行结果：

- `news_saved=15`
- `notice_saved=0`
- `research_saved=15`
- `research_docs_saved=15`
- `fund_flow_saved=17`
- `error_count=0`

### 扩展预热后的库状态

- `vector_documents_total=46`
- `vector_documents_news=18`
- `vector_documents_notice=3`
- `vector_documents_research=25`
- `research_reports=25`
- `stock_fund_flow=17`

### 当前结论更新

- `factor_context` 已具备动态股票池能力，不再依赖固定代码名单。
- 预热范围已经与工厂当前研究任务形成联动。
- 下一步如果要继续增强覆盖度，重点不再是脚本能力，而是：
  - 让最新 snapshot 持续产出 `active_pool.artifact_ids`
  - 把预热周期真正接入日常调度

## 第九轮 startup warmup 自动补 schedule 验证

### 本轮目标

- 解决“数据库里没有 `sync_schedules` 时，startup warmup 直接 skipped”的问题。
- 让 `quant_manager` / `strategy_factory` 的启动前预热真正具备自恢复能力。

### 本轮新增

- `run_runtime_data_warmup(...)` 已新增自动补 schedule 逻辑：
  - 若指定 `task_type` 没有任何启用中的 schedule
  - 则自动创建默认日调度，并走正式 `run_due_schedules(...)` 执行
- 自动 schedule ID：
  - `schedule_runtime_core_market`
  - `schedule_runtime_factor_context`

### 测试结果

- `test_run_runtime_data_warmup_bootstraps_missing_schedules`
  - 已覆盖：
    - 缺省 schedule 自动创建
    - `core_market,factor_context` 双任务类型一起 bootstrap
    - `factor_context` 默认 `scope_sources` 参数固化
- 联合回归：
  - `11 passed`
  - `15 passed`

### 真实库验证

库内当前已存在：

- `schedule_runtime_factor_context`
- `schedule_runtime_core_market`

其中 `schedule_runtime_factor_context.params` 已固化为：

- `news_days=30`
- `notice_days=30`
- `item_limit=10`
- `scope_sources=explicit,representative,active_pool,factory_targets`
- `task_run_limit=50`
- `active_pool_limit=12`

### 强制执行探针

执行：

- `run_runtime_data_warmup(task_type='factor_context', force=True, source='runtime_probe_force')`

返回：

- `status=completed`
- `matched=1`
- `executed=1`
- `schedule_id=schedule_runtime_factor_context`
- `task_id=sync_factor_context_1774229360`

执行时的市场辅助统计：

- `vector_documents=389`
- `vector_documents_news=80`
- `vector_documents_notice=139`
- `vector_documents_research=170`
- `research_reports=170`
- `stock_fund_flow=17`

### 当前结论更新

- `factor_context` 不仅已经接入动态股票池，也已经接入了真实运行态日调度闭环。
- 当前若再次启动因子挖掘或策略工厂：
  - 没有 schedule 时，会自动补默认 schedule
  - 有 schedule 但未到期时，会正常 `skipped`
  - 强制模式下，会命中现有 schedule 执行

## 第七轮因子 Prompt 上下文 DB-first 审查（2026-03-23）

### 本轮目标

- 审查 `factor_prompt_builder` 相关上下文是否仍会直接打外部接口。
- 确认数据库里到底有没有可供因子挖掘直接消费的：
  - 新闻文本
  - 公告文本
  - 研报记录
  - 个股资金流

### 实际数据库审查结果

本轮对当前库内相关表做了直接核查，结果如下：

- `vector_documents`
  - `count=0`
- `research_reports`
  - `count=0`
- `stock_fund_flow`
  - 审查前不存在
  - 本轮已通过 schema 初始化创建
  - 创建后 `count=0`
- `north_fund_flow`
  - `count=235`

这次审查说明：

- 北向资金历史已经有库内数据。
- 但因子挖掘所需的新闻/公告/研报/个股资金流四类上下文，还没有形成可直接复用的实质数据底座。

### 本轮代码侧补强

- 已新增统一 DB-first 读取层：
  - `services/db_first_market_context.py`
- 已改造主消费链路：
  - `factor_prompt_builder.build_factor_mining_prompt()`
  - `_compute_alternative_factors_for_code()`
- 已改造工具入口：
  - `get_stock_news()`
  - `get_stock_notices()`
  - `get_research_reports()`
  - `get_stock_fund_flow()`

当前读取顺序已经统一为：

1. 先读 `vector_documents / research_reports / stock_fund_flow`
2. DB 无数据时，再回退外部接口

### schema 补充

本轮新增了 `stock_fund_flow` 表定义，字段包括：

- `code`
- `trade_date`
- `name`
- `main_net_inflow`
- `main_inflow_percent`
- `super_large_net_inflow`
- `large_net_inflow`
- `middle_net_inflow`
- `small_net_inflow`
- `source`
- `updated_at`

这一步的意义是把“个股资金流已被多个模块假定存在”的隐含依赖，正式补成可初始化的库表结构。

### 测试结果

- 定向回归：
  - [test_factor_llm_p0.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_factor_llm_p0.py)
  - [test_quant_mgr_helpers_db_first.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_quant_mgr_helpers_db_first.py)
  - [test_unified_decision_builders.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_unified_decision_builders.py)
- 结果：
  - `13 passed in 10.68s`

### 当前结论更新

- 现在的瓶颈已经不是“因子挖掘代码是否还默认走外部接口”，而是“这些文本/个股资金流数据是否已持续写入数据库”。
- 因子挖掘链路已具备 `DB-first + fallback` 能力。
- 下一阶段若要进一步降低出网依赖，应继续补齐：
  - `vector_documents` 的新闻/公告/研报写入
  - `research_reports` 的历史回填
  - `stock_fund_flow` 的同步入口与定时补数
- 让核心市场补数脚本可以通过 `data_sync_manager` 正式触发，而不是只靠手动执行脚本。

### 本轮新增

- 名称映射：
  - `get_stock_list_cached()` 现在合并 `stock_basic + fund_basic`
  - `get_name_map()` 已覆盖股票 + 场内基金/ETF
- 同步入口：
  - `data_sync_manager(action='sync', type='core_market')`
  - `data_sync_manager(action='schedule', type='core_market')`

### 测试结果

- 新增测试：
  - [test_market_name_map_fund_merge.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_market_name_map_fund_merge.py)
  - [test_data_sync_manager_core_market.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_data_sync_manager_core_market.py)
- 联合回归：
  - `13 passed in 1.80s`

### 真实探针

- 名称映射：
  - `name_map['511380'] = 可转债ETF博时`
  - `name_map['600519'] = 贵州茅台`
- `get_margin_ranking(top_n=5, sort_by='buy')`
  - 第一条已返回：
    - `code=511380`
    - `name=可转债ETF博时`
    - `source=margin_detail_ranking`
- `data_sync_manager` 实际同步链路（小窗口）：
  - 参数：
    - `years=1`
    - `codes=['600519']`
    - `north_days=5`
    - `margin_days=5`
  - 返回：
    - `exit_code=0`
    - `north_fund_rows=4`
    - `margin_market_rows=10`
    - `margin_detail_rows=14950`

### 当前结论更新

- `margin_ranking` 里的 ETF/基金名称缺失问题已经明显收敛。
- 核心市场同步现在不只是“有脚本”，而是已经可以走 `data_sync_manager` 的正式入口手动触发。
- 但要形成真正的自动定时执行闭环，还需要后续补 `schedule` 参数持久化和 worker 消费链路。

## 第七轮调度闭环验证

### 本轮目标

- 让 `sync_schedules.params` 不只是 schema 字段，而是真正被业务层消费。
- 让 `schedule -> due -> execute -> 回写 runtime` 形成完整闭环。
- 在真实数据库上验证 `core_market` 调度不是“只登记”，而是真的能执行并更新状态。

### 本轮新增

- `schema_market.py`
  - `sync_schedules` 已正式带 `params JSONB`
- `data_sync_manager.py`
  - `schedule` 现在会写入：
    - `params`
    - `next_run`
  - 新增：
    - `action='list_schedules'`
    - `action='run_due_schedules'`
  - `run_due_schedules` 支持：
    - 只执行到期 schedule
    - `force=true` 强制执行
    - `schedule_id` 定向执行，避免误跑其他历史调度
  - 执行完成后会回写：
    - `sync_schedules.last_run`
    - `sync_schedules.next_run`
  - `status` 额外返回：
    - `due_schedule_count`
    - `next_schedule_run`

### 测试结果

- 扩展测试：
  - [test_data_sync_manager_core_market.py](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_data_sync_manager_core_market.py)
- 覆盖点：
  - `core_market sync` 无 `codes` 直接执行
  - `schedule` 持久化 `params + next_run`
  - `run_due_schedules` 读取到期调度并触发 `_sync_core_market_now`
- 联合回归：
  - `15 passed in 1.65s`

### 真实探针

- 实际创建调度：
  - `type='core_market'`
  - `schedule='daily'`
  - `stock_codes=['600519']`
  - `years=1`
  - `north_days=5`
  - `margin_days=5`
- 实际执行：
  - `run_due_schedules(schedule_id=..., force=true, limit=1)`
- 返回结果：
  - `matched=1`
  - `executed=1`
  - `results.exit_code=0`
  - `sync_tasks.status=completed`
- 调度状态落库：
  - `sync_schedules.params.stock_codes = ['600519']`
  - `sync_schedules.last_run` 已更新
  - `sync_schedules.next_run` 已推进到下一日
- 本次真实执行摘要：
  - `north_fund_flow.count=235`
  - `margin_market_flow.count=109`
  - `margin_detail.count=157492`
  - `north_fund_rows=4`
  - `margin_market_rows=10`
  - `margin_detail_rows=14950`

### 当前结论更新

- `core_market` 现在已经具备“登记后可执行”的正式调度闭环，而不是只写一条 schedule 记录。
- 数据同步层继续向数据库驱动收敛，补数执行、运行状态、下次执行时间都能落库追踪。
- 这意味着后续无论是 AI 因子挖掘，还是策略工厂的数据预热，都可以直接挂到统一的 DB-first 调度入口上。

## 第八轮启动前 warmup 集成验证

### 本轮目标

- 把已经完成的 `core_market` 调度闭环真正接到运行时，而不是继续停留在手工调用层。
- 让策略工厂和 AI 因子挖掘在启动前优先消费同一条 DB-first warmup 入口。

### 本轮新增

- 运行时预热 helper
  - `data_sync_manager.py` 新增公开入口：
    - `run_runtime_data_warmup(...)`
  - 统一返回：
    - `status`
    - `matched`
    - `executed`
    - `failed`
    - `executed_task_ids`
- 策略工厂接入
  - `StrategyFactoryScheduler.run_once()` 现在会在 `collect` 之前执行 `warmup`
  - 运行结果新增：
    - `stages.warmup`
    - `summary.warmup_status`
    - `summary.warmup_matched`
    - `summary.warmup_executed`
    - `summary.warmup_failed`
- 因子挖掘接入
  - `quant_manager(action='llm_factor_mining')` 现在会在构建 prompt 前先执行 `startup_warmup`
  - 返回中新增：
    - `startup_warmup`
    - `params.startup_warmup_*`

### 测试结果

- `packages/akshare-mcp`
  - `test_data_sync_manager_core_market.py`
  - `test_factor_llm_p0.py`
  - 结果：`10 passed in 8.39s`
- `packages/strategy-factory`
  - `test_factory_scheduler_readiness_controls.py`
  - 结果：`7 passed in 2.10s`

### 真实探针

- 实际流程：
  1. 创建一条 `core_market` 调度
  2. 运行 `quant_manager(action='llm_factor_mining', startup_warmup=true, startup_warmup_force=true)`
  3. 验证 warmup 是否先执行，再进入因子候选生成
- 真实结果：
  - `startup_warmup.status=completed`
  - `startup_warmup.task_type=core_market`
  - `startup_warmup.matched=1`
  - `startup_warmup.executed=1`
  - `startup_warmup.failed=0`
  - `executed_task_ids=['sync_core_market_1774171789']`
  - 因子挖掘主链继续完成：
    - `quant_success=true`
    - `generation_mode=local_rule_fallback`
    - `candidate_count=2`

### 当前结论更新

- `core_market` 调度已经不仅能“登记并执行”，还已经进入真实运行时的启动前预热路径。
- 策略工厂与 AI 因子挖掘开始共用同一条 DB-first warmup 底座，后续再继续压缩外部接口依赖时，不需要重复造轮子。
