# 策略工厂四工厂 24h 全链路观察记录 (v8 — dispatch 多标的)

> session：`full24h_v8_20260612` | PID：24570 | 起始：2026-06-12 16:45
> 配置：dispatch 全市场子集 universe-limit=300 / claude-sonnet-4-6 / muyuan
>   / 因子挖掘每10轮 / observe 诊断交易开 / 全部死锁修复
> 逐轮：logs/strategy_factory_quality_sessions/full24h_v8_20260612/rounds/

## 里程碑：证据生产链端到端贯通（第 1 轮，2026-06-12 17:2x）

本轮是本次工作链的实质性突破——从"单标的死锁"走到"多标的证据链启动"。

### 关键对比（单标的 v6 → dispatch 300 v8）
| 指标 | v6 单标的(601288) | v8 dispatch 300 |
|---|---|---|
| 候选标的 | 清一色 601288 | 002027/600900/601088/300750/601398/688126... 多只跨板块 |
| spawned / dedup后 | 18 / 8 | 21 / **20**（去重不再砍半→候选不同质） |
| SignalTracker 信号/轮 | ~34（全卖出 -1） | **151**（方向多样,含买入） |
| **observe 诊断单** | **0**（死锁） | **6**（首次破 0！） |
| 策略工厂单轮耗时 | ~110s | 264s（dispatch 300 较重,可接受） |

### 意义
之前诊断的整条死锁链——信号生成断点 → observe 下单闸门 → 证据生产——现在**端到端贯通**：
```
多标的universe → 信号方向多样(151,不再全 -1) → 出现买入信号
  → observe 诊断纸面交易闸门放行 → 6 个 observe_paper_diagnostic 订单
  → (后续) 纸面成交 → 前向收益 → effective_n↑ → observe 可转 formal
```
observe 样本终于开始产生纸面诊断交易,这是积累前向证据、走向 formal 的**燃料**。

## 根因回顾（为何之前一直单标的）
- quality session `--codes` 默认 ["601288"],且 `_run_strategy_factory_once` 写死非 dispatch 模式
- 已修:新增 `--universe-limit` + dispatch 模式分支;v8 用 universe-limit=300

## 后续观察重点
1. **obs_diag 持续增长**:每轮 observe 诊断单是否累积(燃料持续供给)
2. **前向收益/effective_n**:诊断交易成交后,forward returns 能否随交易日开始累计(跨日慢变量)
3. **observe→formal 首次转正**:effective_n 达标后能否出现首个真实晋升
4. **单轮耗时 vs 规模**:300 只单轮约 50min(第1轮含因子挖掘);评估是否可上全 5000
5. **因子 active 池**:dispatch 下因子超市是否保持健康

## 规模决策
300 只验证已证明:多标的→证据链启动成功。下一步可评估放大到全 5217 只
(需权衡单轮耗时——300只策略工厂阶段 264s,5000只可能 1+ 小时/轮)。

---
（24h 运行中,持续追加）
