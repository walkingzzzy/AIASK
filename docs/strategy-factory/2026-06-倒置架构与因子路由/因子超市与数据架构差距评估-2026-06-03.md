# 因子超市与数据架构 · 愿景对照现状差距评估

日期：2026-06-03
版本：FACTOR-SUPERMARKET-GAP-V1
产生方式：sequential-thinking MCP 深度分析（5 轮）+ 逐函数代码勘察。
关联：`策略工厂Stock-First多策略路由方案-2026-06-03.md`、`策略工厂倒置架构设计方案-2026-06-02.md`

---

## 1. 一句话结论

你描述的"因子超市 + AI 流水线 + 残酷质检 + 数据湖"愿景里，
**约 90% 的软件能力已经存在**（尤其"残酷质检"是本项目最成熟的部分）。
真正的缺口只有两个：
- （单点增量）**行业+市值中性化**未见明确实现；
- （规模问题，非功能问题）**存储是单体 SQLite，无数据湖分层**。

**建议**：现阶段（命中率尚未验证、observe farm 样本不足）**不要**照技术栈清单
重建五件套数据湖（过早优化）。先用已有的残酷质检跑出"有 skill 的因子"，
验证方向；存储按实测瓶颈**触发式演进**，且优先轻量的 DuckDB+Parquet。

---

## 2. 能力差距图（逐条 ✅/🟡/❌ + 代码落点）

### A. 标准化因子超市

| 愿景能力 | 现状 | 代码落点 |
|---|---|---|
| 每日收盘后自动计算更新因子 | ✅ | `FactorScheduler`（每日 18:00 CST）→ `batch_compute_factors` |
| 横截面因子 + Rank IC 计算并入库 | ✅ | `handle_batch_compute_factors`（cross-sectional Rank IC，persist `factor_ic_history`/`factor_values`） |
| 前复权（除权除息） | ✅ | backtest `adjust="qfq"` |
| 极值处理（MAD / 3σ） | ✅ | `multi_factor.mad_normalize`（中位数绝对偏差×1.4826+clip）、`z_score`（3σ clip） |
| 缺失值前值填充（forward fill） | ✅ | 多处 `fillna`/`ffill`（signal_dsl / technical_analysis / strategy_dsl runtime） |
| 横截面标准化（Z-Score） | ✅ | `multi_factor.z_score` / `z_score_rolling` |
| **行业+市值中性化（回归取残差）** | 🟡 **未见明确实现** | 仅见横截面 z_score；未见对 Size/行业虚拟变量回归取残差 |
| 因子货架（存储） | ✅ | `factor_ic_history` / `factor_values` / `factor_pool_active` 表 |
| **因子字典（定义/公式/版本/更新时间）** | 🟡 零散 | 因子定义散落在 `SUPPORTED_FACTORS` / DSL，未编目成显式字典 |

### B. AI 研发流水线

| 愿景设想 | 现状（走了另一条路） | 说明 |
|---|---|---|
| AI 写 Python（vectorbt）代码 | 🟡 **不是**。LLM 产出**结构化 DSL** | `compile_strategy_blueprint` / `factor_candidate_compiler`（白名单算子 `SAFE_FUNCTION_ENV`） |
| 代码沙箱防恶意代码/死循环 | ✅ **从根上规避** | DSL 只执行白名单算子，不执行任意 Python → 无 RCE 风险，无需沙箱 |

> 关键澄清：你设想的"AI写代码+沙箱"和本项目"AI产DSL+编译"是两条路。
> **后者已实现且更安全**。若坚持要 vectorbt 自由代码，那是另一套，且引入 RCE 风险，
> 需 RestrictedPython/容器隔离 —— 不建议，除非有明确的"自由形态策略"需求。

### C. 残酷质检与淘汰（本项目最成熟的部分，✅ 几乎全有）

| 愿景能力 | 现状 | 代码落点 |
|---|---|---|
| IC / Rank IC（斯皮尔曼） | ✅ | `_quant_analysis_support`（Pearson+Rank IC+p值）、`calculate_factor_ic`（双IC+bootstrap CI） |
| IR（IC/std 稳定性） | ✅ | `rank_ic_ir` 贯穿 quality/registry |
| 分层回测（quantile 单调性） | ✅ | `backtest_factor`（分组 + top/bottom quantile return） |
| 样本外测试（OOS） | ✅ | `validate_factor_oos`（walk-forward + purged-kfold + bootstrap） |
| 真实摩擦成本（滑点+手续费） | ✅ | `cost_model.resolve_cost_assumptions`（commission + slippage_bps） |
| 过拟合校正（DSR/PBO/reality check） | ✅ | `multiple_testing`（deflated sharpe / PBO / white reality check / hansen SPA） |
| 硬指标卡点（Sharpe/回撤/IC门槛） | ✅ | `PROMOTION_THRESHOLDS` 分档 + `evaluate_validation_evidence`（sample_dates/rank_ic_ir/lookahead_risk） |
| 鲁棒性（多窗口/参数敏感/子样本） | ✅ | `factor_robustness_check` |

### D. 数据架构（❌ 真缺口，但是规模问题不是功能问题）

| 愿景组件 | 现状 |
|---|---|
| ClickHouse（核心 OLAP 货架） | ❌ 无 |
| Parquet + 对象存储（冷/温归档） | ❌ 无 |
| PostgreSQL（元数据/财务/字典） | ❌ 无（用单体 SQLite 兼任） |
| MongoDB（另类数据/新闻文本） | ❌ 无 |
| Redis（实时 Tick/缓存） | ❌ 无（有文件级 `.mcp_cache`） |
| 当前实际 | 单体 SQLite `akshare_mcp.sqlite3`（~3GB，WAL） |

---

## 3. 为什么现在不该上"五件套数据湖"（反过早优化）

1. **当前规模撑得住**：日频 5000 股 × 10 年 ≈ 1200 万行/因子。
   单体 SQLite（列查询 + WAL）在日频回测下不卡。真正撞墙的是**分钟级/Tick + 高并发实时**——
   而那需要先有"值得实时跑"的策略，现在还没有。
2. **方向未验证前堆基建 = 在没确认有矿时先修高速公路**。
   "数据架构决定能跑多远"的前提是**方向对**。当前 alpha 命中率还没被 observe farm 验证出来。
   先确认方向，再扩容。
3. **五件套引入巨大运维复杂度**（Docker 集群 / 网络互通 / ETL 同步 / 数据一致性），
   会把"验证 alpha 命中率"这个真目标的精力吸干。

### 触发式演进（撞墙才动，且选轻量方案）

| 触发条件（实测） | 演进动作 |
|---|---|
| 分钟级因子计算 I/O 成为实测瓶颈 / 回测读取 P99 > 阈值 | 引入 **DuckDB + Parquet**（单机嵌入式列存，零运维，Python 原生读 Parquet，对接现有 ETL 最顺） |
| 元数据/字典需强一致 + 复杂 SQL 联合查询 | SQLite → PostgreSQL（可稍早，非紧急） |
| 明确的非结构化另类数据规模化需求 | 引入 MongoDB |
| 走向盘中实时模拟、需微秒级因子切片 | 引入 Redis |
| 多机 / 超大规模 / 高并发实时 | 才考虑 ClickHouse 集群 |

> 选型原则：**DuckDB+Parquet 优先于 ClickHouse 集群**。ClickHouse 是"多机/超大规模/高并发"
> 的重型武器，单机量化研究阶段用 DuckDB 嵌入式即可，零运维、成本最低。

---

## 4. 优先级路线（按"验证命中率"这个核心目标的杠杆排序）

| 优先级 | 内容 | 性质 | 杠杆 |
|---|---|---|---|
| **P1-a** | **因子字典/货架契约显式化**：把 SUPPORTED_FACTORS + factor_pool 编成显式字典（定义/公式/方向/IC历史/版本/更新时间），让 AI/Router 规范"进货" | 软件，低成本 | 高（你说的"因子字典"，当前零散） |
| **P1-b** | **行业+市值中性化**补全：因子值对 Size/行业虚拟变量回归取残差，剥离偏见纯化 alpha | 软件，单点 | 中高（你强调的"垃圾进垃圾出"铁律） |
| **P2** | **每日因子质检流水线编排**：把已有 `validate_factor_oos` + `backtest_factor`(分层) + `factor_robustness_check` 串成一条流水线，自动给因子打 IC/IR/单调性/OOS 标签并上架/下架 | 编排已有零件 | 高（把已有零件组装成你描述的"质检科"） |
| **P3** | 存储演进（DuckDB+Parquet 优先），触发式 | 基建 | 规模触发才做 |
| 不做 | vectorbt 裸代码 + 沙箱 | — | DSL 路线已更优更安全 |

---

## 5. 诚实边界

- **已验证（代码勘察）**：质检层（IC/IR/OOS/walk-forward/purged-kfold/分层quantile/摩擦成本/DSR/PBO/硬指标）几乎全有；数据清洗 MAD/z-score/ffill/qfq 有；存储是单体 SQLite。
- **未武断**：行业+市值中性化"未见明确实现"（措辞保守，非"绝对没有"）。
- **判断（非事实）**："现在不上五件套"是工程判断，附论证 + 触发式替代方案；不是否定你的关切，是调时机和选型。
- **不承诺赚钱**：因子质检让"垃圾因子"被诚实淘汰；能不能赚钱由前向命中率（observe farm）回答。
- **与 SF-ROUTER 的关系**：本评估关注"因子货架质量"（食材），SF-ROUTER 关注"按个股状态生成多类型策略"（厨师配菜）。两者互补：高质量因子货架 + 诊断驱动的多类型生成 + 残酷质检/前向验证 = 完整工厂。

---

## 6. 待用户拍板的决策点

1. 是否认同"现阶段不上五件套数据湖、先验证命中率"的判断？
2. P1-a 因子字典显式化 + P1-b 行业市值中性化，是否作为下一步开工项？
3. P2 每日因子质检流水线（编排已有零件）是否要做成自动上架/下架？
4. 存储演进触发条件是否采纳（DuckDB+Parquet 优先于 ClickHouse）？
