# 五大生产缺陷 — 剩余闭环修改计划（后置持续项）

> 日期：2026-07-11  
> 对齐：`docs/specs/五大生产缺陷闭环开发方案-2026-07-11.md` §十六  
> **权威状态**：P0 全套 + P1-A + P1-B S1–S5 **核心** + P2 **最小实现** 已落地（有测试证据）。  
> 本文只描述 **不阻断「方案+代码闭环」目标** 的后续工程，勿再把已完成项标为 open。

---

## 0. 已完成基线（勿回退）

| 阶段 | 状态 | 关键产物 |
| --- | --- | --- |
| P0-A 血缘 fail-closed | ✅ | `INCUBATION_FAIL_CLOSED_SIGNAL_ID` 默认 ON；落单拒无 signal_id |
| P0-B 退出连续性 | ✅ 代码路径 | exit 候选 / exit_policy / stale close / exit_funnel / exit_gap |
| P0-C 诊断上收 | ✅ | `FactoryDiagnosticsService` + `agent_factory_formal_diagnostics` + 脚本薄封装 |
| P0-D Live readiness | ✅ | mock 不得 `production_ready`；factory probes；LIVE_SMOKE |
| P1-A 运维产品化 | ✅ | Desktop Formal/Evidence/Exit；Intent run-once/dry_run/maintenance |
| P1-B S1 冻结 | ✅ | 文档 + `.github/CODEOWNERS` |
| P1-B S2 契约上移 | ✅ | `strategy_factory.contracts.{hard_gate,promotion_ready,evidence_gaps}` |
| P1-B S3 评估/规则 | ✅ | `infrastructure/promotion/*`；`infrastructure/matching/rules`；AK adapter |
| P1-B S4 边界文档 | ✅ | `docs/specs/P1-B-S4-lifecycle-load-vs-evaluate-boundary.md` |
| P1-B S5 phase 表 | ✅ 核心 | `runtime/incubation_phases` + runner `get_phase_timeout`；**实现体仍 host** |
| P2 稳态最小 | ✅ | endpoint drift gate；soak Intent；合成金路径；mock≠L4 |

**硬约束（全阶段有效）**

- Desktop → Agent HTTP only（不直连 MCP/DB）
- 不降 hard gate：`production_trade_floor=20`、expectancy>0、pnl_conversion>0、execution_conversion≥0.20
- 写动作：Intent + control token；默认 dry-run
- 诊断脚本与产品 API 同源 service
- strategy-factory **禁止** import `akshare_mcp`
- Mock AI **永不** `production_ready=true`（不得点亮 L4）

---

## 1. 仍 open（明确非本目标「方案+代码闭环」阻断项）

| ID | 项 | 为何仍 open | 下一动作 |
| --- | --- | --- | --- |
| R1 | 有样本生产库 exit 转化 ≥80% | 需真实/准生产轨迹；单测不能代替 | Wave 1 运行态：dry-run/run_once 周期 + funnel 日报 |
| R2 | runner I/O 实现体迁出 AK | phase 表已在 SF；`runner.py` ~2544 行仍 host | PR-S5a phase orchestrator → 逐 phase port |
| R3 | Graphify 全量重建 | 可选；CI 用既有 endpoint map + allowlist | 按需重建；非强制 |
| R4 | phase catalog 上屏 / overview 装载注释分区 | 可选 UX | Desktop 只读 catalog |

### 本机运行态快照（2026-07-11）

路径：`data/db/akshare_mcp.sqlite3`（只读诊断）

```text
formal_count=0 observe_count=0 orders_total=0 trades_total=0
exit_funnel 全 0；hard_gate_histogram 全 0
```

→ **代码闭环已在；L2/L3 运行态未点亮。禁止据此宣称 Live 生产就绪。**

---

## 2. 五大缺陷 × 代码闭环后剩余

| 缺陷 | 代码闭环 | 运行态/深水剩余 |
| --- | --- | --- |
| 1 晋级闭环 | hard gate/promotion 判定在 SF；diagnostics 可解释 | 样本 formal 与 acceptance 回放 |
| 2 宿主过重 | 契约/评估/规则/phase 表上移；CODEOWNERS 冻结 | runner/matching 主机循环深抽离 |
| 3 Mock≠Live | production_ready 否决 mock；factory probes | 非 mock + 有证据才可 L4 |
| 4 退出连续性 | exit 路径 + funnel/gap 产品化 | 有仓样本转化率 SLO |
| 5 运维产品化 | Formal 面板 + Intent + soak | 更多脚本 Intent 化、回放过滤 |

---

## 3. 推荐实施顺序（后续）

| 波次 | 内容 | DoD |
| --- | --- | --- |
| Wave 1 | 样本库 formal/exit 运行态 baseline + 加压 | `reports/ops/runtime_*`；exit 转化可计算 |
| Wave 2 | 脚本 thin wrapper 扫尾；formal=0 next_actions 强化 | 值班不依赖本地 SQL |
| Wave 3 | PR-S5a/b runner orchestrator + phase port | SF 边界测 + 金路径绿；gate 数值不变 |
| Wave 4 | Graphify / phase catalog（可选） | drift 仍绿 |

---

## 4. 回归守门（任何后续 PR 必跑）

见主方案 §16.5。本机最近一次（2026-07-11）：

| 套件 | 结果 |
| --- | --- |
| SF gate/kernel/boundary | 27 passed |
| AK ownership/lineage/diagnostics/golden | 24 passed |
| Agent soak/mock/drift/soak-intents | 11 passed |
| endpoint drift | OK（0 unexplained / 0 stale） |

---

## 5. 明确不做

- 不放宽 hard gate / formal 样本门槛
- Desktop 不接回 MCP
- 不以 Mock 面板完成度替代 Live readiness
- 不把「方案+代码闭环 complete」偷换成「Live 生产宣称」
