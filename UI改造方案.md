# AIASK UI改造方案

更新时间：2026-03-27
适用范围：`apps/web` 前端工作台、认证页、首页、行情页、策略页、全局壳层

## 1. 目标

本次改造不是做一次表层美化，而是把 AIASK 从“功能很多的浅色玻璃卡应用”重构成“面向金融投研与交易流程的专业工作台”。

核心目标：

1. 提升专业感与可信度，弱化模板化、装饰化风格。
2. 提升信息可读性，让价格、指标、风险、动作优先级更清楚。
3. 提升任务聚焦，减少首页、行情页、策略页的首屏噪音。
4. 重构导航和布局，让用户按工作流使用系统，而不是在页面清单里游走。
5. 兼顾桌面高密度场景与移动端快速操作场景。

## 2. 现状诊断

### 2.1 视觉问题

- 全局主题采用浅紫蓝渐变背景、半透明 surface、广泛使用 blur，风格统一但偏轻、偏装饰，不够像高可信的金融工具。
- 品牌蓝、紫色渐变、玻璃白、红绿行情色同时存在，但缺少明确职责，页面重心容易被背景和容器样式分散。
- 多数按钮、输入框、卡片都使用近似视觉语言，主操作和次操作的权重差异不够明显。

代码证据：

- 全局颜色与渐变：[apps/web/app/globals.css](/Users/mac/Desktop/股票/apps/web/app/globals.css#L5)
- `body` 背景渐变：[apps/web/app/globals.css](/Users/mac/Desktop/股票/apps/web/app/globals.css#L79)
- glass 工具类：[apps/web/app/globals.css](/Users/mac/Desktop/股票/apps/web/app/globals.css#L163)
- 默认按钮样式：[apps/web/app/globals.css](/Users/mac/Desktop/股票/apps/web/app/globals.css#L237)

### 2.2 字体与排版问题

- 当前 `--font-sans` 仍是系统字体栈，缺乏产品识别度和金融产品应有的秩序感。
- 标题、正文、注释、数值之间的字号和字重差异有限，层级建立不强。
- 缺少专门承载指标与行情数字的 metric typography。

代码证据：

- 字体 token：[apps/web/app/globals.css](/Users/mac/Desktop/股票/apps/web/app/globals.css#L37)

### 2.3 信息架构问题

- 左侧导航按“功能归类”展开过多入口，学习成本高，且会削弱核心路径。
- 对普通用户而言，首页、行情、研究、策略、交易之间的层级关系不够明确。
- 全局导航、页面局部 tab、右侧摘要动作区、右侧 Copilot 同时存在，造成操作入口分散。

代码证据：

- 导航分组定义：[apps/web/components/app-shell.tsx](/Users/mac/Desktop/股票/apps/web/components/app-shell.tsx#L31)
- 桌面侧栏与右侧 dock：[apps/web/components/app-shell.tsx](/Users/mac/Desktop/股票/apps/web/components/app-shell.tsx#L366)

### 2.4 页面布局问题

- 首页模块数量多，但很多是空状态，大面积占位会稀释“今日最重要事情”。
- 行情页在首屏同时摆放筛选区、摘要动作区、K线、行情摘要、盘口、tab 导航，视觉主轴不够明确。
- 策略超市使用大量卡片承载高度结构化数据，不利于对比与筛选。

### 2.5 移动端问题

- 移动端已有底部 5 导航，同时又保留完整抽屉导航，造成双导航系统并存。
- 行情页移动端仍保留较重的摘要动作区，图表区被压缩。
- 顶部状态按钮、底部导航、页面内快捷操作都在争夺有限空间。

代码证据：

- 移动底部导航：[apps/web/components/mobile-nav.tsx](/Users/mac/Desktop/股票/apps/web/components/mobile-nav.tsx#L7)
- 全局 overlay 注入：[apps/web/components/global-overlays.tsx](/Users/mac/Desktop/股票/apps/web/components/global-overlays.tsx#L18)
- 移动抽屉：[apps/web/components/app-shell.tsx](/Users/mac/Desktop/股票/apps/web/components/app-shell.tsx#L409)

### 2.6 引导与干扰问题

- 新手引导是全屏遮罩式 spotlight，会在用户首次进入时打断对页面本身的理解。
- 对数据产品而言，更适合用轻量 checklist 或渐进式 coach mark。

代码证据：

- Onboarding 实现：[apps/web/components/onboarding.tsx](/Users/mac/Desktop/股票/apps/web/components/onboarding.tsx#L43)

## 3. 外部参考结论

以下结论来自本次联网调研，重点关注企业数据面板、金融工作台、排版与可读性：

1. Carbon Dashboards
   来源：[IBM Carbon Dashboards](https://carbondesignsystem.com/data-visualization/dashboards/)
   结论：仪表盘应该有清晰层级、稳定布局、一致图例，并限制同时展示的核心指标数量。

2. Geckoboard Dashboard Layout
   来源：[Geckoboard Dashboard Layout Guide](https://support.geckoboard.com/en/articles/10306664-planning-your-dashboard-design-and-layout)
   结论：一个 dashboard 应围绕一个目的展开，按决策流程组织内容，而不是按“所有可能功能”组织。

3. Geckoboard Dashboard Communication
   来源：[Geckoboard 文章](https://www.geckoboard.com/blog/mastering-hubspot-reporting-how-sales-leaders-can-build-visual-dashboards-that-drive-performance/)
   结论：颜色应克制使用，只在强调异常、状态和重点时发挥作用。

4. Atlassian Typography
   来源：[Atlassian Typography](https://atlassian.design/foundations/typography/)
   结论：企业应用需要明确 heading、body、code、metric 的排版角色，不能仅依赖默认系统字体。

5. Atlassian Typography GA
   来源：[New typography in GA](https://atlassian.design/whats-new/new-typography-in-general-availability)
   结论：现代企业产品越来越强调产品字体系统和跨场景一致性。

6. Material 3
   来源：[Material 3](https://developer.android.com/develop/ui/compose/designsystems/material3)
   结论：不同按钮需要有明确强调等级；紧凑屏幕底部导航宜保持少量核心入口；抽屉导航更适合中大屏。

## 4. 改造总原则

### 4.1 视觉原则

- 去模板化：去掉大面积紫色渐变和默认玻璃拟态。
- 去装饰化：背景退后，内容前置。
- 去同质化：不同级别的组件必须有明确视觉差异。
- 去噪音化：颜色只为结构、状态和任务服务。

### 4.2 产品原则

- 全局按工作流组织，不按功能清单组织。
- 首页只回答“现在最该做什么”。
- 行情页只回答“当前标的/市场发生了什么”。
- 策略页只回答“哪几个策略值得比较和进一步操作”。

### 4.3 响应式原则

- 桌面端优先保证高密度信息协同。
- 移动端优先保证快速查看、单手操作和主任务闭环。
- 同一页面在移动端不做桌面端全量缩小版，而是进行信息重组。

## 5. 新视觉系统方案

### 5.1 新色彩体系

建议将页面底色改为中性、低干扰的专业金融底色。

推荐基础色板：

- `bg-app`: `#F4F6F8`
- `bg-surface`: `#FFFFFF`
- `bg-surface-alt`: `#F8FAFC`
- `border-default`: `#E2E8F0`
- `text-primary`: `#0F172A`
- `text-secondary`: `#475569`
- `text-muted`: `#64748B`
- `brand-primary`: `#0B6BCB`
- `brand-primary-hover`: `#085AA8`
- `positive`: `#D92D20` 或沿用 A 股红
- `negative`: `#039855` 或沿用 A 股绿
- `warning`: `#B54708`
- `danger-bg`: `#FEF3F2`

使用规则：

- 品牌蓝只用于主按钮、active tab、重点链接、选中态。
- 红绿只用于行情、盈亏、涨跌、风险，不参与背景装饰。
- 页面背景不再使用多段紫蓝渐变，只保留极浅的结构性层次。
- 高级感通过排版、边框、密度、阴影和间距建立，而不是依赖 blur。

### 5.2 容器体系

建议分成四级容器：

1. `App Background`
   用于页面大底，不使用强渐变。

2. `Surface`
   常规卡片、表格、表单容器，白底或浅灰底。

3. `Elevated Surface`
   弹层、抽屉、浮动面板，允许轻微阴影。

4. `Utility Overlay`
   仅用于 toast、spotlight、modal 遮罩，不进入常规页面组件。

处理原则：

- `glass` 和 `glass-strong` 不再作为全局默认样式。
- blur 只保留给 modal、drawer、top command overlay。

### 5.3 阴影与边框

- 默认用边框建立卡片边界，阴影减轻。
- 一级卡片用 `1px` 浅边框，hover 或聚焦时再增加弱阴影。
- 不再给所有按钮和所有面板都使用同类大阴影。

### 5.4 字体系统

推荐组合：

- 中文/西文主字体：`IBM Plex Sans`, `PingFang SC`, `Noto Sans SC`, sans-serif
- 数字/代码字体：`IBM Plex Mono`, `SF Mono`, `Cascadia Code`, monospace

新增排版角色：

- `display`: 页面首页英雄数字、账户总资产
- `page-title`: 页面标题
- `section-title`: 模块标题
- `body`: 正文
- `meta`: 辅助说明
- `metric`: 行情数值、收益率、Sharpe、回撤、成交额
- `code`: 股票代码、策略 ID、路径参数

推荐字号基线：

- `page-title`: 28/34, 700
- `section-title`: 18/24, 600
- `body`: 14/22, 400
- `meta`: 12/18, 400
- `metric-lg`: 32/36, 700
- `metric-md`: 20/24, 600
- `metric-sm`: 14/18, 600

## 6. 信息架构重构

### 6.1 全局导航

当前问题是入口太多，建议把全局导航压缩成工作流入口：

- `看盘`
- `研究`
- `策略`
- `交易`
- `AI`

二级页面组织方式：

- 工作流落地页内使用局部 tabs 或局部 section nav。
- 低频页面不占用全局一级导航。
- 用命令面板和搜索承担部分“找功能”任务。

### 6.2 桌面端壳层

建议统一为三段式：

- 左侧 `workflow rail`
- 中央 `task canvas`
- 右侧 `context drawer`，按需展开，不常驻

调整要点：

- 页面级“右侧摘要动作区”与全局 Copilot 右栏不要同时出现。
- 顶部栏只保留页面标题、关键状态、通知、用户入口。
- 工作区路径说明保留，但弱化视觉权重。

### 6.3 移动端壳层

建议：

- 底部导航保留 5 个核心入口。
- 抽屉导航改成“更多/工作流切换”，不再完整镜像桌面导航树。
- Copilot 用半屏或全屏 sheet 呈现，不与页面信息并排。

## 7. 关键页面改造方案

## 7.1 登录与注册页

目标：从“泛 SaaS 登录页”调整为“专业金融 AI 平台入口”。

改造建议：

- 去掉大面积淡紫渐变，改成浅中性背景 + 一处克制的品牌光带。
- 左侧保留价值主张，但减少营销化装饰，改为更具体的产品能力说明。
- 右侧表单采用实体白卡，不使用强玻璃态。
- 标题、辅助文案、字段说明分层更清晰。
- 可加入一条轻量产品截图或策略/行情界面缩略图，提高真实感。

## 7.2 首页 `/`

目标：成为“今日工作台”，而不是模块陈列页。

建议首屏结构：

1. 顶部市场状态条
   内容：交易状态、主要指数、恐贪、北向、涨停统计

2. 今日任务区
   内容：继续看盘、待处理告警、风险巡检、策略观察

3. 组合与关注区
   内容：持仓、自选、今日异动

4. 关键提醒区
   内容：高优先级快讯、风控异常、执行提醒

下沉模块：

- 模块健康状态
- 次级快讯
- 大面积空状态卡

空状态原则：

- 不再用大块卡片占位。
- 改用紧凑提示 + 明确 CTA。

## 7.3 行情页 `/market`

目标：首屏先回答“现在发生了什么”，再提供下一步动作。

桌面端建议布局：

- 左 8 栏：K 线图、分时/副图切换
- 右 4 栏：实时行情摘要、盘口、快速操作
- 顶部横向工具条：标的输入、周期切换、常用视图、时间状态
- 下方 tab 区：板块、涨停、逐笔、指数、搜索

关键改造点：

- 页面内“右侧摘要动作区”合并到右栏卡片，不再单独形成第二套布局逻辑。
- 顶部工具区压缩成一行或两行，不再纵向堆叠太多轻量按钮。
- 示例标的改为 chip 组，不占大面积。
- 数据加载时间显示弱化为 meta 信息。
- 五档盘口用更紧凑表格展示，数字对齐。

移动端建议布局：

- 首屏只保留：标的输入、主图、核心价格摘要。
- 快捷动作放进底部 action sheet。
- 盘口和其他 tab 作为下滑内容，不抢首屏。

## 7.4 策略超市 `/strategy-market`

目标：从“策略卡片墙”改为“可比较、可筛选、可决策”的策略工作台。

改造建议：

- 默认视图改为表格或 list table。
- 卡片视图只留给精选策略和推荐策略。
- 工厂状态收进折叠 summary bar，不占用过多首屏。
- 策略关键字段固定化：
  - 名称
  - 类型
  - 年化收益
  - Sharpe
  - 最大回撤
  - 订阅人数
  - 最近更新时间
  - 操作
- 所有 0 值字段做弱化处理。
- 排序、筛选、收藏、加入组合作为一级高频操作。

## 7.5 Copilot / AI 区域

目标：把 AI 从“另一个右侧栏”升级为“上下文辅助层”。

建议：

- 默认不常驻展开。
- 在页面关键动作旁提供 AI 入口，如“解读当前 K 线”“解释异常波动”“比较 3 个策略”。
- AI 输出卡片需要与普通系统卡片区分，统一添加 AI 标识与解释入口。
- 把“全局聊天”与“页面上下文 AI”分清，避免所有问题都回到一个通用聊天框。

## 8. 设计系统落地建议

### 8.1 Token 重构

建议重新整理为以下 token 分层：

- `color.background.*`
- `color.surface.*`
- `color.border.*`
- `color.text.*`
- `color.brand.*`
- `color.semantic.*`
- `font.family.*`
- `font.size.*`
- `font.weight.*`
- `radius.*`
- `shadow.*`
- `space.*`

### 8.2 组件优先级重做

优先重构组件：

1. Button
2. Input / Select
3. Card / Section
4. Tab Bar
5. KPI Card
6. Data Table
7. Drawer / Modal
8. Top Bar / Side Rail / Bottom Nav

### 8.3 按钮分级

建议至少定义：

- `Primary`
- `Secondary`
- `Tertiary`
- `Ghost`
- `Danger`
- `Link`

要求：

- 主操作在一个区域里只能有一个。
- 次操作不要和主操作一样重。
- 所有按钮不再默认套统一玻璃样式。

## 9. 实施路线图

### 阶段一：视觉基底重构

范围：

- 全局颜色 token
- 背景
- 字体
- 边框/阴影
- Button/Input/Card 基础组件

目标：

- 先把“产品气质”从玻璃拟态改成专业工作台。

涉及文件：

- [apps/web/app/globals.css](/Users/mac/Desktop/股票/apps/web/app/globals.css)
- 基础 UI 组件目录

### 阶段二：壳层与导航重构

范围：

- `AppShell`
- 桌面侧栏
- 移动底部导航
- 移动抽屉
- Copilot 显示策略

目标：

- 先解决全局结构噪音和入口过载。

涉及文件：

- [apps/web/components/app-shell.tsx](/Users/mac/Desktop/股票/apps/web/components/app-shell.tsx)
- [apps/web/components/mobile-nav.tsx](/Users/mac/Desktop/股票/apps/web/components/mobile-nav.tsx)
- [apps/web/components/global-overlays.tsx](/Users/mac/Desktop/股票/apps/web/components/global-overlays.tsx)

### 阶段三：首页与行情页重构

范围：

- 首页信息层级
- 行情页首屏结构
- 右侧摘要动作区合并

目标：

- 让最常访问的两个页面先真正可用、可读、可聚焦。

### 阶段四：策略页与 AI 辅助重构

范围：

- 策略超市信息视图
- AI 上下文入口
- 表格/卡片切换逻辑

目标：

- 建立高密度工作台体验，而不是继续堆卡片。

### 阶段五：新手引导和空状态重构

范围：

- onboarding
- 首页空状态
- 模块提示与 CTA

目标：

- 减少干扰，提高首次使用成功率。

## 10. 验收标准

### 10.1 视觉验收

- 不再出现大面积紫色品牌偏置背景。
- 常规页面不再默认使用 glass 卡片与 blur 输入框。
- 页面能够仅通过排版和间距建立清晰层级。

### 10.2 结构验收

- 桌面端首屏不再同时存在两套右侧辅助区。
- 全局一级导航压缩到工作流级入口。
- 首页首屏模块数显著减少。

### 10.3 页面验收

- 行情页首屏 3 秒内可定位当前标的、当前价格、图表、下一步动作。
- 策略页可在一个视图中完成排序、筛选、比较和加入组合。
- 移动端首屏不会因导航与摘要组件导致图表区域过小。

### 10.4 体验验收

- 新用户首次进入不会被全屏引导遮挡主内容。
- 主操作、次操作、辅助操作具备稳定且一致的视觉差异。
- 空状态的占位显著减少，CTA 更直接。

## 11. 建议的下一步输出物

如果继续推进，建议按以下顺序产出：

1. 新 design tokens 草案
2. 壳层与导航低保真线框图
3. 首页、行情页、策略页中保真视觉稿
4. 组件级改造清单
5. 开发分批实施清单

## 12. 建议优先修改文件

- [apps/web/app/globals.css](/Users/mac/Desktop/股票/apps/web/app/globals.css)
- [apps/web/components/app-shell.tsx](/Users/mac/Desktop/股票/apps/web/components/app-shell.tsx)
- [apps/web/components/mobile-nav.tsx](/Users/mac/Desktop/股票/apps/web/components/mobile-nav.tsx)
- [apps/web/components/onboarding.tsx](/Users/mac/Desktop/股票/apps/web/components/onboarding.tsx)
- 首页相关组件
- 行情页相关组件
- 策略超市相关组件

## 13. 总结

当前版本的问题不在于“不统一”，而在于“统一得过于表面化”。视觉上已经有一套一致的浅色玻璃语汇，但这套语汇不适合承载高密度、强判断、强信任要求的金融工作台。

本次改造的核心不是增加更多风格，而是做三件事：

1. 让背景退后，让信息上前。
2. 让功能列表退后，让任务流程上前。
3. 让装饰性统一退后，让专业性和可读性上前。

## 14. 全站 UI 审计（Glassmorphism 重构建议）

更新时间：2026-03-27 18:30

说明：

- 本节是基于当前 `apps/web` 已完成的工作台重构做第二轮视觉审计。
- 第一轮方案强调“去装饰化、去泛玻璃化”；第二轮不是回退到旧式紫色磨砂，而是引入更克制、更专业的金融工作台 Glassmorphism。
- 目标不是把所有卡片都做成发亮玻璃，而是用有限的半透明、模糊、柔和高光和层叠关系，把结构、重点和上下文做得更清楚。

### 14.1 审计范围与方法

本次使用 Playwright MCP 对当前前端页面进行自动化遍历与截图，产出物位于：

- 截图目录：`/Users/mac/Desktop/股票/.playwright/ui-audit-2026-03-27/`
- 全站缩略拼图：`/Users/mac/Desktop/股票/.playwright/ui-audit-2026-03-27/contact-sheet.png`

审计覆盖：

- `apps/web/app` 下 46 个路由页面。
- 因本地浏览器已有登录态，额外清空浏览器状态后补拍了真实 `/login` 与 `/register`，所以本轮共保留 48 张有效页面截图，外加 1 张 contact sheet。
- 动态页已使用真实样本补齐：
- `/strategy-market/factory_1774521606_9bd14947`
- `/execution/artifacts/art_ui_audit_20260327`

审计结论中的“证据”来自三部分：

- Playwright 全页截图。
- 页面 DOM 快照与可见层级。
- 对主要页面自动检测 `backdrop-filter` / `backdrop-blur` 后的结果。

重要发现：

- 除 `/assistant` 外，本轮批量截图页面几乎全部未检测到有效 `backdrop-filter: blur(...)`。
- 这说明当前系统虽然已经完成“工作台化”，但实际材质仍然以白底实心卡片为主，并没有形成毛玻璃视觉体系。

### 14.2 外部趋势依据

以下资料用于校正“主流毛玻璃”和“企业工作台”应该如何落地，优先采用官方设计系统和一线平台文档：

1. Apple Liquid Glass
   来源：[Apple Developer - Liquid Glass](https://developer.apple.com/documentation/technologyoverviews/liquid-glass)
   结论：玻璃材质的核心不是把界面全部做模糊，而是通过系统控件、工具栏、导航和局部自定义元素建立层级、和谐和一致性。Apple 特别强调：让最重要内容保持焦点，谨慎使用颜色，让内容透过控件“发光”而不是被控件压住。

2. Apple Design Gallery
   来源：[Apple Design - Liquid Glass Gallery](https://developer.apple.com/cn/design/new-design-gallery/)
   结论：优秀案例把品牌色从“整页背景”移回内容层，把子菜单、弹出层、工具栏做成半透明浮层，从而让照片、图表和主内容成为视觉主角。

3. Microsoft Fluent Acrylic
   来源：[Microsoft Learn - Acrylic material](https://learn.microsoft.com/en-us/windows/apps/design/style/acrylic)
   结论：Acrylic 的作用是增加物理纹理和深度，帮助建立视觉层级；它更适合临时面板、浮层、边栏和需要强调层次的表面，不适合整页大面积铺满。

4. Material 3
   来源：[Android Developers - Material 3](https://developer.android.com/develop/ui/compose/designsystems/material3)
   结论：强调层级不只靠阴影，还要靠不同 `surface`、`surface-variant`、排版权重和 tonal elevation。大屏体验应优先采用适配式导航与稳定的容器分层，而不是堆叠同质卡片。

5. IBM Carbon Dashboards
   来源：[IBM Carbon - Dashboards](https://carbondesignsystem.com/data-visualization/dashboards/)
   结论：仪表盘需要强层级、受控指标数量、一致图例与充足留白。最重要的信息应占据最大面积和最高对比度，其余信息应按 F-pattern 组织，而不是平均分配成卡片墙。

6. Atlassian Typography
   来源：[Atlassian Design - Typography](https://atlassian.design/foundations/typography/)
   结论：企业应用需要稳定的字体系统、明确的 heading/body/code 角色，以及只保留一个 `h1` 的页面结构。排版和空间本身就是层级工具，不能只靠边框与卡片。

### 14.3 全局共性问题

#### 14.3.1 材质层次不足

当前大多数页面仍然是：

- `body` 浅灰背景
- 中间主内容白底卡片
- 左侧导航白底
- 右侧 AI 工作台白底

这造成三个问题：

- 空间被分成很多块，但每一块几乎是同一种材质。
- 页面虽然“整洁”，但缺乏前后景关系，没有工作台应有的空气感和深度感。
- 视觉中心不清楚，用户只能靠文字量判断哪个区域更重要。

#### 14.3.2 卡片墙化严重

首页、策略页、研究页、组合页、管理页都存在同一问题：

- 容器数量多。
- 卡片大小接近。
- 边框和阴影强度接近。
- 主副信息被均质化。

结果是用户看到的是一堵白卡片墙，而不是一个有节奏、有重点的工作台。

#### 14.3.3 圆角与软化关系不成立

当前很多容器已经有圆角，但圆角更像“统一组件样式”，而不是“材质语言”：

- 外层壳体和内层内容板没有圆角层级差。
- 筛选条、工具条、图表卡、表格卡的边缘弧度相近。
- 缺少大圆角玻璃壳体承托小圆角内容模块的关系。

这会让界面看起来“圆了”，但没有真正变柔和。

#### 14.3.4 背景氛围过平

当前背景以非常浅的冷灰为主，稳定但偏干：

- 没有雾化光斑。
- 没有渐变空气层。
- 没有弱纹理。
- 页面滚动时几乎感受不到前景和背景之间的距离。

结果是页面像在纯白画板上摆组件，缺少高端金融产品需要的精致感。

#### 14.3.5 顶栏、筛选条、右侧上下文区没有形成“漂浮表面”

最适合做毛玻璃的区域目前恰恰最平：

- 顶部 breadcrumb / page context bar
- 右侧 AI 工作台
- 页面内部筛选条 / tab 条 / 工具条
- 图表上方浮动控制条

这些区域如果继续使用实心白底，会让页面一直停留在“后台管理系统”观感，而不是“金融操作台”观感。

#### 14.3.6 色彩克制过头，导致页面缺少节奏峰值

现在的系统已经抑制住了早期的紫色装饰感，这是对的；但新的问题是：

- 大多数页面只剩蓝色按钮和红绿数字。
- 过渡色、提示色、状态色层次不足。
- 缺少中性暖灰、冷灰、浅蓝雾、微绿雾等过渡层。

结果是页面稳定但不生动，卡片多的时候尤其显得机械。

#### 14.3.7 宽度利用不足，中轴过窄

不少页面的真实问题不在“内容少”，而在“内容被压进细长列”：

- 左栏宽度固定。
- 中栏又被严格限制。
- 右侧 AI 栏常驻，进一步压缩主画布。

图表、表格、时间线、KPI 其实都有信息，但主内容画布太窄，导致可视化不够舒展，也削弱了玻璃大面板的表现力。

#### 14.3.8 感知质量问题会放大视觉缺陷

本轮截图中还有两类非纯视觉问题，会直接影响质感：

- 管理页若无权限会出现 403 数据请求，当前只是普通空白卡片，不像被精心设计的“权限受限状态”。
- 个别页面存在 hydration mismatch 警告，容易引起首屏闪动或文本跳动，削弱“稳定、精致”的感知。

#### 14.3.9 标题系统与图标语气不一致

重新核对截图后，发现页面标题体系并不统一：

- 有的页面使用“句子型 Hero”作为 `h1`，例如首页、行情页、研究页、组合页。
- 有的页面使用功能名作为 `h1`，例如技术分析、估值分析、条件选股。
- 有的页面直接带 emoji 和括号式英文副标题，例如“🧠 AI 深度诊断报告生成器 (Diagnostic AI)”“📈期权全景分析 (Options)”。
- 有的页面把最关键的信息放进统计标签或评级标签，而非页标题本身，例如策略详情页。

这会导致两个问题：

- 全站的专业语气不稳定，部分页面像金融工作台，部分页面像 demo 或运营页。
- 标题层级和导航语义不一致，用户很难快速建立“当前页面是什么、这一屏最重要的是什么”的固定预期。

#### 14.3.10 审计工具对 sticky 顶栏存在 full-page 拼接伪影

继续深挖后，确认上一轮有一项判断需要纠正：

- 在 `search / research / portfolio / skills` 等长页面 full-page 截图里，曾出现一条横向悬浮条横穿页面中部。
- 进一步使用 viewport 截图和滚动截图对比后，确认这不是实际页面结构，而是 `AppShell` 顶部 `sticky` 顶栏在全页截图拼接时被重复绘制的伪影。
- 根因来自全局顶栏 `header` 使用了 `sticky top-0 z-30`，而 Playwright full-page 截图在长页面滚动拼接时会重复保留固定层。

这条结论对后续审计方法很重要：

- 不能把 full-page 截图中的重复 sticky 顶栏误判为页面中部的真实控件。
- 长页面必须结合 viewport 截图、滚动截图和 DOM 结构一起判断。
- 真实问题不是“中间多了一条横线”，而是当前顶栏材质较平、层级较弱，以及截图审计方法需要排除 sticky 伪影。

#### 14.3.11 右侧 AI 工作台常驻，但利用率不足

之前文档已提到它压缩了主画布，但重新看图后，这个问题比预想更重：

- 右侧 AI 工作台在大量页面中信息密度明显不足。
- 上部是少量按钮和上下文块，下部长时间保留大块空白输入区域。
- 当主内容本身仍处于空状态或低密度状态时，会出现“中间内容窄、右侧也空、整页显得未完成”的观感。

这意味着右栏当前不是稳定提升效率，而是在很多页面里放大了：

- 主画布过窄
- 中轴内容被压扁
- 页面空白列过多

#### 14.3.12 页面密度分配失衡

这一点在 `data / notifications / valuation / user / admin` 等页面尤其明显：

- 有些页面卡片很多，造成卡片墙。
- 有些页面内容偏少，却仍沿用同样的壳层宽度和三栏结构。
- 结果就是高密度页拥挤，低密度页空旷，两者都没有达到舒服的工作台节奏。

说明问题不只是“组件长什么样”，还包括：

- 页面模板没有按内容类型切换。
- 空状态页、单表单页、单图表页、列表页都被放进近似的结构骨架。
- 导致毛玻璃即便引入，如果不先整理模板，依然会出现“玻璃包着空白”的问题。

#### 14.3.13 工作区工具条过高，抢占首屏

继续核对代码后，可以确认另一个真实的全站问题：

- `WorkspaceToolbar` 当前承载了工作区切换、同步状态、模板注入、保存视图、布局参数、任务编排等大量能力。
- 它在至少 12 个页面里都以完整形态直接渲染在正文顶部。
- 这会导致首屏还没进入“研究 / 交易 / 风险 / 绩效”的业务内容，就先看到一个高度很高的配置面板。

这会直接带来三个副作用：

- 首屏主任务被挤压，业务 Hero、图表、结果列表下沉。
- 工具栏本身太像“内部配置台”，削弱页面的产品完成度。
- 后续即使加入毛玻璃，也很容易变成“巨大的玻璃设置面板”，而不是精致的工作台工具条。

### 14.4 新一轮 Glassmorphism 原则

#### 14.4.1 设计方向

新的方向不是“回到浅紫大玻璃背景”，而是：

- 冷静中性底色
- 局部毛玻璃浮层
- 双层面板结构
- 柔和高光描边
- 受控模糊和投影
- 让图表、数值、列表成为焦点

可以理解为：

- 第一层是安静背景。
- 第二层是半透明外壳。
- 第三层是内容面板。
- 第四层才是按钮、标签、图表、输入框。

#### 14.4.2 推荐基础视觉配方

建议新增以下玻璃表面配方：

- `glass-shell`
  `background: rgba(255,255,255,0.62)`
  `backdrop-filter: blur(18px) saturate(140%)`
  `border: 1px solid rgba(255,255,255,0.58)`
  `box-shadow: 0 18px 48px rgba(15,23,42,0.10)`

- `glass-panel`
  `background: rgba(255,255,255,0.72)`
  `backdrop-filter: blur(14px) saturate(130%)`
  `border: 1px solid rgba(255,255,255,0.72)`
  `box-shadow: 0 10px 30px rgba(15,23,42,0.08)`

- `glass-toolbar`
  `background: rgba(248,250,252,0.68)`
  `backdrop-filter: blur(16px)`
  `border: 1px solid rgba(255,255,255,0.65)`

- `glass-overlay-strong`
  `background: rgba(255,255,255,0.78)`
  `backdrop-filter: blur(24px) saturate(145%)`
  `box-shadow: 0 24px 64px rgba(15,23,42,0.14)`

#### 14.4.3 推荐圆角体系

- 页面级大容器：`32px`
- Hero / 壳体卡：`28px`
- 图表、表格、筛选器主卡：`24px`
- 子模块：`20px`
- 输入框与小按钮：`14px` 到 `18px`
- chip / pill：`999px`

圆角必须体现层级：

- 外层更大。
- 内层略小。
- 不能所有容器都同一弧度。

#### 14.4.4 推荐背景体系

建议把当前“浅灰底”升级为三层背景：

1. 基础底色：偏冷白灰 `#EEF3F8`
2. 大面积柔光渐变：左上浅蓝雾，右下极浅青灰雾
3. 极弱噪点或纹理：降低纯色大平面的空旷感

建议只在首屏和壳层保留光斑，不要把图表区做花。

#### 14.4.5 推荐壳层策略

以下区域优先引入玻璃：

- 顶部路径栏
- 左侧 workflow rail 外壳
- 右侧 AI 工作台外壳
- 页面内筛选器与工具条
- 浮动 tab 条
- 弹层、抽屉、详情浮片

以下区域不要直接做重玻璃：

- 大型表格主体
- 高频输入表单主体
- 图表绘图区底层
- 长文本阅读区的正文底板

这些区域应该采用“玻璃外壳 + 实色内板”的双层结构，避免可读性下降。

### 14.5 逐页审计与改造建议

#### 14.5.1 首页与认证

##### `/`

当前问题：
首页首屏已经有 Hero、KPI、今日任务和图表，但信息块过多，首屏下方立即进入卡片瀑布，视觉主峰不够集中。白底卡片之间关系过于平均，导致“欢迎回来”和真正重要的市场信息没有形成足够强的层级。

改造建议：
把 Hero、当前聚焦和右侧行动卡合并进一个 `glass-shell` 首屏容器，顶部路径条改成悬浮玻璃条。首页中段改成“1 个主图表 + 2 个辅助洞察 + 1 个待办动作带”，减少同尺寸 KPI 卡数量，并用浅蓝雾和暖灰投影增强层次。

##### `/login`

当前问题：
登录页结构清楚，但整体还是左右两张白卡直接摆在浅灰背景上，过于平直。标题区、功能说明区、表单区之间没有材质差，登录操作的情绪价值不足。

改造建议：
在认证页使用更强一点的 `glass-shell` 双栏壳体，左侧说明卡使用柔和渐变叠雾，右侧登录卡做高透玻璃面板。按钮、输入框和提示文案之间引入更明显的纵向节奏，底部背景可加入轻微光斑和大半径阴影，建立“进入工作台”的仪式感。

##### `/register`

当前问题：
注册页比登录页信息更多，但仍沿用同一平面结构，步骤感不强。左侧三步说明和右侧表单没有形成明显前后景，导致页面虽整齐但不够吸引。

改造建议：
把左侧步骤区做成半透明“引导玻璃卡”，使用编号胶囊和淡色渐变承托流程。右侧注册面板保持更实一点的内容卡，避免输入可读性下降，同时让页顶的 brand copy 漂浮在背景雾层之上。

#### 14.5.2 市场、搜索与提醒

##### `/market`

当前问题：
行情页内容本身已经完整，但 Hero、标的信息卡、筛选条、图表卡、五档盘口卡都还是实心白板。图表区与右侧实时行情卡的深度关系弱，筛选区像普通表单，不像可交互的交易工具条。

改造建议：
把首屏做成“大玻璃壳体 + 内部图表/报价双面板”结构。筛选条、周期切换、指标开关、下一步操作都放进 `glass-toolbar`，K 线和盘口保留清晰实底，只把外层容器和工具条做模糊，从而兼顾金融可读性和毛玻璃质感。

##### `/stock`

当前问题：
个股详情页的指标摘要和 K 线图已经具备工作台雏形，但顶部摘要卡、行动建议卡和图表区域还是同一材质。当前标的标题很强，下面的内容区却没有顺势拉开层级。

改造建议：
把股票标题、价格带、快捷操作统一包进一个浮动 `glass-shell`。下方 K 线区域采用“玻璃外壳 + 实色图表底板”，五档盘口和信号卡用轻玻璃子卡，提升行情区的层层嵌套感。

##### `/watchlist`

当前问题：
自选股页从缩略图看仍是列表型白卡布局，重点股和观察理由不够突出。它更像一个普通清单页，而不是“交易前观察台”。

改造建议：
把自选股拆成“重点观察位 + 自选列表 + 触发信号”三段式。顶部观察位用横向玻璃卡展示核心标的与预警变化，列表主体保持清晰表格底板，列表上方的筛选和排序做成浮动毛玻璃工具带。

##### `/search`

当前问题：
搜索页结构偏中后台，结果卡片和筛选器均为普通白底面板。搜索行为本应更具聚焦感，但现在缺少明显的输入主轴和结果层次。更深层的问题是：该页首屏会先展示完整 `WorkspaceToolbar`，导致真正的搜索输入和结果摘要被推到下方。

改造建议：
把搜索输入区做成横向玻璃搜索带，结果区采用“上方摘要玻璃条 + 下方实体结果卡”结构。命中高的结果可以用轻微彩色雾层标识，不要只靠蓝链或边框强调。`WorkspaceToolbar` 应收纳成可折叠工作区抽屉、弹层或次级侧栏，不能继续占据搜索页首屏主轴。

##### `/screener`

当前问题：
条件选股页的筛选表单、结果表格和条件 chips 更像标准配置页，柔和度不足。高密度筛选条件没有被组织成一个可拖拽、可折叠、可感知层次的控制台。实现层面上，它同样受 `WorkspaceToolbar + WorkspaceSplitLayout` 的统一骨架影响，首屏先出现工作区配置，再进入筛选任务。

改造建议：
把筛选器区域做成分段式玻璃面板，条件 chips 使用半透明 pill，顶部 summary 显示“已命中数量 / 当前策略口径 / 排序逻辑”。结果区保留清晰表格，但表头和筛选工具条采用毛玻璃材质。工作区配置能力应折叠到次级入口，避免干扰复杂筛选任务的首屏。

##### `/alerts`

当前问题：
告警中心偏功能管理页，表单与列表显得平，状态色只有文字或小标签，缺乏“已触发 / 监控中 / 静默”三种视觉气候。告警作为高优先级信息，本应更具有前景感。

改造建议：
为告警页引入浅红雾、浅黄雾、浅蓝雾三套玻璃状态卡。告警规则编辑区采用 `glass-panel`，触发历史采用实体列表卡，形成“规则在上浮、历史在下沉”的深度关系。

##### `/notifications`

当前问题：
通知中心从截图上看仍是白底列表页，信息节奏过于平均。已读未读、系统通知、交易通知、研究通知的层级差不够强。当前内容较少时，中部大面积留白会放大“三栏但无内容”的空旷感。

改造建议：
顶部加入玻璃筛选条与统计 chips，中部通知流采用时间轴式卡片。高优先级通知使用带轻微模糊高光的通知胶囊，已读通知降低不透明度和阴影权重。若当前通知为空，应切换到更集中、更居中的空状态模板，而不是保留松散三栏结构。

##### `/fund-flow`

当前问题：
资金流向页有数据和图，但容器表现偏“功能演示卡”。图表、榜单、说明块之间没有明确的大中小节奏，缺少更金融化的深度。

改造建议：
把北向资金、主力净流入、板块资金榜做成三块悬浮面板，中间核心图放大。右上角时间粒度和榜单口径切换用毛玻璃 segmented toolbar 承载，增强仪表盘气质。

#### 14.5.3 研究、分析与 AI

##### `/research`

当前问题：
研究页已经转成工作台结构，但顶部工作区、筛选器、摘要和正文仍偏“硬边白卡”。“先看结论”这个定位是对的，只是视觉上还没把结论区抬到前景。

改造建议：
首屏采用大圆角玻璃英雄区，把“当前标的”“三类研究源切换”“结论摘要”做进一张大壳体。正文内容使用更实体的阅读板，顶部筛选条和操作栏使用磨砂悬浮，形成阅读区与控制区材质区分。

##### `/fundamental`

当前问题：
基本面页 KPI 卡和分析卡仍然等权排列，财务信息缺少“概览层”和“拆解层”的区别。页面容易变成会计指标拼盘。

改造建议：
把核心财务结论、估值结论和异常风险先放到一层玻璃摘要带。再把财务表格与解释卡放入下层内容区，使用更明显的边距、圆角层级和浅色雾化分区。

##### `/technical`

当前问题：
技术分析页图表和指标本该最适合做层叠工具面板，但现在工具栏、指标开关和图表外框仍显得普通。图表页的节奏和仪表感不够强。

改造建议：
把指标切换、周期切换和信号说明做成贴着图表上边缘的玻璃工具带。主图保留清晰实体底板，只在外层容器和指标说明卡使用磨砂，以保证蜡烛图和曲线对比度。

##### `/sentiment`

当前问题：
情绪页有仪表盘和评分色彩，但当前组件大多孤立放置，缺乏统一材质。色彩已经比别页丰富，但因为外层容器太白，页面看起来像多个独立组件拼接。

改造建议：
用浅暖色到冷色的背景雾层包住情绪概览区，让温度变化具有空间感。评分仪表、情绪分项和解释文案以玻璃分层组织，避免色块直接贴在灰底上。

##### `/valuation`

当前问题：
估值页通常会展示区间、历史对比和估值模型，但当前页面仍缺少“估值带”这种高辨识度视觉骨架。读者很难一眼看出当前估值所处区间。

改造建议：
把估值结论做成一张大型玻璃估值带卡，横向展示分位、折价/溢价和模型差异。详细指标与模型解释放在下方实体卡中，玻璃只承担“定调”和“分层”作用。

##### `/events`

当前问题：
事件页更像中后台事件列表，时间、类型、影响程度没有做出明显前后景。日历/事件流如果只放在白卡里，节奏会显得很碎。

改造建议：
顶部加入玻璃时间轴导航，事件类型筛选做成半透明标签。事件流采用分级卡片，重大事件使用更高玻璃透明度和更大圆角，轻事件保持轻量。

##### `/macro`

当前问题：
宏观页有明显的模块化分区，但字体、图表和说明卡之间的空间关系仍偏工具页。页面很容易看起来像一组 API demo，而不是宏观工作台。

改造建议：
把“宏观风向摘要”做成全宽玻璃横条，下面用 2 到 3 个大模块承载核心指标与图表。次级说明卡和数据源注释放进较小的实体卡，减少视觉噪音。

##### `/factor`

当前问题：
因子研究页内容多、控件多、信息密度高，但材料语言仍然单一。当前更像一张长配置台，缺少研究流程层级。

改造建议：
将“候选因子输入 / 当前实验状态 / 结果摘要”放入顶部三联玻璃面板。中下部的回测、IC、分组和日志区域采用实体内容板，借由玻璃壳体和悬浮标签把流程分阶段。

##### `/factor-analysis`

当前问题：
因子分析页偏图表和参数组合，但当前视觉重心仍然平均。表单、结果和解释块都采用类似卡片，难以一眼锁定最关键的结论。

改造建议：
让“当前因子结论”和“样本区间”先浮到玻璃首屏，参数控制区做成横向磨砂工具条。表格和图表保持实色底板，避免玻璃直接叠在大量数字上。

##### `/assistant`

当前问题：
这是目前少数仍检测到 blur 的页面，但其玻璃效果并没有形成统一体系，更多体现为局部彩色块与内容卡并置。它与其他工作台页面风格断层最大，看起来像不同产品。

改造建议：
保留这里作为“全站 Glass 试验田”，但要把彩色步骤卡、输入区和结果区统一成同一套玻璃语言。建议把蓝色主行动条做成有高光边缘的毛玻璃胶囊，而不是纯实色条，并让结果区拥有更清晰的浮层切换。

##### `/chat`

当前问题：
AI 对话页如果仍是简单对话卡和输入框，会显得过于基础。对话流本可通过层叠和毛玻璃提升沉浸感，但当前页面较平。

改造建议：
对话输入区使用固定底部玻璃输入栏，消息列表保持可读的实体消息泡。系统回复、上下文建议和可执行动作以不同透明度的浮层卡区分。

##### `/decision`

当前问题：
统一决策工作台包含结论、证据、推荐动作，但这些层级如果都落在白卡中，会削弱“AI 已归纳”的完成感。结论块应该更像决策总览，而不是普通摘要卡。

改造建议：
将“推荐动作 + 置信度 + 关键证据”做成一张主玻璃决策卡，下面再分成证据、反对意见、上下文跳转三个实体区块。顶部操作条和切换视图使用磨砂导航。

#### 14.5.4 交易、策略与组合

##### `/portfolio`

当前问题：
组合页已切到工作台布局，但目前空状态和操作卡过于平，缺少组合总览应有的资产层次。页面中部仍有明显留白断层，像尚未装配完成的板块。

改造建议：
把总资产、风险、现金、仓位集中成一块大玻璃总览壳体。空状态区域引入更柔和的浅蓝雾和轮廓高光，让“下一步该做什么”从普通文字卡升级为可执行行动面板。

##### `/paper-trading`

当前问题：
模拟交易页表单密度较高，但没有交易台应有的层叠关系。下单区、持仓区、订单区如果都使用同材质，会显得偏后台而不是操作台。

改造建议：
把下单表单独立成较强的玻璃浮层，持仓和订单区保持更稳的实体卡。价格、仓位、风险提示和订单动作之间用不同透明度的面板区分主次。

##### `/execution`

当前问题：
执行中心信息很完整，但从视觉上看是一整列白板。执行表单、执行状态、任务列表、复盘摘要全都在一个材质层，缺少策略执行系统的精密感。

改造建议：
让顶部执行上下文卡和关键 KPI 先进入 `glass-shell`，中部参数表单与执行状态采用左右双面板。任务列表、执行告警和复盘摘要保留实体底板，但把表头、搜索与“查询 artifact”入口做成磨砂控制条。

##### `/execution/artifacts/[artifactId]`

当前问题：
Artifact 详情页已经有很清楚的内容，但仍像“普通详情卡 + JSON 卡”。作为结果页，它应该更像一个审计证据包，而不只是表单详情。

改造建议：
把顶部 artifact 摘要、关键编号和状态做成玻璃总览条。下方告警区、原始返回区和上下文跳转区采用分层面板，原始 JSON 外层用玻璃壳、内层用深浅更稳定的代码板承载。

##### `/performance`

当前问题：
绩效中心从整体缩略图看仍偏纵向叠卡，指标、归因、对比与曲线之间没有明显主副节奏。绩效页最需要“大图 + 小结论”的结构，但当前更像模块清单。

改造建议：
将收益曲线或净值曲线提升为首屏主舞台，顶部用玻璃概览卡展示收益、回撤、Sharpe、胜率。二级指标与归因面板放入下层实体区，通过玻璃 tab 条组织视图切换。

##### `/risk`

当前问题：
风险页仍偏分析工具页，风险结论、暴露、情景压力、建议动作的层次不够明显。红黄风险信息没有被好的材质托住，容易显得轻。

改造建议：
风险等级区采用轻微暖色调玻璃摘要卡，压力测试和暴露分析保持实体图表板。高风险提示可使用更高模糊度的小浮层，而非仅用红字和浅红背景。

##### `/strategy`

当前问题：
策略工作台已经具备基础骨架，但从截图看仍以卡片密集排布为主。策略参数、回测结果、指标、操作入口之间的主副关系还不够分明。

改造建议：
把“当前策略 + 关键收益风险指标 + 主操作”合并进一张大玻璃卡，参数与详情放入下层面板。趋势图、配置项和性能指标使用不同层级的圆角与透明度区分。

##### `/strategy-market`

当前问题：
策略超市页已做出 Hero 和筛选，但整体仍是实体卡列表。列表比较强，筛选条和统计摘要却显得弱，没有形成“先筛再比”的操控感。

改造建议：
把顶部统计摘要和筛选器融合为一体化玻璃筛选舱。候选策略卡用“玻璃外壳 + 实体指标芯片 + 彩色趋势微图”的方式增加立体感，让优先策略真正浮出来。

##### `/strategy-market/[id]`

当前问题：
策略详情页信息组织良好，但第一屏仍显偏白、偏平。左上详情、右上当前策略卡、下方 KPI 与标签区之间的材质差不足，导致“这是重点策略”感受不够强。

改造建议：
把策略标题、评级、状态、作者、主动作整合成一张高辨识度玻璃详情头。下方概览、实盘跟踪、工厂审查 tab 做成浮动磨砂分段条，可信信息、评价和图表卡采用二层结构增加深度。

##### `/options`

当前问题：
期权页内容复杂但视觉层级不够，容易沦为一张表单和表格并列页。由于数据和参数很多，如果外层不做分层，页面会显得拥挤且机械。

改造建议：
把期权链概览、标的快照、波动率/Greeks 核心结论放进首屏玻璃壳体。复杂表格仍使用实体底板，上方参数切换与到期月选择使用毛玻璃工具条承托。

##### `/backtest`

当前问题：
回测分析页从工作流角度很重要，但当前更像执行表单 + 结果卡组合。回测结论、参数、曲线和对比没有形成足够强的舞台感。

改造建议：
把回测首屏做成“参数舱 + 主绩效图 + 关键指标摘要”三联大卡。参数区使用半透明玻璃，结果图和交易统计保持高可读实体底板。

#### 14.5.5 平台、设置与管理

##### `/data`

当前问题：
数据中心更像开发后台，模块卡片整齐但偏硬。对用户来说，数据同步、缓存状态、任务状态应该更像“系统状态舱”，而不是普通管理表。当前页面在低数据量下空白占比过高，视觉上像半成品画布。

改造建议：
把全局数据状态做成一排轻玻璃状态条，任务列表与数据源详情留在实体卡。同步操作、刷新、筛选和错误态提示适合放在磨砂工具带中。对于低密度状态，应采用更紧凑的单列或双区模板，而不是继续保留宽阔中轴空白。

##### `/skills`

当前问题：
技能中心页面结构清楚，但如果全部使用实体白卡，会像配置页。技能作为“能力模块”，更适合用稍具未来感的材质语言承托。更深层的问题是它把 `WorkspaceToolbar` 放在技能内容之前，首屏先看到的是工作区和任务编排，而不是技能本身。

改造建议：
技能分类和过滤器放在玻璃分组条中，技能卡使用轻玻璃外框和更有识别度的图标/色块。右侧 AI 工作台可与当前技能上下文形成联动悬浮层。工作区能力应折叠到次级层，不应抢走技能中心的首屏焦点。

##### `/workspace-templates`

当前问题：
模板中心更像资料列表，而不像可挑选的“场景模板库”。缩略图里卡片辨识度不够高，模板间的差异不易一眼识别。当前页面内容很长，但层级峰值不足，从上到下更像配置文档而不是模板商城。

改造建议：
将模板卡片做成封面式玻璃卡，加入轻度封面渐变和功能标签。顶部筛选、排序、模板应用动作适合收纳到一条半透明工具条中。模板详情应做出封面、摘要、应用动作三层节奏，避免长页面只有连续白卡。

##### `/settings`

当前问题：
设置中心输入区和说明区排布合理，但整体非常平。用户资料表单、密码修改和导出按钮都在同一视觉平面，显得缺少重点。

改造建议：
把账户摘要做成顶部玻璃身份卡，表单主体保持清晰实体底板。安全类输入区与资料类输入区之间使用不同透明度或轻色雾层区隔，避免整块白板。

##### `/settings/security`

当前问题：
安全设置天然适合高信任视觉，但目前很可能仍是普通设置卡。对于风控、设备、会话之类内容，缺少“安全壳层”感。

改造建议：
把安全摘要、风险提示和关键开关做成玻璃安全面板，使用更强的描边与柔和阴影建立可信感。设备列表和历史记录保持实体表格。

##### `/settings/audit-log`

当前问题：
审计日志目前更像开发日志表。作为系统证据流，应该具备更强的结构边界与时间轴感，而不是单纯白底列表。

改造建议：
顶部筛选区和导出区采用玻璃工具带，中部日志流改为时间轴式实体卡。高风险操作用更强的状态标签和轻度雾色玻璃强调。

##### `/user`

当前问题：
用户中心结构清楚但情绪偏弱，像表格化的账户摘要。订阅、组合、模拟交易概览等模块都比较“平”，缺少个人工作台的归属感。

改造建议：
把用户身份、风险等级、账户总览先抬到玻璃头部卡。下面的订阅、组合和快捷入口区做成几块有不同透明度的内容板，让个人资产面板看起来更有层次。

##### `/admin`

当前问题：
管理后台概览已经有局部导航和状态卡，但整体仍像平面操作台。系统状态、快捷入口和优先处理区域之间的材质层次不够。

改造建议：
把“当前无需紧急事项”或“异常提醒”区做成高优先级玻璃警示条。管理入口和状态卡用统一玻璃外壳包住，再以实体内卡承接数据，避免管理台变成纯白卡拼盘。

##### `/admin/cache`

当前问题：
缓存管理页属于高功能密度页面，如果维持完全实色，很容易看起来像技术运维表单。继续复核后确认：管理员会话下页面内容正常，之前截图中的 403 属于非管理员会话进入后的受限态；但当前受限态依然缺少专门设计。

改造建议：
把缓存统计、刷新、清理操作集中为玻璃运维舱。无权限状态使用专门的受限提示玻璃卡，而不是空白列表或简单错误信息。

##### `/admin/dead-letters`

当前问题：
死信队列页本质是问题追踪页，但当前更像技术日志面板。严重性、时间线和处理动作没有足够强的视觉差。管理员会话下页面可正常浏览，因此这里的重点不是“页坏了”，而是受限态与正常态都需要更明确的视觉区分。

改造建议：
按严重度分层使用浅红、浅橙、浅灰玻璃标记，顶部筛选与清理操作保持浮层感。列表主体仍采用清晰实体内容卡，确保文本可读。

##### `/admin/tools`

当前问题：
工具仪表盘的图标和状态块很多，如果都落在白底面板上会显得像图标收集页。工具间缺少生态关系和“可用性状态”氛围。

改造建议：
把工具总览做成玻璃状态矩阵，重点工具使用更明显的层叠高亮。操作日志和健康信息区使用更沉稳的实体面板，避免页面失焦。

##### `/admin/users`

当前问题：
用户管理页同样是后台列表感强，筛选、状态和列表之间缺少层次。继续复核后确认管理员会话下列表正常，因此受限态需要被视为单独页面状态，而不是简单 403 文本。

改造建议：
把角色筛选、用户状态统计和搜索栏做成玻璃管理工具带。用户列表保持实体表格，角色和风险等级标签用半透明 pill 提升质感。

### 14.6 实现层根因追踪

继续深挖后，可以把本轮最关键的视觉问题追到以下实现根因：

#### 14.6.1 主画布过窄不是错觉，而是三层宽度约束叠加

涉及实现：

- `apps/web/components/app-shell.tsx`
- `apps/web/components/ui/page-container.tsx`
- `apps/web/components/workspace-split-layout.tsx`

当前宽度链路是：

1. 左侧导航在 `AppShell` 中默认宽度约 `208px` 到 `220px`
2. 右侧 `CopilotDock` 默认宽度约 `380px` 到 `430px`
3. 主内容再经过 `PageContainer max-w-[1320px]`
4. 进入 `WorkspaceSplitLayout` 后，很多页面又把 secondary panel 设到 `38%`

这会导致在 `1440px` 常见桌面宽度下：

- 中间主画布先被左右两栏吃掉大量空间。
- 再进入双栏 split 后，primary 区很容易只剩 `500px` 到 `650px` 级别的可用宽度。
- 图表、表格、筛选器因此都被迫“挤成窄栏”，进一步放大白卡堆叠感。

结论：

- 当前很多页面看起来“信息不舒展”，根因不只是视觉样式，而是布局宽度预算已经不够。
- 如果不先调整壳层宽度策略，单独加毛玻璃只会让窄栏显得更拥挤。

#### 14.6.2 `WorkspaceToolbar` 被用成了首屏常驻配置台

涉及实现：

- `apps/web/components/workspace-toolbar.tsx`
- 使用它的 12 个页面：
- `decision / events / execution / paper-trading / performance / portfolio / research / risk / screener / search / skills / workspace-templates`

当前 `WorkspaceToolbar` 同时承载：

- 工作区切换
- 工作区重命名
- 云端同步
- 工作区模板
- 保存视图
- 布局预设
- 左右栏宽度
- 页面面板模式
- 跨页任务编排

这意味着它本质上是“工作区配置中心”，但现在却直接占据了多个业务页首屏。结果是：

- 页面一进来先看到配置，不是内容。
- Hero、图表、表格、洞察都被压到首屏下方。
- 它即使做成玻璃，也会成为一个过高、过重的大型工具箱。

结论：

- `WorkspaceToolbar` 不适合继续作为正文顶部常驻块。
- 它更适合转为折叠抽屉、右键工作区面板、Command 面板或独立“工作区设置”入口。

#### 14.6.3 标题系统漂移的根因是缺少共享页面头部组件

涉及实现：

- 多数页面直接各自硬编码 `<h1>`
- 当前没有统一的 `PageHero / PageHeader` 基础组件强制约束标题语气、层级和辅助信息结构

已确认的标题漂移包括：

- 句子式标题：如首页、行情页、研究页、组合页
- 功能式标题：如技术分析、估值分析、风险分析
- emoji + 中英混排：如 `🧠 AI 深度诊断报告生成器 (Diagnostic AI)`、`📈 期权全景分析 (Options)`、`🌍 宏观经济数据分析 (Macro Economics)`
- 管理页多处 emoji 标题：如 `💾 缓存管理`、`📭 死信队列`、`🔧 MCP 工具仪表盘`

结论：

- 视觉不统一不是偶发，而是因为标题系统没有被组件化。
- 后续应抽象统一的页面头部规范，限制：
- `h1` 的句式
- eyebrow 的有无
- 是否允许 emoji
- 副标题长度
- 右侧动作区位置

#### 14.6.4 Hydration mismatch 的根因已经定位到工作区持久化与首屏渲染冲突

涉及实现：

- `apps/web/store/workbench-store.ts`
- `apps/web/hooks/use-stock-code.ts`
- `apps/web/app/search/page.tsx`
- `apps/web/app/portfolio/page.tsx`

当前 `workbench-store` 使用：

- `persist`
- `skipHydration: true`
- 浏览器端手动 `rehydrate()`

同时，部分页面在 hydration 完成前就直接使用持久化上下文或默认股票码参与首屏渲染：

- `SearchPage` 使用 `useStockCode('600519')`
- `PortfolioPage` 会在工作区切换后把 `stockCode / portfolioId` 回填到 UI

控制台已确认的真实错误包括：

- `SearchPage` 出现 `Server: 600519 Client: 000001`
- `PortfolioPage` 出现 `Server: 未填写 Client: 000001`
- 随后 Suspense boundary 回退到 client rendering

结论：

- 这不是纯粹的 dev warning，而是会造成首屏文本跳动、状态闪动和精致感下降。
- 只要页面首屏依赖 persisted workbench context，就必须在 hydration 完成前使用稳定占位，或把上下文驱动内容延后到 client-only 区块。

#### 14.6.5 `CopilotDock` 同时占宽度、也占请求预算

涉及实现：

- `apps/web/components/copilot-dock.tsx`
- `apps/web/components/app-shell.tsx`

当前 `CopilotDock` 在展开时会主动加载：

- `chat/conversations`
- `chat/config`
- `auth/me`
- 页面上下文与动作

同时右栏默认宽度可达 `380px` 到 `430px`。这带来两个结果：

- 它显著压缩主内容宽度。
- 它即使只是“待机状态”，仍然显得像一个完整的独立产品区块。

从截图看，右栏常见状态是：

- 上部少量按钮
- 中部几个摘要卡
- 下部大块输入空白

结论：

- 这不是一个“高利用率常驻栏”，而是一个对宽度非常昂贵的副区。
- 它更适合：
- 变成按需唤起
- 或在不同页面按上下文切换为更轻的摘要卡 / 快捷动作条
- 而不是所有页面都保留同一高度和完整聊天输入区

#### 14.6.6 当前模板骨架是一刀切，内容类型没有驱动布局切换

涉及实现：

- `apps/web/components/workspace-split-layout.tsx`
- 多个页面直接共用 `primary + secondary`

当前至少 12 个页面直接共用同一模式：

- 顶部 `WorkspaceToolbar`
- 中部 `WorkspaceSplitLayout`
- 右侧 secondary summary

这对研究、执行、绩效这种高密度工作页是合理的，但对于：

- 搜索页
- 通知页
- 数据页
- 技能页
- 模板页

会出现明显问题：

- 低密度内容也被硬塞进双栏骨架
- summary 区经常只是少量提示文字
- 页面一旦内容不足，就显得像未填满的模板

结论：

- 页面模板需要至少拆成：
- Hero + 单主列
- Hero + 主副双列
- 图表主舞台 + 浮动工具条
- 列表 / 管理页模板
- 空状态集中模板

- 不应继续让所有页面默认复用同一工作区壳层。

### 14.7 分阶段落地优先级

#### P0：先建立全站玻璃骨架

范围：

- `AppShell`
- 顶部路径条
- 左右栏外壳
- 页面内部筛选条 / 工具条
- 通用 `glass-shell / glass-panel / glass-toolbar` token

目标：

- 先解决“全站几乎没有真正毛玻璃层”的问题。
- 建立壳层、工具条、浮层的统一材质，不先碰所有业务正文。

#### P1：先重做最能体现品牌的 8 个页面

优先页面：

- `/`
- `/market`
- `/stock`
- `/research`
- `/portfolio`
- `/strategy-market`
- `/strategy-market/[id]`
- `/execution`

目标：

- 让首页、行情、研究、策略、组合、执行形成一致的高质量工作台体验。
- 先把对外最有代表性的页面从“白卡工具台”升级为“分层玻璃工作台”。

#### P2：扩展到分析与系统页

优先页面：

- `/performance`
- `/risk`
- `/fundamental`
- `/technical`
- `/valuation`
- `/events`
- `/settings`
- `/user`
- `/admin`

目标：

- 把分析页和系统页纳入同一套玻璃语言。
- 同时补齐权限受限态、空状态、异常态的材质和布局。

### 14.8 具体实施要点

1. 不要把图表、表格正文、长文本正文直接放在强 blur 上。
2. 先做“外壳玻璃 + 内板实体”的双层结构，再做颜色和阴影微调。
3. 顶栏、筛选条、tab、浮动工具条比普通内容卡更适合优先上毛玻璃。
4. 红绿行情数字保持原先金融语义，不要因为追求玻璃效果而削弱对比度。
5. 右侧 AI 工作台必须和主内容形成同一材质体系，否则永远像外挂栏。
6. 管理页和权限受限页需要专门的“受限玻璃状态卡”，不能只留报错文本。

### 14.9 本轮审计总结

当前项目已经从“早期浅紫玻璃 SaaS”过渡到了“更克制的工作台”，方向是正确的；但从视觉完成度看，它现在停在了中间态：

- 不再花，但也还不够精致。
- 结构变清楚了，但材质仍然单一。
- 卡片变统一了，但深度、柔和度和空间关系还没有建立起来。

如果这一轮改造要引入毛玻璃，正确做法不是把全站重新刷成模糊白板，而是：

1. 让壳层和工具条先玻璃化。
2. 让主内容保留可读的实体底板。
3. 让背景产生轻微空气感。
4. 让圆角、描边、阴影和透明度一起建立层级。

这样做出来的结果，才会是“专业金融工作台上的克制玻璃”，而不是“装饰化玻璃 SaaS 皮肤”。
