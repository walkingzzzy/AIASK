# 策略超市五期开发方案

> 基线：`77e5aee`（master）| 编制日期：2026-02-21
>
> 依据：「策略超市集成可行性深度分析报告」审查结论 + 「策略超市：商品化与AI推荐」+「策略超市：实时收益与评价」两份核心方案

---

## 总体架构演进路线

```
现状                          目标
─────────────────────────────────────────────────────
Web → BFF → MCP(stdio)       Web → BFF → MCP(stdio)
      ↓                            ↓         ↓
  纯透传网关                   策略超市网关  WebSocket Gateway
      ↓                            ↓         ↓
  Python MCP                   Python MCP + Factor Store
  (158工具,内存状态)            (200+工具, TimescaleDB持久化, Redis在线层)
```

五期总览：

| 期数 | 主题 | 核心交付 |
|---|---|---|
| 一期 | 地基修复与快速见效 | 修复12个已知缺陷 + 回测指标契约 + 回归基线 + 核心页面图表 + Prompt工程 + 协方差收缩 |
| 二期 | 策略超市 MVP | 策略商品数据模型 + 因子存储 + RRF排名 + 电商化UI + 因子库扩展 |
| 三期 | 交易基础设施升级 | 模拟交易持久化 + 自动撮合 + 日频NAV + WebSocket + TCA服务化 + RiskMonitor自动处置 |
| 四期 | AI智能与个性化 | 情感提示工程 + NLP情绪分析 + AI持续推断用户画像 + 动态KYC适当性 + 合规风控 |
| 五期 | 社交生态与高级功能 | 策略生命周期 + 前向信号跟踪 + IP保护 + ~~社交跟单/游戏化（暂缓）~~ |

---

## 可行性评估与交付策略

### 总体可行性评级：中高

方案方向与《商品化与AI推荐》《实时收益与评价》总体一致，但当前代码基础决定了：P1/P2 能落地"平台底座 + 策略商品化初版"，P3 以后必须先补中间层能力（数据治理、实时链路、合规审计、风控执行闭环），否则会出现"文档先进、系统半成品"的交付风险。

### 逐期可行性

| 期数 | 可行性 | 核心依据 | 主要风险 |
|---|---|---|---|
| 一期 | 高 | 问题定位明确，代码修改范围可控 | JIT 策略修改可能影响现有回测结果 |
| 二期 | 中高 | DB 已有账户/交易/用户/向量基础，图表组件齐全 | 指标定义不稳会影响商品化可信度 |
| 三期 | 中 | 模拟交易 schema 已存在但代码未引用，撮合/NAV 从零构建 | 持久化迁移数据一致性；自动处置误触发 |
| 四期 | 中（合规/组织依赖高） | 有提示词框架与限流雏形，但合规规则需法务参与 | LLM 情绪评估准确性；合规责任边界 |
| 五期 | 中低（偏运营体系建设） | 需要数据运营与组织协同，非纯研发问题 | 策略生命周期运维复杂度；运营制度缺位 |

### 代码现状关键对照

**基础设施复用度（利好）**：`schema.py` 中已存在 `paper_accounts` / `paper_positions` / `paper_trades` / `paper_orders` / `alerts` / `price_alerts` / `combo_alerts` / `indicator_alerts` / `users` / `stock_embeddings` 等表结构。数据库层不是从零开始，应优先"字段补齐 + 索引优化 + 迁移规范"，避免重复建模。

**回测与模拟交易底座（一期必修）**：Position.add 双重累加、Sharpe/win_rate 口径差异会污染持仓与 PnL，如果一期不清理，后续推荐、排名、A/B 实验都会被脏指标放大误差。

**风险模型能力（中期可做）**：`risk_model.py` 已有 `calculate_var` / `calculate_barra_risk` / `stress_test`，具备起步条件，但目前是"算法函数级能力"，距离"在线服务级（数据质量、性能 SLA、审计可追踪）"还有工程差距。

**前端可视化（需单列里程碑）**：stock/backtest/portfolio 页面以 DataTable + KpiCard 为主，缺乏图表主视图。图表接入不可被当成"顺手补"，需作为独立交付项跟踪。

### 里程碑收敛式交付时间线

> 采用"降承诺 + 分层交付"策略，每个里程碑有明确的可验收产出。

| 里程碑 | 对应期数 | 核心交付 | 验收标准 |
|---|---|---|---|
| M1 | 一期 | 修 P0 缺陷 + 指标契约 + 回归基线 + 核心页面图表 | 12 个缺陷全部修复；指标契约文档通过评审；回测结果与基线对比差异可解释 |
| M2 | 二期前半 | 策略商品化最小闭环（策略卡 + 订阅 + 基础评价 + 审计日志） | 用户可浏览/订阅策略；评价数据可追溯 |
| M3 | 二期后半 | 离线因子仓 + 离线 RRF 推荐 + 因子库扩展 | 因子值 T+1 持久化；RRF 排名可复现 |
| M4 | 三期 | 模拟交易持久化 + 撮合引擎 + NAV 引擎 + WebSocket + 风控执行闭环 | 进程重启后持仓不丢失；限价单自动成交；NAV 日频更新 |
| M5 | 四期 | 规则引擎版适当性 + MIND-SAFE Prompt + NLP 情绪增强 + AI 画像 | 适当性规则可配置；推荐审计日志完整 |
| M6 | 五期 | 策略生命周期 + 前向信号跟踪 + IP 保护 | 策略状态机运转正常；前向收益数据 T+1 记录 |

### 交付原则

1. **指标可信优先**：一期的核心不是"修 bug"，而是建立"可信指标工程"——统一统计口径、定义指标契约、建立回归基线。后续所有商品化、推荐、评价都建立在可信指标之上
2. **复用优先于新建**：DB 方案以"复用现有 schema + 字段补齐 + 迁移规范"为主，减少冗余表
3. **离线先于实时**：因子计算、RRF 排名先做离线 T+1 版本，验证逻辑正确后再升级为准实时
4. **规则先于 AI**：KYC 适当性先用规则引擎实现确定性逻辑，LLM 仅作为解释层，降低合规风险
5. **平台能力先于运营体系**：五期的生命周期管理，研发只承诺平台能力与仪表板，运营制度由业务团队主导

---

## 一期：地基修复与快速见效

> 目标：修复现有代码缺陷，建立回测指标契约与回归基线，补齐核心交易页面图表，通过Prompt工程实现AI情绪→λ映射的最小闭环

### 1.1 现有代码缺陷修复

#### 1.1.1 Position.add 双倍计数修复
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/paper_trading.py` ~L126-129
- 问题：`self.quantity += quantity` 执行两次，每次买入持仓翻倍
- 修复：删除重复的 `self.quantity += quantity`，保留一处；同步修正 `self.avg_cost` 计算逻辑确保加权平均成本正确
- 测试：编写单元测试验证 `Position.add(100, 10.0)` 后 quantity=100 而非 200

#### 1.1.2 回测 Sharpe 路径统一
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/backtest/strategies.py` + `engine.py`
- 问题：JIT路径 `returns[returns != 0]` 过滤零收益日，mask路径保留零收益日
- 修复方案：
  - 在 `strategies.py` 的4个JIT策略中，移除 `returns = returns[returns != 0]` 过滤
  - 统一为：仅过滤 `equity_prev > 0`（防除零），保留零收益日
  - 在 `engine.py::_simulate_trades_from_masks` 中保持现有逻辑不变（已正确）
- 测试：对同一策略分别走JIT和mask路径，验证Sharpe差异 < 0.01

#### 1.1.3 回测 win_rate 统计口径统一
- 文件：同上
- 问题：momentum/rsi按单边成交计数，ma_cross按完整回合计数
- 修复方案：
  - 统一定义：`win_rate = profitable_round_trips / total_round_trips`
  - 在所有JIT策略中，trades计数改为 `trade_count // 2`（一买一卖为一个回合）
  - mask路径同步修改
- 测试：验证所有策略的win_rate在合理范围（0-100%），且同策略不同路径结果一致

#### 1.1.4 fear_greed_index 工具层修复
- 文件：`packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py`
- 问题：工具层零参数调用，服务层支持外部数据注入但未被使用
- 修复方案：
  ```python
  # 在 calculate_fear_greed_index 工具函数中：
  # 1. 获取上证指数近60日K线作为 index_klines
  index_klines = await get_kline_data("sh000001", period="daily", count=60)
  # 2. 获取涨跌停统计作为 breadth_data
  breadth_data = await get_limit_up_statistics()
  # 3. 传入服务层
  result = sentiment_service.calculate_fear_greed_index(
      index_klines=index_klines,
      breadth_data=breadth_data
  )
  ```
- 测试：验证返回值不再固定为50，且随市场数据变化

#### 1.1.5 压力测试工具层/服务层对齐
- 文件：`packages/akshare-mcp/src/akshare_mcp/tools/portfolio.py` + `services/risk_model.py`
- 问题：工具层用 `-30%` 固定乘法，服务层有行业分类冲击+PE利率敏感度
- 修复：工具层 `stress_test_portfolio` 改为调用 `risk_model.stress_test()` 方法

#### 1.1.6 quality 因子分母修复
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/factor_calculator.py` ~L102
- 问题：当 `profit_growth` 为 None 时分母仍为 3.0，缺少增长数据的股票质量评分被低估约33%
- 修复：动态计算有效分量数 `divisor = sum(1 for x in [roe_score, debt_score, growth_score] if x is not None)`

#### 1.1.7 should_i_buy 目标价 bug
- 文件：`packages/akshare-mcp/src/akshare_mcp/tools/decision.py` ~L448-456
- 问题：`current_price * (PE * 1.2 / PE)` = 永远是当前价×1.2
- 修复：改为基于行业中位数PE估算 `target_price = eps * industry_median_pe * 1.1`

#### 1.1.8 回测 JIT look-ahead bias 修复
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/backtest/engine.py`
- 问题：信号产生的同一根K线收盘价成交
- 修复：信号在第 `i` 根K线产生，成交价取第 `i+1` 根K线的开盘价（next-bar execution）
- 影响范围：所有4个JIT策略函数需同步修改

#### 1.1.9 shape-detector bar类型死代码清理
- 文件：`apps/web/lib/shape-detector.ts`
- 修复：在检测逻辑中补充 bar 类型的匹配条件，或删除未使用的类型定义

#### 1.1.10 协方差矩阵引入 Ledoit-Wolf 收缩
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/portfolio_optimization.py`
- 修复：
  ```python
  from sklearn.covariance import LedoitWolf
  # 替换所有 np.cov(returns_matrix) 调用
  lw = LedoitWolf().fit(returns_matrix.T)
  cov_matrix = lw.covariance_
  ```
- 影响：mean_variance / risk_parity / max_sharpe / risk_budget / black_litterman 五个方法

#### 1.1.11 Barra风险分解接入工具层
- 文件：`packages/akshare-mcp/src/akshare_mcp/tools/portfolio.py`
- 修复：新增 MCP 工具 `analyze_portfolio_risk_barra`，调用 `risk_model.calculate_barra_risk()`

#### 1.1.12 滑点建模风险标注
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/backtest/engine.py`
- 修复：在回测结果中新增字段 `slippage_model_note: "JIT路径使用均值化滑点估算，实际交易成本可能偏高"`

### 1.2 回测指标契约（Metrics Contract）

> 审查反馈指出：如果一期不建立可信指标体系，后续推荐、排名、A/B 实验都会被脏指标放大误差。指标契约是一期的核心交付，不是附属文档。

#### 1.2.1 指标定义规范
- 新增文件：`packages/akshare-mcp/docs/metrics-contract.md`
- 对每个回测输出指标写清：

| 指标 | 计算公式 | 数据口径 | JIT/mask 一致性 | 可比性说明 |
|---|---|---|---|---|
| `sharpe_ratio` | `mean(daily_returns) / std(daily_returns) × √252` | 含零收益日（持仓不变的平盘日计入） | 一致（1.1.2 修复后） | 跨策略可比 |
| `win_rate` | `profitable_round_trips / total_round_trips` | 一买一卖为一个完整回合 | 一致（1.1.3 修复后） | 跨策略可比 |
| `max_drawdown` | `max(1 - equity[i] / running_max[i])` | 基于日频权益曲线 | 一致 | 跨策略可比 |
| `total_return` | `(final_equity - initial_equity) / initial_equity` | 扣除交易成本后 | 一致 | 跨策略可比 |
| `profit_factor` | `gross_profit / gross_loss` | 按完整回合计算 | 一致（1.1.3 修复后） | 跨策略可比 |
| `trade_count` | `total_round_trips`（非单边成交数） | 一买一卖为一笔 | 一致（1.1.3 修复后） | 跨策略可比 |

#### 1.2.2 回归基线建立
- 修复前：对 4 个策略 × 3 个标的（沪深300/中证500/创业板指）× 2 个时间段（近1年/近3年）运行回测，保存 24 组基准结果
- 修复后：重新运行相同参数，对比验证：
  - JIT 路径与 mask 路径的 Sharpe 差异 < 0.01
  - win_rate 差异 < 1%
  - 所有指标符合指标契约定义
- 基线结果存入 `packages/akshare-mcp/tests/baselines/` 目录，后续每次修改回测引擎必须通过回归测试

### 1.3 核心交易页面图表集成

> 审查反馈强调：stock/backtest/portfolio 页面的图表接入不可被当成"顺手补"，需作为独立交付项跟踪验收。

#### 1.3.1 股票详情页 K线图
- 文件：`apps/web/app/stock/page.tsx`
- 实现：
  - 引入已有的 `CandlestickChart` 组件（来自 `@/components/charts`）
  - 数据源：调用 BFF `/api/data/kline` 接口获取日K数据
  - 交互：支持时间范围切换（1月/3月/6月/1年/全部）
  - 叠加均线（MA5/MA10/MA20/MA60），复用 `LineChart` 组件做叠加层
  - 下方成交量柱状图使用 `BarChart` 组件

#### 1.3.2 回测页权益曲线图
- 文件：`apps/web/app/backtest/page.tsx`
- 实现：
  - 引入 `LineChart` 组件
  - 数据源：回测结果中的 `equity_curve` 数组
  - 双Y轴：左轴为策略净值曲线，右轴为基准（沪深300）净值曲线
  - 面积填充模式展示回撤区间（水下曲线）
  - 下方叠加 `BarChart` 展示每日收益率柱状图

#### 1.3.3 组合页配置饼图
- 文件：`apps/web/app/portfolio/page.tsx`
- 实现：
  - 引入 `PieChart` 组件（甜甜圈模式）
  - 数据源：组合优化结果中的 `weights` 对象
  - 展示：各标的权重占比，hover显示具体百分比和预期收益贡献
  - 旁边用 `BarChart` 展示各标的风险贡献度

### 1.4 Prompt工程：AI情绪→λ映射

#### 1.4.1 系统Prompt扩展
- 文件：`apps/bff/src/chat/chat.tools.ts`
- 实现：在现有系统prompt末尾追加情绪评估指令块：
  ```
  ## 情绪感知与动态风险调整
  在每次用户交互中，你需要：
  1. 评估用户消息中的情绪状态，分为五档：
     - 极度焦虑(λ=5.0) → 用户提到"睡不着""害怕""崩盘"等
     - 偏焦虑(λ=3.5) → 用户频繁询问回撤、止损
     - 理性(λ=2.5) → 正常分析讨论
     - 偏乐观(λ=1.5) → 用户提到"抄底""机会""加仓"
     - 极度贪婪(λ=0.5) → 用户要求全仓、杠杆、追涨
  2. 当调用 optimize_portfolio 时，将评估的λ值传入 risk_aversion 参数
  3. 向用户解释你的情绪判断和对应的风险调整逻辑
  ```

#### 1.4.2 UserContext 风险偏好注入增强
- 文件：`apps/bff/src/chat/user-context.service.ts`
- 实现：在注入的上下文中增加用户历史交互模式摘要（最近5次对话的情绪标签统计）

---

## 二期：策略超市 MVP

> 目标：构建策略商品数据模型、因子存储持久化、RRF排名引擎、电商化策略浏览UI

### 2.1 策略商品数据模型

> DB 方案原则：**复用优先于新建**。`schema.py` 中已存在 `paper_accounts` / `paper_positions` / `paper_trades` / `paper_orders` / `alerts` 等表结构，三期持久化迁移时应优先"字段补齐 + 索引优化 + 迁移规范"，避免重复建模。以下仅为策略超市新增的业务表。

#### 2.1.1 数据库 Schema 设计
- 文件：`packages/akshare-mcp/src/akshare_mcp/db/schema.py` 新增表

```sql
-- 策略商品主表
CREATE TABLE strategies (
    id              SERIAL PRIMARY KEY,
    name            VARCHAR(100) NOT NULL,
    slug            VARCHAR(100) UNIQUE NOT NULL,
    description     TEXT,
    category        VARCHAR(50) NOT NULL,  -- momentum/value/quality/growth/macro/multi_factor
    sub_category    VARCHAR(50),
    author_id       INTEGER REFERENCES users(id),
    status          VARCHAR(20) DEFAULT 'draft',  -- draft/incubating/listed/deprecated
    visibility      VARCHAR(20) DEFAULT 'private', -- private/public
    config_json     JSONB NOT NULL,  -- 策略参数配置
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW()
);

-- 策略绩效指标表（每日更新）
CREATE TABLE strategy_metrics (
    id              SERIAL PRIMARY KEY,
    strategy_id     INTEGER REFERENCES strategies(id) ON DELETE CASCADE,
    date            DATE NOT NULL,
    nav             DECIMAL(18,6) DEFAULT 1.0,      -- 单位净值
    cumulative_return DECIMAL(12,6),
    sharpe_ratio    DECIMAL(8,4),
    max_drawdown    DECIMAL(8,6),
    win_rate        DECIMAL(6,4),
    profit_factor   DECIMAL(8,4),
    ic_mean         DECIMAL(8,6),
    ic_ir           DECIMAL(8,4),
    volatility      DECIMAL(8,6),
    turnover        DECIMAL(8,4),
    UNIQUE(strategy_id, date)
);
SELECT create_hypertable('strategy_metrics', 'date');

-- 策略评价表
CREATE TABLE strategy_reviews (
    id              SERIAL PRIMARY KEY,
    strategy_id     INTEGER REFERENCES strategies(id) ON DELETE CASCADE,
    user_id         INTEGER REFERENCES users(id),
    rating          SMALLINT CHECK (rating BETWEEN 1 AND 5),
    comment         TEXT,
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(strategy_id, user_id)
);

-- 策略订阅关系表
CREATE TABLE strategy_subscriptions (
    id              SERIAL PRIMARY KEY,
    strategy_id     INTEGER REFERENCES strategies(id) ON DELETE CASCADE,
    user_id         INTEGER REFERENCES users(id),
    subscribed_at   TIMESTAMPTZ DEFAULT NOW(),
    status          VARCHAR(20) DEFAULT 'active',  -- active/paused/cancelled
    UNIQUE(strategy_id, user_id)
);
```

#### 2.1.2 BFF 策略模块
- 新建：`apps/bff/src/strategy/`
  - `strategy.module.ts` — NestJS模块注册
  - `strategy.controller.ts` — REST API
    - `GET /api/strategies` — 策略列表（支持分页、分类筛选、排序）
    - `GET /api/strategies/:slug` — 策略详情
    - `POST /api/strategies` — 创建策略（需认证）
    - `PUT /api/strategies/:id` — 更新策略
    - `POST /api/strategies/:id/subscribe` — 订阅策略
    - `DELETE /api/strategies/:id/subscribe` — 取消订阅
    - `POST /api/strategies/:id/reviews` — 提交评价
    - `GET /api/strategies/:id/metrics` — 获取绩效时间序列
    - `GET /api/strategies/ranking` — RRF综合排名
  - `strategy.service.ts` — 业务逻辑，调用MCP工具获取回测/因子数据

#### 2.1.3 MCP 策略管理工具
- 新增工具：
  - `create_strategy` — 创建策略记录
  - `update_strategy_metrics` — 更新策略绩效（定时任务调用）
  - `get_strategy_ranking` — RRF排名计算
  - `get_strategy_detail` — 策略详情（含最新指标）

### 2.2 因子存储持久化（Factor Store）

#### 2.2.1 factor_values 表设计
```sql
CREATE TABLE factor_values (
    code            VARCHAR(20) NOT NULL,
    factor_name     VARCHAR(50) NOT NULL,
    date            DATE NOT NULL,
    value           DECIMAL(18,8),
    rank_pct        DECIMAL(6,4),  -- 截面排名百分位
    updated_at      TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (code, factor_name, date)
);
SELECT create_hypertable('factor_values', 'date');

-- 因子IC历史表
CREATE TABLE factor_ic_history (
    factor_name     VARCHAR(50) NOT NULL,
    date            DATE NOT NULL,
    forward_period  INTEGER NOT NULL,  -- 前向天数
    pearson_ic      DECIMAL(8,6),
    spearman_ic     DECIMAL(8,6),
    ic_ir           DECIMAL(8,4),
    PRIMARY KEY (factor_name, date, forward_period)
);
SELECT create_hypertable('factor_ic_history', 'date');
```

#### 2.2.2 定时因子计算任务
- 文件：新增 `packages/akshare-mcp/src/akshare_mcp/services/factor_scheduler.py`
- 实现：
  - 使用 `pg_cron` 或 Python `APScheduler` 在每个交易日收盘后（15:30）触发
  - 遍历全A股（约5000只），计算8个因子值写入 `factor_values`
  - 计算截面排名百分位 `rank_pct`
  - 计算当日IC（Pearson + Spearman）写入 `factor_ic_history`
  - 执行流程：
    ```
    15:30 触发 → 拉取当日收盘数据 → 批量计算因子值 → 写入factor_values
                                    → 计算前向收益(1/5/10/20日) → 计算IC → 写入factor_ic_history
    ```

#### 2.2.3 Redis 在线层
- 文件：`apps/bff/src/common/cache.service.ts` 扩展
- 实现：
  - 新增 `getLatestFactorValues(codes: string[], factors: string[])` 方法
  - 每日因子计算完成后，将最新一期因子值写入Redis（key: `factor:{code}:{factor_name}`，TTL: 25h）
  - 策略超市前端查询因子暴露度时，优先从Redis读取

### 2.3 RRF 混合排名引擎

#### 2.3.1 排名算法实现
- 文件：新增 `packages/akshare-mcp/src/akshare_mcp/services/ranking.py`
- 实现：
  ```python
  def calculate_rrf_ranking(strategies: list, k: int = 60) -> list:
      """
      RRF_score(d) = Σ 1/(k + rank_i(d))
      子排名维度：
        rank_1: sharpe_ratio 降序
        rank_2: max_drawdown 升序（回撤越小排名越前）
        rank_3: avg_rating 降序（用户评分）
        rank_4: subscriber_count 降序（订阅人数）
        rank_5: ic_ir 降序（信息比率）
      """
      sub_rankings = {}
      # 按各维度独立排序
      sub_rankings['sharpe'] = sorted(strategies, key=lambda s: -s.sharpe_ratio)
      sub_rankings['drawdown'] = sorted(strategies, key=lambda s: s.max_drawdown)
      sub_rankings['rating'] = sorted(strategies, key=lambda s: -s.avg_rating)
      sub_rankings['subscribers'] = sorted(strategies, key=lambda s: -s.subscriber_count)
      sub_rankings['ic_ir'] = sorted(strategies, key=lambda s: -s.ic_ir)

      # 计算RRF分数
      rrf_scores = {}
      for dim, ranked_list in sub_rankings.items():
          for rank, strategy in enumerate(ranked_list, 1):
              rrf_scores.setdefault(strategy.id, 0)
              rrf_scores[strategy.id] += 1.0 / (k + rank)

      # 按RRF分数降序排列
      return sorted(strategies, key=lambda s: -rrf_scores[s.id])
  ```

#### 2.3.2 排名定时更新
- 每日收盘后因子计算完成 → 更新 strategy_metrics → 重新计算RRF排名
- 排名结果缓存到Redis（key: `strategy_ranking`，TTL: 25h）

### 2.4 因子库扩展（8→50+）

#### 2.4.1 新增因子清单
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/factor_calculator.py`

| 因子类别 | 新增因子 | 计算逻辑 |
|---|---|---|
| 动量 | momentum_1m, momentum_3m, momentum_6m, momentum_12m | 不同回看窗口的价格变化率 |
| 反转 | reversal_5d, reversal_20d | 短期反转信号 |
| 波动率 | realized_vol_20d, realized_vol_60d, idiosyncratic_vol | 已实现波动率、特质波动率 |
| 价值 | ep_ratio, bp_ratio, sp_ratio, dp_ratio, cf_yield | 盈利/净资产/营收/股息/现金流收益率 |
| 质量 | roe_ttm, roa_ttm, gross_margin, accruals, earnings_stability | 盈利能力、应计项目、盈利稳定性 |
| 成长 | revenue_growth_yoy, profit_growth_yoy, roe_change | 营收/利润增速、ROE变化 |
| 规模 | ln_market_cap, ln_free_float_cap | 对数市值、对数流通市值 |
| 流动性 | turnover_20d, amihud_illiquidity, volume_ratio | 换手率、Amihud非流动性、量比 |
| 技术 | rsi_14, macd_signal, bollinger_position, atr_ratio | RSI、MACD信号、布林带位置、ATR比率 |
| 另类 | north_fund_flow_ratio, margin_balance_ratio | 北向资金流入比、融资余额比 |

#### 2.4.2 因子元数据注册
- 更新 `SUPPORTED_FACTORS` 字典（`tools/quant.py`），从8个扩展到50+
- 每个因子包含：name, category, description, default_period, data_dependency

### 2.5 电商化策略超市 UI

#### 2.5.1 策略超市首页
- 新建：`apps/web/app/strategy-market/page.tsx`
- 布局：
  ```
  ┌─────────────────────────────────────────────┐
  │  策略超市                    [搜索] [筛选]   │
  ├─────────────────────────────────────────────┤
  │  [全部] [动量] [价值] [质量] [多因子] [宏观] │  ← 分类Tab
  ├─────────────────────────────────────────────┤
  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
  │  │策略卡│ │策略卡│ │策略卡│ │策略卡│       │  ← 毛玻璃卡片网格
  │  │片    │ │片    │ │片    │ │片    │       │
  │  └──────┘ └──────┘ └──────┘ └──────┘       │
  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
  │  │      │ │      │ │      │ │      │       │
  │  └──────┘ └──────┘ └──────┘ └──────┘       │
  └─────────────────────────────────────────────┘
  ```

#### 2.5.2 策略商品卡片组件
- 新建：`apps/web/components/strategy-card.tsx`
- 设计：
  - 毛玻璃背景（复用 `chat-structured-markdown.tsx` 的 glass-morphism 风格）
  - 顶部：策略名称 + 分类标签Badge
  - 中部：Sparkline迷你走势图（复用 `LineChart` mini模式，展示近30日净值）
  - 底部KPI行：年化收益率 | Sharpe | 最大回撤
  - 社会化证明：⭐评分 + 订阅人数
  - 操作按钮："查看详情" / "加入组合"

#### 2.5.3 策略详情页（PDP）
- 新建：`apps/web/app/strategy-market/[slug]/page.tsx`
- 布局：
  - 顶部：策略名称、作者、分类、状态Badge、订阅/取消按钮
  - 净值曲线区：`LineChart` 展示历史NAV，叠加沪深300基准，支持时间范围切换
  - KPI仪表盘：年化收益 / Sharpe / 最大回撤 / 胜率 / 盈利因子 / IC均值
  - 因子暴露度：`BarChart` 展示策略在各因子上的暴露度
  - IC衰减曲线：`LineChart` 展示不同forward period下的IC值
  - 用户评价区：星级评分 + 评论列表 + 提交评价表单

#### 2.5.4 投资组合购物车
- 新建：`apps/web/store/cart-store.ts`（Zustand store）
- 功能：
  - `addStrategy(strategyId)` — 添加策略到购物车
  - `removeStrategy(strategyId)` — 移除
  - `setWeight(strategyId, weight)` — 手动调整权重
  - `checkout()` — 调用 `optimize_portfolio` 计算最优权重
- UI：屏幕右侧抽屉式购物车面板，展示已选策略列表和组合预览

### 2.6 IC/信号衰减可视化面板

#### 2.6.1 因子分析仪表盘
- 新建：`apps/web/app/factor-analysis/page.tsx`
- 功能：
  - 因子选择器：下拉选择因子名称
  - IC时间序列图：`LineChart` 展示历史IC值（Pearson + Spearman双线）
  - IC衰减图：`LineChart` 展示IC随forward period的衰减曲线
  - 因子分组回测图：`LineChart` 展示五档分组的累计收益曲线
  - 数据源：`factor_ic_history` 表 + MCP `calculate_factor_ic` 工具

---

## 三期：交易基础设施升级

> 目标：将模拟交易从内存迁移到数据库，构建自动撮合引擎、日频NAV引擎、WebSocket实时推送、RiskMonitor自动处置

### 3.1 模拟交易持久化

#### 3.1.1 激活已有DB Schema
- 现状：`paper_accounts` / `positions` / `trades` / `orders` 四张表已在 `schema.py` 中定义（含索引与部分兼容迁移逻辑），但代码从未引用
- **复用优先**：不新建表，而是对现有 schema 做字段补齐 + 索引优化：
  - `paper_orders` 补充 `status` 字段的状态机枚举约束
  - `paper_positions` 补充 `avg_cost` 精度（DECIMAL(18,6)）
  - 添加必要的复合索引（`account_id + status`、`account_id + code`）
- 实现：在 `paper_trading.py` 中新增 `PaperTradingRepository` 类
  ```python
  class PaperTradingRepository:
      async def save_account(self, account: PaperAccount) -> int: ...
      async def load_account(self, account_id: int) -> PaperAccount: ...
      async def save_order(self, order: Order) -> int: ...
      async def save_trade(self, trade: Trade) -> int: ...
      async def save_position(self, position: Position) -> int: ...
      async def get_account_history(self, account_id, start, end) -> list: ...
  ```
- 迁移策略：双写模式 — 内存操作完成后异步写入DB，读取时优先内存、fallback到DB
- 进程重启恢复：启动时从DB加载所有active账户的positions和pending orders到内存

#### 3.1.2 订单状态机
- 状态流转：`pending → submitted → partial_filled → filled / cancelled / rejected`
- 每次状态变更写入 `orders` 表并记录时间戳
- 新增 `order_events` 表记录完整审计日志

### 3.2 自动撮合引擎

#### 3.2.1 撮合服务设计
- 新增：`packages/akshare-mcp/src/akshare_mcp/services/matching_engine.py`
- 核心逻辑：
  ```python
  class MatchingEngine:
      def __init__(self, price_provider, slippage_model):
          self.pending_orders = []  # 从DB加载
          self.price_provider = price_provider

      async def on_price_update(self, code: str, price: float, volume: int):
          """每次价格更新时检查所有pending orders"""
          for order in self.pending_orders:
              if order.code != code or order.status != 'pending':
                  continue
              if self._should_fill(order, price, volume):
                  fill_price = self._calculate_fill_price(order, price)
                  await self._execute_fill(order, fill_price)

      def _should_fill(self, order, price, volume) -> bool:
          if order.type == 'limit' and order.side == 'buy':
              return price <= order.price
          if order.type == 'limit' and order.side == 'sell':
              return price >= order.price
          if order.type == 'stop' and order.side == 'sell':
              return price <= order.stop_price
          # ... stop-limit 等
  ```

#### 3.2.2 价格数据源
- 交易时段（9:30-15:00）：每15秒从AKShare拉取最新价格
- 非交易时段：不触发撮合
- 涨跌停过滤：复用回测引擎的涨跌停判断逻辑（主板10%/创业板20%/ST 5%）

### 3.3 日频 NAV 引擎

#### 3.3.1 NAV 计算服务
- 新增：`packages/akshare-mcp/src/akshare_mcp/services/nav_engine.py`
- 计算公式：`NAV = (Σ position_i.quantity × close_price_i + cash) / total_shares`
- 每日收盘后触发：
  1. 获取所有持仓标的的收盘价
  2. 按市价计值（Mark-to-Market）
  3. 扣除当日产生的交易费用
  4. 写入 `strategy_metrics` 表

#### 3.3.2 公司行动处理
- 新增：`packages/akshare-mcp/src/akshare_mcp/services/corporate_actions.py`
- 处理类型：
  - 现金分红：除息日自动将 `dividend × quantity` 计入现金账户
  - 送股/转增：按比例调整持仓数量和成本价
  - 股票拆分：调整历史价格序列和持仓数量
- 数据源：AKShare `stock_dividents_cninfo` 接口
- 触发时机：每日开盘前检查当日是否有公司行动事件

### 3.4 WebSocket 实时推送

#### 3.4.1 NestJS WebSocket Gateway
- 新建：`apps/bff/src/ws/ws.gateway.ts`
- 实现：
  ```typescript
  @WebSocketGateway({ namespace: '/ws', cors: true })
  export class WsGateway {
    @SubscribeMessage('subscribe:portfolio')
    handlePortfolioSub(client: Socket, payload: { accountId: number }) {
      client.join(`portfolio:${payload.accountId}`);
    }

    @SubscribeMessage('subscribe:strategy')
    handleStrategySub(client: Socket, payload: { strategyId: number }) {
      client.join(`strategy:${payload.strategyId}`);
    }

    // 由后端服务主动推送
    pushPortfolioUpdate(accountId: number, data: PortfolioSnapshot) {
      this.server.to(`portfolio:${accountId}`).emit('portfolio:update', data);
    }

    pushStrategyNav(strategyId: number, data: NavUpdate) {
      this.server.to(`strategy:${strategyId}`).emit('strategy:nav', data);
    }
  }
  ```

#### 3.4.2 前端 WebSocket 集成
- 新建：`apps/web/lib/ws.ts`
- 使用 `socket.io-client`，封装 `useWebSocket` hook
- 替换现有轮询：
  - 模拟交易页：从15秒轮询改为WebSocket推送
  - 自选股页：从30秒轮询改为WebSocket推送
  - 通知：保留30秒轮询（低频场景不值得WebSocket）

### 3.5 滑点/TCA 服务化

#### 3.5.1 独立TCA服务
- 新增：`packages/akshare-mcp/src/akshare_mcp/services/tca.py`
- 从回测引擎抽取滑点模型为独立服务：
  ```python
  class TCAService:
      def __init__(self):
          self.models = {
              'fixed': FixedSlippageModel(rate=0.001),
              'volume': VolumeBasedSlippageModel(),
              'impact': MarketImpactModel()  # Almgren-Chriss
          }

      def estimate_cost(self, code, side, quantity, price, volume) -> TCAReport:
          """返回各模型的成本估算"""
          return TCAReport(
              commission=self._calc_commission(price, quantity),
              stamp_tax=self._calc_stamp_tax(price, quantity, side),
              transfer_fee=self._calc_transfer_fee(price, quantity),
              slippage={
                  model_name: model.calculate(price, quantity, volume)
                  for model_name, model in self.models.items()
              },
              total_cost=...
          )
  ```

#### 3.5.2 统一前后端费率
- 前端 `apps/web/app/paper-trading/page.tsx` 的费率常量（佣金0.025% min 5元、印花税0.05%、过户费0.001%）
- 后端 `TCAService` 使用相同费率参数
- 通过 BFF 配置接口 `GET /api/config/trading-fees` 统一下发

### 3.6 预警系统修复

#### 3.6.1 指标预警执行层补全
- 文件：`packages/akshare-mcp/src/akshare_mcp/tools/alerts.py`
- 修复 `check_all_alerts`：
  - 支持 `indicator` 类型的 RSI/MACD/Volume 预警评估
  - 对每个指标预警，实时计算当前指标值并与阈值比较
  - 支持 `combo` 类型的组合预警评估（AND/OR逻辑组合）

#### 3.6.2 预警持久化
- 将 `_alerts_store: Dict` 迁移到 TimescaleDB 的 `alerts` 表
- 预警触发记录写入 `alert_events` 表（含触发时间、触发值、通知状态）

### 3.7 RiskMonitor 自动处置升级

> 可行性报告将"RiskMonitor 仅检测不自动处置"列为四大核心风险之一。策略超市若需"可托管运行"，必须在被动检测之上构建自动处置执行层。

#### 3.7.1 现状与问题
- `risk_model.py` 的 `RiskMonitor` 类仅有 `check_position_size` / `check_leverage` / `check_drawdown` 三个检查方法
- 所有方法返回 `(bool, str)` 元组，纯检测/告警角色，不触发任何交易动作
- 策略超市的订阅策略需要自动执行风控规则，不能依赖用户手动干预

#### 3.7.2 自动处置执行层
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/risk_executor.py`（新增）
- 设计：在 `RiskMonitor` 检测结果之上，通过撮合引擎自动执行风控动作
  ```python
  class RiskExecutor:
      def __init__(self, risk_monitor: RiskMonitor, matching_engine: MatchingEngine):
          self.monitor = risk_monitor
          self.engine = matching_engine

      async def enforce(self, account_id: int) -> list[RiskAction]:
          """检查风控规则并自动执行处置"""
          checks = self.monitor.check_all(account)
          actions = []

          # 单持仓超限 → 自动减仓至上限
          for pos_check in checks['position_checks']:
              if not pos_check['passed']:
                  reduce_qty = self._calc_reduce_quantity(pos_check)
                  order = Order(side='sell', quantity=reduce_qty, type='market')
                  await self.engine.submit_order(order)
                  actions.append(RiskAction('reduce_position', order))

          # 回撤超限 → 全部平仓
          if not checks['drawdown']['passed']:
              for position in account.positions:
                  order = Order(side='sell', quantity=position.quantity, type='market')
                  await self.engine.submit_order(order)
                  actions.append(RiskAction('force_liquidate', order))

          return actions
  ```
- 触发时机：每次撮合引擎 `on_price_update` 后自动调用 `enforce`
- 所有自动处置动作写入 `order_events` 表，标记 `source='risk_executor'`

---

## 四期：AI智能与个性化

> 目标：构建情感提示工程框架、NLP情绪分析、AI持续推断用户画像与大五人格、动态KYC适当性管理、合规风控

### 4.1 情感提示工程框架（MIND-SAFE）

#### 4.1.1 多层Prompt架构
- 文件：`apps/bff/src/chat/chat.tools.ts` 重构
- 设计四层Prompt结构：
  ```
  Layer 1: 系统角色定义（不变）
  Layer 2: 情绪感知指令（一期已有基础版，此期升级）
  Layer 3: 安全约束与合规红线
  Layer 4: 用户上下文注入（动态生成）
  ```

#### 4.1.2 Layer 2 情绪感知升级
- 从一期的5档简单映射升级为结构化情绪评估：
  ```
  ## 情绪评估协议
  对每条用户消息执行以下分析（内部推理，不直接输出）：
  1. 情绪极性：-1.0(极度恐慌) ~ 0(中性) ~ +1.0(极度贪婪)
  2. 情绪强度：0(无情绪) ~ 1.0(极强)
  3. 认知偏误检测：
     - 锚定效应：用户是否过度关注某个价格点
     - 确认偏误：用户是否只寻求支持其观点的信息
     - 损失厌恶：用户是否对亏损反应过度
     - 从众心理：用户是否因他人行为而决策
  4. 风险厌恶系数映射：λ = 2.5 + 2.5 × 情绪极性 × 情绪强度
  ```

#### 4.1.3 Layer 3 安全约束
  ```
  ## 合规红线（不可违反）
  1. 永远不得给出"保证收益"的承诺
  2. 每次推荐必须附带风险提示："历史业绩不代表未来表现"
  3. 当检测到用户试图全仓单一标的时，必须发出警告
  4. 不得推荐超出用户KYC风险等级的策略
  5. 所有推荐必须可追溯（记录推理链）
  ```

### 4.2 NLP 情绪分析增强

#### 4.2.1 股票情绪分析重构
- 文件：`packages/akshare-mcp/src/akshare_mcp/services/sentiment.py`
- 现状：`50 + price_change*100 + (volume_ratio-1)*20`（纯价量公式）
- 升级方案：
  - 保留价量分量作为基础层（权重40%）
  - 新增新闻情绪分量（权重30%）：调用LLM对最近新闻标题做情绪分类
  - 新增资金流向分量（权重30%）：北向资金 + 融资余额变化
  ```python
  def analyze_sentiment_v2(self, code: str) -> dict:
      price_score = self._price_momentum_score(code)      # 现有逻辑
      news_score = self._news_sentiment_score(code)        # 新增
      fund_flow_score = self._fund_flow_score(code)        # 新增
      composite = 0.4 * price_score + 0.3 * news_score + 0.3 * fund_flow_score
      return {"score": composite, "components": {...}}
  ```

#### 4.2.2 新闻情绪评分
- 数据源：MCP工具 `get_stock_news` 获取最近20条新闻
- 评分方式：使用ChatService的LLM对新闻标题批量分类
  - 正面(+1) / 中性(0) / 负面(-1)
  - 加权平均（越新的新闻权重越高，指数衰减）

### 4.3 大五人格与投资偏好映射（AI持续推断）

#### 4.3.1 设计理念
- 静态问卷的根本缺陷：人的心理状态和风险偏好随市场环境、个人经历持续变化，一次性问卷只是某个时刻的快照，填完即过时
- 正确做法：AI在每次对话中从上下文持续推断用户的人格特征和风险状态，形成动态演化的用户画像

#### 4.3.2 对话行为信号提取
- 文件：`apps/bff/src/chat/user-context.service.ts` 扩展
- AI在系统Prompt中被指令从对话中提取以下行为信号（内部推理，不直接输出给用户）：
  ```
  ## 用户画像持续推断协议
  在每轮对话中，从用户的语言和行为中提取以下信号：

  【神经质信号】
  - 高：频繁询问回撤/止损、使用"担心""害怕""睡不着"等词汇、短时间内反复查看同一持仓
  - 低：面对亏损时语气平稳、主动讨论长期配置

  【开放性信号】
  - 高：主动询问新策略/另类因子、愿意尝试复杂产品、对技术细节好奇
  - 低：只关注熟悉的标的、拒绝新推荐、偏好简单直接的答案

  【从众信号】
  - 高：提到"别人都在买""热门""排行榜"、询问他人持仓
  - 低：独立分析、不关心他人操作

  【贪婪/恐惧信号】
  - 贪婪：要求加仓/杠杆/全仓、追涨、对收益预期不切实际
  - 恐惧：要求清仓/止损、恐慌性提问、对小幅波动过度反应

  每轮对话结束后，输出结构化画像更新（通过function call写入）：
  {
    "neuroticism": 0.0~1.0,
    "openness": 0.0~1.0,
    "herd_tendency": 0.0~1.0,
    "greed_fear_axis": -1.0(恐惧)~+1.0(贪婪),
    "confidence_level": "low/medium/high",  // 推断置信度
    "evidence": ["用户说'最近跌得我睡不着'", "连续3次询问止损位"]
  }
  ```

#### 4.3.3 画像存储与衰减机制
- 新增表：
  ```sql
  CREATE TABLE user_profile_snapshots (
      id              SERIAL PRIMARY KEY,
      user_id         INTEGER REFERENCES users(id),
      session_id      VARCHAR(50),
      neuroticism     DECIMAL(4,3),
      openness        DECIMAL(4,3),
      herd_tendency   DECIMAL(4,3),
      greed_fear_axis DECIMAL(4,3),
      confidence      VARCHAR(10),
      evidence        JSONB,
      created_at      TIMESTAMPTZ DEFAULT NOW()
  );
  ```
- 衰减机制：计算当前画像时，对历史快照按时间指数衰减加权
  ```python
  def get_current_profile(user_id: int) -> UserProfile:
      snapshots = db.query(user_profile_snapshots)
          .filter(user_id=user_id)
          .order_by(created_at.desc())
          .limit(50)
      # 指数衰减：半衰期7天
      weights = [exp(-0.693 * days_ago / 7) for s in snapshots]
      # 加权平均各维度
      return weighted_average(snapshots, weights)
  ```
- 效果：用户最近的对话行为权重最高，一周前的影响衰减到50%，一个月前基本忽略

#### 4.3.4 人格→因子权重动态映射
```python
def map_profile_to_factors(profile: UserProfile) -> FactorPreference:
    """根据AI持续推断的用户画像，动态计算因子偏好和风险参数"""
    # 风险厌恶系数：神经质越高、恐惧越强 → λ越大
    risk_aversion = 2.5 + 2.0 * profile.neuroticism - 1.5 * profile.greed_fear_axis

    # 最大回撤容忍度：与神经质负相关
    mdd_tolerance = 0.25 - 0.15 * profile.neuroticism

    # 策略类别偏好权重
    if profile.neuroticism > 0.7:
        preferred = ['quality', 'low_volatility', 'value']
    elif profile.openness > 0.7:
        preferred = ['momentum', 'growth', 'alternative']
    else:
        preferred = ['value', 'quality', 'multi_factor']

    return FactorPreference(
        risk_aversion=clamp(risk_aversion, 0.5, 5.0),
        max_drawdown_tolerance=clamp(mdd_tolerance, 0.05, 0.40),
        preferred_categories=preferred,
        avoid_herd=(profile.herd_tendency > 0.6)  # 从众倾向高时，降低热门策略权重
    )
```
- 注入：`UserContextService` 在构建上下文时读取最新画像，注入系统Prompt

### 4.4 KYC 适当性管理（AI动态评估）

> **交付原则：规则先于 AI**。适当性管理的核心逻辑必须用确定性规则引擎实现（可审计、可解释、可配置），LLM 仅作为解释层向用户传达风险提示。这样既降低合规风险，又避免 LLM 幻觉导致不当推荐。

#### 4.4.1 设计理念
- 传统KYC问卷的问题：用户填写时可能不诚实、填完后状态变化、问卷无法捕捉真实的风险承受能力
- 新方案分两层：
  - **规则引擎层（确定性）**：基于交易行为和画像数据的量化评分，所有阈值可配置、可审计
  - **LLM 解释层（辅助）**：AI 在对话中向用户解释风险等级变化的原因和建议，但不参与等级计算本身
- 问卷仅作为初始基线（首次注册时填写），后续由规则引擎持续校准

#### 4.4.2 行为驱动的风险评估
- 新增：`packages/akshare-mcp/src/akshare_mcp/services/kyc_dynamic.py`
- AI从以下维度持续评估用户真实风险承受能力：

| 信号维度 | 数据来源 | 评估逻辑 |
|---|---|---|
| 对话情绪 | 4.3节的画像快照 | 长期高神经质 → 降低风险等级 |
| 交易行为 | 模拟交易记录 | 频繁止损/恐慌清仓 → 降低；稳定持有 → 维持 |
| 持仓集中度 | 组合数据 | 单标的占比>50% → 标记为激进行为 |
| 亏损反应 | 对话上下文 | 亏损后情绪崩溃 → 降低；理性分析 → 维持 |
| 杠杆倾向 | 对话中的请求 | 反复要求加杠杆 → 标记为高风险偏好 |
| 知识水平 | 对话中的提问 | 不理解基本概念 → 限制复杂策略 |

```python
class DynamicKYCService:
    def evaluate_risk_level(self, user_id: int) -> RiskLevel:
        profile = self.get_current_profile(user_id)      # 4.3节的画像
        trading = self.get_trading_behavior(user_id)      # 交易行为统计
        initial_kyc = self.get_initial_questionnaire(user_id)  # 初始问卷基线

        # 综合评分（行为权重60%，画像权重30%，初始问卷10%）
        behavior_score = self._score_trading_behavior(trading)
        profile_score = self._score_profile(profile)
        initial_score = initial_kyc.score if initial_kyc else 50

        composite = 0.6 * behavior_score + 0.3 * profile_score + 0.1 * initial_score

        # 映射为风险等级
        if composite < 20: return RiskLevel.C1  # 保守
        if composite < 40: return RiskLevel.C2  # 稳健
        if composite < 60: return RiskLevel.C3  # 平衡
        if composite < 80: return RiskLevel.C4  # 积极
        return RiskLevel.C5                      # 激进
```

#### 4.4.3 适当性校验与干预
- 策略匹配规则（同原方案）：
  - C1(保守)：仅可订阅 max_drawdown < 5% 的策略
  - C2(稳健)：max_drawdown < 10%
  - C3(平衡)：max_drawdown < 20%
  - C4(积极)：max_drawdown < 30%
  - C5(激进)：无限制
- AI推荐时强制校验：推荐策略的风险等级不得超过用户当前动态KYC等级
- 等级下调干预：当AI检测到用户风险等级应下调时（如持续恐慌情绪），主动在对话中建议调整持仓，而非静默限制
- 等级上调保护：用户风险等级上调需要至少30天的稳定行为数据支撑，防止短期贪婪情绪导致等级虚高

### 4.5 合规风控网关

#### 4.5.1 交易频率断路器
- 文件：`apps/bff/src/common/rate-limit.guard.ts` 扩展
- 现状问题：当前 `RateLimitGuard` 是内存态、按 `ip:path` 的基础限流，适合单实例防护，不适合多实例一致限流
- 升级方案：
  - 限流计数器从内存迁移到 Redis（`INCR` + `EXPIRE` 原子操作），支持多实例部署一致性
  - 新增交易级别限流（参照证监会程序化交易管理细则）：
    - 单账户每秒申报上限：10笔（模拟交易场景远低于监管300笔红线）
    - 单账户每日申报上限：500笔
  - 超限时返回 `429 Too Many Requests` 并记录审计日志
  - 限流阈值参数化存储（Redis config key），可按用户类型/市场状态动态调整

#### 4.5.2 推荐审计日志
- 新增表：`recommendation_audit_log`
  ```sql
  CREATE TABLE recommendation_audit_log (
      id              SERIAL PRIMARY KEY,
      user_id         INTEGER REFERENCES users(id),
      strategy_id     INTEGER REFERENCES strategies(id),
      emotion_state   JSONB,        -- AI评估的情绪状态
      risk_aversion   DECIMAL(4,2), -- 使用的λ值
      kyc_level       VARCHAR(5),   -- 用户动态KYC等级
      profile_snapshot JSONB,       -- 推断时的用户画像快照
      input_features  JSONB,        -- 输入特征快照（因子值、市场状态等）
      model_version   VARCHAR(50),  -- 推荐模型/规则版本号
      reasoning_chain TEXT,          -- LLM推理链
      review_status   VARCHAR(20) DEFAULT 'auto',  -- auto/human_reviewed/flagged
      created_at      TIMESTAMPTZ DEFAULT NOW()
  );
  ```
- 每次AI推荐策略时自动写入，确保推荐逻辑可追溯可审计
- 审计留痕要求：输入特征、模型版本、输出理由三要素缺一不可，支持事后人工复核

---

## 五期：社交生态与高级功能

> 目标：构建策略生命周期管理、前向信号跟踪、IP保护（社交跟单与游戏化暂缓至后续迭代）
>
> 交付定位：本期偏运营体系建设而非纯研发，研发只承诺平台能力与仪表板，运营制度（策略审核标准、上下架规则、策略师认证流程）由业务团队主导制定。

### 5.1 策略生命周期管理

#### 5.1.1 策略接口定义（Strategy ABC）
- 新增：`packages/akshare-mcp/src/akshare_mcp/services/backtest/strategy_base.py`
  ```python
  from abc import ABC, abstractmethod

  class IStrategy(ABC):
      @classmethod
      @abstractmethod
      def name(cls) -> str: ...

      @classmethod
      @abstractmethod
      def description(cls) -> str: ...

      @abstractmethod
      def generate_signals(self, data: pd.DataFrame) -> pd.Series:
          """输入K线数据，输出信号序列 (1=买入, -1=卖出, 0=持有)"""
          ...

      @abstractmethod
      def get_parameters(self) -> dict: ...

      @abstractmethod
      def set_parameters(self, params: dict): ...
  ```
- 将现有4个硬编码策略（ma_cross/momentum/rsi/buy_and_hold）重构为 `IStrategy` 子类

#### 5.1.2 策略注册表
- 新增：`packages/akshare-mcp/src/akshare_mcp/services/backtest/strategy_registry.py`
  ```python
  class StrategyRegistry:
      _strategies: dict[str, type[IStrategy]] = {}

      @classmethod
      def register(cls, strategy_class: type[IStrategy]):
          cls._strategies[strategy_class.name()] = strategy_class

      @classmethod
      def create(cls, name: str, params: dict) -> IStrategy:
          return cls._strategies[name](**params)

      @classmethod
      def list_all(cls) -> list[str]:
          return list(cls._strategies.keys())
  ```

#### 5.1.3 生命周期状态机
```
draft → submitted → incubating → listed → deprecated
  ↑                     ↓            ↑↓
  └─── rejected ←───────┘    suspended
```
- 状态转换规则：
  - `submitted → incubating`：通过自动化质检（OOS验证 + 稳健性检验）
  - `incubating → listed`：孵化期（30天模拟盲测）Sharpe > 0.5 且 MDD < 20%
  - `listed → deprecated`：连续30天Sharpe < 0 或 MDD > 30%
  - `listed → suspended`：检测到风格漂移（IC衰减超过50%）
  - `suspended → listed`：暂停后连续14天IC恢复至暂停前水平的70%以上，自动恢复上架

#### 5.1.4 自动化质检管线
- 复用现有 `validate_factor_oos`（Walk-Forward + Purged K-Fold + Bootstrap）
- 质检流程：
  1. Walk-Forward验证：滑动窗口OOS Sharpe > 0.3
  2. Purged K-Fold CV：5折交叉验证IC均值 > 0.02
  3. Bootstrap IC CI：95%置信区间下界 > 0
  4. 参数敏感性：核心参数±20%变动后Sharpe变化 < 30%
  5. 综合评级：A/B通过，C/D拒绝

### 5.2 样本外前向信号跟踪

#### 5.2.1 信号记录表
```sql
CREATE TABLE strategy_signals (
    id              SERIAL PRIMARY KEY,
    strategy_id     INTEGER REFERENCES strategies(id),
    date            DATE NOT NULL,
    code            VARCHAR(20) NOT NULL,
    signal          SMALLINT NOT NULL,  -- 1=买入, -1=卖出, 0=持有
    score           DECIMAL(8,4),       -- 信号强度
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(strategy_id, date, code)
);
SELECT create_hypertable('strategy_signals', 'date');

-- 前向收益验证表
CREATE TABLE signal_forward_returns (
    signal_id       INTEGER REFERENCES strategy_signals(id),
    forward_days    INTEGER NOT NULL,   -- 1/5/10/20
    actual_return   DECIMAL(10,6),
    calculated_at   TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (signal_id, forward_days)
);
```

#### 5.2.2 定时信号记录任务
- 每个交易日收盘后：
  1. 对所有 `listed` 状态的策略，运行 `generate_signals()` 获取当日信号
  2. 写入 `strategy_signals` 表
  3. 对N天前的信号，计算实际前向收益写入 `signal_forward_returns`
  4. 策略超市展示的所有收益数据100%来自前向记录，不使用回测数据

#### 5.2.3 前向验证仪表盘
- 在策略详情页新增"实盘跟踪"Tab
- 展示：前向信号命中率、前向IC、前向Sharpe
- 与回测指标对比展示，让用户直观看到回测与实盘的差距

### 5.3 社交跟单系统（CopyTrader）— ⏸️ 暂缓

> 本节功能暂缓实现，标记为后续迭代。原因：社交跟单本质上是独立产品级工程，涉及资金安全和容错要求极高，需在前四期基础设施（持久化、撮合引擎、NAV引擎、风控网关）完全稳定后再启动。

待后续迭代时实现的核心模块：
- 策略师认证与等级体系（Cadet/Champion/Elite）
- 跟单关系管理与比例分配
- 跟单止损（CSL）自动断开机制
- 跟单执行引擎（依赖三期的撮合引擎）

### 5.4 游戏化机制 — ⏸️ 暂缓

> 本节功能暂缓实现，标记为后续迭代。原因：游戏化设计需在用户基数和使用数据积累到一定规模后，才能有效验证激励效果；且金融场景的游戏化存在合规风险（SEC/ESMA对"数字互动实践"的监管趋严），需法务评估后再推进。

待后续迭代时实现的核心模块：
- 成就徽章与里程碑系统
- 积分与等级体系
- 合规边界设计（避免诱导过度交易）

### 5.5 IP 保护机制

#### 5.5.1 信号混淆
- 对公开展示的策略历史信号添加随机时间延迟（1-3个交易日）
- 对非订阅用户展示的净值曲线添加微小噪声（±0.1%）
- 真实信号仅对付费订阅者实时推送

#### 5.5.2 MCP架构天然隔离
- 策略代码运行在Python MCP服务端，用户通过BFF API只能获取输出信号
- 策略源码不通过任何API暴露
- 回测参数配置以加密JSONB存储，前端仅展示结果

#### 5.5.3 API访问控制
- 策略详情API添加访问频率限制（防爬虫）
- 信号历史API仅对订阅者开放
- 所有API请求记录审计日志，异常访问模式自动告警

---

## 附录：技术依赖与风险评估

### 新增技术依赖

| 依赖 | 用途 | 引入期数 |
|---|---|---|
| `sklearn.covariance.LedoitWolf` | 协方差收缩估计 | 一期 |
| `APScheduler` | Python定时任务调度 | 二期 |
| `socket.io` / `@nestjs/websockets` | WebSocket实时推送 | 三期 |
| `socket.io-client` | 前端WebSocket客户端 | 三期 |
| Redis 分布式限流（`INCR` + `EXPIRE`） | 交易频率断路器，替代内存态限流 | 四期 |

### 各期风险评估

| 期数 | 主要风险 | 缓解措施 |
|---|---|---|
| 一期 | JIT策略修改可能影响现有回测结果 | 修改前保存24组基准回测结果（4策略×3标的×2时段），修改后对比验证；指标契约文档通过评审 |
| 二期 | 因子定时计算对数据库性能的影响；指标定义不稳影响商品化可信度 | 使用TimescaleDB分区和物化视图优化；商品化上线前指标契约必须通过回归测试 |
| 三期 | 模拟交易持久化迁移的数据一致性；自动处置误触发风险 | 双写模式过渡，灰度切换；自动处置设置冷却期（同一账户60秒内不重复触发） |
| 四期 | LLM情绪评估的准确性和一致性 | 建立情绪评估测试集，定期回归测试 |
| 五期 | 策略生命周期状态机的运维复杂度 | 完善监控告警，异常状态自动通知 |

### 数据库新增表汇总

| 表名 | 引入期数 | 说明 |
|---|---|---|
| `strategies` | 二期 | 策略商品主表 |
| `strategy_metrics` | 二期 | 策略绩效指标（hypertable） |
| `strategy_reviews` | 二期 | 用户评价 |
| `strategy_subscriptions` | 二期 | 订阅关系 |
| `factor_values` | 二期 | 因子值存储（hypertable） |
| `factor_ic_history` | 二期 | 因子IC历史（hypertable） |
| `order_events` | 三期 | 订单事件审计日志 |
| `alert_events` | 三期 | 预警触发记录 |
| `recommendation_audit_log` | 四期 | AI推荐审计日志 |
| `user_profile_snapshots` | 四期 | AI推断的用户画像快照（指数衰减加权） |
| `strategy_signals` | 五期 | 前向信号记录（hypertable） |
| `signal_forward_returns` | 五期 | 前向收益验证 |
| `strategy_authors` | 暂缓 | 策略师认证（社交跟单，待后续迭代） |
| `copy_relationships` | 暂缓 | 跟单关系（社交跟单，待后续迭代） |
| `user_achievements` | 暂缓 | 成就徽章（游戏化，待后续迭代） |
| `user_points` | 暂缓 | 积分记录（游戏化，待后续迭代） |
