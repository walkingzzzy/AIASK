# 项目功能欠缺与量化改进方案（聚焦因子与量化）

- 文档日期：2026-02-13
- 评估方式：本地代码与运行时工具核验 + 深度联网对标
- 适用范围：`c:\Users\1\Desktop\股票` 根目录项目

## 1. 门禁与基线

已执行并通过：

1. `python scripts/skill_coverage_audit.py --check-thresholds`
2. `python scripts/skill_coverage_audit.py --output-json skill_tool_coverage_runtime.json --output-gap skill_tool_gap_list.txt`

结果：`tools=153, skills=20, covered=153, coverage=100%, missing=0, tdx=36/36, manager=31/31`。

说明：覆盖率达标不等于量化研究链路“可验证、可审计、可实盘迁移”。

## 2. 已实现能力（客观）

1. 提供了 manager + 原子工具双层结构，具备基础研究、回测、风险、绩效、执行等接口。
2. 量化回测引擎可运行基础策略（MA/Momentum/RSI/Buy&Hold），支持手续费与滑点参数入口。
3. 因子计算底层模块较丰富（技术、基本面、波动、量价等 mixin）。

## 3. 核心欠缺与不足（按严重度）

## 3.1 S0（高风险，优先修复）

1. `quant_manager` 的 `factor_ic`/`backtest_factor` 存在模板值返回，不是实时计算结果。
2. 这会导致“研究结论看似完整但证据不可复核”，属于量化研究闭环中的关键可信度缺口。

证据：

- `packages/akshare-mcp/src/akshare_mcp/tools/managers/quant_manager.py:315`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/quant_manager.py:330`

运行时复核（2026-02-13 10:27）：

- `quant_manager(action="factor_ic")` 返回固定 `ic=0.15, ic_ir=1.2, win_rate=0.58`
- `quant_manager(action="backtest_factor")` 返回固定 `total_return=0.25, sharpe_ratio=1.5`

## 3.2 S1（重要能力缺口）

1. 对外因子库暴露过窄，当前仅 4 类（momentum/value/quality/volatility）。
2. `calculate_factor` 对部分因子直接降级为 `0.0`，如 `quality`。
3. 底层已实现大量细分因子，但未形成可调用的统一接口层，导致“实现能力 > 对外可用能力”。

证据：

- `packages/akshare-mcp/src/akshare_mcp/tools/quant.py:12`
- `packages/akshare-mcp/src/akshare_mcp/tools/quant.py:36`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_calculator/__init__.py:17`

运行时复核（2026-02-13 10:27）：

- `get_factor_library(category="all")` 仅 4 项
- `calculate_factor(code="600519", factor="quality")` 返回 `0.0`

## 3.3 S1（组合与风险控制）

1. `max_sharpe` 默认约束不足，易出现单票极端集中（近 100%）。
2. 这与机构实践中的集中度/行业/风格约束不一致，实盘迁移风险较高。

证据：

- `packages/akshare-mcp/src/akshare_mcp/services/portfolio_optimization.py:356`
- `packages/akshare-mcp/src/akshare_mcp/services/portfolio_optimization.py:357`
- `packages/akshare-mcp/src/akshare_mcp/tools/portfolio.py:141`

运行时复核（2026-02-13 10:27）：

- `optimize_portfolio(method="max_sharpe")` 出现单只股票权重约 `1.0`

## 3.4 S1（绩效归因严谨性）

1. 部分指标存在近似估算：波动率、回撤、信息比率、归因拆分等。
2. 当前归因逻辑偏启发式，不满足机构级归因与披露一致性要求。

证据：

- `packages/akshare-mcp/src/akshare_mcp/tools/managers/performance_manager.py:147`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/performance_manager.py:150`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/performance_manager.py:231`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/performance_manager.py:289`

## 3.5 S2（流程与可用性）

1. `risk_manager`/`performance_manager` 已转为 `portfolio_id` 模式，和旧式 `codes/weights` 使用习惯存在断层。
2. 回测与研究链路虽可运行，但 OOS/滚动验证、中性化、容量与冲击建模仍偏基础。

证据：

- `packages/akshare-mcp/src/akshare_mcp/tools/managers/risk_manager.py:71`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/performance_manager.py:69`
- `packages/akshare-mcp/src/akshare_mcp/services/backtest/engine.py:41`
- `packages/akshare-mcp/src/akshare_mcp/services/backtest/utils.py:49`

## 4. 对标外部标准的差距（因子/量化重点）

1. 因子构造与口径：应具备明确分组规则、再平衡口径、可重复计算标准（参考 Ken French 因子体系）。
2. 研究到实盘链路：应形成 Universe -> Alpha -> Portfolio -> Execution -> Risk 的清晰职责边界（参考 QuantConnect Framework）。
3. 现实交易建模：手续费、滑点、成交可得性与流动性冲击需独立建模并可归因（参考 QuantConnect/Backtrader）。
4. 验证方法：样本外验证与滚动/走步优化应成为标准流程（Walk-forward）。
5. 披露规范：绩效展示需方法一致、可审计、可追溯（参考 GIPS、SEC Marketing Rule）。

## 5. 优先级改进路线图

## 5.1 P0（立即）

1. 取消 `quant_manager` 模板结果，强制接入真实计算链路。
2. 响应中强制包含：样本数、显著性、时间窗、数据源、降级链。
3. 禁止未知因子静默返回 `0.0`，改为明确错误或可解释降级。

## 5.2 P1（短期）

1. 打通底层已实现因子到对外 API 的映射与分类。
2. 完整化因子研究流程：去极值、标准化、行业/市值中性化、换手后收益、成本后收益。
3. 优化器加入默认硬约束：`max_weight`、最小持仓数、行业/风格暴露上限、换手上限。
4. 绩效模块升级为真实时序口径（收益、波动、回撤、TE、IR、归因）。

## 5.3 P2（中期）

1. 补齐 OOS 与滚动验证模板，形成研究报告标准输出。
2. 完善容量/冲击/执行偏差建模，支持高换手策略评估。
3. 统一 manager 入参契约与迁移文档，降低使用门槛。

## 6. 风险边界与表达

1. 本文为信息与分析辅助，不构成收益承诺。
2. 任何“可执行”建议须附合规检查、风险评估、执行与复盘证据链。
3. TDX 不可用时应走降级链，不能等同于“无机会”。

## 7. 参考来源（深度联网，访问日期：2026-02-13）

1. QuantConnect Algorithm Framework  
   https://www.quantconnect.com/docs/v2/writing-algorithms/algorithm-framework/overview
2. QuantConnect Reality Modeling: Key Concepts  
   https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/key-concepts
3. QuantConnect Reality Modeling: Transaction Fees  
   https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/transaction-fees/key-concepts
4. QuantConnect Reality Modeling: Slippage  
   https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/slippage/key-concepts
5. QuantConnect Walk Forward Optimization  
   https://www.quantconnect.com/docs/v2/writing-algorithms/optimization/walk-forward-optimization
6. Backtrader Commission Schemes  
   https://www.backtrader.com/docu/commission-schemes/commission-schemes/
7. Backtrader Slippage  
   https://www.backtrader.com/docu/slippage/slippage/
8. Ken French Data Library (Fama/French Factors)  
   https://mba.tuck.dartmouth.edu/pages/faculty/ken.french/Data_Library/f-f_factors.html
9. GIPS Standards Handbook for Firms  
   https://www.gipsstandards.org/standards/gips-standards-for-firms/gips-standards-handbook-for-firms/
10. SEC IA-5653 (Marketing Rule Final)  
    https://www.sec.gov/files/rules/final/2020/ia-5653.pdf
11. SEC Marketing Rule FAQ  
    https://www.sec.gov/investment/marketing-rule-faq?mod=article_inline
12. ESMA UCITS Performance Fees  
    https://www.esma.europa.eu/publications-and-data/interactive-single-rulebook/ucits/article-40-performance-fees-ucits-and-certain-types-aifs

