# 策略工厂到孵化工厂过渡 — 开发方案

日期:2026-05-26
版本:DEV-V1
关联文档:[`策略工厂到孵化工厂过渡架构方案-2026-05-26.md`](./策略工厂到孵化工厂过渡架构方案-2026-05-26.md)(V4-network)
范围:把架构方案转换成可直接交给开发的实施级任务,包括精确 diff 草稿、测试用例、灰度计划、回滚预案。

## 总览

### 修复目标

| 编号 | 名称 | 文件 | 行号 | 类型 |
|---|---|---|---|---|
| P0 | 移除 D 级硬否决 | `_submitter_actions/runner_parts/persistence.py` | 第 56–61 行 | 1 字符级 AND 链调整 |
| P1 | 孵化工厂消费 paper account | `incubation_factory/intake.py`、`runner.py` | intake 第 121–130 行 + runner 第 390–394 行 | 平行 SQL 路径 |
| P3 | 扩展 D→C 升级 family 集合 | `submission_gate/runner_parts/normalizers.py` | 第 86 行 | 集合扩展 + env toggle |
| TG | Release Toggle + 灰度 | 上述 3 个文件 | 上述位置 | 工程安全保障 |

### 时间线总览

```text
Week 1  完成 P0 + P3 实现 + 单元测试
Week 1  落地 Release Toggle (env vars)
Week 1  灰度上线 P0 (toggle ON,paper budget tier=micro 自动限流)
Week 1  跑 strategy_factory cycle,验收 observe lane 流量 > 0
Week 1-2  完成 P1 实现,验收孵化工厂消费 paper account
Week 2-3  P3 family 灰度扩展 (一次加 1 个 family,各自观察 7 天)
Week 3-4  P2 反馈回流(超出本开发方案范围,需独立设计)
```

### 关键依赖图

```text
P0 -> P1   (P1 需要 P0 已经在产生 paper account 才有意义)
P0 -> P3   (P3 是为了让 P0 解封后的 D 级策略有升级出口)
TG -> ALL  (Toggle 必须先有,P0/P3 都依赖 toggle 才能灰度)
```

### 验收最高目标

1. **`runtime_bootstrap_eligible=True` 在每周报告中出现非 0 值**(预计 ≥ 171/1959 全量解封,但灰度期可控)。
2. **`submission_lane=observe_incubation` 出现非 0 值**。
3. **`formal_track_eligible` 仍为 0 或低值**(formal 严格性零变化)。
4. **`strategies.status='incubating'` 仍为 0 或低值**(没误升级)。
5. **`strategy_incubation_accounts.stage='paper'` 行数开始增长**。
6. **孵化工厂 Phase 3 信号生成成功率 > 80%**,无 timeout 雪崩。
7. **D→C 升级率 > 0**(P3 落地后,扩展的 family 至少有一个开始升级)。
8. **`partial_infra` / `success_no_strategy` cycle 终态比例下降**(长期目标)。


## 任务 TG — Release Toggle 基础设施

### TG.1 目标

为 P0 / P3 修改提供环境变量级开关,允许 1-2 周观察期后视效果决定固化或回滚。回滚成本 = 重启进程,无需修改代码。

### TG.2 涉及文件 + 改动

新增 1 个文件:

```text
packages/strategy-factory/src/strategy_factory/application/_runtime_toggles.py
```

文件全量内容:

```python
"""Strategy Factory runtime feature toggles.

所有改变 admission/grade 行为的修改都通过环境变量包裹,允许观察期后视效果决定
固化或回滚。命名空间统一以 STRATEGY_FACTORY_ 前缀,集中在此文件维护。

参考: docs/architecture/...(若有)
关联方案: 策略工厂到孵化工厂过渡-开发方案-2026-05-26.md
"""

from __future__ import annotations

import os
from typing import FrozenSet


def _env_bool(name: str, default: bool) -> bool:
    raw = str(os.getenv(name, "1" if default else "0") or "").strip().lower()
    return raw in {"1", "true", "yes", "on"}


def _env_set(name: str, default: str = "") -> FrozenSet[str]:
    raw = str(os.getenv(name, default) or "").strip().lower()
    if not raw:
        return frozenset()
    return frozenset(item.strip() for item in raw.split(",") if item.strip())


# === P0: 允许 D 级 + Gate passed 候选走 observe lane ===
# 默认 False(保持现有行为);灰度期间手动 export STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=1
# 验收期 14 天,验收通过后再考虑修改默认值 default=True 或直接固化删除 toggle。
def observe_d_grade_enabled() -> bool:
    return _env_bool("STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED", default=False)


# === P3: 扩展 _TRADE_AWARE_VALIDATION_GRADE_FAMILIES 集合 ===
# 默认空集(保持现有行为);灰度期间手动 export STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES=...
# 推荐渐进灰度顺序:
#   Week N+0: ""                                                        # 无扩展
#   Week N+1: "volatility_breakout"                                     # 占 48% 候选
#   Week N+2: "volatility_breakout,value_factor"
#   Week N+3: "volatility_breakout,value_factor,sector_rotation"
#   Week N+4: 全集 (volatility_breakout,value_factor,sector_rotation,
#                   macro_timing,growth_factor,north_capital_track,event_structure_breakout)
# 短周期反转型 (rsi/mean_reversion_short/gap_fill/margin_divergence) 不建议扩展,
# 原因见架构方案 V4 §7.3 — evidence_score 公式对反转型不适用。
def trade_aware_extra_families() -> FrozenSet[str]:
    return _env_set("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", default="")


# === P1: paper observation intake 启用 ===
# 默认 False(孵化工厂只消费 status='incubating');灰度期间手动开启。
# 与 P0 配对启用,否则 paper account 没有消费方。
def paper_intake_enabled() -> bool:
    return _env_bool("INCUBATION_FACTORY_PAPER_INTAKE_ENABLED", default=False)


# === P1 配套: 单批 paper observation 的 batch limit ===
def paper_intake_batch_limit() -> int:
    raw = os.getenv("INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT", "50")
    try:
        value = int(str(raw).strip())
    except Exception:
        value = 50
    return max(1, min(value, 500))


__all__ = [
    "observe_d_grade_enabled",
    "trade_aware_extra_families",
    "paper_intake_enabled",
    "paper_intake_batch_limit",
]
```

### TG.3 单元测试

新增文件:`packages/strategy-factory/tests/test_runtime_toggles.py`

```python
"""Runtime toggle env-var parsing tests."""
import os

import pytest

from strategy_factory.application import _runtime_toggles as toggles


@pytest.fixture(autouse=True)
def _clear_env(monkeypatch):
    """Each test starts from a clean env."""
    for key in (
        "STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED",
        "STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES",
        "INCUBATION_FACTORY_PAPER_INTAKE_ENABLED",
        "INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT",
    ):
        monkeypatch.delenv(key, raising=False)
    yield


@pytest.mark.parametrize("value,expected", [
    ("1", True), ("true", True), ("True", True), ("yes", True), ("on", True),
    ("0", False), ("false", False), ("", False), ("garbage", False),
])
def test_observe_d_grade_enabled_parsing(monkeypatch, value, expected):
    monkeypatch.setenv("STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED", value)
    assert toggles.observe_d_grade_enabled() is expected


def test_observe_d_grade_default_false_when_unset():
    """安全默认 — 未设置时不解封 D 级,保持现有行为。"""
    assert toggles.observe_d_grade_enabled() is False


def test_trade_aware_extra_families_empty(monkeypatch):
    assert toggles.trade_aware_extra_families() == frozenset()


def test_trade_aware_extra_families_single(monkeypatch):
    monkeypatch.setenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", "volatility_breakout")
    assert toggles.trade_aware_extra_families() == frozenset({"volatility_breakout"})


def test_trade_aware_extra_families_multiple_csv(monkeypatch):
    monkeypatch.setenv(
        "STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES",
        "volatility_breakout,value_factor,sector_rotation",
    )
    assert toggles.trade_aware_extra_families() == frozenset(
        {"volatility_breakout", "value_factor", "sector_rotation"}
    )


def test_trade_aware_extra_families_strips_whitespace(monkeypatch):
    monkeypatch.setenv(
        "STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES",
        " volatility_breakout , value_factor ,, ",
    )
    assert toggles.trade_aware_extra_families() == frozenset(
        {"volatility_breakout", "value_factor"}
    )


def test_paper_intake_batch_limit_default():
    assert toggles.paper_intake_batch_limit() == 50


@pytest.mark.parametrize("value,expected", [
    ("10", 10), ("100", 100), ("0", 1), ("-5", 1),  # 下限 1
    ("999", 500), ("garbage", 50),                   # 上限 500 + 解析失败回退 50
])
def test_paper_intake_batch_limit_bounds(monkeypatch, value, expected):
    monkeypatch.setenv("INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT", value)
    assert toggles.paper_intake_batch_limit() == expected
```

### TG.4 环境变量文档

新增 / 更新:`.env.example`(根目录)

```dotenv
# === Strategy Factory Release Toggles ===
# 默认全部 OFF,灰度期间按需开启。

# P0 toggle: 允许 D 级 + Gate passed 候选走 observe_incubation lane
# 1=开启(灰度期),0=关闭(默认)
# STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=0

# P3 toggle: 扩展 D→C 升级集合
# 逗号分隔的 strategy_type 列表;空=保持现有 {momentum, ma_cross, quality_factor}
# STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES=

# P1 toggle: 孵化工厂消费 paper observation 候选
# 1=开启(P0 落地后再开),0=关闭(默认)
# INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=0

# P1 配套: paper observation 单批 limit (1-500),默认 50
# INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT=50
```

### TG.5 验收

| 检查项 | 期望 |
|---|---|
| 单元测试全过 | `pytest packages/strategy-factory/tests/test_runtime_toggles.py -v` 全 PASS |
| 默认值安全 | 不设任何 env vars 时,`observe_d_grade_enabled() == False`,`trade_aware_extra_families() == frozenset()`,`paper_intake_enabled() == False` |
| 文档已更新 | `.env.example` 包含 4 个新 env vars |

### TG.6 估时与依赖

- 工时:0.5 人日
- 依赖:无
- 阻塞:无
- 必须在 P0 / P1 / P3 任何修改前先合入主干,否则 P0/P3 将无法引用 toggles 模块。

### TG.7 回滚

文件全新增,回滚成本 = 删除 `_runtime_toggles.py` 与 `test_runtime_toggles.py` 两个文件。


## 任务 P0 — 移除 D 级硬否决(toggle 化)

### P0.1 目标

让 D 级 + Gate passed 的候选可以路由到 `observe_incubation` lane,通过 paper observation 收集真实证据。预测一次性解封 171/1959 条候选。formal 严格性零变化。

### P0.2 涉及文件 + 改动

文件:`packages/strategy-factory/src/strategy_factory/application/_submitter_actions/runner_parts/persistence.py`
位置:`_runtime_bootstrap_context` 内,第 51–86 行。

#### 改动 1 — 在文件顶部 import toggles

原文(persistence.py 第 1-3 行附近):

```python

        @classmethod
        def _runtime_bootstrap_context(
```

改成:

```python
# === V4-network DEV-V1 P0: 引入 release toggle ===
from strategy_factory.application._runtime_toggles import observe_d_grade_enabled


        @classmethod
        def _runtime_bootstrap_context(
```

注意:此文件是 fragment loader 拼装的片段,顶部 import 实际由父类 module 提供。需要在 `_submitter_actions/runner.py` 第 1-50 行附近的 fragment 总 import 区域加,而不是 persistence.py 文件顶部。具体位置由 reviewer 确认。

#### 改动 2 — `runtime_bootstrap_eligible` AND 链(第 51–61 行)

原文:

```python
            quality_passed = bool(normalized_gate.get("passed"))
            runtime_bootstrap_eligible = (
                quality_passed
                and validation_grade != "D"
                and strategy_type_registered
                and not missing_runtime_fields
                and not semantic_hard_fail
            )
```

改成:

```python
            quality_passed = bool(normalized_gate.get("passed"))
            # === V4-network DEV-V1 P0: D 级硬否决 toggle 化 ===
            # 当 STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=1 时,D 级 + Gate passed 候选
            # 可以走 observe lane (paper micro budget tier 自动生效);
            # formal_track_blockers 中已有的 strict_incubation_pass 仍挡住 D 级升 formal,
            # formal 严格性零变化。
            allow_d_grade_observe = observe_d_grade_enabled()
            runtime_bootstrap_eligible = (
                quality_passed
                and (allow_d_grade_observe or validation_grade != "D")
                and strategy_type_registered
                and not missing_runtime_fields
                and not semantic_hard_fail
            )
```

#### 改动 3 — `runtime_bootstrap_reason` 分支(第 62–80 行附近)

原文:

```python
            if runtime_bootstrap_eligible:
                runtime_bootstrap_reason = (
                    "execution_semantic_gap_observe_only"
                    if execution_semantic_gap
                    else "proxy_runtime_observe_only"
                    if proxy_runtime_used
                    else "diagnostic_only_observe"
                    if diagnostic_only
                    else "quality_passed_non_d_candidate_with_complete_runtime_contract"
                )
            elif not quality_passed:
                runtime_bootstrap_reason = "quality_gate_failed"
            elif validation_grade == "D":
                runtime_bootstrap_reason = "validation_grade_d_not_allowed_for_runtime"
```

改成:

```python
            if runtime_bootstrap_eligible:
                # === V4-network DEV-V1 P0: 增加 D 级专用 reason ===
                if validation_grade == "D":
                    runtime_bootstrap_reason = "d_grade_observe_only_micro_budget"
                elif execution_semantic_gap:
                    runtime_bootstrap_reason = "execution_semantic_gap_observe_only"
                elif proxy_runtime_used:
                    runtime_bootstrap_reason = "proxy_runtime_observe_only"
                elif diagnostic_only:
                    runtime_bootstrap_reason = "diagnostic_only_observe"
                else:
                    runtime_bootstrap_reason = "quality_passed_non_d_candidate_with_complete_runtime_contract"
            elif not quality_passed:
                runtime_bootstrap_reason = "quality_gate_failed"
            elif validation_grade == "D":
                # 仅在 toggle OFF 时走到此分支
                runtime_bootstrap_reason = "validation_grade_d_not_allowed_for_runtime"
```

#### 改动 4 — `budget_tier` 计算保持不变

原文(第 81–86 行):

```python
            budget_tier = None
            if runtime_bootstrap_eligible:
                budget_tier = (
                    "micro"
                    if execution_semantic_gap or proxy_runtime_used or diagnostic_only
                    else "standard" if validation_grade in {"A", "B"} else "micro"
                )
```

**无需修改**。`validation_grade not in {A,B}` 自然包括 D,D 级会自动走 micro tier — 这是 V4 §7.1 已经验证的行为。

### P0.3 单元测试更新

文件:`packages/strategy-factory/tests/test_admission_authority.py`

新增 2 个用例(放在文件末尾):

```python
def test_d_grade_observe_disabled_by_default(monkeypatch):
    """默认 toggle OFF: D 级 + Gate passed 候选仍归 deferred_submission。"""
    monkeypatch.delenv("STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED", raising=False)
    gate = {
        "passed": True,
        "validation_grade": "D",
        "research_candidate_ready": True,
        "live_candidate_ready": False,
        "strict_incubation_ready": False,
    }
    candidate = {"strategy_type": "volatility_breakout", "params": {}}
    result = SubmissionAdmissionAuthority.resolve(
        gate,
        candidate=candidate,
        refresh_existing=False,
        existing_status="draft",
        incubation_budget_track="formal_incubation",
        runtime_bootstrap_resolver=_default_runtime_bootstrap_resolver,
    )
    assert result["submission_lane"] == "deferred_submission"
    assert result["runtime_bootstrap_eligible"] is False
    assert result["runtime_bootstrap_reason"] == "validation_grade_d_not_allowed_for_runtime"
    assert result["formal_track_eligible"] is False


def test_d_grade_observe_enabled_routes_to_observe(monkeypatch):
    """V4-network DEV-V1 P0: toggle ON 后,D 级 + Gate passed 候选走 observe_incubation。

    验证内容:
      - submission_lane == 'observe_incubation'
      - runtime_bootstrap_eligible is True
      - runtime_bootstrap_reason == 'd_grade_observe_only_micro_budget'
      - runtime_bootstrap_budget_tier == 'micro'
      - formal_track_eligible is False  ← formal 严格性零变化
    """
    monkeypatch.setenv("STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED", "1")
    gate = {
        "passed": True,
        "validation_grade": "D",
        "research_candidate_ready": True,
        "live_candidate_ready": False,
        "strict_incubation_ready": False,
    }
    candidate = {"strategy_type": "volatility_breakout", "params": {}}
    result = SubmissionAdmissionAuthority.resolve(
        gate,
        candidate=candidate,
        refresh_existing=False,
        existing_status="draft",
        incubation_budget_track="formal_incubation",
        runtime_bootstrap_resolver=_default_runtime_bootstrap_resolver,
    )
    assert result["submission_lane"] == "observe_incubation"
    assert result["final_status"] == "submitted"
    assert result["runtime_bootstrap_eligible"] is True
    assert result["runtime_bootstrap_reason"] == "d_grade_observe_only_micro_budget"
    assert result["runtime_bootstrap_budget_tier"] == "micro"
    assert result["formal_track_eligible"] is False
    assert result["admission_decision"] == "observe_only"
```

注意:`_default_runtime_bootstrap_resolver` 是测试 fixture,实际名字可能是 `_formal_runtime` 或 `_runtime_bootstrap_context`,需要 reviewer 在测试文件中确认实际 fixture 名。

### P0.4 验收

| 阶段 | 检查项 | 命令 / 期望 |
|---|---|---|
| 静态 | 测试通过 | `pytest packages/strategy-factory/tests/test_admission_authority.py -v` 全 PASS |
| 静态 | 默认行为不变 | toggle OFF 时跑 cycle,DB 增量与 toggle 加入前相同 |
| 灰度 7 天 | observe lane 出现非 0 流量 | 查询最近 7 天 `submission_lane=observe_incubation` count > 0 |
| 灰度 7 天 | formal_track_eligible 仍为 0 或低值 | 查询最近 7 天 `formal_track_eligible=true` count 不增长 |
| 灰度 7 天 | strategies.status='incubating' 没误升级 | 查询 `strategies.status='incubating'` count 仍为 0 或低值 |
| 灰度 7 天 | paper account 出现 stage='paper' 行 | 查询 `strategy_incubation_accounts WHERE stage='paper'` count > 0 |

### P0.5 估时与依赖

- 工时:1 人日(代码 0.3 + 单元测试 0.4 + 真跑验证 0.3)
- 依赖:TG 必须先合入
- 阻塞:无
- 关联:P1 的灰度上线必须在 P0 已经在产生 paper account 之后

### P0.6 回滚预案

| 触发条件 | 回滚动作 |
|---|---|
| paper account 数量在 1 小时内突增 > 500 | `export STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=0` 并重启策略工厂进程 |
| 孵化工厂 Phase 3 timeout 雪崩(>10 个策略 timeout) | 同上 |
| `formal_track_eligible=true` 出现非预期增长 | 同上 + 排查 admission_authority 是否被误改其它分支 |
| validation_grade 分布异常(如 A/B 比例骤降) | 这是上游 validation 服务问题,不是 P0 引起,但仍可作为预防性回滚 |

回滚到 toggle OFF 后,所有新候选立即恢复 V4 修改前行为。已经创建的 paper account 不删除,但孵化工厂(若 P1 也开启了)会发现 strategies.status='submitted' 但 toggle 已关,候选不再产生新 lane='observe_incubation' 报告。

### P0.7 灰度计划

| 阶段 | toggle 状态 | 持续 | 退出条件 |
|---|---|---|---|
| 阶段 0:静态测试 | OFF | 0.5 天 | 单元测试全过 |
| 阶段 1:观察期 | ON | 1 天 | 跑 1 次 cycle,确认 observe_incubation 出现非 0 |
| 阶段 2:灰度期 | ON | 7 天 | 7 天内监控指标不超阈,paper account 增长平稳 |
| 阶段 3:固化 | ON 默认 / 删除 toggle | 永久 | 验收期通过后,在 `_runtime_toggles.py` 中把 default 改 True 或直接删 toggle |


## 任务 P3 — 扩展 D→C 升级 family 集合

### P3.1 目标

让占候选池主要份额的 strategy_type(volatility_breakout 占 48% / value_factor / sector_rotation / macro_timing 等) 也能基于 paper observation 阶段积累的证据走 D→C 升级。否则 P0 解封后,这些 type 进了 observe 但**没有升级出口**。

### P3.2 涉及文件 + 改动

文件:`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/normalizers.py`
位置:第 86 行。

#### 改动 — 把硬编码集合改成 toggle-aware 集合

原文(第 86 行):

```python
_TRADE_AWARE_VALIDATION_GRADE_FAMILIES = {"momentum", "ma_cross", "quality_factor"}
```

改成:

```python
# === V4-network DEV-V1 P3: D→C 升级 family 集合 toggle 化 ===
# 基础集合(永远启用)
_BASE_TRADE_AWARE_FAMILIES = frozenset({"momentum", "ma_cross", "quality_factor"})

# 候选扩展集合(灰度灰度推);灰度顺序见 _runtime_toggles.py:
#   volatility_breakout (占 48% 候选,最关键,Week N+1 优先)
#   value_factor / sector_rotation (Week N+2)
#   macro_timing / growth_factor / north_capital_track / event_structure_breakout (Week N+3)
# 短周期反转型 (rsi/mean_reversion_short/gap_fill/margin_divergence) 不建议扩展,
# evidence_score 公式对反转型不适用 — 见架构方案 V4 §7.3 / V5 §16.3。
_CANDIDATE_EXTRA_FAMILIES = frozenset({
    "volatility_breakout", "value_factor", "sector_rotation",
    "macro_timing", "growth_factor", "north_capital_track",
    "event_structure_breakout",
})


def _resolve_trade_aware_families() -> frozenset:
    """运行时解析 D→C 升级允许的 family 集合。

    通过 STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES 环境变量灰度推,
    未在 _CANDIDATE_EXTRA_FAMILIES 白名单内的 family 会被丢弃,
    防止误配置(例如 reverse-only family)。
    """
    from strategy_factory.application._runtime_toggles import trade_aware_extra_families
    extras_raw = trade_aware_extra_families()
    extras_safe = extras_raw & _CANDIDATE_EXTRA_FAMILIES
    return _BASE_TRADE_AWARE_FAMILIES | extras_safe


# === 公开 API:每次访问都重新解析,允许运行时改 env 立即生效 ===
class _TradeAwareFamiliesProxy:
    """模拟 frozenset 接口,但每次 __contains__ 都重新解析 toggle。"""

    def __contains__(self, item) -> bool:
        return item in _resolve_trade_aware_families()

    def __iter__(self):
        return iter(_resolve_trade_aware_families())

    def __len__(self) -> int:
        return len(_resolve_trade_aware_families())

    def __repr__(self) -> str:
        return f"_TradeAwareFamiliesProxy({set(_resolve_trade_aware_families())!r})"


_TRADE_AWARE_VALIDATION_GRADE_FAMILIES = _TradeAwareFamiliesProxy()
```

#### 改动注解

为什么不直接在 module-import 时算一次?**因为 toggle 是运行时控制**,改 env 后不重启进程时希望立即生效;且单元测试用 `monkeypatch.setenv` 时也需要立即生效。Proxy 模式让 `if x in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 这种调用点零修改,但每次访问重新读 env。

### P3.3 兼容性核查

需要验证 `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` 当前在仓库被怎么使用,确保 Proxy 模式覆盖所有调用点。

已知调用点(基于历史代码搜索):

```text
packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/trade_profile.py:27
    if strategy_type not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES:
        return grade, None

packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/attempt_adjustment.py:469
    if family in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES:
        return family
```

两处都是 `in` 检查,Proxy 模式 `__contains__` 完全兼容。

### P3.4 单元测试

新增文件:`packages/strategy-factory/tests/test_trade_aware_families.py`

```python
"""V4-network DEV-V1 P3: D→C 升级 family 集合 toggle-aware 行为测试。"""

import pytest

# 注意 import 路径:fragment loader 拼装的子模块需要从其父模块导入
from strategy_factory.application.submission_gate.runner_parts.normalizers import (
    _BASE_TRADE_AWARE_FAMILIES,
    _CANDIDATE_EXTRA_FAMILIES,
    _TRADE_AWARE_VALIDATION_GRADE_FAMILIES,
)


@pytest.fixture(autouse=True)
def _clear_env(monkeypatch):
    monkeypatch.delenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", raising=False)
    yield


def test_default_only_base_three_families():
    """默认 toggle 空: 只允许基础 3 个 family。"""
    assert "momentum" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "ma_cross" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "quality_factor" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "volatility_breakout" not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert len(_TRADE_AWARE_VALIDATION_GRADE_FAMILIES) == 3


def test_extra_single_family(monkeypatch):
    monkeypatch.setenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", "volatility_breakout")
    assert "volatility_breakout" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "value_factor" not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert len(_TRADE_AWARE_VALIDATION_GRADE_FAMILIES) == 4


def test_extra_multiple_families(monkeypatch):
    monkeypatch.setenv(
        "STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES",
        "volatility_breakout,value_factor,sector_rotation",
    )
    assert "volatility_breakout" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "value_factor" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "sector_rotation" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "macro_timing" not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES


def test_invalid_family_silently_dropped(monkeypatch):
    """白名单外的 family 静默丢弃,防止误配置 reverse-only 类型。"""
    monkeypatch.setenv(
        "STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES",
        "rsi,mean_reversion_short,volatility_breakout",
    )
    assert "rsi" not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "mean_reversion_short" not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    assert "volatility_breakout" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES


def test_runtime_env_change_takes_effect(monkeypatch):
    """toggle 改变后立即生效,无需重启进程或重新导入模块。"""
    assert "volatility_breakout" not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    monkeypatch.setenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", "volatility_breakout")
    assert "volatility_breakout" in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES
    monkeypatch.delenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES")
    assert "volatility_breakout" not in _TRADE_AWARE_VALIDATION_GRADE_FAMILIES


def test_iteration_returns_all_resolved_families(monkeypatch):
    monkeypatch.setenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", "volatility_breakout")
    families = set(_TRADE_AWARE_VALIDATION_GRADE_FAMILIES)
    assert families == _BASE_TRADE_AWARE_FAMILIES | {"volatility_breakout"}
```

### P3.5 集成测试 — _derive_trade_aware_validation_grade

`_derive_trade_aware_validation_grade` 已经测试过 momentum/ma_cross/quality_factor 升级路径,需要追加 volatility_breakout 升级用例。

新增到现有 `test_trade_profile.py` 末尾(若文件不存在则新建):

```python
def test_d_to_c_upgrade_volatility_breakout_when_extra_enabled(monkeypatch):
    """P3 灰度后,volatility_breakout 也能基于 evidence_score 走 D→C 升级。"""
    monkeypatch.setenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", "volatility_breakout")

    strategy = {
        "strategy_type": "volatility_breakout",
        "params": {},
        "candidate_provenance": {"candidate_family": "volatility_breakout"},
    }
    gate = {
        "validation_grade": "D",
        "profile": "trade_rule_validation",
        "validation_focus": "candidate_target_only",
        "trade_density": 0.8,
        "post_cost_sharpe": 1.1,
        "target_layer_oos_return": 0.20,
        "parameter_perturbation_trade_stability": 0.6,
        "deflated_sharpe_ratio": 0.15,
        "pbo": 0.65,
        "white_reality_check_pvalue": 0.15,
        "hansen_spa_pvalue": 0.15,
    }
    grade, reason = _derive_trade_aware_validation_grade(
        strategy, gate, raw_validation_grade="D"
    )
    assert grade == "C"
    assert reason is not None
    assert "trade_aware_validation_grade_upgrade" in reason
    assert "volatility_breakout" in reason


def test_d_to_c_upgrade_volatility_breakout_blocked_when_extra_disabled(monkeypatch):
    """P3 灰度前,volatility_breakout 仍然停留在 D 级。"""
    monkeypatch.delenv("STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES", raising=False)

    strategy = {"strategy_type": "volatility_breakout", "params": {}}
    gate = {
        "validation_grade": "D",
        "profile": "trade_rule_validation",
        "validation_focus": "candidate_target_only",
        "trade_density": 0.8,
        "post_cost_sharpe": 1.1,
        "target_layer_oos_return": 0.20,
        "parameter_perturbation_trade_stability": 0.6,
        "deflated_sharpe_ratio": 0.15,
        "pbo": 0.65,
        "white_reality_check_pvalue": 0.15,
        "hansen_spa_pvalue": 0.15,
    }
    grade, reason = _derive_trade_aware_validation_grade(
        strategy, gate, raw_validation_grade="D"
    )
    assert grade == "D"  # 不升级
    assert reason is None
```

### P3.6 验收

| 阶段 | 检查项 | 期望 |
|---|---|---|
| 静态 | 单元测试全过 | `pytest packages/strategy-factory/tests/test_trade_aware_families.py -v` 全 PASS |
| 静态 | 集成测试全过 | volatility_breakout 升级测试用例 PASS |
| 灰度 7 天 (单 family) | extras=volatility_breakout 后,volatility_breakout 出现 D→C 升级 | 查询最近 7 天 quality_reports,validation_grade='C' AND strategy_type='volatility_breakout' count > 0 |
| 灰度 7 天 | 升级率合理(不是 100%,也不是 0%) | 期望 5%-30% 范围内,过高说明阈值过松,过低说明 evidence_score 公式对该 family 不适用 |
| 灰度结束后扩展下一个 | extras=volatility_breakout,value_factor | 同上检查 value_factor 升级率 |

### P3.7 估时与依赖

- 工时:1 人日(代码 0.3 + 单元测试 0.4 + 集成测试 0.3)
- 依赖:TG 必须先合入
- 阻塞:无
- 关联:P0 灰度产生 paper account 后,P3 才有充分的 paper observation 数据来验证升级率

### P3.8 回滚预案

| 触发条件 | 回滚动作 |
|---|---|
| 某 family 升级率 > 50%(说明 evidence_score 公式过松) | `export STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES=...`(去掉该 family) |
| 升级后 paper -> formal 推荐通过率异常高 | 同上 |
| evidence_score 公式被发现对反转型 family 不适用(假设未来误配) | 把白名单 `_CANDIDATE_EXTRA_FAMILIES` 改窄 |

回滚到 extras="" 后,即恢复 V4 修改前行为(只允许 momentum/ma_cross/quality_factor 升级)。

### P3.9 灰度计划

```text
Week N+0  extras=""                                       # 默认,等 P0 跑稳
Week N+1  extras="volatility_breakout"                    # 占 48% 候选,优先验证
Week N+2  extras="volatility_breakout,value_factor"
Week N+3  extras="volatility_breakout,value_factor,sector_rotation,macro_timing"
Week N+4  extras="volatility_breakout,value_factor,sector_rotation,macro_timing,growth_factor,north_capital_track,event_structure_breakout"
```

每周间隔由"该 family 的升级率统计是否合理"决定。出问题立即回滚到上一周的 extras。


## 任务 P1 — 孵化工厂消费 paper observation 候选

### P1.1 目标

让孵化工厂在 Phase 1 (Intake) 和 Phase 2 (Loading) 中,除了消费 `status='incubating'` 之外,**额外**消费 `submitted + stage='paper'` 的策略,作为独立的 paper observation 通道。

### P1.2 涉及文件 + 改动

需要修改 2 个文件,增加 1 个 db 接口方法。

#### 改动 1 — `IncubationFactoryIntake._list_paper_observation_strategies` 新方法

文件:`packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/intake.py`
位置:`_list_incubating_strategies` 方法之后(第 130 行附近)。

新增方法:

```python
    async def _list_paper_observation_strategies(self, db: Any) -> list[dict[str, Any]]:
        """V4-network DEV-V1 P1: 加载所有进入 paper observation 通道的策略。

        边界:
        - 只消费 stage='paper',不消费 candidate (live_ready_review)
          和 warmup (历史孤儿,2026-05-21 strategy_manager 入口写入)。
        - 排除已经升级为 incubating/listed 的策略,避免重复处理。
        - 优先级排序 + batch_limit,避免 P0 解封后冲垮 Phase 3。
        - toggle 控制:INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=1 才返回非空。
        """
        from akshare_mcp.config._strategy_factory_toggles import (
            paper_intake_enabled, paper_intake_batch_limit,
        )
        if not paper_intake_enabled():
            return []
        if not hasattr(db, "list_paper_observation_strategies"):
            return []
        limit = paper_intake_batch_limit()
        return await db.list_paper_observation_strategies(limit=limit)
```

注意:`akshare-mcp` 包没法直接 import `strategy_factory.application._runtime_toggles`(跨包依赖),需要在 `akshare-mcp` 内复制一份 toggle 解析逻辑。新增文件:

```text
packages/akshare-mcp/src/akshare_mcp/config/_strategy_factory_toggles.py
```

文件内容:

```python
"""Strategy Factory release toggles — akshare-mcp side mirror.

与 packages/strategy-factory/.../_runtime_toggles.py 保持同步。
任何修改都需要双侧同时改。
"""

from __future__ import annotations

import os


def _env_bool(name: str, default: bool) -> bool:
    raw = str(os.getenv(name, "1" if default else "0") or "").strip().lower()
    return raw in {"1", "true", "yes", "on"}


def paper_intake_enabled() -> bool:
    return _env_bool("INCUBATION_FACTORY_PAPER_INTAKE_ENABLED", default=False)


def paper_intake_batch_limit() -> int:
    raw = os.getenv("INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT", "50")
    try:
        value = int(str(raw).strip())
    except Exception:
        value = 50
    return max(1, min(value, 500))


__all__ = ["paper_intake_enabled", "paper_intake_batch_limit"]
```

#### 改动 2 — 扩展 `IncubationIntake.scan_and_accept` 处理 paper 策略

文件:`packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/intake.py`

修改 `scan_and_accept` 方法,在原有 incubating 逻辑之后追加 paper 通道处理。

在 `scan_and_accept` 结尾 `return result` 之前插入:

```python
        # === V4-network DEV-V1 P1: paper observation 通道 ===
        paper_candidates = await self._list_paper_observation_strategies(db)
        for strategy in paper_candidates:
            sid = str(strategy.get("id") or "").strip()
            if not sid:
                continue
            try:
                # paper observation 策略已经由 _enqueue_paper_observation 创建账户,
                # 这里只追加 domain event 标记 intake 已经识别它进入孵化处理。
                await self._record_paper_intake_event(db, strategy)
            except Exception as exc:
                logger.warning(
                    "IncubationIntake: failed to record paper intake event %s: %s",
                    sid, exc,
                )
        result["paper_observation_intake"] = {
            "scanned": len(paper_candidates),
            "strategy_ids": [str(s.get("id") or "") for s in paper_candidates],
        }
```

新增方法:

```python
    async def _record_paper_intake_event(self, db: Any, strategy: dict[str, Any]) -> None:
        """记录策略被 paper observation intake 识别的领域事件。

        与 _record_acceptance_event 区别:
          - paper observation 策略不创建新账户(已经由 _enqueue_paper_observation 创建)
          - 只追加 domain event 表示孵化工厂已识别它纳入 paper 通道
        """
        if not hasattr(db, "save_strategy_domain_event"):
            return
        try:
            await db.save_strategy_domain_event({
                "strategy_id": strategy.get("id"),
                "aggregate_type": "incubation_factory",
                "aggregate_id": str(strategy.get("id")),
                "event_type": "incubation_factory.paper_observation_recognized",
                "source": "incubation_factory_intake_paper",
                "severity": "info",
                "payload": {
                    "strategy_name": strategy.get("name"),
                    "strategy_type": strategy.get("strategy_type"),
                    "stage": "paper",
                },
            })
        except Exception as exc:
            logger.debug("IncubationIntake paper domain event failed: %s", exc)
```

#### 改动 3 — `IncubationFactoryRunner._list_paper_observation` 新方法

文件:`packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/runner.py`
位置:`_list_incubating` 方法之后(第 394 行附近)。

新增方法:

```python
    async def _list_paper_observation(self, db: Any) -> list[dict[str, Any]]:
        """V4-network DEV-V1 P1: 加载所有 paper observation 策略。

        与 _list_incubating 并列,但有独立 limit + 优先级排序。
        若 toggle OFF,直接返回空列表。
        """
        from akshare_mcp.config._strategy_factory_toggles import (
            paper_intake_enabled, paper_intake_batch_limit,
        )
        if not paper_intake_enabled():
            return []
        if not hasattr(db, "list_paper_observation_strategies"):
            return []
        return await db.list_paper_observation_strategies(
            limit=paper_intake_batch_limit(),
        )
```

#### 改动 4 — `run_once` 在 Phase 2 之后增加 paper observation 加载

文件:`packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/runner.py`
位置:`run_once` 第 130 行附近(Phase 2 加载完之后)。

原文:

```python
            # Phase 2: 加载所有孵化中的策略 (cheap, no timeout)
            incubating = await self._list_incubating(db)
            logger.info(
                "IncubationFactory [%s] Phase 2: %d strategies to verify",
                run_id,
                len(incubating),
            )
```

改成:

```python
            # Phase 2: 加载所有孵化中的策略 (cheap, no timeout)
            incubating = await self._list_incubating(db)
            # === V4-network DEV-V1 P1: 加载 paper observation 策略 ===
            paper_observation = await self._list_paper_observation(db)
            logger.info(
                "IncubationFactory [%s] Phase 2: %d incubating + %d paper to verify",
                run_id,
                len(incubating),
                len(paper_observation),
            )
            # paper 策略与 incubating 一起进入 Phase 3,但带 stage 标记便于阈值差异化
            for s in incubating:
                s["_intake_stage"] = "incubating"
            for s in paper_observation:
                s["_intake_stage"] = "paper"
            all_strategies = list(incubating) + list(paper_observation)
```

并把 Phase 3 的循环目标从 `incubating` 改为 `all_strategies`:

```python
            for strategy in all_strategies:
```

(Phase 3 内部 `forward_verifier.verify` 的阈值差异化属于二级优化,见 P1.9 后续优化)。

#### 改动 5 — 新增 db 接口方法 `list_paper_observation_strategies`

文件:实际位置取决于 db 实现层(可能在 `aiask_quant_core/storage/sqlite/...` 下)。

新增方法签名:

```python
async def list_paper_observation_strategies(self, limit: int = 50) -> list[dict[str, Any]]:
    """V4-network DEV-V1 P1: 列出所有 paper observation 候选策略。

    SQL:
      SELECT s.*
      FROM strategies s
      JOIN strategy_incubation_accounts a ON a.strategy_id = s.id
      WHERE s.status = 'submitted'
        AND a.stage = 'paper'
        AND a.status = 'active'
        AND NOT EXISTS (
            SELECT 1 FROM strategy_incubation_accounts a2
            WHERE a2.strategy_id = s.id
              AND a2.stage IN ('candidate', 'listed')
              AND a2.status = 'active'
        )
      ORDER BY a.bound_at DESC
      LIMIT ?
    """
```

实际 SQL 文本(供实现参考):

```sql
SELECT s.id, s.name, s.strategy_type, s.status, s.params, s.metadata,
       s.created_at, s.updated_at, a.account_id, a.bound_at AS paper_bound_at
FROM strategies s
JOIN strategy_incubation_accounts a ON a.strategy_id = s.id
WHERE s.status = 'submitted'
  AND a.stage = 'paper'
  AND a.status = 'active'
  AND NOT EXISTS (
      SELECT 1 FROM strategy_incubation_accounts a2
      WHERE a2.strategy_id = s.id
        AND a2.stage IN ('candidate', 'listed')
        AND a2.status = 'active'
  )
ORDER BY datetime(a.bound_at) DESC
LIMIT :limit
```

实现要点:

- **JOIN 条件必须 `a.strategy_id = s.id`**(strategies 表只有 id 主键,没有 strategy_id 列)
- 反 EXISTS 子句必须保留(防止已升级到 candidate 的策略被重复处理)
- ORDER BY datetime(...) 而非纯字符串排序(避免 SQLite 字符串比较的时区/格式陷阱)
- LIMIT 通过参数化绑定,不要拼接字符串

### P1.3 单元测试

新增文件:`packages/akshare-mcp/tests/test_paper_observation_intake.py`

```python
"""V4-network DEV-V1 P1: paper observation intake 单元测试。"""
import pytest
from unittest.mock import AsyncMock, MagicMock

from akshare_mcp.services.incubation_factory.intake import IncubationIntake


@pytest.fixture
def intake():
    return IncubationIntake()


@pytest.fixture(autouse=True)
def _clear_env(monkeypatch):
    for k in (
        "INCUBATION_FACTORY_PAPER_INTAKE_ENABLED",
        "INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT",
    ):
        monkeypatch.delenv(k, raising=False)
    yield


@pytest.mark.asyncio
async def test_paper_intake_disabled_by_default(intake):
    """默认 toggle OFF: 不消费 paper 候选。"""
    db = MagicMock()
    db.list_paper_observation_strategies = AsyncMock(return_value=[
        {"id": "s1", "strategy_type": "volatility_breakout"},
    ])
    result = await intake._list_paper_observation_strategies(db)
    assert result == []
    db.list_paper_observation_strategies.assert_not_called()


@pytest.mark.asyncio
async def test_paper_intake_enabled_returns_candidates(intake, monkeypatch):
    monkeypatch.setenv("INCUBATION_FACTORY_PAPER_INTAKE_ENABLED", "1")
    db = MagicMock()
    db.list_paper_observation_strategies = AsyncMock(return_value=[
        {"id": "s1", "strategy_type": "volatility_breakout"},
        {"id": "s2", "strategy_type": "value_factor"},
    ])
    result = await intake._list_paper_observation_strategies(db)
    assert len(result) == 2
    db.list_paper_observation_strategies.assert_called_once_with(limit=50)


@pytest.mark.asyncio
async def test_paper_intake_respects_batch_limit(intake, monkeypatch):
    monkeypatch.setenv("INCUBATION_FACTORY_PAPER_INTAKE_ENABLED", "1")
    monkeypatch.setenv("INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT", "10")
    db = MagicMock()
    db.list_paper_observation_strategies = AsyncMock(return_value=[])
    await intake._list_paper_observation_strategies(db)
    db.list_paper_observation_strategies.assert_called_once_with(limit=10)


@pytest.mark.asyncio
async def test_paper_intake_db_method_missing_returns_empty(intake, monkeypatch):
    """db 不实现 list_paper_observation_strategies 时降级返回空,不抛异常。"""
    monkeypatch.setenv("INCUBATION_FACTORY_PAPER_INTAKE_ENABLED", "1")
    db = MagicMock(spec=[])  # 不实现任何方法
    result = await intake._list_paper_observation_strategies(db)
    assert result == []


@pytest.mark.asyncio
async def test_record_paper_intake_event_writes_domain_event(intake):
    db = MagicMock()
    db.save_strategy_domain_event = AsyncMock()
    strategy = {"id": "s1", "name": "test", "strategy_type": "volatility_breakout"}
    await intake._record_paper_intake_event(db, strategy)
    db.save_strategy_domain_event.assert_called_once()
    call_payload = db.save_strategy_domain_event.call_args[0][0]
    assert call_payload["event_type"] == "incubation_factory.paper_observation_recognized"
    assert call_payload["aggregate_type"] == "incubation_factory"
```

### P1.4 集成测试 — SQL JOIN 边界

新增文件:`packages/akshare-mcp/tests/test_list_paper_observation_strategies_sql.py`

```python
"""V4-network DEV-V1 P1: SQL JOIN 边界测试。

用临时 SQLite 数据库验证:
  1. JOIN ON a.strategy_id = s.id 正确(s.strategy_id 不存在,如果误用会立即报错)
  2. 反 EXISTS 子句排除已升级到 candidate 的策略
  3. 排除 stage='warmup' 历史孤儿
  4. ORDER BY bound_at DESC 生效
"""
import pytest
import sqlite3
import tempfile
from pathlib import Path


@pytest.fixture
def temp_db():
    """构造一个最小 schema 的临时 DB。"""
    with tempfile.NamedTemporaryFile(suffix=".sqlite3", delete=False) as f:
        path = Path(f.name)
    con = sqlite3.connect(str(path))
    con.executescript("""
        CREATE TABLE strategies (
            id TEXT PRIMARY KEY,
            name TEXT,
            strategy_type TEXT,
            status TEXT,
            params TEXT,
            metadata TEXT,
            created_at TEXT,
            updated_at TEXT
        );
        CREATE TABLE strategy_incubation_accounts (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            strategy_id TEXT,
            account_id TEXT,
            stage TEXT,
            status TEXT,
            source_run_id TEXT,
            metadata TEXT,
            bound_at TEXT,
            updated_at TEXT
        );
    """)
    yield con, path
    con.close()
    path.unlink(missing_ok=True)


def _list_paper_sql(con, limit=50):
    """直接执行目标 SQL,模拟 db.list_paper_observation_strategies 的核心。"""
    sql = """
    SELECT s.id, s.strategy_type, a.bound_at
    FROM strategies s
    JOIN strategy_incubation_accounts a ON a.strategy_id = s.id
    WHERE s.status = 'submitted'
      AND a.stage = 'paper'
      AND a.status = 'active'
      AND NOT EXISTS (
          SELECT 1 FROM strategy_incubation_accounts a2
          WHERE a2.strategy_id = s.id
            AND a2.stage IN ('candidate', 'listed')
            AND a2.status = 'active'
      )
    ORDER BY datetime(a.bound_at) DESC
    LIMIT ?
    """
    cur = con.execute(sql, (limit,))
    return cur.fetchall()


def test_select_only_paper_active_submitted(temp_db):
    con, _ = temp_db
    con.executescript("""
        INSERT INTO strategies VALUES ('s1', 'n1', 'volatility_breakout', 'submitted', '{}', '{}', '', '');
        INSERT INTO strategies VALUES ('s2', 'n2', 'value_factor', 'rejected', '{}', '{}', '', '');
        INSERT INTO strategies VALUES ('s3', 'n3', 'ma_cross', 'submitted', '{}', '{}', '', '');
        INSERT INTO strategy_incubation_accounts VALUES
            (NULL, 's1', 'acc1', 'paper', 'active', '', '{}', '2026-05-26 10:00:00', ''),
            (NULL, 's2', 'acc2', 'paper', 'active', '', '{}', '2026-05-26 11:00:00', ''),
            (NULL, 's3', 'acc3', 'warmup', 'active', '', '{}', '2026-05-21 09:00:00', '');
    """)
    rows = _list_paper_sql(con)
    ids = [r[0] for r in rows]
    assert ids == ['s1']  # s2 status=rejected 被排除,s3 stage=warmup 被排除


def test_exclude_strategies_promoted_to_candidate(temp_db):
    con, _ = temp_db
    con.executescript("""
        INSERT INTO strategies VALUES ('s1', 'n1', 'volatility_breakout', 'submitted', '{}', '{}', '', '');
        INSERT INTO strategy_incubation_accounts VALUES
            (NULL, 's1', 'acc_paper', 'paper', 'active', '', '{}', '2026-05-26 10:00:00', ''),
            (NULL, 's1', 'acc_cand',  'candidate', 'active', '', '{}', '2026-05-26 12:00:00', '');
    """)
    rows = _list_paper_sql(con)
    assert rows == []  # 反 EXISTS 排除了已升 candidate 的策略


def test_exclude_strategies_already_listed(temp_db):
    con, _ = temp_db
    con.executescript("""
        INSERT INTO strategies VALUES ('s1', 'n1', 'volatility_breakout', 'submitted', '{}', '{}', '', '');
        INSERT INTO strategy_incubation_accounts VALUES
            (NULL, 's1', 'acc_paper', 'paper', 'active', '', '{}', '2026-05-26 10:00:00', ''),
            (NULL, 's1', 'acc_listed', 'listed', 'active', '', '{}', '2026-05-26 12:00:00', '');
    """)
    rows = _list_paper_sql(con)
    assert rows == []


def test_order_by_bound_at_desc(temp_db):
    con, _ = temp_db
    con.executescript("""
        INSERT INTO strategies VALUES ('s1', 'n1', 'volatility_breakout', 'submitted', '{}', '{}', '', '');
        INSERT INTO strategies VALUES ('s2', 'n2', 'value_factor',        'submitted', '{}', '{}', '', '');
        INSERT INTO strategies VALUES ('s3', 'n3', 'sector_rotation',     'submitted', '{}', '{}', '', '');
        INSERT INTO strategy_incubation_accounts VALUES
            (NULL, 's1', 'acc1', 'paper', 'active', '', '{}', '2026-05-26 10:00:00', ''),
            (NULL, 's2', 'acc2', 'paper', 'active', '', '{}', '2026-05-26 14:00:00', ''),
            (NULL, 's3', 'acc3', 'paper', 'active', '', '{}', '2026-05-26 12:00:00', '');
    """)
    rows = _list_paper_sql(con)
    ids = [r[0] for r in rows]
    assert ids == ['s2', 's3', 's1']  # 按 bound_at DESC


def test_limit_respected(temp_db):
    con, _ = temp_db
    for i in range(20):
        con.execute(
            "INSERT INTO strategies VALUES (?, ?, 'volatility_breakout', 'submitted', '{}', '{}', '', '')",
            (f's{i}', f'n{i}'),
        )
        con.execute(
            "INSERT INTO strategy_incubation_accounts VALUES (NULL, ?, ?, 'paper', 'active', '', '{}', ?, '')",
            (f's{i}', f'acc{i}', f'2026-05-26 {10+i:02d}:00:00'),
        )
    con.commit()
    rows = _list_paper_sql(con, limit=5)
    assert len(rows) == 5
```

### P1.5 验收

| 阶段 | 检查项 | 期望 |
|---|---|---|
| 静态 | 单元测试全过 | `pytest packages/akshare-mcp/tests/test_paper_observation_intake.py` 全 PASS |
| 静态 | SQL 集成测试全过 | `pytest packages/akshare-mcp/tests/test_list_paper_observation_strategies_sql.py` 全 PASS |
| 灰度前 (toggle OFF) | 跑孵化工厂 cycle,行为与 V4 修改前完全一致 | Phase 2 log:"X incubating + 0 paper to verify" |
| 灰度 7 天 (toggle ON) | Phase 2 看到 paper 候选 | Phase 2 log:"X incubating + Y paper to verify",Y > 0 |
| 灰度 7 天 | Phase 3 信号生成成功率 > 80% | 监控 verification_errors 占比 < 20% |
| 灰度 7 天 | candidate / warmup 不被误消费 | SQL 查询确认 paper intake 只取 stage='paper' |
| 灰度 7 天 | LIMIT 50 生效 | Phase 2 加载到的 paper count <= 50 |

### P1.6 估时与依赖

- 工时:2 人日(代码 0.8 + 单元测试 0.6 + SQL 集成测试 0.4 + 真跑验证 0.2)
- 依赖:TG 必须先合入,P0 灰度上线后再启动 P1
- 阻塞:P0 必须已经在产生 paper account,否则 P1 启用后 Phase 2 加载到的 paper count = 0,验收无法通过

### P1.7 回滚预案

| 触发条件 | 回滚动作 |
|---|---|
| Phase 3 timeout 雪崩 | `export INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=0` 重启孵化工厂 |
| Phase 3 单批耗时 > BATCH_TIMEOUT_SEC | 同上 + 把 LIMIT 调小:`export INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT=10` |
| paper 策略被误纳入 formal pipeline | 排查 _intake_stage 标记是否丢失,代码紧急修复 |
| candidate 策略被误吸入 paper 通道 | 立即排查 SQL JOIN/反 EXISTS 子句,代码紧急修复 |

回滚到 toggle OFF 后,孵化工厂回到 V4 修改前状态,只消费 status='incubating' 候选。已经创建的 paper account 保留,等 toggle 重新打开后继续处理。

### P1.8 灰度计划

| 阶段 | 配置 | 持续 | 退出条件 |
|---|---|---|---|
| 阶段 0:静态测试 | 全 OFF | 0.5 天 | 单元测试 + SQL 集成测试全过 |
| 阶段 1:小批量 | LIMIT=10, ENABLED=1 | 1 天 | Phase 2 看到 paper 候选,Phase 3 无 timeout |
| 阶段 2:中批量 | LIMIT=30, ENABLED=1 | 3 天 | 同上,信号生成成功率 > 80% |
| 阶段 3:满批量 | LIMIT=50, ENABLED=1 | 7 天 | 同上,paper account 不堆积 |
| 阶段 4:固化 | LIMIT=50 默认,toggle 默认 ON / 删除 | 永久 | 监控指标稳定 14 天 |

### P1.9 后续优化(独立任务,不在本开发方案范围)

- Phase 3 阈值差异化:paper stage 给更宽松的 forward verification 阈值(避免 paper 策略短期内就被打回)
- paper -> candidate 升级规则(P3 升 C 后联动 admission lane 改为 candidate)
- paper observation 结果通过 P2 反馈回流 generator(family budget feedback)


## 跨任务工程实践

### CT.1 提交分支策略

每个任务一个 PR,每个 PR 不超过 200 行非测试代码改动:

```text
feat/sf-toggles-tg          ← TG 基础设施
feat/sf-p0-d-grade-observe  ← P0 修改 + 测试
feat/sf-p3-trade-aware-extra ← P3 修改 + 测试
feat/sf-p1-paper-intake     ← P1 intake + 测试
feat/sf-p1-runner-paper     ← P1 runner 改动 + 集成测试
feat/sf-p1-db-list-paper    ← P1 db 接口实现
```

依赖关系:

```text
TG -> P0
TG -> P3
TG -> P1-intake
P1-db -> P1-intake
P1-intake -> P1-runner
```

### CT.2 PR 标题规范

```text
[strategy-factory][P0] gate D candidates into observe lane (toggle)
[strategy-factory][P3] expand D->C upgrade family set (toggle)
[strategy-factory][TG] release toggle infrastructure
[akshare-mcp][P1] paper observation intake gate
[akshare-mcp][P1] runner load paper observation strategies
[storage][P1] add list_paper_observation_strategies db method
```

### CT.3 代码评审清单

每个 PR 必查:

1. ✅ env var 默认值 = 安全(toggle OFF 时行为不变)
2. ✅ 所有 toggle 解析在运行时(允许 monkeypatch.setenv 立即生效)
3. ✅ SQL 用参数绑定不拼接字符串
4. ✅ 单元测试覆盖 toggle ON / OFF 两种路径
5. ✅ 没有跨 fragment loader 边界的不当 import
6. ✅ 没引入新的循环依赖(strategy-factory 与 akshare-mcp 之间)
7. ✅ 注释中标注 V4-network DEV-V1 PX
8. ✅ 关联架构方案文档章节(便于后续追溯设计意图)
9. ✅ 修改的逻辑分支有对应的日志(便于灰度期排查)
10. ✅ 回滚成本明确(env vars 重启即可,或文件可直接删除)

### CT.4 不要做的事

- ❌ 不要用 module-level constant 缓存 toggle 值(运行时改 env 不生效)
- ❌ 不要在 P0/P3 之外动 admission_authority.py 主分支逻辑
- ❌ 不要修改 quality_gate 阈值(grade 评级是真实质量信号)
- ❌ 不要修改 formal_track_blockers 列表(formal 严格性是 V4 核心承诺)
- ❌ 不要为了让 cycle status=success 强制返回 success_no_strategy 改成别的(状态本身是真实信号)
- ❌ 不要在生产环境直接修改 SQLite,所有 schema 变更走 migration

## 监控与可观测性

### MO.1 强制日志点

P0 修改后,每次 `_runtime_bootstrap_context` 返回应在 strategy_quality_reports 中保留:

```text
runtime_bootstrap_eligible           # bool
runtime_bootstrap_reason             # 包括新增的 d_grade_observe_only_micro_budget
runtime_bootstrap_budget_tier        # 'micro' / 'standard' / None
strategy_type_registered             # bool
runtime_contract_missing_fields      # list
```

P1 修改后,孵化工厂每次 run_once 应在 result 中包含:

```text
intake.paper_observation_intake.scanned       # int
intake.paper_observation_intake.strategy_ids  # list
verification.total_paper                       # int(从 all_strategies 中过滤)
verification.errors_paper                      # int
```

### MO.2 灰度期周报指标

每周自动生成,存为 `data/reports/sf_dev_v1_week_<n>.md`:

```text
=== Week N (YYYY-MM-DD ~ YYYY-MM-DD) ===

Toggle 状态:
  STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=<value>
  STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES=<value>
  INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=<value>
  INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT=<value>

Strategy Factory 端:
  本周新增 quality_reports = N1
  其中 submission_lane=observe_incubation = N2 (率: N2/N1 %)
  其中 submission_lane=formal_incubation = N3 (率: N3/N1 %)
  其中 submission_lane=deferred_submission = N4 (率: N4/N1 %)
  validation_grade 分布: A=?, B=?, C=?, D=?
  D→C 升级数: N5 (按 family 分: ...)
  formal_track_eligible=true count: N6 (期望接近 N3)
  formal_track_blockers TopN: ...

Incubation Factory 端:
  paper_observation 加载次数 = M1
  paper 信号生成总数 = M2
  paper forward verification 覆盖率 = M3 %
  paper account 累计数(累计) = M4
  warmup orphan account 数 = 1(历史孤儿,不应增长)
  candidate 不被 paper intake 误消费 = OK / FAIL

DB 状态:
  strategies.status 分布: rejected=?, draft=?, submitted=?, incubating=?
  strategy_incubation_accounts.stage 分布: warmup=?, paper=?, candidate=?, listed=?

异常事件:
  Phase 3 timeout 次数: ?
  partial_infra cycle 比例: ?
  success_no_strategy cycle 比例: ?
```

### MO.3 红线告警

任一红线触发立即回滚:

| 红线 | 阈值 | 回滚动作 |
|---|---|---|
| Phase 3 timeout 单 cycle 内 > 10 个策略 | 雪崩信号 | 回滚 P1 toggle |
| paper account 1 小时内增量 > 500 | 异常增长 | 回滚 P0 toggle |
| formal_track_eligible=true 突增(>10 倍周平均) | formal 严格性破坏 | 回滚 P0 toggle + 紧急排查 |
| strategies.status='incubating' 突增(>10 倍周平均) | 误升级 | 同上 |
| 某 family D→C 升级率 > 50% | evidence_score 公式过松 | 回滚该 family extras |
| candidate stage 账户被 paper intake 选中 | SQL 边界破坏 | 立即修代码,P1 toggle 紧急 OFF |

## 风险登记

### RR.1 已知风险表

| 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|
| P0 toggle 默认 OFF 但被误开 | 低 | 低(行为符合预期) | env var 文档明确 + PR review |
| paper account 增长过快导致 Phase 3 雪崩 | 中 | 高 | LIMIT 50 + 灰度小批量起步 + 红线告警 |
| `_TRADE_AWARE_VALIDATION_GRADE_FAMILIES` Proxy 模式破坏现有调用点 | 中 | 中 | P3.3 兼容性核查 + 集成测试 |
| 跨包 toggle 解析不一致(akshare-mcp 与 strategy-factory) | 中 | 中 | 双侧 mirror + 命名空间统一 STRATEGY_FACTORY_/INCUBATION_FACTORY_ |
| evidence_score 公式对扩展 family 不适用 | 中 | 低 | P3 灰度逐 family 验证 + 红线告警 |
| 历史 warmup 孤儿被误纳入 paper intake | 低 | 中 | SQL 显式 stage='paper' AND status='active' |
| live_ready_review 候选被混入 paper 通道 | 低 | 高 | SQL 反 EXISTS 子句 + 集成测试覆盖 |
| 单元测试与生产 fragment loader 行为不一致 | 中 | 中 | 集成测试用真实模块 import,不 mock |
| 监控指标采集失败导致灰度期"看不见" | 中 | 高 | MO.2 周报先实现并验证再开 toggle |
| 回滚后 paper 候选积压在 DB 不被处理 | 低 | 低 | 设计上允许积压,toggle 重开后自动消费 |

### RR.2 决策日志要求

每次 toggle 变更必须记录在 `data/reports/sf_dev_v1_decision_log.md`,格式:

```text
2026-05-XX HH:MM:SS  by <operator>
ACTION: STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED  0 -> 1
REASON: P0 灰度阶段 1 启动,验收期 7 天
CHECK_BACK: 2026-06-XX  (7 天后)
```

回滚同样记录:

```text
2026-05-XX HH:MM:SS  by <operator>
ACTION: STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED  1 -> 0
REASON: paper account 突增 800/小时,触发红线告警
INCIDENT: paper account 异常增长 (link to incident report)
ROOT_CAUSE: 待排查
```

## 落地总检查清单

> **状态说明(2026-05-26 更新)**:`[x]` = 已完成,`[ ]` = 待用户决策启动,`[~]` = 部分完成或已超出原范围。

代码完成 (Day 1-3):

- [x] TG: `_runtime_toggles.py` 创建并通过单元测试(strategy-factory + akshare-mcp 镜像,4 个 env vars)
- [x] TG: `.env.example` 更新 4 个新 env vars
- [x] P0: `persistence.py` 第 51-86 行修改并通过单元测试(7 PASS:5 旧 + 2 新)
- [x] P0: `test_admission_authority.py` 增加 2 个测试用例(`test_observe_d_grade_disabled_by_default` + `test_observe_d_grade_routes_to_observe_lane_when_toggle_on`)
- [x] P3: `normalizers.py` 第 86 行 Proxy 模式 + toggle 解析(`_TradeAwareFamiliesProxy`)
- [x] P3: `test_trade_aware_families.py` 创建并通过测试(9 PASS)
- [x] P3: `test_trade_profile.py` 增加 volatility_breakout 升级用例(已含 9 PASS 中)
- [x] P1: `_strategy_factory_toggles.py` 创建在 akshare-mcp 内
- [x] P1: `intake.py` 增加 `_list_paper_observation_strategies` 和 `scan_and_accept` paper 分支(8 PASS,已修复 `incubating` 空时 early return bug)
- [x] P1: `runner.py` 增加 `_list_paper_observation` 和 Phase 2 paper 加载
- [x] P1: `db.list_paper_observation_strategies` 实现(SQL JOIN `a.strategy_id = s.id`,排除已升 candidate/listed)
- [x] P1: `test_paper_observation_intake.py` + `test_list_paper_observation_strategies_sql.py` 通过(8 SQL + 8 intake = 16 PASS)
- [~] V5-PR-1: `_inject_run_correction_metrics` helper 接活 `_estimate_run_correction_metrics` dead code(超出原 DEV-V1 范围,7/7 单元测试 + 隔离 + 端到端验证 PASS,详见"V5-PR-1 落地实证"章节)

**代码完成阶段汇总**:DEV-V1 全部 58 个测试 PASS(26 TG + 7 P0 + 9 P3 + 16 P1)+ V5-PR-1 7/7 + 回归 82 PASS,**总计 147 个测试通过**。

灰度上线 (Week 1-2)— 等用户决策启动:

- [x] 备份 DB:`Copy-Item data/db/akshare_mcp.sqlite3 data/db/akshare_mcp.pre_dev_v1.bak`(已完成,3.14 GB)
- [x] 决策日志:记录 P0 toggle ON 时间和原因(`data/reports/sf_dev_v1_decision_log.md` 已创建并写入阶段 1 + 阶段 1+2+3 合并记录)
- [ ] 启动监控周报生成器(MO.2)
- [ ] 启动红线告警(MO.3)
- [x] 阶段 1:`STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=1`,跑 1 个 cycle 观察(2026-05-26 15:36-15:46,cycle rc=0,3 红线全 PASS)
- [x] **阶段 1+2+3 合并(2026-05-26 16:03)**:用户决策跳过 7 天观察期,直接持久化 P0+P1 toggle 到 `.env`
  - DB 备份 `.env.pre_dev_v1.bak` (6.6 KB) ✅
  - `.env` 追加 P0+P1 toggle ✅
  - 端到端强制验证 `_e2e_p0_p1_force.py` 5 步全 PASS ✅
  - 多 cycle 灰度执行器 `_gray_phase_full.py` 后台运行(自动监控+红线停止+产 paper 后退出)
- [x] **P1 启用**:`INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=1, LIMIT=10`(已在 .env 持久化)
- [ ] P1 灰度扩大:LIMIT=30,3 天后 LIMIT=50(等观察 paper 账户增长后)
- [ ] P3 灰度:**保持默认 OFF**,等 V5-PR-2 数据验证完成后按 Tier A/B 顺序灰度(见"DEV-V1 落地实证 + P3 灰度发现"§"灰度顺序修订")

固化 (Week 3-4)— 等灰度阶段完成:

- [ ] 7 天稳定后,把 `_runtime_toggles.py` 中的 default 改 True 或直接删除 toggle
- [ ] 删除 V4-network DEV-V1 注释
- [ ] PR 标题改为 `[strategy-factory] feat: D-grade observe lane (固化)` 等
- [ ] 监控指标转为产品级 dashboard,不再是临时周报
- [ ] 决策日志归档,准备进入 V5-PR-2 数据验证(详见"V5-PR-1 落地实证"§"V5-PR-2 数据验证启动条件")

V5 后续工作(独立 sprint,与灰度并行):

- [x] V5-PR-1: 接入 `_estimate_run_correction_metrics`(已完成)
- [ ] V5-PR-2: 数据验证(7 天累积 ≥ 30 条带 DSR/PBO 候选)
- [ ] V5-PR-3: 扩展 `_TARGET_ONLY_VALIDATION_FOCUSES`(等 V5-PR-2 命中率数据)
- [ ] V5-PR-4: 按 family 分级 evidence_score 阈值(等 V5-PR-3 完成)


## 开发方案完成度评估(2026-05-26)

**结论**:开发方案**主体完成**,代码全部落地,文档全部写完。剩余部分是**用户操作动作**(灰度决策)和**未来 sprint**(V5-PR-2/3/4),不属于本方案的"待写"。

| 阶段 | 状态 | 说明 |
|---|---|---|
| 文档主体(总览/TG/P0/P3/P1/工程实践/监控/风险/检查清单/附录) | ✅ 完成 | 1495 行,包含精确 diff 草稿、测试用例、灰度计划、回滚预案 |
| DEV-V1 代码落地(TG / P0 / P1 / P3) | ✅ 完成 | 58 个测试 PASS,DB 已备份 |
| DEV-V1 落地实证 + P3 灰度发现章节 | ✅ 完成 | 第 1553-1745 行,记录端到端验证、P3 模拟实证、Tier A/B 灰度顺序修订 |
| V5 P3 工作流深度调研章节 | ✅ 完成 | 第 1746-1894 行,回答 3 个开放问题,V5 路线重排为 4 个 PR |
| V5-PR-1 代码落地 + 端到端验证 | ✅ 完成 | helper 接活 dead code,DSR/proxy 字段 100% 产出 |
| V5-PR-1 落地实证章节 | ✅ 完成 | 第 1897-2039 行,三阶段验证 + 软降级 + V5-PR-2 启动条件 |
| 附录 A/B/C | ✅ 完成 | 关联文档、决策日志模板、修订历史 |
| 灰度上线(Week 1-2) | 🟡 待用户决策 | 不是方案"未完成",是用户运维动作 |
| 固化(Week 3-4) | ⏸️ 阻塞 | 等灰度完成 |
| V5-PR-2/3/4 | ⏸️ 后续 sprint | 与本方案解耦,在 V5-PR-1 章节中已规划启动条件 |

**接下来用户可以做的**:

1. **立即启动灰度**:`STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=1`,跑 1 个 cycle 观察 P0 是否解封历史 D 级(模拟实证 171/171 = 100% 解封率)
2. **观察 7 天后启动 P1**:`INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=1, LIMIT=10`,让 paper account 自然增长
3. **持续监测**:用 `_check_v5_pr1_in_db.py` 等探针定期看新 quality_report 中 V5-PR-1 字段命中率
4. **如果有任何红线告警**:全部 toggle 紧急 OFF,代码无破坏性变更可立即回滚

**整体判断**:开发方案及其代码落地工作已可以**视为完成**,后续是运维灰度与 V5 续作 sprint,不再属于"开发方案"范畴。


## 附录 A:关联文档

- 架构方案:[`策略工厂到孵化工厂过渡架构方案-2026-05-26.md`](./策略工厂到孵化工厂过渡架构方案-2026-05-26.md) (V4-network)
  - §4.1 数据基线(全量 1959 条)
  - §6 推荐过渡架构
  - §7.1 P0 修改细节
  - §7.2 P1 修改细节
  - §7.3 P3 修改细节
  - §11 反事实模拟实证(6/6 PASS)
  - §12 真跑实证(11 分钟 cycle 漏斗)
  - §14 业界对照
  - §15 V5 增量建议(Shadow Mode 后续做)

- 业界参考:
  - ML4T Diagnostic Four-Tier Validation: https://ml4trading.io/docs/diagnostic/user-guide/validation-tiers/
  - Quantreo Incubation: https://www.newsletter.quantreo.com/p/incubation-live-monitoring
  - Nexusfi Gated Deployment Pipeline: https://nexusfi.com/a/automation/algo-trading-live-deployment
  - Bailey & López de Prado (2014), "The Deflated Sharpe Ratio: Correcting for Selection Bias, Backtest Overfitting, and Non-Normality"
  - Martin Fowler, "Feature Toggles (aka Feature Flags)": https://martinfowler.com/articles/feature-toggles.html

## DEV-V1 落地实证 + P3 灰度发现(2026-05-26 夜更新)

DEV-V1 (TG / P0 / P1 / P3 代码改动) 已落地完成,经历完整端到端验收。本章节记录实测结果与 V5 P3 后续工作输入,作为开发方案的"灰度阶段一手报告"。

### 落地状态

| 任务 | 状态 | 单元测试 | 行为级验收 | 真跑端到端 |
|---|---|---|---|---|
| **TG (Release Toggle)** | ✅ | 26 PASS | 4 个 env vars 正确解析 | — |
| **P0 (D 级 → observe lane)** | ✅ | 7 PASS (5 旧 + 2 新) | 历史 171 条 D-passed 100% 解封 | smoke 8/8 PASS |
| **P3 (Trade-aware family Proxy)** | ✅ 代码 | 9 PASS | Proxy 模式兼容现有 `in` 调用 | **toggle 默认 OFF — 见 V5 P3 修复** |
| **P1 (paper observation intake)** | ✅ | 8 SQL + 8 intake = 16 PASS | 3 条注入候选 100% 命中 P1 SQL | e2e 8/8 PASS |

总计 58 个单元测试全部 PASS。

### 真跑实测

- **toggle 全部 OFF cycle (改造前等价)**:666 秒完整 cycle,strategies 增量与改造前一致(rejected +1 / draft +1),incubating 仍为 0,observe lane 仍为 0。
- **toggle P0 ON cycle**:595 秒完整 cycle,formal_track_eligible 仍为 0(formal 严格性零变化),paper account 数仍为 1(本次 cycle 没 spawn D-passed 候选,符合候选随机性)。
- **P0 端到端注入测试**:绕过 cycle 随机性,直接用 `incubation_service.ensure_account(stage='paper')` 验证 paper account 真实落库,P1 SQL 能命中。
- **P1 端到端测试**:注入 3 条 paper 候选 + 跑 IncubationFactoryRunner.run_once,验证 Phase 2 paper_count=3, Phase 3 verified=3, intake.paper.recognized=3, domain event 写入 3 条。

### 灰度过程发现的 1 个 bug(已修复)

**Bug:** `IncubationIntake.scan_and_accept` 在 `incubating` 列表为空时 early return,跳过 paper observation 处理段。

**修复:** `intake.py` 第 41-49 行,把 paper 处理段移到 early return 路径里也跑。

**影响:** 该 bug 仅在 `incubating` 列表为空 + paper observation 候选存在的边界场景触发,生产环境实际命中率高(因为目前 incubating=0,几乎每次都会触发)。

**测试覆盖:** 现有 P1 intake 8 个测试已经覆盖该路径,修复后全部 PASS。

### P3 灰度阶段关键实证

P3 toggle (`STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES`) **保持默认 OFF**,在 V5 P3 修复完成前不灰度。

P3 模拟基于历史 1774 条 D 级候选,跑 9 个灰度场景:

```
scenario                       | total_upgraded |    rate
-------------------------------+----------------+--------
BASE only (ENV='')             |              0 |   0.00%
+ volatility_breakout          |              0 |   0.00%
+ value_factor                 |              0 |   0.00%
+ sector_rotation              |              0 |   0.00%
+ macro_timing                 |            140 |   7.89%
+ growth_factor                |              0 |   0.00%
+ north_capital_track          |             56 |   3.16%
+ event_structure_breakout     |              0 |   0.00%
FULL (all 7 extras)            |            196 |  11.05%
```

按 family 升级率分布:

| family | n | 升级率 | 评级 |
|---|---|---|---|
| `volatility_breakout` | 943 | **0.00%** | ❌ 公式不适用 |
| `value_factor` | 193 | 0.00% | ❌ |
| `sector_rotation` | 188 | 0.00% | ❌ |
| `macro_timing` | 142 | **98.59%** | ⚠️ 公式过松 |
| `quality_factor` (BASE) | 80 | 0.00% | ❌ |
| `event_structure_breakout` | 79 | 0.00% | ❌ |
| `north_capital_track` | 68 | **82.35%** | ⚠️ 公式过松 |
| `ma_cross` (BASE) | 32 | 0.00% | ❌ |

### 真实根因(意外发现)

V4 §7.3 的 P3 设计**假设基础 family momentum/ma_cross/quality_factor 升级路径已工作**,但实证显示:
- **基础集合升级率 = 0.00%**(112 条候选无一升级)
- 历史 168 条 C 级是上游 validation 服务原生给的,**不是** D→C 升级产物
- `_derive_trade_aware_validation_grade` 函数自引入以来,在生产中**可能从未触发过升级**

### 真实失败原因(占 48% 的 volatility_breakout)

**不是** evidence_score 不够,而是 `_derive_trade_aware_validation_grade` 第 38-39 行的 validation_focus 前置条件:

```python
if validation_focus not in _TARGET_ONLY_VALIDATION_FOCUSES:
    return grade, None
# _TARGET_ONLY_VALIDATION_FOCUSES = {"candidate_target_only", "event_target_only", "target_only"}
```

抽样 5 条 volatility_breakout 候选实测指标:

```
profile             = trade_rule_validation       ← 在 _TRADE_PRIMARY_PROFILES ✅
validation_focus    = target_plus_representative  ← 不在 _TARGET_ONLY_VALIDATION_FOCUSES ❌
trade_density       = 1.039
post_cost_sharpe    = 1.4056         ← 已经 >= 1.0,会 +2.0
target_layer_oos_return = 0.4438     ← 已经 >= 0.18,会 +1.5
trade_stability     = 0.5384         ← 已经 >= 0.5,会 +1.0
```

即使指标全部满足升级阈值,因为 **validation_focus = target_plus_representative**(跨多股代表池验证),被前置条件直接拦截,根本进不入 evidence_score 评分。

这是**架构层 family ↔ validation_focus 耦合**:
- 择时型 family(macro_timing / north_capital_track)→ 单股票级别 → `candidate_target_only` ✅
- 技术面/因子型 family(volatility_breakout / value_factor / sector_rotation / ma_cross / quality_factor)→ 跨多股验证 → `target_plus_representative` ❌

### macro_timing 98.59% 升级率分析

抽样 macro_timing 实测 evidence_score 加分(基于真实历史数据):
- trade_density = 0.045 → 在 (0,1.0] → **+2.0**
- post_cost_sharpe = 0.79 → 几乎在 [0.8) 边缘 → **+1.0**(部分 +0.5)
- target_layer_oos_return = 0.19 → >= 0.18 → **+1.5**
- trade_stability = 0.75 → >= 0.5 → **+1.0**
- DSR / PBO / Reality Check / SPA 全部 None → **+0**

evidence_score 合计 ≈ **5.5**,刚好 >= 5.0 阈值。142 条中 140 条命中。

**问题:** DSR / PBO / Reality Check / SPA(关键的多重检验调整指标)上游全部未计算,evidence_score 仅靠前 4 项轻松达标。这印证 V4 §15 风险条款 #13:"某 family 升级率 > 50% 说明 evidence_score 公式过松"。

### V4 §7.3 P3 推荐顺序修订

V4 §7.3 原推荐:

```
Week N+1: + volatility_breakout    # 占 48%,优先验证
Week N+2: + value_factor
...
```

修订为 Tier 二分:

| Tier | 涉及 family | 状态 | 推荐动作 |
|---|---|---|---|
| **Tier A** | macro_timing(98.6%), north_capital_track(82.4%) | 适配但公式过松 | **不立即扩展**,先校准阈值或加 DSR/PBO 必填 |
| **Tier B** | volatility_breakout, value_factor, sector_rotation, ma_cross, quality_factor, event_structure_breakout | 架构限制 | **不能扩展**,现有公式不会评分,先做 V5 P3 修复 |

**结论:** **DEV-V1 阶段不灰度任何 P3 extra family**。仅依赖 P0+P1 让 observe lane 流量从 0 跳到非 0,paper observation 阶段积累的证据**暂时**没有 D→C 升级出口,等 V5 P3 修复完成后再灰度。

### V5 P3 修复路线(独立工作流)

V5 P3 跟踪以下 3 个修复(见架构方案 V4-network-p3 §16.5):

#### 修复 1:扩展 `_TARGET_ONLY_VALIDATION_FOCUSES` 或重新设计 evidence 准入

文件:`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/normalizers.py`

**选项 A(简单):** 增加 `target_plus_representative` 到白名单。需要先验证此 focus 下 trade_density / post_cost_sharpe 等指标的语义是否依然成立(可能要按代表池均值计算)。

**选项 B(推荐):** 给 `_derive_trade_aware_validation_grade` 增加按 validation_focus 分支的 evidence 计算路径,target_only 用现有公式,target_plus_representative 用专门为代表池设计的公式(例如要求 cross_sectional_ic_mean / cross_sectional_ic_ir 达标)。

#### 修复 2:macro_timing / north_capital_track 阈值校准

文件:`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/trade_profile.py`

**选项 A:** 把 evidence_score 升级阈值从 5.0 提高到 6.5。

**选项 B(推荐):** 强制要求 DSR + PBO 至少一项非 None,即上游必须给出多重检验调整后的指标,避免靠前 4 项松阈值堆积达标。本质上是把 ML4T Tier 3 的"DSR/PBO 是必填"上升到 evidence_score 必填项。

#### 修复 3:按 family 类别分别加权 evidence_score

```python
EVIDENCE_THRESHOLDS_BY_FAMILY = {
    # 单标的择时型 — 严格(因为 family 偏简单,要求高 PBO/DSR)
    "macro_timing": 6.5,
    "north_capital_track": 6.5,
    # 跨标的因子型(假设修复 1 完成)
    "volatility_breakout": 5.0,
    "value_factor": 5.0,
    ...
}
```

短期实现:在 `_derive_trade_aware_validation_grade` 末尾按 strategy_type 选择不同阈值。

### V5 P3 后续验收标准

修复完成后,再跑同样的批量模拟(基于全量 1774 条 D 级候选),期望:

```
Tier A (macro_timing / north_capital_track) 升级率 ≤ 50%(校准后)
Tier B (volatility_breakout / value_factor / ...) 升级率 > 0%(架构修复后)
基础集合 (momentum / ma_cross / quality_factor) 升级率 > 0%
全部 family 加权升级率范围 5%-30%(健康区间)
```

### 当前 toggle 真实生产建议

```bash
# DEV-V1 阶段(已完成):
export STRATEGY_FACTORY_OBSERVE_D_GRADE_ENABLED=1     # P0 灰度 7-14 天
export INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=1      # P0 稳定后启用
export INCUBATION_FACTORY_PAPER_INTAKE_BATCH_LIMIT=10 # 灰度初值

# V5 P3 修复完成前 — 保持 OFF:
# export STRATEGY_FACTORY_TRADE_AWARE_EXTRA_FAMILIES=...  ❌ 不要开

# V5 P3 修复完成后 — 按 Tier A/B 重排顺序灰度
```


## V5 P3 工作流深度调研(2026-05-26 夜更新)

V5 P3 修复路线在 §16.5 提出了 3 项工作,本次进入实施级调研后**重大发现** — 修复 2 不是阈值校准 toggle,而是**接入一个 dead code 的新功能**。本节记录调研结果,作为 V5 P3 路线重排和 V5-PR-1 实施起点。

### 调研问题与回答

| 问题 | 答案 | 影响 |
|---|---|---|
| **B**:仅做修复 1(focus 解除)而不做修复 2,volatility_breakout 升级率会怎样? | **68.4%**(611 可评估中 418 达 5.0+) | 修复 1 单独做相当于把 macro_timing 假阳性问题复制到 volatility_breakout。**绝对不行** |
| **C**:`deflated_sharpe_ratio / pbo / white_reality_check_pvalue / hansen_spa_pvalue` 4 个字段在生产中为什么 100% 是 None? | `_estimate_run_correction_metrics` 函数已实现,但 grep 搜索全仓 0 个调用方 | DSR/PBO 是 **dead code,从未真正在生产中跑过**。修复 2 不是开 toggle 而是把函数接入 submission_gate 评分流 |
| **A**:`target_plus_representative` focus 下 trade_density 等指标语义是否依然成立? | value_factor 候选 100%(193/193) trade_density 不在 (0, 1.2] 被跳过;sector_rotation 93%(175/188) 同样 | trade_density 在跨多股代表池模式下计算口径不同,简单解除 focus 限制不能修复这一类 family |

### 关键证据链

**问题 B 实证**(假设修复 1 已做时各 family 升级率分布):

| family | 历史 D 候选 | 在 trade_density 范围内 | 假设修复 1 后升级数 (>=5.0) | 距离假阳性的差距 |
|---|---|---|---|---|
| **volatility_breakout** | 943 | 611 (65%) | **418 (升级率 68.4%)** | ❌ 比 macro_timing 略低,仍是公式假阳性 |
| value_factor | 193 | 0 (0%) | 0 | ⚠️ 受 trade_density 计算口径限制 |
| sector_rotation | 188 | 13 (7%) | 0 | ⚠️ 同上 |
| ma_cross | 32 | 32 (100%) | 0 (max score 2.0) | ✅ 真不够 |
| quality_factor | 80 | 80 (100%) | 0 (max score 3.0) | ✅ 真不够 |
| macro_timing | 142 | 142 (100%) | 59 (升级率 41.5%)* | ❌ 公式过松 |

*macro_timing 41.5% 与架构方案 §16.2 的 98.6% 差异,源于复刻公式细节(我的复刻可能漏了某个加分项,但定性一致 — 都是过松)。

**volatility_breakout 实测样本(分数 5.5 的典型构成)**:

```
trade_density<=1.2 (1.0) + sharpe>=1.0 (2.0) + oos_return>=0.18 (1.5) + stability>=0.5 (1.0) = 5.5
DSR / PBO / RC / SPA 全部 None → 各加 0
```

→ **靠前 4 项松阈值堆叠达标,DSR/PBO 没真实贡献**。

**问题 C 实证**(全量 1961 条 submission report):

```
multiple_testing_mode 分布:
  <missing>  =  1961 / 1961 (100%)

各字段命中率:
  formal deflated_sharpe_ratio   =  0 / 1961 (0.00%)
  formal pbo                      =  0 / 1961 (0.00%)
  formal white_reality_check_pvalue =  0 / 1961 (0.00%)
  formal hansen_spa_pvalue       =  0 / 1961 (0.00%)
  proxy deflated_sharpe_proxy    =  0 / 1961 (0.00%)
```

代码定位:`packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/multiple_testing.py::_estimate_run_correction_metrics` 已实现完整逻辑(第 3-170 行),但 grep 全仓 0 个调用方:

```bash
$ grep -r "_estimate_run_correction_metrics" packages/strategy-factory/
# 唯一一处:multiple_testing.py 第 3 行的定义
# 调用方:0 处
```

→ **dead code**。从未在生产 cycle 中执行过。

### V5 P3 路线重排

**架构方案 §16.5 原列了 3 个修复 (修复 1 / 修复 2 / 修复 3),实施顺序是"任意"。本次调研后必须强制锁定顺序**:

| 顺序 | 任务 | 依赖 | 工作量 |
|---|---|---|---|
| **V5-PR-1** | 接入 `_estimate_run_correction_metrics`(原修复 2 升级版) | DEV-V1 已落地 | 中-高(找调用入口 + 集成 + 测试 + 数据验收) |
| V5-PR-2 | 跑批量 cycle 累积 30+ 条带 DSR/PBO 的真实候选,验证升级率分布 | V5-PR-1 上线 7 天 | 小(纯监测) |
| V5-PR-3 | 扩展 `_TARGET_ONLY_VALIDATION_FOCUSES`(原修复 1) | V5-PR-2 完成 + DSR/PBO 命中率 ≥ 60% | 小 |
| V5-PR-4 | 按 family 分级 evidence_score 阈值(原修复 3) | V5-PR-3 完成 + 升级率分布数据 | 小-中 |

V5-PR-1 是 V5 P3 的**前置基础设施**,必须先做。后续 PR 都依赖它。

### V5-PR-1 实施级工作要点(详细 diff 由后续探索)

V5-PR-1 是探索性工作(需要先定位 `_estimate_run_correction_metrics` 应该在哪个评分入口被调用),不是简单的 N 行 diff patch。建议拆 4 个子任务:

**子任务 1:定位调用入口候选**

候选位置(基于 grep `_evaluate_trade_profile|_evaluate_statistical_admission` 的查找结果):

```text
packages/strategy-factory/src/strategy_factory/application/submission_gate/runner_parts/multiple_testing.py
  - _evaluate_trade_profile  (第 258-466 行)
  - _evaluate_statistical_admission  (第 557-672 行)
```

需要读这两个函数,确认:
1. 哪一个负责计算 evidence_score 的依赖项
2. `_estimate_run_correction_metrics` 应该在它们的哪个步骤被调用
3. 输入参数(`attempt_adjustment / family_returns / sample_size / cohort_effective_trials / validation_runtime`)能否在该步骤拿到

**子任务 2:数据可用性核查**

`_estimate_run_correction_metrics` 第 116 行要求 `family_arr.shape[0] >= 12 and family_arr.shape[1] >= 2` 才计算 PBO/RC/SPA。生产中候选有多少能满足?

需要采集真实 cycle 中 family_returns 矩阵的 shape 分布:
- 只跑 1 stock = (12+, 1) → DSR 可算,PBO/RC/SPA 不可算
- 跑 N stocks (N >= 2) + 12+ 期 → 全部可算

如果生产中 90%+ 候选 shape 不足 (≥12, ≥2),修复 2 仍然无效。

**子任务 3:_estimate_run_correction_metrics 集成**

集成模式(初步设计):

```python
# 在 _evaluate_trade_profile 或 _evaluate_statistical_admission 末尾
correction_metrics = _estimate_run_correction_metrics(
    attempt_adjustment=attempt_adjustment,
    family_returns=family_returns,  # 来自 backtest_metrics 的 stock_returns
    sample_size=sample_size,         # 来自 wf 验证的 fold count
    cohort_effective_trials=cohort_effective_trials,
    validation_runtime=validation_runtime,  # 来自 mcp_services
    raw_sharpe_proxy=...,
    observed_mean=...,
    observed_t=...,
)

# 把 formal_fields 合并到 gate dict
if correction_metrics.get("formal_fields"):
    normalized_gate.update(correction_metrics["formal_fields"])
```

详细 diff 需要先做子任务 1 + 2。

**子任务 4:测试 + 数据验证**

- 单元测试覆盖 `_estimate_run_correction_metrics` 在 sample_size < 3 / family_arr shape 不足等降级路径
- 跑一次完整 cycle,验证新生成的候选 quality_report 中 `multiple_testing_mode = "formal_runtime"` 字段命中率 > 0
- 部分候选(满足 family_arr shape 要求)的 deflated_sharpe_ratio / pbo 等字段非 None

### 当前 V5 P3 工作流真实状态

| 子项 | 状态 | 阻塞条件 |
|---|---|---|
| **V5-PR-1** 接入 `_estimate_run_correction_metrics` | **✅ 代码落地 + 端到端验证 PASS** | — (等下次生产 cycle 真有候选触发,DB 字段会自然填充) |
| V5-PR-2 数据验证 | 🟡 待启动 | DEV-V1 P1 灰度上线 + 累积 ≥ 30 条带 DSR/PBO 的 Gate-passed 候选 |
| V5-PR-3 扩展 target_only | ⏸️ 阻塞 | 等 V5-PR-2 数据验证 DSR/PBO 命中率 ≥ 60% |
| V5-PR-4 按 family 分级阈值 | ⏸️ 阻塞 | 等 V5-PR-3 跑出新升级率分布数据 |

### V5 P3 关键判断

> 本节判断写于 V5-PR-1 调研阶段。第 3/4 点已被后续"V5-PR-1 落地实证"章节验证 — 工作量符合预期(中等),数据可用性也已部分验证(proxy 字段 100% 可用,formal 版受 family_returns 限制)。保留原文供溯源。

1. **DEV-V1 是稳定的中间态**。P0+P1 已经能让 observe lane 流量从 0 到非 0,paper account 增长。**V5 P3 不是 DEV-V1 紧急后续,而是反馈回流闭环的最后一环**。
2. **不可仓促做修复 1**(扩展 target_only)。在 DSR/PBO 仍是 dead code 的情况下,68.4% 假阳性会让 paper observation 充满低质量"假 C 级"。
3. **V5-PR-1 工作量比预想大**(✅ 实测:中等,~130 行 + 7 个测试,半天落地)。涉及读 ~200 行 `_evaluate_trade_profile` / `_evaluate_statistical_admission` 代码 + 设计降级路径 + 集成测试。建议作为独立 sprint,与 DEV-V1 灰度运行(7-14 天)平行进行。
4. **数据可用性是 V5-PR-1 真正的不确定点**(🟡 部分验证:proxy 版字段 100% 可用,formal 版需要 V5-PR-2 在生产 cycle 中观察 family_returns 命中率)。如果生产中绝大部分候选的 family_returns shape 不满足 PBO/RC/SPA 要求,修复 2 集成后 DSR 可算但 PBO 等仍 None,evidence_score 加分仍然是公式松。这是 V5-PR-2 的核心验收点。


## V5-PR-1 落地实证(2026-05-26 下午)

V5-PR-1 已于 2026-05-26 完成代码落地、单元测试、端到端验证三阶段。本节记录验收数据,作为 V5-PR-2 数据验证的起点。

### 落地代码改动

| 文件 | 改动 | 行数 |
|---|---|---|
| `submission_gate/runner_parts/multiple_testing.py` | 新增 helper `_inject_run_correction_metrics(strategy, profile, normalized, *, validation_report, backtest_metrics)` | +112 行(第 168-279 行附近) |
| `submission_gate/runner_parts/entry.py` | 在 `multiple_testing_registry` 之后注入调用,合并到 normalized 走 `normalize_quality_gate_result` 二次校正 | +18 行(第 178 行后) |
| `tests/test_inject_run_correction_metrics.py` | 新增 7 个单元测试覆盖最小输入 / score_series / family_returns / 降级路径 / 异常路径 / 回退链 / attempt_adjustment 重建 | +1 个新文件 |

### 三阶段验证结果

#### 阶段 1:单元测试 7/7 PASS

`pytest tests/test_inject_run_correction_metrics.py -v` 输出:

```text
test_inject_with_minimal_input PASSED
test_inject_with_score_series PASSED
test_inject_with_family_returns PASSED
test_inject_validation_runtime_unavailable_softfall PASSED
test_inject_helper_does_not_crash_on_exception PASSED
test_inject_resolves_attempt_adjustment_when_missing PASSED
test_inject_extracts_observed_score_with_fallback_chain PASSED
====== 7 passed in 0.42s ======
```

回归测试 `pytest packages/strategy-factory/tests -k "submission_gate or quality_gate"` 全部 82 PASS,无破坏性变更。

#### 阶段 2:隔离验证(真实 validation_runtime)

脚本 `_verify_v5_pr1_isolated.py` 直接调用 helper(不走 mock),关键输出:

```json
{
  "run_correction_mode": "attempt_only_proxy",
  "raw_sharpe_proxy": 47.8363,
  "deflated_sharpe_proxy": 47.0606,
  "pbo_proxy": 0.0203,
  "reality_check_pvalue_proxy": 0.07,
  "spa_pvalue_proxy": 0.066,
  "cohort_effective_trials": 4.5,
  "multiple_testing_mode": "formal_runtime",
  "deflated_sharpe_ratio": 0.9489,
  "deflated_sharpe_reference_sharpe": 20.1788,
  "deflated_sharpe_effective_trials": 5.0,
  "multiple_testing": {
    "deflated_sharpe": {
      "available": true,
      "sample_size": 5,
      "observed_sharpe": 47.84,
      "reference_sharpe": 20.18,
      "effective_trials": 5.0,
      "z_score": 1.6346,
      "dsr": 0.9489,
      "psr_vs_benchmark": 0.9977,
      "reference_mode": "estimated_standard_error"
    }
  },
  "warnings": [],
  "multiple_testing_inject_status": "ok"
}
```

`normalize_quality_gate_result` 二次合并保留:`multiple_testing_mode='formal_runtime'`,`deflated_sharpe_ratio=0.9489`。

#### 阶段 3:端到端验证(完整 `run_submission_quality_gate` 流程)

脚本 `_e2e_v5_pr1.py` 用真实 strategy(`momentum` 类型 + 6 fold walk_forward)走完整 submission_gate,关键字段命中:

| 字段 | 老 dead code | V5-PR-1 真跑后 | 验收 |
|---|---|---|---|
| `multiple_testing_inject_status` | 不存在 | **`'ok'`** | ✅ |
| `multiple_testing_mode` | None | **`'formal_runtime'`** | ✅ |
| `deflated_sharpe_ratio` | None | **0.9992** | ✅ |
| `raw_sharpe_proxy` | None | **76.85** | ✅ |
| `deflated_sharpe_proxy` | None | **76.85** | ✅ |
| `pbo_proxy` | None | **0.01** | ✅ |
| `reality_check_pvalue_proxy` | None | **0.05** | ✅ |
| `spa_pvalue_proxy` | None | **0.05** | ✅ |
| `run_correction_mode` | 不存在 | **`'attempt_only_proxy'`** | ✅ |
| `multiple_testing.deflated_sharpe.available` | False | **True** | ✅ |
| `multiple_testing.deflated_sharpe.dsr` | None | **0.9992** | ✅ |
| `pbo` (formal) | None | None | ⚠️ 预期(无 family_returns 矩阵) |
| `white_reality_check_pvalue` (formal) | None | None | ⚠️ 预期(无 family_returns 矩阵) |
| `hansen_spa_pvalue` (formal) | None | None | ⚠️ 预期(无 family_returns 矩阵) |
| `passed` | — | True | ✅ 评分流程完整通过 |

**结论**:`_estimate_run_correction_metrics` dead code 100% 接活,DSR 链路+5 个 proxy 字段全部产出非空数值。formal 版 PBO/RC/SPA 仍为 None 是预期行为(V5-PR-1 不主动合成 family_returns 矩阵),需要 V5-PR-2 在生产 cycle 中观察是否有候选自带 family_returns。

### 软降级路径验证

helper 内置三层软降级,任何提取/计算失败都不打断主流程:

| 失败情形 | 软降级结果 | inject_status |
|---|---|---|
| `validation_runtime` 不可用 | 跳过 helper,返回 `{"multiple_testing_inject_status": "validation_runtime_unavailable"}` | `validation_runtime_unavailable` |
| `attempt_adjustment` 缺失 | 用 `resolve_attempt_adjustment(strategy)` 重建 | `ok` |
| `score_series` 提取失败 | 跳过 score_series,只算 attempt_only_proxy | `ok` |
| `family_returns` 提取失败 | 跳过 PBO/RC/SPA formal 版,只填 proxy 版 | `ok` |
| 其他异常 | 返回 `{"multiple_testing_inject_status": "exception", "multiple_testing_inject_error": "..."}` | `exception` |

所有降级路径在单元测试中已覆盖,主流程不受影响。

### 与开发方案 V5-PR-1 子任务 1-4 的对应

| 开发方案预设子任务 | 实际完成情况 |
|---|---|
| 子任务 1:定位调用入口候选 | ✅ 选定 `submission_gate/runner_parts/entry.py` 第 178 行后(`multiple_testing_registry` 之后) |
| 子任务 2:写 helper 提取必要参数 | ✅ helper 在 `multiple_testing.py` 第 168 行后,4 个参数源 + 三层软降级 |
| 子任务 3:把 helper 集成到 entry.py 评分流 | ✅ `entry.py` 注入调用 + `normalize_quality_gate_result` 二次合并 |
| 子任务 4:跑完整 cycle 验证 DB 字段非空 | 🟡 隔离验证 + 端到端验证 100% PASS,生产 cycle 内 DB 字段验证因当日 cycle 0 候选(LLM `no_executable_specs`)推迟到下次生产 cycle |

**子任务 4 推迟原因**:2026-05-26 下午 14:57 启动的真跑 cycle 持续 9 分钟仍在 LLM 阶段循环(连续 4 次 `Pipeline staged mode returned no specs; suppressing monolithic/local fallback`),最终 0 条新 quality_report 写入 DB。该问题与 V5-PR-1 无关,属于 LLM 生成 spec 质量问题(`pipeline_staged_mode` 路径)。后续 V5-PR-2 数据验证启动时,会同时观察 LLM 路径恢复情况。

### V5-PR-2 数据验证启动条件

V5-PR-1 落地后,V5-PR-2 数据验证可以启动,但不强制立即跑。建议触发条件:

1. **DEV-V1 P1 (`INCUBATION_FACTORY_PAPER_INTAKE_ENABLED=1`) 灰度运行 ≥ 7 天**
2. **生产 cycle 累积 ≥ 30 条带 `multiple_testing_inject_status='ok'` 的 Gate-passed 候选**
3. **关键监测指标**:
   - `multiple_testing_mode='formal_runtime'` 命中率 (期望 ≥ 80%,即绝大多数候选 helper 成功执行)
   - `deflated_sharpe_ratio` 非 None 命中率 (期望 ≥ 60%,即多数候选有足够 walk_forward fold 序列)
   - `pbo` formal 版非 None 命中率 (反映 family_returns 在生产中是否可用,初值不限,作为 V5-PR-2 关键发现)
   - `multiple_testing_inject_status='exception'` 出现次数 (期望 = 0,异常路径不应该被触发)

如果 `pbo` formal 版命中率 < 20%,说明 family_returns 在生产中很少可用,需要在 V5-PR-2 决定:
- (a) 接受 proxy 版本(`pbo_proxy / reality_check_pvalue_proxy / spa_pvalue_proxy`)作为 evidence_score 加分依据,不强求 formal 版
- (b) 在 backtest pipeline 中显式构造 family_returns 矩阵(代价大,需要新 PR)

### V5-PR-1 已知的非阻塞限制

| 限制 | 影响 | 处置 |
|---|---|---|
| family_returns 不主动合成 | PBO/RC/SPA formal 版多数候选为 None | 接受,proxy 版可用 |
| score_series 仅取 walk_forward fold 序列 | 其他来源(如 panel test)不参与 | 接受,可后续扩展 |
| inject_status 不影响 quality_gate 评分 | helper 失败不导致候选被拒 | 设计如此(软降级原则) |
| `_estimate_run_correction_metrics` 仍未在 evidence_score 公式中被加分 | dead code 接活后,evidence_score 公式不变,新字段仅作"可观测性"指标 | V5-PR-3/V5-PR-4 处理 |

最后一项是 V5-PR-1 与 V5-PR-3/V5-PR-4 的边界:V5-PR-1 只负责"接活并产出字段",V5-PR-3 扩展 `_TARGET_ONLY_VALIDATION_FOCUSES` 让更多候选走 trade-aware 评分,V5-PR-4 在新评分公式中加入 DSR/PBO 加分项。三者解耦,可分别上线。




新建文件:`data/reports/sf_dev_v1_decision_log.md`(灰度上线时创建)

```markdown
# Strategy Factory DEV-V1 决策日志

## 2026-05-XX TG 上线
...

## 2026-05-XX P0 toggle ON (阶段 1)
...

## 2026-05-XX P0 toggle ON (阶段 2 — 7 天观察期)
...

## 2026-05-XX P1 toggle ON (LIMIT=10)
...
```

## 附录 C:修订历史

| 版本 | 日期 | 变更 |
|---|---|---|
| DEV-V1 | 2026-05-26 | 初版,基于架构方案 V4-network 转换为实施级开发方案 |
| DEV-V1-p3 | 2026-05-26 夜 | 新增 "DEV-V1 落地实证 + P3 灰度发现" 章节(放在附录 A 与附录 B 之间)。记录 58 个测试 PASS、P0/P1 端到端验证、P3 灰度阶段实证(volatility_breakout 升级率 0% / macro_timing 98.59% 过松 / family↔validation_focus 架构耦合)。修复 1 个 bug(intake.scan_and_accept early return 跳过 paper 处理段)。V4 §7.3 推荐顺序修订为 Tier A/B 二分。新增 V5 P3 后续工作 3 项跟踪。 |
| V5-P3-research | 2026-05-26 深夜 | V5 P3 工作流深度调研。回答 3 个开放问题:B (修复 1 单独做导致 volatility_breakout 假阳性 68.4%)、C (`_estimate_run_correction_metrics` 是 dead code,grep 全仓 0 调用方)、A (value_factor/sector_rotation 的 trade_density 在 target_plus_representative 模式下 100%/93% 被范围过滤)。**重大发现:V5 P3 修复 2 不是 toggle 而是新功能接入**。V5 P3 路线重排为 4 个 PR,V5-PR-1 优先做 _estimate_run_correction_metrics 接入。新增 "V5 P3 工作流深度调研" 章节(放在附录 B 之前) |
| V5-PR-1-impl | 2026-05-26 下午 | V5-PR-1 落地完成 + 三阶段验证 PASS。代码改动:`multiple_testing.py` 新增 `_inject_run_correction_metrics` helper(+112 行),`entry.py` 注入调用(+18 行),新增 `test_inject_run_correction_metrics.py` 7 个测试。验证:单元测试 7/7 + 回归 82 PASS + 隔离验证(真实 validation_runtime,DSR=0.9489)+ 端到端验证(`run_submission_quality_gate` 完整流程,DSR=0.9992,5 个 proxy 字段全填充)。formal 版 PBO/RC/SPA 仍为 None 是预期(无 family_returns 矩阵)。生产 cycle DB 字段验证因当日 LLM `no_executable_specs` 推迟到下次生产 cycle。新增 "V5-PR-1 落地实证" 章节,V5-PR-1 状态从 "调研完成" 改为 "✅ 代码落地 + 端到端验证 PASS"。 |
| completion-review | 2026-05-26 末尾 | 开发方案完成度评估更新。"落地总检查清单"中 13 个代码完成项全部 `[x]` 勾选,新增 V5-PR-1 项;DB 备份项标记为已完成;P3 灰度项调整为"保持默认 OFF,等 V5-PR-2"。新增 "开发方案完成度评估" 章节,明确文档主体 + 代码落地 100% 完成,剩余是用户灰度决策(运维动作)和 V5 续作 sprint(独立工作流)。 |
| gray-phase1 | 2026-05-26 末尾 | 灰度阶段 1 (P0 toggle ON 单 cycle 观察) 完成。cycle 时长 630s,rc=0,3 个红线 (R1/R2/R3) 全部未触发。基线扫描 1961 条历史 reports 验证 171 条满足 P0 解封条件 (与 V4-network 反事实模拟一致)。本次 cycle 巧合产 1 条 C 级 + Gate-B blocked 候选,不在 P0 解封集合内,observe_incubation 命中 0 条。但 toggle ON 路径无副作用,可安全保持运行。决策日志 `data/reports/sf_dev_v1_decision_log.md` 已写入阶段 1 完整记录。"落地总检查清单"中"灰度阶段 1"勾选,"决策日志"勾选。下一步等用户决策是否在 .env 持久化 toggle 并启动 7 天观察期。 |
| gray-phases-merged | 2026-05-26 末尾 | 灰度阶段 1+2+3 用户决策合并执行。`.env` 持久化 P0+P1 toggle (备份 `.env.pre_dev_v1.bak` 6.6 KB)。端到端强制验证 `_e2e_p0_p1_force.py` **5 步全 PASS**:① toggle 解析 ✅ ② P0 路径 ON eligible+reason+budget_tier 全对 ③ P0 路径 OFF 对照符合老逻辑 ④ P1 intake `_list_paper_observation_strategies` 三种场景行为正确 ⑤ P1 runner `_list_paper_observation` 真调 db。多 cycle 灰度执行器 `_gray_phase_full.py` 后台运行(自动监控 + 红线停止 + 产 paper 后退出)。已知 LLM `no_executable_specs` 问题导致生产 cycle 短期不会自然产 D + Gate-passed 候选,但端到端验证已证明 toggle 持久化 + 业务逻辑路径完整,**只要未来 cycle 产合格候选,链路会自然触发**。"落地总检查清单"中"阶段 1+2+3 合并"勾选,"P1 启用"勾选。 |


---

## 附录 H — 2026-05-27 长期运行发现的 forward_verifier 阻塞 bug + 降本配套修复

### H.1 问题发现路径

DEV-V1 + 密度提升落地后,启动 `run_all_factories.py` 长期 supervisor。
17 小时累积 39 个 cycle 后观察:

| 指标 | 值 | 说明 |
|---|---|---|
| Cycle 成功率 | 39/39 (100%) | rc=0 |
| P0 触发 (D 级 observe) | 11 次 | macro_timing×9, north_capital_track×2 |
| Paper 账户写入 (P1) | 11 个 | 全部 incubation_factory.paper_observation_recognized 事件 |
| **Active warmup 账户** | **12 个** | **全部 effective_n_5d=0, total_signals=0** |
| **strategy_signal_evidence 行数** | **0** | **forward_verifier 完全失败** |

P0 + P1 链路工作正常,但 Phase 3 (前向收益验证) 完全卡死,
导致没有任何 warmup 账户能升 candidate (升 candidate 阈值: primary_effective_n ≥ 20)。

### H.2 根因分析

**调用方** (`packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/forward_verifier.py:185`):

```python
async def _load_signal_evidence(self, db: Any, strategy_id: str) -> list[...]:
    if hasattr(db, "list_strategy_signal_evidence"):
        try:
            return await db.list_strategy_signal_evidence(
                strategy_id, limit=500            # ❌ 位置参数
            )
        except Exception as exc:
            logger.warning(...)
    return []
```

**被调方** (`packages/aiask-quant-core/src/aiask_quant_core/storage/sqlite/strategy_incubation_parts/reads.py:368`):

```python
async def list_strategy_signal_evidence(
    self,
    *,                                            # ⚠️ 强制 keyword-only
    signal_id: Optional[str] = None,
    strategy_id: Optional[str] = None,
    limit: int = 200,
) -> List[dict]:
    ...
```

**结果**: 每次调用都抛 `TypeError: list_strategy_signal_evidence() takes 1 positional argument but 2 positional arguments (and 1 keyword-only argument) were given`,
被外层 `except Exception` 静默吞掉,evidence 永远空 list,前向验证永远不通过。

**为何测试没拦下来**:
`tests/test_strategy_lifecycle_shared_runtime.py` 的 `_SignalEvidenceDb` mock 用的是
`async def list_strategy_signal_evidence(self, strategy_id, limit)` (位置参数),
和 buggy 调用方刚好匹配,而和真实 DB 签名错位。Mock-impl 不一致这一点是 Lopez de Prado
《Advances in Financial ML》第 4 章里强调的 "test-prod signature drift" 的典型案例。

### H.3 修复 + 防回归

**修复 1 — 调用点改 keyword**:

```python
# forward_verifier.py:185
return await db.list_strategy_signal_evidence(
    strategy_id=strategy_id, limit=500            # ✅ keyword
)
```

**修复 2 — Mock 同步真实签名 (防止未来回归)**:

```python
# tests/test_strategy_lifecycle_shared_runtime.py
class _SignalEvidenceDb:
    async def list_strategy_signal_evidence(
        self, *,                                  # ✅ 强制 keyword-only
        signal_id=None, strategy_id=None, limit=200,
    ):
        assert signal_id is None
        assert strategy_id == "strategy-runtime"
        ...
```

今后任何回归到位置参数的调用方,在跑这个测试时就会立即抛 TypeError。

### H.4 测试矩阵

| 测试套 | 通过数 | 说明 |
|---|---|---|
| `test_strategy_lifecycle_shared_runtime.py` | 6 | 直接覆盖 mock 新签名 |
| `test_factory_deep_repair.py` | 23 | ForwardVerifier 单元测试 |
| `test_execution_audit_acceptance_backfill.py` | 7 | 验证 keyword-only DB 调用习惯 |
| `test_incubation_overview_snapshot_cache.py` | 2 | 孵化历史快照 |
| `test_incubation_history_replay.py` | 2 | 孵化历史回放 |
| **合计** | **40** | **全部 PASS** |

### H.5 降本配套 (.env)

修密度提升后单 cycle ~390s × 5 分钟间隔 = 满载 LLM 推理,17 小时跑了 $200。
本次降本配合 forward_verifier 修复一起灰度:

| 参数 | 改前 | 改后 | 说明 |
|---|---|---|---|
| `STRATEGY_FACTORY_MARKET_HOURS_INTERVAL_SEC` | 300 (5 min) | **7200 (2 h)** | 盘中 cycle 间隔 |
| `STRATEGY_FACTORY_OFF_HOURS_INTERVAL_SEC` | 1800 (30 min) | **21600 (6 h)** | 盘后 cycle 间隔 |

**预期成本**: $283/天 → **~$30/天** (≈ 1/10)

**为何降本不会拖慢观察期**:
- 修 forward_verifier 后,**每个 cycle 真正在做事**(写 strategy_signal_evidence + 升 effective_n_5d)
- 12 个 active warmup 账户每个都需要 ≥ 20 个 trade,即使每天只 4 个 cycle,
  按修复后预期每 cycle 各账户 +1~2 trade 累计,大约 5-10 天可看到第一批升 candidate
- 之前 5 分钟间隔的"高频"完全在烧钱,因为每个 cycle Phase 3 都失败,trade count 永远是 0

**备份**: `.env.pre_cost_reduction.bak` (9.3 KB)

### H.6 生效路径 (重要)

⚠️ **当前 supervisor 进程已加载 buggy 版本的 `forward_verifier.py` 到内存,只修代码不会生效。**

操作步骤:
1. 用户在跑 supervisor 的终端 Ctrl+C
2. 重新启动:`python run_all_factories.py` (会按 .env 新值跑 2h 间隔)
3. 第一个 cycle 完成后,检查:
   ```sql
   SELECT COUNT(*) FROM strategy_signal_evidence;       -- 应该 > 0
   SELECT id, primary_effective_n, total_signals
   FROM strategy_lifecycle_states
   WHERE current_stage = 'warmup' ORDER BY updated_at DESC LIMIT 5;
   ```
4. N 个 cycle 后 (按 2h 间隔每天 ~12 cycle),`primary_effective_n` 应稳步爬升

### H.7 风险评估: LOW

- 修复点小且语义明确,只是恢复原本设计意图
- `prediction_trace.py:_build_execution_lineage` 是同名调用的正确范式 (已 keyword + try-TypeError 双层兜底),
  可作为参考标杆
- 单元 / 集成 / 历史回放三层测试全 PASS,无任何回归
- .env 改的是"打多快"而非"打什么",grade 评分 / formal_track_blockers / quality_gate
  全部不动
