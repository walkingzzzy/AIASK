# MCP服务修复方案

日期：2026-03-30  
范围：`packages/akshare-mcp`、`packages/strategy-factory`、`apps/bff`、根目录 MCP 配置文件  
目标：把当前 MCP 实现从“可运行但协议契约不稳、HTTP 安全未接线、Manager 输入模型失真”的状态，修复为“协议可直连、能力声明真实、安全边界清晰、可持续演进”的状态。

---

## 1. 修复目标

本方案聚焦四类问题：

1. 修复当前 Manager 工具的 MCP 输入 Schema 失真问题，保证直接 MCP Host 调用可用。
2. 修复 HTTP / SSE 传输的安全接线缺口，避免“只做环境检查、不做真实认证”的伪安全状态。
3. 收敛错误模型、状态模型和高风险操作门禁，降低协议层与业务层行为分裂。
4. 用测试、验收标准和分阶段发布方案，把修复从一次性补丁变成可回归、可持续维护的基线。

本方案不追求一次性重写所有工具，而是优先解决：

- `P0` 安全和协议阻断项
- `P1` 生命周期与能力声明一致性
- `P2` 质量、性能和状态隔离
- `P3` 收尾、文档、联调和验收

---

## 2. 当前问题摘要

### 2.1 P0 阻断问题

- 根目录 [`.mcp.json`](/Users/mac/Desktop/股票/.mcp.json) 存在明文 `TUSHARE_TOKEN` 和 `DB_PASSWORD`。
- 多个 Manager 工具使用 `action + **kwargs` 的旧签名，虽然 `**kwargs` 本身是可选参数不会导致 Pydantic `Field required` 报错，但 FastMCP 自动生成的 input schema **无法表达实际业务参数**（如 `code`、`indicator`、`status` 等），MCP Host 无从得知应传什么字段，导致 schema 不可用。
- `user_manager` 使用 `kwargs: str = '{}', **extra_kwargs` 签名，`**extra_kwargs` 会被 FastMCP 生成 `additionalProperties` 模式，部分 MCP Host 在 Schema 校验阶段会报错。这是当前唯一真正触发 schema 校验异常的签名。
- 28 个使用 `**kwargs` 签名的 Manager 工具虽然不会在 `action="help"` 时触发校验报错，但 schema 缺乏结构化描述使得直接 MCP Host 调用（无 BFF 包装）体验极差——MCP Host 只能看到 `action: str` 一个可用字段。

### 2.2 P1 高风险问题

- 服务端代码检查了 `MCP_ALLOWED_ORIGINS` / `MCP_AUTH_MODE` / `MCP_HOST`，但没有把这些配置真正接入 `FastMCP(auth=..., token_verifier=...)`。
- `live_trading_manager` 暴露了 `submit_order` / `cancel_order`，但没有复用项目内已有的 [`risk_guard.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/risk_guard.py) 确认门禁与统一审计逻辑。
- `user_manager` 直接信任 `user_id` 参数，缺少会话绑定、最小化返回字段和租户边界。

### 2.3 P2 结构与质量问题

- 当前 MCP 服务只真正实现了 `Tools`，`Resources` 和 `Prompts` 在 SDK 层被声明，但业务实现为空。
- 错误处理混用三套方式：
  - FastMCP / Pydantic 的 `ToolError`
  - 项目统一的 `{success, error}` 业务返回
  - 部分 Manager 自己的 `error_code`
- 进程内状态散落在多个模块：
  - [alerts.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/alerts.py)
  - [execution_manager.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py)
  - `paper_trading_manager` 的 DB 状态与进程态组合
- `tool-only` 启动模式只关闭后台调度，并没有减少重型模块导入和工具注册成本。

---

## 3. 修复原则

### 3.1 协议优先

- 任何对外 MCP 工具必须先满足“结构化调用可用”，再谈 BFF 兼容。
- BFF 不能继续作为“补偿型调用层”去掩盖 MCP 契约问题。
- 新的 Manager 契约统一采用 `action + params`，`kwargs` 只保留为兼容输入，且必须是可选字段。

### 3.2 安全优先

- 凭据必须完全退出仓库。
- HTTP / SSE 认证必须真实接线，不能只检查环境变量是否存在。
- 高风险写操作必须具备“可阻止误触发”的确认门禁和审计记录。

### 3.3 能力声明真实

- 如果没有 Resources / Prompts 的真实实现，就不要在产品文档和能力描述里把它们写成已交付能力。
- 如果保留 SDK 自动能力声明，需要同步补齐真实实现；否则需要显式规划如何降级或延后。

### 3.4 渐进修复

- 先修协议阻断，再修安全接线，再修状态与性能。
- 优先改公共签名和基础设施，避免一个个 Manager 单点打补丁。
- 每个阶段都必须配套契约测试和回归验证。

---

## 4. 总体实施顺序

建议按以下顺序推进：

1. `P0-A` 清除凭据与配置风险
2. `P0-B` 统一 Manager 输入签名与 Schema
3. `P0-C` 增补 MCP 直连契约测试
4. `P1-A` 接通 HTTP / SSE 认证与 Origin 校验
5. `P1-B` 实盘写操作接入确认门禁与审计
6. `P1-C` 收敛错误模型与能力声明
7. `P2-A` 收敛进程态与持久化边界
8. `P2-B` 优化启动路径与连接池策略
9. `P3` 文档、发布、联调和验收

---

## 5. P0 修复任务

## T01 凭据治理与配置清理

优先级：`P0`  
目标：把仓库中的真实凭据移除，建立本地配置模板与忽略策略。

涉及文件：

- [`.mcp.json`](/Users/mac/Desktop/股票/.mcp.json)
- [`packages/akshare-mcp/.env.example`](/Users/mac/Desktop/股票/packages/akshare-mcp/.env.example)
- [`.gitignore`](/Users/mac/Desktop/股票/.gitignore)
- [`README.md`](/Users/mac/Desktop/股票/README.md)

实施内容：

1. 删除 [`.mcp.json`](/Users/mac/Desktop/股票/.mcp.json) 中的真实 token / password，改成占位值或切换为不提交的本地模板。
2. 轮换已经暴露的 `TUSHARE_TOKEN` 和数据库密码。
3. 在文档中明确：
   - 本地调试使用 `.env` 或桌面客户端本地配置注入
   - 仓库只保留示例值
4. 如保留 `.mcp.json`，拆成：
   - 提交版：模板文件
   - 本地版：忽略文件

验收标准：

- 仓库中不存在真实密钥、密码、访问 token。
- 新开发者可以只通过示例文件完成本地配置。
- CI 或 pre-commit 能发现新增凭据泄露。

## T02 Manager 输入协议统一

优先级：`P0`  
目标：把所有 Manager 工具统一到结构化输入模型，解决 `**kwargs` 签名导致的 schema 无法表达业务参数问题，以及 `user_manager` 的 `**extra_kwargs` schema 校验异常。

重点文件：

- [`packages/akshare-mcp/src/akshare_mcp/tools/manager_protocol.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/manager_protocol.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/__init__.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/__init__.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/alerts_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/alerts_manager.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/data_sync_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/data_sync_manager.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/decision_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/decision_manager.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/live_trading_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/live_trading_manager.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/paper_trading_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/paper_trading_manager.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/user_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/user_manager.py)
- 其余所有 `*_manager.py`

实施策略：

1. 定义统一签名规范：
   - 推荐形态：`async def xxx_manager(action: str, params: dict | None = None, kwargs: Any = None, code: Optional[str] = None)`
   - 禁止直接使用 `**kwargs` 作为 MCP 顶层函数签名。
2. 在 [`manager_protocol.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/manager_protocol.py) 中新增统一入口函数，例如：
   - `normalize_manager_payload(params=None, kwargs=None, extra=None)`
   - 同步做 alias 归一化、JSON 兼容、字段提取
3. 逐个替换 28 个使用 `**kwargs` 签名、以及 1 个使用 `**extra_kwargs` 签名的 Manager 的函数签名和参数解析逻辑。
4. 保留旧调用兼容：
   - 若 BFF 仍传 `kwargs: JSON.stringify(...)`，服务端仍能解析
   - 但生成出的 MCP schema 必须以结构化字段为主
5. `user_manager` 需移除 `**extra_kwargs` 顶层签名，避免生成 `extra_kwargs` 必填 schema。

第一批必须优先修复的工具：

- `alerts_manager`
- `data_sync_manager`
- `decision_manager`
- `live_trading_manager`
- `paper_trading_manager`
- `portfolio_manager`
- `performance_manager`
- `research_manager`
- `risk_manager`
- `vector_search_manager`
- `watchlist_manager`
- `user_manager`

验收标准：

- 所有 Manager 工具的 input schema 能够完整描述业务参数（如 `code`、`indicator` 等），MCP Host 可根据 schema 正确构造调用。
- `user_manager` 不再使用 `**extra_kwargs`，消除 schema 校验异常。
- `tool.run({"action":"help"})` 对所有 Manager 工具都能正常返回帮助信息。
- 运行时 schema 中，`kwargs` 仅作为可选兼容字段存在，不主导 schema。
- BFF 现有调用不回退。

## T03 MCP 契约测试补齐

优先级：`P0`  
目标：让“直接 MCP 调用”成为第一层回归基线，而不是只测 BFF 包装后的调用。

涉及文件：

- [`packages/akshare-mcp/tests/test_mcp_functional.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/tests/test_mcp_functional.py)
- 新增 `packages/akshare-mcp/tests/test_manager_schema_contract.py`
- 新增 `packages/akshare-mcp/tests/test_manager_help_contract.py`
- 视情况补充 `apps/bff` 集成测试

实施内容：

1. 对所有 `*_manager` 做 schema 快照检查：
   - `required` 字段不能包含 `kwargs`
   - `required` 字段不能包含 `extra_kwargs`
2. 对所有 `*_manager` 跑最小 `help` 调用：
   - `await tool.run({"action":"help"})`
   - 如果必须带业务字段，再测最小结构化 `params`
3. 为 `strategy_manager`、`quant_manager` 追加“结构化 params 调用”正例测试，作为统一迁移的参考基线。
4. 对 BFF 继续保留“旧 kwargs JSON”兼容测试，避免回归。

验收标准：

- 所有 manager 至少具备：
  - schema 合法性测试
  - `help` 最小调用测试
  - 1 个结构化调用正例

---

## 6. P1 修复任务

## T04 HTTP / SSE 认证与 Origin 校验真实接线

优先级：`P1`  
目标：把当前“只检查环境变量”的 HTTP 安全基线，升级为实际生效的认证与传输保护。

重点文件：

- [`packages/akshare-mcp/src/akshare_mcp/server.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/server.py)
- [`packages/akshare-mcp/start_server.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/start_server.py)
- 新增 `packages/akshare-mcp/src/akshare_mcp/auth/*.py`

实施内容：

1. 在 `FastMCP(...)` 构造阶段显式接入：
   - `auth`
   - `token_verifier`
   - 必要的 `transport_security`
2. 把环境变量：
   - `MCP_AUTH_MODE`
   - `MCP_ALLOWED_ORIGINS`
   - `MCP_HOST`
   - 可能的 issuer / audience / API key 配置
   转换成实际 `FastMCP` 配置对象。
3. 区分三类场景：
   - `stdio`：不要求 HTTP 认证
   - `sse`
   - `streamable-http`
4. 增加 HTTP 运输层联调测试：
   - 未带凭据拒绝
   - 非允许 Origin 拒绝
   - 合法凭据放行

验收标准：

- HTTP / SSE 模式下，请求未经认证无法访问 tool routes。
- Origin 校验真实生效，而不是仅启动前做字符串检查。
- `stdio` 模式不受影响。

## T05 高风险写操作确认门禁与审计

优先级：`P1`  
目标：为实盘写操作补充确认门禁、审计记录和误操作防护。

重点文件：

- [`packages/akshare-mcp/src/akshare_mcp/tools/risk_guard.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/risk_guard.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/live_trading_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/live_trading_manager.py)
- [`packages/akshare-mcp/src/akshare_mcp/services/live_broker.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/live_broker.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py)

实施内容：

1. 把 `submit_order` / `cancel_order` 纳入统一确认门禁：
   - `confirm_token`
   - 或显式 `dry_run`
2. 在 `live_trading_manager` 中统一记录审计事件：
   - 请求参数摘要
   - 是否通过确认
   - 是否处于只读模式
   - 最终经纪商响应
3. 对写操作引入双门禁：
   - 环境级写权限 `allow_write/read_only`
   - 请求级确认 token
4. 审计结果统一写入可检索日志或持久化表，不只保留 stdout / stderr。
5. **注意**：现有 `risk_guard.py` 中的 `@risk_audited` 装饰器为同步版本（内部调用 `func(*args, **kwargs)` 而非 `await`），`live_trading_manager` 的处理函数为 async，因此需要新增 async 版本的装饰器（如 `@risk_audited_async`），或改为在 `submit_order` / `cancel_order` 路径中手动调用 `require_confirmation_if_needed()` + `audit_event()`。

验收标准：

- 未提供确认 token 时，写操作不能直达 broker。
- `dry_run` 仍可用于预览。
- 每次写操作都能留下审计记录。

## T06 用户与租户边界修复

优先级：`P1`  
目标：降低 `user_manager` 暴露全部用户信息、信任任意 `user_id` 的风险。

重点文件：

- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/user_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/user_manager.py)
- [`apps/bff/src/auth`](/Users/mac/Desktop/股票/apps/bff/src/auth)
- [`apps/bff/src/mcp-gateway/mcp-gateway.service.ts`](/Users/mac/Desktop/股票/apps/bff/src/mcp-gateway/mcp-gateway.service.ts)

实施内容：

1. 明确 `user_manager` 的使用边界：
   - 是仅供受信后端调用
   - 还是允许通用 MCP Host 直接调用
2. 如果继续暴露为通用 MCP 工具：
   - 移除 `list_users` 对邮箱等敏感字段的默认暴露
   - 限制可查询范围
3. 如果只供 BFF 使用：
   - 在工具文档和代码中显式标注内部用途
   - 通过上下文或参数签名减少“外部任意 user_id”写入

验收标准：

- 不再默认返回用户邮箱等敏感字段。
- `user_id` 的来源和权限边界清晰可解释。

## T07 错误模型统一

优先级：`P1`  
目标：统一协议级错误、业务级失败和 BFF 错误转译，避免同类错误多种表现。

重点文件：

- [`packages/akshare-mcp/src/akshare_mcp/utils.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/utils.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/manager_protocol.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/manager_protocol.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/strategy_manager.py)
- [`apps/bff/src/mcp-gateway/mcp-gateway.service.ts`](/Users/mac/Desktop/股票/apps/bff/src/mcp-gateway/mcp-gateway.service.ts)

实施内容：

1. 约定错误分类：
   - 参数错误
   - 资源不存在
   - 权限错误
   - 后端依赖错误
   - 未知内部错误
2. 统一 payload 字段：
   - `success`
   - `error`
   - `error_code`
   - `detail`
   - `meta.trace_id`
3. 明确何时应抛 MCP 协议错误，何时应返回业务失败结果。
4. BFF 统一只消费一种错误外形，不再靠正则匹配 `"validation error"` 文本。

验收标准：

- 同类错误在 MCP 和 BFF 看到的是同一套分类。
- 新增测试可断言 `error_code` 和 `trace_id`。

---

## 7. P2 修复任务

## T08 Resources / Prompts 能力收敛

优先级：`P2`  
目标：解决“SDK 能力已声明、业务实现为空”的不一致。

涉及文件：

- [`packages/akshare-mcp/src/akshare_mcp/server.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/server.py)
- 新增 `packages/akshare-mcp/src/akshare_mcp/resources/*.py`
- 新增 `packages/akshare-mcp/src/akshare_mcp/prompts/*.py`

两种可选路径：

1. `短期路径`
   - 在方案和文档中明确当前只承诺 `Tools`
   - 不新增对外产品承诺
2. `中期路径`
   - 选取高价值对象实现真实 MCP Resources / Prompts

建议优先实现的原生对象：

- `resource://stock/{code}/profile`
- `resource://strategy/{id}/review`
- `resource://watchlist/{user_id}/snapshot`
- `prompt://factor-mining`
- `prompt://strategy-review`

验收标准：

- 若不实现，则文档与对外说明中不再把 Resources / Prompts 视为已交付能力。
- 若实现，则 `list_resources` / `list_resource_templates` / `list_prompts` 必须返回非空，并配套读取测试。

## T09 进程态与持久化边界收敛

优先级：`P2`  
目标：减少 stdio 多进程、BFF 连接池和后台任务并存时的状态漂移。

重点文件：

- [`packages/akshare-mcp/src/akshare_mcp/tools/alerts.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/alerts.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/alerts_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/alerts_manager.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py)
- [`packages/akshare-mcp/src/akshare_mcp/tools/managers/paper_trading_manager.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/paper_trading_manager.py)

实施内容：

1. 评估哪些状态允许留在进程内：
   - 缓存
   - 临时预览
2. 评估哪些状态必须持久化：
   - 告警
   - 执行任务
   - 审计记录
   - 风险确认
3. 对 `execution_manager` 的 `_EXECUTION_TASKS` 建立持久化或最少可恢复策略。
4. 对 `alerts_manager` 逐步从 `_alerts_store` 迁移到 DB / Redis。

验收标准：

- 同一 BFF 连接池下，不同 MCP 子进程之间的状态不再出现明显分叉。
- 重启后关键状态可恢复或明确丢失语义。

## T10 启动路径和性能优化

优先级：`P2`  
目标：让 `tool-only` 模式真正减少启动成本，而不仅是少开调度器。

重点文件：

- [`packages/akshare-mcp/src/akshare_mcp/server.py`](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/server.py)
- [`apps/bff/src/mcp-gateway/mcp-gateway.service.ts`](/Users/mac/Desktop/股票/apps/bff/src/mcp-gateway/mcp-gateway.service.ts)

实施内容：

1. 把重型模块导入和工具注册按类别分层：
   - 核心轻量工具
   - manager 工具
   - 重型计算与实验工具
2. 评估是否可把部分后台服务和超重工具做懒加载。
3. 优化 BFF 连接池策略：
   - 标准请求使用 `tool-only`
   - 只有确需后台服务的连接使用 `full`
4. 为启动时间、首个 `listTools` 延迟、首个 `callTool` 延迟建立基线。

验收标准：

- `tool-only` 子进程的启动耗时明显下降。
- BFF 连接池扩容时不再明显放大初始化抖动。

---

## 8. P3 发布与验收任务

## T11 文档与调用规范同步

优先级：`P3`

涉及文件：

- [`README.md`](/Users/mac/Desktop/股票/README.md)
- [`packages/akshare-mcp/README.md`](/Users/mac/Desktop/股票/packages/akshare-mcp/README.md)
- [`docs/AGENTS.md`](/Users/mac/Desktop/股票/docs/AGENTS.md)
- 新增 `packages/akshare-mcp/MCP_MANAGER_CONTRACT.md`

实施内容：

1. 更新 Manager 调用规范：
   - 推荐 `params`
   - `kwargs` 为兼容模式
2. 更新 HTTP 认证配置方式。
3. 明确 Resources / Prompts 当前状态。
4. 给 BFF 说明哪些调用仍在过渡兼容期。

## T12 发布前回归清单

优先级：`P3`

必须执行的回归：

1. 运行时 `listTools` 数量和 schema 巡检
2. 所有 Manager 的 `help` 调用巡检
3. BFF 关键页面联调：
   - 告警
   - 组合
   - 风险
   - 回测
   - 执行
   - 事件
   - 因子
4. `stdio` 模式端到端回归
5. `sse` / `streamable-http` 安全联调
6. 实盘写接口只读 / 确认 / 审计回归

---

## 9. 验收标准

### 9.1 协议验收

- 所有 Manager 工具都支持直接 MCP 结构化调用。
- `help` 调用不依赖额外 `kwargs="{}"` 填充。
- `initialize` 返回的能力声明与真实实现一致。

### 9.2 安全验收

- 仓库无真实凭据。
- HTTP / SSE 模式下，认证与 Origin 校验真实生效。
- 实盘写操作必须显式确认且留审计记录。

### 9.3 兼容验收

- BFF 现有页面调用不回退。
- `strategy_manager` / `quant_manager` 的结构化模式保持稳定。
- 旧 `kwargs JSON` 兼容保留到迁移完成，但不再主导 schema。

### 9.4 质量验收

- 新增契约测试覆盖所有 Manager。
- 错误模型、日志、trace_id 可被稳定观察。
- 多连接、多子进程情况下的状态行为可解释。

---

## 10. 分阶段交付建议

### 第一周

- T01 凭据治理
- T02 第一批 Manager 签名迁移
- T03 契约测试基线

### 第二周

- 完成所有 Manager 输入协议统一
- T05 实盘写门禁与审计
- T07 错误模型统一第一版

### 第三周

- T04 HTTP / SSE 认证真实接线
- T06 用户与租户边界修复
- T08 Resources / Prompts 收敛决策

### 第四周

- T09 状态边界收敛
- T10 启动性能优化
- T11 / T12 文档、联调、发布验收

---

## 11. 建议先做的最小闭环

如果当前只能先做一轮最小修复，建议先完成以下闭环：

1. 删除仓库明文凭据。
2. 修复所有 Manager 的 `kwargs` / `extra_kwargs` schema 问题。
3. 为所有 Manager 增加 `help` 最小调用测试。
4. 给 `live_trading_manager` 接入确认门禁和审计。
5. 在 `server.py` 中完成 HTTP 认证真实接线，或明确暂不支持 HTTP 公开暴露。

做到这一步后，MCP 服务会从“BFF 专用兼容态”进入“协议层基本合格态”，后续再继续处理 Resources / Prompts、状态持久化和性能优化。
