# 策略工厂→孵化→真实交易 端到端断链诊断报告

> 日期：2026-06-11
> 性质：基于数据库实证 + 真实代码取证的深度审查诊断（每条结论附代码/数据证据）
> 数据库：`data/db/akshare_mcp.sqlite3`
> 结论先行：**当前链路在"信号生成"环节断裂，没有任何策略走完孵化到达真实交易。**

---

## 一、端到端状态总览（数据库实证）

查询 `strategies.status` 与孵化 pipeline：

```
strategies.status:  submitted=2934  rejected=2373  draft=1667  archived=706
                    diagnostic=47   incubating=2    listed=1
incubation pipeline snapshots: warmup=79759  (candidate/graduation_ready/promoted 各 0)
incubation accounts:  warmup active=2841  diagnostic archived=696
paper_trades 总数: 41   (最后成交 2026-06-09，之后无新成交)
strategy_signals 总数: 263  (每天约 1 条)
```

证据解读：
- `listed=1` 是 `strategy_type='demo'` 的测试数据（`id=sqlite-listed`），不是真实晋升策略。
- `incubating=2` 是 6 月初的两个 `multi_factor` 老策略，近期 30+10 轮运行未新增。
- **79759 条 pipeline snapshot 100% 停在 warmup，candidate/graduation/promoted 全为 0** → 无任何策略走完孵化。

---

## 二、断点定位：孵化每轮 signals=0

`logs_run10.log`（本次 10 轮）每轮孵化 Phase 3 日志：

```
Phase 3: signals=0, verified=303
Phase 3: signals=0, verified=303
... (10 轮全部 signals=0)
```

即：每轮验证了 ~303 个策略，却**生成 0 个信号**。无信号 → 无下单 → 无成交 → 无前向证据 → 永远 effective_n 不足 → 全卡 warmup。

---

## 三、根因取证（真实代码）

### 3.1 孵化工厂的 "signal generator" 名不副实——只消费不生产

孵化 runner 每轮对所有策略调用 `signal_generator.generate`：

`packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/runner.py:294-298`
```python
# 信号生成 — 单策略级超时
signal_result = await asyncio.wait_for(
    self._signal_generator.generate(db, strategy),
    timeout=STRATEGY_TIMEOUT_SEC,
)
```

`generate` 内部转调 `sync_signals_to_orders`：

`packages/akshare-mcp/src/akshare_mcp/services/incubation_factory/signal_generator.py:66-68`
```python
sync_result = await incubation_service.sync_signals_to_orders(db, strategy, today)
```

而 `sync_signals_to_orders` 的第一步是 **读取已有信号**，不是生成：

`packages/akshare-mcp/src/akshare_mcp/services/incubation_parts/specs.py:13`
```python
signals = await db.get_signals(strategy['id'], start_date=signal_date, end_date=signal_date, limit=200)
```

**结论**：孵化工厂的"signal generator"只是把 `strategy_signals` 表里**已存在的当日信号**同步成 paper_orders。若当日没有预先写入的信号，它什么都不做（signals=0）。它不执行 DSL 规则引擎、不产生新信号。

### 3.2 真正生成信号的是独立的 Signal Tracker，且 session 从不调用它

真正跑规则引擎并写 `strategy_signals` 的代码在 Signal Tracker：

`packages/akshare-mcp/src/akshare_mcp/services/signal_tracker_parts/context.py:281`
```python
signal_series = np.asarray(instance.generate_signals(closes, volumes), dtype=np.int8)
```

`packages/akshare-mcp/src/akshare_mcp/services/signal_tracker_parts/specs.py:352`
```python
count = await db.save_signals(s["id"], today, signals_batch)
results["signals_generated"] += count
```

它有独立入口 `scripts/factories/run_signal_tracker.py`，但本次质量 session **从不调用它**：

```
$ grep -c "signal_tracker|run_signal_tracker|SignalTracker" scripts/factories/run_strategy_factory_quality_session.py
0
```

**结论**：信号生成（Signal Tracker）与孵化验证（Incubation Factory）是两条独立流程。质量 session 只跑了"策略工厂 + 孵化工厂"，**没跑 Signal Tracker**，所以没有新信号写入；孵化工厂读不到信号，signals=0。每天仅有的 1 条信号来自那 2 个老 incubating 策略的 `paper_trading_bridge` 旁路。

### 3.3 即使跑 Signal Tracker，observe 样本也无可执行 DSL

规则引擎需要编译好的 DSL。抽查一个 observe(submitted) 样本：

```
test strategy: factory_1781168023_7c145570 (rsi, submitted)
has dsl: False | execution_semantic_mode: None | dsl_compiled: None
submission_lane: observe_incubation
```

`generate_signals` 需要可执行策略实例；DSL 执行依赖 `evaluate_dsl_masks`：

`packages/aiask-quant-core/src/aiask_quant_core/strategy_dsl_parts/context.py:542`
```python
entry_mask, exit_mask = evaluate_dsl_masks(frame, normalized)
```

无 DSL 的样本无法产出 entry/exit 掩码 → 无信号。最近 200 个 submitted 中仅 84 个带 compiled_dsl（生成时自带），其余无。

---

## 四、附带发现：recompile remediation 在真实数据上未生效

我此前新增的 `_run_recompile_remediation`（补 compiled_dsl）日志显示每轮 `recompiled=6 updated=47`，但数据库实证矛盾：

```
全库 submitted+incubating 带 runtime_recompile_backfill 标记: 0 / 2936
```

对真实库样本（副本库）实跑 `backfill_historical_trend_strategies`：

```
backfill: scanned=1 recompiled=0 updated=1 promoted_to_formal=0 revision_required=1
item.status = revision_required
item.reason = deterministic_recompile_did_not_produce_compiled_dsl
AFTER DB: exec_mode=compiled_dsl  mark=False  status=submitted
```

两个真实 bug：
1. **重编译对真实库样本失败**（`deterministic_recompile_did_not_produce_compiled_dsl`）——冒烟用的合成数据能编译成功，真实库样本编译失败，说明 `build_trend_strategy_recompile_backfill` 重建 spec 时编译路径在真实数据上不稳定。
2. **`runtime_recompile_backfill` 标记未持久化**（全库 0 个带标记，尽管 `updated` 计数 >0）——save 链路丢失了该字段，导致无法追溯 remediation 是否真正作用过。

代码位置（待修）：
- `packages/akshare-mcp/src/akshare_mcp/services/strategy_recompile_backfill.py:242` `build_trend_strategy_recompile_backfill`
- 同文件 `_merge_params` / `_strategy_save_payload`（标记字段未落库）

---

## 五、因果链总结

```
Signal Tracker 未在运行循环中调度
  → strategy_signals 表无新信号（每天仅 1 条来自老策略旁路）
  → 孵化工厂 sync_signals_to_orders 读不到信号 → signals=0
  → 无 paper_orders / paper_trades（41 笔历史成交，6/9 后归零）
  → 无前向收益证据，primary_effective_n 长期为 0
  → resolve_incubation_pipeline_stage 永远判 warmup（effective_n < 门槛）
  → 79759 snapshot 全 warmup，0 graduation，0 真实交易

叠加：observe 样本普遍无 compiled_dsl，即便 Signal Tracker 运行也产不出信号；
      remediation 本应补 DSL，但在真实数据上重编译失败 + 标记未落库，实际未生效。
```

---

## 六、整改方向（按影响排序）

| 优先级 | 问题 | 性质 | 建议 |
|---|---|---|---|
| **P0** | Signal Tracker 未纳入运行循环 | 编排缺失 | 质量 session / 三工厂循环中加入 signal tracker 轮次，或孵化前置触发；否则永远 0 信号 |
| **P0** | observe 样本无 compiled_dsl | 数据/生成 | 修复 remediation 重编译在真实数据上失败的 bug，并使其真正落库（含标记） |
| **P1** | remediation 标记未持久化 | 落库 bug | 修 `_merge_params`/save 链路，确保 `runtime_recompile_backfill` 等字段写入 strategies.params |
| **P1** | remediation 每轮仅扫 50 个 | 容量 | 2934 个 submitted 需 ~59 轮扫完一遍，需提高 batch_limit 或分批轮转 |
| **P2** | 纸面交易引擎停摆 | 运营 | 依赖 P0 解决后，确认 paper 撮合随信号恢复 |
| 外部 | LLM endpoint 超时率高 | 外部 | zzshu.cc 10 轮 timeout 93 次；rule-first 兜底不阻塞，属 endpoint 稳定性 |

---

## 七、已确认未复发（前序修复仍有效）

本次审查同时确认前序两个历史死锁修复持续有效，不是本次断链的原因：
- `gate_3_recorded=0`（10/10 轮）— record-only 卡死未复发
- gate 正常放行（每轮 submitted 6-8）— G3 全拦未复发

**即：策略"能进 observe"已解决；但"能走完孵化到真实交易"卡在更下游的信号生成编排缺失，这是本次诊断的核心新发现。**

---

## 附录：复现命令

```bash
# 状态分布
sqlite3 data/db/akshare_mcp.sqlite3 "SELECT status,COUNT(*) FROM strategies GROUP BY status"
# pipeline 阶段
sqlite3 data/db/akshare_mcp.sqlite3 "SELECT pipeline_stage,COUNT(*) FROM strategy_incubation_pipeline_snapshots GROUP BY pipeline_stage"
# 信号量与时间
sqlite3 data/db/akshare_mcp.sqlite3 "SELECT COUNT(*),MIN(created_at),MAX(created_at) FROM strategy_signals"
# 孵化每轮信号
grep -o "Phase 3: signals=[0-9]*, verified=[0-9]*" logs_run10.log
# session 是否调用 signal tracker
grep -c "signal_tracker" scripts/factories/run_strategy_factory_quality_session.py
```
