# AIASK 项目修复方案（2026-02-13）

## 1. 目标与范围

### 1.1 修复目标
- 修复“文档/接口契约不一致”导致的调用失败与误用问题。
- 补齐“占位能力”并提升关键链路（合规、执行、风控）的可用性。
- 降低代码维护风险（同名模块并存、重复实现）。
- 强化工程质量门禁（CI、回归、性能与数据质量）。

### 1.2 修复范围
- 目录：`packages/akshare-mcp/src/akshare_mcp/**`、`.github/workflows/**`、`scripts/**`、根目录文档。
- 不在本轮范围：新增全新业务模块、改造为实盘下单系统。

## 2. 当前问题清单（按优先级）

### P0（必须立即修）
1. 参数契约不一致。
- `execution_manager` 文档写 `total_quantity/duration_minutes`，实现实际用 `total_shares/duration`。
- `risk_manager` 文档支持 `codes/weights`，实现仅支持 `portfolio_id`。

2. 关键能力占位。
- `options_manager(action=list)` 返回“期权列表功能待实现”。
- `calculate_fear_greed_index` 固定返回 50。
- `performance_manager` 归因中的 `timing` 仍是“暂未实现”。

3. 合规校验过于宽松。
- `compliance_manager(check_order)` 在极端数量/价格下仍可能 `passed=true`。

### P1（应尽快修）
1. 模块维护风险。
- `tools` 与 `services` 下存在“同名文件 + 同名包目录”并存（如 `market.py` 与 `market/`）。

2. CI 门禁覆盖不足。
- 目前 CI 主要是依赖一致性与 skill 覆盖审计，缺少自动化单测/集成/数据质量回归。

3. 安全与传输规范增强。
- 若未来启用 Streamable HTTP，需要补充 `Origin` 校验、鉴权、127.0.0.1 绑定策略。
- 部分行情兜底链路仍存在明文 HTTP 数据源访问。

### P2（优化项）
1. 执行管理器从“计划输出”升级为“可追踪任务状态”。
2. 风险模型加入更多可解释指标（如因子暴露/情景分层）。
3. 报告与审计产物自动化沉淀（日报/周报/月报模板自动填充）。

## 3. 分阶段修复计划

## 阶段 P0（目标：3~5 个工作日）

### P0-1 统一参数契约与兼容层
- 改动文件：
`packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py`
`packages/akshare-mcp/src/akshare_mcp/tools/managers/risk_manager.py`
`packages/akshare-mcp/src/akshare_mcp/tools/managers/options_manager.py`
- 改动内容：
- 为 `execution_manager` 增加参数别名映射：`total_quantity -> total_shares`、`duration_minutes -> duration`。
- 为 `risk_manager` 增加双模式：
  - 模式A：`portfolio_id`（保留现有逻辑）
  - 模式B：`codes + weights`（直接计算 VaR/压力测试）
- `help` 返回参数说明与实际实现完全一致。
- 验收标准：
- 旧参数和新参数均可成功调用。
- 文档示例参数可直接运行通过。

### P0-2 补齐“占位能力”
- 改动文件：
`packages/akshare-mcp/src/akshare_mcp/tools/managers/options_manager.py`
`packages/akshare-mcp/src/akshare_mcp/services/sentiment.py`
`packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py`
`packages/akshare-mcp/src/akshare_mcp/tools/managers/performance_manager.py`
- 改动内容：
- `options_manager(list)` 接入 `get_option_chain`，至少返回基础合约列表。
- `fear_greed_index` 改为基于真实可用数据计算，保留数据缺失降级逻辑并标注来源。
- `performance_manager` 的 `timing` 子项从固定 0 升级为可解释实现；若暂无法完整实现，必须明确 `status=partial` 与缺失原因。
- 验收标准：
- 不再出现“功能待实现”占位返回。
- 情绪指数结果非固定常量，且可追溯输入源。

### P0-3 强化合规检查
- 改动文件：
`packages/akshare-mcp/src/akshare_mcp/tools/managers/compliance_manager.py`
- 改动内容：
- 接入最小规则集：最大单笔数量、最大委托金额、最小交易单位、交易时段检查、涨跌停限制提示。
- `check_order` 返回结构加入：`violations[]`、`blocked`、`warnings[]`。
- 验收标准：
- 极端参数（超大数量/超高价格）必须触发 `passed=false` 或 `blocked=true`。

## 阶段 P1（目标：1~2 周）

### P1-1 清理同名模块并存风险
- 改动文件：
`packages/akshare-mcp/src/akshare_mcp/tools/*.py`
`packages/akshare-mcp/src/akshare_mcp/tools/*/__init__.py`
`packages/akshare-mcp/src/akshare_mcp/services/*.py`
`packages/akshare-mcp/src/akshare_mcp/services/*/__init__.py`
- 改动内容：
- 确认唯一导入路径，移除或冻结未使用的旧实现。
- 在 `scripts/skill_coverage_audit.py` 中增加“同名文件/目录冲突提示”。
- 验收标准：
- `python -c "import ...; print(__file__)"` 指向路径一致且无歧义。
- 覆盖审计报告可明确标记冲突状态。

### P1-2 扩展 CI 质量门禁
- 改动文件：
`.github/workflows/*.yml`
`packages/akshare-mcp/tests/**`
- 改动内容：
- 新增 workflow：
  - `unit-and-integration.yml`
  - `data-quality-smoke.yml`
  - `tool-contract-check.yml`
- 关键 manager 的入参/出参契约快照测试。
- 验收标准：
- PR 必须通过：依赖一致性 + 覆盖审计 + 核心测试三道门禁。

### P1-3 安全与传输规范增强
- 改动文件：
`packages/akshare-mcp/start_server.py`
`packages/akshare-mcp/src/akshare_mcp/server.py`
`packages/akshare-mcp/MCP_CONFIG_GUIDE.md`
`packages/akshare-mcp/src/akshare_mcp/tools/market/quote.py`
- 改动内容：
- 文档中增加 MCP HTTP 模式安全基线（仅 localhost、Origin 校验、鉴权、禁 token passthrough）。
- 可替代时优先 HTTPS 数据源，无法替代时增加风险提示与超时/校验策略。
- 验收标准：
- 安全文档与配置示例可直接用于部署审查。
- 明文 HTTP 使用点有明确降级说明与治理计划。

## 阶段 P2（目标：2~4 周）

### P2-1 执行与风控深度化
- 将 `execution_manager` 从“计划生成”升级为“任务生命周期”：
创建 -> 排队 -> 执行中 -> 完成/失败 -> 汇总。
- 补充成本模型参数披露：手续费、滑点模型、冲击成本。

### P2-2 报告自动化与留痕
- 结合日报/周报/月报模板输出固定字段：
数据窗口、来源、降级链、风险指标、异常处理、下一步触发条件。
- 输出 JSON + Markdown 双份留痕，便于审计与二次处理。

## 4. 回归与验收命令

```bash
# 1) 强制预检
python scripts/skill_coverage_audit.py --check-thresholds
python scripts/skill_coverage_audit.py --output-json skill_tool_coverage_runtime.json --output-gap skill_tool_gap_list.txt

# 2) 核心测试
cd packages/akshare-mcp
pytest tests/test_integration.py -q
pytest tests/test_performance_benchmark.py -q
pytest ../tests/data-quality -q

# 3) 关键链路抽样
# options_manager(list) 不得返回“待实现”
# risk_manager 支持 portfolio_id 与 codes/weights 双模式
# compliance_manager(check_order) 对极端订单应触发阻断
```

## 5. 风险与回滚

1. 风险：参数兼容改造影响已有客户端调用。
- 控制：先加兼容层，不立即删除旧参数，观察 1~2 个版本周期。

2. 风险：替换占位逻辑后性能下降。
- 控制：为新逻辑加缓存与超时，必要时保留“快速模式”。

3. 风险：清理同名模块误删引用。
- 控制：先加导入路径快照测试，再执行删除动作。

## 6. 交付物清单

1. 修复代码（P0 必选项）。
2. 更新后的工具说明文档与示例调用。
3. 新增 CI workflow 与测试报告。
4. 修复后基线报告：
`skill_tool_coverage_runtime.json`
`skill_tool_gap_list.txt`
`tests/data-quality/DATA_QUALITY_REPORT.md`

## 7. 建议执行顺序

1. 先做 P0-1（契约统一）与 P0-3（合规强化），避免继续输出错误建议。
2. 再做 P0-2（占位能力补齐），提升可用性。
3. 最后推进 P1/P2，降低长期维护与审计风险。

