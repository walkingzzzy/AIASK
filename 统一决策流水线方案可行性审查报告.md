# AIASK 统一决策流水线方案可行性审查报告

> 审查日期：2026-03-19
> 审查对象：`统一决策流水线优化方案.md`
> 审查方式：本地代码深度审查 + 联网检索官方/权威资料验证

---

## 1. 结论摘要

结论：**方案总体可行，但不能按“纯增量新增 1 个统一工具 + 1 个新页面”来理解。**

更准确的判断是：

- 架构方向正确，且与当前仓库能力高度匹配。
- MCP 协议、Python FastMCP、TypeScript SDK 都支持方案所需的“结构化工具结果 + BFF 编排”。
- 当前仓库已经具备量化上下文、事件素材、闸门、统一卡片等零件，具备实施基础。
- 但方案里有 4 个前置约束没有写透，如果不补上，落地时会出现“理论成立、实施卡顿”的情况。

建议将该方案评级为：

- **架构可行性：高**
- **增量改造可行性：中高**
- **一次性交付可行性：中**

---

## 2. 核心审查结论

## 2.1 成立的部分

以下判断经本地代码与官方资料双重验证，结论成立：

1. **“统一决策对象 + 统一主链”是当前项目最合理的下一步演进方向。**
2. **MCP 层完全可以承载 `stock_context` / `quant_context` / `event_context` / `final_decision_payload` 这种结构化对象。**
3. **BFF 已具备消费 MCP 结构化结果的能力，不需要推翻现有网关。**
4. **现有量化工具足以支撑 `quant_context` 首版，不必从零开发预测模型。**
5. **现有事件/文本能力足以支撑 `event_context` 首版，但精度只能先做到“结构化增强版”，还达不到真正强语义事件引擎。**
6. **规则闸门设计可行，而且当前仓库已有合规闸门、执行软硬闸门、聊天风险约束等可复用逻辑。**

## 2.2 需要修正的部分

以下部分不影响方案方向，但需要在实施文档中补充，否则会显得过于乐观：

1. 必须单列“**MCP 结构化输出契约层**”。
2. 必须单列“**用户上下文透传链路**”。
3. 必须把 `get_unified_decision` 改成“**摘要入口 + 明细展开**”双层策略，不能直接假设一个大对象首屏全拉。
4. 必须增加“**旧入口兼容与灰度验证**”章节，尤其是 `should_i_buy`、`smart_stock_diagnosis`、`decision_manager` 的迁移关系。

---

## 3. 主要发现

### [高] 方案把 `user_gate` 设为核心规则层，但当前助手接口没有用户身份和画像透传链路

参考：

- [统一决策流水线优化方案.md](/Users/mac/Desktop/股票/统一决策流水线优化方案.md#L212)
- [assistant.controller.ts](/Users/mac/Desktop/股票/apps/bff/src/assistant/assistant.controller.ts#L31)
- [user-context.service.ts](/Users/mac/Desktop/股票/apps/bff/src/chat/user-context.service.ts#L18)

原因：

- 方案里把 `KYC`、风险等级、情绪偏差纳入 `user_gate`，这在方向上是正确的。
- 但当前 `POST /assistant/diagnosis`、`/assistant/should-buy`、`/assistant/should-sell` 只接收 `code`、`buyPrice`、`holdingDays` 之类参数，没有统一传递 `userId`。
- 真正具备用户上下文注入能力的是聊天链路，而不是助手接口链路。

影响：

- 如果直接上 `get_unified_decision(code, user_id, horizon, explain)`，BFF 现有助手控制器并不能稳定提供 `user_id`。
- 结果会导致方案中的 `user_gate` 只能退化为默认值，无法兑现“用户级风控”。

建议：

- 在 Phase 0 增加“用户上下文透传协议”。
- 统一定义助手接口如何取 `userId`、`riskLevel`、`kycLevel`、`profileSummary`。
- 若短期做不到用户级上下文，首版应明确写成 `user_gate = optional / default-profile fallback`。

### [高] 方案假设统一 payload 可以直接稳定走 MCP，但没有明确“结构化输出契约”

参考：

- [统一决策流水线优化方案.md](/Users/mac/Desktop/股票/统一决策流水线优化方案.md#L418)
- [server.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/server.py#L106)
- [decision.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/decision.py#L54)
- [mcp-gateway.service.ts](/Users/mac/Desktop/股票/apps/bff/src/mcp-gateway/mcp-gateway.service.ts#L423)

原因：

- 当前 BFF 网关已经能优先读取 `structuredContent`，这是好消息。
- 但当前 Python MCP 工具大多是“直接返回 dict，且无明确返回类型注解”的风格。
- 这意味着首版虽然能跑，但不会天然具备强约束的 `outputSchema` 和稳定的结构化结果契约。

影响：

- `get_unified_decision` 如果仍沿用“随手返回 dict”模式，后续字段膨胀和兼容问题会很快出现。
- BFF 会再次沦为“兜底适配层”，与方案“先统一对象协议”的目标相冲突。

建议：

- 在 Phase 0 增加一条硬要求：
  - 统一决策工具必须定义明确的输出 schema。
- 优先方案：
  - 用 Pydantic 模型约束输出字段。
  - 在 Python MCP 工具层尽量走结构化结果而不是文本 JSON 回传。

### [高] `get_unified_decision` 作为单一大工具是可行的，但首屏全量返回会放大 stdio 同步调用和连接池瓶颈

参考：

- [统一决策流水线优化方案.md](/Users/mac/Desktop/股票/统一决策流水线优化方案.md#L351)
- [统一决策流水线优化方案.md](/Users/mac/Desktop/股票/统一决策流水线优化方案.md#L579)
- [mcp-gateway.service.ts](/Users/mac/Desktop/股票/apps/bff/src/mcp-gateway/mcp-gateway.service.ts#L34)
- [mcp-gateway.service.ts](/Users/mac/Desktop/股票/apps/bff/src/mcp-gateway/mcp-gateway.service.ts#L84)

原因：

- 当前 BFF MCP 连接池默认大小是 `3`。
- 每次 `callTool` 仍是同步请求-响应模式。
- 如果统一工具内部串行拉行情、财务、文本、因子、相似形态、OOS 验证、产业链、用户画像，再一次性返回全 payload，单次耗时会明显上升。

影响：

- 首屏体验容易从“统一决策页”变成“重型报告页”。
- 高峰期会占满 MCP 连接池，影响其他页面和聊天调用。

建议：

- 将方案调整为“双层工具”：
  - `get_unified_decision_summary`
  - `get_unified_decision_details`
- 首屏只返回：
  - action
  - confidence
  - summary
  - gate flags
  - top reasons
  - minimal provenance
- 深层证据按需展开拉取。

### [中] `event_context` 的方向成立，但当前文本信号能力仍偏关键词级，短期内不适合作为强 veto 唯一依据

参考：

- [统一决策流水线优化方案.md](/Users/mac/Desktop/股票/统一决策流水线优化方案.md#L169)
- [llm_alpha.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/services/llm_alpha.py#L26)
- [sentiment.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py#L268)

原因：

- 当前文本信号管线核心仍是关键词词典打分。
- `get_stock_text_signals` 已经能聚合新闻、公告、研报，这很好，但“事件抽取”还没有被标准化成 `event_type / horizon / confidence / direction / entities`。

影响：

- 方案中“事件 veto 覆盖量化正向”在方向上没问题，但如果首版直接把关键词命中当 veto，会有误伤风险。

建议：

- 首版先把 `event_context` 分成两个等级：
  - `event_warning`
  - `hard_event_veto`
- `hard_event_veto` 只允许来源于结构化强事件，如：
  - 重大处罚
  - ST/退市风险
  - 财报爆雷
  - 停牌/重大风险公告
- 其他文本侧信号先只做 `warning / downgrade`，不要直接 veto。

### [中] 方案没有把现有 `comprehensive_manager` / `smart_stock_diagnosis` / `get_investment_analysis` 的重用边界写清楚，存在重复建模风险

参考：

- [统一决策流水线优化方案.md](/Users/mac/Desktop/股票/统一决策流水线优化方案.md#L326)
- [comprehensive_manager.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/managers/comprehensive_manager.py#L38)
- [diagnosis.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/semantic/diagnosis.py#L86)
- [decision.py](/Users/mac/Desktop/股票/packages/akshare-mcp/src/akshare_mcp/tools/decision.py#L179)

原因：

- 当前仓库里已经存在多套“综合分析上下文”雏形。
- 如果新增 `build_stock_context` 又不明确它与 `get_investment_analysis`、`comprehensive_manager` 的关系，后续会出现三套相似对象并存。

影响：

- 字段重复、口径分裂、维护成本上升。

建议：

- 在实施文档中增加一条原则：
  - `build_stock_context` 优先复用 `get_investment_analysis` 能提供的基础上下文。
- 避免另起一套完全独立的股票画像。

### [中] 方案缺少测试与灰度验收章节

参考：

- [统一决策流水线优化方案.md](/Users/mac/Desktop/股票/统一决策流水线优化方案.md#L495)

原因：

- 当前方案已经有 Phase 4 替代旧逻辑，但没有定义：
  - contract tests
  - snapshot tests
  - latency baseline
  - old/new 决策 diff 对比

影响：

- 上线后很难判断新主链到底是“更强”还是“只是更复杂”。

建议：

- 增加以下验收项：
  - `UnifiedDecisionPayload` schema contract test
  - `assistant.controller` 兼容测试
  - 新旧入口 diff 报表
  - MCP 调用耗时基线
  - 页面首屏渲染耗时基线

---

## 4. 联网验证结果

## 4.1 MCP 协议与 SDK 层面：方案可行

经官方资料确认：

- MCP Tools 支持返回结构化结果。
- 工具结果可携带 `structuredContent`，并且可由 `outputSchema` 约束。
- 工具同样支持返回文本内容和资源链接，因此“摘要 + 明细”双层设计符合协议能力边界。

验证结论：

- 方案中的 `stock_context`、`quant_context`、`event_context`、`final_decision_payload` 作为 MCP 工具结果完全可行。
- BFF 作为 MCP client 编排层也完全符合官方使用方式。

官方参考：

- [MCP Tools 规范](https://modelcontextprotocol.io/specification/2025-06-18/server/tools)
- [MCP Python SDK（FastMCP / Structured Output）](https://github.com/modelcontextprotocol/python-sdk)
- [MCP TypeScript SDK](https://github.com/modelcontextprotocol/typescript-sdk)

## 4.2 当前项目网关适配：可行

本地代码显示：

- `apps/bff/src/mcp-gateway/mcp-gateway.service.ts` 已优先读取 `structuredContent`。
- 若无 `structuredContent`，也会尝试从文本块中解析 JSON。

验证结论：

- 统一决策工具并不需要重写 BFF 网关。
- 但建议后续优先走 `structuredContent`，减少“文本 JSON 再解析”的脆弱链路。

## 4.3 Python SDK 与版本风险：可行，但应保持版本意识

本地运行环境实际版本：

- `mcp = 1.25.0`
- `@modelcontextprotocol/sdk = 1.26.0`

联网核验结果：

- 官方 TypeScript SDK 当前最新 release 已到 `1.27.1`。
- 官方 Python SDK 曾有 FastMCP 相关高危修复，受影响范围为 `<1.9.4`。
- 当前本地 Python `mcp 1.25.0` 已高于受影响版本，风险可接受。

验证结论：

- 方案不受当前本地 MCP 版本阻断。
- 但实施前建议将 SDK 版本和输出契约一并纳入一次兼容性检查。

官方参考：

- [TypeScript SDK Releases](https://github.com/modelcontextprotocol/typescript-sdk/releases)
- [Python SDK Security Advisory](https://github.com/advisories/GHSA-v2fc-5mxj-v274)

## 4.4 “摘要优先、明细展开”的设计比“单个超大工具全量返回”更符合官方工具调用模式

官方工具规范强调：

- 工具是请求-响应式调用。
- 结构化内容适合明确对象返回。
- 大对象或附加材料可以通过资源引用或分层调用组织。

验证结论：

- 本方案方向可行，但实施形态应从“单一巨型工具”调整为“摘要主入口 + 明细子入口 + 可选资源引用”。

---

## 5. 修订后的可行性判断

## 5.1 可以立即执行的部分

- 定义统一对象协议
- 定义 `UnifiedDecisionPayload` schema
- 建立 `stock_context` builder
- 建立 `quant_context` builder
- 在 BFF 新增统一决策接口
- 在前端助手页增加统一决策卡

## 5.2 需要降级推进的部分

- `event_context` 的强 veto
- 用户级 `rule_gate` 全量生效
- 一次性替代所有旧入口

## 5.3 不建议首版就做的部分

- 单工具全量返回所有证据明细
- 用固定权重做全局 AI/Quant 融合
- 用关键词情绪直接做硬 veto

---

## 6. 对原方案的修订建议

建议对原方案追加以下 6 项：

1. **新增章节：MCP 结构化输出契约**
   - 明确 `structuredContent`、schema、Pydantic 输出模型。

2. **新增章节：用户上下文透传**
   - 明确助手接口如何获取 `userId`、`riskLevel`、`kycLevel`。

3. **调整工具设计**
   - 将 `get_unified_decision` 拆成 `summary + details`。

4. **调整事件层表述**
   - 首版只允许结构化强事件触发硬 veto。

5. **新增章节：重用边界**
   - 明确 `get_investment_analysis`、`smart_stock_diagnosis`、`comprehensive_manager` 如何复用。

6. **新增章节：测试与灰度**
   - 增加 contract、diff、latency、回归四类验证。

---

## 7. 最终判断

如果按“原文不变”执行，这份方案会遇到一些实施层阻力，但**不会推翻方案本身**。

如果按本报告建议修订后执行，方案具备较高落地成功率，且非常适合作为 AIASK 下一阶段的主线架构升级方向。

一句话结论：

**方向正确，协议要先补，用户链路要补，首版要做轻，事件 veto 要谨慎。**
