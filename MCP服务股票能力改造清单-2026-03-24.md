# MCP 服务股票能力改造清单

日期：2026-03-24  
范围：`packages/akshare-mcp` 为主，涉及 `packages/strategy-factory`、`apps/bff`、数据库与向量检索的配套改造  
关联报告：`/Users/mac/Desktop/股票/项目审查报告-MCP核心-2026-03-24.md`

---

## 1. 改造目标

本清单不再重复“系统已经实现了什么”，只回答三个问题：

1. 当前 MCP 在股票研究与决策上，哪些地方已经够用，哪些地方精度不够。
2. 哪些改造应优先做，才能把系统从“广覆盖研究平台”推进到“高精度决策引擎”。
3. 每项改造落到什么模块、补什么能力、验收标准是什么。

当前判断：

- 系统优势在 `功能广度`、`解释性`、`治理骨架`。
- 系统短板在 `预测精度`、`概率校准`、`不确定性表达`、`实时交易约束`、`高质量情绪 NLP`、`因子发现深度`。
- 当前最需要补的不是新工具数量，而是 `精度底座`。

---

## 2. 总体判断

### 2.1 当前真实状态

- `should_i_buy` 已能输出概率，但概率来自手工 `logit(score, confidence, volatility)` 压缩，不是经验校准后的模型概率。
- 统一决策已具备 `stock / quant / event` 三路融合与规则闸门，但融合权重仍是固定配置，闸门仍以规则阻断和分数调整为主。
- 因子验证链路已经比较完整，包含 `compile -> cross-section -> OOS -> robustness -> similarity -> turnover -> cost/capacity -> rating`。
- 因子“挖掘”仍偏轻量，`llm_alpha.py` 目前主体仍是本地规则版，`automl_discovery` 本质是相关性筛选加线性/排序集成。
- 情绪层已有“价量动量 + 新闻关键词 + 资金流”框架，但新闻理解仍是词典法，缺乏财经语义模型和情绪信号的 OOS 证明。
- 执行层已有 `TWAP / VWAP` 规划、软硬闸门、虚拟盘，但实盘部分仍是“仅监控，不实际交易”。

### 2.2 改造原则

- 优先补 `精度底座`，再补 `高级模型`，最后补 `生产级交易闭环`。
- 每条新信号都必须同时补 `验证、漂移监控、回退策略`。
- 所有“概率”“评分”“情绪值”都必须给出来源、校准方法、置信区间和验收指标。
- 不以增加工具数量作为目标，以 `可验证提升` 为目标。

### 2.3 本次联网研究基线

本清单已补充多条 `官方文档 / 官方仓库 / 论文正文页` 的外部基线，不以搜索结果摘要为依据。选源标准如下：

- 优先使用 `官方文档`、`官方 GitHub 仓库`、`arXiv 论文页`、`Hugging Face 官方模型卡`。
- 优先选择能够直接回答“生产级能力边界”的材料，而不是营销页。
- 重点覆盖 `预测分析 / 因子研究 / 情绪 NLP / 交易执行` 四条链路。

#### 预测分析与不确定性

- [scikit-learn Probability Calibration](https://scikit-learn.org/stable/modules/calibration.html)
- [MAPIE Documentation](https://mapie.readthedocs.io/)
- [MAPIE Quick Start](https://mapie.readthedocs.io/en/stable/quick_start.html)
- [Qlib PIT Database](https://qlib.readthedocs.io/en/latest/advanced/PIT.html)
- [Qlib Online & Offline Mode](https://qlib.readthedocs.io/en/latest/advanced/server.html)
- [Qlib Task Management / RollingGen](https://qlib.readthedocs.io/en/v0.8.1/advanced/task_management.html)

#### 因子研究与验证

- [Alphalens Overview](https://alphalens.ml4trading.io/notebooks/overview.html)
- [Alphalens Documentation Home](https://alphalens.ml4trading.io/)
- [mlfinlab Cross Validation](https://random-docs.readthedocs.io/en/latest/implementations/cross_validation.html)
- [Hudson & Thames mlfinlab Overview](https://hudsonthames.org/mlfinlab/)

#### 情绪价值与金融 NLP

- [FinBERT Paper](https://arxiv.org/abs/1908.10063)
- [ProsusAI FinBERT Model Card](https://huggingface.co/ProsusAI/finbert)
- [FinGPT Paper](https://arxiv.org/abs/2307.10485)
- [FinGPT Official Repository](https://github.com/AI4Finance-Foundation/FinGPT)

#### 交易执行与生产化交易

- [Alpaca Paper Trading](https://docs.alpaca.markets/docs/paper-trading)
- [Alpaca MCP Server](https://docs.alpaca.markets/docs/alpaca-mcp-server)
- [QuantConnect Trading and Orders Key Concepts](https://www.quantconnect.com/docs/v2/writing-algorithms/trading-and-orders/key-concepts)
- [QuantConnect Slippage Models](https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/slippage/supported-models)
- [QuantConnect Live Trading](https://www.quantconnect.com/docs/v2/writing-algorithms/live-trading)
- [FinRL Official Repository](https://github.com/AI4Finance-Foundation/FinRL)
- [FinRL-Trading / FinRL-X](https://github.com/AI4Finance-Foundation/FinRL-Trading)

### 2.4 外部基线对比结论

#### 预测分析

- `scikit-learn` 的基线不是“输出概率”就结束，而是要求做 `calibration curves`、`reliability diagrams`、`Brier/log_loss`，并通过 `CalibratedClassifierCV` 在独立数据上做概率校准。
- `MAPIE` 给出的基线已经不是单点预测，而是 `prediction intervals / prediction sets / time series uncertainty / risk control`。
- `Qlib` 给出的基线不仅是模型训练，还包括 `PIT database`、`Online mode`、`RollingGen`、`Online Serving`。
- 对比之下，当前 MCP 的最大缺口是：`有评分，有概率字段，但没有完整的校准与区间化体系，也没有平台级 PIT 与滚动训练底座`。

#### 因子研究

- `Alphalens` 的核心基线是 `Returns Analysis + Information Analysis + Turnover Analysis`，并且明确区分“alpha factor 分析”和“交易算法分析”。
- `Alphalens` 还明确要求价格对齐必须避免 `lookahead bias`。
- `mlfinlab` 的基线已经覆盖 `Purged/Embargo CV`、`CPCV`、`Triple-Barrier`、`Meta-Labeling`、`feature importance`、`position sizing`。
- `Qlib` 进一步补齐 `rolling task generation`、`signal record`、`portfolio analysis`、`online serving`。
- 对比之下，当前 MCP 的因子层 `验证链路较完整`，但 `候选生成深度、去泄漏标准化、线上滚动治理` 仍不足。

#### 情绪价值

- `FinBERT` 给出的外部基线是 `金融领域继续预训练 + 金融情绪分类`，不是关键词词典。
- `ProsusAI/finbert` 模型卡明确给出 `positive / negative / neutral` 的 softmax 输出，并说明其建立在金融语料和 `Financial PhraseBank` 微调之上。
- `FinGPT` 进一步把基线推进到 `34 个互联网金融数据源`、`RAG`、`中国市场标注数据`、`面向金融任务的指令微调`。
- 对比之下，当前 MCP 情绪层的主要缺口是：`语义理解、事件抽取、市场反馈微调、中文金融语境能力、情绪信号 OOS 验证`。

#### 交易执行

- `Alpaca Paper Trading` 已经明确区分 `paper vs live`，并把 `market impact / information leakage / latency slippage / queue position / regulatory fees` 这些仿真边界写清楚。
- `Alpaca MCP Server` 已经提供 `43` 个交易相关能力，覆盖 `market data / order actions / portfolio / alerts / risk checks`。
- `QuantConnect` 的基线已经包含 `order tickets / order events / async fills / brokerage messages / custom fill-fee-slippage-margin models / market impact slippage model`。
- `FinRL-X` 给出的新基线是 `professional backtesting + paper/live trading + risk-gated execution + multi-account integration`。
- 对比之下，当前 MCP 执行层的主要缺口是：`真实订单状态机、成交回报、可插拔交易现实模型、券商接入、交易后反馈闭环`。

---

## 3. 优先级总表

## P0：必须先做

- [ ] 建立 `时点一致性/PIT` 数据基线，避免预测、因子、情绪使用未来信息。
- [ ] 重构 `buy_probability`，从手工映射升级为“模型输出 + 概率校准 + 可靠性评估”。
- [ ] 给收益预测和交易建议增加 `置信区间/预测区间` 表达。
- [ ] 把停牌、ST、涨跌停、交易时段、可成交量等约束从静态规则升级为 `实时复核`。
- [ ] 给情绪分数建立 `未来收益命中率 / 分层回测 / OOS 验证`。
- [ ] 给统一决策补 `离线评估基线`，明确 buy/hold/sell 的历史表现，而不是只输出解释。

## P1：中期改造

- [ ] 引入 `滚动训练 / champion-challenger / 模型注册` 机制。
- [ ] 升级因子发现，从相关性筛选扩展到 `meta-labeling / triple-barrier / CPCV`。
- [ ] 升级情绪 NLP，从关键词词典升级到 `中文财经语义模型 + 事件抽取`。
- [ ] 升级执行层，从规划器提升到 `更真实的滑点/冲击/参与率/TCA` 模型。
- [ ] 建立跨模块 `数据质量、信号质量、概率质量、交易质量` 统一监控面板。

## P2：高阶能力

- [ ] 接入真实券商或交易网关，形成 `订单状态机 + 成交回报 + 风控回路`。
- [ ] 做生产级 `在线学习/再训练调度`，支持市场状态切换与模型退化检测。
- [ ] 构建多模态事件理解能力，融合 `公告、研报、新闻、资金、盘口、行业链`。
- [ ] 建立情绪、事件、因子的统一 alpha 研究平台，而不是分散的单点工具。

---

## 4. 分模块改造清单

## 4.1 数据与时点一致性底座

### 当前问题

- 回测、因子验证、情绪分数、统一决策均依赖多源数据，但尚未形成平台级 `PIT` 约束。
- 财务、公告、研报、新闻、事件和行情在“可见时间”与“事件发生时间”之间的边界不够严格。
- 这会直接污染预测、因子 OOS、情绪收益验证结果。

### 目标

- 所有研究、预测、情绪、决策、回测过程都以 `as_of_time` 为统一切面。
- 明确每一类数据的 `event_time / ingest_time / available_time / effective_time`。

### 改造项

- [ ] 为 K 线、财务、公告、研报、新闻、事件统一补 `available_time` 字段与查询约束。
- [ ] 在 `decision_context_builder`、`decision_quant_builder`、`factor_validation_pipeline`、`sentiment` 相关调用链中统一传递 `as_of`。
- [ ] 对 `get_financials`、`get_stock_notices`、`get_research_reports`、`get_stock_news` 增加时点切片测试。
- [ ] 在回测与验证中默认禁止读取 `available_time > as_of_time` 的记录。
- [ ] 新增 `pit_audit` 测试集，对核心工具做未来函数检查。

### 验收标准

- [ ] 同一 `as_of` 下，多次运行决策、因子验证、情绪计算结果稳定一致。
- [ ] 随机抽检历史日期时，财报、公告、研报不会提前泄露。
- [ ] 至少补 1 组未来函数回归测试覆盖核心研究链路。

### 主要代码锚点

- `packages/akshare-mcp/src/akshare_mcp/services/decision_context_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_quant_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_validation_pipeline.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py`

---

## 4.2 预测分析能力

### 当前问题

- `should_i_buy` 的概率输出本质上仍是手工映射，不是经过样本验证和概率校准的预测器。
- `decision_quant_builder` 的概率融合包含显式启发式权重，当前更接近解释性评分器。
- 系统缺少 `Brier Score`、`ECE`、`reliability curve`、`分层命中率` 这样的概率质量指标。
- 系统尚未统一输出 `point estimate + interval + confidence quality`。

### 目标

- 将“买入概率”从解释性概率升级为可监控、可校准、可分层评估的预测概率。
- 所有预测结果必须给出 `点预测 + 区间 + 校准质量`。

### 联网基线补充

- [scikit-learn Probability Calibration](https://scikit-learn.org/stable/modules/calibration.html) 明确把 `calibration curves`、`Brier/log_loss`、`CalibratedClassifierCV` 作为概率质量基线。
- [MAPIE](https://mapie.readthedocs.io/) 明确把 `prediction intervals / prediction sets / time series` 纳入标准工具箱。
- [Qlib PIT](https://qlib.readthedocs.io/en/latest/advanced/PIT.html) 和 [Qlib RollingGen](https://qlib.readthedocs.io/en/v0.8.1/advanced/task_management.html) 表明成熟研究平台会把 `时点一致性` 和 `滚动任务` 作为底层能力，而不是后补的测试。

### 改造项

- [ ] 把 `should_i_buy` 的概率层改成真正的分类/排序模型输出。
- [ ] 引入 `Platt scaling / isotonic calibration`，输出校准后概率。
- [ ] 增加 `reliability diagram / calibration curve / Brier decomposition` 输出。
- [ ] 建立 `buy / hold / sell` 的历史样本标签定义，并标准化 horizon。
- [ ] 为统一决策增加 `prediction_quality` 字段，至少包含 `brier`, `ece`, `calibration_bucket`, `sample_size`。
- [ ] 为收益或上涨概率增加 `prediction_interval`、`coverage`、`interval_width` 或 `confidence band`。
- [ ] 对不同投资风格单独校准，而不是仅用风格偏置项平移。
- [ ] 增加滚动窗口评估，区分牛市、熊市、震荡市的模型稳定性。
- [ ] 引入 `conformalize` 过程，使区间预测和风险控制有统一实现。

### 验收标准

- [ ] 新模型相对现有启发式概率在验证集上 `Brier Score` 明显下降。
- [ ] `ECE` 控制在约定阈值内。
- [ ] 预测区间具备明确的 `coverage` 监控，不允许只返回区间不评估区间。
- [ ] 任一对外暴露的概率字段都能追溯到训练版本、样本窗口、校准版本。
- [ ] 文档明确区分“解释性评分”和“统计概率”。

### 主要代码锚点

- `packages/akshare-mcp/src/akshare_mcp/tools/decision.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/decision_helpers.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_quant_builder.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_fusion.py`

---

## 4.3 交易决策与执行能力

### 当前问题

- 统一决策当前主要是固定权重融合和规则闸门，缺少数据驱动的动态权重与交易后反馈闭环。
- `execution_manager` 目前是规划器，`soft_gate` 只告警不阻断，适合 pre-trade 规划，不是 OMS/EMS。
- `compliance_manager` 对停牌/ST/涨跌停的判断仍有静态规则痕迹。
- 实盘管理器尚未触达真实委托与成交回报。

### 目标

- 将“建议”和“执行”打通，形成从信号到订单再到成交回报的闭环。
- 将风险控制从静态规则扩展到实时市场约束与成交可行性。

### 联网基线补充

- [Alpaca Paper Trading](https://docs.alpaca.markets/docs/paper-trading) 已明确列出 paper 环境不覆盖的现实因素：`market impact`、`information leakage`、`latency slippage`、`queue position`、`regulatory fees` 等。
- [QuantConnect Trading and Orders](https://www.quantconnect.com/docs/v2/writing-algorithms/trading-and-orders/key-concepts) 已把 `order ticket`、`order event`、`async execution`、`brokerage message` 作为标准交易对象。
- [QuantConnect Slippage Models](https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/slippage/supported-models) 已提供 `constant`、`volume share`、`market impact` 以及 `custom slippage model` 扩展点。
- [Alpaca MCP Server](https://docs.alpaca.markets/docs/alpaca-mcp-server) 已把 `market data + order actions + portfolio + risk checks` 打成统一 MCP 接口。
- [FinRL-X](https://github.com/AI4Finance-Foundation/FinRL-Trading) 已把 `professional backtesting + Alpaca paper/live + risk-gated execution` 作为生产化基线。

### 改造项

- [ ] 统一决策增加 `position sizing`、`max loss`、`expected slippage`、`execution feasibility`。
- [ ] 将 `decision_rule_gate` 输出扩展为 `risk budget` 和 `execution block reason`，而不是只做分数调整。
- [ ] 为 `execution_manager` 增加更真实的成交模拟：盘口冲击、参与率、时间分布、滑点、撤单。
- [ ] 把 `soft_gate` 升级为部分可配置的 `hard execution gate`，至少对 `流动性不足 / 风险超限 / 无法成交` 进行阻断。
- [ ] 对接实时行情源，补齐停牌/ST/涨跌停/交易时段/盘口量/分钟均量的实时复核。
- [ ] 给 `paper_trading_manager` 增加成交回报质量统计和 TCA。
- [ ] 引入 `order_ticket / order_event / fill_event / brokerage_event` 对象层，补全订单状态机。
- [ ] 为模拟执行增加 `volume_limit`、`price_impact`、`queue_position`、`partial_fill` 参数化。
- [ ] 明确 `live_trading_manager` 的演进路径：监控 -> 仿真下单 -> 真正交易。
- [ ] 若接入实盘，先实现 `只读持仓 + 纸面委托镜像 + 风险检查`，再做真实下单。

### 验收标准

- [ ] 执行建议能给出可交易性判断，而不是只返回 warning。
- [ ] 模拟盘能够回放订单、成交、滑点和成本。
- [ ] 实时约束与盘中行情一致，不能再依赖静态停牌/ST 判断。
- [ ] 统一决策的建议在交易层有可追踪的后验表现。

### 主要代码锚点

- `packages/akshare-mcp/src/akshare_mcp/services/decision_rule_gate.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/execution_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/compliance_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/paper_trading_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/live_trading_manager.py`

---

## 4.4 因子挖掘与研究平台

### 当前问题

- 候选因子验证已经比较完整，但候选生成深度仍不足。
- `llm_alpha.py` 当前主体仍是本地规则版，缺少高质量候选搜索器。
- `automl_discovery` 目前本质是 feature selection + 线性/排序集成，尚不足以承担高阶 alpha discovery。
- 缺少行业/风格/市值中性化、冗余因子群组分析、数据泄漏防线和更严格的过拟合控制。

### 目标

- 把现有“验证型平台”升级为“发现 + 验证 + 治理”一体化因子平台。

### 联网基线补充

- [Alphalens Overview](https://alphalens.ml4trading.io/notebooks/overview.html) 将 `Returns / Information / Turnover` 明确拆开，并强调 `alpha factor` 与 `trading algorithm` 是不同层次的问题。
- [Alphalens Overview](https://alphalens.ml4trading.io/notebooks/overview.html) 还明确要求价格对齐时避免 `lookahead bias`。
- [mlfinlab Cross Validation](https://random-docs.readthedocs.io/en/latest/implementations/cross_validation.html) 将 `purging`、`embargo`、`CPCV` 作为金融机器学习交叉验证基线。
- [Hudson & Thames mlfinlab](https://hudsonthames.org/mlfinlab/) 把 `Triple-Barrier`、`Meta-Labeling`、`feature importance`、`position sizing`、`sequentially bootstrapped ensembles` 都纳入标准组件。
- [Qlib](https://qlib.readthedocs.io/en/latest/advanced/server.html) 则进一步覆盖 `Online Serving`、`RollingGen`、`SignalRecord`、`PortAnaRecord` 这种研究到部署的统一链路。

### 改造项

- [ ] 引入 `meta-labeling`、`triple-barrier labeling`、`purged k-fold / CPCV` 作为默认研究工具链。
- [ ] 为因子验证补 `neutralization`、`orthogonalization`、`style exposure`、`sector exposure` 分析。
- [ ] 为每个候选因子增加 `IC / RankIC / quantile return / turnover / rank autocorrelation` 标准 tearsheet。
- [ ] 把 `lookahead bias audit` 设为因子入池前置条件。
- [ ] `automl_discovery` 增加非线性模型候选，如 GBDT、Tabular NN、符号回归、时序模型。
- [ ] 增加“候选因子家族”概念，对相似表达式进行聚类，减少伪创新。
- [ ] 将 `feature_store` 从快照存储升级到真正的实验追踪，包括样本、标签、版本、指标、审批状态。
- [ ] 将 `position sizing / cost model / capacity` 结果纳入因子评分，而不是仅做附属告警。
- [ ] 在策略工厂中引入 `champion / challenger` 因子与策略联动评测。
- [ ] 对文本、事件、情绪类因子建立独立研究通道，不再只当作附属分数。

### 验收标准

- [ ] 每个入池因子都带有 `cross-section`, `oos`, `robustness`, `turnover`, `cost`, `exposure` 报告。
- [ ] 因子入池前必须经过去泄漏验证与中性化检验。
- [ ] 自动挖掘结果相比当前基线，在 OOS 稳定性和 IC/RankIC 上有明确提升。

### 主要代码锚点

- `packages/akshare-mcp/src/akshare_mcp/tools/managers/quant_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/services/factor_validation_pipeline.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/quant_mgr_automl.py`
- `packages/akshare-mcp/src/akshare_mcp/services/llm_alpha.py`
- `packages/strategy-factory`

---

## 4.5 情绪价值与事件理解

### 当前问题

- 当前新闻情绪主要依赖关键词词典，语义层不够。
- `sentiment_manager` 简版能力更接近“涨跌天数 + 量比”的市场热度，而不是财经情绪建模。
- 情绪值目前更多用于解释与辅助，不是经过证明的 alpha 因子。
- 公告、研报、新闻、论坛、资金行为之间尚未形成统一事件画像。

### 目标

- 把情绪模块从“辅助评分器”升级为“可验证的事件与情绪因子系统”。

### 联网基线补充

- [FinBERT Paper](https://arxiv.org/abs/1908.10063) 明确指出通用模型在金融语境下不够有效，需做金融语料继续预训练。
- [ProsusAI FinBERT Model Card](https://huggingface.co/ProsusAI/finbert) 提供了 `金融文本 -> 三分类 softmax` 的直接基线。
- [FinGPT Paper](https://arxiv.org/abs/2307.10485) 进一步引入 `34 个实时金融数据源`、`RLSP`、低成本定制和金融任务应用。
- [FinGPT Repository](https://github.com/AI4Finance-Foundation/FinGPT) 已把 `FinGPT-RAG`、`sentiment dataset`、`Chinese Market` 微调线路、金融 NER/关系抽取等纳入公开框架。

### 改造项

- [ ] 引入中文财经 NLP 模型，对公告、新闻、研报做情绪分类、事件分类、风险标签提取。
- [ ] 情绪分析拆分为 `entity sentiment`, `event sentiment`, `surprise`, `credibility`, `recency`。
- [ ] 对不同来源内容做权重隔离，至少区分公告、主流媒体、研报、自媒体。
- [ ] 将关键词词典法保留为 `fallback baseline`，但主路径切换为领域模型或金融指令微调模型。
- [ ] 建立 `headline sentiment / report sentiment / notice risk / event extraction / NER` 多任务框架。
- [ ] 为情绪分数建立历史收益验证和衰减分析，确认哪些情绪类型有可交易价值。
- [ ] 为中文市场建立独立数据集与评测集，不混用英文金融情绪基线。
- [ ] 评估 `RAG + 财经知识源` 对公告、研报、新闻情绪判定的提升，而不是只做裸文本分类。
- [ ] 将情绪、资金、盘口、龙虎榜、北向数据统一到 `event_context` 中，避免重复打分。
- [ ] 对用户侧“贪婪/恐惧/从众”画像，与市场情绪分离，避免行为信号污染市场信号。

### 验收标准

- [ ] 任一情绪分数都能回答“来源是什么、语义模型是什么、历史命中率是多少、衰减周期多长”。
- [ ] 情绪因子在分层回测中优于关键词基线。
- [ ] 事件与情绪字段在统一决策中不再只是文字解释，而是有统计贡献。

### 主要代码锚点

- `packages/akshare-mcp/src/akshare_mcp/services/sentiment.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/sentiment.py`
- `packages/akshare-mcp/src/akshare_mcp/tools/managers/sentiment_manager.py`
- `packages/akshare-mcp/src/akshare_mcp/services/decision_event_builder.py`

---

## 4.6 评测、监控与回归体系

### 当前问题

- 目前系统已有不少单点测试，但缺少贯穿“预测概率、决策表现、情绪信号、执行质量”的统一质量门槛。
- 部分输出是“能算出来”，但缺少“是否可靠”的持续监控。

### 目标

- 所有高价值信号都必须具备 `离线评估 + 回归测试 + 漂移监控 + 降级逻辑`。

### 改造项

- [ ] 为 `buy_probability` 建 `概率质量测试集`。
- [ ] 为 `sentiment_score` 建 `情绪 alpha 测试集`。
- [ ] 为 `factor candidate` 建 `去泄漏 / 过拟合 / 中性化 / 容量` 测试集。
- [ ] 为 `execution_manager` 建 `交易可行性 / 成本 / 滑点` 基线测试。
- [ ] 引入统一 metrics registry，输出到日报/周报或监控面板。
- [ ] 对每个核心工具定义“降级后是否仍可用、结果应如何标记”的 contract。

### 验收标准

- [ ] 核心输出字段具备质量元信息，不再只返回裸值。
- [ ] 新模型或新因子上线前，自动触发回归与离线评估。
- [ ] 漂移或精度退化时，系统能自动切换到安全基线。

---

## 5. 建议排期

## 第一阶段：2 周

- [ ] 完成 PIT 约束设计与关键查询链路改造
- [ ] 补 `buy_probability` 校准评估框架
- [ ] 补情绪分数 OOS 验证框架
- [ ] 补实时交易约束复核接口
- [ ] 补统一决策离线评估基线

## 第二阶段：4 到 6 周

- [ ] 上线滚动训练与模型注册
- [ ] 升级因子挖掘工具链
- [ ] 升级情绪 NLP
- [ ] 升级模拟执行与 TCA
- [ ] 建立统一质量监控

## 第三阶段：6 到 12 周

- [ ] 接入真实交易网关
- [ ] 建立在线策略/模型治理
- [ ] 建立跨情绪、事件、因子的一体化研究平台

---

## 6. 明确不优先做的事项

以下事项当前不应优先于精度底座改造：

- [ ] 新增更多外层 Manager 或包装器
- [ ] 继续扩张工具数量但不补评测体系
- [ ] 先做前端展示升级而不补概率质量
- [ ] 先做复杂 AI 交互，但不补 PIT、校准、OOS、交易约束

---

## 7. 结论

当前 MCP 的主要问题不是“功能不够多”，而是“精度核心还不够硬”。  
后续改造应明确按以下顺序推进：

1. 先补 `时点一致性、概率校准、区间化表达、实时交易约束、情绪 OOS`。
2. 再补 `滚动训练、因子发现深度、情绪语义模型、执行质量模型`。
3. 最后再做 `真实交易接入、在线学习、统一 alpha 平台`。

如果顺序反过来，系统会继续堆功能，但不会显著提高真实可用性。
