# 策略工厂实际问题调整方案

> 文档日期：2026-04-07  
> 放置位置：项目根目录  
> 适用范围：`packages/strategy-factory`、`packages/akshare-mcp`、策略工厂生成/验证/去重/提交流程  
> 关联文档：`策略工厂完整调整方案-2026-04-05.md`  
> 本文目标：基于 2026-04-07 的真实代码、真实运行结果与外部研究基线，收敛出下一轮应执行的调整方案

---

## 一、结论先行

当前策略工厂达不到目标，不是因为“工厂主链不存在”，而是因为下面四个关键问题仍未收口：

1. 策略对象定义仍然过粗，`candidate_contract_hash` / `candidate_identity_signature` 还不能稳定区分真实 alpha 变体。
2. `refresh_existing` 对父策略 lineage 的短路过强，导致 RL/bandit 派生候选几乎总会被吸回旧策略。
3. 生成阶段与任务合同还未完全对齐，大量错误候选在 pre-gate 才暴露，浪费生成与验证预算。
4. P3 反馈控制能力已经存在，但 target pool / generator mode 级别的硬停启权还没有真正进入上游生成计划。

这意味着下一轮工作的重点不该再是“补更多模块”，而应该是：

1. 重新定义 tested object。
2. 收紧 refresh / revision 语义。
3. 把 task contract 约束前移到生成器。
4. 把反馈控制真正接进调度与生成。

---

## 二、已确认的真实运行症状

基于最近真实运行与排查结果，当前系统至少存在以下稳定现象：

1. 外部 LLM fallback 很多，但已经确认不再是统计口径误报。
2. 即使修正了 external provider 兼容统计，最终仍然经常 `created_total = 0`。
3. 最近多轮运行中，幸存候选会稳定命中同一个历史策略，然后走 `refresh_existing`。
4. 大多数候选在 pre-gate 被淘汰，主要原因是：
   - `target_universe_alignment_too_low`
   - `outside_allowed_strategy_types`
   - `duplicate_candidate_signature`
5. 少数进入 Gate-0 的候选仍会因为交易合同字段缺失失败。

这说明：

1. “新增策略为零”不只是 LLM provider 问题。
2. 当前主链最大的结构性问题，是候选对象识别、任务合同收口与去重刷新语义。

---

## 三、根因拆解

### 3.1 tested object 定义错误

当前 `candidate_contract_hash` / `candidate_identity_signature` 更多描述的是：

1. target pool
2. trade/risk/portfolio contract
3. validation profile
4. lineage/task signature

但没有真正编码：

1. DSL 逻辑本体
2. 关键参数模板
3. factor 权重结构
4. alpha 假设差异

这会导致两个不同逻辑的候选，只要共享相近 target/trade contract，就可能被视为同一个对象。

直接后果：

1. dedup 可能把不同策略逻辑误判为同一候选。
2. `tested_object_hash` 失真，multiple-testing 口径偏乐观。
3. refresh / revision 决策建立在过粗的 identity 上。

### 3.2 parent lineage 短路导致 refresh 过强

当前 dedup 中，只要命中 `parent_strategy_ids`，就会优先进入 `refresh_existing` 倾向。

这在 RL/bandit 派生策略里尤其危险，因为：

1. RL 派生候选天然会继承 parent lineage。
2. 如果 identity 又没有纳入真实 alpha 逻辑差异，那么任何小变体都会被视为旧策略刷新。
3. 最终表现为“工厂一直在跑，但几乎总是不新建策略”。

### 3.3 生成器对 task contract 的服从仍不够早

现在系统已经有：

1. `target_alignment_contract`
2. `allowed_strategy_types`
3. `validation_profile`
4. `resolved_candidate_envelope`

但这些约束主要还是在 normalize、pre-gate、gate_0、gate_1 才发挥决定性作用。

结果是：

1. 生成器产出了一批从一开始就不合规的候选。
2. pre-gate 才承担真正的“清洗器”职责。
3. 生成预算和验证预算被大量消耗在低质量候选上。

### 3.4 P3 反馈只完成了一半

当前系统已经具备：

1. `cooldown`
2. `suppress`
3. `freeze`
4. family / target pool / generator mode feedback 聚合

但这些能力主要影响：

1. budget multiplier
2. priority adjustment
3. incubation budgeting

尚未完全控制：

1. 下一轮生成是否还会继续排同一 target pool
2. 下一轮生成是否还会继续启用同一 generator mode
3. 上游任务规划是否真正绕开高风险 family / target pool / mode

---

## 四、调整原则

下一轮调整遵循以下原则：

1. 不重写已有主链，只收紧语义与边界。
2. 先修 tested object 与 refresh/revision，再谈更多生成能力。
3. 先把“错误候选不要被生成出来”做好，再优化 gate。
4. 所有新增规则都要进入 run summary，避免只能靠翻原始 payload 排查。

---

## 五、分阶段调整方案

### P0：重新定义 tested object，并收紧 refresh 语义

#### P0-1 扩展 candidate contract，使其能识别真实 alpha 变体

目标：

1. 让 `candidate_contract_hash` 不再只是 target/trade contract 哈希。
2. 让 `candidate_identity_signature` 可以区分“同合同下的不同策略逻辑”。

执行项：

1. 在 contract 中新增 alpha identity 维度，例如：
   - `logic_signature`
   - `dsl_signature`
   - `factor_signature`
   - `entry_exit_signature`
2. 对 `dsl_rule`，至少将标准化后的 DSL 结构摘要纳入 tested object。
3. 对 factor/multi-factor 类策略，将关键 factor 权重或排名逻辑纳入 tested object。
4. 区分两个概念：
   - `execution_contract_hash`：描述 target/trade/risk/validation 壳
   - `tested_object_hash`：描述真实被验证的 alpha 对象
5. submission gate、multiple-testing registry、backtest audit 统一切到新的 `tested_object_hash`。

验收：

1. 不同 DSL 逻辑但同 target/trade contract 的候选，不再共享同一个 tested object。
2. multiple-testing 统计按真实 alpha 变体计数。

#### P0-2 重写 refresh / revision 判定优先级

目标：

1. 取消“parent 命中即优先 refresh”的过强短路。
2. 把 refresh 约束在“identity 真正不变”的情形下。

执行项：

1. 调整 dedup 决策顺序：
   - 先比 `tested_object_hash`
   - 再比 `candidate_identity_signature`
   - 再看 lineage / parent
2. 命中 parent strategy 时：
   - 若 alpha identity 改变，优先 `spawn_revision_from_existing`
   - 仅当 tested object 与 identity 都稳定不变时，才允许 `refresh_metrics_only`
3. 把以下情况直接视为 revision 而不是 refresh：
   - DSL entry/exit 结构改变
   - factor 权重结构改变
   - 核心参数模板改变
4. 把 refresh 判定写入 run detail，显式暴露：
   - `refresh_decision_basis`
   - `identity_changed`
   - `tested_object_changed`

验收：

1. RL/bandit 派生候选不再因为 parent 命中而稳定被吞成 refresh。
2. `refresh_metrics_only` 只发生在 tested object 不变时。

#### P0-3 历史策略 contract backfill

目标：

1. 让存量策略也具备新的 identity / tested object 字段。
2. 结束“新候选按新语义、老策略按旧语义”的双轨判定。

执行项：

1. 为历史策略回填：
   - `candidate_contract_snapshot`
   - `candidate_contract_hash`
   - `candidate_identity_signature`
   - `tested_object_hash`
   - `target_pool_id`
2. 对无法可靠回填逻辑签名的旧策略打上：
   - `legacy_identity_partial`
   - `tested_object_backfill_incomplete`
3. dedup 对 `legacy_identity_partial` 采取保守策略：
   - 降低 refresh 倾向
   - 提高 revision 倾向

验收：

1. 绝大部分存量策略具备新 contract。
2. dedup 不再频繁退回旧式 `task_signature + target_overlap + 相似度` 组合判断。

---

### P1：把任务合同约束前移到生成器

#### P1-1 统一生成前的 task contract 预校验

目标：

1. 不让明显不合规的候选进入主链。
2. 把 pre-gate 的一部分职责前移到生成阶段。

执行项：

1. 在 generator 输出 spec 前统一校验：
   - `allowed_strategy_types`
   - `validation_profile`
   - target alignment contract
   - contract completeness
2. 对 external/local/pipeline 三条生成路径统一采用同一份轻量 precompile contract validator。
3. 生成器在不满足条件时直接 reject，并记录 reject reason，而不是继续生成后交给 pre-gate。

验收：

1. `outside_allowed_strategy_types` 在 pre-gate 中的占比明显下降。
2. `target_universe_alignment_too_low` 的大量问题前移到生成阶段被挡住。

#### P1-2 收紧 snapshot 定向任务定义

目标：

1. 所有“显式带 target_symbols 的 snapshot 任务”都视为定向任务。
2. 结束单标的 snapshot 仍可能走宽松 target 语义的问题。

执行项：

1. 重新定义 `targeted_snapshot`：
   - 不再要求 `len(target_symbols) > 1`
   - 只要 snapshot 任务显式带 target，就视为定向
2. 对所有定向 snapshot：
   - 默认 `strict_intersection`
   - 默认 `forbid`
3. prompt 规则、normalize 规则、target resolution 规则保持一致。

验收：

1. 单标的定向 snapshot 不再使用 `prefer_intersection_with_research_task` 作为默认提示语义。
2. target drift 规则在 prompt、normalize、gate 中一致。

#### P1-3 research context 严格分流

目标：

1. 定向任务只吃 target context。
2. broad market context 只能服务非定向任务。

执行项：

1. 在 prompt compact 阶段明确区分：
   - `task_target_context`
   - `market_background_context`
2. 对定向任务：
   - prompt 只允许引用 target context
   - synthetic frame 只允许从 target context 构造
3. 若 target context 无法建立，直接阻断该任务。

验收：

1. 定向任务的 prompt 不再携带 broad candidate universe 作为主要上下文。
2. research context summary 能清晰区分 target-only 与 broad fallback。

---

### P2：让 run detail 可以直接定位失败原因

#### P2-1 观测字段升级

目标：

1. 不再依赖人工翻 payload 才知道失败原因。

执行项：

1. 在 run summary / stage summary 固定暴露以下字段：
   - `tested_object_hash_changed`
   - `refresh_decision_basis`
   - `target_alignment_violation`
   - `contract_reject_reasons`
   - `generator_precompile_reject_reason`
   - `feedback_control_mode`
   - `feedback_target_pool_control_mode`
   - `feedback_generator_mode_control_mode`
2. 在 dedup / submit 结果中增加：
   - `existing_identity_available`
   - `existing_tested_object_available`
   - `revision_trigger_reason`

验收：

1. 单看 run detail 就能解释“为什么没新建策略”。
2. 单看 run detail 就能解释“为什么走 refresh 而不是 revision”。

#### P2-2 external provider 健康度一等化

目标：

1. 继续区分真实请求与兼容性跳过。
2. 让 provider 健康状况更容易作为调度输入。

执行项：

1. 维持现有口径拆分：
   - `network_request_count`
   - `compatibility_skip_count`
   - `cooldown_skip_count`
2. 增加 provider 级 summary：
   - `compatibility_failure_ratio`
   - `effective_response_ratio`
   - `empty_200_response_count`
3. 调度层读取 provider 健康度，控制 external mode 是否继续参与。

验收：

1. 不会再把“200 但无内容”误读成成功。
2. 工厂调度能识别 provider 处于假成功状态。

---

### P3：让 target pool / generator mode 级反馈真正进入调度

#### P3-1 把 feedback control 从 budgeting 扩展到 generation planning

目标：

1. target pool / generator mode 的负反馈不只是影响预算。
2. 它们要直接影响下一轮是否生成。

执行项：

1. 在上游 generation planning 调用 `resolve_feedback_metrics(...)` 时，显式传入：
   - `family`
   - `target_pool_id`
   - `generator_mode`
2. 如果 control mode 为：
   - `cooldown`：降低优先级并限制生成次数
   - `suppress`：本轮默认不生成
   - `freeze`：直接冻结该 scope
3. 让 scheduler 在 task planning 层输出：
   - 被压制的 family
   - 被压制的 target pool
   - 被压制的 generator mode

验收：

1. 高风险 target pool 不再只是在预算层面被弱化。
2. 高故障 generator mode 可以被直接停用。

#### P3-2 对 external/provider 和 RL mode 增加硬停启规则

目标：

1. 让“兼容失败反复出现的 provider”和“反复只产出 refresh 的生成模式”都能被自动降级。

执行项：

1. 若 external provider 在滚动窗口内：
   - compatibility skip 过高
   - effective response ratio 过低
   - 空内容 200 过高
   则自动 `cooldown` 或 `suppress`
2. 若某 generator mode 连续多轮：
   - `created_total = 0`
   - `refresh_metrics_only_count` 高
   - `tested_object_hash_changed` 低
   则触发 mode 级别 `cooldown`
3. 对 RL/bandit 增加专项观测：
   - `revision_creation_ratio`
   - `refresh_absorption_ratio`

验收：

1. provider 假成功不会长期消耗主链预算。
2. RL/bandit 不会长期处于“只刷新不新建”的假繁荣状态。

---

## 六、实施顺序

建议按以下顺序执行：

1. 先做 P0-1、P0-2、P0-3。  
   原因：必须先定义清楚 tested object，后续 gate、dedup、multiple-testing 才有意义。

2. 再做 P1-1、P1-2、P1-3。  
   原因：要尽量把错误候选挡在生成器出口，而不是把 pre-gate 继续当主清洗器。

3. 然后做 P2。  
   原因：需要让每轮运行都能直接证明“修复是否奏效”。

4. 最后做 P3。  
   原因：调度硬控制必须建立在对象语义与观测口径稳定之后。

---

## 七、验收指标

### P0 验收

1. 同 target/trade contract 下的不同 DSL/alpha 逻辑，会得到不同 `tested_object_hash`。
2. `refresh_metrics_only` 只发生在 tested object 与 identity 都未变化时。
3. RL/bandit 候选的 `spawn_revision_from_existing` 占比显著提升，不再被 lineage 大量吸收成 refresh。

### P1 验收

1. pre-gate 中 `outside_allowed_strategy_types` 明显下降。
2. pre-gate 中 `target_universe_alignment_too_low` 明显下降。
3. 单标的定向 snapshot 不再走默认宽松 target 语义。

### P2 验收

1. run detail 可直接解释“为什么没新增策略”。
2. run detail 可直接解释“为什么 refresh 而不是 revision”。
3. run detail 可直接看到 tested object 是否变化。

### P3 验收

1. 高风险 target pool 可以被明确 `cooldown / suppress / freeze`。
2. 高故障 generator mode 可以被明确停用。
3. external provider 健康差时，工厂会自动降级而不是长期空耗预算。

---

## 八、明确不再采用的错误方向

以下方向不建议继续投入：

1. 继续把问题归结为“外部 LLM fallback 太多，所以没新增策略”。
2. 继续围绕 Gate-2、submit lane 做局部打补丁，而不先修 tested object。
3. 继续默认认为 parent lineage 命中就应 refresh。
4. 继续把 family feedback 当成已等价覆盖 target pool / generator mode feedback。

---

## 九、最终判断

当前策略工厂真正缺的，不是更多模块，而是更硬的对象边界。

只有完成下面四件事，工厂才会从“会跑但总 refresh”的状态，升级为“能持续产出新策略”的真实工厂：

1. 让 `tested_object_hash` 真正代表被验证的 alpha 对象。
2. 让 `refresh_existing` 只服务 identity 不变的对象。
3. 让生成器在出口就服从任务合同。
4. 让 target pool / generator mode 级负反馈真正参与下一轮调度。

在这四件事完成前，继续扩展生成能力、继续增加候选数量、继续提高 LLM 参与度，都只会放大当前的语义错误。
